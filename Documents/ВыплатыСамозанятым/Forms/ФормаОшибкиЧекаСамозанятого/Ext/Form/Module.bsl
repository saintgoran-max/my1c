#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Самозанятый = Параметры.Самозанятый;
	ЭтотОбъект.Элементы.НадписьСамозанятый.Заголовок = Строка(Самозанятый);
	ЭтотОбъект.Элементы.НадписьОшибкиБанка.Заголовок = Параметры.ОшибкаБанка;
	Если Параметры.ПричинаОтказа = Перечисления.ПричиныОтказаВЧекахВыплатСамозанятым.НеКорректныеРеквизитыСчета Тогда
		Элементы.НадписьНеверныйСчет.Видимость = Истина;
		Элементы.НадписьЧтоСделатьНеверныйСчет.Видимость = Истина;
	ИначеЕсли Параметры.ПричинаОтказа = Перечисления.ПричиныОтказаВЧекахВыплатСамозанятым.ПолучательНеЯвляетсяСамозанятым Тогда
		Элементы.НадписьСтатусНеСамозанятого.Видимость = Истина;
		Элементы.НадписьЧтоСделатьСтатусНеСамозанятого.Видимость = Истина;
	Иначе
		Элементы.ГруппаПояснение.Видимость = Ложь;
		Элементы.НадписьЧтоСделатьПрочиеПричины.Видимость = Истина;
		Элементы.ДекорацияКотик.Высота = 5;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура НадписьСамозанятыйНажатие(Элемент)
	
	Ключ = Новый Структура;
	Ключ.Вставить("Ключ", Самозанятый);
	ОткрытьФорму("Справочник.Контрагенты.ФормаОбъекта", Ключ, ЭтотОбъект,,,,,РежимОткрытияОкнаФормы.Независимый);
	
КонецПроцедуры

#КонецОбласти


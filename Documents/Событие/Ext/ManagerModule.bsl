#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.УправлениеДоступом

// Параметры:
//   Ограничение - см. УправлениеДоступомПереопределяемый.ПриЗаполненииОграниченияДоступа.Ограничение.
//
Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт

	Ограничение.Текст =
	"РазрешитьЧтениеИзменение
	|ГДЕ
	|	(ТипСобытия <> Значение(Перечисление.ТипыСобытий.ЭлектронноеПисьмо)
	|		Или ЗначениеРазрешено(УчетнаяЗапись))
	|	И ДляВсехСтрок(ЗначениеРазрешено(Участники.Контакт, ПустаяСсылка КАК Истина))";
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.УправлениеДоступом

// Определяет список команд заполнения.
//
// Параметры:
//   КомандыЗаполнения - ТаблицаЗначений - Таблица с командами заполнения. Для изменения.
//       См. описание 1 параметра процедуры ЗаполнениеОбъектовПереопределяемый.ПередДобавлениемКомандЗаполнения().
//   Параметры - Структура - Вспомогательные параметры. Для чтения.
//       См. описание 2 параметра процедуры ЗаполнениеОбъектовПереопределяемый.ПередДобавлениемКомандЗаполнения().
//
Процедура ДобавитьКомандыЗаполнения(КомандыЗаполнения, Параметры) Экспорт
	
КонецПроцедуры

// Вызывается из Обработка.ДокументыПоКритериюОтбора для изменения запроса по контрагенту
Процедура ИзменитьЗапросОтбораПоКонтрагенту(ТекстЗапроса) Экспорт
	
	СекцияНаИзменение = 
	"( 1 В
	|	(ВЫБРАТЬ ПЕРВЫЕ 1
	|			1
	|	ИЗ
	|		Документ.Событие.УдалитьУчастники КАК ТаблицаСтрок
	|	ГДЕ
	|		ТаблицаСтрок.Ссылка =  ТаблицаШапки.Ссылка
	|		И (  ТаблицаСтрок.Контакт = &Параметр  )
	|	) ИЛИ  1 В
	|	(ВЫБРАТЬ ПЕРВЫЕ 1
	|			1
	|	ИЗ
	|		Документ.Событие.Участники КАК ТаблицаСтрок
	|	ГДЕ
	|		ТаблицаСтрок.Ссылка =  ТаблицаШапки.Ссылка
	|		И (  ТаблицаСтрок.Контакт = &Параметр  )
	|	)";
	
	НоваяСекция = 
	"(ТаблицаШапки.ТипСобытия <> ЗНАЧЕНИЕ(Перечисление.ТипыСобытий.ЭлектронноеПисьмо) И
	|( 1 В
	|	(ВЫБРАТЬ ПЕРВЫЕ 1
	|			1
	|	ИЗ
	|		Документ.Событие.УдалитьУчастники КАК ТаблицаСтрок
	|	ГДЕ
	|		ТаблицаСтрок.Ссылка =  ТаблицаШапки.Ссылка
	|		И (  ТаблицаСтрок.Контакт = &Параметр  )
	|	) ИЛИ  1 В
	|	(ВЫБРАТЬ ПЕРВЫЕ 1
	|			1
	|	ИЗ
	|		Документ.Событие.Участники КАК ТаблицаСтрок
	|	ГДЕ
	|		ТаблицаСтрок.Ссылка =  ТаблицаШапки.Ссылка
	|		И (  ТаблицаСтрок.Контакт = &Параметр  )
	|	))
	|ИЛИ (ТаблицаШапки.ТипСобытия = ЗНАЧЕНИЕ(Перечисление.ТипыСобытий.ЭлектронноеПисьмо) И
	|( 1 В
	|	(ВЫБРАТЬ ПЕРВЫЕ 1
	|			1
	|	ИЗ
	|		Документ.Событие.УдалитьУчастники КАК ТаблицаСтрок
	|	ГДЕ
	|		ТаблицаСтрок.Ссылка =  ТаблицаШапки.Ссылка
	|		И (  ТаблицаСтрок.Контакт = &Параметр  )
	|	) ИЛИ  1 В
	|	(ВЫБРАТЬ ПЕРВЫЕ 1
	|			1
	|	ИЗ
	|		Документ.Событие.Участники КАК ТаблицаСтрок
	|	ГДЕ
	|		ТаблицаСтрок.Ссылка =  ТаблицаШапки.Ссылка
	|		И (  ТаблицаСтрок.Контакт В (ВЫБРАТЬ
	|	СвязиАдресатКонтактCRM.Адресат КАК СсылкаКакСвязаться
	|ИЗ
	|	РегистрСведений.СвязиАдресатКонтактCRM КАК СвязиАдресатКонтактCRM
	|ГДЕ
	|	СвязиАдресатКонтактCRM.КонтактCRM В(&Параметр))  )
	|	)))";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, СекцияНаИзменение, НоваяСекция);
	
КонецПроцедуры

#КонецОбласти

// Функция - Получить как связаться
//
// Параметры:
//  Контакт				 - СправочникСсылка.Контрагенты, СправочникСсылка.КонтактныеЛица	 - ссылка на контакт
// Возвращаемое значение:
//  Строка - значение для связи с контактом.
Функция ПолучитьКакСвязаться(Контакт, ТипКИ = Неопределено) Экспорт
	
	Контакты = Новый Массив;
	Контакты.Добавить(Контакт);
	
	Если ТипКИ = Неопределено Тогда
		ТипыКИ = Новый Массив;
		ТипыКИ.Добавить(Перечисления.ТипыКонтактнойИнформации.Телефон);
		ТипыКИ.Добавить(Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты);
	ИначеЕсли ТипЗнч(ТипКИ) = Тип("Массив") Тогда
		ТипыКИ = ТипКИ;
	Иначе
		ТипыКИ = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(ТипКИ);
	КонецЕсли;
	
	ТаблицаКИ = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъектов(Контакты, ТипыКИ, , ТекущаяДатаСеанса());
	ТаблицаКИ.Сортировать("Тип УБЫВ");
	
	Представления = ТаблицаКИ.ВыгрузитьКолонку("Представление");
	
	Возврат СтрСоединить(Представления, "; ");
	
КонецФункции

Функция ПолучитьКакСвязатьсяКонтактов(Контакт, ТипКИ = Неопределено) Экспорт
	
	Контакты = Новый Массив;
	Контакты.Добавить(Контакт);
	
	ТипыКИ = Новый Массив;
	Если ТипКИ = Неопределено Тогда
		ТипыКИ.Добавить(Перечисления.ТипыКонтактнойИнформации.Телефон);
		ТипыКИ.Добавить(Перечисления.ТипыКонтактнойИнформации.АдресЭлектроннойПочты);
	Иначе
		ТипыКИ.Добавить(ТипКИ);
	КонецЕсли;
	
	ТаблицаКИ = УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъектов(Контакты, ТипыКИ, , ТекущаяДатаСеанса());
	ТаблицаКИ.Сортировать("Тип УБЫВ");
	
	КонтактКакСвязаться = Новый Соответствие;
	Для каждого ДанныеКИ Из ТаблицаКИ Цикл
		СтрокаКакСвязаться = КонтактКакСвязаться.Получить(ДанныеКИ.ИдентификаторСтрокиТабличнойЧасти);
		Если СтрокаКакСвязаться = Неопределено Тогда
			СтрокаКакСвязаться = "";
		Иначе
			СтрокаКакСвязаться = СтрокаКакСвязаться + "; ";
		КонецЕсли;
		
		СтрокаКакСвязаться = СтрокаКакСвязаться + ДанныеКИ.Представление;
		КонтактКакСвязаться.Вставить(ДанныеКИ.ИдентификаторСтрокиТабличнойЧасти, СтрокаКакСвязаться);
	КонецЦикла;
	
	Возврат КонтактКакСвязаться;
	
КонецФункции

Процедура ПроверитьВозможностьВводаПоЛиду(Объект, ЗначенияЗаполнения, Отказ) Экспорт
	
	Лид = Неопределено;
	
	Если Объект.Участники.Количество() <> 0 И ТипЗнч(Объект.Участники[0].Контакт) = Тип("СправочникСсылка.Лиды") Тогда
		Лид = Объект.Участники[0].Контакт;
	КонецЕсли;
	
	Если Лид = Неопределено И ТипЗнч(ЗначенияЗаполнения) = Тип("Структура") Тогда
		Если ЗначенияЗаполнения.Свойство("Лид") И ТипЗнч(ЗначенияЗаполнения.Лид) = Тип("СправочникСсылка.Лиды") Тогда
			Лид = ЗначенияЗаполнения.Лид;
		ИначеЕсли ЗначенияЗаполнения.Свойство("Контакт") И ТипЗнч(ЗначенияЗаполнения.Контакт) = Тип("СправочникСсылка.Лиды") Тогда
			Лид = ЗначенияЗаполнения.Контакт;
		ИначеЕсли ЗначенияЗаполнения.Свойство("КонтактРодитель") И ТипЗнч(ЗначенияЗаполнения.КонтактРодитель) = Тип("СправочникСсылка.Лиды") Тогда
			Лид = ЗначенияЗаполнения.КонтактРодитель;
		КонецЕсли;
	КонецЕсли;
	
	Если Лид = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(Лид) Тогда
		Возврат;
	КонецЕсли;
	
	Отказ = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Лид, "СостояниеЛида") = ОбщегоНазначения.ПредопределенныйЭлемент("Справочник.СостоянияЛидов.Завершен");
	Если Отказ Тогда
		ОбщегоНазначения.СообщитьПользователю(НСтр("ru='Нельзя создать событие по лиду, с которым завершена работа.'"));
	КонецЕсли;
	
КонецПроцедуры

// Изменяет состояние документов
//
// Параметры:
//  Состояние -	СправочникСсылка.СостоянияСобытий 
//  МассивОбъектов - Массив из  ДокументСсылка.Событие
//  ИзмененныеДокументы	 -  Массив - ДокументСсылка.Событие
//
Процедура УстановитьСостояние(Состояние, МассивОбъектов, ИзмененныеДокументы) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Документы.Ссылка КАК Ссылка,
	|	Документы.Состояние КАК Состояние
	|ИЗ
	|	Документ.Событие КАК Документы
	|ГДЕ
	|	Документы.Ссылка В(&Документы)";
	
	Запрос.УстановитьПараметр("Документы", МассивОбъектов);

	Выборка = Запрос.Выполнить().Выбрать();
	
	Пока Выборка.Следующий() Цикл  
		
		Если Выборка.Состояние = Состояние Тогда
			ИзмененныеДокументы.Добавить(Выборка.Ссылка);
			Продолжить;
		КонецЕсли;                        
		
		ДокументОбъект = Выборка.Ссылка.ПолучитьОбъект();
		ДокументОбъект.Состояние = Состояние;
		
		Попытка
			ДокументОбъект.Заблокировать();
			ДокументОбъект.Записать(РежимЗаписиДокумента.Запись);
			ДокументОбъект.Разблокировать();
		Исключение
			ТекстОшибки = СтрШаблон(нСтр("ru='Ошибка изменения состояния %1. %2'"), 
				Строка(Выборка.Ссылка), КраткоеПредставлениеОшибки(ИнформацияОбОшибке()));
		    ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
			Продолжить;
		КонецПопытки;
		
		ИзмененныеДокументы.Добавить(Выборка.Ссылка);
		
	КонецЦикла;
	
КонецПроцедуры

#Область ОбновлениеИнформационнойБазы


// Перенос участников события в служебный справочник и новую таблицу
//  Версия 3.0.2
//  Удалить при удалении обработчика
//
// Параметры:
//  СобытиеОбъект	 - 	ДокументСсылка.Событие - 
//
Процедура ПеренестиУчастниковСобытия(СобытиеОбъект) Экспорт
	
	НачатьТранзакцию();
	
	Попытка
		
		СоответствиеНовыхАдресатов = Новый Соответствие;
		СобытиеОбъект.Заблокировать();
		МассивАдресов = Новый Массив;
				
		Для Каждого Участник Из СобытиеОбъект.УдалитьУчастники Цикл
			
			Если ТипЗнч(Участник.Контакт) <> Тип("Строка") Тогда
				УправлениеДоступом.ПроверитьЧтениеРазрешено(Участник.Контакт);
			КонецЕсли;
			
			Если СобытиеОбъект.ТипСобытия <> Перечисления.ТипыСобытий.ЭлектронноеПисьмо Тогда
				Прервать;
			КонецЕсли;
			
			Адреса = ОбщегоНазначенияКлиентСервер.АдресаЭлектроннойПочтыИзСтроки(Участник.КакСвязаться);
			Для Каждого Адрес Из Адреса Цикл
				Если НЕ ЗначениеЗаполнено(Адрес.ОписаниеОшибки) Тогда
					МассивАдресов.Добавить(Адрес.Адрес);
				Иначе
					МассивАдресов.Добавить(Адрес.Псевдоним);
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
		
		АдресатыСобытий = Справочники.АдресатыПисем.АдресатыПисемПоКакСвязаться(МассивАдресов);
		
		Для Каждого Участник Из СобытиеОбъект.УдалитьУчастники Цикл

			Если СобытиеОбъект.ТипСобытия <> Перечисления.ТипыСобытий.ЭлектронноеПисьмо Тогда
				НовыйУчастник = СобытиеОбъект.Участники.Добавить();
				ЗаполнитьЗначенияСвойств(НовыйУчастник, Участник);
				Продолжить;
			КонецЕсли;
			
			ПеренестиДанныеСобытияЭлектронноеПисьмо(СобытиеОбъект, Участник, АдресатыСобытий, СоответствиеНовыхАдресатов);
			
		КонецЦикла;
		
		СобытиеОбъект.КонтактыПеренесены = Истина;
		
		ОбновлениеИнформационнойБазы.ЗаписатьДанные(СобытиеОбъект);
		СобытиеОбъект.Разблокировать();
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		ОбщегоНазначения.СообщитьПользователю(КраткоеПредставлениеОшибки(ИнформацияОбОшибке()), СобытиеОбъект.Ссылка);
		
	КонецПопытки;
	
КонецПроцедуры

#КонецОбласти

// СтандартныеПодсистемы.ПодключаемыеКоманды

// Определяет список команд создания на основании.
//
// Параметры:
//  КомандыСозданияНаОсновании - см. СозданиеНаОснованииПереопределяемый.ПередДобавлениемКомандСозданияНаОсновании.КомандыСозданияНаОсновании
//  Параметры - см. СозданиеНаОснованииПереопределяемый.ПередДобавлениемКомандСозданияНаОсновании.Параметры
//
Процедура ДобавитьКомандыСозданияНаОсновании(КомандыСозданияНаОсновании, Параметры) Экспорт
	
	Документы.Событие.ДобавитьКомандуСоздатьНаОсновании(КомандыСозданияНаОсновании);
	
КонецПроцедуры

// Для использования в процедуре ДобавитьКомандыСозданияНаОсновании других модулей менеджеров объектов.
// Добавляет в список команд создания на основании этот объект.
//
// Параметры:
//  КомандыСозданияНаОсновании - см. СозданиеНаОснованииПереопределяемый.ПередДобавлениемКомандСозданияНаОсновании.КомандыСозданияНаОсновании
//
// Возвращаемое значение:
//  СтрокаТаблицыЗначений, Неопределено - описание добавленной команды.
//
Функция ДобавитьКомандуСоздатьНаОсновании(КомандыСозданияНаОсновании) Экспорт
	
	Если ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.ПодключаемыеКоманды") Тогда
		МодульСозданиеНаОсновании = ОбщегоНазначения.ОбщийМодуль("СозданиеНаОсновании");
		КомандаСозданияНаОсновании = МодульСозданиеНаОсновании.ДобавитьКомандуСозданияНаОсновании(КомандыСозданияНаОсновании, Метаданные.Документы.Событие);
		Возврат КомандаСозданияНаОсновании;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

Функция ЗагрузитьДанныеРассылкиСервисаДляДанныхФормы(МассоваяРассылка, Событие) Экспорт
	
	ДанныеРассылки = МассоваяРассылка.ПолучитьОбъект().ДанныеРассылкиСервиса();
	ДанныеРассылки.Получатели.Очистить();
	ЗаполнитьПолучателейРассылкиПолучателямиПисьма(ДанныеРассылки, Событие);
	МассовыеРассылкиИнтеграция.ПолучитьСостояниеРассылки(ДанныеРассылки);
	Возврат ДанныеРассылки;
	
КонецФункции

#КонецОбласти

#Область ВерсионированиеОбъектов

// СтандартныеПодсистемы.ВерсионированиеОбъектов

// Определяет настройки объекта для подсистемы ВерсионированиеОбъектов.
//
// Параметры:
//  Настройки - Структура - настройки подсистемы.
Процедура ПриОпределенииНастроекВерсионированияОбъектов(Настройки) Экспорт

КонецПроцедуры

// Конец СтандартныеПодсистемы.ВерсионированиеОбъектов

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Поля.Добавить("Дата");
	Поля.Добавить("НачалоСобытия");
	Поля.Добавить("ТипСобытия");
	Поля.Добавить("ВходящееИсходящееСобытие");
	Поля.Добавить("ПометкаУдаления");
	Поля.Добавить("Тема");
	Поля.Добавить("Состояние");
	Поля.Добавить("Ссылка");
	
КонецПроцедуры

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если Данные.ТипСобытия = Перечисления.ТипыСобытий.ЭлектронноеПисьмо Тогда
		
		Если Данные.ВходящееИсходящееСобытие = Перечисления.ВходящееИсходящееСобытие.Входящее Тогда
			
			ПредставлениеСобытия = НСтр("ru = 'входящее письмо'");
			
		Иначе
			
			Если Данные.Ссылка.Пустая() Или Данные.НачалоСобытия <> '00010101' Тогда
				ПредставлениеСобытия = НСтр("ru = 'исходящее письмо'");
			Иначе
				ПредставлениеСобытия = НСтр("ru = 'черновик'");
			КонецЕсли;
			
		КонецЕсли;
		
		Если ТипЗнч(Данные.Тема) = Тип("СправочникСсылка.ТемыСобытий") Тогда
			ТемаПисьма = Строка(Данные.Тема);
		Иначе
			ТемаПисьма = Данные.Тема;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(ТемаПисьма) Тогда
			ТемаПисьма = НСтр("ru = '<Без темы>'");
		КонецЕсли;
		
		ТемаПисьма = ОбщегоНазначенияКлиентСервер.УдалитьНедопустимыеСимволыXML(ТемаПисьма);
		
		Если СтрДлина(ТемаПисьма) > 35 Тогда
			ТемаПисьма = СтрШаблон(НСтр("ru = '%1...'"), Сред(ТемаПисьма, 1, 35));
			ТемаПисьма = ОбщегоНазначенияКлиентСервер.УдалитьНедопустимыеСимволыXML(ТемаПисьма);
			ТемаПисьма = ОбщегоНазначенияКлиентСервер.УдалитьНедопустимыеСимволыXML(ТемаПисьма);
		КонецЕсли;
		
		Состояние = "";
		Если Данные.ПометкаУдаления Тогда
			Состояние = НСтр("ru='(удален)'");
		КонецЕсли;
		
		Представление = СтрШаблон(НСтр("ru = '%1 (%2, %3) %4'"), 
			ТемаПисьма, 
			ПредставлениеСобытия, 
			Формат(Данные.Дата, "ДФ=dd.MM.yyyy"), 
			Состояние);
		Возврат;
	Иначе
		ПредставлениеСобытия = Данные.ТипСобытия;
	КонецЕсли;
	
	Состояние = "";
	Если Данные.ПометкаУдаления Тогда
		Состояние = НСтр("ru='(удален)'");
	КонецЕсли;
		
	Представление = СтрШаблон(НСтр("ru = 'Событие: %1 от %2 %3'"), ПредставлениеСобытия, Формат(Данные.Дата, "ДФ=dd.MM.yyyy"), Состояние);
	
КонецПроцедуры

Процедура ОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка)
	
	Если ВидФормы <> "ФормаДокумента"
		И ВидФормы <> "ФормаОбъекта" Тогда
		Возврат;
	КонецЕсли;
	
	ТипСобытия = Неопределено; 
	
	Если Параметры.Свойство("Ключ") И ЗначениеЗаполнено(Параметры.Ключ) Тогда
		ТипСобытия	= ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Параметры.Ключ, "ТипСобытия");
	КонецЕсли;
	
	// Если документ копируется, то тип события получаем из копируемого документа.
	Если НЕ ЗначениеЗаполнено(ТипСобытия) Тогда
		Если Параметры.Свойство("ЗначениеКопирования")
			И ЗначениеЗаполнено(Параметры.ЗначениеКопирования) Тогда
			ТипСобытия = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Параметры.ЗначениеКопирования, "ТипСобытия");
		КонецЕсли;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТипСобытия) Тогда
		Если Параметры.Свойство("ЗначенияЗаполнения") 
			И ТипЗнч(Параметры.ЗначенияЗаполнения) = Тип("Структура") Тогда
			
			Если Параметры.ЗначенияЗаполнения.Свойство("ШаблонДокумента") Тогда
				ТипСобытия = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(
				Параметры.ЗначенияЗаполнения.ШаблонДокумента,
				"ТипСобытия");
			КонецЕсли;
			
			Если Параметры.ЗначенияЗаполнения.Свойство("ТипСобытия") Тогда
				ТипСобытия	= Параметры.ЗначенияЗаполнения.ТипСобытия;
			КонецЕсли;
			
		КонецЕсли;
	КонецЕсли;
	
	СтандартнаяОбработка = Ложь;
	ФормыСобытия = ПолучитьСоответствиеВидовОперацийФормам();
	ВыбраннаяФорма = ФормыСобытия[ТипСобытия];
	Если ВыбраннаяФорма = Неопределено Тогда
		ВыбраннаяФорма = "ФормаДокумента";
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область ИнтерфейсКалендаряСотрудника

// Функция определяет пиктограмму для элемента записи календаря
//
// Параметры:
//  Событие	 - ДокументСсылка.Событие	 - событие, для записи календаря которого подбирается картинка
// 
// Возвращаемое значение:
//  Картинка - пиктограмма записи календаря
//
Функция КартинкаЗаписиКалендаря(Событие) Экспорт
	
	ТипСобытия = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Событие, "ТипСобытия");
	СоответствиеТиповКартинкам = ПолучитьСоответствиеТиповСобытийКартинкам();
	Картинка = СоответствиеТиповКартинкам[ТипСобытия];
	Если Картинка = Неопределено Тогда
		Картинка = Новый Картинка;
	КонецЕсли;
	
	Возврат Картинка;
	
КонецФункции

// Функция определяет цвет текста для элемента записи календаря
//
// Параметры:
//  Событие	 - ДокументСсылка.Событие	 - событие, для записи календаря которого подбирается цвет
// 
// Возвращаемое значение:
//  Цвет - цвет текста записи календаря
//
Функция ЦветТекстаЗаписиКалендаря(Событие) Экспорт
	
	ЦветСостояния = Событие.Состояние.Цвет.Получить();
	Если ЦветСостояния = Неопределено Тогда
		ЦветСостояния = Новый Цвет;
	КонецЕсли;
	
	Возврат ЦветСостояния;
	
КонецФункции

// Процедура заполняет таблицу описаний расширенного ввода записи календаря
//
// Параметры:
//  ТаблицаОписаний	 - ТаблицаЗначений	 - описание колонок см. Справочник.ЗаписиКалендаряСотрудника.ПриЗаполненииРасширенногоВводаЗаписиКалендаря()
//
Процедура ПриЗаполненииРасширенногоВводаЗаписиКалендаря(ТаблицаОписаний) Экспорт
	
	НоваяСтрока = ТаблицаОписаний.Добавить();
	НоваяСтрока.ИмяФормы = "Документ.Событие.Форма.ФормаСобытия";
	НоваяСтрока.ПараметрыФормы = Новый Структура("ЗначенияЗаполнения", Новый Структура("ТипСобытия", Перечисления.ТипыСобытий.ЛичнаяВстреча));
	НоваяСтрока.Представление = НСтр("ru='Событие: Личная встреча'");
	
	НоваяСтрока = ТаблицаОписаний.Добавить();
	НоваяСтрока.ИмяФормы = "Документ.Событие.Форма.ФормаСобытия";
	НоваяСтрока.ПараметрыФормы = Новый Структура("ЗначенияЗаполнения", Новый Структура("ТипСобытия", Перечисления.ТипыСобытий.ТелефонныйЗвонок));
	НоваяСтрока.Представление = НСтр("ru='Событие: Телефонный звонок'");
	
	НоваяСтрока = ТаблицаОписаний.Добавить();
	НоваяСтрока.ИмяФормы = "Документ.Событие.Форма.ФормаСобытия";
	НоваяСтрока.ПараметрыФормы = Новый Структура("ЗначенияЗаполнения", Новый Структура("ТипСобытия", Перечисления.ТипыСобытий.Прочее));
	НоваяСтрока.Представление = НСтр("ru='Событие: Прочее'");
	
КонецПроцедуры

#КонецОбласти

#Область ИнтерфейсРабочегоПроцесса

Процедура СоставПолейЗаполненияДляРабочегоПроцесса(ТаблицаОписанияПолей, знач ТипДействия) Экспорт
	
	Если ТипДействия = Перечисления.ТипыДействийРабочегоПроцесса.СоздатьСобытие Тогда
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.ТипСобытия.Имя;
		НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.ТипСобытия.Синоним;
		НовоеОписание.ВариантЗаполнения			= "Указанный";
		НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.ТипСобытия.Тип;
		НовоеОписание.Значение					= Перечисления.ТипыСобытий.ЛичнаяВстреча;
		НовоеОписание.ОбязательноеЗаполнение	= Истина;
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.Состояние.Имя;
		НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.Состояние.Синоним;
		НовоеОписание.ВариантЗаполнения			= "Указанный";
		НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.Состояние.Тип;
		НовоеОписание.Значение					= Метаданные.Документы.Событие.Реквизиты.Состояние.ЗначениеЗаполнения;
		НовоеОписание.ОбязательноеЗаполнение	= Истина;
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.Важность.Имя;
		НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.Важность.Синоним;
		НовоеОписание.ВариантЗаполнения			= "Указанный";
		НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.Важность.Тип;
		НовоеОписание.Значение					= Метаданные.Документы.Событие.Реквизиты.Важность.ЗначениеЗаполнения;
		НовоеОписание.ОбязательноеЗаполнение	= Ложь;
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.НачалоСобытия.Имя;
		НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.НачалоСобытия.Синоним;
		НовоеОписание.ВариантЗаполнения			= "Смещение";
		НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.НачалоСобытия.Тип;
		НовоеОписание.ОбязательноеЗаполнение	= Истина;
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.ОкончаниеСобытия.Имя;
		НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.ОкончаниеСобытия.Синоним;
		НовоеОписание.ВариантЗаполнения			= "Смещение";
		НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.ОкончаниеСобытия.Тип;
		НовоеОписание.ОбязательноеЗаполнение	= Истина;
		
	КонецЕсли;
	
	Если ТипДействия = Перечисления.ТипыДействийРабочегоПроцесса.ОтправитьEmail
		Или ТипДействия = Перечисления.ТипыДействийРабочегоПроцесса.ОтправитьSMS Тогда
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= "Контакт";
		НовоеОписание.Заголовок					= НСтр("ru='Кому'");
		НовоеОписание.ВариантЗаполнения			= "ИзОснования";
		НовоеОписание.ТипЗначения				= Новый ОписаниеТипов("СправочникСсылка.Контрагенты, СправочникСсылка.КонтактныеЛица, СправочникСсылка.Пользователи");
		НовоеОписание.ОбязательноеЗаполнение	= Истина;
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= "ВидКонтактнойИнформации";
		НовоеОписание.Заголовок					= НСтр("ru='Вид контактной информации'");
		НовоеОписание.ВариантЗаполнения			= "Указанный";
		НовоеОписание.ТипЗначения				= Новый ОписаниеТипов("СправочникСсылка.ВидыКонтактнойИнформации");
		Если ТипДействия = Перечисления.ТипыДействийРабочегоПроцесса.ОтправитьEmail Тогда
			НовоеОписание.Значение				= Справочники.ВидыКонтактнойИнформации.EmailКонтрагента;
		Иначе
			НовоеОписание.Значение				= Справочники.ВидыКонтактнойИнформации.ТелефонКонтрагента;
		КонецЕсли;
		НовоеОписание.ОбязательноеЗаполнение	= Истина;
		
		Если ТипДействия = Перечисления.ТипыДействийРабочегоПроцесса.ОтправитьEmail Тогда
			НовоеОписание = ТаблицаОписанияПолей.Добавить();
			НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.УчетнаяЗапись.Имя;
			НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.УчетнаяЗапись.Синоним;
			НовоеОписание.ВариантЗаполнения			= "Указанный";
			НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.УчетнаяЗапись.Тип;
			НовоеОписание.ОбязательноеЗаполнение	= Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	НовоеОписание = ТаблицаОписанияПолей.Добавить();
	НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.Ответственный.Имя;
	НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.Ответственный.Синоним;
	НовоеОписание.ВариантЗаполнения			= "Указанный";
	НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.Ответственный.Тип;
	
	Если ТипДействия = Перечисления.ТипыДействийРабочегоПроцесса.СоздатьСобытие Тогда
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.Содержание.Имя;
		НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.Содержание.Синоним;
		НовоеОписание.ВариантЗаполнения			= "Указанный";
		НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.Содержание.Тип;
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.Тема.Имя;
		НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.Тема.Синоним;
		НовоеОписание.ВариантЗаполнения			= "Указанный";
		НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.Тема.Тип;
		
		НовоеОписание = ТаблицаОписанияПолей.Добавить();
		НовоеОписание.ИмяРеквизита				= Метаданные.Документы.Событие.Реквизиты.КалендарьСотрудника.Имя;
		НовоеОписание.Заголовок					= Метаданные.Документы.Событие.Реквизиты.КалендарьСотрудника.Синоним;
		НовоеОписание.ВариантЗаполнения			= "ИзОснования";
		НовоеОписание.ТипЗначения				= Метаданные.Документы.Событие.Реквизиты.КалендарьСотрудника.Тип;
		
	КонецЕсли;
	
КонецПроцедуры

Функция РеквизитыНедоступныеДляИсточникаРабочегоПроцесса(ТипДействия = Неопределено) Экспорт
	
	НедоступныеРеквизиты = Новый Массив;
	
	НедоступныеРеквизиты.Добавить("СодержаниеHTML");
	НедоступныеРеквизиты.Добавить("КартинкиHTML");
	НедоступныеРеквизиты.Добавить("СписокУчастников");
	
	Возврат НедоступныеРеквизиты;
	
КонецФункции

#КонецОбласти

#Область ШаблоныСообщений

// Вызывается при подготовке шаблонов сообщений и позволяет переопределить список реквизитов и вложений.
//
// Параметры:
//  Реквизиты               - ДеревоЗначений - список реквизитов шаблона.
//         ** Имя            - Строка - Уникальное имя общего реквизита.
//         ** Представление  - Строка - Представление общего реквизита.
//         ** Тип            - Тип    - Тип реквизита. По умолчанию строка.
//         ** Формат         - Строка - Формат вывода значения для чисел, дат, строк и булевых значений.
//  Вложения                - ТаблицаЗначений - печатные формы и вложения
//         ** Имя            - Строка - Уникальное имя вложения.
//         ** Представление  - Строка - Представление варианта.
//         ** ТипФайла       - Строка - Тип вложения, который соответствует расширению файла: "pdf", "png", "jpg", mxl"
//                                      и др.
//  ДополнительныеПараметры - Структура - дополнительные сведения о шаблоне сообщений.
//
Процедура ПриПодготовкеШаблонаСообщения(Реквизиты, Вложения, ДополнительныеПараметры) Экспорт
	ПриПодготовкеШаблонаСообщенияПоУслугам(Реквизиты);
КонецПроцедуры

// Вызывается в момент создания сообщений по шаблону для заполнения значений реквизитов и вложений.
//
// Параметры:
//  Сообщение - Структура - структура с ключами:
//    * ЗначенияРеквизитов - Соответствие - список используемых в шаблоне реквизитов.
//      ** Ключ     - Строка - имя реквизита в шаблоне;
//      ** Значение - Строка - значение заполнения в шаблоне.
//    * ЗначенияОбщихРеквизитов - Соответствие - список используемых в шаблоне общих реквизитов.
//      ** Ключ     - Строка - имя реквизита в шаблоне;
//      ** Значение - Строка - значение заполнения в шаблоне.
//    * Вложения - Соответствие - значения реквизитов 
//      ** Ключ     - Строка - имя вложения в шаблоне;
//      ** Значение - ДвоичныеДанные, Строка - двоичные данные или адрес во временном хранилище вложения.
//  ПредметСообщения - ЛюбаяСсылка - ссылка на объект являющийся источником данных.
//  ДополнительныеПараметры - Структура -  Дополнительная информация о шаблоне сообщения.
//
Процедура ПриФормированииСообщения(Сообщение, ПредметСообщения, ДополнительныеПараметры) Экспорт
	СформироватьПараметрыПоУслугам(Сообщение, ПредметСообщения);
КонецПроцедуры

// Заполняет список получателей SMS при отправке сообщения сформированного по шаблону.
//
// Параметры:
//   ПолучателиSMS - ТаблицаЗначений - список получается SMS.
//     * НомерТелефона - Строка - номер телефона, куда будет отправлено сообщение SMS.
//     * Представление - Строка - представление получателя сообщения SMS.
//     * Контакт       - Произвольный - контакт, которому принадлежит номер телефона.
//  ПредметСообщения - ЛюбаяСсылка, Структура - ссылка на объект являющийся источником данных, либо структура,
//    * Предмет               - ЛюбаяСсылка - ссылка на объект являющийся источником данных
//    * ПроизвольныеПараметры - Соответствие - заполненный список произвольных параметров.//
//
Процедура ПриЗаполненииТелефоновПолучателейВСообщении(ПолучателиSMS, ПредметСообщения) Экспорт
	
КонецПроцедуры

// Заполняет список получателей письма при отправки сообщения сформированного по шаблону.
//
// Параметры:
//   ПолучателиПисьма - ТаблицаЗначений - список получается письма.
//     * Адрес           - Строка - адрес электронной почты получателя.
//     * Представление   - Строка - представление получается письма.
//     * Контакт         - Произвольный - контакт, которому принадлежит адрес электронной почты.
//  ПредметСообщения - ЛюбаяСсылка, Структура - ссылка на объект являющийся источником данных, либо структура,
//                                              если шаблон содержит произвольные параметры:
//    * Предмет               - ЛюбаяСсылка - ссылка на объект являющийся источником данных
//    * ПроизвольныеПараметры - Соответствие - заполненный список произвольных параметров.
//
Процедура ПриЗаполненииПочтыПолучателейВСообщении(ПолучателиПисьма, ПредметСообщения) Экспорт
	
КонецПроцедуры

#Область ШаблоныСообщенийУслуги

// Шаблоны для услуг по умолчанию
Процедура СоздатьПоставляемыеШаблоныСообщенийПоУслугам() Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ШаблоныСообщений.Наименование КАК Наименование
	|ИЗ
	|	Справочник.ШаблоныСообщений КАК ШаблоныСообщений
	|ГДЕ
	|	((ШаблоныСообщений.Наименование = &ПодтверждениеЗаписи
	|			ИЛИ ШаблоныСообщений.Наименование = &НапоминаниеОбУслуге
	|			ИЛИ ШаблоныСообщений.Наименование = &ПодтверждениеОтмены
	|			ИЛИ ШаблоныСообщений.Наименование = &СборОтзывов)
	|				И ШаблоныСообщений.ПолноеИмяТипаПараметраВводаНаОсновании = ""Документ.Событие"")";
	
	Запрос.УстановитьПараметр("ПодтверждениеЗаписи", НаименованиеШаблонаПодтверждениеЗаписи());
	Запрос.УстановитьПараметр("НапоминаниеОбУслуге", НаименованиеШаблонаНапоминаниеОЗаписи());
	Запрос.УстановитьПараметр("ПодтверждениеОтмены", НаименованиеШаблонаПодтверждениеОтменыЗаписи());
	Запрос.УстановитьПараметр("СборОтзывов",         НаименованиеШаблонаЗапросНаСборОтзыва());
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	ШаблоныПодтверждения       = Результат.НайтиСтроки(Новый Структура("Наименование", НаименованиеШаблонаПодтверждениеЗаписи()));
	ШаблоныНапоминания         = Результат.НайтиСтроки(Новый Структура("Наименование", НаименованиеШаблонаНапоминаниеОЗаписи()));
	ШаблоныПодтвержденияОтмены = Результат.НайтиСтроки(Новый Структура("Наименование", НаименованиеШаблонаПодтверждениеОтменыЗаписи()));
	ШаблоныСборОтзывов         = Результат.НайтиСтроки(Новый Структура("Наименование", НаименованиеШаблонаЗапросНаСборОтзыва()));
	
	Если ШаблоныПодтверждения.Количество() = 0 Тогда
		ДанныеШаблона = ДанныеШаблонаПодтверждениеЗаписи();
		ПараметрыШаблона = ШаблоныСообщенийКлиентСервер.ОписаниеПараметровШаблона();
		ПараметрыШаблона.Текст = ДанныеШаблона.Текст;
		ПараметрыШаблона.Тема = НСтр("ru='Информация о записи'");
		ПараметрыШаблона.ТипШаблона = "Письмо";
		ПараметрыШаблона.ПолноеИмяТипаНазначения = "Документ.Событие";
		ШаблоныСообщенийУНФ.СоздатьШаблонEmail(НаименованиеШаблонаПодтверждениеЗаписи(), ПараметрыШаблона, ,
			ДанныеШаблона.Вложения);
	КонецЕсли;
	
	Если ШаблоныНапоминания.Количество() = 0 Тогда
		ДанныеШаблона = ДанныеШаблонаНапоминаниеОЗаписи();
		ПараметрыШаблона = ШаблоныСообщенийКлиентСервер.ОписаниеПараметровШаблона();
		ПараметрыШаблона.Текст = ДанныеШаблона.Текст;
		ПараметрыШаблона.Тема = НСтр("ru='Информация о записи'");
		ПараметрыШаблона.ТипШаблона = "Письмо";
		ПараметрыШаблона.ПолноеИмяТипаНазначения = "Документ.Событие";
		ШаблоныСообщенийУНФ.СоздатьШаблонEmail(НаименованиеШаблонаНапоминаниеОЗаписи(), ПараметрыШаблона, ,
			ДанныеШаблона.Вложения);
	КонецЕсли;
	
	Если ШаблоныПодтвержденияОтмены.Количество() = 0 Тогда
		ДанныеШаблона = ДанныеШаблонаПодтверждениеОтменыЗаписи();
		ПараметрыШаблона = ШаблоныСообщенийКлиентСервер.ОписаниеПараметровШаблона();
		ПараметрыШаблона.Текст = ДанныеШаблона.Текст;
		ПараметрыШаблона.Тема = НСтр("ru='Подтверждение отмены записи'");
		ПараметрыШаблона.ТипШаблона = "Письмо";
		ПараметрыШаблона.ПолноеИмяТипаНазначения = "Документ.Событие";
		ШаблоныСообщенийУНФ.СоздатьШаблонEmail(НаименованиеШаблонаПодтверждениеОтменыЗаписи(), ПараметрыШаблона, ,
			ДанныеШаблона.Вложения);
	КонецЕсли;
	
	Если ШаблоныСборОтзывов.Количество() = 0 Тогда
		ДанныеШаблона = ДанныеШаблонаЗапросНаСборОтзыва();
		ПараметрыШаблона = ШаблоныСообщенийКлиентСервер.ОписаниеПараметровШаблона();
		ПараметрыШаблона.Текст = ДанныеШаблона.Текст;
		ПараметрыШаблона.Тема = НСтр("ru='Оставьте отзыв о визите'");
		ПараметрыШаблона.ТипШаблона = "Письмо";
		ПараметрыШаблона.ПолноеИмяТипаНазначения = "Документ.Событие";
		ШаблоныСообщенийУНФ.СоздатьШаблонEmail(НаименованиеШаблонаЗапросНаСборОтзыва(), ПараметрыШаблона, ,
			ДанныеШаблона.Вложения);
	КонецЕсли;

КонецПроцедуры

Процедура СформироватьПараметрыПоУслугам(Сообщение, ПредметСообщения)
	
	Если ТипЗнч(ПредметСообщения) = Тип("ДокументСсылка.Событие") 
		И ПредметСообщения.ТипСобытия <> Перечисления.ТипыСобытий.Запись Тогда
		Возврат;
	КонецЕсли;
	
	Параметр = ИмяПараметраВремяДоНачалаЗаписи();
	Если Сообщение.ЗначенияРеквизитов.Получить(Параметр) <> Неопределено Тогда
		Сообщение.ЗначенияРеквизитов[Параметр] = ПараметрВремяДоНачалаЗаписи(Сообщение, ПредметСообщения);
	КонецЕсли;
	
	Параметр = ИмяПараметраСписокРесурсовСФотоИУказаниемДлительностиРесурса();
	Если Сообщение.ЗначенияРеквизитов.Получить(Параметр) <> Неопределено Тогда
		Сообщение.ЗначенияРеквизитов[Параметр] = СформироватьЗначениеПараметра(Параметр, ПредметСообщения, Сообщение);
	КонецЕсли;
	
	Параметр = ИмяПараметраСписокРесурсовСФотоИУказаниемПериодаРесурса();
	Если Сообщение.ЗначенияРеквизитов.Получить(Параметр) <> Неопределено Тогда
		Сообщение.ЗначенияРеквизитов[Параметр] = СформироватьЗначениеПараметра(Параметр, ПредметСообщения, Сообщение);
	КонецЕсли;
	
	Параметр = ИмяПараметраСписокРесурсовСУказаниемДлительностиРесурса();
	Если Сообщение.ЗначенияРеквизитов.Получить(Параметр) <> Неопределено Тогда
		Сообщение.ЗначенияРеквизитов[Параметр] = СформироватьЗначениеПараметра(Параметр, ПредметСообщения, Сообщение);
	КонецЕсли;
	
	Параметр = ИмяПараметраСписокРесурсовСУказаниемПериодаРесурса();
	Если Сообщение.ЗначенияРеквизитов.Получить(Параметр) <> Неопределено Тогда
		Сообщение.ЗначенияРеквизитов[Параметр] = СформироватьЗначениеПараметра(Параметр, ПредметСообщения, Сообщение);
	КонецЕсли;
	
КонецПроцедуры

Функция СформироватьЗначениеПараметра(ФормируемыйПараметр, ПредметСообщения, Сообщение)
	
	ТаблицаДвижений = ПланированиеРесурсовУНФ.ТаблицаДвиженийДокументов(ПредметСообщения);
	ФотографииМастеров = ТаблицаФотографий(ПредметСообщения);
	
	Если Сообщение.ДополнительныеПараметры.Свойство("ДеньНапоминанияПоРесурсам") Тогда
		ПреобразоватьТаблицуДвиженийПоДнюНапоминания(ТаблицаДвижений, Сообщение);
	КонецЕсли;

	СписокРесурсов = Новый Массив;
	МинимальнаяДата = Дата(1,1,1);
	
	Для Каждого Движение Из ТаблицаДвижений Цикл
		
		Если Движение.Период > МинимальнаяДата Тогда
			МинимальнаяДата = Движение.Период;
			СписокРесурсов.Добавить(ПереносСтрокиПоТипуСообщения(Сообщение) + Формат(Движение.Период,"ДФ=дд.ММ.гг")+  ПереносСтрокиПоТипуСообщения(Сообщение));
		КонецЕсли;
		
		ДобавитьКартинкуМастера(ФормируемыйПараметр, Движение, СписокРесурсов, ФотографииМастеров, Сообщение);
				
		ИнформацияОРесурсе = Новый Массив;
		ИнформацияОРесурсе.Добавить(Строка(Движение.РесурсПредприятия));
		ИнформацияОРесурсе.Добавить(ИнформацияОВремениИПериоде(ФормируемыйПараметр, Движение, Сообщение));
		
		СписокРесурсов.Добавить(СтрСоединить(ИнформацияОРесурсе, Символы.НПП));
		
	КонецЦикла;
	
	Возврат СтрСоединить(СписокРесурсов, ПереносСтрокиПоТипуСообщения(Сообщение));
	
КонецФункции

Функция ПараметрВремяДоНачалаЗаписи(Сообщение, ПредметСообщения)
	
	ТаблицаДвижений = ПланированиеРесурсовУНФ.ТаблицаДвиженийДокументов(ПредметСообщения);
	
	Если Сообщение.ДополнительныеПараметры.Свойство("ДеньНапоминанияПоРесурсам") Тогда
		ПреобразоватьТаблицуДвиженийПоДнюНапоминания(ТаблицаДвижений, Сообщение);
	КонецЕсли;
	
	МаксимальныеГраницы = ПланированиеРесурсовУНФ.МаксимальныеГраницыИнтервала(ТаблицаДвижений, "Старт", "Финиш");
	ВремяДоЗаписи = МаксимальныеГраницы.НачалоИнтервала - ТекущаяДатаСеанса();
		
	Дни    = Цел(ВремяДоЗаписи/3600/24);
	Часы   = Цел((ВремяДоЗаписи - Дни*3600*24)/3600);
	Минуты = Цел((ВремяДоЗаписи- Дни*3600*24 - Часы*3600)/60);
	КомпонентыВремени = Новый Массив;
	
	Если ЗначениеЗаполнено(Дни)Тогда
		КомпонентыВремени.Добавить(Строка(Дни));
		КомпонентыВремени.Добавить("д.");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Часы)Тогда
		КомпонентыВремени.Добавить(Строка(Часы));
		КомпонентыВремени.Добавить("ч.");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Минуты) Тогда
		КомпонентыВремени.Добавить(Строка(Минуты));
		КомпонентыВремени.Добавить("мин.");
	КонецЕсли;
	
	Время = СтрСоединить(КомпонентыВремени, Символы.НПП);
	Возврат Время;
	
КонецФункции

Процедура ПреобразоватьТаблицуДвиженийПоДнюНапоминания(ТаблицаДвижений, Сообщение)
	
	ТекущийИндекс = ТаблицаДвижений.Количество() - 1;
	
	Пока ТекущийИндекс >= 0 Цикл
		Если ТаблицаДвижений[ТекущийИндекс].Старт < НачалоДня(Сообщение.ДополнительныеПараметры.ДеньНапоминанияПоРесурсам) 
			И ТаблицаДвижений[ТекущийИндекс].Финиш > КонецДня(Сообщение.ДополнительныеПараметры.ДеньНапоминанияПоРесурсам) Тогда
			ТаблицаДвижений.Удалить(ТекущийИндекс);
		КонецЕсли;
		
		ТекущийИндекс = ТекущийИндекс -1;
	КонецЦикла;

КонецПроцедуры

Процедура ДобавитьКартинкуМастера(ФормируемыйПараметр, Движение, СписокРесурсов, ФотографииМастеров, Сообщение)
	
	Если Сообщение.ДополнительныеПараметры.ВидСообщения = "СообщениеSMS" Тогда
		Возврат;
	КонецЕсли;
		
	Если НЕ ФормируемыйПараметр = "СписокРесурсовСФотоИУказаниемПериодаРесурса"
		ИЛИ ФормируемыйПараметр = "СписокРесурсовСФотоИУказаниемДлительностиРесурса" Тогда
		Возврат;
	КонецЕсли;
	
	КартинкаМастера = ФотографииМастеров.НайтиСтроки(Новый Структура("РесурсПредприятия", Движение.РесурсПредприятия));
	Если КартинкаМастера.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	ТекстКартинки = "<img height=""90"" src=""cid:[ИдентификаторКартинки]"" style=""border:none;"" width=""90""></img><br/>";
	НовоеВложение = ШаблоныСообщенийУНФ.НовыйУникальныйИдентификаторВложенияHTML();
	
	ТекстКартинки = СтрЗаменить(ТекстКартинки, "cid:[ИдентификаторКартинки]", "cid:" + НовоеВложение);
	Если ЗначениеЗаполнено(КартинкаМастера[0].ФайлКартинки) Тогда
		ДвоичныеДанныеФайла = РаботаСФайлами.ДвоичныеДанныеФайла(КартинкаМастера[0].ФайлКартинки, Ложь);
		СсылкаНаДвоичныеДанные = ПоместитьВоВременноеХранилище(ДвоичныеДанныеФайла);
		Сообщение.Вложения.Вставить(НовоеВложение, СсылкаНаДвоичныеДанные);
		СписокРесурсов.Добавить(ТекстКартинки);
	КонецЕсли;
	
КонецПроцедуры

Функция ИнформацияОВремениИПериоде(ФормируемыйПараметр, Движение, Сообщение)
	
	Время = "";
	
	Если ФормируемыйПараметр = "СписокРесурсовСФотоИУказаниемПериодаРесурса"
		ИЛИ ФормируемыйПараметр ="СписокРесурсовСУказаниемПериодаРесурса" Тогда
		
		Время = "c " + Строка(Формат(Движение.Старт,"ДФ=ЧЧ:мм")) + " до " +  Строка(Формат(Движение.Финиш, "ДФ=ЧЧ:мм")) + ПереносСтрокиПоТипуСообщения(Сообщение);
		Возврат Строка(Время);
		
	КонецЕсли;
					
	КомпонентыВремени = Новый Массив;
	
	Длительность = Движение.Финиш - Движение.Старт;
	Часы = Цел(Длительность/3600);
	Минуты = Цел((Длительность-Часы*3600)/60);
	
	КомпонентыВремени.Добавить("c");
	КомпонентыВремени.Добавить(Строка(Формат(Движение.Старт,"ДФ=ЧЧ:мм")));
	
	КомпонентыВремени.Добавить("(");
	Если ЗначениеЗаполнено(Часы)Тогда
		КомпонентыВремени.Добавить(Строка(Часы));
		КомпонентыВремени.Добавить("ч.");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Минуты) Тогда
		КомпонентыВремени.Добавить(Строка(Минуты));
		КомпонентыВремени.Добавить("мин.");
	КонецЕсли;
	КомпонентыВремени.Добавить(")");
	КомпонентыВремени.Добавить(ПереносСтрокиПоТипуСообщения(Сообщение));
	
	Время = СтрСоединить(КомпонентыВремени, Символы.НПП);
	
	Возврат Строка(Время);
	
КонецФункции

Функция ПереносСтрокиПоТипуСообщения(Сообщение)
	
	Если Сообщение.ДополнительныеПараметры.ВидСообщения = "СообщениеSMS" Тогда
		Возврат Символы.НПП;
	КонецЕсли;
	
	Возврат "<br/>";
	
КонецФункции

Функция ДанныеШаблонаПодтверждениеЗаписи()
	
	ТекстШаблона = "<html><head><meta http-equiv=""Content-Type"" content=""text/html; charset=utf-8""></meta><style type=""text/css"">
	|body{margin:0;padding:8px;}
	|p{line-height:1.15;margin:0;white-space:pre-wrap;}
	|ol,ul{margin-top:0;margin-bottom:0;}
	|img{border:none;}
	|li>p{display:inline;}
	|</style></head><body>
	|<p style=""text-align: center;""><img height=""57"" src=""cid:[ИдентификаторКартинки]"" style=""border:none;"" width=""200""></img></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Здравствуйте, [Событие.Контакт]!</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Вы успешно записаны:</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;"">[Событие.СписокРесурсовСФотоИУказаниемПериодаРесурса]</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;"">[Событие.КнопкаОтменитьЗапись]</span></p>
	|<p style=""line-height: 1.5;text-align: center;""><a href=""http://unf.1c.ru"" style=""line-height: 1.5;"">unf.1c.ru</a><br><span style=""color: #999999;font-family: Calibri Light;font-size: 10pt;font-style: normal;font-weight: normal;text-decoration: none;"">© Дополните имя вашей компании </span><br><span style=""color: #999999;font-family: Calibri Light;font-size: 10pt;font-style: normal;font-weight: normal;text-decoration: none;"">А еще юридический адрес и публичные реквизиты</span></p>
	|</body></html>";
	
	НовоеВложение = ШаблоныСообщенийУНФ.НовыйУникальныйИдентификаторВложенияHTML();
	ТекстШаблона = СтрЗаменить(ТекстШаблона, "cid:[ИдентификаторКартинки]", "cid:%1" + НовоеВложение);
	
	Вложения = Новый Массив;
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов");
	ПараметрыФайла.Вставить("ИмяБезРасширения",   НовоеВложение);
	ПараметрыФайла.Вставить("РасширениеБезТочки", "png");
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", Неопределено);
	ПараметрыФайла.Вставить("СсылкаНаДвоичныеДанные", ПоместитьВоВременноеХранилище(БиблиотекаКартинок.Логотип1СУправлениеНашейФирмой.ПолучитьДвоичныеДанные()));

	Вложения.Добавить(ПараметрыФайла);
	
	Результат = Новый Структура;
	Результат.Вставить("Текст", ТекстШаблона);
	Результат.Вставить("Вложения", Вложения);
	
	Возврат Результат;
	
КонецФункции

Функция ДанныеШаблонаНапоминаниеОЗаписи()
	
	ТекстШаблона = "<html><head><meta http-equiv=""Content-Type"" content=""text/html; charset=utf-8""></meta><style type=""text/css"">
	|body{margin:0;padding:8px;}
	|p{line-height:1.15;margin:0;white-space:pre-wrap;}
	|ol,ul{margin-top:0;margin-bottom:0;}
	|img{border:none;}
	|li>p{display:inline;}
	|</style></head><body>
	|<p style=""text-align: center;""><img height=""57"" src=""cid:[ИдентификаторКартинки]"" style=""border:none;"" width=""200""></img></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Здравствуйте, [Событие.Контакт]!</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Напоминаем Вам о записи через [Событие.ВремяДоНачалаЗаписи]:</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;"">[Событие.СписокРесурсовСФотоИУказаниемПериодаРесурса]</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;"">[Событие.КнопкаОтменитьЗапись]</span></p>
	|<p style=""line-height: 1.5;text-align: center;""><a href=""http://unf.1c.ru"" style=""line-height: 1.5;"">unf.1c.ru</a><br><span style=""color: #999999;font-family: Calibri Light;font-size: 10pt;font-style: normal;font-weight: normal;text-decoration: none;"">© Дополните имя вашей компании </span><br><span style=""color: #999999;font-family: Calibri Light;font-size: 10pt;font-style: normal;font-weight: normal;text-decoration: none;"">А еще юридический адрес и публичные реквизиты</span></p>
	|</body></html>";
	
	НовоеВложение = ШаблоныСообщенийУНФ.НовыйУникальныйИдентификаторВложенияHTML();
	ТекстШаблона = СтрЗаменить(ТекстШаблона, "cid:[ИдентификаторКартинки]", "cid:%1" + НовоеВложение);
	
	Вложения = Новый Массив;
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов");
	ПараметрыФайла.Вставить("ИмяБезРасширения",   НовоеВложение);
	ПараметрыФайла.Вставить("РасширениеБезТочки", "png");
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", Неопределено);
	ПараметрыФайла.Вставить("СсылкаНаДвоичныеДанные", ПоместитьВоВременноеХранилище(БиблиотекаКартинок.Логотип1СУправлениеНашейФирмой.ПолучитьДвоичныеДанные()));

	Вложения.Добавить(ПараметрыФайла);
	
	Результат = Новый Структура;
	Результат.Вставить("Текст", ТекстШаблона);
	Результат.Вставить("Вложения", Вложения);
	
	Возврат Результат;
	
КонецФункции

Функция ДанныеШаблонаПодтверждениеОтменыЗаписи()
	
	ТекстШаблона = "<html><head><meta http-equiv=""Content-Type"" content=""text/html; charset=utf-8""></meta><style type=""text/css"">
	|body{margin:0;padding:8px;}
	|p{line-height:1.15;margin:0;white-space:pre-wrap;}
	|ol,ul{margin-top:0;margin-bottom:0;}
	|img{border:none;}
	|li>p{display:inline;}
	|</style></head><body>
	|<p style=""text-align: center;""><img height=""57"" src=""cid:[ИдентификаторКартинки]"" style=""border:none;"" width=""200""></img></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Здравствуйте, [Событие.Контакт]!</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Ваша запись успешно отменена</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Ожидайте звонка менеджера для уточнения возможности перезаписи</span></p>
	|<p style=""line-height: 1.5;text-align: center;""><a href=""http://unf.1c.ru"" style=""line-height: 1.5;"">unf.1c.ru</a><br><span style=""color: #999999;font-family: Calibri Light;font-size: 10pt;font-style: normal;font-weight: normal;text-decoration: none;"">© Дополните имя вашей компании </span><br><span style=""color: #999999;font-family: Calibri Light;font-size: 10pt;font-style: normal;font-weight: normal;text-decoration: none;"">А еще юридический адрес и публичные реквизиты</span></p>
	|</body></html>";
	
	НовоеВложение = ШаблоныСообщенийУНФ.НовыйУникальныйИдентификаторВложенияHTML();
	ТекстШаблона = СтрЗаменить(ТекстШаблона, "cid:[ИдентификаторКартинки]", "cid:%1" + НовоеВложение);
	
	Вложения = Новый Массив;
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов");
	ПараметрыФайла.Вставить("ИмяБезРасширения",   НовоеВложение);
	ПараметрыФайла.Вставить("РасширениеБезТочки", "png");
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", Неопределено);
	ПараметрыФайла.Вставить("СсылкаНаДвоичныеДанные", ПоместитьВоВременноеХранилище(БиблиотекаКартинок.Логотип1СУправлениеНашейФирмой.ПолучитьДвоичныеДанные()));

	Вложения.Добавить(ПараметрыФайла);
	
	Результат = Новый Структура;
	Результат.Вставить("Текст", ТекстШаблона);
	Результат.Вставить("Вложения", Вложения);
	
	Возврат Результат;
	
КонецФункции

Функция ДанныеШаблонаЗапросНаСборОтзыва()
	
	ТекстШаблона = "<html><head><meta http-equiv=""Content-Type"" content=""text/html; charset=utf-8""></meta><style type=""text/css"">
	|body{margin:0;padding:8px;}
	|p{line-height:1.15;margin:0;white-space:pre-wrap;}
	|ol,ul{margin-top:0;margin-bottom:0;}
	|img{border:none;}
	|li>p{display:inline;}
	|</style></head><body>
	|<p style=""text-align: center;""><img height=""57"" src=""cid:[ИдентификаторКартинки]"" style=""border:none;"" width=""200""></img></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Здравствуйте, [Событие.Контакт]!</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Спасибо за то, что выбрали нашу компанию. Надеемся, Вы остались довольны.</span></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Оставьте пожалуйста свой отзыв о посещении, перейдя по ссылке:</span></p>
	|<p style=""text-align: center;""><a href=""https://docs.google.com/forms/d/e/1FAIpQLSf9Q93nN3YC9ZIEM-ICk09pJ0k0wvozy-aXUs9-9guLW2ZtUA/viewform?usp=pp_url&amp;entry.59920305=[ЗаказПокупателя.Номер]"">https://docs.google.com/forms/d/e/1FAIpQLSf9Q93nN3YC9ZIEM-ICk09pJ0k0wvozy-aXUs9-9guLW2ZtUA/viewform?usp=pp_url&amp;entry.59920305=[Событие.Номер]</a></p>
	|<p style=""text-align: center;""><span style=""font-size: 12pt;text-align: center;"">Ваш отзыв поможет нам стать лучше.</span></p>
	|<p style=""line-height: 1.5;text-align: center;""><a href=""http://unf.1c.ru"" style=""line-height: 1.5;"">unf.1c.ru</a><br><span style=""color: #999999;font-family: Calibri Light;font-size: 10pt;font-style: normal;font-weight: normal;text-decoration: none;"">© Дополните имя вашей компании </span><br><span style=""color: #999999;font-family: Calibri Light;font-size: 10pt;font-style: normal;font-weight: normal;text-decoration: none;"">А еще юридический адрес и публичные реквизиты</span></p>
	|</body></html>";
	
	НовоеВложение = ШаблоныСообщенийУНФ.НовыйУникальныйИдентификаторВложенияHTML();
	ТекстШаблона = СтрЗаменить(ТекстШаблона, "cid:[ИдентификаторКартинки]", "cid:%1" + НовоеВложение);
	
	Вложения = Новый Массив;
	
	ПараметрыФайла = Новый Структура;
	ПараметрыФайла.Вставить("Автор", Неопределено);
	ПараметрыФайла.Вставить("ВладелецФайлов");
	ПараметрыФайла.Вставить("ИмяБезРасширения",   НовоеВложение);
	ПараметрыФайла.Вставить("РасширениеБезТочки", "png");
	ПараметрыФайла.Вставить("ВремяИзмененияУниверсальное", Неопределено);
	ПараметрыФайла.Вставить("СсылкаНаДвоичныеДанные", ПоместитьВоВременноеХранилище(БиблиотекаКартинок.Логотип1СУправлениеНашейФирмой.ПолучитьДвоичныеДанные()));

	Вложения.Добавить(ПараметрыФайла);
	
	Результат = Новый Структура;
	Результат.Вставить("Текст", ТекстШаблона);
	Результат.Вставить("Вложения", Вложения);
	
	Возврат Результат;
	
КонецФункции

Функция НаименованиеШаблонаПодтверждениеЗаписи() Экспорт
	
	Возврат НСтр("ru='[Пример] Подтверждение записи'");
	
КонецФункции

Функция НаименованиеШаблонаНапоминаниеОЗаписи() Экспорт
	
	Возврат НСтр("ru='[Пример] Напоминание о записи'");
	
КонецФункции

Функция НаименованиеШаблонаПодтверждениеОтменыЗаписи() Экспорт
	
	Возврат НСтр("ru='[Пример] Подтверждение отмены записи'");
	
КонецФункции

Функция НаименованиеШаблонаЗапросНаСборОтзыва() Экспорт
	
	Возврат НСтр("ru='[Пример] Сбор отзыва после записи'");
	
КонецФункции

Процедура ПриПодготовкеШаблонаСообщенияПоУслугам(Реквизиты)
	
	ИспользоватьРесурсы = ПолучитьФункциональнуюОпцию("ПланироватьЗагрузкуРесурсовПредприятияЖурналЗаписи");
	
	Если ИспользоватьРесурсы Тогда
		Возврат;
	КонецЕсли;
	
	ТекущийИндекс = Реквизиты.Количество() - 1;
	
	Пока ТекущийИндекс >= 0 Цикл
		Если ЭтоПараметрСообщенияДляУслуг(Реквизиты[ТекущийИндекс].Имя) Тогда
			Реквизиты.Удалить(ТекущийИндекс);
		КонецЕсли;
		ТекущийИндекс = ТекущийИндекс - 1;
	КонецЦикла;
	
КонецПроцедуры

Функция ЭтоПараметрСообщенияДляУслуг(ПараметрСообщения)
	
	Возврат ПараметрСообщения = ИмяПараметраКнопкаОтменитьЗапись()
			ИЛИ ПараметрСообщения = "Событие." + ИмяПараметраСписокРесурсовСУказаниемПериодаРесурса()
			ИЛИ ПараметрСообщения = "Событие." + ИмяПараметраСписокРесурсовСФотоИУказаниемПериодаРесурса()
			ИЛИ ПараметрСообщения = "Событие." + ИмяПараметраСписокРесурсовСФотоИУказаниемДлительностиРесурса()
			ИЛИ ПараметрСообщения = "Событие." + ИмяПараметраСписокРесурсовСУказаниемДлительностиРесурса()
			ИЛИ ПараметрСообщения = "Событие." + ИмяПараметраВремяДоНачалаЗаписи();
			
КонецФункции

Функция ИмяПараметраКнопкаОтменитьЗапись()
	
	Возврат "КнопкаОтменитьЗапись";
	
КонецФункции

Функция ИмяПараметраВремяДоНачалаЗаписи()
	
	Возврат "ВремяДоНачалаЗаписи";
	
КонецФункции

Функция ИмяПараметраСписокРесурсовСУказаниемПериодаРесурса()
	
	Возврат "СписокРесурсовСУказаниемПериодаРесурса";
	
КонецФункции

Функция ИмяПараметраСписокРесурсовСФотоИУказаниемПериодаРесурса()
	
	Возврат "СписокРесурсовСФотоИУказаниемПериодаРесурса";
	
КонецФункции

Функция ИмяПараметраСписокРесурсовСФотоИУказаниемДлительностиРесурса()
	
	Возврат "СписокРесурсовСФотоИУказаниемДлительностиРесурса";
	
КонецФункции

Функция ИмяПараметраСписокРесурсовСУказаниемДлительностиРесурса()
	
	Возврат "СписокРесурсовСУказаниемДлительностиРесурса";
	
КонецФункции

Функция ТаблицаФотографий(Событие)
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	СобытиеРесурсыПредприятия.РесурсПредприятия КАК РесурсПредприятия,
	|	КлючевыеРесурсы.ФайлКартинки КАК ФайлКартинки
	|ИЗ
	|	Документ.Событие.РесурсыПредприятия КАК СобытиеРесурсыПредприятия
	|		ЛЕВОЕ СОЕДИНЕНИЕ Справочник.КлючевыеРесурсы КАК КлючевыеРесурсы
	|		ПО СобытиеРесурсыПредприятия.РесурсПредприятия = КлючевыеРесурсы.Ссылка
	|ГДЕ
	|	СобытиеРесурсыПредприятия.Ссылка = &Событие";
	
	Запрос.УстановитьПараметр("Событие", Событие);
	
	Результат = Запрос.Выполнить().Выгрузить();
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ПеренестиДанныеСобытияЭлектронноеПисьмо(Событие, Участник, АдресатыСобытий, СоответствиеНовыхАдресатов)
	
	Если НЕ ЗначениеЗаполнено(Участник.КакСвязаться) Тогда
		Возврат;
	КонецЕсли;
	
	Адреса = ОбщегоНазначенияКлиентСервер.АдресаЭлектроннойПочтыИзСтроки(Участник.КакСвязаться);
		
	Для Каждого Адрес Из Адреса Цикл
		
		КакСвязаться = "";
		АдресатПисьма = Неопределено;
		
		Если НЕ ЗначениеЗаполнено(Адрес.ОписаниеОшибки) Тогда
			КакСвязаться = Адрес.Адрес;
		Иначе
			КакСвязаться = Адрес.Псевдоним;
		КонецЕсли; 
		
		СсылкаНаАдресата = АдресатыСобытий.НайтиСтроки(Новый Структура("Адресат", НРег(КакСвязаться)));
		
		Если СсылкаНаАдресата.Количество() > 0 Тогда
			АдресатПисьма = СсылкаНаАдресата[0].Ссылка;
		ИначеЕсли СоответствиеНовыхАдресатов[КакСвязаться] <> Неопределено Тогда
			АдресатПисьма = СоответствиеНовыхАдресатов[КакСвязаться];
		Иначе
			АдресатПисьма = Неопределено;
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(АдресатПисьма) И ЗначениеЗаполнено(КакСвязаться) Тогда
			
			АдресатПисьма = Справочники.АдресатыПисем.НовыйАдресатПисьмаПоДаннымКонтакта(Участник.Контакт, КакСвязаться);
			СоответствиеНовыхАдресатов.Вставить(КакСвязаться,
				АдресатПисьма);
			
		КонецЕсли;
		
		АдресатУжеЕстьВКонтактах = Событие.Участники.НайтиСтроки(Новый Структура("Контакт,ТипПолучателяЭлектронногоПисьма", 
			АдресатПисьма.Ссылка, Участник.ТипПолучателяЭлектронногоПисьма));
		
		Если АдресатУжеЕстьВКонтактах.Количество() > 0  Тогда
			Возврат;
		КонецЕсли;
		
		НоваяСтрока = Событие.Участники.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, Участник);
		НоваяСтрока.Контакт = АдресатПисьма;
		НоваяСтрока.КакСвязаться = КакСвязаться;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ПолучитьСоответствиеВидовОперацийФормам() Экспорт

	ФормыСобытия = Новый Соответствие;
	ФормыСобытия.Вставить(Перечисления.ТипыСобытий.ЭлектронноеПисьмо, "ФормаЭлектронногоПисьма");
	ФормыСобытия.Вставить(Перечисления.ТипыСобытий.СообщениеSMS,      "ФормаСообщенияSMS");
	ФормыСобытия.Вставить(Перечисления.ТипыСобытий.ТелефонныйЗвонок,  "ФормаСобытия");
	ФормыСобытия.Вставить(Перечисления.ТипыСобытий.ЛичнаяВстреча,     "ФормаСобытия");
	ФормыСобытия.Вставить(Перечисления.ТипыСобытий.Запись, "ФормаСобытияЗаписьКонтрагента");
	ФормыСобытия.Вставить(Перечисления.ТипыСобытий.Прочее,            "ФормаСобытия");
	
	Возврат ФормыСобытия;

КонецФункции 

Функция ПолучитьСоответствиеТиповСобытийКартинкам() Экспорт
	
	СоответствиеТиповКартинкам = Новый Соответствие;
	СоответствиеТиповКартинкам.Вставить(Перечисления.ТипыСобытий.ЛичнаяВстреча, БиблиотекаКартинок.СобытиеЛичнаяВстреча);
	СоответствиеТиповКартинкам.Вставить(Перечисления.ТипыСобытий.Прочее, БиблиотекаКартинок.СобытиеПрочее);
	СоответствиеТиповКартинкам.Вставить(Перечисления.ТипыСобытий.СообщениеSMS, БиблиотекаКартинок.СобытиеСообщениеSMS);
	СоответствиеТиповКартинкам.Вставить(Перечисления.ТипыСобытий.ТелефонныйЗвонок, БиблиотекаКартинок.СобытиеТелефонныйЗвонок);
	СоответствиеТиповКартинкам.Вставить(Перечисления.ТипыСобытий.ЭлектронноеПисьмо, БиблиотекаКартинок.СобытиеЭлектронноеПисьмо);
	СоответствиеТиповКартинкам.Вставить(Перечисления.ТипыСобытий.Запись, БиблиотекаКартинок.СобытиеЗапись);
	
	Возврат СоответствиеТиповКартинкам;
	
КонецФункции

Функция ЗаполнитьСтруктуруОбластейМакета()
	
	СтруктураОбластей = Новый Структура;
	
	Макет = УправлениеПечатью.МакетПечатнойФормы("Документ.Событие.ПФ_MXL_КарточкаСобытия");
	
	СтруктураОбластей.Вставить("Заголовок",				Макет.ПолучитьОбласть("Заголовок"));
	СтруктураОбластей.Вставить("Шапка",					Макет.ПолучитьОбласть("Шапка"));
	СтруктураОбластей.Вставить("Тема",					Макет.ПолучитьОбласть("Тема"));
	СтруктураОбластей.Вставить("Содержание",			Макет.ПолучитьОбласть("Содержание"));
	СтруктураОбластей.Вставить("ШапкаУчастники",		Макет.ПолучитьОбласть("ШапкаУчастники"));
	СтруктураОбластей.Вставить("СтрокаУчастники",		Макет.ПолучитьОбласть("СтрокаУчастники"));
	СтруктураОбластей.Вставить("Отступ",				Макет.ПолучитьОбласть("Отступ"));
	СтруктураОбластей.Вставить("ОтступСПодчеркиванием",	Макет.ПолучитьОбласть("ОтступСПодчеркиванием"));
	
	Возврат СтруктураОбластей;
	
КонецФункции

Функция ПолучитьОтветственногоИзДокумента(Документ)
	
	Ответственный = Неопределено;
	
	Если НЕ ОбщегоНазначения.ЭтоДокумент(Документ.Метаданные()) Тогда
		Возврат Ответственный;
	КонецЕсли;
	
	РеквизитОтветственный = Документ.Метаданные().Реквизиты.Найти("Ответственный");
	Если РеквизитОтветственный <> Неопределено
		И РеквизитОтветственный.Тип.СодержитТип(Тип("СправочникСсылка.Сотрудники")) Тогда
		Ответственный = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Документ, "Ответственный");
	КонецЕсли;
	
	Возврат Ответственный;
	
КонецФункции

Процедура ЗаполнитьПолучателейРассылкиПолучателямиПисьма(ДанныеРассылки, Событие)
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СобытиеУчастники.Контакт КАК Контакт,
	|	СобытиеУчастники.КакСвязаться КАК Email
	|ИЗ
	|	Документ.Событие.Участники КАК СобытиеУчастники
	|ГДЕ
	|	СобытиеУчастники.Ссылка = &Событие";
	Запрос.УстановитьПараметр("Событие", Событие);
	
	Результат = Запрос.Выполнить().Выбрать();
	Пока Результат.Следующий() Цикл
		ЗаполнитьЗначенияСвойств(ДанныеРассылки.Получатели.Добавить(), Результат);
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ИнтерфейсПечати

// Заполняет список команд печати.
// 
// Параметры:
//   КомандыПечати - ТаблицаЗначений - состав полей см. в функции УправлениеПечатью.СоздатьКоллекциюКомандПечати.
//
Процедура ДобавитьКомандыПечати(КомандыПечати) Экспорт
	
	КомандаПечати = КомандыПечати.Добавить();
	КомандаПечати.Идентификатор = "ПечатьСобытия";
	КомандаПечати.Представление = НСтр("ru = 'Письмо'");
	КомандаПечати.Порядок = 1;
	КомандаПечати.Обработчик = "ПечатьДокументовУНФКлиент.ПечатьСобытияПисьма";
	
КонецПроцедуры

Функция ПечатнаяФормаКарточкиСобытий(События) Экспорт
	
	ТабличныйДокумент = Новый ТабличныйДокумент;
	ТабличныйДокумент.КлючПараметровПечати = "ПараметрыПечати_КарточкиСобытий";
	ТабличныйДокумент.ИмяПараметровПечати = "ПАРАМЕТРЫ_ПЕЧАТИ_КарточкиСобытий";
	ТабличныйДокумент.ОриентацияСтраницы = ОриентацияСтраницы.Портрет;
	ТабличныйДокумент.АвтоМасштаб = Истина;
	
	СтруктураОбластей = ЗаполнитьСтруктуруОбластейМакета();
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
	|	Событие.Ссылка КАК Событие,
	|	Событие.ТипСобытия КАК ТипСобытия,
	|	Событие.Тема КАК Тема,
	|	Событие.Содержание КАК Содержание,
	|	Событие.НачалоСобытия КАК НачалоСобытия,
	|	Событие.ОкончаниеСобытия КАК ОкончаниеСобытия,
	|	Событие.Ответственный КАК Ответственный,
	|	Событие.Состояние.Цвет КАК ЦветСостояния,
	|	ВЫБОР
	|		КОГДА ТИПЗНАЧЕНИЯ(СобытиеУчастники.Контакт) = ТИП(Справочник.АдресатыПисем)
	|			ТОГДА ЕСТЬNULL(СвязиАдресатКонтактCRM.КонтактCRM, СобытиеУчастники.КакСвязаться)
	|		ИНАЧЕ ЕСТЬNULL(СобытиеУчастники.Контакт, ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка))
	|	КОНЕЦ КАК Контакт,
	|	СобытиеУчастники.КакСвязаться КАК КакСвязаться,
	|	ВЫБОР
	|		КОГДА ТИПЗНАЧЕНИЯ(СобытиеУчастники.Контакт) = ТИП(Справочник.АдресатыПисем)
	|			ТОГДА ЕСТЬNULL(ПРЕДСТАВЛЕНИЕ(СвязиАдресатКонтактCRM.КонтактCRM), СобытиеУчастники.КакСвязаться)
	|		ИНАЧЕ ЕСТЬNULL(ПРЕДСТАВЛЕНИЕ(СобытиеУчастники.Контакт), ЗНАЧЕНИЕ(Справочник.Контрагенты.ПустаяСсылка))
	|	КОНЕЦ КАК КонтактПредставление,
	|	ПРЕДСТАВЛЕНИЕ(Событие.Ссылка) КАК СобытиеПредставление,
	|	ПРЕДСТАВЛЕНИЕ(Событие.Состояние) КАК СостояниеПредставление,
	|	ПРЕДСТАВЛЕНИЕ(Событие.Тема) КАК ТемаПредставление,
	|	ПРЕДСТАВЛЕНИЕ(Событие.Ответственный) КАК ОтветственныйПредставление
	|ИЗ
	|	Документ.Событие КАК Событие
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.Событие.Участники КАК СобытиеУчастники
	|		ПО (СобытиеУчастники.Ссылка = Событие.Ссылка)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СвязиАдресатКонтактCRM КАК СвязиАдресатКонтактCRM
	|		ПО (СвязиАдресатКонтактCRM.Адресат = СобытиеУчастники.Контакт)
	|ГДЕ
	|	Событие.Ссылка В(&События)
	|
	|УПОРЯДОЧИТЬ ПО
	|	НачалоСобытия,
	|	ОкончаниеСобытия
	|ИТОГИ ПО
	|	Событие";
	
	Запрос.УстановитьПараметр("События", События);
	
	ВыборкаСобытия = Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	
	Пока ВыборкаСобытия.Следующий() Цикл
		
		ТабличныйДокумент.Вывести(СтруктураОбластей.Отступ);
		
		СтруктураОбластей.Заголовок.Параметры.Заполнить(ВыборкаСобытия);
		ТабличныйДокумент.Вывести(СтруктураОбластей.Заголовок);
		
		ВыборкаУчастники = ВыборкаСобытия.Выбрать();
		
		СтруктураОбластей.Шапка.Параметры.Заполнить(ВыборкаСобытия);
		СтруктураОбластей.Шапка.Параметры.Продолжительность = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = 'с %1 по %2'"),
			Формат(ВыборкаСобытия.НачалоСобытия, "ДЛФ=DT"),
			Формат(ВыборкаСобытия.ОкончаниеСобытия, "ДЛФ=DT"));
			
		ВыборкаУчастники.Следующий();
		
		Если ЗначениеЗаполнено(ВыборкаУчастники.КонтактПредставление) Тогда
			СтруктураОбластей.Шапка.Параметры.ОсновнойКонтактПредставление = ВыборкаУчастники.КонтактПредставление;
			СтруктураОбластей.Шапка.Параметры.ОсновнойКонтакт = ВыборкаУчастники.Контакт;
		КонецЕсли;
		
		ВыборкаУчастники.Сбросить();
		
		ЦветСостояния = ВыборкаСобытия.ЦветСостояния.Получить();
		Если ЦветСостояния <> Неопределено Тогда
			ОбластьСостояние = СтруктураОбластей.Шапка.Область("ОбластьСостояние");
			ОбластьСостояние.ЦветТекста = ЦветСостояния;
		КонецЕсли;
		
		ТабличныйДокумент.Вывести(СтруктураОбластей.Шапка);
		ТабличныйДокумент.Вывести(СтруктураОбластей.Отступ);
		
		СтруктураОбластей.Тема.Параметры.Заполнить(ВыборкаСобытия);
		ТабличныйДокумент.Вывести(СтруктураОбластей.Тема);
		ТабличныйДокумент.Вывести(СтруктураОбластей.Отступ);
		
		СтруктураОбластей.Содержание.Параметры.Заполнить(ВыборкаСобытия);
		ТабличныйДокумент.Вывести(СтруктураОбластей.Содержание);
		ТабличныйДокумент.Вывести(СтруктураОбластей.Отступ);
		
		ТабличныйДокумент.Вывести(СтруктураОбластей.ШапкаУчастники);
		
		Пока ВыборкаУчастники.Следующий() Цикл
			СтруктураОбластей.СтрокаУчастники.Параметры.Заполнить(ВыборкаУчастники);
			ТабличныйДокумент.Вывести(СтруктураОбластей.СтрокаУчастники);
		КонецЦикла;
		
		ТабличныйДокумент.Вывести(СтруктураОбластей.ОтступСПодчеркиванием);
		
	КонецЦикла;
	
	Возврат ТабличныйДокумент;
	
КонецФункции

#КонецОбласти

#Область ТекущиеДела

// Заполняет список текущих дел пользователя.
// Описание параметров процедуры см. в ТекущиеДелаСлужебный.НоваяТаблицаТекущихДел()
//
Процедура ПриЗаполненииСпискаТекущихДел(ТекущиеДела) Экспорт
	
	ГруппаДел	= НСтр("ru = 'События'");
	ИмяФормы	= "Документ.Событие.Форма.ФормаСписка";
	
	Если Не ПравоДоступа("Редактирование", Метаданные.Документы.Событие)
		Или ТекущиеДелаСервер.ДелоОтключено(ГруппаДел) Тогда
		
		Возврат;
	КонецЕсли;
	
	ОбщиеПараметрыЗапросов = ТекущиеДелаСлужебный.ОбщиеПараметрыЗапросов();
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ События.Ссылка) КАК СобытияВсегоВРаботе,
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ВЫБОР
	|			КОГДА События.НачалоСобытия <> ДАТАВРЕМЯ(1, 1, 1)
	|					И События.ОкончаниеСобытия < НАЧАЛОПЕРИОДА(&ТекущаяДата, ДЕНЬ)
	|				ТОГДА События.Ссылка
	|		КОНЕЦ) КАК СобытияПросроченоВыполнение,
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ВЫБОР
	|			КОГДА События.НачалоСобытия <= КОНЕЦПЕРИОДА(&ТекущаяДата, ДЕНЬ)
	|					И События.ОкончаниеСобытия >= НАЧАЛОПЕРИОДА(&ТекущаяДата, ДЕНЬ)
	|				ТОГДА События.Ссылка
	|		КОНЕЦ) КАК СобытияНаСегодня
	|ИЗ
	|	Документ.Событие КАК События
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ДанныеЗвонков КАК ДанныеЗвонков
	|		ПО (События.ТипСобытия = ЗНАЧЕНИЕ(Перечисление.ТипыСобытий.ТелефонныйЗвонок))
	|			И События.Ссылка = ДанныеЗвонков.Событие
	|ГДЕ
	|	События.Состояние <> ЗНАЧЕНИЕ(Справочник.СостоянияСобытий.Завершено)
	|	И События.Состояние <> ЗНАЧЕНИЕ(Справочник.СостоянияСобытий.Отменено)
	|	И ВЫБОР
	|			КОГДА ДанныеЗвонков.Сотрудник ЕСТЬ NULL
	|				ТОГДА События.Ответственный В (&СотрудникиПользователя)
	|			ИНАЧЕ ДанныеЗвонков.Сотрудник В (&СотрудникиПользователя)
	|		КОНЕЦ
	|	И НЕ События.ПометкаУдаления";
	
	ДанныеДел = ТекущиеДелаСлужебный.ЧисловыеПоказателиТекущихДел(Запрос, ОбщиеПараметрыЗапросов);
	
	Дело = ТекущиеДела.Добавить();
	Дело.Идентификатор	 = "СобытияПросроченоВыполнение";
	Дело.ЕстьДела		= ДанныеДел.СобытияПросроченоВыполнение > 0;
	Дело.Важное			= Истина;
	Дело.Представление	= НСтр("ru = 'Просроченные'");
	Дело.Количество		= ДанныеДел.СобытияПросроченоВыполнение;
	Дело.Форма			= ИмяФормы;
	Дело.ПараметрыФормы	= Новый Структура("КлючНазначенияИспользования,ТекущиеДела,ПросроченоВыполнение", "ПросроченоВыполнение");
	Дело.Владелец		= ГруппаДел;
	Дело.Подсказка		= "";
	
	Дело = ТекущиеДела.Добавить();
	Дело.Идентификатор	= "СобытияНаСегодня";
	Дело.ЕстьДела		= ДанныеДел.СобытияНаСегодня > 0;
	Дело.Важное			= Ложь;
	Дело.Представление	= НСтр("ru = 'На сегодня'");
	Дело.Количество		= ДанныеДел.СобытияНаСегодня;
	Дело.Форма			= ИмяФормы;
	Дело.ПараметрыФормы	= Новый Структура("КлючНазначенияИспользования,ТекущиеДела,НаСегодня", "НаСегодня");
	Дело.Владелец		= ГруппаДел;
	Дело.Подсказка		= "";
	
	Дело = ТекущиеДела.Добавить();
	Дело.Идентификатор	= "СобытияВсегоВРаботе";
	Дело.ЕстьДела		= ДанныеДел.СобытияВсегоВРаботе > 0;
	Дело.Важное			= Ложь;
	Дело.Представление	= НСтр("ru = 'В работе'");
	Дело.Количество		= ДанныеДел.СобытияВсегоВРаботе;
	Дело.Форма			= ИмяФормы;
	Дело.ПараметрыФормы	= Новый Структура("КлючНазначенияИспользования,ТекущиеДела,ВРаботе", "ВРаботе");
	Дело.Владелец		= ГруппаДел;
	Дело.Подсказка		= "";
	
КонецПроцедуры

#КонецОбласти

#Область ЭлектроннаяПочтаУНФ

Функция РассортированныеУчастникиПоТипу(Участники, УчетнаяЗапись, ВходящееИсходящее) Экспорт
	
	Результат = Новый Структура;
	
	Кому = Новый Массив;
	Копии = Новый Массив;
	ОтКого = Неопределено;
	
	Для Каждого Участник Из Участники Цикл
		ОбработатьОдногоУчастникаДляСортировки(Участник, ОтКого, Кому, Копии);
	КонецЦикла;
	
	Если ОтКого = Неопределено Тогда
		ОтКого = УчетнаяЗапись;
	КонецЕсли;
	
	Если ВходящееИсходящее = Перечисления.ВходящееИсходящееСобытие.Входящее Тогда
		
		Если Не ЗначениеВМассиве(УчетнаяЗапись, Кому) И Не ЗначениеВМассиве(УчетнаяЗапись, Копии) Тогда
			Кому.Вставить(0, УчетнаяЗапись);
		КонецЕсли;
		
	КонецЕсли;
	
	Результат.Вставить("Кому", Кому);
	Результат.Вставить("Копии", Копии);
	Результат.Вставить("ОтКого", ОтКого);
	
	Возврат Результат;
	
КонецФункции

Процедура ОбработатьОдногоУчастникаДляСортировки(Участник, ОтКого, Кому, Копии)
	
	ТипПолучателяЭлектронногоПисьма = Участник.ТипПолучателяЭлектронногоПисьма;
	
	Если ТипПолучателяЭлектронногоПисьма = Перечисления.ТипыПолучателейЭлектронногоПисьма.ОтКого Тогда
		
		Если ОтКого = Неопределено Тогда
			
			ОтКого = Участник;
			
		Иначе
			
			ВажностьОтКого = ЭлектроннаяПочтаУНФ.ВажностьОтображенияКонтакта(ОтКого.Ссылка);
			ВажностьДубляОтправителя = ЭлектроннаяПочтаУНФ.ВажностьОтображенияКонтакта(Участник.Ссылка);
			Если ВажностьДубляОтправителя > ВажностьОтКого Тогда
				ОтКого = Участник;
			КонецЕсли;
			
		КонецЕсли;
		
	ИначеЕсли ТипПолучателяЭлектронногоПисьма = Перечисления.ТипыПолучателейЭлектронногоПисьма.Кому Тогда
		Кому.Добавить(Участник);
	ИначеЕсли ТипПолучателяЭлектронногоПисьма = Перечисления.ТипыПолучателейЭлектронногоПисьма.Копия Тогда
		Копии.Добавить(Участник);
	Иначе
		
		ЭтоСозданныйКонтактВРеквизитахСобытий =
			ТипПолучателяЭлектронногоПисьма = Перечисления.ТипыПолучателейЭлектронногоПисьма.ПустаяСсылка()
			И ТипЗнч(Участник.КонтактВПисьме) <> Тип("Строка")
			И Не Участник.КонтактВПисьме.Пустая();
		Если ЭтоСозданныйКонтактВРеквизитахСобытий Тогда
			Кому.Добавить(Участник);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ЗначениеВМассиве(Значение, Массив)
	
	Для Каждого Элемент Из Массив Цикл
		Если Элемент = Значение Тогда
			Возврат Истина;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ложь;
	
КонецФункции

// Данные о вложениях события
//
// Параметры:
//  Событие - ДокументСсылка.Событие - ссылка на событие
//  УникальныйИдентификатор - УникальныйИдентификатор - идентификатор формы
// 
// Возвращаемое значение:
//  Массив из Структура:
//  * Ссылка - СправочникСсылка.СобытиеПрисоединенныеФайлы - ссылка на файл
//  * Идентификатор - Строка - идентификатор файла
//  * ИндексКартинки - Число - индекс иконки расширения файла
//  * Данные - ДвоичныеДанные, Неопределено - данные файла; если Неопределено, значит данные не были найдены
//  * Письмо - ДокументСсылка.Событие - ссылка на вложенное письмо, если такое есть
//  * ЭтоВложениеЭлектронногоПисьма - Булево
//  * Представление - Строка - представление файла
//  * АдресВоВременномХранилище - Строка, Неопределено - адрес с данными файла; Неопределено, если данных нет
//
Функция ВложенияСобытия(Событие, УникальныйИдентификатор) Экспорт
	
	Результат = Новый Массив;
	Если Событие.Пустая() Тогда
		Возврат Результат;
	КонецЕсли;
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	СобытиеПрисоединенныеФайлы.Ссылка КАК Ссылка,
	|	СобытиеПрисоединенныеФайлы.Наименование КАК Наименование,
	|	СобытиеПрисоединенныеФайлы.Расширение КАК Расширение,
	|	СобытиеПрисоединенныеФайлы.ИндексКартинки КАК ИндексКартинки,
	|	СобытиеПрисоединенныеФайлы.ИДФайлаЭлектронногоПисьма КАК ИДФайлаЭлектронногоПисьма,
	|	СобытиеПрисоединенныеФайлы.ЭтоВложениеЭлектронногоПисьма КАК ЭтоВложениеЭлектронногоПисьма,
	|	ВложенныеПисьма.Письмо КАК Письмо
	|ИЗ
	|	РегистрСведений.ВложенныеПисьма КАК ВложенныеПисьма
	|		ПРАВОЕ СОЕДИНЕНИЕ Справочник.СобытиеПрисоединенныеФайлы КАК СобытиеПрисоединенныеФайлы
	|		ПО (ВложенныеПисьма.ВложенноеПисьмо = СобытиеПрисоединенныеФайлы.Ссылка)
	|ГДЕ
	|	СобытиеПрисоединенныеФайлы.ВладелецФайла = &ВладелецФайла
	|	И СобытиеПрисоединенныеФайлы.ПометкаУдаления = ЛОЖЬ");
	Запрос.УстановитьПараметр("ВладелецФайла", Событие);
	
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Результат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.Следующий() Цикл
		
		ОписаниеВложения = Новый Структура;
		ОписаниеВложения.Вставить("Ссылка", Выборка.Ссылка);
		ОписаниеВложения.Вставить("Идентификатор", Выборка.ИДФайлаЭлектронногоПисьма);
		ОписаниеВложения.Вставить("ИндексКартинки", Выборка.ИндексКартинки);
		ОписаниеВложения.Вставить("Данные", РаботаСФайлами.ДвоичныеДанныеФайла(Выборка.Ссылка, Ложь));
		ОписаниеВложения.Вставить("Письмо", Выборка.Письмо);
		ОписаниеВложения.Вставить("ЭтоВложениеЭлектронногоПисьма", Выборка.ЭтоВложениеЭлектронногоПисьма);
		
		ПредставлениеВложения = Выборка.Наименование + ?(ПустаяСтрока(Выборка.Расширение), "", "." + Выборка.Расширение);
		ОписаниеВложения.Вставить("Представление", ПредставлениеВложения);
		
		Если ОписаниеВложения.Данные <> Неопределено Тогда
			АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(ОписаниеВложения.Данные, УникальныйИдентификатор);
			ОписаниеВложения.Вставить("АдресВоВременномХранилище", АдресВоВременномХранилище);
		Иначе
			ОписаниеВложения.Вставить("АдресВоВременномХранилище", Неопределено);
		КонецЕсли;
		
		Результат.Добавить(ОписаниеВложения);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТемаИСодержание(Тема, Содержание, СодержаниеHTML) Экспорт
	
	КомпонентыПредставления = Новый Массив;
	Если ЗначениеЗаполнено(Тема) Тогда
		КомпонентыПредставления.Добавить(Тема);
	КонецЕсли;
	
	СодержаниеВОднуСтроку = СодержаниеВОднуСтроку(Содержание, СодержаниеHTML, 150);
	
	Если ЗначениеЗаполнено(СодержаниеВОднуСтроку) Тогда
		КомпонентыПредставления.Добавить(СодержаниеВОднуСтроку);
	КонецЕсли;
	
	Возврат СтрСоединить(КомпонентыПредставления, " — ");
	
КонецФункции

Функция НовыеДанныеСобытия() Экспорт
	
	ДанныеСобытия = Новый Структура;
	ДанныеСобытия.Вставить("Тема");
	ДанныеСобытия.Вставить("Содержание");
	ДанныеСобытия.Вставить("СодержаниеHTML");
	
	Возврат ДанныеСобытия;
	
КонецФункции

Процедура ЗаполнитьТекстыИзПочтовогоСообщения(ДанныеСобытия, ИнтернетПочтовоеСообщение) Экспорт
	
	ДанныеСобытия.Тема = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыXML(ИнтернетПочтовоеСообщение.Тема);
	
	Для Каждого ТекТекст Из ИнтернетПочтовоеСообщение.Тексты Цикл
		
		Если ТекТекст.ТипТекста = ТипТекстаПочтовогоСообщения.ПростойТекст Тогда
			
			Если СтрДлина(ТекТекст.Текст) > СтрДлина(ДанныеСобытия.Содержание) Тогда
				ДанныеСобытия.Содержание = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыXML(ТекТекст.Текст);
			КонецЕсли;
			
		ИначеЕсли ТекТекст.ТипТекста = ТипТекстаПочтовогоСообщения.HTML Тогда
			
			Если СтрДлина(ТекТекст.Текст) > СтрДлина(ДанныеСобытия.СодержаниеHTML) Тогда
				ДанныеСобытия.СодержаниеHTML = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыXML(Гипертекст.ОбработатьТекстHTML(ТекТекст.Текст));
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

// Процедура - Заполнить вложения из почтового сообщения
//
// Параметры:
//  ДанныеСобытия				 - Структура - к этой структуре добавляется свойство "Вложения"
//                                             если в ИнтернетПочтовоеСообщение есть вложения,
//  ИнтернетПочтовоеСообщение	 - ИнтернетПочтовоеСообщение - почтовое сообщение.
//
Процедура ЗаполнитьВложенияИзПочтовогоСообщения(ДанныеСобытия, ИнтернетПочтовоеСообщение) Экспорт
	
	Если Не ЗначениеЗаполнено(ИнтернетПочтовоеСообщение.Вложения) Тогда
		Возврат;
	КонецЕсли;
	
	ДанныеСобытия.Вставить("Вложения", Новый Массив);
	
	Для Каждого ИнтернетПочтовоеСообщениеВложение Из ИнтернетПочтовоеСообщение.Вложения Цикл
		НовоеВложение = Новый Структура;
		НовоеВложение.Вставить("ИмяФайла",
			ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыXML(ИнтернетПочтовоеСообщениеВложение.ИмяФайла));
		НовоеВложение.Вставить("Данные", ИнтернетПочтовоеСообщениеВложение.Данные);
		НовоеВложение.Вставить("Идентификатор", ИнтернетПочтовоеСообщениеВложение.Идентификатор);
		ДанныеСобытия.Вложения.Добавить(НовоеВложение);
	КонецЦикла;
	
КонецПроцедуры

Функция ТемаСПрефиксомОтвета(Тема, Команда) Экспорт
	
	Если Команда = ЭлектроннаяПочтаУНФКлиентСервер.КомандаОтветить() Тогда
		
		Если СтрНачинаетсяС(ВРег(Тема), "RE:") Тогда
			Возврат Тема;
		КонецЕсли;
		
		Возврат СтрШаблон("Re: %1", Тема);
		
	ИначеЕсли Команда = ЭлектроннаяПочтаУНФКлиентСервер.КомандаПереслать() Тогда
		
		Если СтрНачинаетсяС(ВРег(Тема), "Fw:") Тогда
			Возврат Тема;
		КонецЕсли;
		
		Возврат СтрШаблон("Fw: %1", Тема);
		
	Иначе
		
		Возврат Тема;
		
	КонецЕсли;
	
КонецФункции

Функция ПроизошлаОшибка(Прогресс) Экспорт
	
	Если ТипЗнч(Прогресс) <> Тип("Структура") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Не Прогресс.Свойство("ДополнительныеПараметры") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если Не Прогресс.ДополнительныеПараметры.Свойство("ПроизошлаОшибка") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Прогресс.ДополнительныеПараметры.ПроизошлаОшибка;
	
КонецФункции

Функция СодержаниеВОднуСтроку(Содержание, СодержаниеHTML, МаксДлина)
	
	Результат = "";
	
	Если ЗначениеЗаполнено(Содержание) Тогда
		Если СтрНайти(НРег(Содержание), "<html") Тогда
			Буфер = Гипертекст.ПолучитьТекстИзHTML(Содержание);
		Иначе
			Буфер = Содержание;
		КонецЕсли;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Буфер) Тогда
		Буфер = Гипертекст.ПолучитьТекстИзHTML(СодержаниеHTML);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Буфер) Тогда
		Возврат "";
	КонецЕсли;
	
	Для ИндексСтроки = 1 По СтрЧислоСтрок(Буфер) Цикл
		
		Если СтрДлина(Результат) > МаксДлина Тогда
			Прервать;
		КонецЕсли;
		
		СтрокаПодстановки = СокрЛ(СтрПолучитьСтроку(Буфер, ИндексСтроки));
		
		Результат = СокрЛП(
		СтрШаблон("%1 %2",
		Результат,
		СтрокаПодстановки));
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Функция - Форматированные вложения после загрузки письма
//
// Параметры:
//  Вложения - Массив из Структура:
//   * ИмяФайла - Строка,
//   * Данные - ДвоичныеДанные,
//   * Идентификатор - Строка.
//  УникальныйИдентификатор - УникальныйИдентификатор - идентификатор формы
// 
// Возвращаемое значение:
//  ТаблицаЗначений:
//   * ЭтоВложениеЭлектронногоПисьма - Булево,
//   * Идентификатор - Строка,
//   * Представление - Строка,
//   * АдресВоВременномХранилище - Строка,
//   * ИндексКартинки - Число,
//   * Письмо - ДокументСсылка.Событие,
//   * Ссылка - СправочникСсылка.СобытиеПрисоединенныеФайлы.
//
Функция ФорматированныеВложенияПослеЗагрузкиПисьма(Вложения, УникальныйИдентификатор) Экспорт
	
	ВложенияТЗ = Новый ТаблицаЗначений;
	
	ТипСтрока = Новый ОписаниеТипов("Строка");
	ВложенияТЗ.Колонки.Добавить("ЭтоВложениеЭлектронногоПисьма", Новый ОписаниеТипов("Булево"));
	ВложенияТЗ.Колонки.Добавить("Идентификатор", ТипСтрока);
	ВложенияТЗ.Колонки.Добавить("Представление", ТипСтрока);
	ВложенияТЗ.Колонки.Добавить("АдресВоВременномХранилище", ТипСтрока);
	ВложенияТЗ.Колонки.Добавить("ИндексКартинки", Новый ОписаниеТипов("Число"));
	ВложенияТЗ.Колонки.Добавить("Письмо", Новый ОписаниеТипов("ДокументСсылка.Событие"));
	ВложенияТЗ.Колонки.Добавить("Ссылка", Новый ОписаниеТипов("СправочникСсылка.СобытиеПрисоединенныеФайлы"));
	
	Для Каждого ТекВложение Из Вложения Цикл
		
		ОписаниеВложения = ВложенияТЗ.Добавить();
		ОписаниеВложения.ЭтоВложениеЭлектронногоПисьма = Истина;
		ОписаниеВложения.Идентификатор = ТекВложение.Идентификатор;
		
		Если ТипЗнч(ТекВложение.Данные) = Тип("ДвоичныеДанные") Тогда
			
			ИмяФайла = Неопределено;
			Если Не ТекВложение.Свойство("Имя", ИмяФайла) Или ИмяФайла = Неопределено Тогда
				ТекВложение.Свойство("ИмяФайла", ИмяФайла);
			КонецЕсли;
			
			Если ТекВложение.Свойство("ЭтоВложениеПисьмаВложения") Тогда
				СтруктураДанныхФайла = ЭлектроннаяПочтаУНФ.ТипСодержимогоИРасширениеПоMIMEТипу(ТекВложение.ТипСодержимого);
				
				Расширение = ОбщегоНазначенияКлиентСервер.ПолучитьРасширениеИмениФайла(ИмяФайла);
				Если Не ЗначениеЗаполнено(Расширение) Тогда
					ИмяФайла = СтрШаблон("%1.%2", ИмяФайла, СтруктураДанныхФайла.Расширение);
				КонецЕсли;
				
			КонецЕсли;
			
			ОписаниеВложения.Представление = ИмяФайла;
			ОписаниеВложения.АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(ТекВложение.Данные, УникальныйИдентификатор);
			ЧастиИмениФайла = СтрРазделить(ОписаниеВложения.Представление, ".");
			Если ЗначениеЗаполнено(ЧастиИмениФайла) Тогда
				РасширениеБезТочки = ЧастиИмениФайла[ЧастиИмениФайла.ВГраница()];
				ОписаниеВложения.ИндексКартинки = РаботаСФайламиСлужебныйКлиентСервер.ИндексПиктограммыФайла(РасширениеБезТочки);
			КонецЕсли;
			
		ИначеЕсли ТипЗнч(ТекВложение.Данные) = Тип("ИнтернетПочтовоеСообщение") Тогда
			
			Наименование = ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыВИмениФайла(ТекВложение.Данные.Тема);
			
			Если ТекВложение.Свойство("Имя") Тогда
				ОписаниеВложения.Представление = ТекВложение.Имя;
			Иначе
				ОписаниеВложения.Представление = СтрШаблон("%1.eml", Наименование);
			КонецЕсли;
			
			ОписаниеВложения.ИндексКартинки = РаботаСФайламиСлужебныйКлиентСервер.ИндексПиктограммыФайла("eml");
			ОписаниеВложения.АдресВоВременномХранилище = ПоместитьВоВременноеХранилище(
			ТекВложение.Данные.ПолучитьИсходныеДанные(),
			УникальныйИдентификатор);
			
			Если ТекВложение.Свойство("Письмо") Тогда
				ОписаниеВложения.Письмо = ТекВложение.Письмо;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат ВложенияТЗ;
	
КонецФункции

#КонецОбласти

#Область АссистентУправления

#Область АссистентУправления_Интеграция

Процедура ПриОпределенииТиповПредметовСобытий(Предметы) Экспорт
	
	Предметы.Вставить("ЭлектронноеПисьмо", НСтр("ru='Электронное письмо'"));
	
КонецПроцедуры

Процедура ПриОпределенииСобытий(События, Предмет) Экспорт
	
КонецПроцедуры

Процедура ПриОпределенииДействий(Действия) Экспорт
	
	// 1.
	НовоеДействие = Действия.Добавить();
	НовоеДействие.Идентификатор = "СоздатьОтправитьЭлектронноеПисьмо";
	НовоеДействие.Представление = НСтр("ru='Отправить email'");
	НовоеДействие.Общее = Истина;
	
	АссистентУправления.ДобавитьОписаниеПолейДействия(НовоеДействие);
	
	НовоеПоле = НовоеДействие.ОписаниеПолей.Добавить();
	НовоеПоле.Имя           = "УчетнаяЗапись";
	НовоеПоле.Представление = НСтр("ru='Учетная запись'");
	НовоеПоле.ОписаниеТипов = Новый ОписаниеТипов("СправочникСсылка.УчетныеЗаписиЭлектроннойПочты");
	НовоеПоле.ОбязательноеЗаполнение = Истина;
	НовоеПоле.ВариантЗаполнения = "Указанный";
	
	НовоеПоле = НовоеДействие.ОписаниеПолей.Добавить();
	НовоеПоле.Имя           = "Получатель";
	НовоеПоле.Представление = НСтр("ru='Кому'");
	НовоеПоле.ОписаниеТипов = Новый ОписаниеТипов("Строка");
	НовоеПоле.ОбязательноеЗаполнение = Истина;
	НовоеПоле.ВариантЗаполнения = "Указанный";
	
	НовоеПоле = НовоеДействие.ОписаниеПолей.Добавить();
	НовоеПоле.Имя           = "ШаблонСообщения";
	НовоеПоле.Представление = НСтр("ru='Шаблон сообщения'");
	НовоеПоле.ОписаниеТипов = Новый ОписаниеТипов("СправочникСсылка.ШаблоныСообщений");
	НовоеПоле.ОбязательноеЗаполнение = Ложь;
	НовоеПоле.ВариантЗаполнения = "Указанный";
	
	// 2.
	НовоеДействие = Действия.Добавить();
	НовоеДействие.Идентификатор = "СоздатьОтправитьSMS";
	НовоеДействие.Представление = НСтр("ru='Отправить SMS'");
	НовоеДействие.Общее = Истина;
	
	АссистентУправления.ДобавитьОписаниеПолейДействия(НовоеДействие);
	НовоеПоле = НовоеДействие.ОписаниеПолей.Добавить();
	НовоеПоле.Имя           = "Получатель";
	НовоеПоле.Представление = НСтр("ru='Кому'");
	НовоеПоле.ОписаниеТипов = Новый ОписаниеТипов("Строка");
	НовоеПоле.ОбязательноеЗаполнение = Истина;
	НовоеПоле.ВариантЗаполнения = "Указанный";
	
	НовоеПоле = НовоеДействие.ОписаниеПолей.Добавить();
	НовоеПоле.Имя           = "ШаблонСообщения";
	НовоеПоле.Представление = НСтр("ru='Шаблон сообщения'");
	НовоеПоле.ОписаниеТипов = Новый ОписаниеТипов("СправочникСсылка.ШаблоныСообщений");
	НовоеПоле.ОбязательноеЗаполнение = Ложь;
	НовоеПоле.ВариантЗаполнения = "Указанный";
	
КонецПроцедуры

Процедура ПриОпределенииУсловий(Условия) Экспорт
	
	
	
КонецПроцедуры

Функция ВыполненыУсловия(Предмет, Условия) Экспорт
	
КонецФункции

Процедура ПередОбработкойСобытия(Предмет, Событие, Действие, ЗначенияЗаполнения, ДополнительныеПараметры, Результат) Экспорт
	
	
	
КонецПроцедуры

Процедура ВыполнитьДействие(Предмет, Действие, ЗначенияЗаполнения, ДополнительныеПараметры, Результат) Экспорт
	
	ТипПредмета = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДополнительныеПараметры.ЗадачаАссистентаУправления, "ТипПредмета");
	
	Если Действие.Идентификатор = "СоздатьОтправитьЭлектронноеПисьмо" Тогда
		
		АссистентУправления.ПроверитьНаличиеОбязательныхПараметров(ЗначенияЗаполнения, "Получатель,ШаблонСообщения,УчетнаяЗапись");
		
		Ответственный = Неопределено;
		Если ДополнительныеПараметры.Источник <> Неопределено Тогда
			Ответственный = ПолучитьОтветственногоИзДокумента(ДополнительныеПараметры.Источник);
		КонецЕсли;
		Если Ответственный = Неопределено И ЗначенияЗаполнения.Свойство("Ответственный") Тогда
			Ответственный = ЗначенияЗаполнения.Ответственный;
		КонецЕсли;
		
		ПараметрыСобытия = Новый Структура;
		ПараметрыСобытия.Вставить("Ответственный", Ответственный);
		ПараметрыСобытия.Вставить("УчетнаяЗапись", ЗначенияЗаполнения.УчетнаяЗапись);
		ПараметрыСобытия.Вставить("ОснованиеЗаполнения", Предмет);
		ПараметрыСобытия.Вставить("ВыбранныйПолучатель", Новый Структура);
		ПараметрыСобытия.ВыбранныйПолучатель.Вставить("ВыбранныйПолучатель", ЗначенияЗаполнения.Получатель);
		Если ЗначенияЗаполнения.Свойство("ВидКИ") Тогда
			ПараметрыСобытия.ВыбранныйПолучатель.Вставить("ВыбранныйВидКИ", ЗначенияЗаполнения.ВидКИ);
		КонецЕсли;
		
		ШаблонСообщения = Неопределено;
		ЗначенияЗаполнения.Свойство("ШаблонСообщения", ШаблонСообщения);
		
		ДополнительныеПараметры.ДополнительныеПараметрыСобытия.Вставить("ЭтоЗаписьАссистентом", Истина);
		ДополнительныеПараметры.ДополнительныеПараметрыСобытия.Вставить("ЗадачаАссистентаУправления", ДополнительныеПараметры.ЗадачаАссистентаУправления);
		Если ДополнительныеПараметры.Источник <> Неопределено 
			И ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДополнительныеПараметры.Источник, "Ссылка") Тогда
			ПараметрыСобытия.Вставить("ДокументОснование", ДополнительныеПараметры.Источник.Ссылка);
		КонецЕсли;
		
		РезультатСозданияПисьма = СоздатьОтправитьЭлектронноеПисьмо(ПараметрыСобытия, ШаблонСообщения, ДополнительныеПараметры.ДополнительныеПараметрыСобытия);
		
	ИначеЕсли Действие.Идентификатор = "СоздатьОтправитьSMS" Тогда
		
		АссистентУправления.ПроверитьНаличиеОбязательныхПараметров(ЗначенияЗаполнения, "Получатель,ШаблонСообщения");
		
		Ответственный = Неопределено;
		Если ДополнительныеПараметры.Источник <> Неопределено Тогда
			Ответственный = ПолучитьОтветственногоИзДокумента(ДополнительныеПараметры.Источник);
		КонецЕсли;
		Если Ответственный = Неопределено И ЗначенияЗаполнения.Свойство("Ответственный") Тогда
			Ответственный = ЗначенияЗаполнения.Ответственный;
		КонецЕсли;
		
		ПараметрыСобытия = Новый Структура;
		ПараметрыСобытия.Вставить("Ответственный", Ответственный);
		ПараметрыСобытия.Вставить("ОснованиеЗаполнения", Предмет);
		ПараметрыСобытия.Вставить("ВыбранныйПолучатель", Новый Структура);
		ПараметрыСобытия.ВыбранныйПолучатель.Вставить("ВыбранныйПолучатель", ЗначенияЗаполнения.Получатель);
		Если ЗначенияЗаполнения.Свойство("ВидКИ") Тогда
			ПараметрыСобытия.ВыбранныйПолучатель.Вставить("ВыбранныйВидКИ", ЗначенияЗаполнения.ВидКИ);
		КонецЕсли;
		
		ШаблонСообщения = Неопределено;
		ЗначенияЗаполнения.Свойство("ШаблонСообщения", ШаблонСообщения);
		
		ДополнительныеПараметры.ДополнительныеПараметрыСобытия.Вставить("ЭтоЗаписьАссистентом", Истина);
		ДополнительныеПараметры.ДополнительныеПараметрыСобытия.Вставить("ЗадачаАссистентаУправления", ДополнительныеПараметры.ЗадачаАссистентаУправления);
		Если ДополнительныеПараметры.Источник <> Неопределено 
			И ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ДополнительныеПараметры.Источник, "Ссылка") Тогда
			ПараметрыСобытия.Вставить("ДокументОснование", ДополнительныеПараметры.Источник.Ссылка);
		КонецЕсли;
		
		РезультатСозданияПисьма = СоздатьОтправитьSMS(ПараметрыСобытия, ШаблонСообщения, ДополнительныеПараметры.ДополнительныеПараметрыСобытия);
		
	КонецЕсли;
	
	Результат.Успешно = Истина;
	Результат.Вставить("Документ", РезультатСозданияПисьма.Событие);
	
	Если ЗначениеЗаполнено(РезультатСозданияПисьма.Событие) И ЗначениеЗаполнено(РезультатСозданияПисьма.ТекстОшибки) Тогда
		ДанныеСообщения = ОбсужденияУНФ.НовыйДанныеСообщения();
		ДанныеСообщения.Объект = РезультатСозданияПисьма.Событие;
		ДанныеСообщения.Текст = РезультатСозданияПисьма.ТекстОшибки;
		ДанныеСообщения.Дата = ТекущаяДатаСеанса();
		ДанныеСообщения.Автор = АссистентУправления.ПользовательАссистент();
		ДанныеСообщения.Получатель = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(РезультатСозданияПисьма.Событие, "Ответственный");
		ОбсужденияУНФ.СоздатьСообщениеОтложенно(ДанныеСообщения);
	КонецЕсли;
	
КонецПроцедуры

Процедура ПослеОбработкиСобытия(Предмет, Событие, Действие, ЗначенияЗаполнения, ДополнительныеПараметры, Результат) Экспорт
	
КонецПроцедуры

#КонецОбласти

Процедура ОтправитьЭлектронноеПисьмоБезДокументаСобытие( ЗначенияЗаполнения, ШаблонСообщения = Неопределено, ДополнительныеПараметры = Неопределено )

	ПредметСообщения = ЗначенияЗаполнения.ОснованиеЗаполнения;
	СообщениеПоШаблону = ШаблоныСообщений.СформироватьСообщение(ШаблонСообщения, ПредметСообщения, Новый УникальныйИдентификатор, ДополнительныеПараметры);

	Содержание = Гипертекст.ПолучитьТекстИзHTML(СообщениеПоШаблону.Текст);
	СодержаниеHTML = СообщениеПоШаблону.Текст;
	ТекстПисьмаСКартинками = СодержаниеHTML;

	ПочтовыеАдресаПолучателейДляОтправкиПисьма = Новый Массив;
	ПочтовыеАдресаПоиск = Новый Соответствие;
	Для Каждого Получатель Из СообщениеПоШаблону.Получатель Цикл
	
		Попытка
			ПриведенныеПочтовыеАдреса = ОбщегоНазначенияКлиентСервер.РазобратьСтрокуСПочтовымиАдресами(Получатель.Адрес);
		Исключение
			ВызватьИсключение НСтр("ru='Некорректный адрес получателя для отправляемого письма'") + ":"
			+ Символы.ПС + КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
		КонецПопытки;
	    
	    Для Каждого ПриведенныйПочтовыйАдрес Из ПриведенныеПочтовыеАдреса Цикл
	    	
	    	Если ПочтовыеАдресаПоиск.Получить( ПриведенныйПочтовыйАдрес.Адрес ) = Истина Тогда
	    		Продолжить;
	    	КонецЕсли;
	    	
			ПочтовыеАдресаПолучателейДляОтправкиПисьма.Добавить( ПриведенныйПочтовыйАдрес );
			ПочтовыеАдресаПоиск.Вставить( ПриведенныйПочтовыйАдрес.Адрес, Истина );
			
		КонецЦикла;
	КонецЦикла;

	ПараметрыПисьма = Новый Структура;
	
	Если ПочтовыеАдресаПолучателейДляОтправкиПисьма.Количество() > 0 Тогда
		ПараметрыПисьма.Вставить( "Кому", ПочтовыеАдресаПолучателейДляОтправкиПисьма );
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СообщениеПоШаблону.Тема) Тогда
		ПараметрыПисьма.Вставить( "Тема", Строка(СообщениеПоШаблону.Тема) );
	КонецЕсли;
	
	АдресПолучателяСкрытойКопии = УправлениеНебольшойФирмойВзаимодействия.ПолучитьАдресПолучателяСкрытойКопии();
	Если ЗначениеЗаполнено(АдресПолучателяСкрытойКопии) Тогда
		ПараметрыПисьма.Вставить( "СкрытыеКопии", АдресПолучателяСкрытойКопии );
	КонецЕсли;
	
	ВложенияПисьма = Новый Соответствие;
	
	Если СообщениеПоШаблону.Вложения.Количество() > 0 Тогда
		
		Для Каждого Вложение Из СообщениеПоШаблону.Вложения Цикл

			Если ЗначениеЗаполнено( Вложение.Идентификатор ) Тогда

				ВложениеКартинка = Новый Структура( "ДвоичныеДанные, Идентификатор" );
				КартинкаОбъект = Новый Картинка( ПолучитьИзВременногоХранилища( Вложение.АдресВоВременномХранилище ) );
				ВложениеКартинка.ДвоичныеДанные = КартинкаОбъект.ПолучитьДвоичныеДанные();
				ВложениеКартинка.Идентификатор = Вложение.Идентификатор;
				
				ВложенияПисьма.Вставить( Вложение.Представление, ВложениеКартинка );
				
			Иначе
			
				ВложенияПисьма.Вставить( Вложение.Представление, Вложение.АдресВоВременномХранилище );
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	ПараметрыПисьма.Вставить( "Тело", ТекстПисьмаСКартинками);
	ПараметрыПисьма.Вставить( "Вложения", ВложенияПисьма);
	ПараметрыПисьма.Вставить( "ТипТекста", "HTML");
	
	УчетнаяЗапись = ЗначенияЗаполнения.УчетнаяЗапись;
	
	Попытка
		
		РезультатОтправки = ЭлектроннаяПочтаУНФ.ОтправитьПочтовоеСообщение( ПараметрыПисьма, УчетнаяЗапись);
		
	Исключение
	
		ТекстОшибкиШаблон = НСтр("ru='Не удалось выполнить отправку письма по причине:
		|%1'");
		
		ПричинаОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ТекстОшибки = СтрШаблон(ТекстОшибкиШаблон, ПричинаОшибки);
		
		ВызватьИсключение ТекстОшибки;
		
	КонецПопытки;
	
КонецПроцедуры

Функция СоздатьОтправитьЭлектронноеПисьмо(ЗначенияЗаполнения, ШаблонСообщения = Неопределено, ДополнительныеПараметры = Неопределено)
	
	Результат = Новый Структура;
	Результат.Вставить("Событие", Документы.Событие.ПустаяСсылка());
	Результат.Вставить("ТекстОшибки", "");

	Если НЕ ПолучитьФункциональнуюОпцию( "ИспользоватьСобытия" ) Тогда
	
		Если НЕ РегламентныеЗаданияСервер.РаботаСВнешнимиРесурсамиЗаблокирована() Тогда
			ОтправитьЭлектронноеПисьмоБезДокументаСобытие( ЗначенияЗаполнения, ШаблонСообщения, ДополнительныеПараметры );
		КонецЕсли;
		
		Возврат Результат;
	КонецЕсли;
	
	Если НЕ ЗначенияЗаполнения.Свойство("ТипСобытия") Тогда
		ЗначенияЗаполнения.Вставить("ТипСобытия", Перечисления.ТипыСобытий.ЭлектронноеПисьмо);
	КонецЕсли;
	
	Если НЕ ЗначенияЗаполнения.Свойство("Автор") Тогда
		ЗначенияЗаполнения.Вставить("Автор", Пользователи.АвторизованныйПользователь());
	КонецЕсли;
	
	СобытиеОбъект = Документы.Событие.СоздатьДокумент();
	СобытиеОбъект.Заполнить(ЗначенияЗаполнения);
	СобытиеОбъект.Дата = ТекущаяДатаСеанса();
		
	Если ШаблонСообщения <> Неопределено Тогда
		Если ДополнительныеПараметры = Неопределено Тогда
			ДополнительныеПараметры = Новый Структура;
		КонецЕсли;
		
		Если Не ДополнительныеПараметры.Свойство("ПараметрыСообщения") Тогда
			ДополнительныеПараметры.Вставить("ПараметрыСообщения", Новый Структура);
		КонецЕсли;
		
		Если ЗначенияЗаполнения.Свойство("ВыбранныйПолучатель") Тогда
			ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(ДополнительныеПараметры.ПараметрыСообщения, ЗначенияЗаполнения.ВыбранныйПолучатель, Истина);
		КонецЕсли;
		
		СобытиеОбъект.ЗаполнитьПоШаблону(ШаблонСообщения, ЗначенияЗаполнения.ОснованиеЗаполнения, ДополнительныеПараметры);
	КонецЕсли;
	
	Если НЕ РегламентныеЗаданияСервер.РаботаСВнешнимиРесурсамиЗаблокирована() Тогда
		Попытка
			СобытиеОбъект.ОтправитьЭлектронноеПисьмо(Ложь);
		Исключение
			Результат.ТекстОшибки = СтрШаблон(НСтр("ru='Не удалось автоматически отправить электронное письмо, требуется отправка из формы События.
				|По причине: %1'"), ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		КонецПопытки;
	Иначе
		Результат.ТекстОшибки = НСтр("ru='Автоматическая отправка письма невозможна, т.к. в базе установлена блокировка работы с внешними ресурсами.
			|Для разблокирования перейдите в Еще больше возможностей - Администрирование - Регламентные операции - Разблокировать работу с внешними ресурсами.'");
	КонецЕсли;
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ЭтоЗаписьАссистентом") Тогда
		СобытиеОбъект.ДополнительныеСвойства.Вставить("ЭтоЗаписьАссистентом", ДополнительныеПараметры.ЭтоЗаписьАссистентом);
	КонецЕсли;
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ЗадачаАссистентаУправления") Тогда
		СобытиеОбъект.ЗадачаАссистентаУправления = ДополнительныеПараметры.ЗадачаАссистентаУправления;
	КонецЕсли;
	
	СобытиеОбъект.Записать();
	Результат.Событие = СобытиеОбъект.Ссылка;
	
	Возврат Результат;
	
КонецФункции

Процедура ОтправитьSMSБезДокументаСобытие( ЗначенияЗаполнения, ШаблонСообщения = Неопределено, ДополнительныеПараметры = Неопределено )
	
	ПредметСообщения = ЗначенияЗаполнения.ОснованиеЗаполнения;
	СообщениеПоШаблону = ШаблоныСообщений.СформироватьСообщение(ШаблонСообщения, ПредметСообщения, Новый УникальныйИдентификатор, ДополнительныеПараметры);
	Содержание = СообщениеПоШаблону.Текст;
	
	МассивНомеров = Новый Массив;
	Для Каждого Получатель Из СообщениеПоШаблону.Получатель Цикл
		РезультатПроверки = УправлениеНебольшойФирмойКлиентСервер.ПреобразоватьНомерДляОтправкиSMS(Получатель.НомерТелефона);
		Если РезультатПроверки.НомерКорректен Тогда
			Если МассивНомеров.Найти( РезультатПроверки.НомерОтправки ) = Неопределено Тогда
				МассивНомеров.Добавить( РезультатПроверки.НомерОтправки );
			КонецЕсли;
		Иначе
			ТекстОшибки = НСтр("ru='Неверный формат номера в строке %1 списка получателей'") + ":" + Символы.ПС + РезультатПроверки.СообщениеОбОшибке;
			ВызватьИсключение ТекстОшибки;
		КонецЕсли;
	КонецЦикла;
	
	Если МассивНомеров.Количество() > 0 Тогда
		РезультатОтправки = ОтправкаSMS.ОтправитьSMS(МассивНомеров, Содержание);
		
		Если Не ПустаяСтрока(РезультатОтправки.ОписаниеОшибки) Тогда
			ТекстОшибки = НСтр("ru='Не удалось выполнить отправку SMS по причине'") + ":" + Символы.ПС + РезультатОтправки.ОписаниеОшибки;
			ВызватьИсключение ТекстОшибки;
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция СоздатьОтправитьSMS(ЗначенияЗаполнения, ШаблонСообщения = Неопределено, ДополнительныеПараметры = Неопределено)
	
	Результат = Новый Структура;
	Результат.Вставить("Событие", Документы.Событие.ПустаяСсылка());
	Результат.Вставить("ТекстОшибки", "");

	Если НЕ ПолучитьФункциональнуюОпцию( "ИспользоватьСобытия" ) Тогда
		
		Если НЕ РегламентныеЗаданияСервер.РаботаСВнешнимиРесурсамиЗаблокирована() Тогда
			ОтправитьSMSБезДокументаСобытие( ЗначенияЗаполнения, ШаблонСообщения, ДополнительныеПараметры );
		КонецЕсли;
		
		Возврат Результат;
	КонецЕсли;
	
	Если НЕ ЗначенияЗаполнения.Свойство("ТипСобытия") Тогда
		ЗначенияЗаполнения.Вставить("ТипСобытия", Перечисления.ТипыСобытий.СообщениеSMS);
	КонецЕсли;
	
	Если НЕ ЗначенияЗаполнения.Свойство("Автор") Тогда
		ЗначенияЗаполнения.Вставить("Автор", Пользователи.АвторизованныйПользователь());
	КонецЕсли;
	
	СобытиеОбъект = Документы.Событие.СоздатьДокумент();
	СобытиеОбъект.Заполнить(ЗначенияЗаполнения);
	СобытиеОбъект.Дата = ТекущаяДатаСеанса();
	
	Если ШаблонСообщения <> Неопределено Тогда
		Если ДополнительныеПараметры = Неопределено Тогда
			ДополнительныеПараметры = Новый Структура;
		КонецЕсли;
		
		Если Не ДополнительныеПараметры.Свойство("ПараметрыСообщения") Тогда
			ДополнительныеПараметры.Вставить("ПараметрыСообщения", Новый Структура);
		КонецЕсли;
		
		Если ЗначенияЗаполнения.Свойство("ВыбранныйПолучатель") Тогда
			ОбщегоНазначенияКлиентСервер.ДополнитьСтруктуру(ДополнительныеПараметры.ПараметрыСообщения, ЗначенияЗаполнения.ВыбранныйПолучатель, Истина);
		КонецЕсли;
		
		СобытиеОбъект.ЗаполнитьПоШаблону(ШаблонСообщения, ЗначенияЗаполнения.ОснованиеЗаполнения, ДополнительныеПараметры);
	КонецЕсли;
	
	Если НЕ РегламентныеЗаданияСервер.РаботаСВнешнимиРесурсамиЗаблокирована() Тогда
		Попытка
			СобытиеОбъект.ОтправитьSMS(Ложь);
		Исключение
			Результат.ТекстОшибки = СтрШаблон(НСтр("ru='Не удалось автоматически отправить сообщение SMS, требуется отправка из формы События.
				|По причине: %1'"), ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		КонецПопытки;
	Иначе
		Результат.ТекстОшибки = НСтр("ru='Автоматическая отправка сообщения SMS невозможна, т.к. в базе установлена блокировка работы с внешними ресурсами.
			|Для разблокирования перейдите в Еще больше возможностей - Администрирование - Регламентные операции - Разблокировать работу с внешними ресурсами.'");
	КонецЕсли;
	
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ЭтоЗаписьАссистентом") Тогда
		СобытиеОбъект.ДополнительныеСвойства.Вставить("ЭтоЗаписьАссистентом", ДополнительныеПараметры.ЭтоЗаписьАссистентом);
	КонецЕсли;
	Если ТипЗнч(ДополнительныеПараметры) = Тип("Структура")
		И ДополнительныеПараметры.Свойство("ЗадачаАссистентаУправления") Тогда
		СобытиеОбъект.ЗадачаАссистентаУправления = ДополнительныеПараметры.ЗадачаАссистентаУправления;
	КонецЕсли;
	
	СобытиеОбъект.Записать();
	Результат.Событие = СобытиеОбъект.Ссылка;
	
	Возврат Результат;
	
КонецФункции

#КонецОбласти

#КонецЕсли

#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Параметры.Свойство("Должность", Должность);
	Параметры.Свойство("РабочаяСмена", РабочаяСмена);
	
	Если НЕ ЗначениеЗаполнено(Должность) Тогда
		ТекущийЭлемент = Элементы.Должность;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(РабочаяСмена) Тогда
		ТекущийЭлемент = Элементы.РабочаяСмена;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Должность) И ЗначениеЗаполнено(РабочаяСмена) Тогда
		ТекущийЭлемент = Элементы.Сотрудник;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗаписатьИЗакрыть(Команда)
	
	Если ПроверитьЗаполнение() Тогда
		
		ПараметрЗакрытия = Новый Структура;
		ПараметрЗакрытия.Вставить("РабочаяСмена", РабочаяСмена);
		ПараметрЗакрытия.Вставить("Должность", Должность);
		ПараметрЗакрытия.Вставить("Сотрудник", Сотрудник);
		
		ЭтотОбъект.Закрыть(ПараметрЗакрытия);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
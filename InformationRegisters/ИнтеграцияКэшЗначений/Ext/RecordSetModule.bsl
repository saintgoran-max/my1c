#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)

	Если ОбменДанными.Загрузка Тогда
         Возврат;
    КонецЕсли;	
    
	ДатаАктуальностиПоУмолчанию = ИнтеграцияКэшСервер.ДатаАктуальностиКэшаПоУмолчанию();
	
	НаборЗаписей = ЭтотОбъект;
	Для Каждого НаборЗаписейСтрока Из НаборЗаписей Цикл
		Если Не ЗначениеЗаполнено( НаборЗаписейСтрока.ДатаАктуальности ) Тогда
			НаборЗаписейСтрока.ДатаАктуальности = ДатаАктуальностиПоУмолчанию;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Иначе

  ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
  
#КонецЕсли
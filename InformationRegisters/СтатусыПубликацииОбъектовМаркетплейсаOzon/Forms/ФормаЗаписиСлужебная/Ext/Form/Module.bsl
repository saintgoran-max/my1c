#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ДанныеОбъектаНаСервере()
	лЗапись = РеквизитФормыВЗначение( "Запись" );
	лДанныеОбъекта = лЗапись.ДанныеОбъекта.Получить();
	Стр = ЗначениеВСтрокуВнутр( лДанныеОбъекта );
	Возврат Стр;
КонецФункции

&НаКлиенте
Процедура КнопкаДанныеОбъекта(Команда)
	Стр = ДанныеОбъектаНаСервере();
	Текст = Новый ТекстовыйДокумент;
	Текст.ДобавитьСтроку( Стр );
	Текст.Показать( "ДанныеОбъекта" );
КонецПроцедуры

#КонецОбласти
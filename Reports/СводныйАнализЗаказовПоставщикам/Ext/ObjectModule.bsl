#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.ВариантыОтчетов

// Задать настройки формы отчета.
//
// Параметры:
//  Форма		 - ФормаКлиентскогоПриложения	 - Форма отчета
//  КлючВарианта - Строка						 - Ключ загружаемого варианта
//  Настройки	 - Структура					 - см. ОтчетыКлиентСервер.НастройкиОтчетаПоУмолчанию
//
Процедура ОпределитьНастройкиФормы(Форма, КлючВарианта, Настройки) Экспорт
	
	Настройки.События.ПриСозданииНаСервере = Истина;
	Настройки.События.ПриЗагрузкеВариантаНаСервере = Истина;
	Настройки.События.ПриЗагрузкеПользовательскихНастроекНаСервере = Истина;
	Настройки.События.ПослеЗаполненияПанелиБыстрыхНастроек = Истина;
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.ВариантыОтчетов

// Процедура - Обработчик заполнения настроек отчета и варианта
//
// Параметры:
//  НастройкиОтчета		 - Структура - Настройки отчета, подробнее см. процедуру ОтчетыУНФ.ИнициализироватьНастройкиОтчета 
//  НастройкиВариантов	 - Структура - Настройки варианта отчета, подробнее см. процедуру ОтчетыУНФ.ИнициализироватьНастройкиВарианта
//
Процедура ПриОпределенииНастроекОтчета(НастройкиОтчета, НастройкиВариантов) Экспорт
	
	УстановитьТегиВариантов(НастройкиВариантов);
	ДобавитьОписанияСвязанныхПолей(НастройкиВариантов);
	
КонецПроцедуры

// Выводит и группирует элементы формы, связанные с пользовательскими настройками.
//
// Параметры:
//   Форма - ФормаКлиентскогоПриложения - Форма отчета.
//   ПараметрыОбновления - Структура
//                       - Неопределено
//
Процедура ПослеЗаполненияПанелиБыстрыхНастроек(Форма, ПараметрыОбновления) Экспорт
	
	ЗаполнитьСпискиВыбораПриОпределенииНастроек(Форма);
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область ОбработчикиСобытий

// Вызывается в обработчике одноименного события формы отчета после выполнения кода формы.
//
// Параметры:
//   Форма - ФормаКлиентскогоПриложения - Форма отчета.
//   Отказ - Передается из параметров обработчика "как есть".
//   СтандартнаяОбработка - Передается из параметров обработчика "как есть".
//
// См. также:
//   "ФормаКлиентскогоПриложения.ПриСозданииНаСервере" в синтакс-помощнике.
//
Процедура ПриСозданииНаСервере(Форма, Отказ, СтандартнаяОбработка) Экспорт
	
	ОтчетыУНФ.ФормаОтчетаПриСозданииНаСервере(Форма, Отказ, СтандартнаяОбработка);
	
КонецПроцедуры

// Обработчик события ПриЗагрузкеВариантаНаСервере
//
// Параметры:
//  Форма			 - ФормаКлиентскогоПриложения	 - Форма отчета
//  НовыеНастройкиКД - НастройкиКомпоновкиДанных	 - Загружаемые настройки КД
//
Процедура ПриЗагрузкеВариантаНаСервере(Форма, НовыеНастройкиКД) Экспорт
	
	ОтчетыУНФ.ПреобразоватьСтарыеНастройки(Форма, НовыеНастройкиКД);	
	ОтчетыУНФ.ОбновитьВидимостьОтбораОрганизация(Форма.Отчет.КомпоновщикНастроек);	
	ОтчетыУНФ.ФормаОтчетаПриЗагрузкеВариантаНаСервере(Форма, НовыеНастройкиКД);
	
КонецПроцедуры

// Обработчик события ПриЗагрузкеПользовательскихНастроекНаСервере
//
// Параметры:
//  Форма							 - ФормаКлиентскогоПриложения				 - Форма отчета
//  НовыеПользовательскиеНастройкиКД - ПользовательскиеНастройкиКомпоновкиДанных - Загружаемые пользовательские
//                                                                                 настройки КД
//
Процедура ПриЗагрузкеПользовательскихНастроекНаСервере(Форма, НовыеПользовательскиеНастройкиКД) Экспорт
	
	ОтчетыУНФ.ПеренестиПараметрыЗаголовкаВНастройки(КомпоновщикНастроек.Настройки, НовыеПользовательскиеНастройкиКД);	
	
КонецПроцедуры

Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	ОтчетыУНФ.ОбъединитьСПользовательскимиНастройками(КомпоновщикНастроек);
	ОтчетыУНФ.ПриКомпоновкеРезультата(КомпоновщикНастроек, СхемаКомпоновкиДанных, ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область ИзменениеЭлементовФормы

Процедура ЗаполнитьСпискиВыбораПриОпределенииНастроек(Форма)
	
	ЗаполнитьСписокВыбораСостоянийОплаты(Форма);
	ЗаполнитьСписокВыбораСостоянийПоступления(Форма);
	
КонецПроцедуры

Процедура ЗаполнитьСписокВыбораСостоянийОплаты(Форма)
	
	ИмяЭлемента = "КомпоновщикНастроекПользовательскиеНастройкиЭлемент2Значение";
	
	Если Не ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма.Элементы, ИмяЭлемента) Тогда
		Возврат;
	КонецЕсли;
	
	ЕстьПолеВыбораСостояния = Ложь;
	ПараметрКомпоновки = Новый ПараметрКомпоновкиДанных("СостояниеОплаты");
	Для Каждого ЭлементНастройки Из Форма.Отчет.КомпоновщикНастроек.ПользовательскиеНастройки.Элементы Цикл
		
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ЭлементНастройки, "Параметр")
			И ЭлементНастройки.Параметр = ПараметрКомпоновки Тогда
			
			Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ЭлементНастройки, "Использование") Тогда
				
				Если ЭлементНастройки.Использование = Истина Тогда
					ЕстьПолеВыбораСостояния = Истина;
					Прервать;
				КонецЕсли;
				
			Иначе
				
				ЕстьПолеВыбораСостояния = Истина;
				Прервать;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если Не ЕстьПолеВыбораСостояния Тогда
		Возврат;
	КонецЕсли;
	
	ЭлементФормы = Форма.Элементы[ИмяЭлемента];
	Если ТипЗнч(ЭлементФормы) <> Тип("ПолеФормы") Или ЭлементФормы.Заголовок <> "Состояние оплаты" Тогда
		Возврат;
	КонецЕсли;
	
	Отступ = СтрШаблон("%1%2", Символы.НПП, Символы.НПП);
	Для Каждого ЭлементСписка Из ЭлементФормы.СписокВыбора Цикл
		
		Если ЭлементСписка.Значение = "Не оплаченные" Тогда
			ЭлементСписка.Представление = НСтр("ru = 'Без оплаты'");
		ИначеЕсли ЭлементСписка.Значение = "Оплаченные частично" Тогда
			ЭлементСписка.Представление = СтрШаблон("%1%2", Отступ, НСтр("ru = 'Оплачен частично'"));
		ИначеЕсли ЭлементСписка.Значение = "Оплаченные полностью" Тогда
			ЭлементСписка.Представление = СтрШаблон("%1%2", Отступ, НСтр("ru = 'Оплачен полностью'"));
		ИначеЕсли ЭлементСписка.Значение = "Без полной оплаты" Тогда
			ЭлементСписка.Представление = НСтр("ru = 'Без оплаты или оплачен частично'");
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьСписокВыбораСостоянийПоступления(Форма)
	
	ИмяЭлемента = "КомпоновщикНастроекПользовательскиеНастройкиЭлемент3Значение";
	
	Если Не ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма.Элементы, ИмяЭлемента) Тогда
		Возврат;
	КонецЕсли;
	
	ЕстьПолеВыбораСостояния = Ложь;
	ПараметрКомпоновки = Новый ПараметрКомпоновкиДанных("СостояниеПоступления");
	Для Каждого ЭлементНастройки Из Форма.Отчет.КомпоновщикНастроек.ПользовательскиеНастройки.Элементы Цикл
		
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ЭлементНастройки, "Параметр")
			И ЭлементНастройки.Параметр = ПараметрКомпоновки Тогда
			
			Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ЭлементНастройки, "Использование") Тогда
				
				Если ЭлементНастройки.Использование = Истина Тогда
					ЕстьПолеВыбораСостояния = Истина;
					Прервать;
				КонецЕсли;
				
			Иначе
				
				ЕстьПолеВыбораСостояния = Истина;
				Прервать;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если Не ЕстьПолеВыбораСостояния Тогда
		Возврат;
	КонецЕсли;
	
	ЭлементФормы = Форма.Элементы[ИмяЭлемента];
	Если ТипЗнч(ЭлементФормы) <> Тип("ПолеФормы") Или ЭлементФормы.Заголовок <> "Состояние поступления" Тогда
		Возврат;
	КонецЕсли;
	
	Отступ = СтрШаблон("%1%2", Символы.НПП, Символы.НПП);
	Для Каждого ЭлементСписка Из ЭлементФормы.СписокВыбора Цикл
		
		Если ЭлементСписка.Значение = "Непоступившие" Тогда
			ЭлементСписка.Представление = НСтр("ru = 'Без поступлений'");
		ИначеЕсли ЭлементСписка.Значение = "Поступившие частично" Тогда
			ЭлементСписка.Представление = СтрШаблон("%1%2", Отступ, НСтр("ru = 'Поступил частично'"));
		ИначеЕсли ЭлементСписка.Значение = "Поступившие полностью" Тогда
			ЭлементСписка.Представление = СтрШаблон("%1%2", Отступ, НСтр("ru = 'Поступил полностью'"));
		ИначеЕсли ЭлементСписка.Значение = "Не поступившие полностью" Тогда
			ЭлементСписка.Представление = НСтр("ru = 'Без поступлений или поступил частично'");
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

Процедура УстановитьТегиВариантов(НастройкиВариантов)
	
	Для Каждого НастройкиТекВарианта Из НастройкиВариантов Цикл
		НастройкиТекВарианта.Значение.Теги = НСтр("ru = 'Запасы,Закупки,Заказы,Контрагенты,Поставщики,Номенклатура,Поступления,Оплаты'");
	КонецЦикла;
	
КонецПроцедуры

Процедура ДобавитьОписанияСвязанныхПолей(НастройкиВариантов)
	
	Для Каждого НастройкиТекВарианта Из НастройкиВариантов Цикл
		ОтчетыУНФ.ДобавитьОписаниеПривязки(НастройкиТекВарианта.Значение.СвязанныеПоля, "Контрагент", "Справочник.Контрагенты");
		ОтчетыУНФ.ДобавитьОписаниеПривязки(НастройкиТекВарианта.Значение.СвязанныеПоля, "Номенклатура", "Справочник.Номенклатура");
		ОтчетыУНФ.ДобавитьОписаниеПривязки(НастройкиТекВарианта.Значение.СвязанныеПоля, "ЗаказПоставщику", "Документ.ЗаказПоставщику");
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область Инициализация

ЭтоОтчетУНФ = Истина;

#КонецОбласти

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли
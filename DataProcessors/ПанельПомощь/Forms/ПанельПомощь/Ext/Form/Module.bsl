#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ЭтоУНФ = ОбщегоНазначенияУНФ.ЭтоУНФ();
	МаксимальноеКоличествоРезультатовПоиска = 10;
	ЗаполнитьРазделы();
	ЗаполнитьБаннеры();
	ВывестиБаннер(0);
	ПрочитатьИсториюПоиска();
	
КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)
	
	Если ЗавершениеРаботы Тогда
		Возврат;
	КонецЕсли;
	
	СохранитьИсториюПоиска();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура СообществоVKНажатие(Элемент)
	
	ОткрытьСсылкуРаздела(Элемент.Имя);
		
КонецПроцедуры

&НаКлиенте
Процедура СообществоTelegramНажатие(Элемент)
	
	ОткрытьСсылкуРаздела(Элемент.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура СообществоRutubeНажатие(Элемент)
	
	ОткрытьСсылкуРаздела(Элемент.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура СообществоYoutubeНажатие(Элемент)
	
	ОткрытьСсылкуРаздела(Элемент.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура СообществоBiz360Нажатие(Элемент)
	
	ОткрытьСсылкуРаздела(Элемент.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура ПредыдущийБаннерНажатие(Элемент)
	
	Если НомерТекущегоБаннера = 0 Тогда
		НомерТекущегоБаннера = СписокБаннеров.Количество() - 1;
	Иначе
		НомерТекущегоБаннера = НомерТекущегоБаннера - 1;
	КонецЕсли;
	
	ВывестиБаннер(НомерТекущегоБаннера);
	
КонецПроцедуры

&НаКлиенте
Процедура СледующийБаннерНажатие(Элемент)
	
	Если НомерТекущегоБаннера = СписокБаннеров.Количество() - 1 Тогда
		НомерТекущегоБаннера = 0;
	Иначе
		НомерТекущегоБаннера = НомерТекущегоБаннера + 1;
	КонецЕсли;
	ВывестиБаннер(НомерТекущегоБаннера);
	
КонецПроцедуры

&НаКлиенте
Процедура ПредложитьВозможностьОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки, 
	СтандартнаяОбработка)

	СтандартнаяОбработка = Ложь;
	Тег = НСтр("ru = 'Подсистема Помощь'");
	УправлениеНебольшойФирмойКлиент.ПредложитьВозможностьНажатие(Тег);
	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьОбращениеВТехподдержкуОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки,
		СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	РазделениеВключено = ОбщегоНазначенияКлиент.РазделениеВключено();
	
	ШаблонСообщения = ШаблонСообщенияВПоддержку();
	
	Если Не РазделениеВключено
		И ОбщегоНазначенияКлиент.ПодсистемаСуществует("ИнтернетПоддержкаПользователей.СообщенияВСлужбуТехническойПоддержки") Тогда
		
		МодульСообщенияВСлужбуТехническойПоддержкиКлиентСервер =
			ОбщегоНазначенияКлиент.ОбщийМодуль("СообщенияВСлужбуТехническойПоддержкиКлиентСервер");
		ДанныеСообщения = МодульСообщенияВСлужбуТехническойПоддержкиКлиентСервер.ДанныеСообщения();
		ДанныеСообщения.Получатель = "webIts";
		ДанныеСообщения.Тема       = НСтр("ru = 'Сообщение из раздела Помощь:'");
		ДанныеСообщения.Сообщение  = ШаблонСообщения;
		ДанныеСообщения.ИспользоватьСтандартныйШаблон = Ложь;
		
		МодульСообщенияВСлужбуТехническойПоддержкиКлиент = ОбщегоНазначенияКлиент.ОбщийМодуль(
			"СообщенияВСлужбуТехническойПоддержкиКлиент");
		МодульСообщенияВСлужбуТехническойПоддержкиКлиент.ОтправитьСообщение(
			ДанныеСообщения);
		
	ИначеЕсли РазделениеВключено Тогда
		
		ПараметрыОбращения = ИнформационныйЦентрКлиент.НовыеПараметрыОбращенияВСлужбуПоддержки();
		ПараметрыОбращения.ИспользоватьСтандартныйШаблон = Ложь;
		ПараметрыОбращения.Тема = НСтр("ru = 'Сообщение из раздела Помощь:'");
		ПараметрыОбращения.Текст = ШаблонСообщения;
		ИнформационныйЦентрКлиент.ОткрытьФормуОтправкиСообщенияВСлужбуПоддержки(Истина, , , ПараметрыОбращения);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура БаннерПереключатель_0Нажатие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	НомерТекущегоБаннера = 0;
	ВывестиБаннер(НомерТекущегоБаннера);
	
КонецПроцедуры

&НаКлиенте
Процедура БаннерПереключатель_1Нажатие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	НомерТекущегоБаннера = 1;
	ВывестиБаннер(НомерТекущегоБаннера);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Искать(Команда)
	
	Если Не ПустаяСтрока(СтрокаПоиска) Тогда
		ДобавитьВИсториюПоиска(СтрокаПоиска);
		ДлительнаяОперация = ОперацияВыполнитьПоискНаСервере();
		ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
		ПараметрыОжидания.ВыводитьОкноОжидания = Ложь;
		ОповещениеОЗавершении = Новый ОписаниеОповещения("ПриЗавершенииПоиска", ЭтотОбъект);
		ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);
	Иначе
		ОчиститьПодчиненныеЭлементы("ГруппаРезультатыПоиска");
		Элементы.ГруппаСтраницы.ТекущаяСтраница = Элементы.СтраницаРезультатыПоиска;
		ТекстНайденоСтрок = НСтр("ru = 'По вашему запросу ничего не найдено. Попробуйте изменить поисковый запрос.'");
		ТекстЗаголовкаИТС = "";
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "НадписьКоличествоСтрок", 
		"Заголовок", ТекстНайденоСтрок);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "НадписьСсылкаНаИТС", 
		"Заголовок", ТекстЗаголовкаИТС);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "Назад", 
		"Видимость", Истина);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РазделВебинары(Команда)
	
	ОткрытьСсылкуРаздела(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура РазделКейсы(Команда)
	
	ОткрытьСсылкуРаздела(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура РазделКниги(Команда)
	
	ОткрытьСсылкуРаздела(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура РазделКурсы(Команда)
	
	ОткрытьСсылкуРаздела(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура РазделНовыеВозможности(Команда)
	
	ОткрытьСсылкуРаздела(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура РазделОтветыНаВопросы(Команда)
	
	ОткрытьСсылкуРаздела(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура РазделСтатьиИСправочники(Команда)
	
	ОткрытьСсылкуРаздела(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура РазделЭкзамены(Команда)
	
	ОткрытьСсылкуРаздела(Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура Назад(Команда)
	ПоказатьГлавнуюСтраницу();
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура ПоказатьГлавнуюСтраницу()
	
	СтрокаПоиска = "";
	Элементы.СтрокаПоиска.КнопкаОчистки = Ложь;
	Элементы.Назад.Видимость = Ложь;
	Элементы.ГруппаСтраницы.ТекущаяСтраница = Элементы.СтраницаПанельРазделов;
	ОчиститьПодчиненныеЭлементы("ГруппаРезультатыПоиска");
	
КонецПроцедуры

&НаСервере
Процедура СохранитьИсториюПоиска()
	
	КлючаОбъекта = "ФормаПомощь";
	КлючНастроек = "ИсторияПоиска";
	Настройки = ИсторияПоиска.ВыгрузитьЗначения();
	ХранилищеСистемныхНастроек.Сохранить(КлючаОбъекта, КлючНастроек, Настройки, , );
	
КонецПроцедуры

&НаСервере
Процедура ПрочитатьИсториюПоиска()
	
	КлючаОбъекта = "ФормаПомощь";
	КлючНастроек = "ИсторияПоиска";
	
	Настройки = ХранилищеСистемныхНастроек.Загрузить(КлючаОбъекта, КлючНастроек);
	Если Настройки <> Неопределено Тогда
		ИсторияПоиска.ЗагрузитьЗначения(Настройки);
	КонецЕсли;
	Элементы.СтрокаПоиска.СписокВыбора.ЗагрузитьЗначения(ИсторияПоиска.ВыгрузитьЗначения());
	Элементы.СтрокаПоиска.КнопкаВыпадающегоСписка = ИсторияПоиска.Количество() > 0;
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьВИсториюПоиска(СтрокаПоиска)
	
	Если ИсторияПоиска.НайтиПоЗначению(СокрЛП(СтрокаПоиска)) = Неопределено Тогда
		Если ИсторияПоиска.Количество() = 10 Тогда
			ИсторияПоиска.Удалить(9);
		КонецЕсли;
		ИсторияПоиска.Вставить(0, СтрокаПоиска);
	КонецЕсли;
	
	Элементы.СтрокаПоиска.СписокВыбора.ЗагрузитьЗначения(ИсторияПоиска.ВыгрузитьЗначения());
	Элементы.СтрокаПоиска.КнопкаВыпадающегоСписка = ИсторияПоиска.Количество() > 0;
	
КонецПроцедуры

&НаСервере
Функция ОперацияВыполнитьПоискНаСервере()
	
	ОчиститьПодчиненныеЭлементы("ГруппаРезультатыПоиска");
	Элементы.ГруппаСтраницы.ТекущаяСтраница = Элементы.СтраницаФоновоеЗадание;
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияФункции(УникальныйИдентификатор);
	Возврат ДлительныеОперации.ВыполнитьФункцию(ПараметрыВыполнения, "Обработки.ПанельПомощь.ВыполнитьПоиск",
		СтрокаПоиска);
	
КонецФункции

&НаКлиенте
Процедура ПриЗавершенииПоиска(Результат, ДополнительныеПараметры) Экспорт

	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Результат.Статус = "Ошибка" Тогда
		Элементы.ГруппаСтраницы.ТекущаяСтраница = Элементы.СтраницаПанельРазделов;
		ВызватьИсключение Результат.КраткоеПредставлениеОшибки;
	КонецЕсли;
	
	Элементы.Назад.Видимость = Истина;
	Элементы.ГруппаСтраницы.ТекущаяСтраница = Элементы.СтраницаРезультатыПоиска;
	
	ВывестиРезультатыПоиска(Результат.АдресРезультата);

КонецПроцедуры

&НаСервере
Процедура ЗаполнитьРазделы()
	
	Если ЭтоУНФ Тогда
		ДобавитьСтраницуРаздела("РазделВебинары",           "https://rutube.ru/plst/536629/");
		ДобавитьСтраницуРаздела("РазделКейсы",              "https://v8.1c.ru/small.biz/poleznye-materialy/cases/");
		ДобавитьСтраницуРаздела("РазделКниги",              "https://v8.1c.ru/small.biz/support/#p01");
		ДобавитьСтраницуРаздела("РазделКурсы",              "https://v8.1c.ru/small.biz/support/#p07");
		ДобавитьСтраницуРаздела("РазделНовыеВозможности",   "https://its.1c.ru/db/updinfo#content:44:hdoc");
		ДобавитьСтраницуРаздела("РазделОтветыНаВопросы",    "https://its.1c.ru/db/answers1c/content/928/hdoc");
		ДобавитьСтраницуРаздела("РазделСтатьиИСправочники", "https://v8.1c.ru/small.biz/support/#p04");
		ДобавитьСтраницуРаздела("РазделЭкзамены",           "https://v8.1c.ru/small.biz/support/#p08");
		ДобавитьСтраницуРаздела("СообществоRutube",         "https://rutube.ru/channel/10005354");
		ДобавитьСтраницуРаздела("СообществоYoutube",        "https://www.youtube.com/c/unf");
		ДобавитьСтраницуРаздела("СообществоTelegram",       "https://t.me/small_biz");
		ДобавитьСтраницуРаздела("СообществоVK",             "https://vk.com/1sunf");
		ДобавитьСтраницуРаздела("СообществоBiz360",         "https://biz360.ru/");
	Иначе
		ДобавитьСтраницуРаздела("РазделВебинары",           "https://rutube.ru/plst/536629/");
		ДобавитьСтраницуРаздела("РазделКейсы",              "https://v8.1c.ru/small.biz/poleznye-materialy/cases/");
		ДобавитьСтраницуРаздела("РазделКниги",              "https://v8.1c.ru/retail/support_retail/#p01");
		ДобавитьСтраницуРаздела("РазделКурсы",              "https://v8.1c.ru/retail/support_retail/#p07");
		ДобавитьСтраницуРаздела("РазделНовыеВозможности",   "https://its.1c.ru/db/updinfo#content:1254:hdoc");
		ДобавитьСтраницуРаздела("РазделОтветыНаВопросы",    "https://its.1c.ru/db/answers1c/content/928/hdoc");
		ДобавитьСтраницуРаздела("РазделСтатьиИСправочники", "https://v8.1c.ru/retail/support_retail/#p04");
		ДобавитьСтраницуРаздела("РазделЭкзамены",           "https://v8.1c.ru/retail/support_retail/#p08");
		ДобавитьСтраницуРаздела("СообществоRutube",         "https://rutube.ru/channel/10005354");
		ДобавитьСтраницуРаздела("СообществоYoutube",        "https://www.youtube.com/c/unf");
		ДобавитьСтраницуРаздела("СообществоTelegram",       "https://t.me/small_biz");
		ДобавитьСтраницуРаздела("СообществоVK",             "https://vk.com/1sunf");
		ДобавитьСтраницуРаздела("СообществоBiz360",         "https://biz360.ru/");
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ДобавитьСтраницуРаздела(ИмяРаздела, АдресСтраницы)
	
	НоваяСтрока = СсылкиПоРазделам.Добавить();
	НоваяСтрока.ИмяРаздела = ИмяРаздела;
	НоваяСтрока.АдресСтраницы = АдресСтраницы;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьБаннеры()

	Если ЭтоУНФ Тогда
		АдресРуководства = "https://its.1c.ru/db/unfdoc#bookmark:unf:UNF000018";
	Иначе
		АдресРуководства = "https://its.1c.ru/db/retail30doc#bookmark:ur:UR000018";
	КонецЕсли;

	НоваяНовость = СписокБаннеров.Добавить();
	НоваяНовость.Заголовок = НСтр("ru = 'Мини-уроки «Старт в 1С:Розница и 1С:УНФ»'");
	НоваяНовость.АдресСтраницы = "https://rutube.ru/plst/536586/";
	НоваяНовость.ЗаголовокСсылки = НСтр("ru = 'Смотрите на rutube.ru'");
	БаннерПереключатель_0 = 1;

	НоваяНовость = СписокБаннеров.Добавить();
	НоваяНовость.Заголовок = НСтр("ru = 'Руководство пользователя'");
	НоваяНовость.АдресСтраницы = АдресРуководства;
	НоваяНовость.ЗаголовокСсылки = НСтр("ru = 'Читайте на its.1c.ru'");
	БаннерПереключатель_1 = 1;
	
	ДанныеБаннер = Обработки.ПанельПомощь.ПолучитьМакет("Баннер");
	АдресКартинкиБаннера = ПоместитьВоВременноеХранилище(ДанныеБаннер);
	
КонецПроцедуры

&НаСервере
Процедура ВывестиБаннер(НомерБаннера)
	
	ТекстБаннера = СписокБаннеров[НомерБаннера].Заголовок;
	АдресБаннера = СписокБаннеров[НомерБаннера].АдресСтраницы;
	ЗаголовокСсылки = СписокБаннеров[НомерБаннера].ЗаголовокСсылки;
	
	ТекстБаннера = СтроковыеФункции.ФорматированнаяСтрока(
		"<span style = ""font: ШрифтЗаголовкаБаннераКрупный"">%1</span>", ТекстБаннера);
	СсылкаБаннера = СтроковыеФункции.ФорматированнаяСтрока(
		"<span style = ""font: ШрифтТекстаБаннера""><a href = ""%1"">%2</a></span>", АдресБаннера, ЗаголовокСсылки);
	
	Элементы.ТекстБаннера.Заголовок = ТекстБаннера;
	Элементы.АдресБаннера.Заголовок = СсылкаБаннера;
	
	ВключитьПереключательБаннера(НомерБаннера);
	
КонецПроцедуры

&НаСервере
Процедура ВключитьПереключательБаннера(НомерБаннера)
	
	ПрефиксИмени = "БаннерПереключатель_";
	Для Индекс = 0 По СписокБаннеров.Количество() - 1 Цикл
		Если Индекс = НомерБаннера Тогда
			ЭтотОбъект[ПрефиксИмени + Строка(Индекс)] = 2			
		Иначе
			ЭтотОбъект[ПрефиксИмени + Строка(Индекс)] = 1
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Процедура ВывестиРезультатыПоиска(АдресРезультата)
	
	РезультатПоиска = ПолучитьИзВременногоХранилища(АдресРезультата);
	УдалитьИзВременногоХранилища(АдресРезультата);
	
	Если РезультатПоиска = Неопределено Тогда
		Возврат
	КонецЕсли;
		
	Элементы.ГруппаСтраницы.ТекущаяСтраница = Элементы.СтраницаРезультатыПоиска;
	Элементы.СтрокаПоиска.КнопкаОчистки = Истина;
	
	РезультатыПоискаЗаписи = РезультатПоиска.Получить("items");
	
	КоличествоДокументов = РезультатПоиска.Получить("count");
	ТекстНайденоСтрокШаблон = НСтр("ru = 'Найдено: %1'");
	ТекстНайденоСтрок = СтрШаблон(ТекстНайденоСтрокШаблон, КоличествоДокументов);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "НадписьКоличествоСтрок", 
		"Заголовок", ТекстНайденоСтрок);
	
	РезультатыПоиска.Очистить();
	 
	СчетчикРезультатов = 0;
	СтруктураРезультата = Новый Структура;
	
	Для каждого Элемент Из РезультатыПоискаЗаписи Цикл
		
		СчетчикРезультатов = СчетчикРезультатов + 1;
		
		Если ТипЗнч(Элемент["snippet_text"]) = Тип("Массив") И Элемент["snippet_text"].Количество() > 0 Тогда
			СтруктураРезультата.Очистить();
			СтруктураРезультата.Вставить("ДатаПубликации", Элемент["datetime"]);
			СтруктураРезультата.Вставить("Название", Элемент["title"]);
			СтруктураРезультата.Вставить("Адрес", Элемент["url"]);
			СтруктураРезультата.Вставить("Фрагмент", Элемент["snippet_text"][0]);
			Если СтрДлина(СтруктураРезультата.Фрагмент) > 3 И Лев(СтруктураРезультата.Фрагмент, 3) = "..." Тогда
				СтруктураРезультата.Фрагмент = Прав(СтруктураРезультата.Фрагмент, 
					СтрДлина(СтруктураРезультата.Фрагмент) - 3);
			КонецЕсли;
			ДобавитьЭлементРезультатПоиска(СтруктураРезультата, Строка(СчетчикРезультатов));
		КонецЕсли;
		
		Если СчетчикРезультатов = МаксимальноеКоличествоРезультатовПоиска Тогда
			Прервать;
		КонецЕсли;
				
	КонецЦикла;
	
	Если СчетчикРезультатов = 0 Тогда
		
		ТекстНайденоСтрок = НСтр("ru = 'По вашему запросу ничего не найдено. Попробуйте изменить поисковый запрос.'");
		ТекстЗаголовкаИТС = "";
		
	ИначеЕсли СчетчикРезультатов < МаксимальноеКоличествоРезультатовПоиска  Тогда
		
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "НадписьСсылкаНаИТС", 
			"Видимость", Ложь);
		Возврат;
		
	Иначе
		
		ШаблонКоличествоРезультатов = НСтр("ru = 'Показано %1 результатов.'");
		СтрокаКоличествоРезультатов = СтрШаблон(ШаблонКоличествоРезультатов, СчетчикРезультатов);
		СтрокаИТС = НСтр("ru = 'Полный список результатов поиска на'");
		СоставСсылки = "http://its.1c.ru/db/alldb#search:its:" 
			+ СтрокаПоиска 
			+ "?attributes[dbnick]=&attributes[conf][]=УНФ&attribute=conf";
		СсылкаИТС = Новый ФорматированнаяСтрока("its.1c.ru", , , , СоставСсылки);
		ТекстЗаголовкаИТС = Новый ФорматированнаяСтрока(СтрокаКоличествоРезультатов, " ", СтрокаИТС, " ", СсылкаИТС);
					
	КонецЕсли;

	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "НадписьКоличествоСтрок", 
		"Заголовок", ТекстНайденоСтрок);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "НадписьСсылкаНаИТС", 
		"Заголовок", ТекстЗаголовкаИТС);
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Элементы, "НадписьСсылкаНаИТС", 
		"Видимость", Истина);
		
КонецПроцедуры

&НаСервере
Процедура ДобавитьЭлементРезультатПоиска(ЭлементОтвета, Идентификатор)
	
	СтрокаРезультатПоиска = РезультатыПоиска.Добавить();
	СтрокаРезультатПоиска.ИмяКоманды = "КомандаРезультатПоиска"+ Идентификатор;
	СтрокаРезультатПоиска.АдресСтраницы = ЭлементОтвета.Адрес;
	
	РодительскаяГруппа = Элементы.Добавить("ГруппаРезультатыПоиска" + Идентификатор, 
		Тип("ГруппаФормы"), Элементы.ГруппаРезультатыПоиска);
	РодительскаяГруппа.Вид = ВидГруппыФормы.ОбычнаяГруппа;
	РодительскаяГруппа.ОтображатьЗаголовок = Ложь;
	РодительскаяГруппа.Отображение = ОтображениеОбычнойГруппы.Нет;
	РодительскаяГруппа.Группировка = ГруппировкаПодчиненныхЭлементовФормы.Вертикальная;
	РодительскаяГруппа.ГоризонтальноеПоложениеВГруппе = ГоризонтальноеПоложениеЭлемента.Лево;
	РодительскаяГруппа.ВертикальныйИнтервал = ИнтервалМеждуЭлементамиФормы.Половинный;
	
	НоваяКоманда = Команды.Добавить("КомандаРезультатПоиска"+ Идентификатор);
	НоваяКоманда.Действие = "Подключаемый_ОткрытьСтатьюПоАдресу";

	НовоеПоле = Элементы.Добавить("НазваниеРезультатПоиска" + Идентификатор,
		Тип("КнопкаФормы"), РодительскаяГруппа);
	НовоеПоле.ИмяКоманды = НоваяКоманда.Имя;
	НовоеПоле.Заголовок = ЭлементОтвета.Название;
	НовоеПоле.ОтображениеФигуры = ОтображениеФигурыКнопки.Нет;
	ЦветЗаголовка = ЦветаСтиля.ГиперссылкаЦвет;	
	ШрифтЗаголовка = ШрифтыСтиля.ВариантОтчетаОбычныйШрифт;
	НовоеПоле.Шрифт = ШрифтЗаголовка;
	НовоеПоле.ЦветТекста = ЦветЗаголовка;
	
	НовоеПоле = Элементы.Добавить("ФрагментРезультатПоиска" + Идентификатор,
		Тип("ДекорацияФормы"), РодительскаяГруппа);
	НовоеПоле.Вид = ВидДекорацииФормы.Надпись;
	НовоеПоле.АвтоМаксимальнаяШирина = Ложь;
	НовоеПоле.ГоризонтальноеПоложениеВГруппе = ГоризонтальноеПоложениеЭлемента.Лево;
	НовоеПоле.Заголовок = ЭлементОтвета.Фрагмент;
	
	НовоеПоле = Элементы.Добавить("ДатаПубликацииРезультатПоиска" + Идентификатор,
		Тип("ДекорацияФормы"), РодительскаяГруппа);
	НовоеПоле.Вид = ВидДекорацииФормы.Надпись;
	НовоеПоле.АвтоМаксимальнаяШирина = Ложь;
	НовоеПоле.ГоризонтальноеПоложениеВГруппе = ГоризонтальноеПоложениеЭлемента.Лево;
	НовоеПоле.Заголовок = ЭлементОтвета.ДатаПубликации;
	ШрифтЗаголовка = ШрифтыСтиля.МелкийШрифтТекста;
	ЦветЗаголовка = ЦветаСтиля.ЦветТекстаКнопки;
	НовоеПоле.Шрифт = ШрифтЗаголовка;
	НовоеПоле.ЦветТекста = ЦветЗаголовка;		
	
КонецПроцедуры

&НаСервере
Процедура ОчиститьПодчиненныеЭлементы(ИмяГруппы)
	
	МассивКУдалению = Новый Массив;
	Для каждого Элемент Из Элементы[ИмяГруппы].ПодчиненныеЭлементы Цикл
		МассивКУдалению.Добавить(Элемент);
	КонецЦикла;
	
	Для каждого Элемент Из МассивКУдалению Цикл
		Элементы.Удалить(Элемент);
	КонецЦикла;

	Для ИД = 1 По МаксимальноеКоличествоРезультатовПоиска Цикл
		Команда = Команды.Найти("КомандаРезультатПоиска" + Строка(ИД));
		Если Команда <> Неопределено Тогда
			Команды.Удалить(Команда);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОткрытьСтатьюПоАдресу(Команда)

	НайтиОткрытьСсылку("РезультатыПоиска", "ИмяКоманды", Команда.Имя);
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьСсылкуРаздела(ИмяРаздела)
	
	НайтиОткрытьСсылку("СсылкиПоРазделам", "ИмяРаздела", ИмяРаздела);
	
КонецПроцедуры

&НаКлиенте
Процедура НайтиОткрытьСсылку(ИмяТаблицы, ИмяКолонки, Значение)
	
	Отбор = Новый Структура(ИмяКолонки, Значение);
	АдресСтраницы = ЭтотОбъект[ИмяТаблицы].НайтиСтроки(Отбор);
	Если АдресСтраницы = Неопределено Тогда
		Адрес = ""
	Иначе
		Адрес = АдресСтраницы[0].АдресСтраницы;
	КонецЕсли;
	
	Если Не ПустаяСтрока(Адрес) Тогда
		ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку(Адрес);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СтрокаПоискаПриИзменении(Элемент)
	
	Элементы.СтрокаПоиска.КнопкаОчистки = Не ПустаяСтрока(СтрокаПоиска);
	
КонецПроцедуры

&НаКлиенте
Функция ШаблонСообщенияВПоддержку();
	
	ТекстСообщения = НСтр("ru = 'Уважаемые пользователи!
	|
	|Чтобы ускорить решение проблемы и улучшить качество обслуживания, просим заполнить следующие данные:
	|
	|1. Описание симптомов возникновения ошибки/проблемы
	|• Шаги воспроизведения:
	|
	|• Ошибочный результат:
	|
	|• Ожидаемый результат:
	|
	|2. Воспроизводится ли в тонком клиенте (да/нет/не знаю):
	|
	|3. Описание действий для обхода или устранения ошибки/проблемы (если есть):
	|
	|
	|Мы благодарим вас за понимание и сотрудничество.
	|
	|С уважением,
	|Команда поддержки
	|
	|PS: Просим не удалять из темы сообщения текст ""Сообщение из раздела Помощь"", так как он нужен нам для статистики.'");
	
	Возврат ТекстСообщения;
	
КонецФункции

&НаКлиенте
Процедура БаннерКартинкаНажатие(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку("https://1c.link/to/EI2muI3");
	
КонецПроцедуры

#КонецОбласти


#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Параметры.Свойство( "УчетнаяЗаписьМаркетплейса", УчетнаяЗаписьМаркетплейса );
	Параметры.Свойство( "ВариантЗагрузкиДанных", ВариантЗагрузкиДанных );

	Параметры.Свойство( "ИмяИдентификатора", ИмяИдентификатора );
	Если Не ЗначениеЗаполнено( ИмяИдентификатора ) Тогда
		ИмяИдентификатора = "ИдентификаторОбъектаМаркетплейса";
	КонецЕсли;

	Параметры.Свойство( "РазмерПорцииЗагрузкиДанных", РазмерПорцииЗагрузкиДанных );
	
	Если ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда 
		Если Не ЗначениеЗаполнено( РазмерПорцииЗагрузкиДанных ) Тогда
			РазмерПорцииЗагрузкиДанных = 100;
		КонецЕсли;
	КонецЕсли;
	
	Параметры.Свойство( "СоздатьЭлементыНоменклатуры", СоздатьЭлементыНоменклатуры );
	Параметры.Свойство( "ПерезаполнитьЗначенияРеквизитовНоменклатуры", ПерезаполнитьЗначенияРеквизитовНоменклатуры );
	Параметры.Свойство( "СоздатьИерархиюНоменклатуры", СоздатьИерархиюНоменклатуры );
	Параметры.Свойство( "СоздатьХарактеристикиНоменклатуры", СоздатьХарактеристикиНоменклатуры );
	
	Параметры.Свойство( "СоздатьКатегорииНоменклатуры", СоздатьКатегорииНоменклатуры);
	Параметры.Свойство( "СоздатьИерархиюКатегорийНоменклатуры", СоздатьИерархиюКатегорийНоменклатуры );
	
	Параметры.Свойство( "ЗагрузитьИзображения", ЗагрузитьИзображения );	
	Параметры.Свойство( "ЗагрузитьЦеныПродажи", ЗагрузитьЦеныПродажи );
	
	Параметры.Свойство( "ЗагрузитьОписанияАрхивныхПозиций", ЗагрузитьОписанияАрхивныхПозиций );
	Параметры.Свойство( "СортироватьСтрокиДереваТоваров", СортироватьСтрокиДереваТоваров );
	
	
	ДанныеВыделенныхСтрок = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( Параметры, "ДанныеВыделенныхСтрок", Новый Массив );
	Если ТипЗнч( ДанныеВыделенныхСтрок ) <> Тип( "Массив" ) Тогда
		ДанныеВыделенныхСтрок = Новый Массив;
	КонецЕсли;
	
	ЗаписатьЦеныОтдельнойКомандой = Ложь;
	
	ИзменитьРеквизитыФормы();
    
	Если ДанныеВыделенныхСтрок.Количество() > 0 Тогда 
		СтрокаДереваГруппаОбновить = ИмпортируемыеТовары.ПолучитьЭлементы().Добавить();
		СтрокаДереваГруппаОбновить.ВариантЗагрузки = 0;
		СтрокаДереваГруппаОбновить.ИндексСтатуса = -2;

		Для каждого КлючЗаписи Из ДанныеВыделенныхСтрок Цикл
			Запись = РегистрыСведений.СтатусыПубликацииОбъектовМаркетплейсаOzon.СоздатьМенеджерЗаписи();
			ЗаполнитьЗначенияСвойств(Запись, КлючЗаписи);
			Запись.Прочитать();
			
			Если Запись.Выбран() Тогда
				СтрокаДереваВторогоУровня = СтрокаДереваГруппаОбновить.ПолучитьЭлементы().Добавить();
				ЗаполнитьЗначенияСвойств(СтрокаДереваВторогоУровня, Запись,, "ДанныеОбъекта, ИсточникиСхожихПредложений");
				
				СтрокаДереваВторогоУровня.Пометка = Истина;
				СтрокаДереваВторогоУровня.ВариантЗагрузки = 0;
				СтрокаДереваВторогоУровня.ИндексСтатуса = 5;
				СтрокаДереваВторогоУровня.ПродаетсяПоСхемеРаботыFBO = Истина;

				Если ИнтеграцияСМаркетплейсомOzonКлиентСервер.КатегориияМаркетплейсаУстарела( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса ) Тогда
					СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса = СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса
					+ ИнтеграцияСМаркетплейсомOzonКлиентСервер.ПостфиксУстаревшейКатегории();
				КонецЕсли;
				
				ДанныеОбъекта = Запись.ДанныеОбъекта.Получить();
				Если ДанныеОбъекта <> Неопределено Тогда
					СтрокаДереваВторогоУровня.ДанныеОбъекта.Загрузить(ДанныеОбъекта);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;

	ЭтаФорма.ИсточникКатегории = ОбщегоНазначения.ЗначениеРеквизитаОбъекта( УчетнаяЗаписьМаркетплейса, "ИсточникКатегории" );
	
	Если ЭтаФорма.ИсточникКатегории = ПредопределенноеЗначение("Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры") Тогда
		Элементы.ИмпортируемыеТоварыКатегорияИлиНоменклатураДляСопоставленияКатегорииOzon.Заголовок = НСтр("ru = 'Категория для сопоставления с категорией Ozon'");
		Элементы.ИмпортируемыеТоварыКатегорияИлиНоменклатураДляСопоставленияКатегорииOzon.ОграничениеТипа = Новый ОписаниеТипов("СправочникСсылка.КатегорииНоменклатуры");
	ИначеЕсли ЭтаФорма.ИсточникКатегории = ПредопределенноеЗначение("Перечисление.ИсточникиКатегорийДляМаркетплейса.ИерархияНоменклатуры") Тогда
		Элементы.ИмпортируемыеТоварыКатегорияИлиНоменклатураДляСопоставленияКатегорииOzon.Заголовок = НСтр("ru = 'Номенклатура или группа для сопоставления с категорией Ozon'");
		Элементы.ИмпортируемыеТоварыКатегорияИлиНоменклатураДляСопоставленияКатегорииOzon.ОграничениеТипа = Новый ОписаниеТипов("СправочникСсылка.Номенклатура");
	КонецЕсли;
	
	ОтображатьСопоставленные = 0;
	ОтображатьПоСтатусу = -1;
	ОбновитьКнопкуОтбораПоСтатусу("ОтобратьПоСтатусуВсе");
	
	ЭтаФорма.Заголовок = НСтр("ru = 'Импорт описаний товаров с торговой площадки Ozon'") + ", " + СокрЛП( УчетнаяЗаписьМаркетплейса );
	УстановитьУсловноеОформление();

	Если ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда 
		Элементы.ГруппаИмпортДанных.Видимость = Ложь;
	КонецЕсли;
	
	Если ЭтаФорма.СоздатьЭлементыНоменклатуры = Ложь Тогда
		ЭтаФорма.ПерезаполнитьЗначенияРеквизитовНоменклатуры = Ложь;
		ЭтаФорма.СоздатьИерархиюНоменклатуры = Ложь;
		ЭтаФорма.СоздатьХарактеристикиНоменклатуры = Ложь;
		ЭтаФорма.СоздатьКатегорииНоменклатуры = Ложь;
		ЭтаФорма.СоздатьИерархиюКатегорийНоменклатуры = Ложь;
		ЭтаФорма.ЗагрузитьИзображения = Ложь;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ВариантЗагрузкиДанныхПриИзменении(Неопределено);
	
	Если ИмпортируемыеТовары.ПолучитьЭлементы().Количество() > 0 Тогда
		ПодключитьОбработчикОжидания( "ОбновитьСлужебныеДанные", 0.1, Истина);
	КонецЕсли;
	
	ФормаРежимОтображенияЭлементыОбновить( ИнтеграцияСМаркетплейсомOzonКлиентСервер.ФормаРежимОтображенияОткрытие() );
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)

	Если ЗавершениеРаботы Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗакрытьФормуОбработки Тогда
		Отказ = Истина;

		ОповещениеОЗавершении = Новый ОписаниеОповещения("ПередЗакрытиемЗавершение", ЭтотОбъект,);
		ПоказатьВопрос(ОповещениеОЗавершении, 
			НСтр("ru = 'Закрыть форму?'"), 
			РежимДиалогаВопрос.ДаНет, 
			, 
			КодВозвратаДиалога.Нет, 
			НСтр("ru = 'Импорт данных'"), 
			КодВозвратаДиалога.Нет);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытиемЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		ЗакрытьФормуОбработки = Истина;
		Закрыть();
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии(ЗавершениеРаботы)
	
	Если ЗавершениеРаботы Тогда
		Возврат;
	КонецЕсли;
	
	Если ЭтоАдресВременногоХранилища(АдресИзображения) Тогда
		УдалитьИзВременногоХранилища(АдресИзображения);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)

	Если ИмяСобытия = "УчетнаяЗаписьДеактивирована" Тогда
		Если Параметр = УчетнаяЗаписьМаркетплейса Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(НСтр("ru = 'Учетная запись маркетплейса удалена. Дальнейшее использование обработки недоступно.'"));
            ТолькоПросмотр = Истина;
			УстановитьВидимостьДоступность();
			ФормаПараметрыИмпортаДанныхОписаниеОбновить();
		КонецЕсли;

	ИначеЕсли ИмяСобытия = "Запись_УчетныеЗаписиМаркетплейсов" Тогда
		Если Параметр = УчетнаяЗаписьМаркетплейса Тогда
			УстановитьВидимостьДоступность();
			ФормаПараметрыИмпортаДанныхОписаниеОбновить();
		КонецЕсли;
		
	ИначеЕсли ИмяСобытия = "ИсточникКатегорииИзменен" Тогда
		Если УчетнаяЗаписьМаркетплейса = ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(Параметр, "УчетнаяЗаписьМаркетплейса", ПредопределенноеЗначение("Справочник.УчетныеЗаписиМаркетплейсов.ПустаяСсылка")) Тогда
			ОбновитьКатегорииТоваров(ОбщегоНазначенияКлиентСервер.СвойствоСтруктуры(Параметр, "ИсточникКатегории", ПредопределенноеЗначение("Перечисление.ИсточникиКатегорийДляМаркетплейса.ПустаяСсылка")));
		КонецЕсли; 
		
	ИначеЕсли ИмяСобытия = "Запись_Номенклатура" Тогда
		Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
			Для каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.ПолучитьЭлементы() Цикл
				Если СтрокаДереваВторогоУровня.Номенклатура = Источник Тогда
					ИмпортируемыеТоварыНоменклатураПриИзменении(СтрокаДереваВторогоУровня);
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
		
	ИначеЕсли ИмяСобытия = "Запись_СтатусыПубликацииОбъектовМаркетплейсаOzon" Тогда
		Если Параметр.Свойство("ИсходныйКлючЗаписи")
			 И Источник.УчетнаяЗаписьМаркетплейса = УчетнаяЗаписьМаркетплейса Тогда
			ОбновитьСтрокуДереваТоваров(Параметр.ИсходныйКлючЗаписи, Источник);
		КонецЕсли;
		
	ИначеЕсли ИмяСобытия = "OzonЗаписаныСопоставленияКатегорий" Тогда
		Если ТипЗнч( Параметр ) = Тип( "ДанныеФормыКоллекция" ) Тогда
			
			СопоставлениеКатегорий = Параметр;
			ИмпортируемыеТоварыКатегорииПоСопоставлениюЗаполнить( СопоставлениеКатегорий );

		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ВариантЗагрузкиДанныхПриИзменении(Элемент)

	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		Если СтрокаДереваПервогоУровня.ВариантЗагрузки = 0 И ВариантЗагрузкиДанных = 2
			 ИЛИ СтрокаДереваПервогоУровня.ВариантЗагрузки = 2 И ВариантЗагрузкиДанных = 0 
			 ИЛИ СтрокаДереваПервогоУровня.ВариантЗагрузки = 3 Тогда
			 
			СтрокаДереваПервогоУровня.Пометка = Ложь;
			УстановитьСнятьПометки(СтрокаДереваПервогоУровня, СтрокаДереваПервогоУровня.Пометка);
			Элементы.ИмпортируемыеТовары.Свернуть(СтрокаДереваПервогоУровня.ПолучитьИдентификатор());
			
		ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 0 И ВариантЗагрузкиДанных < 2
			 	  ИЛИ СтрокаДереваПервогоУровня.ВариантЗагрузки = 2 И ВариантЗагрузкиДанных > 0 Тогда
			 	  
			Элементы.ИмпортируемыеТовары.Развернуть(СтрокаДереваПервогоУровня.ПолучитьИдентификатор());
			
		КонецЕсли;
	КонецЦикла;
	
	ОпределитьНаименованияГруппДереваТоваров();
	УстановитьВидимостьДоступность();
	ФормаПараметрыИмпортаДанныхОписаниеОбновить();

КонецПроцедуры

&НаКлиенте
Процедура ОтображатьСопоставленныеПриИзменении(Элемент)
	
	ОпределитьНаименованияГруппДереваТоваров();
	
КонецПроцедуры

&НаКлиенте
Процедура ОтображатьДанныеТовараПриИзменении(Элемент)

	УстановитьВидимостьДоступность();

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыИмпортируемыеТовары

&НаКлиенте
Процедура ИмпортируемыеТоварыВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	ТекущиеДанные = Элемент.ТекущиеДанные;
	
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Поле.Имя = "ИмпортируемыеТоварыНаименованиеКатегорииМаркетплейса" Тогда
		
		СтандартнаяОбработка = Ложь;
		СопоставитьКатегорииТовара(ТекущиеДанные);
	
	ИначеЕсли Поле.Имя = "ИмпортируемыеТоварыИдентификаторОбъектаМаркетплейса" 
	И ЗначениеЗаполнено( ТекущиеДанные.ИдентификаторОбъектаМаркетплейса )  Тогда

	    СтандартнаяОбработка = Ложь;
	    URL = СтрШаблон( "https://seller.ozon.ru/app/products/%1/edit/preview", ТекущиеДанные.ИдентификаторОбъектаМаркетплейса );
	    ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку( URL );

	ИначеЕсли Поле.Имя = "ИмпортируемыеТоварыИдентификаторПубликации" 
	И ЗначениеЗаполнено( ТекущиеДанные.ИдентификаторПубликации )  Тогда

	    СтандартнаяОбработка = Ложь;
	    URL = СтрШаблон( "https://seller.ozon.ru/app/products?search=%1&filter=all", ТекущиеДанные.ИдентификаторПубликации );
	    ФайловаяСистемаКлиент.ОткрытьНавигационнуюСсылку( URL );
	
	ИначеЕсли Поле.Имя = "ИмпортируемыеТоварыРезультатПоискаКнопкаВыбрать" Тогда
		
		Если ЗначениеЗаполнено( ТекущиеДанные.РезультатПоискаНоменклатура )
		Или ЗначениеЗаполнено( ТекущиеДанные.РезультатПоискаХарактеристика ) Тогда
			ТекущиеДанные.Номенклатура = ТекущиеДанные.РезультатПоискаНоменклатура;
			ТекущиеДанные.Характеристика = ТекущиеДанные.РезультатПоискаХарактеристика;
			
			НоменклатураЗначенияРеквизитов = ЗначенияРеквизитовОбъекта( ТекущиеДанные.Номенклатура, "ИспользоватьХарактеристики, КатегорияНоменклатуры" );
			ТекущиеДанные.ХарактеристикиИспользуются = НоменклатураЗначенияРеквизитов.ИспользоватьХарактеристики;
			ТекущиеДанные.Категория = НоменклатураЗначенияРеквизитов.КатегорияНоменклатуры;
			
			ТекущиеДанные.Пометка = Истина;
		КонецЕсли;
		
	ИначеЕсли Поле.Имя = "ИмпортируемыеТоварыРезультатПоискаНоменклатура"
	 ИЛИ Поле.Имя = "ИмпортируемыеТоварыРезультатПоискаХарактеристика"
	 ИЛИ Поле.Имя = "ИмпортируемыеТоварыРезультатПоискаКоличество"
	 ИЛИ Поле.Имя = "ИмпортируемыеТоварыРезультатПоискаВероятностьСовпадения"
	Тогда
		
		Если ТекущиеДанные.РезультатыПоиска.Количество() > 0 Тогда
			РезультатПоискаВыбратьЭлемент( ТекущиеДанные );
		КонецЕсли;
		
	ИначеЕсли ТекущиеДанные.ВариантЗагрузки <> 2 
			  И ТекущиеДанные.ИндексСтатуса >= 0 
			  И Поле.Имя <> "ИмпортируемыеТоварыСтавкаНДС" Тогда
		
		СтандартнаяОбработка = Ложь;
		
		Если Поле.Имя <> "ИмпортируемыеТоварыНоменклатура" 
			 И Поле.Имя <> "ИмпортируемыеТоварыХарактеристика" 
			 И Поле.Имя <> "ИмпортируемыеТоварыУпаковка" 
			 И Поле.Имя <> "ИмпортируемыеТоварыКатегория" 
			 И Поле.Имя <> "ИмпортируемыеТоварыКатегорияИлиНоменклатураДляСопоставленияКатегорииOzon" 
			 И Поле.Имя <> "ИмпортируемыеТоварыИсточникКатегорииМаркетплейса" Тогда
			 
			Измерения = Новый Структура;
			Измерения.Вставить("УчетнаяЗаписьМаркетплейса", УчетнаяЗаписьМаркетплейса);
			Измерения.Вставить("ВидОбъектаМаркетплейса",    ПредопределенноеЗначение("Перечисление.ВидыОбъектовМаркетплейсов.Товар"));
			Измерения.Вставить("Номенклатура",              ТекущиеДанные.Номенклатура);
			Измерения.Вставить("Характеристика",            ТекущиеДанные.Характеристика);
			Измерения.Вставить("Упаковка",                  ТекущиеДанные.Упаковка);
			Измерения.Вставить("ОбъектПубликации",          Неопределено);

			ПараметрыЗаписи = Новый Массив;
			ПараметрыЗаписи.Добавить(Измерения);

			КлючЗаписи = Новый(Тип("РегистрСведенийКлючЗаписи.СтатусыПубликацииОбъектовМаркетплейсаOzon"), ПараметрыЗаписи);

			ПараметрыОткрытия = Новый Структура;
			ПараметрыОткрытия.Вставить("Ключ", КлючЗаписи);

			ОткрытьФорму("РегистрСведений.СтатусыПубликацииОбъектовМаркетплейсаOzon.ФормаЗаписи", ПараметрыОткрытия);
			
		Иначе
			ПоказатьЗначение(, ТекущиеДанные[СтрЗаменить(Поле.Имя, "ИмпортируемыеТовары", "")]);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РезультатПоискаВыбратьЭлементПослеВыбора( РезультатВыбора, ДополнительныйПараметр ) Экспорт
	
	ТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если РезультатВыбора = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ТипЗнч( РезультатВыбора ) <> Тип( "Структура" ) Тогда
		Возврат;
	КонецЕсли;
	
	ТекущиеДанные.Номенклатура = РезультатВыбора.Номенклатура;
	ТекущиеДанные.Характеристика = РезультатВыбора.Характеристика;
	
КонецПроцедуры

&НаКлиенте
Асинх Процедура РезультатПоискаВыбратьЭлемент( ТекущиеДанные )
    
    РезультатыПоиска = ТекущиеДанные.РезультатыПоиска;
    ПараметрыФормы = Новый Структура;
    ПараметрыФормы.Вставить( "Товары", РезультатыПоиска );
    ПараметрыФормы.Вставить( "ЗаголовокФормы",  "Выбор из результатов поиска" );
    ОповещениеПослеЗакрытия = Новый ОписаниеОповещения( "РезультатПоискаВыбратьЭлементПослеВыбора", ЭтотОбъект );
    ОткрытьФорму( "Обработка.МаркетплейсOzon.Форма.ФормаВыборТоваровИзРезультатовПоиска", ПараметрыФормы, ЭтотОбъект, , , , ОповещениеПослеЗакрытия, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца );

КонецПроцедуры

&НаКлиенте
Процедура ФормаОбновитьСтатусЗагрузкиТовара( ТекущиеДанные )

	Если ТекущиеДанные.ИндексСтатуса < 0 Тогда
		Если ТекущиеДанные.ПолучитьРодителя() = Неопределено Тогда
			Если ТекущиеДанные.Пометка Тогда
				СтатусЗагрузкиТовара = НСтр("ru = 'Ожидается обработка отмеченных строк товаров.'");
			Иначе
				СтатусЗагрузкиТовара = НСтр("ru = 'Ожидается выбор строк товаров для обработки.'");
			КонецЕсли;
		Иначе
			Если ТекущиеДанные.Пометка Тогда
				СтатусЗагрузкиТовара = НСтр("ru = 'Ожидается получение данных с торговой площадки.'");
			Иначе
				СтатусЗагрузкиТовара = НСтр("ru = 'Не выполнялось получение данных с торговой площадки.'");
			КонецЕсли;
		КонецЕсли;
	ИначеЕсли ТекущиеДанные.ИндексСтатуса = 0 Тогда
		СтатусЗагрузкиТовара = НСтр("ru = 'Товар загружен с ошибками, требующими исправления.'");
	ИначеЕсли ТекущиеДанные.ИндексСтатуса = 1 Тогда
		СтатусЗагрузкиТовара = НСтр("ru = 'Товар загружен и находится на модерации.'");
	ИначеЕсли ТекущиеДанные.ИндексСтатуса = 2 Тогда
		СтатусЗагрузкиТовара = НСтр("ru = 'Товар загружен и находится в архиве.'");
	ИначеЕсли ТекущиеДанные.ИндексСтатуса = 3 Тогда
		СтатусЗагрузкиТовара = НСтр("ru = 'Товар загружен и готов к продаже.'");
	ИначеЕсли ТекущиеДанные.ИндексСтатуса = 4 Тогда
		Если ЭтаФорма.ИмяИдентификатора = "ИдентификаторПубликации" Тогда
			СтатусЗагрузкиТовара = НСтр("ru = 'Товар не может быть обновлен, т.к. не найден на торговой площадке по идентификатору 1С.'");
		Иначе
			СтатусЗагрузкиТовара = НСтр("ru = 'Товар не может быть обновлен, т.к. не найден на торговой площадке по идентификатору Ozon.'");
		КонецЕсли;
	ИначеЕсли ТекущиеДанные.ИндексСтатуса = 5 Тогда
		СтатусЗагрузкиТовара = НСтр("ru = 'Товар ожидает получения данных с торговой площадки.'");
	ИначеЕсли ТекущиеДанные.ИндексСтатуса = 6 Тогда
		Если ТекущиеДанные.ВариантЗагрузки = 2 Тогда
			Если НЕ ЗначениеЗаполнено(ТекущиеДанные.Номенклатура) Тогда
				СтатусЗагрузкиТовара = НСтр("ru = 'Требуется выполнить сопоставление данным учетной системы 1С.'");
			Иначе
				СтатусЗагрузкиТовара = НСтр("ru = 'Доступна загрузка нового товара из торговой площадки.'");
			КонецЕсли;
		Иначе
			Если ТекущиеДанные.ЕстьРазличияСтавкаНДС Тогда
				СтатусЗагрузкиТовара = НСтр("ru = 'Доступна загрузка товара. Для товара обнаружены расхождения в данных 1С и торговой площадки. При загрузке будут заменены текущие данные товара.'")
					+ " " + НСтр("ru = 'Различаются значения ставки НДС - внесите исправления вручную.'");
			Иначе
				СтатусЗагрузкиТовара = НСтр("ru = 'Доступна загрузка товара. Для товара обнаружены расхождения в данных 1С и торговой площадки. При загрузке будут заменены текущие данные товара.'");
			КонецЕсли;
		КонецЕсли;
	ИначеЕсли ТекущиеДанные.ИндексСтатуса = 7 Тогда
		Если ТекущиеДанные.ЕстьРазличияСтавкаНДС Тогда
			СтатусЗагрузкиТовара = НСтр("ru = 'Товар не требует загрузки.'")
				+ " " + НСтр("ru = 'Различаются значения ставки НДС - внесите исправления вручную.'");
		Иначе
			СтатусЗагрузкиТовара = НСтр("ru = 'Товар не требует загрузки, т.к. не обнаружены расхождения в данных.'");
		КонецЕсли;
	КонецЕсли;
	
	Если ТекущиеДанные.ИндексСтатуса >= 6 
		 И ТекущиеДанные.ЕстьРазличияЦеновыеПоказатели Тогда
		СтатусЗагрузкиТовара = СтатусЗагрузкиТовара
			+ " " + НСтр("ru = 'Требуется загрузка цен продажи с торговой площадки.'");
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТекущиеДанные.НаименованиеКатегорииМаркетплейса) Тогда
		СтатусЗагрузкиТовара = СтатусЗагрузкиТовара
			+ " " + НСтр("ru = 'Категория маркетплейса не определена.'");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ТекущиеДанные.Номенклатура) 
		 И НЕ ЗначениеЗаполнено(ТекущиеДанные.ЕдиницаИзмерения) Тогда
		СтатусЗагрузкиТовара = СтатусЗагрузкиТовара
			+ " " + НСтр("ru = 'Проверьте и заполните карточку номенклатуры.'");
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыПриАктивизацииСтроки(Элемент)

	ТекущиеДанные = Элемент.ТекущиеДанные;
	
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ФормаОбновитьСтатусЗагрузкиТовара( ТекущиеДанные );
	
	ТаблицаИмпортированныхТоваровИзображенияПриАктивизацииСтроки(Элементы.ТаблицаИмпортированныхТоваровИзображения);

КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	
	Отказ = Истина;
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыПередНачаломИзменения(Элемент, Отказ)
	
	ТекущиеДанные = Элемент.ТекущиеДанные;
	
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыПередУдалением(Элемент, Отказ)
	
	Отказ = Истина;
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыПометкаПриИзменении(Элемент)

	ТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;

	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьСнятьПометки( ТекущиеДанные, ТекущиеДанные.Пометка );
	ОпределитьНаименованияГруппДереваТоваров(ТекущиеДанные.ПолучитьРодителя());

КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗначениеРеквизитаОбъекта( Ссылка, ИмяРеквизита, ВыбратьРазрешенные = Ложь, Знач КодЯзыка = Неопределено )
	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта( Ссылка, ИмяРеквизита, ВыбратьРазрешенные, КодЯзыка);
КонецФункции

&НаСервереБезКонтекста
Функция ЗначенияРеквизитовОбъекта(Ссылка, Знач Реквизиты, ВыбратьРазрешенные = Ложь, Знач КодЯзыка = Неопределено)
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Ссылка, Реквизиты, ВыбратьРазрешенные, КодЯзыка );
КонецФункции

&НаСервереБезКонтекста
Функция ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоНоменклатуре(УчетнаяЗаписьМаркетплейса, Номенклатура, ИдентификаторКатегории, ИсточникКатегории)
	Возврат ИнтеграцияСМаркетплейсомOzonСервер.ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоНоменклатуре(УчетнаяЗаписьМаркетплейса, 
	Номенклатура, ИдентификаторКатегории, ИсточникКатегории);
КонецФункции

&НаКлиенте
Процедура ИмпортируемыеТоварыНоменклатураПриИзменении(Элемент)
	
	Если ТипЗнч(Элемент) = Тип("ДанныеФормыЭлементДерева") Тогда
		ТекущиеДанные = Элемент;
		
		ВыполнитьЗаполнениеПоОбъединению = Ложь;
	Иначе
		ТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;
		
		ВыполнитьЗаполнениеПоОбъединению = Истина;
	КонецЕсли;

	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Если ТекущиеДанные.ИндексСтатуса = -2 Тогда
		Возврат;
	КонецЕсли;
	
	НоменклатураЗначенияРеквизитов = ЗначенияРеквизитовОбъекта(ТекущиеДанные.Номенклатура, "ЕдиницаИзмерения, ИспользоватьХарактеристики, КатегорияНоменклатуры, Родитель" );
	Если ТекущиеДанные.ВариантЗагрузки = 2 Тогда
		
		ТекущаяКатегория = ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоНоменклатуре(
			УчетнаяЗаписьМаркетплейса,
			ТекущиеДанные.Номенклатура,
			ТекущиеДанные.ИдентификаторКатегорииМаркетплейса,
			ИсточникКатегории );
			
		ТекущиеДанные.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon = ТекущаяКатегория.Категория;
		ТекущиеДанные.ИсточникКатегорииМаркетплейса = ТекущаяКатегория.ИсточникКатегории;
		ТекущиеДанные.ТекущееИдентификаторКатегорииМаркетплейса = ТекущаяКатегория.ИдентификаторКатегории;
		ТекущиеДанные.ТекущееНаименованиеКатегорииМаркетплейса = ТекущаяКатегория.НаименованиеКатегории;
	КонецЕсли;
		
	ТекущиеДанные.Категория = НоменклатураЗначенияРеквизитов.КатегорияНоменклатуры;
	ТекущиеДанные.ЕдиницаИзмерения = НоменклатураЗначенияРеквизитов.ЕдиницаИзмерения;
	ТекущиеДанные.ХарактеристикиИспользуются = НоменклатураЗначенияРеквизитов.ИспользоватьХарактеристики;
	
	Если ТекущиеДанные.ХарактеристикиИспользуются = Ложь Тогда
		ТекущиеДанные.Характеристика = Неопределено;
	КонецЕсли;
	
	Если ТекущиеДанные.ДеревоРеквизитов.ПолучитьЭлементы().Количество() > 0 Тогда
		Для каждого ЭлементКоллекции Из ТекущиеДанные.ДеревоРеквизитов.ПолучитьЭлементы()[0].ПолучитьЭлементы() Цикл
			Если ЭлементКоллекции.ИдентификаторАтрибутаМаркетплейса = "СтавкаНДС" Тогда
				ЭлементКоллекции.ТекущееЗначениеАтрибутаМаркетплейса = ЗначениеРеквизитаОбъекта(ТекущиеДанные.СтавкаНДС, "Ставка");
				ЭлементКоллекции.ЕстьРазличия = (ЭлементКоллекции.ТекущееЗначениеАтрибутаМаркетплейса <> ЭлементКоллекции.ЗначениеАтрибутаМаркетплейса);
				ТекущиеДанные.Ставка = ЭлементКоллекции.ТекущееЗначениеАтрибутаМаркетплейса;
				ТекущиеДанные.ЕстьРазличияСтавкаНДС = ЭлементКоллекции.ЕстьРазличия;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
    
    Если ВыполнитьЗаполнениеПоОбъединению = Истина Тогда
		ИмпортируемыеТоварыПоОбъединениюНоменклатураЗаполнить();
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыНоменклатураСоздание(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;

	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ДополнительныеПараметры = Новый Структура;	
	ДополнительныеПараметры.Вставить( "ТипНоменклатуры",    ПредопределенноеЗначение( "Перечисление.ТипыНоменклатуры.Запас" ) );
	ДополнительныеПараметры.Вставить( "Наименование",       ТекущиеДанные.ПредставлениеОбъектаМаркетплейса );
	ДополнительныеПараметры.Вставить( "НаименованиеПолное", ТекущиеДанные.ПредставлениеОбъектаМаркетплейса );
	ДополнительныеПараметры.Вставить( "Описание",           ТекущиеДанные.ОписаниеОбъектаМаркетплейса );
	ДополнительныеПараметры.Вставить( "Артикул",            ТекущиеДанные.ИдентификаторПубликации );
	
	ПараметрыРасчетаСтавкиНДС = ОпределитьПараметрыРасчетаСтавкиНДС(УчетнаяЗаписьМаркетплейса);
	
	Если ТекущиеДанные.ДанныеТовараИзСервиса.Количество() > 0 Тогда
		ДополнительныеПараметры.Вставить("Штрихкод", ТекущиеДанные.ДанныеТовараИзСервиса[0].Штрихкод);
	КонецЕсли;
	
	Если ЗначениеЗаполнено( ТекущиеДанные.Категория ) Тогда
		
		Если ИсточникКатегории = ПредопределенноеЗначение("Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры") Тогда
		
			ДополнительныеПараметры.Вставить( "ВидНоменклатуры", ТекущиеДанные.Категория );
			ДополнительныеПараметры.Вставить( "ТипНоменклатуры", ЗначениеРеквизитаОбъекта( ТекущиеДанные.Категория, "ТипНоменклатурыПоУмолчанию") );
			
		ИначеЕсли ЗначениеРеквизитаОбъекта( ТекущиеДанные.Категория, "ЭтоГруппа" ) Тогда
		
			ДополнительныеПараметры.Вставить("Родитель", ТекущиеДанные.Категория);
			
		КонецЕсли;
	КонецЕсли;
	
	ПараметрыСоздания = Новый Структура;
	ПараметрыСоздания.Вставить( "ЗначенияЗаполнения", ДополнительныеПараметры );
	ПараметрыСоздания.Вставить( "РежимВыбора", Истина);

	ОткрытьФорму( "Справочник.Номенклатура.ФормаОбъекта", ПараметрыСоздания, Элемент );
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ПолучитьТекущийИсточникКатегорииМаркетплейса(УчетнаяЗаписьМаркетплейса, Категория1С, БлижайшийРодитель = Истина)
	Возврат ИнтеграцияСМаркетплейсомOzonСервер.ПолучитьТекущийИсточникКатегорииМаркетплейса(УчетнаяЗаписьМаркетплейса, Категория1С, БлижайшийРодитель);
КонецФункции

&НаКлиенте
Процедура ИмпортируемыеТоварыКатегорияПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;

	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ТекущиеДанные.ИсточникКатегорииМаркетплейса = ТекущиеДанные.Категория;
	
	Пока Истина Цикл
		ВладелецКатегорииМаркетплейса = ПолучитьТекущийИсточникКатегорииМаркетплейса(
			УчетнаяЗаписьМаркетплейса, 
			ТекущиеДанные.ИсточникКатегорииМаркетплейса,
			Ложь);
		
		Если ЗначениеЗаполнено(ВладелецКатегорииМаркетплейса.ИсточникКатегорииМаркетплейса) Тогда
			Если ТекущиеДанные.ИдентификаторКатегорииМаркетплейса = ВладелецКатегорииМаркетплейса.ИдентификаторИсточникаКатегорииМаркетплейса Тогда
				ТекущиеДанные.ИсточникКатегорииМаркетплейса = ВладелецКатегорииМаркетплейса.ИсточникКатегорииМаркетплейса;
				Прервать;
			КонецЕсли;
			
			ТекущиеДанные.ИсточникКатегорииМаркетплейса = ЗначениеРеквизитаОбъекта(ВладелецКатегорииМаркетплейса.ИсточникКатегорииМаркетплейса, "Родитель");
			
		Иначе 
			ТекущиеДанные.ИсточникКатегорииМаркетплейса = ТекущиеДанные.Категория;
			Прервать;
		КонецЕсли;
	КонецЦикла;

	Если ЗначениеЗаполнено( ТекущиеДанные.ИдентификаторКатегорииМаркетплейса )
	И ЗначениеЗаполнено( ТекущиеДанные.Категория ) Тогда
		
		Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
			Если СтрокаДереваПервогоУровня.ВариантЗагрузки <> 2 Тогда
				Продолжить;
			КонецЕсли;
			
			Для каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.ПолучитьЭлементы() Цикл
				
				Если СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса = ТекущиеДанные.ИдентификаторКатегорииМаркетплейса 
					И НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория)
					И СтрокаДереваВторогоУровня.ИндексСтатуса <> 7 Тогда
					СтрокаДереваВторогоУровня.Категория = ТекущиеДанные.Категория;
					СтрокаДереваВторогоУровня.ИсточникКатегорииМаркетплейса = ТекущиеДанные.ИсточникКатегорииМаркетплейса;
				КонецЕсли;
				
			КонецЦикла;
		КонецЦикла;
	КонецЕсли;

	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыКатегорияНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)

	ИсточникКатегории = ЗначениеРеквизитаОбъекта(УчетнаяЗаписьМаркетплейса, "ИсточникКатегории");
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыТаблицаИмпортированныхТоваровИзображения

&НаКлиенте
Процедура ТаблицаИмпортированныхТоваровИзображенияПриАктивизацииСтроки(Элемент)

	ТекущиеДанные = Элемент.ТекущиеДанные;
	ИдентификаторИзображения = -1;
	
	Если ОтображатьДанныеТовара Тогда
		Если ТекущиеДанные <> Неопределено Тогда
			ИдентификаторИзображения = ТекущиеДанные.ПолучитьИдентификатор();
		КонецЕсли;
		ПодключитьОбработчикОжидания("Подключаемый_ЗагрузитьИзображение", 0.1, Истина);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура УстановитьФлажки(Команда)

	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		УстановитьСнятьПометки(СтрокаДереваПервогоУровня, Истина);
	КонецЦикла;
	
	ОпределитьНаименованияГруппДереваТоваров();
	Модифицированность = Ложь;

КонецПроцедуры

&НаКлиенте
Процедура УстановитьФлажкиДляТребующихПолученияДанных(Команда)

	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		УстановитьСнятьПометки(СтрокаДереваПервогоУровня, Истина, "ТребующиеПолученияДанных");
	КонецЦикла;
	
	ОпределитьНаименованияГруппДереваТоваров();
	Модифицированность = Ложь;

КонецПроцедуры

&НаКлиенте
Процедура УстановитьФлажкиДляТребующихЗаписиТоваров(Команда)

	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		УстановитьСнятьПометки(СтрокаДереваПервогоУровня, Истина, ?(ЗагрузитьЦеныПродажи, "ТребующиеЗаписиТоваровИЦен", "ТребующиеЗаписиТоваров"));
	КонецЦикла;
	
	ОпределитьНаименованияГруппДереваТоваров();
	Модифицированность = Ложь;

КонецПроцедуры

&НаКлиенте
Процедура УстановитьФлажкиДляТребующихЗаписиЦен(Команда)

	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		УстановитьСнятьПометки(СтрокаДереваПервогоУровня, Истина, "ТребующиеЗаписиЦен");
	КонецЦикла;
	
	ОпределитьНаименованияГруппДереваТоваров();
	Модифицированность = Ложь;

КонецПроцедуры

&НаКлиенте
Процедура УстановитьФлажкиДляНеСопоставленнойНоменклатуры(Команда)

	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		УстановитьСнятьПометки(СтрокаДереваПервогоУровня, Истина, "НеСопоставленнаяНоменклатура");
	КонецЦикла;
	
	ОпределитьНаименованияГруппДереваТоваров();
	Модифицированность = Ложь;

КонецПроцедуры

&НаКлиенте
Процедура СнятьФлажки(Команда)

	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		УстановитьСнятьПометки(СтрокаДереваПервогоУровня, Ложь);
	КонецЦикла;
	
	ОпределитьНаименованияГруппДереваТоваров();
	Модифицированность = Ложь;

КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуВсе(Команда)
    
    ОтображатьПоСтатусу = -1;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуОжидаютИсправленияОшибок(Команда)
    
    ОтображатьПоСтатусу = 0;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуПодготовленыКПубликации(Команда)
    
    ОтображатьПоСтатусу = 1;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуПеремещеныВАрхив(Команда)
    
    ОтображатьПоСтатусу = 2;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуГотовыКПродаже(Команда)
    
    ОтображатьПоСтатусу = 3;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуОжидаютПолученияДанных(Команда)
    
    ОтображатьПоСтатусу = 5;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуНеНайденыНаПлощадке(Команда)
    
    ОтображатьПоСтатусу = 4;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуОбнаруженыРасхождения(Команда)
    
    ОтображатьПоСтатусу = 6;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ОтобратьПоСтатусуОбновлениеНеТребуется(Команда)
    
    ОтображатьПоСтатусу = 7;
    ОбновитьКнопкуОтбораПоСтатусу(Команда.Имя);
    ОпределитьНаименованияГруппДереваТоваров();
    
КонецПроцедуры

&НаКлиенте
Процедура ПоКнопкеПараметрыИмпортаДанных(Команда)
	
	ПараметрыОткрытия = Новый Структура;
	
	ПараметрыОткрытия.Вставить( "ИмяИдентификатора", ИмяИдентификатора );
	ПараметрыОткрытия.Вставить( "РазмерПорцииЗагрузкиДанных", РазмерПорцииЗагрузкиДанных );
	ПараметрыОткрытия.Вставить( "СортироватьСтрокиДереваТоваров", СортироватьСтрокиДереваТоваров );
	
	ПараметрыОткрытия.Вставить( "УчетнаяЗаписьМаркетплейса", УчетнаяЗаписьМаркетплейса );
	ПараметрыОткрытия.Вставить( "УчетнаяЗапись", УчетнаяЗаписьМаркетплейса );
	ПараметрыОткрытия.Вставить( "ВариантЗагрузкиДанных", ВариантЗагрузкиДанных );
	
	ПараметрыОткрытия.Вставить( "ЗагрузитьЦеныПродажи", ЗагрузитьЦеныПродажи );
	ПараметрыОткрытия.Вставить( "ЗаписатьЦеныОтдельнойКомандой", ЗаписатьЦеныОтдельнойКомандой );
	ПараметрыОткрытия.Вставить( "ЗагрузитьИзображения", ЗагрузитьИзображения );
	ПараметрыОткрытия.Вставить( "ДокументУстановкаЦенНоменклатуры", ДокументУстановкаЦенНоменклатуры );
	
	ПараметрыОткрытия.Вставить( "СоздатьЭлементыНоменклатуры", СоздатьЭлементыНоменклатуры );
	ПараметрыОткрытия.Вставить( "ПерезаполнитьЗначенияРеквизитовНоменклатуры", ПерезаполнитьЗначенияРеквизитовНоменклатуры );
	ПараметрыОткрытия.Вставить( "ГруппаДляСозданияНовыхТоваров", ГруппаДляСозданияНовыхТоваров );
	ПараметрыОткрытия.Вставить( "СоздатьИерархиюНоменклатуры", СоздатьИерархиюНоменклатуры );
	ПараметрыОткрытия.Вставить( "СоздатьХарактеристикиНоменклатуры", СоздатьХарактеристикиНоменклатуры );
	
	ПараметрыОткрытия.Вставить( "СоздатьКатегорииНоменклатуры", СоздатьКатегорииНоменклатуры );
	ПараметрыОткрытия.Вставить( "СоздатьИерархиюКатегорийНоменклатуры", СоздатьИерархиюКатегорийНоменклатуры );
	
	ПараметрыОткрытия.Вставить( "ЗагрузитьОписанияАрхивныхПозиций", ЗагрузитьОписанияАрхивныхПозиций );
	
	ОповещениеОЗакрытии = Новый ОписаниеОповещения( "ПоКнопкеПараметрыИмпортаДанныхЗавершение", ЭтотОбъект );
	ОткрытьФорму( "Обработка.МаркетплейсOzon.Форма.ИмпортДанныхНастройка", ПараметрыОткрытия, ЭтотОбъект,,,, 
	ОповещениеОЗакрытии, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоКнопкеПараметрыИмпортаДанныхЗавершение( РезультатЗакрытия, ДополнительныеПараметры ) Экспорт
	
	Если РезультатЗакрытия <> Неопределено Тогда
	
		ИмяИдентификатора = РезультатЗакрытия.ИмяИдентификатора;
		РазмерПорцииЗагрузкиДанных = РезультатЗакрытия.РазмерПорцииЗагрузкиДанных;
		СортироватьСтрокиДереваТоваров = РезультатЗакрытия.СортироватьСтрокиДереваТоваров;
		
		СоздатьЭлементыНоменклатуры = РезультатЗакрытия.СоздатьЭлементыНоменклатуры;
		ПерезаполнитьЗначенияРеквизитовНоменклатуры = РезультатЗакрытия.ПерезаполнитьЗначенияРеквизитовНоменклатуры;
		ГруппаДляСозданияНовыхТоваров = РезультатЗакрытия.ГруппаДляСозданияНовыхТоваров;
		СоздатьИерархиюНоменклатуры = РезультатЗакрытия.СоздатьИерархиюНоменклатуры;
		СоздатьХарактеристикиНоменклатуры = РезультатЗакрытия.СоздатьХарактеристикиНоменклатуры;
		
		СоздатьКатегорииНоменклатуры = РезультатЗакрытия.СоздатьКатегорииНоменклатуры;
		СоздатьИерархиюКатегорийНоменклатуры = РезультатЗакрытия.СоздатьИерархиюКатегорийНоменклатуры;
		
		ЗагрузитьЦеныПродажи = РезультатЗакрытия.ЗагрузитьЦеныПродажи;
		ЗаписатьЦеныОтдельнойКомандой = РезультатЗакрытия.ЗаписатьЦеныОтдельнойКомандой;
		ДокументУстановкаЦенНоменклатуры = РезультатЗакрытия.ДокументУстановкаЦенНоменклатуры;

		ЗагрузитьИзображения = РезультатЗакрытия.ЗагрузитьИзображения;
		
		ЗагрузитьОписанияАрхивныхПозиций = РезультатЗакрытия.ЗагрузитьОписанияАрхивныхПозиций;

		Если ВариантЗагрузкиДанных <> РезультатЗакрытия.ВариантЗагрузкиДанных Тогда
			ВариантЗагрузкиДанных = РезультатЗакрытия.ВариантЗагрузкиДанных;
			ВариантЗагрузкиДанныхПриИзменении(Неопределено);
		Иначе
			УстановитьВидимостьДоступность();
			ФормаПараметрыИмпортаДанныхОписаниеОбновить();
        КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПолучитьДанныеСТорговойПлощадки(Команда)

	Модифицированность = Ложь;
	ОчиститьСообщения();
	ПоказатьОповещениеПользователя(
		НСтр("ru = 'Импорт данных'"),
		, 
		НСтр("ru = 'Получение данных с торговой площадки Ozon.'"),
		БиблиотекаКартинок.ЛоготипOzon2);
		
	КэшКатегорий = ИнтеграцияСМаркетплейсомOzonКлиент.КэшКатегорий();
	КэшКатегорий.Удалить( "ДеревоКатегорийДерево" );
	
	ДлительнаяОперация = ПолучитьДанныеСТорговойПлощадкиНаСервере( КэшКатегорий, (Команда.Имя = "ПолучитьСледующуюПорциюДанныхСТорговойПлощадки") );
	ОповещениеОЗавершении = Новый ОписаниеОповещения("ПолучитьДанныеСТорговойПлощадкиЗавершениеФоновогоЗадания", ЭтотОбъект,);
	
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
	ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
	ПараметрыОжидания.ТекстСообщения = НСтр("ru = 'Получение данных по товарным позициям с торговой площадки Ozon.'");
	ПараметрыОжидания.ОповещениеПользователя.Показать = Истина;
	ПараметрыОжидания.ОповещениеПользователя.Текст = НСтр("ru = 'Импорт данных'");
	ПараметрыОжидания.ОповещениеПользователя.Пояснение = НСтр("ru = 'Завершено получение данных по товарным позициям с торговой площадки Ozon.'");
	ПараметрыОжидания.ОповещениеПользователя.Картинка = БиблиотекаКартинок.ЛоготипOzon2;
	
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);

КонецПроцедуры

&НаКлиенте
Функция ИмпортируемыеТоварыЕстьПометки()

	ИмпортируемыеТоварыЭлементы = ЭтотОбъект.ИмпортируемыеТовары.ПолучитьЭлементы();
	Для Каждого ИмпортируемыеТоварыЭлемент Из ИмпортируемыеТоварыЭлементы Цикл
		ИмпортируемыеТоварыЭлементыВторогоУровня = ИмпортируемыеТоварыЭлемент.ПолучитьЭлементы();
		Для Каждого ИмпортируемыеТоварыЭлементВторогоУровня Из ИмпортируемыеТоварыЭлементыВторогоУровня Цикл
			Если ИмпортируемыеТоварыЭлементВторогоУровня.Пометка = Истина Тогда
				Возврат Истина;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
	Возврат Ложь;
КонецФункции

&НаКлиенте
Процедура ЗаписатьЗагруженныеДанные(Команда)
	
	// перед отправкой дерева на сервер проверим наличие пометок в дереве
	Если ИмпортируемыеТоварыЕстьПометки() = Ложь Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Необходимо пометить строки с товарами.'"));
		Возврат;
	КонецЕсли;
	
	Если ОтображатьСопоставленные = 1 Тогда
		ТекстСообщения = НСтр("ru = 'Выполнить запись данных?'");
	ИначеЕсли ОтображатьСопоставленные = 2 Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Запись несопоставленных товарных позиций невозможна.'"));
		Возврат;
	ИначеЕсли СоздатьЭлементыНоменклатуры Тогда
		ТекстСообщения = НСтр("ru = 'Несопоставленные элементы справочника ""Номенклатура"" будут созданы автоматически." + Символы.ПС + "Продолжить запись данных?'");
	Иначе
		ТекстСообщения = НСтр("ru = 'Несопоставленные товарные позиции будут пропущены из обработки." + Символы.ПС + "Продолжить запись данных?'");
	КонецЕсли;
	
    Модифицированность = Ложь;

	ОповещениеОЗавершении = Новый ОписаниеОповещения( "ЗаписатьЗагруженныеДанныеЗавершение", ЭтотОбъект, );
	ПоказатьВопрос(ОповещениеОЗавершении, 
		ТекстСообщения, 
		РежимДиалогаВопрос.ДаНет, 
		, 
		КодВозвратаДиалога.Нет, 
		НСтр("ru = 'Запись данных'"), 
		КодВозвратаДиалога.Нет);
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыКатегорииПоСопоставлениюЗаполнить( СопоставлениеКатегорий )
	
	ТипСоответствие = (ТипЗнч( СопоставлениеКатегорий ) = Тип( "Соответствие" ));
	ОтборПоИдентификаторКатегорииМаркетплейса = Новый Структура( "ИдентификаторКатегорииМаркетплейса" );
	
	Для Каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		Для Каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.ПолучитьЭлементы() Цикл
			
			Если СтрокаДереваВторогоУровня.Пометка <> Истина Тогда
				Продолжить;
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса ) Тогда
				Продолжить;
			КонецЕсли;
			
			Если ТипСоответствие = Истина Тогда
				СопоставлениеКатегории = СопоставлениеКатегорий.Получить( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса );
				Если СопоставлениеКатегории = Неопределено Тогда
					Продолжить;
				КонецЕсли;
				
				Категория = СопоставлениеКатегории.Категория;
				КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon = СопоставлениеКатегории.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon;
				
			Иначе
				ОтборПоИдентификаторКатегорииМаркетплейса.ИдентификаторКатегорииМаркетплейса = СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса;
				СопоставлениеКатегории = СопоставлениеКатегорий.НайтиСтроки( ОтборПоИдентификаторКатегорииМаркетплейса  );
				Если СопоставлениеКатегории.Количество() = 0 Тогда
					Продолжить;
				КонецЕсли;
				Категория = СопоставлениеКатегории[ 0 ].Категория;
				КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon = СопоставлениеКатегории[ 0 ].КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon;
			КонецЕсли;
			
			СтрокаДереваВторогоУровня.Категория = Категория;
			СтрокаДереваВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon = КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon;
			
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗаписатьЗагруженныеДанныеЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;

	ОчиститьСообщения();
	
	ПоказатьОповещениеПользователя(
	НСтр("ru = 'Импорт данных'"),
	, 
	НСтр("ru = 'Запись загруженных данных с торговой площадки Ozon.'"),
	БиблиотекаКартинок.ЛоготипOzon2);
	
	
	КэшКатегорийДерево = Неопределено;
	
	ДлительнаяОперация = ЗаписатьЗагруженныеДанныеНаСервере( Ложь, Ложь, КэшКатегорийДерево );	
	ОповещениеОЗавершении = Новый ОписаниеОповещения( "ЗаписатьЗагруженныеДанныеЗавершениеФоновогоЗадания", ЭтотОбъект );
	
	ПараметрыОжидания 								   = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
	ПараметрыОжидания.ВыводитьОкноОжидания 			   = Истина;
	ПараметрыОжидания.ТекстСообщения                   = НСтр("ru = 'Запись загруженных данных по товарным позициям с торговой площадки Ozon.'");
	ПараметрыОжидания.ОповещениеПользователя.Показать  = Истина;
	ПараметрыОжидания.ОповещениеПользователя.Текст 	   = НСтр("ru = 'Импорт данных'");
	ПараметрыОжидания.ОповещениеПользователя.Пояснение = НСтр("ru = 'Завершена запись загруженных данных по товарным позициям с торговой площадки Ozon.'");
	ПараметрыОжидания.ОповещениеПользователя.Картинка  = БиблиотекаКартинок.ЛоготипOzon2;
	
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);

КонецПроцедуры

&НаКлиенте
Процедура ЗаписатьЦены(Команда)
	
	Модифицированность = Ложь;
	ОповещениеОЗавершении = Новый ОписаниеОповещения("ЗаписатьЦеныЗавершение", ЭтотОбъект,);
	
	ПоказатьВопрос(ОповещениеОЗавершении, 
		НСтр("ru = 'Записать цены товаров согласно данных торговой площадки?'"), 
		РежимДиалогаВопрос.ДаНет, 
		, 
		КодВозвратаДиалога.Нет, 
		НСтр("ru = 'Запись данных'"), 
		КодВозвратаДиалога.Нет);
		
КонецПроцедуры
	
&НаКлиенте
Процедура ЗаписатьЦеныЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		ОчиститьСообщения();
		ПоказатьОповещениеПользователя(
			НСтр("ru = 'Импорт данных'"),
			, 
			НСтр("ru = 'Запись цен с торговой площадки Ozon.'"),
			БиблиотекаКартинок.ЛоготипOzon2);
			
		ДлительнаяОперация    = ЗаписатьЗагруженныеДанныеНаСервере( Истина, Ложь );
		ОповещениеОЗавершении = Новый ОписаниеОповещения("ЗаписатьЗагруженныеДанныеЗавершениеФоновогоЗадания", ЭтотОбъект,);
		
		ПараметрыОжидания 								   = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
		ПараметрыОжидания.ВыводитьОкноОжидания 			   = Истина;
		ПараметрыОжидания.ТекстСообщения                   = НСтр("ru = 'Запись цен по товарным позициям с торговой площадки Ozon.'");
		ПараметрыОжидания.ОповещениеПользователя.Показать  = Истина;
		ПараметрыОжидания.ОповещениеПользователя.Текст 	   = НСтр("ru = 'Импорт данных'");
		ПараметрыОжидания.ОповещениеПользователя.Пояснение = НСтр("ru = 'Завершена запись цен по товарным позициям с торговой площадки Ozon.'");
		ПараметрыОжидания.ОповещениеПользователя.Картинка  = БиблиотекаКартинок.ЛоготипOzon2;
		
		ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура СоздатьНоменклатуру(Команда)
	
	Модифицированность = Ложь;
	ОповещениеОЗавершении = Новый ОписаниеОповещения("СоздатьНоменклатуруЗавершение", ЭтотОбъект,);
	
	ПоказатьВопрос(ОповещениеОЗавершении, 
		НСтр("ru = 'Создать несопоставленные элементы справочника «Номенклатура»?'"), 
		РежимДиалогаВопрос.ДаНет, 
		, 
		КодВозвратаДиалога.Нет, 
		НСтр("ru = 'Запись данных'"), 
		КодВозвратаДиалога.Нет);
		
КонецПроцедуры

&НаКлиенте
Процедура СоздатьНоменклатуруЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		ОчиститьСообщения();
		ПоказатьОповещениеПользователя(
			НСтр("ru = 'Импорт данных'"),
			, 
			НСтр("ru = 'Создание номенклатуры для несопоставленных позиций.'"),
			БиблиотекаКартинок.ЛоготипOzon2);
			
		ДлительнаяОперация    = ЗаписатьЗагруженныеДанныеНаСервере( Ложь, Истина );
		ОповещениеОЗавершении = Новый ОписаниеОповещения("ЗаписатьЗагруженныеДанныеЗавершениеФоновогоЗадания", ЭтотОбъект,);
		
		ПараметрыОжидания 								   = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
		ПараметрыОжидания.ВыводитьОкноОжидания 			   = Истина;
		ПараметрыОжидания.ТекстСообщения                   = НСтр("ru = 'Создание номенклатуры для несопоставленных позиций.'");
		ПараметрыОжидания.ОповещениеПользователя.Показать  = Истина;
		ПараметрыОжидания.ОповещениеПользователя.Текст 	   = НСтр("ru = 'Импорт данных'");
		ПараметрыОжидания.ОповещениеПользователя.Пояснение = НСтр("ru = 'Завершено создание номенклатуры для несопоставленных позиций.'");
		ПараметрыОжидания.ОповещениеПользователя.Картинка  = БиблиотекаКартинок.ЛоготипOzon2;
		
		ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ИзменитьРеквизитыФормы()

	ДобавляемыеРеквизиты = Новый Массив;
	УдаляемыеРеквизиты   = Новый Массив;
	
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары", Метаданные.РегистрыСведений.СтатусыПубликацииОбъектовМаркетплейсаOzon.Измерения);
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары", Метаданные.РегистрыСведений.СтатусыПубликацииОбъектовМаркетплейсаOzon.Ресурсы);
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары", Метаданные.РегистрыСведений.СтатусыПубликацииОбъектовМаркетплейсаOzon.Реквизиты);
	
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИдентификаторыТоваров", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаИдентификаторовТоваров().Колонки);
	
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары.ДанныеОбъекта", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаЗначенийРеквизитов().Колонки);
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары.Изображения", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаИзображений().Колонки);
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары.ИсточникиСхожихПредложений", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаИсточникиСхожихПредложений().Колонки);
	
    ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары.ДанныеТовараИзСервиса", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаОписанияТоваров().Колонки);
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары.ДанныеТовараИзСервиса.ИнформацияОКомиссиях", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаИнформацияОКомиссиях().Колонки);
    ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары.ДанныеТовараИзСервиса.ИсточникиСхожихПредложений", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаИсточникиСхожихПредложений().Колонки);
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары.ДанныеТовараИзСервиса.Изображения", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаИзображений().Колонки);
	
	ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, "ИмпортируемыеТовары.ЗначенияРеквизитовИзСервиса", ИнтеграцияСМаркетплейсомOzonСервер.НоваяТаблицаЗначенийРеквизитов(Истина).Колонки);
	
	ИзменитьРеквизиты(ДобавляемыеРеквизиты, УдаляемыеРеквизиты);
	
	
	// Элемент ЗначениеОбъединенияВОднуКарточку
	ЭлементЗначениеОбъединенияВОднуКарточку = Элементы.Добавить( "ИмпортируемыеТоварыЗначениеОбъединенияВОднуКарточку", Тип( "ПолеФормы" ), Элементы.ГруппаЗначениеОбъединенияВОднуКарточку );
	ЭлементЗначениеОбъединенияВОднуКарточку.Вид = ВидПоляФормы.ПолеВвода;
	ЭлементЗначениеОбъединенияВОднуКарточку.ПутьКДанным = "ИмпортируемыеТовары.ЗначениеОбъединенияВОднуКарточку";
	ЭлементЗначениеОбъединенияВОднуКарточку.ТолькоПросмотр = Истина;
	ЭлементЗначениеОбъединенияВОднуКарточку.Заголовок = НСтр("ru = 'Значение для объединения в одну карточку'");
	ЭлементЗначениеОбъединенияВОднуКарточку.Ширина = 15;
	
КонецПроцедуры

&НаСервере
Процедура ДобавитьРеквизитыФормы(ДобавляемыеРеквизиты, Знач Путь, Знач ИсточникДанных)
	
	Реквизиты = ПолучитьИменаРеквизитовФормы(Путь);
	
	Для каждого ЭлементКоллекции Из ИсточникДанных Цикл
		Если Реквизиты.Найти(ЭлементКоллекции.Имя) = Неопределено Тогда 
			Если ТипЗнч(ЭлементКоллекции) = Тип("КолонкаТаблицыЗначений") Тогда
				Тип = ЭлементКоллекции.ТипЗначения;
			ИначеЕсли ЭлементКоллекции.Тип.СодержитТип(Тип("ХранилищеЗначения")) Тогда
				Тип = Новый ОписаниеТипов("ТаблицаЗначений");
			Иначе
				Тип = ЭлементКоллекции.Тип;
			КонецЕсли;			
			
			ДобавляемыеРеквизиты.Добавить(Новый РеквизитФормы(ЭлементКоллекции.Имя, Тип, Путь));
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры
	
&НаСервере
Функция ПолучитьИменаРеквизитовФормы(Знач Путь)
	
	Реквизиты = Новый Массив;
	
	Попытка
		Для каждого ЭлементКоллекции Из ПолучитьРеквизиты(Путь) Цикл
			Реквизиты.Добавить(ЭлементКоллекции.Имя);
		КонецЦикла;
	Исключение
		Реквизиты = Новый Массив;
	КонецПопытки;
	
	Возврат Реквизиты;
	
КонецФункции

&НаСервере
Функция ПолучитьПодчиненныеЭлементыФормы(Знач Элемент)
	
	Результат = Новый Массив;
	
	Для каждого ЭлементКоллекции Из Элемент.ПодчиненныеЭлементы Цикл
		Если ТипЗнч(ЭлементКоллекции) = Тип("ГруппаФормы") Тогда
			ОбщегоНазначенияКлиентСервер.ДополнитьМассив(Результат, ПолучитьПодчиненныеЭлементыФормы(ЭлементКоллекции));
		Иначе
			Результат.Добавить(ЭлементКоллекции);
		КонецЕсли;	
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Процедура УстановитьУсловноеОформление()
	
	УсловноеОформление.Элементы.Очистить();

	// Отображение сопоставленных 
	ЭлементыДерева = ПолучитьПодчиненныеЭлементыФормы(Элементы.ИмпортируемыеТовары);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	Для каждого ЭлементКоллекции Из ЭлементыДерева Цикл
		ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
		ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(ЭлементКоллекции.Имя);
	КонецЦикла;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ОтображатьСопоставленные");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = 1;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ГруппаОтбора1           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора1.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИЛИ;

		ОтборЭлемента               = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Номенклатура");
		ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.НеЗаполнено;

		ГруппаОтбора2           = ГруппаОтбора1.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
		ГруппаОтбора2.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ;
		
			ОтборЭлемента                = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ХарактеристикиИспользуются");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = Истина;

			ОтборЭлемента               = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Характеристика");
			ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.НеЗаполнено;

		ОтборЭлемента               = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Категория");
		ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	Для каждого ЭлементКоллекции Из ЭлементыДерева Цикл
		ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
		ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(ЭлементКоллекции.Имя);
	КонецЦикла;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ОтображатьСопоставленные");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = 2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ГруппаОтбора1           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора1.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ;

		ОтборЭлемента               = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Номенклатура");
		ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.Заполнено;

		ГруппаОтбора2           = ГруппаОтбора1.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
		ГруппаОтбора2.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИЛИ;
		
			ОтборЭлемента                = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ХарактеристикиИспользуются");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = Ложь;

			ОтборЭлемента               = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Характеристика");
			ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.Заполнено;

		ОтборЭлемента               = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Категория");
		ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.Заполнено;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	// Отображение по статусу
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	Для каждого ЭлементКоллекции Из ЭлементыДерева Цикл
		ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
		ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(ЭлементКоллекции.Имя);
	КонецЦикла;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ОтображатьПоСтатусу");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -1;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = Новый ПолеКомпоновкиДанных("ОтображатьПоСтатусу");

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	// Группы первого уровня
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыПредставлениеОбъектаМаркетплейса.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Шрифт", ШрифтыСтиля.ВажнаяНадписьШрифт);

	// Пометка
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыПометка.Имя);
	
	ГруппаОтбора1           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора1.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИЛИ;
	
		ГруппаОтбора2           = ГруппаОтбора1.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
		ГруппаОтбора2.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ;
		
			ОтборЭлемента                = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = 0;

			ОтборЭлемента                = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ВариантЗагрузкиДанных");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = 2;

		ГруппаОтбора3           = ГруппаОтбора1.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
		ГруппаОтбора3.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ;
		
			ОтборЭлемента                = ГруппаОтбора3.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = 2;

			ОтборЭлемента                = ГруппаОтбора3.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ВариантЗагрузкиДанных");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = 0;

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 3;

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 4;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ТолькоПросмотр", Истина);

	// Представление объекта маркетплейса
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыПредставлениеОбъектаМаркетплейса.Имя);
	
	ОтборЭлемента               = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Номенклатура");
	ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Шрифт", ШрифтыСтиля.НаклонныйШрифтРаботаСНоменклатурой);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыПредставлениеОбъектаМаркетплейса.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = 4;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст);

	// Категория Ozon
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыНаименованиеКатегорииМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыИдентификаторКатегорииМаркетплейса.Имя);
	
	ОтборЭлемента               = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Категория");
	ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.Заполнено;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.НаименованиеКатегорииМаркетплейса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Заполнено;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Шрифт", ШрифтыСтиля.ПодчеркнутыйШрифтДиалоговМаркетплейсов);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыНаименованиеКатегорииМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыИдентификаторКатегорииМаркетплейса.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.НаименованиеКатегорииМаркетплейса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст);
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Текст",      НСтр("ru = '<не определена>'"));

	// Номенклатура, характеристика, упаковка
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыНоменклатура.Имя);
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыХарактеристика.Имя);
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыУпаковка.Имя);
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыКатегория.Имя);
	
	ГруппаОтбора1           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора1.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИЛИ;
	
		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
		ОтборЭлемента.ПравоеЗначение = 2;

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ОтметкаНезаполненного", Ложь);
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ТолькоПросмотр",        Истина);
	
	// Номенклатура
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТоварыНоменклатура" );
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "СоздатьЭлементыНоменклатуры" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ОтображатьСопоставленные");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = 2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Номенклатура");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = 2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст );
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "Текст", НСтр("ru = '<выберите номенклатуру>'") );

	
	// Номенклатура
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТоварыНоменклатура" );

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст);
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "Текст", НСтр("ru = '<выберите номенклатуру или будет создана новая>'"));
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "СоздатьЭлементыНоменклатуры" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.Номенклатура" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.ВариантЗагрузки" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров();;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;// ветка 1го уровня
   

	// Категория
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТоварыКатегория" );

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст );
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "Текст", НСтр("ru = '<выберите категорию>'" ));

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "СоздатьКатегорииНоменклатуры" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;
	
	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Категория");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.ВариантЗагрузки" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров();

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;// ветка 1го уровня



	
	// Категория
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТоварыКатегория" );

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст );
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "Текст", НСтр("ru = '<выберите категорию или будет создана новая>'") );
	
	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "СоздатьКатегорииНоменклатуры" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.Категория" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.ВариантЗагрузки" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров();

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;// ветка 1го уровня

    


	// КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТоварыКатегорияИлиНоменклатураДляСопоставленияКатегорииOzon" );

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст );
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "Текст", НСтр("ru = '<выберите категорию или будет создана новая>'") );
	
	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "СоздатьИерархиюНоменклатуры" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.ВариантЗагрузки" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров();
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;// ветка 1го уровня





	// КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТоварыКатегорияИлиНоменклатураДляСопоставленияКатегорииOzon" );

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст );
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "Текст", НСтр("ru = '<выберите категорию>'") );
	
	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "СоздатьИерархиюНоменклатуры" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТовары.ВариантЗагрузки" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров();
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;// ветка 1го уровня
	
	
	// 
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( Элементы.ИмпортируемыеТоварыНоменклатура.Имя);
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Номенклатура");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ОтметкаНезаполненного", Истина );
	
	
	// Характеристика
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыХарактеристика.Имя);
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ХарактеристикиИспользуются");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ОтметкаНезаполненного", Ложь );
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра( "ТолькоПросмотр", Истина );
	
	// Характеристика
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТоварыХарактеристика" );

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст);
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Текст", НСтр("ru = '<выберите характеристику>'"));
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ХарактеристикиИспользуются");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "СоздатьХарактеристикиНоменклатуры" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Характеристика");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = 2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;
	

	// Характеристика
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных( "ИмпортируемыеТоварыХарактеристика" );

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст);
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Текст", НСтр("ru = '<выберите характеристику или будет создана новая>'"));
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ХарактеристикиИспользуются");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ОтборЭлемента = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип( "ЭлементОтбораКомпоновкиДанных" ));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных( "СоздатьХарактеристикиНоменклатуры" );
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Характеристика");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = 2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;



	
	// Упаковка
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыУпаковка.Имя);
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИспользоватьУпаковки");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста",     ЦветаСтиля.ТекстЗапрещеннойЯчейкиЦвет);
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Текст",          Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ЕдиницаИзмерения"));
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ТолькоПросмотр", Истина);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыУпаковка.Имя);
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИспользоватьУпаковки");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.Упаковка");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеЗаполнено;
			
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ТекстЗапрещеннойЯчейкиЦвет);
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Текст",      Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ЕдиницаИзмерения"));
	
	
	// Ставка НДС 
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыСтавкаНДС.Имя);
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ЕстьРазличияСтавкаНДС");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = 4;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст); 
	
	// Продается по схемам
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыПродаетсяПоСхемеРаботыFBS.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ИмпортируемыеТоварыПродаетсяПоСхемеРаботыDBS.Имя);

	ГруппаОтбора1           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора1.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИЛИ;
	
		ГруппаОтбора2           = ГруппаОтбора1.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
		ГруппаОтбора2.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ;
		
			ОтборЭлемента                = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = 0;

			ОтборЭлемента                = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ВариантЗагрузкиДанных");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = 2;

		ГруппаОтбора3           = ГруппаОтбора1.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
		ГруппаОтбора3.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ;
		
			ОтборЭлемента                = ГруппаОтбора3.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = 2;

			ОтборЭлемента                = ГруппаОтбора3.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ВариантЗагрузкиДанных");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = 0;

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 0;

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 3;

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 4;

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = -2;
		
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ТолькоПросмотр", Истина);

	// Данные товара
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ПредставлениеОбъектаМаркетплейса.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = -2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Текст", НСтр("ru = 'Отображения данных о товаре для группы действий недоступно.'"));
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Шрифт", ШрифтыСтиля.НаклонныйШрифтРаботаСНоменклатурой);
	
	// Дерево реквизитов (наименование атрибута)
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовНаименованиеАтрибутаМаркетплейса.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ОбязательныйАтрибут");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Истина;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Шрифт", ШрифтыСтиля.ВажнаяНадписьШрифт);

	// Дерево реквизитов (значение атрибута Ozon)
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ЭлементыДерева = ПолучитьПодчиненныеЭлементыФормы(Элементы.ДеревоРеквизитовГруппаЗначенияАтрибута);
	Для каждого ЭлементКоллекции Из ЭлементыДерева Цикл
		ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
		ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(ЭлементКоллекции.Имя);
	КонецЦикла;
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 4;

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 5;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаТекущее.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначенияАтрибутаМаркетплейсаТекущее.Имя);
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 4;

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 5;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("МинимальнаяШирина", 10);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейса.Имя);
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТипЗначенияАтрибута");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = "boolean";

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ДоступенВыборЗначенияИзСписка");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Истина;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейса.Имя);
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТипЗначенияАтрибута");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = "boolean";

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ЦеновыеПоказатели");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Истина;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ГоризонтальноеПоложение", ГоризонтальноеПоложение.Право);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовИдентификаторЗначенияАтрибутаМаркетплейса.Имя);

	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных")); 
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ДоступенВыборЗначенияИзСписка");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Ложь;

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ЯвляетсяКоллекциейЗначений");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Истина;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовИдентификаторЗначенияАтрибутаМаркетплейса.Имя);

	ОтборЭлемента               = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ИдентификаторЗначенияАтрибутаМаркетплейса");
	ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.Заполнено;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Текст", 
	Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ЗначениеАтрибутаМаркетплейса"));

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаБулево.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТипЗначенияАтрибута");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = "boolean";

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначенияАтрибутаМаркетплейса.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ЯвляетсяКоллекциейЗначений");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	// Дерево реквизитов (есть различия)
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЕстьРазличия.Имя);
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 4;

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ИндексСтатуса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 5;

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = 2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	// Дерево реквизитов (значение атрибута 1С)
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ЭлементыДерева = ПолучитьПодчиненныеЭлементыФормы(Элементы.ДеревоРеквизитовГруппаЗначенияАтрибутаТекущее);
	Для каждого ЭлементКоллекции Из ЭлементыДерева Цикл
		ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
		ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(ЭлементКоллекции.Имя);
	КонецЦикла;
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = 2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначенияАтрибутаМаркетплейса.Имя);
	
	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("ИмпортируемыеТовары.ВариантЗагрузки");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = 2;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("МинимальнаяШирина", 10);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаТекущее.Имя);
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееТипЗначенияАтрибута");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = "boolean";

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееДоступенВыборЗначенияИзСписка");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Истина;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаТекущее.Имя);
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееТипЗначенияАтрибута");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = "boolean";

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ЦеновыеПоказатели");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Истина;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ГоризонтальноеПоложение", ГоризонтальноеПоложение.Право);
	
	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовИдентификаторЗначенияАтрибутаМаркетплейсаТекущее.Имя);

	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных")); 
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееДоступенВыборЗначенияИзСписка");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Ложь;

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееЯвляетсяКоллекциейЗначений");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Истина;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовИдентификаторЗначенияАтрибутаМаркетплейсаТекущее.Имя);

	ОтборЭлемента               = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееИдентификаторЗначенияАтрибутаМаркетплейса");
	ОтборЭлемента.ВидСравнения  = ВидСравненияКомпоновкиДанных.Заполнено;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Текст", Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееЗначениеАтрибутаМаркетплейса"));

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаБулевоТекущее.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееТипЗначенияАтрибута");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
	ОтборЭлемента.ПравоеЗначение = "boolean";

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();

	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначенияАтрибутаМаркетплейсаТекущее.Имя);

	ОтборЭлемента                = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееЯвляетсяКоллекциейЗначений");
	ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
	ОтборЭлемента.ПравоеЗначение = Ложь;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Видимость", Ложь);

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаТекущее.Имя);
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ИдентификаторАтрибутаМаркетплейса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = "СтавкаНДС";

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ЗначениеАтрибутаМаркетплейса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
		ОтборЭлемента.ПравоеЗначение = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееЗначениеАтрибутаМаркетплейса");

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст);
	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("Шрифт",      ШрифтыСтиля.ВажнаяНадписьШрифт);

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовИдентификаторЗначенияАтрибутаМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаТекущее.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовИдентификаторЗначенияАтрибутаМаркетплейсаТекущее.Имя);
	
	ГруппаОтбора           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ; 

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ИдентификаторАтрибутаМаркетплейса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = "ИдентификаторКатегорииМаркетплейса";

		ОтборЭлемента                = ГруппаОтбора.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ЗначениеАтрибутаМаркетплейса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
		ОтборЭлемента.ПравоеЗначение = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееЗначениеАтрибутаМаркетплейса");

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ПоясняющийОшибкуТекст);

	//
	ЭлементУсловногоОформления = УсловноеОформление.Элементы.Добавить();
	
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовИдентификаторЗначенияАтрибутаМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаБулево.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначенияАтрибутаМаркетплейса.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаТекущее.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовИдентификаторЗначенияАтрибутаМаркетплейсаТекущее.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначениеАтрибутаМаркетплейсаБулевоТекущее.Имя);
	ПолеЭлемента      = ЭлементУсловногоОформления.Поля.Элементы.Добавить();
	ПолеЭлемента.Поле = Новый ПолеКомпоновкиДанных(Элементы.ДеревоРеквизитовЗначенияАтрибутаМаркетплейсаТекущее.Имя);
	
	ГруппаОтбора1           = ЭлементУсловногоОформления.Отбор.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
	ГруппаОтбора1.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИ; 

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ИдентификаторАтрибутаМаркетплейса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
		ОтборЭлемента.ПравоеЗначение = "СтавкаНДС";

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ИдентификаторАтрибутаМаркетплейса");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.НеРавно;
		ОтборЭлемента.ПравоеЗначение = "ИдентификаторКатегорииМаркетплейса";

		ОтборЭлемента                = ГруппаОтбора1.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ЕстьРазличия");
		ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
		ОтборЭлемента.ПравоеЗначение = Истина;

		ГруппаОтбора2           = ГруппаОтбора1.Элементы.Добавить(Тип("ГруппаЭлементовОтбораКомпоновкиДанных"));
		ГруппаОтбора2.ТипГруппы = ТипГруппыЭлементовОтбораКомпоновкиДанных.ГруппаИли; 

			ОтборЭлемента                = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ОбязательныйАтрибут");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = Истина;

			ОтборЭлемента                = ГруппаОтбора2.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
			ОтборЭлемента.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Элементы.ИмпортируемыеТовары.ТекущиеДанные.ДеревоРеквизитов.ТекущееОбязательныйАтрибут");
			ОтборЭлемента.ВидСравнения   = ВидСравненияКомпоновкиДанных.Равно;
			ОтборЭлемента.ПравоеЗначение = Истина;

	ЭлементУсловногоОформления.Оформление.УстановитьЗначениеПараметра("ЦветТекста", ЦветаСтиля.ЦветТекстаРоли);
	
КонецПроцедуры

&НаКлиенте
Процедура ФормаПараметрыИмпортаДанныхОписаниеОбновить()

	Если ВариантЗагрузкиДанных = 0 Тогда
		ПараметрыИмпортаДанныхЗаголовок = НСтр("ru = ' • Обновлять сопоставленные карточки товаров'");
	Иначе
		ПараметрыИмпортаДанныхЗаголовок = НСтр("ru = ' • Загружать несопоставленные карточки товаров'");
	КонецЕсли;

	Если ЗначениеЗаполнено( ЭтаФорма.РазмерПорцииЗагрузкиДанных ) Тогда
		ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
		+ СтрШаблон( " " + НСтр( "ru = '(%1 позиций за один запрос)'" ),
		ЭтаФорма.РазмерПорцииЗагрузкиДанных );
	КонецЕсли;
	
	Если ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
		Если ЭтотОбъект.ЗагрузитьОписанияАрхивныхПозиций Тогда
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ НСтр("ru = ' • Загружать архивные позиции'");
		Иначе
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ НСтр("ru = ' • Не загружать архивные позиции'");
		КонецЕсли;

		Если СоздатьЭлементыНоменклатуры Тогда
			ДополнениеЗаголовкаШаблон = НСтр("ru = ' • Создавать «Номенклатуру» в группе ""%1""'");
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ СтрШаблон( ДополнениеЗаголовкаШаблон, ЭтотОбъект.ГруппаДляСозданияНовыхТоваров );
		Иначе
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ НСтр("ru = ' • Не создавать «Номенклатуру»'");
		КонецЕсли;

	КонецЕсли;
    
	ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок + Символы.ПС;

	Если ПерезаполнитьЗначенияРеквизитовНоменклатуры Тогда
		ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
		+ НСтр("ru = ' • Перезаполнять значения реквизитов «Номенклатуры»'")
	КонецЕсли;

	Если ЭтотОбъект.ЗагрузитьИзображения Тогда
		ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
		+ НСтр("ru = ' • Загружать изображения'");
	Иначе
		ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
		+ НСтр("ru = ' • Не загружать изображения'");
	КонецЕсли;

	Если ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
		Если ЭтотОбъект.СоздатьИерархиюНоменклатуры Тогда
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ НСтр("ru = ' • Создавать иерархию номенклатуры'");
		Иначе
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ НСтр("ru = ' • Не создавать иерархию номенклатуры'");
		КонецЕсли;

		Если СоздатьХарактеристикиНоменклатуры Тогда
			ДополнениеЗаголовкаТекст = НСтр("ru = ' • Создавать «Характеристики номенклатуры»'");
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ ДополнениеЗаголовкаТекст;
		Иначе
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
				+ НСтр("ru = ' • Не создавать «Характеристики номенклатуры»'");
		КонецЕсли;

		Если СоздатьКатегорииНоменклатуры Тогда
			ДополнениеЗаголовкаТекст = НСтр("ru = ' • Создавать «Категории номенклатуры»'");
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ ДополнениеЗаголовкаТекст;
		Иначе
			ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ НСтр("ru = ' • Не создавать «Категории номенклатуры»'");
		КонецЕсли;
	КонецЕсли;
	
	Если ЗагрузитьЦеныПродажи Тогда
		ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ НСтр("ru = ' • Загружать цены продажи'");
	Иначе
		ПараметрыИмпортаДанныхЗаголовок = ПараметрыИмпортаДанныхЗаголовок
			+ НСтр("ru = ' • Не загружать цены продажи'");
	КонецЕсли;
	
	Элементы.ПараметрыИмпортаДанныхОписание.Заголовок = ПараметрыИмпортаДанныхЗаголовок;

КонецПроцедуры

&НаСервере
Процедура УстановитьВидимостьДоступность()
	
	ДоступнаУстановкаЦен = ПравоДоступа( "Проведение", Метаданные.Документы.УстановкаЦенНоменклатуры );
	
	Элементы.ГруппаШапка.ТолькоПросмотр = ТолькоПросмотр;
	Элементы.ГруппаТаблицаТоваров.ТолькоПросмотр = ТолькоПросмотр;
	Элементы.ГруппаКоманднаяПанель.ТолькоПросмотр = ТолькоПросмотр;
	Элементы.ГруппаКоманднаяПанельЕще.ТолькоПросмотр = ТолькоПросмотр;


	Если СоздатьЭлементыНоменклатуры = Истина
	И ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
		Элементы.ЗаписатьЗагруженныеДанные.Заголовок = НСтр("ru = 'Создать и записать данные в 1С'");
	Иначе
		Элементы.ЗаписатьЗагруженныеДанные.Заголовок = НСтр("ru = 'Записать данные в 1С'");
	КонецЕсли;
	
	Элементы.ГруппаФлажки.Доступность                        = (ИмпортируемыеТовары.ПолучитьЭлементы().Количество() > 0);
	Элементы.УстановитьФлажкиДляТребующихЗаписиЦен.Видимость = ЗагрузитьЦеныПродажи И ДоступнаУстановкаЦен И ЗаписатьЦеныОтдельнойКомандой;
	Элементы.ЗаписатьЦены.Видимость                          = ЗагрузитьЦеныПродажи И ДоступнаУстановкаЦен И ЗаписатьЦеныОтдельнойКомандой;
	Элементы.ГруппаДанныеКарточкиТовара.Видимость            = ОтображатьДанныеТовара;
	
	Элементы.ГруппаЛегендаОжидаютИсправленияОшибок.Видимость = ЗначениеЗаполнено(ЛегендаОжидаютИсправленияОшибок);
	Элементы.ГруппаЛегендаПодготовленыКПубликации.Видимость  = ЗначениеЗаполнено(ЛегендаПодготовленыКПубликации);
	Элементы.ГруппаЛегендаПеремещеныВАрхив.Видимость         = ЗначениеЗаполнено(ЛегендаПеремещеныВАрхив);
	Элементы.ГруппаЛегендаГотовыКПродаже.Видимость           = ЗначениеЗаполнено(ЛегендаГотовыКПродаже);
	
	Элементы.ГруппаЛегендаОжидаютПолученияДанных.Видимость   = ЗначениеЗаполнено(ЛегендаОжидаютПолученияДанных);
	Элементы.ГруппаЛегендаНеНайденыНаПлощадке.Видимость      = ЗначениеЗаполнено(ЛегендаНеНайденыНаПлощадке);
	Элементы.ГруппаЛегендаОбнаруженыРасхождения.Видимость    = ЗначениеЗаполнено(ЛегендаОбнаруженыРасхождения);
	Элементы.ГруппаЛегендаОбновлениеНеТребуется.Видимость    = ЗначениеЗаполнено(ЛегендаОбновлениеНеТребуется);
	
	Элементы.ИмпортируемыеТоварыХарактеристика.Видимость     = ПолучитьФункциональнуюОпцию("ИспользоватьХарактеристики");
	
	
	Элементы.ИмпортируемыеТоварыСтатус.Видимость = ОтображатьДанныеТовара;
	Элементы.ИмпортируемыеТоварыИдентификаторОбъектаМаркетплейса.Видимость = ОтображатьДанныеТовара;
	Элементы.ИмпортируемыеТоварыИдентификаторПубликации.Видимость = ОтображатьДанныеТовара;
	Элементы.ИмпортируемыеТоварыИдентификаторКатегорииМаркетплейса.Видимость = ОтображатьДанныеТовара;
	
	Если ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда 
		Элементы.РежимПоискаИСозданияТоваров.Видимость = Истина;
	Иначе
		
		РежимПоискаИСозданияТоваров = Ложь;
		Элементы.РежимПоискаИСозданияТоваров.Видимость = Ложь;

		Элементы.КнопкаИмпортДанныхСопоставлениеКатегорий.Видимость = Ложь;
		
		Элементы.ОтображатьСопоставленные.Видимость = Ложь;
		
	КонецЕсли;
	
	Элементы.ИмпортируемыеТоварыРезультатыПоискаГруппа.Видимость = РежимПоискаИСозданияТоваров;
	Элементы.КнопкаНайтиТовары.Видимость = РежимПоискаИСозданияТоваров;
	
КонецПроцедуры

&НаКлиенте
Процедура ФормаРежимОтображенияЭлементыОбновить( ПараметрФормаРежимОтображения )
	
	ЭтаФорма.ФормаРежимОтображенияТекущий = ПараметрФормаРежимОтображения;
	
	Если ЭтаФорма.ФормаРежимОтображенияТекущий = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ФормаРежимОтображенияОткрытие() Тогда

		Элементы.ГруппаНазадВперед.Видимость = Ложь;

		Если ЭтаФорма.ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиОбновитьСопоставленныеКарточкиТоваров() Тогда 
		
    		Элементы.ГруппаДополнительныеДействия.Видимость = Ложь;
			Элементы.ГруппаИмпортДанных.Видимость = Истина;
			Элементы.ГруппаДетальныеНастройкиИмпорта.Видимость = Истина;
			
		ИначеЕсли ЭтаФорма.ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда 
		
			Элементы.ГруппаДополнительныеДействия.Видимость = Истина;
			Элементы.ГруппаИмпортДанных.Видимость = Ложь;
			Элементы.ГруппаДетальныеНастройкиИмпорта.Видимость = Истина;
			
		КонецЕсли;
	    
	    Элементы.ПолучитьДанныеСТорговойПлощадки.Видимость = Истина;
	    
	ИначеЕсли ЭтаФорма.ФормаРежимОтображенияТекущий = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ФормаРежимОтображенияЗагрузитьВсеОписания() Тогда

		Элементы.ГруппаНазадВперед.Видимость = Истина;

   		Элементы.ГруппаДополнительныеДействия.Видимость = Ложь;
		Элементы.ГруппаИмпортДанных.Видимость = Истина;
		Элементы.ГруппаДетальныеНастройкиИмпорта.Видимость = Ложь;
		
		Элементы.ПолучитьДанныеСТорговойПлощадки.Видимость = Ложь;
		Элементы.ОтображатьСопоставленные.Видимость = Ложь;
		
		РежимПоискаИСозданияТоваров = Ложь;
		Элементы.РежимПоискаИСозданияТоваров.Видимость = Ложь;
		Элементы.КнопкаИмпортДанныхСопоставлениеКатегорий.Видимость = Ложь;
		
	
	ИначеЕсли ЭтаФорма.ФормаРежимОтображенияТекущий = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ФормаРежимОтображенияЗагрузитьСДетальнымиНастройками() Тогда

		Элементы.ГруппаНазадВперед.Видимость = Истина;

   		Элементы.ГруппаДополнительныеДействия.Видимость = Ложь;
		Элементы.ГруппаИмпортДанных.Видимость = Истина;
		Элементы.ГруппаДетальныеНастройкиИмпорта.Видимость = Истина;
		
		Элементы.ПолучитьДанныеСТорговойПлощадки.Видимость = Истина;
		
	КонецЕсли;

КонецПроцедуры

&НаСервере
Процедура ОбновитьКнопкуОтбораПоСтатусу(ИмяКоманды)

	Команда = Команды.Найти(ИмяКоманды);
	Если Команда = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Элементы.ОтборПоСтатусам.Заголовок = Команда.Заголовок;
	Элементы.ОтборПоСтатусам.Картинка  = Команда.Картинка;

КонецПроцедуры

&НаКлиенте
Процедура УстановитьСнятьПометки(Знач ЭлементДерева, Знач Пометка, Знач ИмяКоманды = "")
	
	Если ЭлементДерева.ИндексСтатуса = -2 Тогда
		Если Пометка 
			 И (ЭлементДерева.ВариантЗагрузки = 0 И ВариантЗагрузкиДанных = 2
				ИЛИ ЭлементДерева.ВариантЗагрузки = 2 И ВариантЗагрузкиДанных = 0
				ИЛИ ЭлементДерева.ВариантЗагрузки = 3) Тогда
			Пометка = Ложь;
		КонецЕсли;
		
		Для каждого СтрокаДереваВторогоУровня Из ЭлементДерева.ПолучитьЭлементы() Цикл
			УстановитьСнятьПометки(СтрокаДереваВторогоУровня, Пометка, ИмяКоманды);
		КонецЦикла;
		
	Иначе
		Если Пометка 
			 И ЭлементДерева.ИндексСтатуса = 4 Тогда
			Пометка = Ложь;
		КонецЕсли;
		
		Если Пометка
			 И ЗначениеЗаполнено(ИмяКоманды) Тогда
			Если ИмяКоманды = "ТребующиеПолученияДанных" 
				 И ЭлементДерева.ИндексСтатуса <> 5 Тогда
				Пометка = Ложь;
			ИначеЕсли ИмяКоманды = "ТребующиеЗаписиТоваровИЦен" 
					  И ЭлементДерева.ИндексСтатуса <> 6 
					  И НЕ ЭлементДерева.ЕстьРазличияЦеновыеПоказатели Тогда
				Пометка = Ложь;
			ИначеЕсли ИмяКоманды = "ТребующиеЗаписиТоваров" 
					  И ЭлементДерева.ИндексСтатуса <> 6 Тогда
				Пометка = Ложь;
			ИначеЕсли ИмяКоманды = "ТребующиеЗаписиЦен" 
				 	  И НЕ ЭлементДерева.ЕстьРазличияЦеновыеПоказатели Тогда
				Пометка = Ложь;
			ИначеЕсли ИмяКоманды = "НеСопоставленнаяНоменклатура" 
				 	  И ЗначениеЗаполнено(ЭлементДерева.Номенклатура) Тогда
				Пометка = Ложь;
			КонецЕсли;
		КонецЕсли;

		Если Пометка И ЭлементДерева.ВариантЗагрузки = 0 И ВариантЗагрузкиДанных = 2 Тогда
			// Позиция уже записана
			Пометка = Ложь;
		КонецЕсли;
		
		ЭлементДерева.Пометка = Пометка;
		ЭлементДереваРодитель = ЭлементДерева.ПолучитьРодителя();
		ЭлементДереваРодитель.КоличествоОтмеченныхСтрок = ЭлементДереваРодитель.КоличествоОтмеченныхСтрок + ?(ЭлементДерева.Пометка, 1, -1);
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ОпределитьНаименованияГруппДереваТоваров(Знач СтрокаДереваПервогоУровня = Неопределено)
	
	Если СтрокаДереваПервогоУровня = Неопределено Тогда
		Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
			ОпределитьНаименованияГруппДереваТоваров(СтрокаДереваПервогоУровня);
		КонецЦикла;
		
	Иначе
		Если СтрокаДереваПервогоУровня.ВариантЗагрузки = 0 Тогда
			ШаблонПредставления = НСтр("ru = 'Обновить (%1 из %2)'");
		ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 2 Тогда
			ШаблонПредставления = НСтр("ru = 'Загрузить (%1 из %2)'");
		ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 3 Тогда
			ШаблонПредставления = НСтр("ru = 'Не найдены (%2)'");
		КонецЕсли;
		
		КоличествоСтрок           = СтрокаДереваПервогоУровня.ПолучитьЭлементы().Количество();
		КоличествоОтмеченныхСтрок = 0;
		
		Для каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.ПолучитьЭлементы() Цикл
			Если ОтображатьСопоставленные = 1
				 И (НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
				 	ИЛИ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
				        И НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика)
				 	ИЛИ НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория)) Тогда
				КоличествоСтрок = КоличествоСтрок - 1;
				Продолжить;
				
			ИначеЕсли ОтображатьСопоставленные = 2
				 	  И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
				 	  И (НЕ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
				         ИЛИ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика))
				 	  И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория) Тогда
				КоличествоСтрок = КоличествоСтрок - 1;
				Продолжить;
			КонецЕсли;
			
			Если ОтображатьПоСтатусу <> -1
				 И СтрокаДереваВторогоУровня.ИндексСтатуса <> ОтображатьПоСтатусу Тогда
				КоличествоСтрок = КоличествоСтрок - 1;
				Продолжить;
			КонецЕсли;
			
			КоличествоОтмеченныхСтрок = КоличествоОтмеченныхСтрок + ?(СтрокаДереваВторогоУровня.Пометка, 1, 0);
		КонецЦикла;
		
		Если КоличествоСтрок = 0 Тогда
			ШаблонПредставления = СтрЗаменить(ШаблонПредставления, " (%1 из %2)", "");
			ШаблонПредставления = СтрЗаменить(ШаблонПредставления, " (%2)", "");
		КонецЕсли;
		
		СтрокаДереваПервогоУровня.Пометка = (КоличествоОтмеченныхСтрок > 0);
		СтрокаДереваПервогоУровня.КоличествоОтмеченныхСтрок = КоличествоОтмеченныхСтрок;
		СтрокаДереваПервогоУровня.ПредставлениеОбъектаМаркетплейса = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			ШаблонПредставления,
			КоличествоОтмеченныхСтрок,
			КоличествоСтрок);

		ТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;
		
		Если ТекущиеДанные <> Неопределено Тогда
			Если ОтображатьСопоставленные = 1
				 И (НЕ ЗначениеЗаполнено(ТекущиеДанные.Номенклатура) 
				 	ИЛИ ТекущиеДанные.ХарактеристикиИспользуются
				        И НЕ ЗначениеЗаполнено(ТекущиеДанные.Характеристика)
				 	ИЛИ НЕ ЗначениеЗаполнено(ТекущиеДанные.Категория)) Тогда
				Элементы.ИмпортируемыеТовары.ТекущаяСтрока = СтрокаДереваПервогоУровня.ПолучитьИдентификатор();
				
			ИначеЕсли ОтображатьСопоставленные = 2
				 	  И ЗначениеЗаполнено(ТекущиеДанные.Номенклатура) 
				 	  И (НЕ ТекущиеДанные.ХарактеристикиИспользуются
				         ИЛИ ЗначениеЗаполнено(ТекущиеДанные.Характеристика))
				 	  И ЗначениеЗаполнено(ТекущиеДанные.Категория) Тогда
				Элементы.ИмпортируемыеТовары.ТекущаяСтрока = СтрокаДереваПервогоУровня.ПолучитьИдентификатор();
			КонецЕсли;
			
			Если ОтображатьПоСтатусу <> -1
				 И ТекущиеДанные.ИндексСтатуса <> ОтображатьПоСтатусу Тогда
				Элементы.ИмпортируемыеТовары.ТекущаяСтрока = СтрокаДереваПервогоУровня.ПолучитьИдентификатор();
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
		
КонецПроцедуры

&НаСервере
Процедура ПолучитьДанныеЛегенды(Знач ДеревоТоваров = Неопределено)
	
	Если ДеревоТоваров = Неопределено Тогда
		ДеревоТоваров = РеквизитФормыВЗначение("ИмпортируемыеТовары");
	КонецЕсли;
	
	ОжидаютИсправленияОшибок = ДеревоТоваров.Строки.НайтиСтроки(Новый Структура("ВариантЗагрузки, ИндексСтатуса", 0, 0), Истина).Количество();
	ПодготовленыКПубликации  = ДеревоТоваров.Строки.НайтиСтроки(Новый Структура("ВариантЗагрузки, ИндексСтатуса", 0, 1), Истина).Количество();
	ПеремещеныВАрхив         = ДеревоТоваров.Строки.НайтиСтроки(Новый Структура("ВариантЗагрузки, ИндексСтатуса", 0, 2), Истина).Количество();
	ГотовыКПродаже           = ДеревоТоваров.Строки.НайтиСтроки(Новый Структура("ВариантЗагрузки, ИндексСтатуса", 0, 3), Истина).Количество();
	
	ОжидаютПолученияДанных   = ДеревоТоваров.Строки.НайтиСтроки(Новый Структура("ВариантЗагрузки, ИндексСтатуса", 0, 5), Истина).Количество();
	НеНайденыНаПлощадке      = ДеревоТоваров.Строки.НайтиСтроки(Новый Структура("ИндексСтатуса", 4), Истина).Количество();
	ОбнаруженыРасхождения    = ДеревоТоваров.Строки.НайтиСтроки(Новый Структура("ИндексСтатуса", 6), Истина).Количество();
	ОбновлениеНеТребуется    = ДеревоТоваров.Строки.НайтиСтроки(Новый Структура("ИндексСтатуса", 7), Истина).Количество();
	
	ЛегендаОжидаютИсправленияОшибок = ?(ОжидаютИсправленияОшибок = 0,
										"",
										СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
											НСтр("ru = 'Ожидают исправления ошибок: %1'"),
											ОжидаютИсправленияОшибок));
	ЛегендаПодготовленыКПубликации  = ?(ПодготовленыКПубликации = 0,
										"",
										СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
											НСтр("ru = 'Подготовлены к публикации: %1'"),
											ПодготовленыКПубликации));
	ЛегендаПеремещеныВАрхив         = ?(ПеремещеныВАрхив = 0,
										"",
										СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
											НСтр("ru = 'Перемещены в архив: %1'"),
											ПеремещеныВАрхив));
	ЛегендаГотовыКПродаже           = ?(ГотовыКПродаже = 0,
										"",
										СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
											НСтр("ru = 'Готовы к продаже: %1'"),
											ГотовыКПродаже));

	ЛегендаОжидаютПолученияДанных   = ?(ОжидаютПолученияДанных = 0,
										"",
										СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
											НСтр("ru = 'Ожидают получения данных: %1'"),
											ОжидаютПолученияДанных));
	ЛегендаНеНайденыНаПлощадке      = ?(НеНайденыНаПлощадке = 0,
										"",
										СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
											НСтр("ru = 'Не найдены на площадке: %1'"),
											НеНайденыНаПлощадке));
	ЛегендаОбнаруженыРасхождения    = ?(ОбнаруженыРасхождения = 0,
										"",
										СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
											НСтр("ru = 'Обнаружены расхождения: %1'"),
											ОбнаруженыРасхождения));
	ЛегендаОбновлениеНеТребуется    = ?(ОбновлениеНеТребуется = 0,
										"",
										СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
											НСтр("ru = 'Обновление не требуется: %1'"),
											ОбновлениеНеТребуется));
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьСлужебныеДанные()

	Модифицированность = Ложь;
	ОчиститьСообщения();
	ПоказатьОповещениеПользователя(
		НСтр("ru = 'Импорт данных'"),
		, 
		НСтр("ru = 'Обновление служебных данных по выбранным товарным позициям.'"),
		БиблиотекаКартинок.ЛоготипOzon2);
		
	ДлительнаяОперация    = ОбновитьСлужебныеДанныеНаСервере();
	ОповещениеОЗавершении = Новый ОписаниеОповещения("ОбновитьСлужебныеДанныеЗавершениеФоновогоЗадания", ЭтотОбъект,);
	
	ПараметрыОжидания 								   = ДлительныеОперацииКлиент.ПараметрыОжидания(ЭтотОбъект);
	ПараметрыОжидания.ВыводитьОкноОжидания 			   = Истина;
	ПараметрыОжидания.ТекстСообщения                   = НСтр("ru = 'Обновление служебных данных по товарным позициям.'");
	ПараметрыОжидания.ОповещениеПользователя.Показать  = Истина;
	ПараметрыОжидания.ОповещениеПользователя.Текст 	   = НСтр("ru = 'Импорт данных'");
	ПараметрыОжидания.ОповещениеПользователя.Пояснение = НСтр("ru = 'Завершено обновление служебных данных по товарным позициям.'");
	ПараметрыОжидания.ОповещениеПользователя.Картинка  = БиблиотекаКартинок.ЛоготипOzon2;
	
	ДлительныеОперацииКлиент.ОжидатьЗавершение(ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания);

КонецПроцедуры

&НаСервере
Функция ОбновитьСлужебныеДанныеНаСервере()
	
	ПараметрыВыполнения 							 = ДлительныеОперации.ПараметрыВыполненияФункции(УникальныйИдентификатор);
	ПараметрыВыполнения.ОжидатьЗавершение            = 0;
	ПараметрыВыполнения.НаименованиеФоновогоЗадания  = НСтр("ru = 'Ozon. Обновление служебных данных по товарным позициям.'");
	ПараметрыВыполнения.ЗапуститьВФоне               = Истина;
	ПараметрыВыполнения.ПрерватьВыполнениеЕслиОшибка = Истина;
	
	ИмяМетода = "ИнтеграцияСМаркетплейсомOzonСервер.ИмпортИзOzonОбновитьСлужебныеДанные";
	Возврат ДлительныеОперации.ВыполнитьФункцию(ПараметрыВыполнения, 
		ИмяМетода, 
		УчетнаяЗаписьМаркетплейса, 
		РеквизитФормыВЗначение("ИмпортируемыеТовары"),
		ЭтаФорма.СортироватьСтрокиДереваТоваров);
	
КонецФункции

&НаКлиенте
Процедура ОбновитьСлужебныеДанныеЗавершениеФоновогоЗадания(Результат, ДополнительныеПараметры) Экспорт

	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		Если Результат.Статус = "Ошибка" Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(Результат.ПодробноеПредставлениеОшибки);
		
		ИначеЕсли Результат.Статус = "Выполнено" И Результат.Свойство("АдресРезультата") Тогда
		
			Ошибка = РезультатОбновленияСлужебныхДанныхНаСервере(Результат.АдресРезультата);
			ИнтеграцияСМаркетплейсомOzonКлиент.ВывестиСостояние(Ошибка, ДополнительныеПараметры, Истина);
			
			РазвернутьУзлыДерева(Истина);
			
			ПараметрыКоманда = Новый Структура( "Имя", "ПолучитьДанныеСТорговойПлощадки" );
			ПолучитьДанныеСТорговойПлощадки( ПараметрыКоманда );
			
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция РезультатОбновленияСлужебныхДанныхНаСервере(Знач АдресРезультата)
	
	Результат = ПолучитьИзВременногоХранилища(АдресРезультата);
	
	Если ПустаяСтрока(Результат.Ошибка.КодОшибки) Тогда
		Если Результат.Свойство("ДеревоТоваров") Тогда
			ЗначениеВРеквизитФормы(Результат.ДеревоТоваров, "ИмпортируемыеТовары");
			ПолучитьДанныеЛегенды(Результат.ДеревоТоваров);
			УстановитьВидимостьДоступность();
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат.Ошибка;
	
КонецФункции

&НаСервере
Функция ПолучитьДанныеСТорговойПлощадкиНаСервере( КэшКатегорий, Знач СледующаяПорцияДанных = Ложь)
	
	ПараметрыВыполнения 							 = ДлительныеОперации.ПараметрыВыполненияФункции(УникальныйИдентификатор);
	ПараметрыВыполнения.ОжидатьЗавершение            = 0;
	ПараметрыВыполнения.НаименованиеФоновогоЗадания  = НСтр("ru = 'Ozon. Получение данных по товарным позициям с торговой площадки.'");
	ПараметрыВыполнения.ЗапуститьВФоне               = Истина;
	ПараметрыВыполнения.ПрерватьВыполнениеЕслиОшибка = Истина;
	
	ПараметрыФункции = ИнтеграцияСМаркетплейсомOzonСервер.ИмпортИзOzonПараметрыНовый();
	
	ПараметрыФункции.ВариантЗагрузкиДанных = ВариантЗагрузкиДанных;
	ПараметрыФункции.КэшКатегорий = КэшКатегорий;
	ПараметрыФункции.ЗагрузитьЦеныПродажи = ЗагрузитьЦеныПродажи;
	ПараметрыФункции.ИмяИдентификатора = ЭтаФорма.ИмяИдентификатора;
	ПараметрыФункции.РазмерПорцииЗагрузкиДанных = ЭтаФорма.РазмерПорцииЗагрузкиДанных;
	
	ПараметрыФункции.ЗагрузитьОписанияАрхивныхПозиций = ЗагрузитьОписанияАрхивныхПозиций;
	
	ПараметрыФункции.СоздатьЭлементыНоменклатуры = СоздатьЭлементыНоменклатуры;
	ПараметрыФункции.ПерезаполнитьЗначенияРеквизитовНоменклатуры = ПерезаполнитьЗначенияРеквизитовНоменклатуры;
	ПараметрыФункции.СоздатьХарактеристикиНоменклатуры = СоздатьХарактеристикиНоменклатуры;
	
	ПараметрыФункции.СоздатьКатегорииНоменклатуры = СоздатьКатегорииНоменклатуры;
	ПараметрыФункции.СоздатьИерархиюКатегорийНоменклатуры = СоздатьИерархиюКатегорийНоменклатуры;
	
	ПараметрыФункции.СортироватьСтрокиДереваТоваров = ЭтаФорма.СортироватьСтрокиДереваТоваров;
	ПараметрыФункции.ОтображатьСопоставленные = ОтображатьСопоставленные;
	ПараметрыФункции.ОтображатьПоСтатусу = ОтображатьПоСтатусу;
	ПараметрыФункции.СледующаяПорцияДанных = СледующаяПорцияДанных;
	ПараметрыФункции.ИдентификаторыТоваров = ИдентификаторыТоваров.Выгрузить();
	
	Если ЭтаФорма.ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиОбновитьСопоставленныеКарточкиТоваров() Тогда
		ПараметрыФункции.ТоварыНайти = Ложь;
	Иначе
		ПараметрыФункции.ТоварыНайти = Истина;
	КонецЕсли;

	ПараметрИмпортируемыеТовары = РеквизитФормыВЗначение( "ИмпортируемыеТовары" );
	
	Если ЭтаФорма.ВариантЗагрузкиДанных = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
		// удаление всех пометок для получения новой порции
		ПараметрИмпортируемыеТоварыСтроки = ПараметрИмпортируемыеТовары.Строки.НайтиСтроки( Новый Структура( "Пометка", Истина ), Истина );
		Для Каждого ПараметрИмпортируемыеТоварыСтрока Из ПараметрИмпортируемыеТоварыСтроки Цикл
			ПараметрИмпортируемыеТоварыСтрока.Пометка = Ложь;
		КонецЦикла;
	КонецЕсли;

	ИмяМетода = "ИнтеграцияСМаркетплейсомOzonСервер.ИмпортИзOzonПолучитьИмпортируемыеДанныеСТорговойПлощадки";
	Возврат ДлительныеОперации.ВыполнитьФункцию(
		ПараметрыВыполнения, 
		ИмяМетода, 
		УчетнаяЗаписьМаркетплейса, 
		ПараметрыФункции, 
		ПараметрИмпортируемыеТовары );
	
КонецФункции

&НаКлиенте
Процедура ПолучитьДанныеСТорговойПлощадкиЗавершениеФоновогоЗадания( Результат, ДополнительныеПараметры ) Экспорт

	Если ТипЗнч(Результат) = Тип("Структура") Тогда
	
		Если Результат.Свойство( "Сообщения" ) Тогда
			Для Каждого СообщениеЭлемент Из Результат.Сообщения Цикл
				ОбщегоНазначенияКлиент.СообщитьПользователю( СообщениеЭлемент.Текст );
			КонецЦикла;
		КонецЕсли;
	
		Если Результат.Статус = "Ошибка" Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(Результат.ПодробноеПредставлениеОшибки);
		
		ИначеЕсли Результат.Статус = "Выполнено" 
		 		  И Результат.Свойство("АдресРезультата") Тогда
		 		  
			КэшКатегорий = Неопределено;
			
			Ошибка = РезультатПолученияДанныхСТорговойПлощадкиНаСервере(Результат.АдресРезультата, КэшКатегорий);
			
			ИнтеграцияСМаркетплейсомOzonКлиент.ВывестиСостояние(Ошибка, ДополнительныеПараметры, Истина);
			
			Если ПустаяСтрока(Ошибка.КодОшибки) Тогда
				Если ЗначениеЗаполнено(КэшКатегорий) Тогда
					Для каждого КлючЗначение Из КэшКатегорий Цикл
						ИнтеграцияСМаркетплейсомOzonКлиент.СохранитьДанныеВКэшКатегорий(КлючЗначение.Значение, КлючЗначение.Ключ);	
					КонецЦикла;
				КонецЕсли;
			КонецЕсли;
			
			УстановитьФлажкиДляТребующихЗаписиТоваров( Неопределено );
			
			РазвернутьУзлыДерева(Истина);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция РезультатПолученияДанныхСТорговойПлощадкиНаСервере(Знач АдресРезультата, КэшКатегорий = Неопределено)
	
	Результат = ПолучитьИзВременногоХранилища(АдресРезультата);
	
	Если ПустаяСтрока(Результат.Ошибка.КодОшибки) Тогда
		Если Результат.Свойство("ДеревоТоваров") Тогда
		
			ЗначениеВРеквизитФормы( Результат.ДеревоТоваров, "ИмпортируемыеТовары" );
			
			ПолучитьДанныеЛегенды(Результат.ДеревоТоваров);
			УстановитьВидимостьДоступность();
		КонецЕсли;
		Если Результат.Свойство("ИдентификаторыТоваров") Тогда
			ИдентификаторыТоваров.Загрузить(Результат.ИдентификаторыТоваров);
		КонецЕсли;
		Если Результат.Свойство("КэшКатегорий") Тогда
			КэшКатегорий = Результат.КэшКатегорий;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат.Ошибка;
	
КонецФункции

&НаСервере
Функция ЗаписатьЗагруженныеДанныеНаСервере( Знач ЗаписатьТолькоЦены = Ложь, Знач ТолькоСоздатьНоменклатуру = Ложь, КэшКатегорийДерево = Неопределено )

	ПараметрыВыполнения 							 = ДлительныеОперации.ПараметрыВыполненияФункции(УникальныйИдентификатор);
	ПараметрыВыполнения.ОжидатьЗавершение            = 0;
	ПараметрыВыполнения.НаименованиеФоновогоЗадания  = НСтр("ru = 'Ozon. Запись загруженных данных по товарным позициям с торговой площадки.'");
	ПараметрыВыполнения.ЗапуститьВФоне               = Истина;
	ПараметрыВыполнения.ПрерватьВыполнениеЕслиОшибка = Истина;
	
	ДоступнаУстановкаЦен = ПравоДоступа("Проведение", Метаданные.Документы.УстановкаЦенНоменклатуры);
	
	ПараметрыФункции = ИнтеграцияСМаркетплейсомOzonСервер.ИмпортИзOzonПараметрыНовый();
	
	ПараметрыФункции.УчетнаяЗаписьМаркетплейса = УчетнаяЗаписьМаркетплейса;
	
	ПараметрыФункции.ВариантЗагрузкиДанных = ВариантЗагрузкиДанных;
	ПараметрыФункции.ЗагрузитьЦеныПродажи = ЗагрузитьЦеныПродажи И ДоступнаУстановкаЦен И НЕ ЗаписатьЦеныОтдельнойКомандой;
	ПараметрыФункции.ЗаписатьТолькоЦены = ЗагрузитьЦеныПродажи И ДоступнаУстановкаЦен И ЗаписатьТолькоЦены;
	ПараметрыФункции.ТолькоСоздатьНоменклатуру = ТолькоСоздатьНоменклатуру;
	
	ПараметрыФункции.ЗагрузитьОписанияАрхивныхПозиций = ЗагрузитьОписанияАрхивныхПозиций;
	
	ПараметрыФункции.ИмяИдентификатора = ЭтаФорма.ИмяИдентификатора;
	ПараметрыФункции.РазмерПорцииЗагрузкиДанных = ЭтаФорма.РазмерПорцииЗагрузкиДанных;
	
	ПараметрыФункции.СоздатьЭлементыНоменклатуры = СоздатьЭлементыНоменклатуры;
	ПараметрыФункции.ПерезаполнитьЗначенияРеквизитовНоменклатуры = ПерезаполнитьЗначенияРеквизитовНоменклатуры;
	ПараметрыФункции.ГруппаДляСозданияНовыхТоваров = ГруппаДляСозданияНовыхТоваров;
	ПараметрыФункции.СоздатьИерархиюНоменклатуры = СоздатьИерархиюНоменклатуры;
	ПараметрыФункции.СоздатьХарактеристикиНоменклатуры = СоздатьХарактеристикиНоменклатуры;
	ПараметрыФункции.ЗагрузитьИзображения = ЗагрузитьИзображения;
	
	ПараметрыФункции.СоздатьКатегорииНоменклатуры = СоздатьКатегорииНоменклатуры;
	ПараметрыФункции.СоздатьИерархиюКатегорийНоменклатуры = СоздатьИерархиюКатегорийНоменклатуры;
	
	ПараметрыФункции.ИсточникКатегории = ИсточникКатегории;
	ПараметрыФункции.КэшКатегорийДерево = КэшКатегорийДерево;
	
	ПараметрыФункции.СортироватьСтрокиДереваТоваров = ЭтаФорма.СортироватьСтрокиДереваТоваров;
	ПараметрыФункции.ОтображатьСопоставленные = ОтображатьСопоставленные;
	ПараметрыФункции.ОтображатьПоСтатусу = ОтображатьПоСтатусу;
	ПараметрыФункции.ДокументУстановкаЦенНоменклатуры = ДокументУстановкаЦенНоменклатуры;
	
	ИмяМетода = "ИнтеграцияСМаркетплейсомOzonСервер.ИмпортИзOzonЗаписатьЗагруженныеДанныеСТорговойПлощадки";
	Возврат ДлительныеОперации.ВыполнитьФункцию(ПараметрыВыполнения, 
		ИмяМетода, 
		УчетнаяЗаписьМаркетплейса, 
		РеквизитФормыВЗначение( "ИмпортируемыеТовары" ), 
		ПараметрыФункции);
	
КонецФункции

&НаКлиенте
Процедура ЗаписатьЗагруженныеДанныеЗавершениеФоновогоЗадания(Результат, ДополнительныеПараметры) Экспорт

	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		
		Если Результат.Свойство( "Сообщения" ) Тогда
			Для Каждого СообщениеЭлемент Из Результат.Сообщения Цикл
				ОбщегоНазначенияКлиент.СообщитьПользователю( СообщениеЭлемент.Текст );
			КонецЦикла;
		КонецЕсли;
		
		Если Результат.Статус = "Ошибка" Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю(Результат.ПодробноеПредставлениеОшибки);
			
		ИначеЕсли Результат.Статус = "Выполнено" 
			И Результат.Свойство("АдресРезультата") Тогда
			
			// текущая позиция курсора
			Если Элементы.ИмпортируемыеТовары.ТекущиеДанные <> Неопределено Тогда
				ТекущаяПозицияИдентификаторОбъектаМаркетплейса = Элементы.ИмпортируемыеТовары.ТекущиеДанные.ИдентификаторОбъектаМаркетплейса;
			КонецЕсли;
			
			Ошибка = РезультатЗаписиЗагруженныхДанныхНаСервере(Результат.АдресРезультата);
			ИнтеграцияСМаркетплейсомOzonКлиент.ВывестиСостояние(Ошибка, ДополнительныеПараметры, Истина);
			
			РазвернутьУзлыДерева(Истина);
			
			// текущая позиция курсора
			Если ЗначениеЗаполнено( ТекущаяПозицияИдентификаторОбъектаМаркетплейса ) Тогда
				ИмпортируемыеТоварыТекущаяСтрокаУстановить( ТекущаяПозицияИдентификаторОбъектаМаркетплейса );
			КонецЕсли;
			
		КонецЕсли;
	КонецЕсли;
	
	Оповестить( "OzonТоварыСписокОбновить" );
	
КонецПроцедуры

&НаСервере
Функция РезультатЗаписиЗагруженныхДанныхНаСервере(Знач АдресРезультата)

	Результат = ПолучитьИзВременногоХранилища(АдресРезультата);
		
	Если ПустаяСтрока(Результат.Ошибка.КодОшибки) Тогда
	
		ЗначениеВРеквизитФормы( Результат.ДеревоТоваров, "ИмпортируемыеТовары" );
		
		ПолучитьДанныеЛегенды(Результат.ДеревоТоваров);
		УстановитьВидимостьДоступность();
		
		Если Результат.НеЗаписано > 0 Тогда
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Не удалось записать товарных позиций: %1.'", 
					ОбщегоНазначения.КодОсновногоЯзыка()),
				Результат.НеЗаписано);
			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
			
			Для каждого ЭлементКоллекции Из Результат.Пропущено Цикл
				ОбщегоНазначения.СообщитьПользователю(
					Символы.Таб + "" + ЭлементКоллекции.Сообщение,,,
					ОбщегоНазначенияКлиентСервер.ПутьКТабличнойЧасти("ИмпортируемыеТовары", ЭлементКоллекции.НомерСтроки, "Номенклатура"));
			КонецЦикла;
		КонецЕсли;
		
		Если Результат.Записано > 0 Тогда
			Если Результат.ВТомЧислеНовых > 0 Тогда
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Записано товарных позиций: %1, в том числе новых: %2.'", 
						ОбщегоНазначения.КодОсновногоЯзыка()),
					Результат.Записано,
					Результат.ВТомЧислеНовых);
			Иначе
				ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'Записано товарных позиций: %1.'", 
						ОбщегоНазначения.КодОсновногоЯзыка()),
					Результат.Записано);
			КонецЕсли;
			ОбщегоНазначения.СообщитьПользователю( ТекстСообщения );
		    
		    
			ТекстСообщения = СтрШаблон( НСтр("ru = 'Номенклатура: создано %1, обновлено %2.'" ),
		    Результат.НоменклатураКоличествоСоздано, Результат.НоменклатураКоличествоОбновлено );
		    ОбщегоНазначения.СообщитьПользователю( ТекстСообщения );
			
		    ТекстСообщения = СтрШаблон( НСтр("ru = 'Характеристики номенклатуры: создано %1, обновлено %2.'" ),
		    Результат.ХарактеристикиКоличествоСоздано, Результат.ХарактеристикиКоличествоОбновлено );
		    ОбщегоНазначения.СообщитьПользователю( ТекстСообщения );

			ТекстСообщения = СтрШаблон( НСтр("ru = 'Категории номенклатуры: создано %1.'" ),
		    Результат.КатегорииНоменклатурыСоздано );
		    ОбщегоНазначения.СообщитьПользователю( ТекстСообщения );

			ТекстСообщения = СтрШаблон( НСтр("ru = 'Файлы: создано %1.'" ),
		    Результат.ФайлыКоличествоСоздано );
		    ОбщегоНазначения.СообщитьПользователю( ТекстСообщения );
			
			ТекстСообщения = СтрШаблон( НСтр("ru = 'Штрихкоды: создано %1.'" ),
		    Результат.ШтрихкодыКоличествоСоздано );
		    ОбщегоНазначения.СообщитьПользователю( ТекстСообщения );
		    
		    
			Если Результат.ЗагруженыЦены Тогда

				СозданныеДокументыУстановкаЦенНоменклатуры = ИнтеграцияСМаркетплейсамиСервер.СтруктураСвойствоЗначениеПолучить( Результат, 
				"СозданныеДокументыУстановкаЦенНоменклатуры" );

				Если ТипЗнч( СозданныеДокументыУстановкаЦенНоменклатуры ) = Тип( "Соответствие" ) Тогда
					ДокументыУстановкаЦенНоменклатуры = СозданныеДокументыУстановкаЦенНоменклатуры.Получить( УчетнаяЗаписьМаркетплейса );
					Если ТипЗнч( ДокументыУстановкаЦенНоменклатуры ) = Тип( "Массив" )
					И ДокументыУстановкаЦенНоменклатуры.Количество() > 0
					Тогда
						ЭтотОбъект.ДокументУстановкаЦенНоменклатуры = ДокументыУстановкаЦенНоменклатуры[ 0 ];
					КонецЕсли;
				КонецЕсли;
				
			КонецЕсли;
		КонецЕсли;
		
		
	КонецЕсли;

	Возврат Результат.Ошибка;
	
КонецФункции

Функция ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоИдентификатору(УчетнаяЗаписьМаркетплейса, ИсточникКатегории, ИдентификаторКатегории)
	Возврат ИнтеграцияСМаркетплейсомOzonСервер.ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоИдентификатору(УчетнаяЗаписьМаркетплейса, ИсточникКатегории, ИдентификаторКатегории);
КонецФункции

&НаКлиенте
Процедура СопоставитьКатегорииТовара(ТекущиеДанные)
	
	Если НЕ ЗначениеЗаполнено(ТекущиеДанные.ИдентификаторКатегорииМаркетплейса) Тогда 
		Возврат;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ТекущиеДанные.НаименованиеКатегорииМаркетплейса) Тогда 
		Возврат;
	КонецЕсли;
	
	ДанныеТекущейКатегорииМаркетплейса = Новый Структура;
	ДанныеТекущейКатегорииМаркетплейса.Вставить( "ВладелецКатегорииМаркетплейса", ТекущиеДанные.ИсточникКатегорииМаркетплейса );
	ДанныеТекущейКатегорииМаркетплейса.Вставить( "ИдентификаторКатегорииМаркетплейса", ТекущиеДанные.ИдентификаторКатегорииМаркетплейса );
	ДанныеТекущейКатегорииМаркетплейса.Вставить( "НаименованиеКатегорииМаркетплейса", ТекущиеДанные.НаименованиеКатегорииМаркетплейса );
	ДанныеТекущейКатегорииМаркетплейса.Вставить( "ПредставлениеКатегорииМаркетплейса", ТекущиеДанные.НаименованиеКатегорииМаркетплейса );
	ДанныеТекущейКатегорииМаркетплейса.Вставить( "Категория1С", ТекущиеДанные.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon );

	ДополнительныеПараметры = Новый Структура;
	ДополнительныеПараметры.Вставить( "УчетнаяЗаписьМаркетплейса", УчетнаяЗаписьМаркетплейса );
	ДополнительныеПараметры.Вставить( "ТекущаяКатегория1С", ?(ЗначениеЗаполнено(ТекущиеДанные.ИсточникКатегорииМаркетплейса ),
	ТекущиеДанные.ИсточникКатегорииМаркетплейса, ТекущиеДанные.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon) );
	ДополнительныеПараметры.Вставить( "ДанныеТекущейКатегорииМаркетплейса", ДанныеТекущейКатегорииМаркетплейса );
	
	Если НЕ ЗначениеЗаполнено( ДополнительныеПараметры.ТекущаяКатегория1С ) Тогда
		Возврат;
	КонецЕсли;
	
	Если ТекущиеДанные.ВариантЗагрузки = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
		
		СопоставлениеКатегорий = ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоИдентификатору(
			УчетнаяЗаписьМаркетплейса, 
			ТекущиеДанные.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon,
			ТекущиеДанные.ИдентификаторКатегорииМаркетплейса);
	
		Если СопоставлениеКатегорий = Неопределено Тогда
			ОповещениеОЗавершении = Новый ОписаниеОповещения("СопоставитьКатегорииТовараЗавершение", ЭтотОбъект, ДополнительныеПараметры);
			ПоказатьВопрос(ОповещениеОЗавершении, 
				НСтр("ru = 'Текущие категории учетной системы и маркетплейса не сопоставлены. Выполнить запись сопоставления категорий?'"), 
				РежимДиалогаВопрос.ДаНет, 
				, 
				КодВозвратаДиалога.Нет, 
				НСтр("ru = 'Сопоставление категорий'"), 
				КодВозвратаДиалога.Нет);
			Возврат;
		КонецЕсли;
		
	КонецЕсли;

	СопоставитьКатегорииТовараЗавершение( КодВозвратаДиалога.ОК, ДополнительныеПараметры );
	
КонецПроцедуры

&НаКлиенте
Процедура СопоставитьКатегорииТовараЗавершение(РезультатВопроса, ДополнительныеПараметры) Экспорт
	
	Если РезультатВопроса = КодВозвратаДиалога.Да Тогда
		
		ДанныеЗаписи = Новый Структура;
		ДанныеЗаписи.Вставить( "УчетнаяЗаписьМаркетплейса", УчетнаяЗаписьМаркетплейса);
		ДанныеЗаписи.Вставить( "ИдентификаторОбъектаМаркетплейса", ДополнительныеПараметры.ДанныеТекущейКатегорииМаркетплейса.ИдентификаторКатегорииМаркетплейса );
		ДанныеЗаписи.Вставить( "НаименованиеОбъектаМаркетплейса", ДополнительныеПараметры.ДанныеТекущейКатегорииМаркетплейса.НаименованиеКатегорииМаркетплейса );
		ДанныеЗаписи.Вставить( "Объект1С", ДополнительныеПараметры.ДанныеТекущейКатегорииМаркетплейса.Категория1С );

		СопоставлениеКатегорииТовараЗаписать( ДанныеЗаписи );
	КонецЕсли;
	
	ОткрытьФорму("Обработка.МаркетплейсOzon.Форма.СопоставлениеКатегорий", ДополнительныеПараметры);
	Оповестить("ОткрытиеФормыСопоставленияКатегорий", ДополнительныеПараметры);

КонецПроцедуры

&НаСервереБезКонтекста
Процедура СопоставлениеКатегорииТовараЗаписать( ДанныеЗаписи )
    ИнтеграцияСМаркетплейсомOzonСервер.СопоставлениеКатегорииТовараЗаписать( ДанныеЗаписи );
КонецПроцедуры

&НаСервере
Процедура ОбновитьКатегорииТоваров(Знач ИсточникКатегории)
	
	Если ИсточникКатегории = ПредопределенноеЗначение("Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры") Тогда
		Элементы.ИмпортируемыеТоварыКатегория.ОграничениеТипа = Новый ОписаниеТипов("СправочникСсылка.КатегорииНоменклатуры");
		ТипЗначения = ТипЗнч(Справочники.КатегорииНоменклатуры.ПустаяСсылка());
	ИначеЕсли ИсточникКатегории = ПредопределенноеЗначение("Перечисление.ИсточникиКатегорийДляМаркетплейса.ИерархияНоменклатуры") Тогда
		Элементы.ИмпортируемыеТоварыКатегория.ОграничениеТипа = Новый ОписаниеТипов("СправочникСсылка.Номенклатура");
		ТипЗначения = ТипЗнч(Справочники.Номенклатура.ПустаяСсылка());
	Иначе
		ТипЗначения = ТипЗнч(Неопределено);
	КонецЕсли;
	
	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		Если СтрокаДереваПервогоУровня.ВариантЗагрузки <> 2 Тогда
			Продолжить;
		КонецЕсли;
		
		Для каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.ПолучитьЭлементы() Цикл
			Если ТипЗнч(СтрокаДереваВторогоУровня.Категория) <> ТипЗначения Тогда
				ТекущаяКатегория = ИнтеграцияСМаркетплейсомOzonСервер.ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоНоменклатуре(
					УчетнаяЗаписьМаркетплейса,
					СтрокаДереваВторогоУровня.Номенклатура,
					СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса,
					ИсточникКатегории);
						
				СтрокаДереваВторогоУровня.Категория                                 = ТекущаяКатегория.Категория;
				СтрокаДереваВторогоУровня.ИсточникКатегорииМаркетплейса             = ТекущаяКатегория.ИсточникКатегории;
				СтрокаДереваВторогоУровня.ТекущееИдентификаторКатегорииМаркетплейса = ТекущаяКатегория.ИдентификаторКатегории;
				СтрокаДереваВторогоУровня.ТекущееНаименованиеКатегорииМаркетплейса  = ТекущаяКатегория.НаименованиеКатегории;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ЗагрузитьИзображение()
    
    // замена на поле HTML
    ТекущиеДанные = Элементы.ТаблицаИмпортированныхТоваровИзображения.ТекущиеДанные;
    Если ТекущиеДанные = Неопределено Тогда
    	ИзображениеПолеHTML = "";
    Иначе
    
    ТекстHTML = 
	"<html style=""height: 100%;"">
	|<head>
	|<meta name=""viewport"" content=""width=device-width, minimum-scale=0.1"">
	|<title> </title>
	|</head>
	|<body style=""margin: 0px; height: 100%;"">
	|<img style=""display: block; height: 100%; margin: 0 auto;"" src=""#Адрес изображения#"">
	|</body>
	|</html>";
    
    	ТекстHTML = СтрЗаменить( ТекстHTML, "#Адрес изображения#", ТекущиеДанные.ПутьКФайлу );
		ИзображениеПолеHTML = ТекстHTML;
	КонецЕсли;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ОпределитьПараметрыРасчетаСтавкиНДС(Знач УчетнаяЗаписьМаркетплейса)

	НастройкиУчетнойЗаписи = Справочники.УчетныеЗаписиМаркетплейсов.НастройкиУчетнойЗаписи(УчетнаяЗаписьМаркетплейса);
	ТипНалогообложенияНДС  = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС;
	//?(ПолучитьФункциональнуюОпцию("ИспользоватьУчетНДС"), Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС, Перечисления.ТипыНалогообложенияНДС.НеОблагаетсяНДС);
	
	Возврат Новый Структура("НалогообложениеНДС, Организация, Дата", 
		ТипНалогообложенияНДС, НастройкиУчетнойЗаписи.Организация, ТекущаяДатаСеанса());
	
КонецФункции

&НаСервере
Процедура ОбновитьСтрокуДереваТоваров(ИсходныйКлючЗаписи, КлючЗаписи)
	
	Запись = РегистрыСведений.СтатусыПубликацииОбъектовМаркетплейсаOzon.СоздатьМенеджерЗаписи();
	ЗаполнитьЗначенияСвойств(Запись, КлючЗаписи);
	Запись.Прочитать();
	
	Если НЕ Запись.Выбран() Тогда
		Возврат;
	КонецЕсли;
	
	НастройкиУчетнойЗаписи = Справочники.УчетныеЗаписиМаркетплейсов.НастройкиУчетнойЗаписи(УчетнаяЗаписьМаркетплейса);
	ВидыЦен                = ИнтеграцияСМаркетплейсомOzonСервер.ВидыЦенУчетнойЗаписи(УчетнаяЗаписьМаркетплейса, Истина, Истина);
	ИсточникКатегории      = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗаписьМаркетплейса, "ИсточникКатегории");
	
	Для Каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		Для Каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.ПолучитьЭлементы() Цикл 
		
			Если СтрокаДереваВторогоУровня.Номенклатура = ИсходныйКлючЗаписи.Номенклатура 
				 И СтрокаДереваВторогоУровня.Характеристика = ИсходныйКлючЗаписи.Характеристика
				 И СтрокаДереваВторогоУровня.Упаковка = ИсходныйКлючЗаписи.Упаковка Тогда
				 
				ДанныеОбъекта = Запись.ДанныеОбъекта.Получить();
				ЗначенияРеквизитовИзСервиса = СтрокаДереваВторогоУровня.ЗначенияРеквизитовИзСервиса.Выгрузить();

				// Текущие сопоставления категории
				ТекущаяКатегория = ИнтеграцияСМаркетплейсомOzonСервер.ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоНоменклатуре(
					УчетнаяЗаписьМаркетплейса,
					СтрокаДереваВторогоУровня.Номенклатура,
					СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса,
					ИсточникКатегории);
					
				СтрокаДереваВторогоУровня.ТекущееИдентификаторКатегорииМаркетплейса = ТекущаяКатегория.ИдентификаторКатегории;
				СтрокаДереваВторогоУровня.ТекущееНаименованиеКатегорииМаркетплейса  = ТекущаяКатегория.НаименованиеКатегории;
				
				// Значения реквизитов
				ИнтеграцияСМаркетплейсомOzonСервер.ДополнитьТаблицуЗначенийРеквизитов(
					УчетнаяЗаписьМаркетплейса,
					ЗначенияРеквизитовИзСервиса,
					Неопределено,
					Неопределено,
					СтрокаДереваВторогоУровня.Категория,
					ДанныеОбъекта,
					Истина);
					
				// Дерево реквизитов
				ДеревоРеквизитов = ИнтеграцияСМаркетплейсомOzonСервер.СформироватьДеревоРеквизитов(
					УчетнаяЗаписьМаркетплейса,
					ЗначенияРеквизитовИзСервиса,
					СтрокаДереваВторогоУровня,
					Неопределено,
					НастройкиУчетнойЗаписи,
					ВидыЦен);
					
				// Различия
				Если СтрокаДереваВторогоУровня.ВариантЗагрузки = 0 
					 И СтрокаДереваВторогоУровня.ИндексСтатуса <> 4 
					 И СтрокаДереваВторогоУровня.ИндексСтатуса <> 5 Тогда
					Отбор = Новый Структура("ЕстьРазличия, ИдентификаторАтрибутаМаркетплейса", Истина, "СтавкаНДС");
					ЕстьРазличияСтавкаНДС = ДеревоРеквизитов.Строки.НайтиСтроки(Отбор, Истина).Количество();
					СтрокаДереваВторогоУровня.ЕстьРазличияСтавкаНДС = (ЕстьРазличияСтавкаНДС > 0);
					
					Отбор = Новый Структура("ЕстьРазличия, ИдентификаторАтрибутаМаркетплейса", Истина, "ИдентификаторКатегорииМаркетплейса");
					ЕстьРазличияКатегорииOzon = ДеревоРеквизитов.Строки.НайтиСтроки(Отбор, Истина).Количество();
					СтрокаДереваВторогоУровня.ЕстьРазличияКатегорииOzon = (ЕстьРазличияКатегорииOzon > 0);
					
					Отбор = Новый Структура("ЕстьРазличия, ЦеновыеПоказатели", Истина, Истина);
					ЕстьРазличияЦеновыеПоказатели = ДеревоРеквизитов.Строки.НайтиСтроки(Отбор, Истина).Количество();
					СтрокаДереваВторогоУровня.ЕстьРазличияЦеновыеПоказатели = (ЕстьРазличияЦеновыеПоказатели > 0);
					
					Отбор = Новый Структура("ЕстьРазличия", Истина, Ложь);
					ЕстьРазличия = ДеревоРеквизитов.Строки.НайтиСтроки(Отбор, Истина).Количество() - ЕстьРазличияСтавкаНДС - ЕстьРазличияКатегорииOzon - ЕстьРазличияЦеновыеПоказатели;
					
					Если ЕстьРазличия > 0 Тогда
						СтрокаДереваВторогоУровня.Пометка = (ВариантЗагрузкиДанных <= 1);
						СтрокаДереваВторогоУровня.ИндексСтатуса = 6;
					ИначеЕсли ЗагрузитьЦеныПродажи И (ЕстьРазличияЦеновыеПоказатели > 0) Тогда
						СтрокаДереваВторогоУровня.Пометка = (ВариантЗагрузкиДанных <= 1);
					КонецЕсли;
					
				Иначе
					Отбор = Новый Структура("ЕстьРазличия, ИдентификаторАтрибутаМаркетплейса", Истина, "СтавкаНДС");
					ЕстьРазличияСтавкаНДС = ДеревоРеквизитов.Строки.НайтиСтроки(Отбор, Истина).Количество();
					СтрокаДереваВторогоУровня.ЕстьРазличияСтавкаНДС = (ЕстьРазличияСтавкаНДС > 0);
				КонецЕсли;
				
				// Перезаполнить реквизиты
				ЗаполнитьЗначенияСвойств(СтрокаДереваВторогоУровня, Запись,, "ДанныеОбъекта, ИсточникиСхожихПредложений");
				
				Если ДанныеОбъекта <> Неопределено Тогда
					СтрокаДереваВторогоУровня.ДанныеОбъекта.Загрузить(ДанныеОбъекта.Скопировать());
				КонецЕсли;
				
				СтрокаДереваВторогоУровня.ЗначенияРеквизитовИзСервиса.Загрузить(ЗначенияРеквизитовИзСервиса);
				
				СтрокаДереваВторогоУровня.ДеревоРеквизитов.ПолучитьЭлементы().Очистить();
				ОбщегоНазначения.ЗаполнитьКоллекциюЭлементовДереваДанныхФормы(СтрокаДереваВторогоУровня.ДеревоРеквизитов.ПолучитьЭлементы(), ДеревоРеквизитов);
				
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры	

&НаКлиенте
Процедура РазвернутьУзлыДерева(РазвернутьСПодчиненными = Ложь)
	
	Для каждого СтрокаДереваПервогоУровня Из ИмпортируемыеТовары.ПолучитьЭлементы() Цикл
		Элементы.ИмпортируемыеТовары.Развернуть(СтрокаДереваПервогоУровня.ПолучитьИдентификатор(), РазвернутьСПодчиненными);
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Функция ТоварыНайтиНаСервере()
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияФункции( ЭтаФорма.УникальныйИдентификатор );
	ПараметрыВыполнения.ОжидатьЗавершение = 0;
	ПараметрыВыполнения.НаименованиеФоновогоЗадания = НСтр("ru = 'Ozon. Поиск данных по товарным позициям.'");
	ПараметрыВыполнения.ЗапуститьВФоне = Истина;
	ПараметрыВыполнения.ПрерватьВыполнениеЕслиОшибка = Истина;
	
	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить( "ИмпортируемыеТовары", РеквизитФормыВЗначение( "ИмпортируемыеТовары" ) );
	ПараметрыФункции.Вставить( "УчетнаяЗаписьМаркетплейса", УчетнаяЗаписьМаркетплейса );
	ПараметрыФункции.Вставить( "ИсточникКатегории", ИсточникКатегории );
	
	ИмяМетода = "ИнтеграцияСМаркетплейсомOzonСервер.ТоварыНайти";
	Возврат ДлительныеОперации.ВыполнитьФункцию( ПараметрыВыполнения, ИмяМетода, ПараметрыФункции );
		
КонецФункции

&НаСервере
Функция ТоварыНайтиНаСервереЗаполнитьРезультатНаФорме( АдресРезультата )
	
	Результат = ПолучитьИзВременногоХранилища( АдресРезультата );
	ЗначениеВРеквизитФормы( Результат.ИмпортируемыеТовары, "ИмпортируемыеТовары" );
	
	Возврат Истина;
КонецФункции

&НаКлиенте
Процедура ТоварыНайтиНаСервереЗавершениеФоновогоЗадания( Результат, ДополнительныеПараметры) Экспорт
	
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		
		Если Результат.Свойство( "Сообщения" ) Тогда
			Для Каждого СообщениеЭлемент Из Результат.Сообщения Цикл
				ОбщегоНазначенияКлиент.СообщитьПользователю( СообщениеЭлемент.Текст );
			КонецЦикла;
		КонецЕсли;
		
		Если Результат.Статус = "Ошибка" Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю( Результат.ПодробноеПредставлениеОшибки );
			
		ИначеЕсли Результат.Статус = "Выполнено" И Результат.Свойство( "АдресРезультата" ) Тогда
			
			// текущая позиция курсора
			Если Элементы.ИмпортируемыеТовары.ТекущиеДанные <> Неопределено Тогда
				ТекущаяПозицияИдентификаторОбъектаМаркетплейса = Элементы.ИмпортируемыеТовары.ТекущиеДанные.ИдентификаторОбъектаМаркетплейса;
			КонецЕсли;
			
			ТоварыНайтиНаСервереЗаполнитьРезультатНаФорме( Результат.АдресРезультата );
			
			РазвернутьУзлыДерева( Истина );
			
			// текущая позиция курсора
			Если ЗначениеЗаполнено( ТекущаяПозицияИдентификаторОбъектаМаркетплейса ) Тогда
				ИмпортируемыеТоварыТекущаяСтрокаУстановить( ТекущаяПозицияИдентификаторОбъектаМаркетплейса );
			КонецЕсли;
			
		КонецЕсли;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура КнопкаНайтиТовары(Команда)

	// перед отправкой дерева на сервер проверим наличие пометок в дереве
	Если ИмпортируемыеТоварыЕстьПометки() = Ложь Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Необходимо пометить строки с товарами.'"));
		Возврат;
	КонецЕсли;
    
	ДлительнаяОперация = ТоварыНайтиНаСервере();
	ОповещениеОЗавершении = Новый ОписаниеОповещения( "ТоварыНайтиНаСервереЗавершениеФоновогоЗадания", ЭтотОбъект,);
	
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания( ЭтотОбъект );
	ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
	ПараметрыОжидания.ТекстСообщения = НСтр("ru = 'Поиск по товарным позициям.'");
	ПараметрыОжидания.ОповещениеПользователя.Показать = Истина;
	ПараметрыОжидания.ОповещениеПользователя.Текст = НСтр("ru = 'Поиск данных'");
	ПараметрыОжидания.ОповещениеПользователя.Пояснение = НСтр("ru = 'Завершен поиск данных по товарным позициям.'");
	ПараметрыОжидания.ОповещениеПользователя.Картинка = БиблиотекаКартинок.ЛоготипOzon2;
	
	ДлительныеОперацииКлиент.ОжидатьЗавершение( ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания );
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыТекущаяСтрокаУстановить( ИдентификаторОбъектаМаркетплейса )

		ИмпортируемыеТоварыТекущаяСтрока = Неопределено;
		
		СтрокиПервыйУровень = ИмпортируемыеТовары.ПолучитьЭлементы();
		Для Каждого СтрокаПервыйУровень Из СтрокиПервыйУровень Цикл

			СтрокиВторойУровень = СтрокаПервыйУровень.ПолучитьЭлементы();

			Для Каждого СтрокаВторойУровень Из СтрокиВторойУровень Цикл

				Если СтрокаВторойУровень.ИдентификаторОбъектаМаркетплейса = ИдентификаторОбъектаМаркетплейса Тогда
					ИмпортируемыеТоварыТекущаяСтрока = СтрокаВторойУровень.ПолучитьИдентификатор();
					Прервать;
				КонецЕсли;

			КонецЦикла;

			Если ЗначениеЗаполнено( ИмпортируемыеТоварыТекущаяСтрока ) Тогда
				Прервать;
			КонецЕсли;

		КонецЦикла;

		Если ЗначениеЗаполнено( ИмпортируемыеТоварыТекущаяСтрока ) Тогда
			Элементы.ИмпортируемыеТовары.ТекущаяСтрока = ИмпортируемыеТоварыТекущаяСтрока;
		КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура РежимПоискаИСозданияТоваровПриИзменении(Элемент)
	Если РежимПоискаИСозданияТоваров = Истина Тогда
		ОтображатьДанныеТовара = Ложь;
	КонецЕсли;
	УстановитьВидимостьДоступность();
КонецПроцедуры

&НаКлиенте
Асинх Процедура ИмпортируемыеТоварыПоОбъединениюНоменклатураЗаполнить()
	
	ИмпортируемыеТоварыТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;
	Если ИмпортируемыеТоварыТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Номенклатура = ИмпортируемыеТоварыТекущиеДанные.Номенклатура;
	
	ИдентификаторОбъектаМаркетплейса = ИмпортируемыеТоварыТекущиеДанные.ИдентификаторОбъектаМаркетплейса;
	ЗначениеОбъединенияВОднуКарточку = ИмпортируемыеТоварыТекущиеДанные.ЗначениеОбъединенияВОднуКарточку;
	ИдентификаторКатегорииМаркетплейса = ИмпортируемыеТоварыТекущиеДанные.ИдентификаторКатегорииМаркетплейса;
	
	ИмпортируемыеТоварыСтроки = ИмпортируемыеТоварыТекущиеДанные.ПолучитьРодителя().ПолучитьЭлементы();
	СтрокиПоЗначениюОбъединения = Новый Соответствие;
	Для Каждого ИмпортируемыеТоварыСтрока Из ИмпортируемыеТоварыСтроки Цикл
	
	   	Если ПустаяСтрока( ИдентификаторКатегорииМаркетплейса ) 
	   	Или ПустаяСтрока( ЗначениеОбъединенияВОднуКарточку ) Тогда
	   		Продолжить;
	   	КонецЕсли;
	
		Если ИмпортируемыеТоварыСтрока.ИдентификаторКатегорииМаркетплейса = ИдентификаторКатегорииМаркетплейса
		И ИмпортируемыеТоварыСтрока.ЗначениеОбъединенияВОднуКарточку = ЗначениеОбъединенияВОднуКарточку Тогда
			СтрокиПоЗначениюОбъединения.Вставить( ИмпортируемыеТоварыСтрока.ИдентификаторОбъектаМаркетплейса, ИмпортируемыеТоварыСтрока );
		КонецЕсли;
	КонецЦикла;
	
	Если СтрокиПоЗначениюОбъединения.Количество() <= 1 Тогда
		Возврат;
	КонецЕсли;

	ВопросШаблон = 
	НСтр( "ru = 'Найдено строк по значению объединения: %1.
	|Заполнить выбранную номенклатуру для всех
	|строк с одинаковым значением объединения ?'" );
	ВопросТекст = СтрШаблон( ВопросШаблон, СтрокиПоЗначениюОбъединения.Количество() );
    ОтветОбещание = ВопросАсинх( ВопросТекст, РежимДиалогаВопрос.ДаНетОтмена, , , "Заполнение по значению объединения" );
    Ответ = Ждать ОтветОбещание;
    Если Ответ <> КодВозвратаДиалога.Да Тогда
    	Возврат;
    КонецЕсли;
	
	// одинаковые значения по объединению	
	Для Каждого СтрокаПоЗначениюОбъединения Из СтрокиПоЗначениюОбъединения Цикл
		
		ИмпортируемыеТоварыДанные = СтрокаПоЗначениюОбъединения.Значение;
		
		ИмпортируемыеТоварыДанные.Номенклатура = Номенклатура;
		ИмпортируемыеТоварыДанные.Характеристика = Неопределено;
		
		ИмпортируемыеТоварыДанные.Категория = ИмпортируемыеТоварыТекущиеДанные.Категория;
		ИмпортируемыеТоварыДанные.ИсточникКатегорииМаркетплейса = ИмпортируемыеТоварыТекущиеДанные.ИсточникКатегорииМаркетплейса;
		ИмпортируемыеТоварыДанные.ЕдиницаИзмерения = ИмпортируемыеТоварыТекущиеДанные.ЕдиницаИзмерения;
		ИмпортируемыеТоварыДанные.ХарактеристикиИспользуются = ИмпортируемыеТоварыТекущиеДанные.ХарактеристикиИспользуются;
		
		ИмпортируемыеТоварыДанные.Пометка = Истина;
		
	КонецЦикла;
	
	СообщениеШаблон = НСтр( "ru = 'Заполное строк по значению объединения: %1'" );
	СообщениеТекст = СтрШаблон( СообщениеШаблон, СтрокиПоЗначениюОбъединения.Количество()) ;
	ПоказатьОповещениеПользователя( СообщениеТекст, , , БиблиотекаКартинок.ЛоготипOzon2 );

КонецПроцедуры

&НаКлиенте
Асинх Процедура ИмпортируемыеТоварыПоОбъединениюФлагСоздатьХарактеристикуЗаполнить()
	
	ИмпортируемыеТоварыТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;
	Если ИмпортируемыеТоварыТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	СоздатьХарактеристику = ИмпортируемыеТоварыТекущиеДанные.СоздатьХарактеристику;
	
	ИдентификаторОбъектаМаркетплейса = ИмпортируемыеТоварыТекущиеДанные.ИдентификаторОбъектаМаркетплейса;
	ЗначениеОбъединенияВОднуКарточку = ИмпортируемыеТоварыТекущиеДанные.ЗначениеОбъединенияВОднуКарточку;
	НаименованиеКатегорииМаркетплейса = ИмпортируемыеТоварыТекущиеДанные.НаименованиеКатегорииМаркетплейса;
	
	ИмпортируемыеТоварыСтроки = ИмпортируемыеТоварыТекущиеДанные.ПолучитьРодителя().ПолучитьЭлементы();
	СтрокиПоЗначениюОбъединения = Новый Соответствие;
	Для Каждого ИмпортируемыеТоварыСтрока Из ИмпортируемыеТоварыСтроки Цикл
		Если ИмпортируемыеТоварыСтрока.НаименованиеКатегорииМаркетплейса = НаименованиеКатегорииМаркетплейса
		И ИмпортируемыеТоварыСтрока.ЗначениеОбъединенияВОднуКарточку = ЗначениеОбъединенияВОднуКарточку Тогда
			СтрокиПоЗначениюОбъединения.Вставить( ИмпортируемыеТоварыСтрока.ИдентификаторОбъектаМаркетплейса, ИмпортируемыеТоварыСтрока );
		КонецЕсли;
	КонецЦикла;
	
	Если СтрокиПоЗначениюОбъединения.Количество() <= 1 Тогда
		Возврат;
	КонецЕсли;

	ВопросШаблон = 
	НСтр( "ru = 'Найдено строк по значению объединения: %1.
	|Заполнить флаг ""Создать характеристику"" для всех
	|строк с одинаковым значением объединения ?'" );
	ВопросТекст = СтрШаблон( ВопросШаблон, СтрокиПоЗначениюОбъединения.Количество() );
    ОтветОбещание = ВопросАсинх( ВопросТекст, РежимДиалогаВопрос.ДаНетОтмена, , , "Заполнение по значению объединения" );
    Ответ = Ждать ОтветОбещание;
    Если Ответ <> КодВозвратаДиалога.Да Тогда
    	Возврат;
    КонецЕсли;
	
	// одинаковые значения по объединению	
	Для Каждого СтрокаПоЗначениюОбъединения Из СтрокиПоЗначениюОбъединения Цикл
		
		ИмпортируемыеТоварыДанные = СтрокаПоЗначениюОбъединения.Значение;
		
		ИмпортируемыеТоварыДанные.СоздатьХарактеристику = СоздатьХарактеристику;
		
		ИмпортируемыеТоварыДанные.Пометка = Истина;
		
	КонецЦикла;
	
	СообщениеШаблон = НСтр( "ru = 'Заполное строк по значению объединения: %1'" );
	СообщениеТекст = СтрШаблон( СообщениеШаблон, СтрокиПоЗначениюОбъединения.Количество()) ;
	ПоказатьОповещениеПользователя( СообщениеТекст, , , БиблиотекаКартинок.ЛоготипOzon2 );

КонецПроцедуры

&НаКлиенте
Асинх Процедура ПоКнопкеЗагрузитьИСоздатьВсеТоварыИзOzon(Команда)

	ВопросТекст = 
	НСтр( "ru = 'Загрузить ВСЕ описания товаров из Ozon, кроме архивных, и создать элементы справочников Номенклатура, Характеристики, Категории номенклатуры, Присоединенные файлы, Штрихкоды, Цены и другие ?'" );
    ОтветОбещание = ВопросАсинх( ВопросТекст, РежимДиалогаВопрос.ДаНетОтмена, , , "Импорт описаний товаров из Ozon" );
    Ответ = Ждать ОтветОбещание;
    Если Ответ <> КодВозвратаДиалога.Да Тогда
    	Возврат;
    КонецЕсли;
	
	ПоказатьОповещениеПользователя(
		НСтр("ru = 'Импорт данных'"),
		, 
		НСтр("ru = 'Получение ВСЕХ описаний товаров с торговой площадки Ozon.'"),
		БиблиотекаКартинок.ЛоготипOzon2);
	
	
	ИмпортируемыеТовары.ПолучитьЭлементы().Очистить();
	
		
	КэшКатегорий = ИнтеграцияСМаркетплейсомOzonКлиент.КэшКатегорий();
	КэшКатегорий.Удалить( "ДеревоКатегорийДерево" );
	
	ДлительнаяОперация = ЗагрузитьИСоздатьВсеТоварыИзOzonНаСервере( КэшКатегорий );
	ОповещениеОЗавершении = Новый ОписаниеОповещения( "ЗагрузитьИСоздатьВсеТоварыИзOzonЗавершениеФоновогоЗадания", ЭтотОбъект );
	
	ПараметрыОжидания = ДлительныеОперацииКлиент.ПараметрыОжидания( ЭтотОбъект );
	ПараметрыОжидания.ВыводитьОкноОжидания = Истина;
	ПараметрыОжидания.ТекстСообщения = НСтр("ru = 'Получение данных по ВСЕМ товарным позициям с торговой площадки Ozon, кроме архивных.'");
	ПараметрыОжидания.ОповещениеПользователя.Показать = Истина;
	ПараметрыОжидания.ОповещениеПользователя.Текст = НСтр("ru = 'Импорт данных'");
	ПараметрыОжидания.ОповещениеПользователя.Пояснение = НСтр("ru = 'Завершено получение данных по товарным позициям с торговой площадки Ozon.'");
	ПараметрыОжидания.ОповещениеПользователя.Картинка = БиблиотекаКартинок.ЛоготипOzon2;
	
	ДлительныеОперацииКлиент.ОжидатьЗавершение( ДлительнаяОперация, ОповещениеОЗавершении, ПараметрыОжидания );
	
	
КонецПроцедуры

&НаСервере
Функция ЗагрузитьИСоздатьВсеТоварыИзOzonНаСервере( КэшКатегорий )
	
	ПараметрыВыполнения = ДлительныеОперации.ПараметрыВыполненияФункции(УникальныйИдентификатор);
	ПараметрыВыполнения.ОжидатьЗавершение = 0;
	ПараметрыВыполнения.НаименованиеФоновогоЗадания  = НСтр("ru = 'Ozon. Получение ВСЕХ описаний товаров с торговой площадки.'");
	ПараметрыВыполнения.ЗапуститьВФоне = Истина;
	ПараметрыВыполнения.ПрерватьВыполнениеЕслиОшибка = Истина;
	
	ПараметрыФункции = ИнтеграцияСМаркетплейсомOzonСервер.ИмпортИзOzonПараметрыНовый();
	
	ПараметрыФункции.ВариантЗагрузкиДанных = ВариантЗагрузкиДанных;
	ПараметрыФункции.КэшКатегорий = КэшКатегорий;
	ПараметрыФункции.ЗагрузитьЦеныПродажи = Истина;
	ПараметрыФункции.ИмяИдентификатора = ЭтаФорма.ИмяИдентификатора;
	
	РазмерПорцииЗагрузкиДанных = 100000;
	ПараметрыФункции.РазмерПорцииЗагрузкиДанных = РазмерПорцииЗагрузкиДанных;
	
	ПараметрыФункции.СоздатьЭлементыНоменклатуры = Истина;
	ПараметрыФункции.ПерезаполнитьЗначенияРеквизитовНоменклатуры = Истина;
	ПараметрыФункции.ГруппаДляСозданияНовыхТоваров = ГруппаДляСозданияНовыхТоваров;
	ПараметрыФункции.СоздатьИерархиюНоменклатуры = Истина;
	ПараметрыФункции.СоздатьХарактеристикиНоменклатуры = Истина;
	
	ПараметрыФункции.СоздатьКатегорииНоменклатуры = Истина;
	ПараметрыФункции.СоздатьИерархиюКатегорийНоменклатуры = Истина;
	
	ПараметрыФункции.СортироватьСтрокиДереваТоваров = Истина;
	ПараметрыФункции.ОтображатьСопоставленные = ОтображатьСопоставленные;
	ПараметрыФункции.ОтображатьПоСтатусу = ОтображатьПоСтатусу;
	ПараметрыФункции.СледующаяПорцияДанных = Ложь;
	ПараметрыФункции.ИдентификаторыТоваров = ИдентификаторыТоваров.Выгрузить();
	
	ПараметрыФункции.ТоварыНайти = Истина;
	
	ПараметрыФункции.ИмпортируемыеТовары = РеквизитФормыВЗначение( "ИмпортируемыеТовары" );
	
	ПараметрыФункции.УчетнаяЗаписьМаркетплейса = УчетнаяЗаписьМаркетплейса;
	ПараметрыФункции.ИсточникКатегории = ИсточникКатегории;
	
	ПараметрыФункции.ЗагрузитьИзображения = Истина;
	
	ПараметрыФункции.ЗаписатьТолькоЦены = Ложь;
	ПараметрыФункции.ТолькоСоздатьНоменклатуру = Ложь;
	ПараметрыФункции.КэшКатегорийДерево = Неопределено;
	ПараметрыФункции.ДокументУстановкаЦенНоменклатуры = Неопределено;
	ПараметрыФункции.ЗагрузитьОписанияАрхивныхПозиций = Ложь;
	
	ЭтаФорма.ЗагрузитьОписанияАрхивныхПозиций = Ложь;
	
	ИмяМетода = "ИнтеграцияСМаркетплейсомOzonСервер.ЗагрузитьИСоздатьВсеТоварыИзOzon";
	Возврат ДлительныеОперации.ВыполнитьФункцию( ПараметрыВыполнения, ИмяМетода, ПараметрыФункции );
	
КонецФункции

&НаКлиенте
Процедура ЗагрузитьИСоздатьВсеТоварыИзOzonЗавершениеФоновогоЗадания( Результат, ДополнительныеПараметры ) Экспорт

	Если ТипЗнч( Результат ) = Тип( "Структура" ) Тогда

		Если Результат.Свойство( "Сообщения" ) Тогда
			Для Каждого СообщениеЭлемент Из Результат.Сообщения Цикл
				ОбщегоНазначенияКлиент.СообщитьПользователю( СообщениеЭлемент.Текст );
			КонецЦикла;
		КонецЕсли;

		Если Результат.Статус = "Ошибка" Тогда
			ОбщегоНазначенияКлиент.СообщитьПользователю( Результат.ПодробноеПредставлениеОшибки );
		КонецЕсли;
		
		Если Результат.Свойство( "АдресРезультата" ) Тогда
			ЗагрузитьИСоздатьВсеТоварыИзOzonРезультатОбработатьНаСервере( Результат.АдресРезультата );
		КонецЕсли;
		
	КонецЕсли;
	
	РазвернутьУзлыДерева( Истина );
	
	Если ИмпортируемыеТовары.ПолучитьЭлементы().Количество() > 0 Тогда
		ФормаРежимОтображенияЭлементыОбновить( ИнтеграцияСМаркетплейсомOzonКлиентСервер.ФормаРежимОтображенияЗагрузитьВсеОписания() ); 
	Иначе
		ПоказатьПредупреждение( Новый ОписаниеОповещения, НСтр("ru = 'Не найдено новых описаний товаров для импорта на сайте Ozon.'") );
		ФормаРежимОтображенияЭлементыОбновить( ИнтеграцияСМаркетплейсомOzonКлиентСервер.ФормаРежимОтображенияОткрытие() ); 
	КонецЕсли;
	
	Оповестить( "КатегорииПослеЗаписиСоответствия" );
	Оповестить( "OzonТоварыСписокОбновить" );
	
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьИСоздатьВсеТоварыИзOzonРезультатОбработатьНаСервере( АдресРезультата )
	
	Результат = ПолучитьИзВременногоХранилища( АдресРезультата );
	Если Результат = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ЗначениеВРеквизитФормы( Результат.ДеревоТоваров, "ИмпортируемыеТовары" );
	
	ПолучитьДанныеЛегенды( Результат.ДеревоТоваров );
	УстановитьВидимостьДоступность();
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортируемыеТоварыСоздатьХарактеристикуПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.ИмпортируемыеТовары.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

    Если ТекущиеДанные.СоздатьХарактеристику = Ложь Тогда
    	Возврат;
    КонецЕсли;
	
	ИмпортируемыеТоварыПоОбъединениюФлагСоздатьХарактеристикуЗаполнить();
	
КонецПроцедуры

&НаКлиенте
Функция КатегорииИзИмпортируемыеТоварыПолучить()
	
	Категории = Новый Соответствие;
	
	ИмпортируемыеТоварыЭлементы = ЭтотОбъект.ИмпортируемыеТовары.ПолучитьЭлементы();
	Для Каждого ИмпортируемыеТоварыЭлемент Из ИмпортируемыеТоварыЭлементы Цикл
		ИмпортируемыеТоварыЭлементыВторогоУровня = ИмпортируемыеТоварыЭлемент.ПолучитьЭлементы();
		Для Каждого ИмпортируемыеТоварыЭлементВторогоУровня Из ИмпортируемыеТоварыЭлементыВторогоУровня Цикл

			Если ИмпортируемыеТоварыЭлементВторогоУровня.Пометка <> Истина Тогда
				Продолжить;
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено( ИмпортируемыеТоварыЭлементВторогоУровня.ИдентификаторКатегорииМаркетплейса ) Тогда
				Продолжить;
			КонецЕсли;
			
			Если Категории.Получить( ИмпортируемыеТоварыЭлементВторогоУровня.ИдентификаторКатегорииМаркетплейса ) <> Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			КатегорииЭлемент = Новый Структура;
			КатегорииЭлемент.Вставить( "ИдентификаторКатегорииМаркетплейса", ИмпортируемыеТоварыЭлементВторогоУровня.ИдентификаторКатегорииМаркетплейса );
			КатегорииЭлемент.Вставить( "НаименованиеКатегорииМаркетплейса", ИмпортируемыеТоварыЭлементВторогоУровня.НаименованиеКатегорииМаркетплейса );
			КатегорииЭлемент.Вставить( "Категория", ИмпортируемыеТоварыЭлементВторогоУровня.Категория );
			КатегорииЭлемент.Вставить( "КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon", ИмпортируемыеТоварыЭлементВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииOzon );
			
			Категории.Вставить( ИмпортируемыеТоварыЭлементВторогоУровня.ИдентификаторКатегорииМаркетплейса, КатегорииЭлемент );
		КонецЦикла;
	КонецЦикла;
	
	Возврат Категории;
КонецФункции

&НаКлиенте
Процедура КнопкаИмпортДанныхСопоставлениеКатегорий(Команда)
	
	Категории = КатегорииИзИмпортируемыеТоварыПолучить();
	Если Категории.Количество() = 0 Тогда
		СообщениеТекст = 
		"В описаниях товаров Ozon не заполнены категории.
		|Нажмите кнопку ""Получить данные Ozon"".";
		ПоказатьПредупреждение( Новый ОписаниеОповещения, СообщениеТекст );
		Возврат;
	КонецЕсли;
	
	ПараметрыФормы = Новый Структура;
	ПараметрыФормы.Вставить( "УчетнаяЗапись", УчетнаяЗаписьМаркетплейса );
	ПараметрыФормы.Вставить( "СопоставлениеКатегорий", Категории );
	ПараметрыФормы.Вставить( "ИсточникКатегории", ИсточникКатегории );
	ПараметрыФормы.Вставить( "ГруппаДляСозданияНовыхТоваров", ГруппаДляСозданияНовыхТоваров );
	
	ОткрытьФорму( "Обработка.МаркетплейсOzon.Форма.ИмпортДанныхСопоставлениеКатегорий", ПараметрыФормы, 
	ЭтотОбъект.УникальныйИдентификатор,,,,, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца );
КонецПроцедуры

&НаКлиенте
Процедура ПоКнопкеВключитьНастройкиИмпорта(Команда)

	ПараметрыИмпорта = ИнтеграцияСМаркетплейсомOzonКлиентСервер.ПараметрыИмпортаДляДетальнойЗагрузки();
	
	ЭтаФорма.ВариантЗагрузкиДанных = ПараметрыИмпорта.ВариантЗагрузкиДанных;

	ЭтаФорма.СоздатьЭлементыНоменклатуры = ПараметрыИмпорта.СоздатьЭлементыНоменклатуры;
	ЭтаФорма.ПерезаполнитьЗначенияРеквизитовНоменклатуры = ПараметрыИмпорта.ПерезаполнитьЗначенияРеквизитовНоменклатуры;
	ЭтаФорма.СоздатьИерархиюНоменклатуры = ПараметрыИмпорта.СоздатьИерархиюНоменклатуры;
	ЭтаФорма.СоздатьХарактеристикиНоменклатуры = ПараметрыИмпорта.СоздатьХарактеристикиНоменклатуры;
	
	ЭтаФорма.СоздатьКатегорииНоменклатуры = ПараметрыИмпорта.СоздатьКатегорииНоменклатуры;
	ЭтаФорма.СоздатьИерархиюКатегорийНоменклатуры = ПараметрыИмпорта.СоздатьИерархиюКатегорийНоменклатуры;

	ЭтаФорма.ЗагрузитьИзображения = ПараметрыИмпорта.ЗагрузитьИзображения;
	ЭтаФорма.ЗагрузитьЦеныПродажи = ПараметрыИмпорта.ЗагрузитьЦеныПродажи;
	
	ЭтаФорма.ЗагрузитьОписанияАрхивныхПозиций = ПараметрыИмпорта.ЗагрузитьОписанияАрхивныхПозиций;
	ЭтаФорма.РазмерПорцииЗагрузкиДанных = ПараметрыИмпорта.РазмерПорцииЗагрузкиДанных;
	

	ФормаПараметрыИмпортаДанныхОписаниеОбновить();
	
	
	ИмпортируемыеТовары.ПолучитьЭлементы().Очистить();    
	
	ФормаРежимОтображенияЭлементыОбновить( ИнтеграцияСМаркетплейсомOzonКлиентСервер.ФормаРежимОтображенияЗагрузитьСДетальнымиНастройками() );

	ПолучитьДанныеСТорговойПлощадки( Элементы.ПолучитьДанныеСТорговойПлощадки );
	
КонецПроцедуры

&НаКлиенте
Функция СтатистикаПоСтатусамОчистить()
	
	ЛегендаГотовыКПродаже = "";
	ЛегендаНеНайденыНаПлощадке = "";
	ЛегендаОбнаруженыРасхождения = "";
	ЛегендаОбновлениеНеТребуется = "";
	ЛегендаОжидаютИсправленияОшибок = "";
	ЛегендаОжидаютПолученияДанных = "";
	ЛегендаПеремещеныВАрхив = "";
	ЛегендаПодготовленыКПубликации = "";
	
КонецФункции

&НаКлиенте
Асинх Процедура КнопкаНазад(Команда)
	
	Если ИмпортируемыеТовары.ПолучитьЭлементы().Количество() > 0 Тогда
	
		ВопросТекст = 
		НСтр( "ru = 'Таблица товаров на форме будет очищена.
		|Продолжить?'" );
		ОтветОбещание = ВопросАсинх( ВопросТекст, РежимДиалогаВопрос.ДаНетОтмена, , , "Импорт описаний товаров из Ozon" );
		Ответ = Ждать ОтветОбещание;
		Если Ответ <> КодВозвратаДиалога.Да Тогда
			Возврат;
		КонецЕсли;
		
	КонецЕсли;
	
	ИмпортируемыеТовары.ПолучитьЭлементы().Очистить();    
	СтатистикаПоСтатусамОчистить();
	
	ФормаРежимОтображенияЭлементыОбновить( ИнтеграцияСМаркетплейсомOzonКлиентСервер.ФормаРежимОтображенияОткрытие() );
	
КонецПроцедуры

#КонецОбласти




#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "ТипПодключаемогоОборудования", Параметры.ТипПодключаемогоОборудования, ВидСравненияКомпоновкиДанных.Равно,, ЗначениеЗаполнено(Параметры.ТипПодключаемогоОборудования));
	
	Если Параметры.Свойство("ЭтоМППроверкаЦенников")Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбора(Список.Отбор, "ЭтоМППроверкаЦенников", Параметры.ЭтоМППроверкаЦенников, ВидСравненияКомпоновкиДанных.Равно,, ЗначениеЗаполнено(Параметры.ЭтоМППроверкаЦенников));
		ЭтоМППроверкаЦенников = Параметры.ЭтоМППроверкаЦенников;
		ЭтаФорма.АвтоЗаголовок = Ложь;
		ЭтаФорма.Заголовок = НСтр("ru = 'Правила обмена с мобильным приложением 1С:Проверка ценников'");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СписокПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	
	Если ЭтоМППроверкаЦенников Тогда
		
		Отказ = Истина;
		
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("ЭтоМППроверкаЦенников", Истина);
		
		ОткрытьФорму("Справочник.ПравилаОбменаСПодключаемымОборудованиемOffline.ФормаОбъекта", ПараметрыФормы, Элемент);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
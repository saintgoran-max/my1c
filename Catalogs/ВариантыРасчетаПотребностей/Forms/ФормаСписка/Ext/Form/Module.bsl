#Область ОбработчикиСобытийЭлементовТаблицыФормыСписок

&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ОткрытьФорму("Обработка.РасчетПотребностей.Форма", ПараметрыСпособаОбеспечения(ВыбраннаяСтрока));
	
	
КонецПроцедуры

&НаСервере
Функция ПараметрыСпособаОбеспечения(Ссылка)
	
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(
		Ссылка, 
		Новый Структура("СпособПополнения, ВариантРасчетаПотребностей", "СпособПополнения", "Ссылка"));
	
КонецФункции

#КонецОбласти

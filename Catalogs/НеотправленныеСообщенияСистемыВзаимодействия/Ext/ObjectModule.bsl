#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

Функция ОбъектыСообщенияСуществуют() Экспорт
	
	ПроверяемыеСсылки = Новый Массив;
	Если ЗначениеЗаполнено(Автор) Тогда
		ПроверяемыеСсылки.Добавить(Автор);
	КонецЕсли;
	Если ЗначениеЗаполнено(ОбсуждениеКонтекст) Тогда
		ПроверяемыеСсылки.Добавить(ОбсуждениеКонтекст);
	КонецЕсли;
	
	Для каждого ПроверяемаяСсылка Из ПроверяемыеСсылки Цикл
		Если Не ОбщегоНазначения.СсылкаСуществует(ПроверяемаяСсылка) Тогда
			Возврат Ложь;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

#КонецОбласти 

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли
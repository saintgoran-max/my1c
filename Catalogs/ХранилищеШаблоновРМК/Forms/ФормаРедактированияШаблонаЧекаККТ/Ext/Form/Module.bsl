
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ШаблонОбъекта         = Неопределено;
	ТаблицаУсловийОбъекта = Неопределено;
	СозданиеШаблона       = Ложь;
	ОбрабатыватьОбъектВФормеРедактирования = Параметры.ОбрабатыватьОбъектВФормеРедактирования;
	ИмяСвойстваВидимость = "Видимость";
	
	Если ОбрабатыватьОбъектВФормеРедактирования Тогда
		
		Если НЕ Параметры.Ключ.Пустая() Тогда
			ХранилищеШаблоновРМКОбъект = Параметры.Ключ.ПолучитьОбъект();
		ИначеЕсли НЕ Параметры.ЗначениеКопирования.Пустая() Тогда
			ДанныеКопирования = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Параметры.ЗначениеКопирования,
				"Наименование, Шаблон, Условия");
			ДанныеКопирования.Условия = ДанныеКопирования.Условия.Выгрузить();
			
			ХранилищеШаблоновРМКОбъект = Справочники.ХранилищеШаблоновРМК.СоздатьЭлемент();
			ЗаполнитьЗначенияСвойств(ХранилищеШаблоновРМКОбъект,ДанныеКопирования,,"Условия");
			ХранилищеШаблоновРМКОбъект.Условия.Загрузить(ДанныеКопирования.Условия);
			
			Модифицированность = Истина;
			СозданиеШаблона    = Истина;
		Иначе
			ХранилищеШаблоновРМКОбъект = Справочники.ХранилищеШаблоновРМК.СоздатьЭлемент();
			СозданиеШаблона            = Истина;
		КонецЕсли;
		Наименование          = ХранилищеШаблоновРМКОбъект.Наименование;
		ШаблонОбъекта         = ХранилищеШаблоновРМКОбъект.Шаблон.Получить();
		ТаблицаУсловийОбъекта = ХранилищеШаблоновРМКОбъект.Условия;
		
		ЗначениеВРеквизитФормы(ХранилищеШаблоновРМКОбъект, "ХранилищеШаблоновРМК");
		
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
			Элементы,
			"ГруппаЛево",
			ИмяСвойстваВидимость,
			Ложь);
	Иначе
		Если НЕ ПустаяСтрока(Параметры.АдресШаблона) Тогда
			ШаблонОбъекта = ПолучитьИзВременногоХранилища(Параметры.АдресШаблона);
		КонецЕсли;
		Если Параметры.ТаблицаУсловий <> Неопределено Тогда
			ТаблицаУсловийОбъекта = Параметры.ТаблицаУсловий;
		КонецЕсли;
		Если НЕ Параметры.Ссылка.Пустая() Тогда
			Наименование = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Параметры.Ссылка,
				"Наименование");
		КонецЕсли;
		
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
			Элементы,
			"Наименование",
			ИмяСвойстваВидимость,
			Ложь);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
			Элементы,
			"ФормаРедактироватьШаблон",
			ИмяСвойстваВидимость,
			Ложь);
		ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
			Элементы,
			"ФормаСохранитьФорму",
			ИмяСвойстваВидимость,
			Ложь);
	КонецЕсли;
	
	Заголовок = СформироватьЗаголовок(Наименование, СозданиеШаблона);
	
	ОбщегоНазначенияРМКПереопределяемый.ПереопределитьНастройкиФормыРедактированияШаблонаЧекаККТ(ЭтотОбъект);
	
	Если ТолькоПросмотрФормы Тогда
		НастроитьФормуТолькоДляПросмотра();
	КонецЕсли;
	
	Если ШаблонОбъекта <> Неопределено Тогда
		
		Если ШаблонОбъекта.Свойство("ДокументСсылка") Тогда
			ДокументСсылка = ШаблонОбъекта.ДокументСсылка;
		КонецЕсли;
		
		Если ШаблонОбъекта.Свойство("ШиринаЧека") Тогда
			ШиринаЧека = ШаблонОбъекта.ШиринаЧека;
		КонецЕсли;
		
		Если ШаблонОбъекта.Шаблон <> Неопределено
			И ТаблицаУсловийОбъекта <> Неопределено Тогда
			
			Для Каждого ОписаниеУсловия Из ТаблицаУсловийОбъекта Цикл
				ЗаполнитьЗначенияСвойств(ТаблицаУсловий.Добавить(), ОписаниеУсловия);
			КонецЦикла;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ШиринаЧека = 0 Тогда
		
		ОбщегоНазначенияРМКПереопределяемый.ЗаполнитьТекущуюКассуККМШаблонаЧека(ПараметрыСеанса, ТекущаяКассаККМ);
		
	КонецЕсли;
	
	// Поиск документа для тестирования шаблона.
	Если НЕ ЗначениеЗаполнено(ДокументСсылка) Тогда
		
		ОбщегоНазначенияРМКПереопределяемый.ЗаполнитьДокументДляШаблонаЧека(ДокументСсылка);
		
	КонецЕсли;
	
	СКД = Новый СхемаКомпоновкиДанных;
	ОбщегоНазначенияРМКПереопределяемый.ЗаполнитьМакетШаблонаЧека(СКД);
	АдресСКД = ПоместитьВоВременноеХранилище(СКД, УникальныйИдентификатор);
	ВозвращатьСКД = Истина;
	ИнициализироватьКомпоновщикНастроек(КомпоновщикНастроек,
		Элементы.КомпоновщикНастроекНастройкиВыборДоступныеПоляВыбора, АдресСКД);
	
	МиллиметровВПикселе =1;
	ОбщегоНазначенияРМКПереопределяемый.ЗаполнитьКоличествоМиллиметровВПикселе(МиллиметровВПикселе);
	КоличествоМиллиметровВПикселе = МиллиметровВПикселе;
	
	Если ШаблонОбъекта = Неопределено Тогда
		ЗагрузитьШаблонПоУмолчаниюСервер();
	ИначеЕсли ШаблонОбъекта.Шаблон = Неопределено Тогда
		ЗагрузитьШаблонПоУмолчаниюСервер();
	Иначе
		
		Попытка
			
			ШаблонПустой = РеквизитФормыВЗначение("ШаблонЧека");
			ТаблицаКолонок = Новый ТаблицаЗначений;
			ТаблицаКолонок.Колонки.Добавить("ИмяКолонки");
			ТаблицаКолонок.Колонки.Добавить("ВШаблоне");
			ТаблицаКолонок.Колонки.Добавить("ВСохраненном");
			
			КолонкиШаблона = ШаблонПустой.Колонки;
			
			Для Каждого КолонкаШаблона Из КолонкиШаблона Цикл
				
				НоваяСтрока = ТаблицаКолонок.Добавить();
				НоваяСтрока.ИмяКолонки = КолонкаШаблона.Имя;
				НоваяСтрока.ВШаблоне = 1;
				НоваяСтрока.ВСохраненном = 0;
				
			КонецЦикла;
			
			КолонкиСохраненные = ШаблонОбъекта.Шаблон.Колонки;
			
			Для Каждого КолонкаШаблона Из КолонкиСохраненные Цикл
				
				НоваяСтрока = ТаблицаКолонок.Добавить();
				НоваяСтрока.ИмяКолонки = КолонкаШаблона.Имя;
				НоваяСтрока.ВШаблоне = 0;
				НоваяСтрока.ВСохраненном = 1;
				
			КонецЦикла;
			
			ТаблицаКолонок.Свернуть("ИмяКолонки", "ВШаблоне,ВСохраненном");
			
			Для Каждого ПроверяемаяКолонка Из ТаблицаКолонок Цикл
				
				Если ПроверяемаяКолонка.ВШаблоне <> ПроверяемаяКолонка.ВСохраненном Тогда
					
					Если ПроверяемаяКолонка.ВШаблоне = 1 Тогда
						// Добавить в сохраненный.
						КолонкаДерева = КолонкиШаблона.Найти(ПроверяемаяКолонка.ИмяКолонки);
						Если КолонкаДерева <> Неопределено Тогда
							
							НоваяКолонка = КолонкиСохраненные.Добавить(
											КолонкаДерева.Имя,
											КолонкаДерева.ТипЗначения);
											
						КонецЕсли;
					
					Иначе
						// Удалить из сохраненного.
						КолонкаДерева = КолонкиСохраненные.Найти(ПроверяемаяКолонка.ИмяКолонки);
						
						Если КолонкаДерева <> Неопределено Тогда
							КолонкиСохраненные.Удалить(КолонкаДерева);
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЦикла;
			
			ЗаполнитьЗаголовкиПараметров(ШаблонОбъекта.Шаблон);
			ЗначениеВРеквизитФормы(ШаблонОбъекта.Шаблон, "ШаблонЧека");
			ОбойтиПредопределенныеРазделы("Перезаполнить");
			СформироватьМакетФискальногоЧека(Ложь);
			
		Исключение
			
			СтрокаОшибки = СтрШаблон("%1%2%3",
				НСтр("ru = 'Не удалось открыть сохраненный шаблон по причине:'"),
					Символы.ПС, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
				
			ОчиститьШаблонСервер();
			ОбщегоНазначения.СообщитьПользователю(СтрокаОшибки,,
				"ШаблонЧека");
				
		КонецПопытки;
			
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	Если ШиринаЧека = 0 Тогда
	
		Если ЗначениеЗаполнено(ТекущаяКассаККМ) Тогда
			
			ОповещениеПриЗавершении = Новый ОписаниеОповещения("ЗавершениеПолученияШириныЛенты",
				ЭтотОбъект);
				
			ОборудованиеЧекопечатающиеУстройстваКлиент.НачатьПолучениеШириныСтрокиПечатающегоУстройства(
				ОповещениеПриЗавершении, УникальныйИдентификатор, ТекущаяКассаККМ);
			
		Иначе
			ШиринаЧека = 32;
		КонецЕсли;
	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытием(Отказ, ЗавершениеРаботы, ТекстПредупреждения, СтандартнаяОбработка)
	
	Если ТолькоПросмотрФормы Тогда
		Возврат;
	КонецЕсли;
	
	Если Модифицированность И ЗавершениеРаботы Тогда
		
		Отказ = Истина;
		Возврат;
		
	КонецЕсли;
	
	Если Модифицированность И НЕ ПараметрЗакрытия Тогда
		
		Отказ = Истина;
		ОписаниеОповещения = Новый ОписаниеОповещения("ПередЗакрытиемЗавершение", ЭтотОбъект);
		ПоказатьВопрос(ОписаниеОповещения, НСтр("ru = 'Шаблон был изменен. Сохранить изменения?'"),
			РежимДиалогаВопрос.ДаНетОтмена,, КодВозвратаДиалога.Да);
		Возврат;
		
	КонецЕсли;
		
	Если Модифицированность И НЕ ОбрабатыватьОбъектВФормеРедактирования Тогда
		
		АдресРезультирующий = ПоместитьВоВременноеХранилищеШаблон();
		Оповестить("ИзмененШаблонЧекаРМК", АдресРезультирующий, ЭтотОбъект);
		
	КонецЕсли;
	
	Если Модифицированность И ОбрабатыватьОбъектВФормеРедактирования Тогда
		СохранитьХранилищеШаблоновРМКНаСервере(Отказ);
		Если НЕ Отказ Тогда
			ОповеститьОбИзменении(ХранилищеШаблоновРМК.Ссылка);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура ДокументСсылкаПриИзменении(Элемент)
	
	СформироватьМакетФискальногоЧека(Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ШиринаЧекаПриИзменении(Элемент)
	
	СформироватьМакетФискальногоЧека(Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура НаименованиеПриИзменении(Элемент)
	
	Модифицированность = Истина;
	
КонецПроцедуры

&НаКлиенте
Процедура НадписьТолькоПросмотрФормыОбработкаНавигационнойСсылки(Элемент, НавигационнаяСсылкаФорматированнойСтроки,
	СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ОбщегоНазначенияРМККлиентПереопределяемый.ПереопределитьОбработкуНавигационнойСсылкиШаблонаЧекаККТ(ЭтотОбъект,
		НавигационнаяСсылкаФорматированнойСтроки);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыШаблонЧека

&НаКлиенте
Процедура ШаблонЧекаПриИзменении(Элемент)
	
	СформироватьМакетФискальногоЧека(Истина);
	
КонецПроцедуры

&НаКлиенте
Процедура ШаблонЧекаПередНачаломДобавления(Элемент, Отказ, Копирование, Родитель, Группа, Параметр)
	
	Отказ = Истина;
	
КонецПроцедуры

&НаКлиенте
Процедура ШаблонЧекаПередНачаломИзменения(Элемент, Отказ)
	
	Отказ = Истина;
	
	Если ТекущееПолеЗапрещеноРедактировать() Тогда
		
		ПоказатьПредупреждение(, НСтр("ru = 'Текущее поле редактировать нельзя.'"));
		Возврат;
		
	КонецЕсли;
	
	ВыбраннаяСтрока = Элементы.ШаблонЧека.ТекущаяСтрока;
	Если ВыбраннаяСтрока <> Неопределено Тогда
		ОткрытиеФормыРедактированияПараметров(ВыбраннаяСтрока);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ШаблонЧекаВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	Если ТекущееПолеЗапрещеноРедактировать() Тогда
		
		ПоказатьПредупреждение(, НСтр("ru = 'Текущее поле редактировать нельзя.'"));
		Возврат;
		
	КонецЕсли;
	ОткрытиеФормыРедактированияПараметров(ВыбраннаяСтрока);
	
КонецПроцедуры

&НаКлиенте
Процедура ШаблонЧекаПередУдалением(Элемент, Отказ)
	
	Если ТекущееПолеЗапрещеноУдалять() Тогда
		ПоказатьПредупреждение(, НСтр("ru = 'Это поле удалять нельзя.'"));
		Отказ = Истина;
		Возврат;
	КонецЕсли;
	
	ТекущаяСтрокаДерева = Элементы.ШаблонЧека.ТекущиеДанные;
	Если ЗначениеЗаполнено(ТекущаяСтрокаДерева.Идентификатор) Тогда
		УдалитьСтрокиУсловийПоИдентификаторуКлиент(ТекущаяСтрокаДерева.Идентификатор);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ШаблонЧекаПриАктивизацииСтроки(Элемент)
	
	Элементы.ШаблонЧека.ИзменятьПорядокСтрок = НЕ ТекущееПолеЗапрещеноРедактировать();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыКомпоновщикНастроек

&НаКлиенте
Процедура КомпоновщикНастроекНастройкиВыборДоступныеПоляВыбораВыбор(Элемент, ВыбраннаяСтрока, Поле,
	СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Если ВыбраннаяСтрока <> Неопределено Тогда
		
		ПолеВыбора = КомпоновщикНастроек.Настройки.ДоступныеПоляВыбора.
			ПолучитьОбъектПоИдентификатору(ВыбраннаяСтрока);
			
		Если Не ПолеВыбора = Неопределено Тогда
			ИмяПараметра = Строка(ПолеВыбора.Поле);
			СтруктураПараметра = ОбщегоНазначенияРМККлиент.ДанныеВыбораПараметраШаблонаЧека(ПолеВыбора, ИмяПараметра);
			Если СтруктураПараметра <> Неопределено Тогда
				ПодготовленноеИмяПараметра = СтрЗаменить(ИмяПараметра,".","");
				ДобавитьПолеВШаблон("Текст", ПодготовленноеИмяПараметра, СтруктураПараметра);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура СохранитьЗакрытьФорму(Команда)
	
	ПроверкаУспешна = ОбойтиПредопределенныеРазделы("ПроверитьКорректность");
	
	Если ПроверкаУспешна Тогда
		
		ПараметрЗакрытия = Истина;
		Закрыть();
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СохранитьФорму(Команда)
	
	ПроверкаУспешна = ОбойтиПредопределенныеРазделы("ПроверитьКорректность");
	
	Если Модифицированность И ПроверкаУспешна И ОбрабатыватьОбъектВФормеРедактирования Тогда
		Отказ = Ложь;
		СохранитьХранилищеШаблоновРМКНаСервере(Отказ);
		Если НЕ Отказ Тогда
			ОповеститьОбИзменении(ХранилищеШаблоновРМК.Ссылка);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьШаблонПоУмолчанию(Команда)
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗагрузитьШаблонПоУмолчаниюОбработкаВопроса", ЭтотОбъект);
	ПоказатьВопрос(ОписаниеОповещения, НСтр("ru = 'Текущий шаблон будет заменен на шаблон по умолчанию?'"),
		РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьШаблонРасширенный(Команда)
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗагрузитьШаблонРасширенныйОбработкаВопроса", ЭтотОбъект);
	ПоказатьВопрос(ОписаниеОповещения, НСтр("ru = 'Текущий шаблон будет заменен расширенным примером шаблона?'"),
		РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьШаблон(Команда)
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ОчиститьШаблонОбработкаВопроса", ЭтотОбъект);
	ПоказатьВопрос(ОписаниеОповещения, НСтр("ru = 'Очистить текущий шаблон?'"), РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортироватьШаблонИзФайла(Команда)
	
	ДополнительныеПараметры = Новый Структура;
	ОписаниеОповещения = Новый ОписаниеОповещения("ИмпортироватьШаблонИзФайлаОбработкаВопроса",
		ЭтотОбъект, ДополнительныеПараметры);
	ПоказатьВопрос(ОписаниеОповещения, НСтр("ru = 'Текущий шаблон будет потерян. Продолжить?'"),
		РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

&НаКлиенте
Процедура ЭкспортироватьШаблонВФайл(Команда)
	
	ПараметрыСохранения = ФайловаяСистемаКлиент.ПараметрыСохраненияФайла();
	ПараметрыСохранения.Диалог.Расширение = "txt";
	ПараметрыСохранения.Диалог.Фильтр = НСтр("ru = 'Шаблон чека ККТ(*.txt)|*.txt'");
	
	АдресФайлаНаСервере = ЭкспортироватьШаблонВФайлСервер();
	
	ФайловаяСистемаКлиент.СохранитьФайл(Неопределено, АдресФайлаНаСервере,, ПараметрыСохранения);
	
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьПримерЧека(Команда)
	
	СформироватьМакетФискальногоЧека(Ложь);
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьСтроку(Команда)
	
	ИмяПараметра = "Текст";
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	ЗаголовокПараметра = ЗаголовкиПараметров.Получить(ИмяПараметра);
	
	СтруктураСтроки = Новый Структура;
	СтруктураСтроки.Вставить("ИндексКартинки", 4);
	СтруктураСтроки.Вставить("ЭтоГруппа", Ложь);
	СтруктураСтроки.Вставить("Заголовок", ЗаголовокПараметра);
	
	ДобавитьПолеВШаблон(ИмяПараметра, ЗаголовокПараметра, СтруктураСтроки);
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьСоставнуюСтроку(Команда)
	
	ИмяПараметра = "СоставнаяСтрока";
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	ЗаголовокПараметра = ЗаголовкиПараметров.Получить(ИмяПараметра);
	
	СтруктураСоставнойСтроки = Новый Структура;
	СтруктураСоставнойСтроки.Вставить("ИндексКартинки", 14);
	СтруктураСоставнойСтроки.Вставить("ЭтоГруппа", Истина);
	СтруктураСоставнойСтроки.Вставить("Заголовок", ЗаголовокПараметра);
	
	ДобавитьПолеВШаблон(ИмяПараметра, ЗаголовокПараметра, СтруктураСоставнойСтроки);
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьНефискальныйДокумент(Команда)
	
	ИмяПараметра = "НефискальныйДокумент";
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	ЗаголовокПараметра = ЗаголовкиПараметров.Получить(ИмяПараметра);
	
	СтруктураПоля = Новый Структура;
	СтруктураПоля.Вставить("ИндексКартинки", 1);
	СтруктураПоля.Вставить("ЭтоГруппа", Истина);
	СтруктураПоля.Вставить("Заголовок", ЗаголовокПараметра);
	
	ДобавитьПолеВШаблон(ИмяПараметра, ЗаголовокПараметра, СтруктураПоля);
	
КонецПроцедуры

&НаКлиенте
Процедура ДобавитьОтрезку(Команда)
	
	ИмяПараметра = "Отрезка";
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	ЗаголовокПараметра = ЗаголовкиПараметров.Получить(ИмяПараметра);
	
	СтруктураПоля = Новый Структура;
	СтруктураПоля.Вставить("ИндексКартинки", 4);
	СтруктураПоля.Вставить("ЭтоГруппа", Истина);
	СтруктураПоля.Вставить("Выравнивание", "Центр");
	СтруктураПоля.Вставить("Заголовок", ЗаголовокПараметра);
	
	ДобавитьПолеВШаблон(ИмяПараметра, ЗаголовокПараметра, СтруктураПоля);
	
КонецПроцедуры

&НаКлиенте
Процедура РедактироватьШаблон(Команда)
	
	РедактироватьШаблонНаСервере();
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область ОбработчикиОповещения

// АПК:1242-выкл. обработчик оповещения

&НаКлиенте
Процедура ЗагрузитьШаблонПоУмолчаниюОбработкаВопроса(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	ЗагрузитьШаблонПоУмолчаниюСервер();
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьШаблонРасширенныйОбработкаВопроса(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	ЗагрузитьШаблонРасширенныйСервер();
	
КонецПроцедуры

&НаКлиенте
Процедура ОчиститьШаблонОбработкаВопроса(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	ОчиститьШаблонСервер();
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортироватьШаблонИзФайлаОбработкаВопроса(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ <> КодВозвратаДиалога.Да Тогда
		Возврат;
	КонецЕсли;
	
	ПараметрыЗагрузки = ФайловаяСистемаКлиент.ПараметрыЗагрузкиФайла();
	ПараметрыЗагрузки.Диалог.Расширение = "txt";
	ПараметрыЗагрузки.Диалог.Фильтр = НСтр("ru = 'Шаблон фискального чека(*.txt)|*.txt'");
	
	ОбработчикОповещения = Новый ОписаниеОповещения(
		"ИмпортироватьШаблонИзФайлаОбработкаПомещенияФайлов",
		ЭтотОбъект,
		ДополнительныеПараметры); 
		
	ФайловаяСистемаКлиент.ЗагрузитьФайл(ОбработчикОповещения, ПараметрыЗагрузки);
	
КонецПроцедуры

&НаКлиенте
Процедура ИмпортироватьШаблонИзФайлаОбработкаПомещенияФайлов(ПомещенныйФайл, ДополнительныеПараметры) Экспорт
	
	Если ТипЗнч(ПомещенныйФайл) = Тип("Структура") Тогда
		
		АдресФайла = ПомещенныйФайл.Хранение;
		ИмпортироватьШаблонИзФайлаСервер(АдресФайла);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПередЗакрытиемЗавершение(Ответ, ДополнительныеПараметры) Экспорт
	
	Если Ответ = КодВозвратаДиалога.Отмена Тогда
		// отмена
	ИначеЕсли Ответ = КодВозвратаДиалога.Нет Тогда
		
		ПараметрЗакрытия = Ложь;
		Модифицированность = Ложь;
		Закрыть();
		
	Иначе
		
		ПроверкаУспешна = ОбойтиПредопределенныеРазделы("ПроверитьКорректность");
		
		Если ПроверкаУспешна Тогда
			
			ПараметрЗакрытия = Истина;
			Закрыть();
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОповещениеРедактированиеШаблонаЧека(ОтветФормы, ДополнительныеПараметры) Экспорт
	
	Если ТипЗнч(ОтветФормы) = Тип("Структура") Тогда
		
		ТекущаяСтрока = ШаблонЧека.НайтиПоИдентификатору(ОтветФормы.Идентификатор);
		ИдентификаторУсловий = ТекущаяСтрока.Идентификатор;
		Если Не ЗначениеЗаполнено(ТекущаяСтрока.Идентификатор) Тогда
			ИдентификаторУсловий = ОтветФормы.Идентификатор;
		КонецЕсли;
		ЗаполнитьЗначенияСвойств(ТекущаяСтрока, ОтветФормы);
		
		ТекущаяСтрока.Идентификатор = ИдентификаторУсловий;
		ПараметрыОтбора = Новый Структура;
		ПараметрыОтбора.Вставить("Идентификатор", Число(ИдентификаторУсловий));
		СтарыеСтрокиУсловий = ТаблицаУсловий.НайтиСтроки(ПараметрыОтбора);
		Для Каждого СтараяСтрока Из СтарыеСтрокиУсловий Цикл
			ТаблицаУсловий.Удалить(СтараяСтрока);
		КонецЦикла;
		Для Каждого АктуальноеУсловие Из ОтветФормы.ТаблицаУсловий Цикл
			НовоеУсловие = ТаблицаУсловий.Добавить();
			ЗаполнитьЗначенияСвойств(НовоеУсловие, АктуальноеУсловие);
			НовоеУсловие.Идентификатор = ИдентификаторУсловий;
		КонецЦикла;
		
		СформироватьМакетФискальногоЧека();
		
	КонецЕсли;
	
КонецПроцедуры

// АПК:1242-вкл.

#КонецОбласти

#Область ЗаполнениеШаблона

&НаКлиенте
Процедура ЗавершениеПолученияШириныЛенты(ПолученныеДанные, ДополнительныеДанные)Экспорт

	ШиринаЧекаПоУмолчанию = 32;
	
	ШиринаЧека = ?(ЕстьСвойство(ПолученныеДанные, "Результат")
		И ПолученныеДанные.Результат
		И ЕстьСвойство(ПолученныеДанные, "ВыходныеПараметры")
		И ЗначениеЗаполнено(ПолученныеДанные.ВыходныеПараметры),
			ПолученныеДанные.ВыходныеПараметры[0], ШиринаЧекаПоУмолчанию);
			
		СформироватьМакетФискальногоЧека(Истина);
		
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьШаблонПоУмолчаниюСервер()
	
	// совсем очищать шаблон нельзя - он не будет работать на ККТ.
	ЭлементыВерхнегоУровня = ШаблонЧека.ПолучитьЭлементы();
	ЭлементыВерхнегоУровня.Очистить();
	
	ИмяСвойстваЗаголовок = "Заголовок";
	
	СтруктураПараметраГруппы = Новый Структура;
	СтруктураПараметраГруппы.Вставить("ИндексКартинки", 3);
	СтруктураПараметраГруппы.Вставить("ЭтоГруппа", Истина);
	СтруктураПараметраГруппы.Вставить(ИмяСвойстваЗаголовок, "");
	
	СтруктураТаблицы = Новый Структура;
	СтруктураТаблицы.Вставить("ИндексКартинки", 10);
	СтруктураТаблицы.Вставить("ЭтоГруппа", Истина);
	СтруктураТаблицы.Вставить(ИмяСвойстваЗаголовок, "");
	
	СтруктураСоставнойСтроки = Новый Структура;
	СтруктураСоставнойСтроки.Вставить("ИндексКартинки", 14);
	СтруктураСоставнойСтроки.Вставить("ЭтоГруппа", Истина);
	СтруктураСоставнойСтроки.Вставить(ИмяСвойстваЗаголовок, "");
	
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	
	ИмяПараметра = "ФискальныйДокумент";
	СтруктураПараметраГруппы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаВерхнийУровень = ДобавитьПараметрВДерево(ШаблонЧека, ИмяПараметра,
		СтруктураПараметраГруппы.Заголовок, СтруктураПараметраГруппы);
	ИмяПараметра = "ПараметрыШапкиЧека";
	СтруктураПараметраГруппы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаШапка = ДобавитьПараметрВДерево(СтрокаВерхнийУровень, ИмяПараметра,
		СтруктураПараметраГруппы.Заголовок, СтруктураПараметраГруппы);
	ЗаполнитьРазделШапки(СтрокаШапка);
	
	ИмяПараметра = "СоставЧека";
	СтруктураПараметраГруппы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаСоставЧека = ДобавитьПараметрВДерево(СтрокаВерхнийУровень, ИмяПараметра,
		СтруктураПараметраГруппы.Заголовок, СтруктураПараметраГруппы);
	ИмяПараметра = "ТаблицаТовары";
	СтруктураТаблицы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаПозицииЧека = ДобавитьПараметрВДерево(СтрокаСоставЧека, ИмяПараметра,
		СтруктураТаблицы.Заголовок, СтруктураТаблицы);
	ИмяПараметра = "ФискальныйРаздел";
	СтруктураСоставнойСтроки.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаФискальная = ДобавитьПараметрВДерево(СтрокаПозицииЧека, ИмяПараметра,
		СтруктураСоставнойСтроки.Заголовок, СтруктураСоставнойСтроки);
	ЗаполнитьПоляФискальногоРаздела(СтрокаФискальная);
	
	ИмяПараметра = "ТаблицаОплат";
	СтруктураПараметраГруппы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаОплаты = ДобавитьПараметрВДерево(СтрокаВерхнийУровень, ИмяПараметра,
		СтруктураПараметраГруппы.Заголовок, СтруктураПараметраГруппы);
	ЗаполнитьРазделОплат(СтрокаОплаты);
	
	СформироватьМакетФискальногоЧека();
	
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьШаблонРасширенныйСервер()
	
	// совсем очищать шаблон нельзя - он не будет работать на ККТ.
	ЭлементыВерхнегоУровня = ШаблонЧека.ПолучитьЭлементы();
	ЭлементыВерхнегоУровня.Очистить();
	
	ИмяСвойстваЗаголовок = "Заголовок";
	
	СтруктураПараметраГруппы = Новый Структура;
	СтруктураПараметраГруппы.Вставить("ИндексКартинки", 3);
	СтруктураПараметраГруппы.Вставить("ЭтоГруппа", Истина);
	СтруктураПараметраГруппы.Вставить(ИмяСвойстваЗаголовок, "");
	
	СтруктураТаблицы = Новый Структура;
	СтруктураТаблицы.Вставить("ИндексКартинки", 10);
	СтруктураТаблицы.Вставить("ЭтоГруппа", Истина);
	СтруктураТаблицы.Вставить(ИмяСвойстваЗаголовок, "");
	
	СтруктураСоставнойСтроки = Новый Структура;
	СтруктураСоставнойСтроки.Вставить("ИндексКартинки", 14);
	СтруктураСоставнойСтроки.Вставить("ЭтоГруппа", Истина);
	СтруктураСоставнойСтроки.Вставить(ИмяСвойстваЗаголовок, "");
	
	СтруктураВычисляемойСтроки = Новый Структура;
	СтруктураВычисляемойСтроки.Вставить("ИндексКартинки", 4);
	СтруктураВычисляемойСтроки.Вставить("ЭтоГруппа", Ложь);
	СтруктураВычисляемойСтроки.Вставить("Вычислять", Истина);
	СтруктураВычисляемойСтроки.Вставить("Префикс", "");
	СтруктураВычисляемойСтроки.Вставить("Постфикс", "");
	СтруктураВычисляемойСтроки.Вставить(ИмяСвойстваЗаголовок, "");
	
	СтруктураНеВычисляемойСтроки = Новый Структура;
	СтруктураНеВычисляемойСтроки.Вставить("ИндексКартинки", 4);
	СтруктураНеВычисляемойСтроки.Вставить("ЭтоГруппа", Ложь);
	СтруктураНеВычисляемойСтроки.Вставить("Вычислять", Ложь);
	СтруктураНеВычисляемойСтроки.Вставить("Префикс", "");
	СтруктураНеВычисляемойСтроки.Вставить("Постфикс", "");
	СтруктураНеВычисляемойСтроки.Вставить(ИмяСвойстваЗаголовок, "");
	
	СтруктураСтрокиШтрихкода = Новый Структура;
	СтруктураСтрокиШтрихкода.Вставить("ИндексКартинки", 4);
	СтруктураСтрокиШтрихкода.Вставить("ЭтоГруппа", Ложь);
	СтруктураСтрокиШтрихкода.Вставить("Вычислять", Истина);
	СтруктураСтрокиШтрихкода.Вставить("ВыводитьКакШтрихкод", Истина);
	СтруктураСтрокиШтрихкода.Вставить("Префикс", "");
	СтруктураСтрокиШтрихкода.Вставить("Постфикс", "");
	СтруктураСтрокиШтрихкода.Вставить("ТипШтрихкода", 99);
	СтруктураСтрокиШтрихкода.Вставить(ИмяСвойстваЗаголовок, "");
	
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	ПараметрТекст = "Текст";
	
	ИмяПараметра = "ФискальныйДокумент";
	СтруктураПараметраГруппы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаВерхнийУровень = ДобавитьПараметрВДерево(ШаблонЧека, ИмяПараметра,
		СтруктураПараметраГруппы.Заголовок, СтруктураПараметраГруппы);
	ИмяПараметра = "ПараметрыШапкиЧека";
	СтруктураПараметраГруппы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаШапка = ДобавитьПараметрВДерево(СтрокаВерхнийУровень, ИмяПараметра,
		СтруктураПараметраГруппы.Заголовок, СтруктураПараметраГруппы);
	ЗаполнитьРазделШапки(СтрокаШапка);
	
	ИмяПараметра = "СоставЧека";
	СтруктураПараметраГруппы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаСоставЧека = ДобавитьПараметрВДерево(СтрокаВерхнийУровень, ИмяПараметра,
		СтруктураПараметраГруппы.Заголовок, СтруктураПараметраГруппы);
	ИмяПараметра = "ДисконтнаяКарта";
	СтруктураВычисляемойСтроки.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураВычисляемойСтроки.Префикс = СтрШаблон("%1 ", НСтр("ru = 'Карта:'"));
	СтрокаКарты = ДобавитьПараметрВДерево(СтрокаСоставЧека, ПараметрТекст, ИмяПараметра,
		СтруктураВычисляемойСтроки);
	ИмяПараметра = "ТаблицаТовары";
	СтруктураТаблицы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаПозицииЧека = ДобавитьПараметрВДерево(СтрокаСоставЧека, ИмяПараметра,
		СтруктураТаблицы.Заголовок, СтруктураТаблицы);
	
	КоличествоСимволов = ?(ШиринаЧека < 5, ШиринаЧека, ШиринаЧека - 3);
	ТекстРазделителя = "";
	
	Для Счетчик = 1 По КоличествоСимволов Цикл
		ТекстРазделителя = ТекстРазделителя + "-";
	КонецЦикла;
	
	СтруктураНеВычисляемойСтроки.Заголовок = ТекстРазделителя;
	СтрокаРазделительСтроки = ДобавитьПараметрВДерево(СтрокаПозицииЧека, ПараметрТекст, ТекстРазделителя,
		СтруктураНеВычисляемойСтроки);
	
	ИмяПараметра = "НомерСтрокиТовара";
	СтруктураВычисляемойСтроки.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураВычисляемойСтроки.Префикс = СтрШаблон("%1 ", НСтр("ru = 'Строка №:'"));
	СтруктураВычисляемойСтроки.Постфикс = "";
	СтрокаНомерСтроки = ДобавитьПараметрВДерево(СтрокаПозицииЧека, ПараметрТекст, ИмяПараметра,
		СтруктураВычисляемойСтроки);
	
	ИмяПараметра = "ФискальныйРаздел";
	СтруктураСоставнойСтроки.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаФискальная = ДобавитьПараметрВДерево(СтрокаПозицииЧека, ИмяПараметра,
		СтруктураСоставнойСтроки.Заголовок, СтруктураСоставнойСтроки);
	ЗаполнитьПоляФискальногоРаздела(СтрокаФискальная);
	
	ИмяПараметра = "СуммаСкидки";
	СтруктураВычисляемойСтроки.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураВычисляемойСтроки.Префикс = НСтр("ru = 'Сумма скидки по строке:'") + " ";
	СтруктураВычисляемойСтроки.Постфикс = "";
	СтрокаСкидки = ДобавитьПараметрВДерево(СтрокаПозицииЧека, ПараметрТекст, ИмяПараметра,
		СтруктураВычисляемойСтроки);
		
	ИмяПараметра = "СуммаБезСкидки";
	СтруктураВычисляемойСтроки.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураВычисляемойСтроки.Префикс = НСтр("ru = 'Сумма без скидки по строке:'") + " ";
	СтруктураВычисляемойСтроки.Постфикс = "";
	СтрокаСуммаБезСкидки = ДобавитьПараметрВДерево(СтрокаПозицииЧека, ПараметрТекст, ИмяПараметра,
		СтруктураВычисляемойСтроки);
		
	ИмяПараметра = "Штрихкод";
	СтруктураСтрокиШтрихкода.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаШтрихкода = ДобавитьПараметрВДерево(СтрокаПозицииЧека, ПараметрТекст, ИмяПараметра,
		СтруктураСтрокиШтрихкода);
	
	ШаблонПостфиксаКодаВозврата = НСтр("ru = '%1%2Сохраните чек для возврата%2'");
	
	ИмяПараметра = "КодВозвратаСБП";
	СтруктураСтрокиШтрихкода.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураСтрокиШтрихкода.ТипШтрихкода = 16;
	ТекстТипаПлатежнойСистемыККТ = Символы.ПС
		+ Строка(ПредопределенноеЗначение("Перечисление.ТипыПлатежнойСистемыККТ.СистемаБыстрыхПлатежей"));
	ПостфиксКодаВозврата = СтрШаблон(ШаблонПостфиксаКодаВозврата, ТекстТипаПлатежнойСистемыККТ, Символы.ПС);
	СтруктураСтрокиШтрихкода.Постфикс = ПостфиксКодаВозврата;
	СтрокаКодаВозвратаСБП = ДобавитьПараметрВДерево(СтрокаСоставЧека, ПараметрТекст, ИмяПараметра,
		СтруктураСтрокиШтрихкода);
	
	ИмяПараметра = "КодВозвратаНСПК";
	СтруктураСтрокиШтрихкода.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураСтрокиШтрихкода.ТипШтрихкода = 16;
	ТекстТипаПлатежнойСистемыККТ = Символы.ПС
		+ Строка(ПредопределенноеЗначение("Перечисление.ТипыПлатежнойСистемыККТ.СертификатНСПК"));
	ПостфиксКодаВозврата = СтрШаблон(ШаблонПостфиксаКодаВозврата, ТекстТипаПлатежнойСистемыККТ, Символы.ПС);
	СтруктураСтрокиШтрихкода.Постфикс = ПостфиксКодаВозврата;
	СтрокаКодаВозвратаНСПК = ДобавитьПараметрВДерево(СтрокаСоставЧека, ПараметрТекст, ИмяПараметра,
		СтруктураСтрокиШтрихкода);
	
	ИмяПараметра = "ТаблицаОплат";
	СтруктураПараметраГруппы.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтрокаОплаты = ДобавитьПараметрВДерево(СтрокаВерхнийУровень, ИмяПараметра,
		СтруктураПараметраГруппы.Заголовок, СтруктураПараметраГруппы);
	ЗаполнитьРазделОплат(СтрокаОплаты);
	
	ОбщегоНазначенияРМКПереопределяемый.ДобавитьПараметрыВШаблонЧека(ШаблонЧека);
	
	СформироватьМакетФискальногоЧека();
	
КонецПроцедуры

&НаСервере
Функция ЭкспортироватьШаблонВФайлСервер()
	
	АдресФайла = "";
	ШаблонЧекаОбъект = РеквизитФормыВЗначение("ШаблонЧека", Тип("ДеревоЗначений"));
	УсловияШаблонаЧека = РеквизитФормыВЗначение("ТаблицаУсловий", Тип("ТаблицаЗначений"));
	ДанныеШаблонаЧека = Новый Структура;
	ДанныеШаблонаЧека.Вставить("Шаблон", ШаблонЧекаОбъект);
	ДанныеШаблонаЧека.Вставить("Условия", УсловияШаблонаЧека);
	ИмяВременногоФайла = ПолучитьИмяВременногоФайла("xml");
	СохранениеВыполнено = ЗначениеВФайл(ИмяВременногоФайла, ДанныеШаблонаЧека);
	
	Если СохранениеВыполнено Тогда
		
		ДвоичныеДанные = Новый ДвоичныеДанные(ИмяВременногоФайла);
		АдресФайла = ПоместитьВоВременноеХранилище(ДвоичныеДанные, УникальныйИдентификатор);
		УдалитьФайлы(ИмяВременногоФайла);
		
	КонецЕсли;
	
	Возврат АдресФайла;
	
КонецФункции

&НаСервере
Процедура ИмпортироватьШаблонИзФайлаСервер(АдресФайла)
	
	Попытка
		
		ДвоичныеДанные = ПолучитьИзВременногоХранилища(АдресФайла);
		ИмяВременногоФайла = ПолучитьИмяВременногоФайла("xml");
		ДвоичныеДанные.Записать(ИмяВременногоФайла);
		ДанныеШаблонаЧека = ЗначениеИзФайла(ИмяВременногоФайла);
		
		УдалитьФайлы(ИмяВременногоФайла);
		
		Если ТипЗнч(ДанныеШаблонаЧека) = Тип("Структура") Тогда
			ШаблонЧекаОбъект = ДанныеШаблонаЧека.Шаблон.Скопировать();
			УсловияШаблонаЧека = ДанныеШаблонаЧека.Условия.Скопировать();
		Иначе
			ШаблонЧекаОбъект = ДанныеШаблонаЧека.Скопировать();
			УсловияШаблонаЧека = Новый ТаблицаЗначений;
		КонецЕсли;
		
		КолонкиОбъекта = ШаблонЧекаОбъект.Колонки;
		ШаблонСФормы = РеквизитФормыВЗначение("ШаблонЧека");
		КолонкиШаблона = ШаблонСФормы.Колонки;
		ДополнитьКолонкиОбъектаКолонкамиШаблона(КолонкиОбъекта, КолонкиШаблона);
		
		ЗаполнитьЗаголовкиПараметров(ШаблонЧекаОбъект);
		ЗначениеВРеквизитФормы(ШаблонЧекаОбъект, "ШаблонЧека");
		
		КолонкиОбъекта = УсловияШаблонаЧека.Колонки;
		УсловияСФормы = РеквизитФормыВЗначение("ТаблицаУсловий");
		КолонкиШаблона = УсловияСФормы.Колонки;
		ДополнитьКолонкиОбъектаКолонкамиШаблона(КолонкиОбъекта, КолонкиШаблона);
		ЗначениеВРеквизитФормы(УсловияШаблонаЧека, "ТаблицаУсловий");
		
		СформироватьМакетФискальногоЧека();
		
	Исключение
		
		ТекстОшибки = НСтр("ru = 'Ошибка загрузки шаблона по причине:'") + Символы.ПС 
			+ ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ВызватьИсключение ТекстОшибки;
		
	КонецПопытки;
	
КонецПроцедуры

&НаКлиенте
Функция ДобавитьПолеВШаблон(ИмяПараметра, ПредставлениеПараметра, СтруктураПараметра)
	
	ТекстСообщения = "";
	ИдентификаторСтроки = Неопределено;
	МожноДобавлять = Истина;
	Результат = Неопределено;
	ИмяПараметраТаблица = "Таблица";
	ИмяСвойстваЗаголовок = "Заголовок";
	Если ИмяПараметра <> "НефискальныйДокумент"
		И ИмяПараметра <> "Отрезка" Тогда
		
		ТекущаяСтрокаДерева = Элементы.ШаблонЧека.ТекущиеДанные;
		
		Если ТекущаяСтрокаДерева = Неопределено Тогда
			
			ТекстСообщения = ТекстСообщения + НСтр("ru = 'Добавлять строки в корневой раздел нельзя.'");
			МожноДобавлять = Ложь;
			
		Иначе
			
			Если НЕ ТекущаяСтрокаДерева.ЭтоГруппа Тогда
				
				Родитель = ТекущаяСтрокаДерева.ПолучитьРодителя();
				
				Если Родитель = Неопределено Тогда
					
					ТекстСообщения = ТекстСообщения + НСтр("ru = 'Добавлять строки в корневой раздел нельзя.'");
					МожноДобавлять = Ложь;
					
				Иначе
					ТекущаяСтрокаДерева = Родитель;
				КонецЕсли;
				
			КонецЕсли;
			
			Если ТекущаяСтрокаДерева <> Неопределено Тогда
				
				Если ТекущаяСтрокаДерева.ИмяКолонки = "НефискальныйДокумент"
					ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ФискальныйДокумент"
					ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ПараметрыШапкиЧека"
					ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ТаблицаОплат"
					ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "Отрезка"
					ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ФискальныйРаздел" Тогда
					
						ТекстСообщения = СтрШаблон("%1%2", ТекстСообщения,
							НСтр("ru = 'Раздел ""%1"" закрыт для редактирования. Добавлять строки в него нельзя.'"));
						ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстСообщения,
							ТекущаяСтрокаДерева.Элемент);
						МожноДобавлять = Ложь;
						
				ИначеЕсли ИмяПараметра = ИмяПараметраТаблица И ТекущаяСтрокаДерева.ИмяКолонки <> "СоставЧека" Тогда

					ТекстСообщения = СтрШаблон("%1%2", ТекстСообщения,
						НСтр("ru = 'Таблицу можно добавлять только в состав чека.'"));
					ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстСообщения,
						ТекущаяСтрокаДерева.Элемент);
					МожноДобавлять = Ложь;
					
				ИначеЕсли ИмяПараметра = "СоставнаяСтрока"
					И ТекущаяСтрокаДерева.ИмяКолонки <> "СоставЧека"
					И ТекущаяСтрокаДерева.ИмяКолонки <> ИмяПараметраТаблица
					И ТекущаяСтрокаДерева.ИмяКолонки <> "ТаблицаТовары"Тогда
					
						ТекстСообщения = СтрШаблон("%1%2", ТекстСообщения,
							НСтр("ru = 'Составную строку можно добавлять только в состав чека или в таблицу.'"));
						ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстСообщения,
							ТекущаяСтрокаДерева.Элемент);
						МожноДобавлять = Ложь;
						
					КонецЕсли;
					
				ИдентификаторСтроки = ТекущаяСтрокаДерева.ПолучитьИдентификатор();
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если МожноДобавлять Тогда
		
		Результат = ДобавитьПолеВШаблонСервер(ИдентификаторСтроки, ИмяПараметра, ПредставлениеПараметра, СтруктураПараметра);
		Если ИмяПараметра = "НефискальныйДокумент" Тогда
			
			ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
			
			ПараметрСоставЧека = "СоставЧека";
			ЗаголовокПараметраСоставЧека = ЗаголовкиПараметров.Получить(ПараметрСоставЧека);
			СтруктураПараметра.Вставить(ИмяСвойстваЗаголовок, ЗаголовокПараметраСоставЧека);
			Результат = ДобавитьПолеВШаблонСервер(Результат, ПараметрСоставЧека,
				ЗаголовокПараметраСоставЧека, СтруктураПараметра);
				
			Если ИспользоватьПромокоды Тогда
				ПараметрПромокоды = "Промокоды";
				ЗаголовокПараметраПромокоды = ЗаголовкиПараметров.Получить(ПараметрПромокоды);
				СтруктураПараметра.Вставить(ИмяСвойстваЗаголовок, ЗаголовокПараметраПромокоды);
				РезультатПромокоды = ДобавитьПолеВШаблонСервер(Результат,
					ПараметрПромокоды, ЗаголовокПараметраПромокоды, СтруктураПараметра);
				СтруктураТаблицыПромокодов = Новый Структура;
				СтруктураТаблицыПромокодов.Вставить("ИндексКартинки", 10);
				СтруктураТаблицыПромокодов.Вставить("ЭтоГруппа", Истина); 
				СтруктураТаблицыПромокодов.Вставить(ИмяСвойстваЗаголовок, ЗаголовокПараметраПромокоды);
				РезультатПромокодыКВыдаче = ДобавитьПолеВШаблонСервер(РезультатПромокоды,
					ИмяПараметраТаблица, "ПромокодыКВыдаче", СтруктураТаблицыПромокодов);
				ПараметрПоляПромокод = "ПромокодЗначениеПромокода";
				ЗаголовокПоляПромокод = ЗаголовкиПараметров.Получить(ПараметрПоляПромокод);
				СтруктураПоляПромокод = Новый Структура;
				СтруктураПоляПромокод.Вставить("ИндексКартинки", 4);
				СтруктураПоляПромокод.Вставить("ЭтоГруппа", Ложь);
				СтруктураПоляПромокод.Вставить("Вычислять", Истина);
				СтруктураПоляПромокод.Вставить("ВыводитьКакШтрихкод", Истина);
				СтруктураПоляПромокод.Вставить("ТипШтрихкода", 16);
				СтруктураПоляПромокод.Вставить(ИмяСвойстваЗаголовок, ЗаголовокПоляПромокод);
				РезультатПромокод = ДобавитьПолеВШаблонСервер(РезультатПромокодыКВыдаче,
					"Текст", ПараметрПоляПромокод, СтруктураПоляПромокод);
			КонецЕсли;
		КонецЕсли;
		
	Иначе
		ПоказатьПредупреждение(, ТекстСообщения);
	КонецЕсли;
	
	Если Результат <> Неопределено Тогда
		Элементы.ШаблонЧека.ТекущаяСтрока = Результат;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаСервере
Функция ДобавитьПолеВШаблонСервер(Знач ИдентификаторСтроки, Знач ИмяПараметра, Знач ПредставлениеПараметра, Знач СтруктураПараметра)
	
	Если ИдентификаторСтроки = Неопределено Тогда
		Родитель = ШаблонЧека;
	Иначе
		Родитель = ШаблонЧека.НайтиПоИдентификатору(ИдентификаторСтроки);
	КонецЕсли;
	СтрокаПараметр = ДобавитьПараметрВДерево(Родитель, ИмяПараметра, ПредставлениеПараметра, СтруктураПараметра);
	ИдентификаторРезультат = СтрокаПараметр.ПолучитьИдентификатор(); 
	
	СформироватьМакетФискальногоЧека();
	
	Возврат ИдентификаторРезультат;
	
КонецФункции

&НаСервере
Функция ДобавитьПараметрВДерево(Родитель, ИмяПараметра, ПредставлениеПараметра, СтруктураПараметра)
	
	ЭлементыДерева = Родитель.ПолучитьЭлементы();
	СтрокаПараметр = ЭлементыДерева.Добавить();
	ЗаполнитьЗначенияСвойств(СтрокаПараметр, СтруктураПараметра);
	
	СтрокаПараметр.ИмяКолонки = ИмяПараметра;
	СтрокаПараметр.Элемент = ПредставлениеПараметра;
	СтрокаПараметр.ИмяОбъекта = "ЧекККМ";
	СтрокаПараметр.ИмяМакета = "ПоляШаблона";
	
	Возврат СтрокаПараметр;
	
КонецФункции

&НаСервере
Процедура ОчиститьШаблонСервер()
	
	ШаблонДерево = РеквизитФормыВЗначение("ШаблонЧека");
	ШаблонДерево.Строки.Очистить();
	ЗначениеВРеквизитФормы(ШаблонДерево, "ШаблонЧека");
	СформироватьМакетФискальногоЧека();
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьРазделШапки(СтрокаШапка)
	
	СтруктураПредопределенногоПоля = Новый Структура;
	СтруктураПредопределенногоПоля.Вставить("ИндексКартинки", 6);
	СтруктураПредопределенногоПоля.Вставить("ЭтоГруппа", Ложь);
	СтруктураПредопределенногоПоля.Вставить("Вычислять", Истина);
	СтруктураПредопределенногоПоля.Вставить("Заголовок", "");
	
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	
	ПараметрыРазделаШапки = Новый Массив;
	ПараметрыРазделаШапки.Добавить("ТипРасчета");
	ПараметрыРазделаШапки.Добавить("КодСистемыНалогообложения");
	ПараметрыРазделаШапки.Добавить("ОтправительEmail");
	ПараметрыРазделаШапки.Добавить("ПокупательEmail");
	ПараметрыРазделаШапки.Добавить("ПокупательНомер");
	ПараметрыРазделаШапки.Добавить("ТелефонПлатежногоАгента");
	ПараметрыРазделаШапки.Добавить("ТелефонОператораПоПриемуПлатежей");
	ПараметрыРазделаШапки.Добавить("ТелефонОператораПеревода");
	ПараметрыРазделаШапки.Добавить("ТелефонПоставщика");
	ПараметрыРазделаШапки.Добавить("АдресОператораПеревода");
	ПараметрыРазделаШапки.Добавить("ИННОператораПеревода");
	ПараметрыРазделаШапки.Добавить("НаименованиеОператораПеревода");
	ПараметрыРазделаШапки.Добавить("ТелефонБанковскогоАгента");
	ПараметрыРазделаШапки.Добавить("ТелефонБанковскогоСубагента");
	ПараметрыРазделаШапки.Добавить("ОперацияПлатежногоАгента");
	ПараметрыРазделаШапки.Добавить("ОперацияБанковскогоАгента");
	ПараметрыРазделаШапки.Добавить("ОперацияБанковскогоСубагента");
	
	Для каждого ИмяПараметра Из ПараметрыРазделаШапки Цикл
		
		СтруктураПредопределенногоПоля.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
		ДобавитьПараметрВДерево(СтрокаШапка, ИмяПараметра, СтруктураПредопределенногоПоля.Заголовок,
		СтруктураПредопределенногоПоля);
		
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьПоляФискальногоРаздела(СтрокаФискальная)
	
	СтруктураФискальногоПоля = Новый Структура;
	СтруктураФискальногоПоля.Вставить("ИндексКартинки", 6);
	СтруктураФискальногоПоля.Вставить("ЭтоГруппа", Ложь);
	СтруктураФискальногоПоля.Вставить("Вычислять", Истина);
	СтруктураФискальногоПоля.Вставить("Префикс", "");
	СтруктураФискальногоПоля.Вставить("Постфикс", Символ(182));
	СтруктураФискальногоПоля.Вставить("Заголовок", "");
	
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	
	ИмяПараметра = "Наименование";
	СтруктураФискальногоПоля.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	ДобавитьПараметрВДерево(СтрокаФискальная, ИмяПараметра, ИмяПараметра, СтруктураФискальногоПоля);
	
	ИмяПараметра = "Количество";
	СтруктураФискальногоПоля.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураФискальногоПоля.Префикс = "";
	СтруктураФискальногоПоля.Постфикс = СтрШаблон(" %1 ", НСтр("ru = 'х'"));
	ДобавитьПараметрВДерево(СтрокаФискальная, ИмяПараметра, ИмяПараметра, СтруктураФискальногоПоля);
	
	ИмяПараметра = "ЦенаСоСкидками";
	СтруктураФискальногоПоля.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураФискальногоПоля.Префикс = "";
	СтруктураФискальногоПоля.Постфикс = СтрШаблон(" %1 ", НСтр("ru = '='"));
	ДобавитьПараметрВДерево(СтрокаФискальная, ИмяПараметра, ИмяПараметра, СтруктураФискальногоПоля);
	
	ИмяПараметра = "Сумма";
	СтруктураФискальногоПоля.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураФискальногоПоля.Префикс = "";
	СтруктураФискальногоПоля.Постфикс = Символ(182);
	ДобавитьПараметрВДерево(СтрокаФискальная, ИмяПараметра, ИмяПараметра, СтруктураФискальногоПоля);
	
	ИмяПараметра = "СуммаСкидок";
	СтруктураФискальногоПоля.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураФискальногоПоля.Префикс = СтрШаблон("%1 ", НСтр("ru = 'В т.ч. скидка:'"));
	СтруктураФискальногоПоля.Постфикс = Символ(182);
	ДобавитьПараметрВДерево(СтрокаФискальная, ИмяПараметра, ИмяПараметра, СтруктураФискальногоПоля);
	
	ИмяПараметра = "СтавкаНДС";
	СтруктураФискальногоПоля.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
	СтруктураФискальногоПоля.Префикс = СтрШаблон("%1 ", НСтр("ru = 'Ставка НДС:'"));
	СтруктураФискальногоПоля.Постфикс = "%";
	ДобавитьПараметрВДерево(СтрокаФискальная, ИмяПараметра, ИмяПараметра, СтруктураФискальногоПоля);
	
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьРазделОплат(СтрокаОплаты)
	
	СтруктураПоля = Новый Структура;
	СтруктураПоля.Вставить("ИндексКартинки", 6);
	СтруктураПоля.Вставить("ЭтоГруппа", Ложь);
	СтруктураПоля.Вставить("Вычислять", Истина);
	СтруктураПоля.Вставить("Заголовок", "");
	
	ЗаголовкиПараметров = СловарьЗаголовковПредопределенныхПараметров();
	
	ПараметрыРазделаОплат = Новый Массив;
	ПараметрыРазделаОплат.Добавить("Наличные");
	ПараметрыРазделаОплат.Добавить("Электронный1");
	ПараметрыРазделаОплат.Добавить("Электронный2");
	ПараметрыРазделаОплат.Добавить("Электронный3");
	ПараметрыРазделаОплат.Добавить("Электронный4");
	
	Для каждого ИмяПараметра Из ПараметрыРазделаОплат Цикл
		
		СтруктураПоля.Заголовок = ЗаголовкиПараметров.Получить(ИмяПараметра);
		ДобавитьПараметрВДерево(СтрокаОплаты, ИмяПараметра, СтруктураПоля.Заголовок, СтруктураПоля);
		
	КонецЦикла;
	
КонецПроцедуры

&НаСервере
Функция ОбойтиПредопределенныеРазделы(Знач Команда)
	
	Результат = Истина;
	ПозицияФискальногоДокумента = 0;
	ЕстьШапка = Ложь;
	ЕстьОплата = Ложь;
	ЕстьТовары = Ложь;
	ЕстьФискальныйРаздел = Ложь;
	
	ЭлементыВерхнегоУровня = ШаблонЧека.ПолучитьЭлементы();
	
	СчетчикСтрок = 0;
	Для Каждого СтрокаВерхнийУровень Из ЭлементыВерхнегоУровня Цикл
		
		СчетчикСтрок = СчетчикСтрок + 1;
		
		Если СтрокаВерхнийУровень.ИмяКолонки = "ФискальныйДокумент" Тогда
			
			ПозицияФискальногоДокумента = СчетчикСтрок;
			ЭлементыФискальногоЧека = СтрокаВерхнийУровень.ПолучитьЭлементы();
			
			Для Каждого СтрокаЧека Из ЭлементыФискальногоЧека Цикл
				
				Если СтрокаЧека.ИмяКолонки = "ПараметрыШапкиЧека" Тогда
					
					ЕстьШапка = Истина;
					
					Если Команда = "Перезаполнить" Тогда
						
						ЭлементыШапки = СтрокаЧека.ПолучитьЭлементы();
						ЭлементыШапки.Очистить();
						ЗаполнитьРазделШапки(СтрокаЧека);
						
					КонецЕсли;
					
				ИначеЕсли СтрокаЧека.ИмяКолонки = "СоставЧека" Тогда
					
					ЭлементыСостава = СтрокаЧека.ПолучитьЭлементы();
					
					Для Каждого СтрокаСостава Из ЭлементыСостава Цикл
						
						Если СтрокаСостава.ИмяКолонки = "ТаблицаТовары" Тогда
							
							ЕстьТовары = Истина;
							ЭлементыТоваров = СтрокаСостава.ПолучитьЭлементы();
							
							Для Каждого СтрокаТовара Из ЭлементыТоваров Цикл
								
								Если СтрокаТовара.ИмяКолонки = "ФискальныйРаздел" Тогда
									
									ЕстьФискальныйРаздел = Истина;
									
									Если Команда = "Перезаполнить" Тогда
										ЭлементыФискальные = СтрокаТовара.ПолучитьЭлементы();
										ЭлементыФискальные.Очистить();
										ЗаполнитьПоляФискальногоРаздела(СтрокаТовара);
									КонецЕсли;
									
								КонецЕсли;
								
							КонецЦикла;
							
						КонецЕсли;
						
					КонецЦикла;
					
				ИначеЕсли СтрокаЧека.ИмяКолонки = "ТаблицаОплат" Тогда
					
					ЕстьОплата = Истина;
					
					Если Команда = "Перезаполнить" Тогда
						
						ЭлементыОплаты = СтрокаЧека.ПолучитьЭлементы();
						ЭлементыОплаты.Очистить();
						ЗаполнитьРазделОплат(СтрокаЧека);
						
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если Команда = "ПроверитьКорректность" Тогда
		
		Если ПозицияФискальногоДокумента = 0 Тогда
			
			ОбщегоНазначения.СообщитьПользователю(
				НСтр("ru = 'Отсутствует обязательный раздел ""Фискальный документ""'"),
				,
				"ШаблонЧека");
			Результат = Ложь;
			
		ИначеЕсли ПозицияФискальногоДокумента > 1 Тогда
			
			ОбщегоНазначения.СообщитьПользователю(
				НСтр("ru = 'Фискальный документ должен быть первым в шаблоне'"),
				,
				"ШаблонЧека");
			Результат = Ложь;
			
		Иначе
			
			Если НЕ ЕстьШапка Тогда
				
				ОбщегоНазначения.СообщитьПользователю(
					НСтр("ru = 'Отсутствует обязательный раздел ""Шапка"" в фискальном документе'"),
					,
					"ШаблонЧека");
				Результат = Ложь;
				
			КонецЕсли;
			
			Если ЕстьТовары Тогда
				
				Если НЕ ЕстьФискальныйРаздел Тогда
					
					ОбщегоНазначения.СообщитьПользователю(
						НСтр("ru = 'Отсутствует ""Фискальный раздел"" в таблице товаров'"),
						,
						"ШаблонЧека");
					Результат = Ложь;
					
				КонецЕсли;
				
			Иначе
				
				ОбщегоНазначения.СообщитьПользователю(
					НСтр("ru = 'Отсутствует обязательный раздел ""Товары"" в фискальном документе'"),
					,
					"ШаблонЧека");
				Результат = Ложь;
				
			КонецЕсли;
			
			Если НЕ ЕстьОплата Тогда
				
				ОбщегоНазначения.СообщитьПользователю(
					НСтр("ru = 'Отсутствует обязательный раздел ""Оплата"" в фискальном документе'"),
					,
					"ШаблонЧека");
				Результат = Ложь;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// Очистка от лишних условий, которые могли остаться после удаления строки чека
	ИмяКолонкиИдентификатор = "Идентификатор";
	ТаблицаИдентификаторов = ТаблицаУсловий.Выгрузить(,ИмяКолонкиИдентификатор);
	ТаблицаИдентификаторов.Свернуть(ИмяКолонкиИдентификатор);
	ИдентификаторыУсловий = ТаблицаИдентификаторов.ВыгрузитьКолонку(ИмяКолонкиИдентификатор);
	Для Каждого СуществующийИдентификатор Из ПолучитьИдентификаторыЭлементов(ШаблонЧека) Цикл
		ИндексИдентификатора = ИдентификаторыУсловий.Найти(СуществующийИдентификатор);
		Если Не ИндексИдентификатора = Неопределено Тогда
			ИдентификаторыУсловий.Удалить(ИндексИдентификатора);
		КонецЕсли;
	КонецЦикла;
	// Идентификаторы, которые остались - не используются. Удалим лишние условия.
	УдалитьСтрокиУсловийПоИдентификатору(ИдентификаторыУсловий);
	
	Возврат Результат;
	
КонецФункции

&НаСервереБезКонтекста
Функция ПолучитьИдентификаторыЭлементов(СтрокаДерева)
	
	Результат = Новый Массив;
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(СтрокаДерева, "Идентификатор") Тогда
		Если ЗначениеЗаполнено(СтрокаДерева.Идентификатор) Тогда
			Результат.Добавить(Число(СтрокаДерева.Идентификатор));
		КонецЕсли;
	КонецЕсли;
	
	Строки = СтрокаДерева.ПолучитьЭлементы();
	Для Каждого ПодчиненнаяСтрока Из Строки Цикл
		ИдентификаторыПодчиненнойСтроки = ПолучитьИдентификаторыЭлементов(ПодчиненнаяСтрока);
		Для Каждого ИдентификаторПодчиненнойСтроки Из ИдентификаторыПодчиненнойСтроки Цикл
			Результат.Добавить(ИдентификаторПодчиненнойСтроки);
		КонецЦикла;
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

&НаСервере
Процедура УдалитьСтрокиУсловийПоИдентификатору(Идентификаторы)
	
	ИдентификаторыУсловий = Новый Массив();
	Если ТипЗнч(Идентификаторы) = Тип("Массив") Тогда
		ИдентификаторыУсловий = Идентификаторы;
	ИначеЕсли ТипЗнч(Идентификаторы) = Тип("Строка") Тогда
		ИдентификаторыУсловий = СтрРазделить(Идентификаторы, ",");
	Иначе
		ИдентификаторыУсловий.Добавить(Идентификаторы);
	КонецЕсли;
	Для Каждого ИдентификаторУдалить Из ИдентификаторыУсловий Цикл
		ПараметрыОтбора = Новый Структура;
		ПараметрыОтбора.Вставить("Идентификатор", Число(ИдентификаторУдалить));
		СтрокиУдалить = ТаблицаУсловий.НайтиСтроки(ПараметрыОтбора);
		Для Каждого СтрокаУдалить Из СтрокиУдалить Цикл
			ТаблицаУсловий.Удалить(СтрокаУдалить);
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура УдалитьСтрокиУсловийПоИдентификаторуКлиент(Идентификаторы)
	
	ИдентификаторыУсловий = Новый Массив();
	Если ТипЗнч(Идентификаторы) = Тип("Массив") Тогда
		ИдентификаторыУсловий = Идентификаторы;
	ИначеЕсли ТипЗнч(Идентификаторы) = Тип("Строка") Тогда
		ИдентификаторыУсловий = СтрРазделить(Идентификаторы, ",");
	Иначе
		ИдентификаторыУсловий.Добавить(Идентификаторы);
	КонецЕсли;
	Для Каждого ИдентификаторУдалить Из ИдентификаторыУсловий Цикл
		ПараметрыОтбора = Новый Структура;
		ПараметрыОтбора.Вставить("Идентификатор", Число(ИдентификаторУдалить));
		СтрокиУдалить = ТаблицаУсловий.НайтиСтроки(ПараметрыОтбора);
		Для Каждого СтрокаУдалить Из СтрокиУдалить Цикл
			ТаблицаУсловий.Удалить(СтрокаУдалить);
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Функция СловарьЗаголовковПредопределенныхПараметров()
	
	// Заголовки параметров, использующиеся командами таблицы формы ШаблонЧека
	ЗаголовкиПараметров = Новый Соответствие;
	ЗаголовкиПараметров.Вставить("Текст", НСтр("ru = 'Текст'"));
	ЗаголовкиПараметров.Вставить("СоставнаяСтрока", НСтр("ru = 'Составная строка'"));
	ЗаголовкиПараметров.Вставить("НефискальныйДокумент", НСтр("ru = 'Нефискальный документ'"));
	ЗаголовкиПараметров.Вставить("Отрезка", НСтр("ru = 'Отрезка'"));
	
	ЗаголовкиПараметров.Вставить("СоставЧека", НСтр("ru = 'Состав чека'"));
	ЗаголовкиПараметров.Вставить("Промокоды", НСтр("ru = 'Промокоды к выдаче'"));
	ЗаголовкиПараметров.Вставить("ПромокодыКВыдаче", НСтр("ru = 'Промокоды к выдаче'"));
	ЗаголовкиПараметров.Вставить("ПромокодЗначениеПромокода", НСтр("ru = 'Промокод.Значение промокода'"));
	
	// Заголовки параметров, использующиеся в ЗагрузитьШаблонПоУмолчаниюСервер и ЗагрузитьШаблонРасширенныйСервер
	ЗаголовкиПараметров.Вставить("ФискальныйДокумент", НСтр("ru = 'Фискальный документ'"));
	ЗаголовкиПараметров.Вставить("ПараметрыШапкиЧека", НСтр("ru = 'Шапка'"));
	ЗаголовкиПараметров.Вставить("ТаблицаТовары", НСтр("ru = 'Товары'"));
	ЗаголовкиПараметров.Вставить("ФискальныйРаздел", НСтр("ru = 'Фискальный раздел'"));
	ЗаголовкиПараметров.Вставить("ТаблицаОплат", НСтр("ru = 'Оплата'"));
	
	// Заголовки полей СКД используемые в ЗагрузитьШаблонРасширенныйСервер
	ЗаголовкиПараметров.Вставить("ДисконтнаяКарта", НСтр("ru = 'Дисконтная карта'"));
	ЗаголовкиПараметров.Вставить("НомерСтрокиТовара", НСтр("ru = 'Номер строки товара'"));
	ЗаголовкиПараметров.Вставить("СуммаСкидки", НСтр("ru = 'Сумма скидки'"));
	ЗаголовкиПараметров.Вставить("СуммаБезСкидки", НСтр("ru = 'Сумма без скидки'"));
	ЗаголовкиПараметров.Вставить("Штрихкод", НСтр("ru = 'Штрихкод'"));
	ЗаголовкиПараметров.Вставить("КодВозвратаСБП", НСтр("ru = 'QR-код для возврата, при оплате через СБП'"));
	ЗаголовкиПараметров.Вставить("КодВозвратаНСПК", НСтр("ru = 'QR-код для возврата, при оплате ЭС (ФЗ-491)'"));
	
	// Заголовки параметров, использующиеся в ЗаполнитьРазделШапки
	ЗаголовкиПараметров.Вставить("ТипРасчета", НСтр("ru = 'Тип расчета'"));
	ЗаголовкиПараметров.Вставить("КодСистемыНалогообложения", НСтр("ru = 'Код системы налогообложения'"));
	ЗаголовкиПараметров.Вставить("ОтправительEmail", НСтр("ru = 'E-mail отправителя'"));
	ЗаголовкиПараметров.Вставить("ПокупательEmail", НСтр("ru = 'E-mail получателя'"));
	ЗаголовкиПараметров.Вставить("ПокупательНомер", НСтр("ru = 'Телефон получателя'"));
	ЗаголовкиПараметров.Вставить("ТелефонПлатежногоАгента", НСтр("ru = 'Телефон платежного агента'"));
	ЗаголовкиПараметров.Вставить("ТелефонОператораПоПриемуПлатежей", НСтр("ru = 'Телефон оператора по приему платежей'"));
	ЗаголовкиПараметров.Вставить("ТелефонОператораПеревода", НСтр("ru = 'Телефон оператора перевода'"));
	ЗаголовкиПараметров.Вставить("ТелефонПоставщика", НСтр("ru = 'Телефон поставщика услуг'"));
	ЗаголовкиПараметров.Вставить("АдресОператораПеревода", НСтр("ru = 'Адрес оператора перевода'"));
	ЗаголовкиПараметров.Вставить("ИННОператораПеревода", НСтр("ru = 'ИНН оператора перевода'"));
	ЗаголовкиПараметров.Вставить("НаименованиеОператораПеревода", НСтр("ru = 'Наименование оператора перевода'"));
	ЗаголовкиПараметров.Вставить("ТелефонБанковскогоАгента", НСтр("ru = 'Телефон банковского агента'"));
	ЗаголовкиПараметров.Вставить("ТелефонБанковскогоСубагента", НСтр("ru = 'Телефон банковского субагента'"));
	ЗаголовкиПараметров.Вставить("ОперацияПлатежногоАгента", НСтр("ru = 'Операция платежного агента'"));
	ЗаголовкиПараметров.Вставить("ОперацияБанковскогоАгента", НСтр("ru = 'Операция банковского агента'"));
	ЗаголовкиПараметров.Вставить("ОперацияБанковскогоСубагента", НСтр("ru = 'Операция банковского субагента'"));
	
	// Заголовки параметров, использующиеся в ЗаполнитьПоляФискальногоРаздела
	ЗаголовкиПараметров.Вставить("Наименование", НСтр("ru = 'Наименование'"));
	ЗаголовкиПараметров.Вставить("Количество", НСтр("ru = 'Количество'"));
	ЗаголовкиПараметров.Вставить("ЦенаСоСкидками", НСтр("ru = 'Цена со скидками'"));
	ЗаголовкиПараметров.Вставить("Сумма", НСтр("ru = 'Сумма'"));
	ЗаголовкиПараметров.Вставить("СуммаСкидок", НСтр("ru = 'Сумма скидок'"));
	ЗаголовкиПараметров.Вставить("СтавкаНДС", НСтр("ru = 'Ставка НДС'"));
	
	// Заголовки параметров, использующиеся в ЗаполнитьРазделОплат
	ЗаголовкиПараметров.Вставить("Наличные", НСтр("ru = 'Наличными'"));
	ЗаголовкиПараметров.Вставить("Электронный1", НСтр("ru = 'Электронно'"));
	ЗаголовкиПараметров.Вставить("Электронный2", НСтр("ru = 'Постоплата'"));
	ЗаголовкиПараметров.Вставить("Электронный3", НСтр("ru = 'Предоплата'"));
	ЗаголовкиПараметров.Вставить("Электронный4", НСтр("ru = 'Встречное предоставление'"));
	
	Возврат ЗаголовкиПараметров;
	
КонецФункции

&НаСервере
Процедура ЗаполнитьЗаголовкиПараметров(Шаблон)
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Заголовок", "");
	СтрокиСПустымиЗаголовками = Шаблон.Строки.НайтиСтроки(ПараметрыОтбора, Истина);
	
	Если СтрокиСПустымиЗаголовками.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	Если НЕ ТолькоПросмотрФормы Тогда
		Модифицированность = Истина;
	КонецЕсли;
	
	ЗаголовкиПредопределенныхПараметров = СловарьЗаголовковПредопределенныхПараметров();
	ДоступныеПоляВыбора = КомпоновщикНастроек.Настройки.ДоступныеПоляВыбора.Элементы;
	
	Для каждого СтрокаШаблонаЧека Из СтрокиСПустымиЗаголовками Цикл
		ЗаголовокПараметра = Неопределено;
		Если СтрокаШаблонаЧека.ИмяКолонки <> "Текст" Тогда
			ЗаголовокПараметра = ЗаголовкиПредопределенныхПараметров.Получить(СтрокаШаблонаЧека.ИмяКолонки);
			Если ЗаголовокПараметра = Неопределено И НЕ ПустаяСтрока(СтрокаШаблонаЧека.Элемент) Тогда
				ЗаголовокПараметра = СтрокаШаблонаЧека.Элемент;
			ИначеЕсли ЗаголовокПараметра = Неопределено Тогда
				ЗаголовокПараметра = СтрокаШаблонаЧека.ИмяКолонки;
			Иначе
				// Заголовок найден, действие не требуется
			КонецЕсли;
		ИначеЕсли СтрокаШаблонаЧека.Вычислять Тогда
			ЗаголовокПараметра = ЗаголовокСКДПоля(ДоступныеПоляВыбора, СтрокаШаблонаЧека.Элемент);
			Если ЗаголовокПараметра = Неопределено Тогда
				ЗаголовокПараметра = СтрокаШаблонаЧека.Элемент;
			КонецЕсли;
		Иначе
			ЗаголовокПараметра = СтрокаШаблонаЧека.Элемент;
		КонецЕсли;
		
		СтрокаШаблонаЧека.Заголовок = ЗаголовокПараметра;
	КонецЦикла;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция ЗаголовокСКДПоля(Знач ДоступныеПоляВыбора, Знач ИмяПоля)
	
	ЗаголовокПоля = Неопределено;
	
	Для каждого ПолеВыбора Из ДоступныеПоляВыбора Цикл
		ИмяПоляВыбора = СтрЗаменить(ПолеВыбора.Поле,".","");
		Если ИмяПоляВыбора = ИмяПоля Тогда
			ЗаголовокПоля = ПолеВыбора.Заголовок;
			Прервать;
		КонецЕсли;
		Если Найти(ИмяПоля, ИмяПоляВыбора) И ПолеВыбора.Элементы.Количество() Тогда
			ЗаголовокПоля = ЗаголовокСКДПоля(ПолеВыбора.Элементы, ИмяПоля);
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Возврат ЗаголовокПоля;
	
КонецФункции

&НаСервереБезКонтекста
Процедура ДополнитьКолонкиОбъектаКолонкамиШаблона(КолонкиОбъекта, Знач КолонкиШаблона)
	
	Для Каждого КолонкаШаблона Из КолонкиШаблона Цикл
			КолонкаДерева = КолонкиОбъекта.Найти(КолонкаШаблона.Имя);
			Если КолонкаДерева = Неопределено Тогда
				
				КолонкиОбъекта.Добавить(
					КолонкаШаблона.Имя,
					КолонкаШаблона.ТипЗначения);
				
			КонецЕсли;
		КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ВыводВТабличныйДокумент

&НаСервере
Процедура СформироватьМакетФискальногоЧека(Знач НовоеЗначениеМодифицированностиФормы = Истина)
	
	Если НЕ Модифицированность И НЕ ТолькоПросмотрФормы Тогда
		
		Модифицированность = НовоеЗначениеМодифицированностиФормы;
	КонецЕсли;
	
	ПолеТабличногоДокумента.Очистить();
	
	Если НЕ ЗначениеЗаполнено(ДокументСсылка) Тогда
		Возврат;
	КонецЕсли;
	
	ИмяКолонкиСоставЧека = "СоставЧека";
	
	Попытка
		
		СКД = ПолучитьИзВременногоХранилища(АдресСКД);
		ТаблицаДанных = ПечатьФискальныхЧековРМК.ТаблицаСКД(СКД, ДокументСсылка);
		// Список доступных таблиц нужен только для выбора таблицы в полях шаблона.
		СписокДоступныхТаблицСКД.Очистить();
		СписокТаблиц = ТаблицаДанных.Скопировать();
		СписокТаблиц.Свернуть("ИмяТаблицы");
		СписокТаблиц.Сортировать("ИмяТаблицы");
		
		Для Каждого СтрокаСписка Из СписокТаблиц Цикл
			
			Если СтрокаСписка.ИмяТаблицы = ИмяКолонкиСоставЧека
				ИЛИ СтрокаСписка.ИмяТаблицы = "Товары" Тогда
					Продолжить;
			КонецЕсли;
			СписокДоступныхТаблицСКД.Добавить(СтрокаСписка.ИмяТаблицы);
			
		КонецЦикла;
		
		ПредопределенныеДанныеЧека = Новый Структура;
		ОбщегоНазначенияРМКПереопределяемый.ЗаполнитьДанныеДляПробитияЧека(ДокументСсылка, ПредопределенныеДанныеЧека);
		
	Исключение
		
		ТекстОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		ИмяСобытия =
			НСтр("ru = 'Не удалось подготовить данные чека ккм для выведения в форме редактирования шаблона'",
				ОбщегоНазначения.КодОсновногоЯзыка());
		ЗаписьЖурналаРегистрации(ИмяСобытия,
				УровеньЖурналаРегистрации.Ошибка,,, ТекстОшибки);
		
		Возврат;
		
	КонецПопытки;
	
	ШаблонДерево = РеквизитФормыВЗначение("ШаблонЧека");
	
	ТекстовыйДокумент = Справочники.ХранилищеШаблоновРМК.ПолучитьМакет("ТабличныйШаблон");
	Область = ТекстовыйДокумент.ПолучитьОбласть("Строка|Колонка");
	ОбластьШтрихкод = ТекстовыйДокумент.ПолучитьОбласть("СтрокаШтрихкод|Колонка");
	ПолеТабличногоДокумента.Вывести(ТекстовыйДокумент.ПолучитьОбласть("ПерваяСтрока|Колонка"));
	ЦветСерый = ЦветаСтиля.ИтогиФонГруппы;
	
	КоличествоСтрокТабличнойЧасти = 0;
	Для Каждого СтрокаДерева Из ШаблонДерево.Строки Цикл
		
		Если СтрокаДерева.ИмяКолонки = "ФискальныйДокумент" Тогда
			
			ПредопределенныеДанныеЧека.Вставить("ФискальнаяСуммаЧека", 0);
			Для Каждого СтрокаЧека Из СтрокаДерева.Строки Цикл
				
				Если СтрокаЧека.ИмяКолонки = "ПараметрыШапкиЧека" Тогда
					// Шапка предопределенная, в ней ничего пользовательского не добавляется.
					// Сначала идет клише из драйвера ККТ.
					Область.Параметры.Текст = НСтр("ru = 'Предопределенный раздел.'");
					ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
					ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
					ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Центр;
					ВыведеннаяОбласть.ЦветФона = ЦветСерый;
					
					Область.Параметры.Текст = НСтр("ru = 'Заполняется ККТ.'");
					ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
					ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
					ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Центр;
					ВыведеннаяОбласть.ЦветФона = ЦветСерый;
					
					Область.Параметры.Текст = "";
					ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
					
					// Потом идут параметры шапки, передаваемые в ОФД.
					Для Каждого СтрокаПараметра Из СтрокаЧека.Строки Цикл
						
						Если СтрокаПараметра.НеПечатается Тогда
							Продолжить;
						КонецЕсли;
						
						Если ЗначениеЗаполнено(СтрокаПараметра.ИмяКолонки)
							И ПредопределенныеДанныеЧека.Свойство(СтрокаПараметра.ИмяКолонки) Тогда
							
								ЗначениеПараметра = ПредопределенныеДанныеЧека[СтрокаПараметра.ИмяКолонки];
							
							Если СтрокаПараметра.ИмяКолонки = ""
								ИЛИ ЗначениеЗаполнено(ЗначениеПараметра) Тогда
									ЗаполнитьИВывестиОбласть(Область, ЗначениеПараметра, СтрокаПараметра);
									
							КонецЕсли;
								
						КонецЕсли;
						
					КонецЦикла;
					
				ИначеЕсли СтрокаЧека.ИмяКолонки = ИмяКолонкиСоставЧека Тогда
					
					Для Каждого СтрокаОсновногоРаздела Из СтрокаЧека.Строки Цикл
						ОбработатьСоставЧека(СтрокаОсновногоРаздела, Область, ОбластьШтрихкод, ПредопределенныеДанныеЧека,
						ТаблицаДанных);
					КонецЦикла;
					
				ИначеЕсли СтрокаЧека.ИмяКолонки = "ТаблицаОплат" Тогда
					// Оплата предопределенная, в ней ничего пользовательского так же не добавляется.
					// При этом в оплате все печатается, если заполнено.
					Наличные  = 0;
					Электронный1 = 0;
					Электронный2 = 0;
					Электронный3 = 0;
					Электронный4 = 0;
					МассивОплат = ПредопределенныеДанныеЧека.ТаблицаОплат;
					
					Для Каждого Оплата Из МассивОплат Цикл
						
						Если Оплата.ТипОплаты = Перечисления.ТипыОплатыККТ.Наличные Тогда
							Наличные = Оплата.Сумма;
						ИначеЕсли Оплата.ТипОплаты = Перечисления.ТипыОплатыККТ.Электронно Тогда
							Электронный1 = Оплата.Сумма;
						ИначеЕсли Оплата.ТипОплаты = Перечисления.ТипыОплатыККТ.Постоплата Тогда
							Электронный2 = Оплата.Сумма;
						ИначеЕсли Оплата.ТипОплаты = Перечисления.ТипыОплатыККТ.Предоплата Тогда
							Электронный3 = Оплата.Сумма;
						ИначеЕсли Оплата.ТипОплаты = Перечисления.ТипыОплатыККТ.ВстречноеПредоставление Тогда
							Электронный4 = Оплата.Сумма;
						КонецЕсли;
						
					КонецЦикла;
					
					// Сначала, как правило, выводится общий итог оплаты.
					ИтогоОплат = Наличные + Электронный1 + Электронный2 + Электронный3 + Электронный4;
					Итого = ПредопределенныеДанныеЧека.ФискальнаяСуммаЧека;
					Сдача = ИтогоОплат - Итого;
					Область.Параметры.Текст = СтрШаблон("%1 %2", НСтр("ru = 'ИТОГО:'"), Формат(Итого, "ЧДЦ=2; ЧН=0,00"));
					ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
					ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
					// Потом детализируется по типам оплаты.
					Если Наличные <> 0 Тогда
						
						Область.Параметры.Текст = СтрШаблон(" %1 %2", НСтр("ru = 'Наличными:'"),
							Формат(Наличные, "ЧДЦ=2; ЧН=0,00"));
						ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
						ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
						
					КонецЕсли;
					Если Электронный1 <> 0 Тогда
						
						Область.Параметры.Текст = СтрШаблон(" %1 %2", НСтр("ru = 'Электронно:'"),
							Формат(Электронный1, "ЧДЦ=2; ЧН=0,00"));
						ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
						ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
						
					КонецЕсли;
					
					Если Электронный2 <> 0 Тогда
						
						Область.Параметры.Текст = СтрШаблон(" %1 %2", НСтр("ru = 'Постоплата:'"),
							Формат(Электронный2, "ЧДЦ=2; ЧН=0,00"));
						ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
						ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
						
					КонецЕсли;
					
					Если Электронный3 <> 0 Тогда
						
						Область.Параметры.Текст = СтрШаблон(" %1 %2", НСтр("ru = 'Предоплата:'"),
							Формат(Электронный3, "ЧДЦ=2; ЧН=0,00"));
						ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
						ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
						
					КонецЕсли;
					Если Электронный4 <> 0 Тогда
						
						Область.Параметры.Текст = СтрШаблон(" %1 %2", НСтр("ru = 'Встречное предоставление:'"),
							Формат(Электронный4, "ЧДЦ=2; ЧН=0,00"));
						ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
						ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
						
					КонецЕсли;
					Если Сдача > 0 Тогда
						
						Область.Параметры.Текст = СтрШаблон("%1 %2", НСтр("ru = 'Сдача:'"),
							Формат(Сдача, "ЧДЦ=2; ЧН=0,00"));
						ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
						ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
						
					КонецЕсли;
					// Потом идет подвал из драйвера ККТ.
					Область.Параметры.Текст = "";
					ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
					
					Область.Параметры.Текст = НСтр("ru = 'Предопределенный раздел.'");
					ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
					ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
					ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Центр;
					ВыведеннаяОбласть.ЦветФона = ЦветСерый;
					
					Область.Параметры.Текст = НСтр("ru = 'Заполняется ККТ.'");
					ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
					ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
					ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Центр;
					ВыведеннаяОбласть.ЦветФона = ЦветСерый;
					
					Область.Параметры.Текст = НСтр("ru = 'Содержит фискальные подписи.'");
					ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
					ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Переносить;
					ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Центр;
					ВыведеннаяОбласть.ЦветФона = ЦветСерый;
					
				КонецЕсли;
				
			КонецЦикла;
			
		ИначеЕсли СтрокаДерева.ИмяКолонки = "НефискальныйДокумент" Тогда
			
			ЭтоПерваяСтрокаВНефискальномДокументе = Истина;
			
			Для Каждого СтрокаРазделНефискальногоДокумента Из СтрокаДерева.Строки Цикл
				
				Если СтрокаРазделНефискальногоДокумента.ИмяКолонки = ИмяКолонкиСоставЧека
					ИЛИ (СтрокаРазделНефискальногоДокумента.ИмяКолонки = "Промокоды"
					И ИспользоватьПромокоды) Тогда
					
					Для Каждого СтрокаОсновногоРаздела Из СтрокаРазделНефискальногоДокумента.Строки Цикл
						ОбработатьСоставЧека(СтрокаОсновногоРаздела, Область, ОбластьШтрихкод,
							ПредопределенныеДанныеЧека, ТаблицаДанных);
					КонецЦикла;
					
				КонецЕсли;
				
			КонецЦикла;
			
			ЭтоПерваяСтрокаВНефискальномДокументе = Ложь;
			
		ИначеЕсли СтрокаДерева.ИмяКолонки = "Отрезка" Тогда
			
			ТекстОтрезки = "";
			
			Если ШиринаЧека > 14 Тогда
				
				ПоловинаДлины = Цел((ШиринаЧека - 12) / 2);
				
				Для Счетчик = 1 По ПоловинаДлины Цикл
					ТекстОтрезки = СтрШаблон("%1_", ТекстОтрезки);
				КонецЦикла;
				
				ТекстОтрезки = СтрШаблон("%1%2", ТекстОтрезки, НСтр("ru = 'Линия отреза'"));
				
				Для Счетчик = 1 По ПоловинаДлины Цикл
					ТекстОтрезки = СтрШаблон("%1_", ТекстОтрезки);
				КонецЦикла;
				
			Иначе
				
				Для Счетчик = 1 По ШиринаЧека Цикл
					ТекстОтрезки = СтрШаблон("%1_", ТекстОтрезки);
				КонецЦикла;
				
			КонецЕсли;
			
			ЗаполнитьИВывестиОбласть(Область, ТекстОтрезки, СтрокаДерева);
			
		КонецЕсли;
	КонецЦикла;
	
	ПолеТабличногоДокумента.Вывести(ТекстовыйДокумент.ПолучитьОбласть("ПоследняяСтрока|Колонка"));
	
	// Подгоняем размер первой колонки под ширину символов.
	ОбластьПервойКолонки = ПолеТабличногоДокумента.Область("C2");
	// Ширина колонки в средних символах шрифта получается на несколько символов больше,
	// чем столько же символов шрифтом по умолчанию или каким-либо другим.
	ОбластьПервойКолонки.ШиринаКолонки = Цел(ШиринаЧека * 0.95);
	
КонецПроцедуры

&НаСервере
Процедура ОбработатьСоставЧека(СтрокаОсновногоРаздела, Область, ОбластьШтрихкод,
	
	ПредопределенныеДанныеЧека, ТаблицаДанных)
	
	СтрокаШапкиЧека = Неопределено;
	СтруктураОтбора = Новый Структура;
	СтруктураОтбора.Вставить("ИмяТаблицы", "СоставЧека");
	СтрокиВыборки = ТаблицаДанных.НайтиСтроки(СтруктураОтбора);
	
	Для Каждого СтрокаВыборки Из СтрокиВыборки Цикл
		
		СтрокаШапкиЧека = СтрокаВыборки;
		Прервать;
		
	КонецЦикла;
	
	Если СтрокаОсновногоРаздела.ИмяКолонки = "Текст" Тогда
		
		Если СтрокаОсновногоРаздела.ВыводитьКакШтрихкод Тогда
			ОбработатьДанныеШтрихкода(ОбластьШтрихкод, Область, СтрокаОсновногоРаздела, СтрокаШапкиЧека, ТаблицаДанных);
		Иначе
			ОбработатьТекстовуюСтроку(Область, СтрокаОсновногоРаздела, СтрокаШапкиЧека, ТаблицаДанных);
		КонецЕсли;
		
	ИначеЕсли СтрокаОсновногоРаздела.ИмяКолонки = "СоставнаяСтрока" Тогда
		ОбработатьСоставнуюСтроку(Область, СтрокаОсновногоРаздела, СтрокаШапкиЧека, ТаблицаДанных);
	ИначеЕсли СтрокаОсновногоРаздела.ИмяКолонки = "Таблица" Тогда
		
		СтруктураОтбора = Новый Структура;
		ИмяТаблицы = СтрокаОсновногоРаздела.Элемент;
		СтруктураОтбора.Вставить("ИмяТаблицы", ИмяТаблицы);
		СтрокиВыборки = ТаблицаДанных.НайтиСтроки(СтруктураОтбора);
		
		Для Каждого СтрокаВыборки Из СтрокиВыборки Цикл
			
			Для Каждого ПоляСтрокиТаблицы Из СтрокаОсновногоРаздела.Строки Цикл
				
				Если ПоляСтрокиТаблицы.ИмяКолонки = "СоставнаяСтрока" Тогда
					
					ОбработатьСоставнуюСтроку(Область, ПоляСтрокиТаблицы, СтрокаШапкиЧека, ТаблицаДанных, СтрокаВыборки, ИмяТаблицы);
					
				ИначеЕсли ПоляСтрокиТаблицы.ИмяКолонки = "Текст" Тогда
					
					Если ПоляСтрокиТаблицы.ВыводитьКакШтрихкод Тогда
						
						ОбработатьДанныеШтрихкода(ОбластьШтрихкод, Область, ПоляСтрокиТаблицы, СтрокаШапкиЧека, ТаблицаДанных,
							СтрокаВыборки, ИмяТаблицы);
							
					Иначе
						ОбработатьТекстовуюСтроку(Область, ПоляСтрокиТаблицы, СтрокаШапкиЧека, ТаблицаДанных, СтрокаВыборки, ИмяТаблицы);
					КонецЕсли;
					
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЦикла;
		
	ИначеЕсли СтрокаОсновногоРаздела.ИмяКолонки = "ТаблицаТовары" Тогда
		
		ИндексПоследнейПозиции = ПредопределенныеДанныеЧека.ПозицииЧека.ВГраница();
		ИндексТекущейПозиции = 0;
		
		Пока ИндексТекущейПозиции <= ИндексПоследнейПозиции Цикл
			
			СтрокаВыборки = ПредопределенныеДанныеЧека.ПозицииЧека[ИндексТекущейПозиции];
			ФискальныйРазделБыл = Ложь;
			ВыводитьНефискальныеПосле = Истина;
			
		Если СтрокаВыборки.Свойство("НомерСтрокиТовара") Тогда
			
				Для Каждого ПоляСтрокиТаблицы Из СтрокаОсновногоРаздела.Строки Цикл
					
					Если ПоляСтрокиТаблицы.ИмяКолонки = "ФискальныйРаздел" Тогда
						
						ФискальныйРазделБыл = Истина;
						ПредопределенныеДанныеЧека.ФискальнаяСуммаЧека = ПредопределенныеДанныеЧека.ФискальнаяСуммаЧека 
							+ СтрокаВыборки.Сумма;
						ОбработатьСоставнуюСтроку(Область, ПоляСтрокиТаблицы, СтрокаШапкиЧека, ТаблицаДанных, СтрокаВыборки,
							ИмяТаблицы);
						
						Если ИндексТекущейПозиции < ИндексПоследнейПозиции Тогда
							
							СледующаяСтрока = ПредопределенныеДанныеЧека.ПозицииЧека[ИндексТекущейПозиции + 1];
							
							Если СледующаяСтрока.НомерСтрокиТовара = СтрокаВыборки.НомерСтрокиТовара Тогда
								ВыводитьНефискальныеПосле = Ложь;
							КонецЕсли;
							
						КонецЕсли;
						
					ИначеЕсли ПоляСтрокиТаблицы.ИмяКолонки = "СоставнаяСтрока" Тогда
						
						Если ФискальныйРазделБыл Тогда
							
							Если НЕ ВыводитьНефискальныеПосле Тогда
								Продолжить;
							КонецЕсли;
							
						Иначе
							
							Если ИндексТекущейПозиции > 0 Тогда
								
								ПредыдущаяСтрока = ПредопределенныеДанныеЧека.ПозицииЧека[ИндексТекущейПозиции - 1];
								
								Если ПредыдущаяСтрока.Свойство("НомерСтрокиТовара")
									И ПредыдущаяСтрока.НомерСтрокиТовара = СтрокаВыборки.НомерСтрокиТовара Тогда
										Продолжить;
								КонецЕсли;
								
							КонецЕсли;
							
						КонецЕсли;
						
						СтруктураПоиска = Новый Структура;
						СтруктураПоиска.Вставить("НомерСтрокиТовара", СтрокаВыборки.НомерСтрокиТовара);
						СтруктураПоиска.Вставить("ИмяТаблицы", "Товары");
						СтрокиДанных = ТаблицаДанных.НайтиСтроки(СтруктураПоиска);
						
						СтрокаДанных = ?(СтрокиДанных.Количество() > 0, СтрокиДанных[0], СтрокаВыборки);
						
						ОбработатьСоставнуюСтроку(Область, ПоляСтрокиТаблицы, СтрокаШапкиЧека, ТаблицаДанных, СтрокаДанных, ИмяТаблицы);
						
					ИначеЕсли ПоляСтрокиТаблицы.ИмяКолонки = "Текст" Тогда
						
						Если ФискальныйРазделБыл Тогда
							
							Если НЕ ВыводитьНефискальныеПосле Тогда
								Продолжить;
							КонецЕсли;
							
						Иначе
							
							Если ИндексТекущейПозиции > 0 Тогда
								
								ПредыдущаяСтрока = ПредопределенныеДанныеЧека.ПозицииЧека[ИндексТекущейПозиции - 1];
								
								Если ПредыдущаяСтрока.Свойство("НомерСтрокиТовара")
									И ПредыдущаяСтрока.НомерСтрокиТовара = СтрокаВыборки.НомерСтрокиТовара Тогда
										Продолжить;
								КонецЕсли;
								
							КонецЕсли;
							
						КонецЕсли;
						
						СтруктураПоиска = Новый Структура;
						СтруктураПоиска.Вставить("НомерСтрокиТовара", СтрокаВыборки.НомерСтрокиТовара);
						СтруктураПоиска.Вставить("ИмяТаблицы", "Товары");
						СтрокиДанных = ТаблицаДанных.НайтиСтроки(СтруктураПоиска);
						
						
						СтрокаДанных = ?(СтрокиДанных.Количество() > 0, СтрокиДанных[0], СтрокаВыборки);

						Если ПоляСтрокиТаблицы.ВыводитьКакШтрихкод Тогда
							ОбработатьДанныеШтрихкода(ОбластьШтрихкод, Область, ПоляСтрокиТаблицы, СтрокаШапкиЧека,
								ТаблицаДанных, СтрокаДанных, ИмяТаблицы);
						Иначе
							ОбработатьТекстовуюСтроку(Область, ПоляСтрокиТаблицы, СтрокаШапкиЧека, ТаблицаДанных,
								СтрокаДанных, ИмяТаблицы);
						КонецЕсли;
							
					КонецЕсли;
					
				КонецЦикла;
			
			КонецЕсли;
			
			ИндексТекущейПозиции = ИндексТекущейПозиции + 1;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ОбработатьТекстовуюСтроку(Область, ПоляСтроки, СтрокаШапкиЧека, ТаблицаДанных,
	СтрокаДанных = Неопределено, ИмяРаздела = "СоставЧека")
	
	Если Не ПоляСтроки.Идентификатор = "" Тогда
		ПредставлениеШаблонаДляУсловий = Новый Структура("Условия", ТаблицаУсловий.Выгрузить());
		Если Не ПечатьФискальныхЧековРМК.УсловияВыполнены(ПредставлениеШаблонаДляУсловий,
			ПоляСтроки.Идентификатор, ИмяРаздела, СтрокаШапкиЧека, СтрокаДанных) Тогда
			
			// Данный блок чека не выводится, т.к. не выполнены условия проверки.
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ЗначениеСтроки = ПечатьФискальныхЧековРМК.ЗначениеТекстовогоПоля(ДокументСсылка, ПоляСтроки, СтрокаШапкиЧека,
		ТаблицаДанных, СтрокаДанных, ИмяРаздела);
		
	Если ЗначениеЗаполнено(ЗначениеСтроки) ИЛИ ПоляСтроки.ВыводитьПустоеЗначение Тогда
		ЗаполнитьИВывестиОбласть(Область, ЗначениеСтроки, ПоляСтроки);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ОбработатьДанныеШтрихкода(ОбластьШтрихкода, ОбластьДополнительныхПолей, ПоляСтроки, СтрокаШапкиЧека,
	ТаблицаДанных, СтрокаДанных = Неопределено, ИмяРаздела = "СоставЧека")
	
	Если Не ПоляСтроки.Идентификатор = "" Тогда
		ПредставлениеШаблонаДляУсловий = Новый Структура("Условия", ТаблицаУсловий.Выгрузить());
		Если Не ПечатьФискальныхЧековРМК.УсловияВыполнены(ПредставлениеШаблонаДляУсловий,
			ПоляСтроки.Идентификатор, ИмяРаздела, СтрокаШапкиЧека, СтрокаДанных) Тогда
			
			// Данный блок чека не выводится, т.к. не выполнены условия проверки.
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	ЗначениеСтроки = ПечатьФискальныхЧековРМК.ЗначениеТекстовогоПоля(ДокументСсылка, ПоляСтроки, СтрокаШапкиЧека,
		ТаблицаДанных, СтрокаДанных, ИмяРаздела);
		
	Если ЗначениеЗаполнено(ЗначениеСтроки)
		ИЛИ ПоляСтроки.ВыводитьПустоеЗначение Тогда
			ЗаполнитьИВывестиОбластьШтрихкода(ОбластьШтрихкода, ОбластьДополнительныхПолей, ЗначениеСтроки, ПоляСтроки);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ОбработатьСоставнуюСтроку(Область, ПоляСтроки, СтрокаШапкиЧека, ТаблицаДанных, СтрокаДанных = Неопределено,
	ИмяРаздела = "СоставЧека")
	
	ТекстСтроки = "";
	
	Если Не ПоляСтроки.Идентификатор = "" Тогда
		ПредставлениеШаблонаДляУсловий = Новый Структура("Условия", ТаблицаУсловий.Выгрузить());
		Если Не ПечатьФискальныхЧековРМК.УсловияВыполнены(ПредставлениеШаблонаДляУсловий,
			ПоляСтроки.Идентификатор, ИмяРаздела, СтрокаШапкиЧека, СтрокаДанных) Тогда
			
			// Данный блок чека не выводится, т.к. не выполнены условия проверки.
			Возврат;
		КонецЕсли;
	КонецЕсли;
	
	Для Каждого ПараметрСтроки Из ПоляСтроки.Строки Цикл
		
		Если ЗначениеЗаполнено(ПараметрСтроки.ИмяКолонки) Тогда
			
			ЗначениеПоля = ПечатьФискальныхЧековРМК.ЗначениеТекстовогоПоля(ДокументСсылка, ПараметрСтроки,
				СтрокаШапкиЧека, ТаблицаДанных, СтрокаДанных, ИмяРаздела);
			
			Если ЗначениеЗаполнено(ЗначениеПоля)
				ИЛИ ПоляСтроки.ВыводитьПустоеЗначение Тогда
			
				ДлинаПостфикса = СтрДлина(ПараметрСтроки.Постфикс);
				ПозицияСимволаПереноса = СтрНайти(ПараметрСтроки.Постфикс, Символ(182));
				ЕстьПереносСтроки = ПозицияСимволаПереноса > 0;
				
				ТекстСтроки = СтрШаблон("%1%2%3%4", ТекстСтроки, ПараметрСтроки.Префикс,
					Формат(ЗначениеПоля, ПараметрСтроки.Формат),
					?(ЕстьПереносСтроки, Лев(ПараметрСтроки.Постфикс, ПозицияСимволаПереноса - 1),
					ПараметрСтроки.Постфикс));
					
				Если ЕстьПереносСтроки Тогда
					
					ЗаполнитьИВывестиОбласть(Область, ТекстСтроки, ПоляСтроки);
					ТекстСтроки = Прав(ПараметрСтроки.Постфикс, ДлинаПостфикса - ПозицияСимволаПереноса);
					
				КонецЕсли;
				
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Если ЗначениеЗаполнено(ТекстСтроки) Тогда
		ЗаполнитьИВывестиОбласть(Область, ТекстСтроки, ПоляСтроки);
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ЗаполнитьИВывестиОбласть(Область, ЗначениеПараметра, ДанныеШаблона)
	
	Если ЭтоПерваяСтрокаВНефискальномДокументе Тогда
		
		ЭтоПерваяСтрокаВНефискальномДокументе = Ложь;
		// В нефискальном разделе может не оказаться строк для вывода.
		// Поэтому отрезка выводится только при выводе первой строки
		ВыведеннаяОбласть = ВывестиОтрезку(Область);
		
	КонецЕсли;
	
	Область.Параметры.Текст = СтрШаблон("%1%2%3", ДанныеШаблона.Префикс, 
		Формат(ЗначениеПараметра, ДанныеШаблона.Формат), ДанныеШаблона.Постфикс);
	
	ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
	
	Если ДанныеШаблона.Выравнивание = "Право" Тогда
		ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Право;
	ИначеЕсли ДанныеШаблона.Выравнивание = "Центр" Тогда
		ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Центр;
	Иначе
		ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Авто;
	КонецЕсли;
	
	ВыведеннаяОбласть.РазмещениеТекста = ?(ДанныеШаблона.РазмещениеТекста = 1,
		ТипРазмещенияТекстаТабличногоДокумента.Обрезать,
		ТипРазмещенияТекстаТабличногоДокумента.Переносить);
	
	Возврат ВыведеннаяОбласть;
	
КонецФункции

&НаСервере
Функция ЗаполнитьИВывестиОбластьШтрихкода(ОбластьШтрихкода, ОбластьДополнительныхПолей, ЗначениеПараметра, ДанныеШаблона)
	
	Рисунок = Неопределено;
	Для Каждого РисунокОбласти Из ОбластьШтрихкода.Рисунки Цикл
		Рисунок = РисунокОбласти;
		Прервать;
	КонецЦикла;
	
	Если ЭтоПерваяСтрокаВНефискальномДокументе Тогда
		ЭтоПерваяСтрокаВНефискальномДокументе = Ложь;
		// В нефискальном разделе может не оказаться строк для вывода.
		// Поэтому отрезка выводится только при выводе первой строки
		ВыведеннаяОбластьДополнительная = ВывестиОтрезку(ОбластьДополнительныхПолей);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДанныеШаблона.Префикс) Тогда
		ОбластьДополнительныхПолей.Параметры.Текст = ДанныеШаблона.Префикс;
		ВыведеннаяОбластьДополнительная = ПолеТабличногоДокумента.Вывести(ОбластьДополнительныхПолей);
	КонецЕсли;
	
	Если Рисунок = Неопределено Тогда
		ВыведеннаяОбласть = Неопределено;
	Иначе
		
		ПараметрыШтрихкода = ГенерацияШтрихкода.ПараметрыГенерацииШтрихкода();
		ПараметрыШтрихкода.Ширина = Окр(Рисунок.Ширина / КоличествоМиллиметровВПикселе);
		ПараметрыШтрихкода.Высота = Окр(Рисунок.Высота / КоличествоМиллиметровВПикселе);
		ПараметрыШтрихкода.УровеньКоррекцииQR = 0;
		ПараметрыШтрихкода.Штрихкод = ЗначениеПараметра;
		ПараметрыШтрихкода.ТипКода = ДанныеШаблона.ТипШтрихкода;
		ПараметрыШтрихкода.ОтображатьТекст = Истина;
		ПараметрыШтрихкода.РазмерШрифта = 7;
		ПараметрыШтрихкода.УголПоворота = 0;

		РезультатВыполнения = ГенерацияШтрихкода.ИзображениеШтрихкода(ПараметрыШтрихкода);
		
		Если РезультатВыполнения.Результат Тогда
			
			Рисунок.Картинка = РезультатВыполнения.Картинка;
			ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(ОбластьШтрихкода);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ДанныеШаблона.Постфикс) Тогда
		ОбластьДополнительныхПолей.Параметры.Текст = ДанныеШаблона.Постфикс;
		ВыведеннаяОбластьДополнительная = ПолеТабличногоДокумента.Вывести(ОбластьДополнительныхПолей);
	КонецЕсли;
	
	Возврат ВыведеннаяОбласть;
	
КонецФункции

&НаСервере
Функция ВывестиОтрезку(Область)
	
	ТекстОтрезки = "";
	
	Если ШиринаЧека > 14 Тогда
		
		ПоловинаДлины = Цел((ШиринаЧека - 12) / 2);
		Для Счетчик = 1 По ПоловинаДлины Цикл
			ТекстОтрезки = ТекстОтрезки + "_";
		КонецЦикла;
		
		ТекстОтрезки = СтрШаблон("%1%2", ТекстОтрезки, НСтр("ru = 'Линия отреза'"));
		
		Для Счетчик = 1 По ПоловинаДлины Цикл
			ТекстОтрезки = СтрШаблон("%1_", ТекстОтрезки);
		КонецЦикла;
		
	Иначе
		
		Для Счетчик = 1 По ШиринаЧека Цикл
			ТекстОтрезки = СтрШаблон("%1_", ТекстОтрезки);
		КонецЦикла;
		
	КонецЕсли;
	
	Область.Параметры.Текст = ТекстОтрезки;
	ВыведеннаяОбласть = ПолеТабличногоДокумента.Вывести(Область);
	ВыведеннаяОбласть.РазмещениеТекста = ТипРазмещенияТекстаТабличногоДокумента.Обрезать;
	ВыведеннаяОбласть.ГоризонтальноеПоложение = ГоризонтальноеПоложение.Центр;
	ВыведеннаяОбласть.ЦветФона = ЦветаСтиля.ИтогиФонГруппы;
	
	Возврат ВыведеннаяОбласть;
	
КонецФункции

#КонецОбласти

#Область ПрочиеСлужебные

&НаСервереБезКонтекста
Процедура ИнициализироватьКомпоновщикНастроек(КомпоновщикНастроек, ЭлементФормыДоступныеПоляВыбора,
	АдресСхемыКомпоновкиДанных, АдресНастроекКомпоновкиДанных = Неопределено)
	
	Попытка
		
		СхемаКомпоновкиДанных = ПолучитьИзВременногоХранилища(АдресСхемыКомпоновкиДанных);
		
		Если ЗначениеЗаполнено(АдресНастроекКомпоновкиДанных) Тогда
			НастройкиКомпоновкиДанных = ПолучитьИзВременногоХранилища(АдресНастроекКомпоновкиДанных);
		КонецЕсли;
		
		КомпоновщикНастроек.Инициализировать(Новый ИсточникДоступныхНастроекКомпоновкиДанных(АдресСхемыКомпоновкиДанных));
		
		Если ЗначениеЗаполнено(АдресНастроекКомпоновкиДанных) Тогда
			КомпоновщикНастроек.ЗагрузитьНастройки(НастройкиКомпоновкиДанных);
		Иначе
			КомпоновщикНастроек.ЗагрузитьНастройки(СхемаКомпоновкиДанных.НастройкиПоУмолчанию);
		КонецЕсли;
		
		КомпоновщикНастроек.Восстановить();
		
		ОграничениеСистемныхПолей = ЭлементФормыДоступныеПоляВыбора.ОграниченияИспользования.Добавить();
		ОграничениеСистемныхПолей.Поле = Новый ПолеКомпоновкиДанных("СистемныеПоля");
		ОграничениеСистемныхПолей.Доступность = Ложь;
		
		ОграничениеПоляИмяТаблицы = ЭлементФормыДоступныеПоляВыбора.ОграниченияИспользования.Добавить();
		ОграничениеПоляИмяТаблицы.Поле = Новый ПолеКомпоновкиДанных("ИмяТаблицы");
		ОграничениеПоляИмяТаблицы.Доступность = Ложь;
		
	Исключение
		
		КодЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
		ТекстОшибки = СтрШаблон("%1%2%3",
			НСтр("ru = 'Не удалось инициализировать компоновщик настроек по причине:'", КодЯзыка),
			Символы.ПС, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		ЗаписьЖурналаРегистрации(ТекстОшибки,
			УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));

		
	КонецПопытки;
	
КонецПроцедуры

&НаСервере
Функция ПоместитьВоВременноеХранилищеШаблон()
	
	ШаблонСтруктура = ПараметрыДляСохранения();
	
	Если ВозвращатьСКД Тогда
		ШаблонСтруктура.Вставить("АдресСКДВХранилище", АдресСКД);
	КонецЕсли;
	
	Возврат ПоместитьВоВременноеХранилище(ШаблонСтруктура, Новый УникальныйИдентификатор);
	
КонецФункции

&НаКлиенте
Процедура ОткрытиеФормыРедактированияПараметров(ВыбраннаяСтрока)
	
	ТекущаяСтрока = ШаблонЧека.НайтиПоИдентификатору(ВыбраннаяСтрока);
	
	Если ТекущаяСтрока <> Неопределено Тогда
		
		СтруктураПараметров = Новый Структура();
		СтруктураПараметров.Вставить("Идентификатор", ВыбраннаяСтрока);
		СтруктураПараметров.Вставить("ИмяКолонки", ТекущаяСтрока.ИмяКолонки);
		СтруктураПараметров.Вставить("Формат", ТекущаяСтрока.Формат);
		СтруктураПараметров.Вставить("ШиринаСтроки", ТекущаяСтрока.Ширина);
		СтруктураПараметров.Вставить("РазмещениеТекста", ТекущаяСтрока.РазмещениеТекста);
		СтруктураПараметров.Вставить("Выравнивание", ТекущаяСтрока.Выравнивание);
		СтруктураПараметров.Вставить("Наименование", ТекущаяСтрока.Заголовок);
		СтруктураПараметров.Вставить("ЭлементШаблона", ТекущаяСтрока.Элемент);
		СтруктураПараметров.Вставить("Вычислять", ТекущаяСтрока.Вычислять);
		СтруктураПараметров.Вставить("ВыводитьКакШтрихкод", ТекущаяСтрока.ВыводитьКакШтрихкод);
		СтруктураПараметров.Вставить("ТипШтрихкода", ТекущаяСтрока.ТипШтрихкода);
		СтруктураПараметров.Вставить("ВыводитьПустоеЗначение", ТекущаяСтрока.ВыводитьПустоеЗначение);
		СтруктураПараметров.Вставить("Префикс", ТекущаяСтрока.Префикс);
		СтруктураПараметров.Вставить("Постфикс", ТекущаяСтрока.Постфикс);
		СтруктураПараметров.Вставить("ТипЭлемента", ТекущаяСтрока.ТипЭлемента);
		СтруктураПараметров.Вставить("ОписаниеТипа", ТекущаяСтрока.ОписаниеТипа);
		СтруктураПараметров.Вставить("ПустоеЗначение", ТекущаяСтрока.ПустоеЗначение);
		СтруктураПараметров.Вставить("АдресСКД", АдресСКД);
		
		СтруктураПараметров.Вставить("ВидимостьУсловий", Истина);
		ИдентификаторУсловий = ТекущаяСтрока.Идентификатор;
		Если Не ЗначениеЗаполнено(ТекущаяСтрока.Идентификатор) Тогда
			ИдентификаторУсловий = ВыбраннаяСтрока;
		КонецЕсли;
		СуществующиеУсловия = ПолучитьПредставлениеТаблицыУсловийПоИдентификатору(ИдентификаторУсловий);
		СтруктураПараметров.Вставить("ТаблицаУсловий", СуществующиеУсловия);
		
		ДоступенВыводПромокода = ИспользоватьПромокоды И ЭтоСтрокаВыводаПромокода(ТекущаяСтрока);
		СтруктураПараметров.Вставить("ДоступенВыводПромокода", ДоступенВыводПромокода);
		
		ДополнительныеПараметры = Новый Структура();
		
		Обработчик = Новый ОписаниеОповещения("ОповещениеРедактированиеШаблонаЧека", ЭтотОбъект, ДополнительныеПараметры);
		Режим = РежимОткрытияОкнаФормы.БлокироватьОкноВладельца;
		ОткрытьФорму("Справочник.ХранилищеШаблоновРМК.Форма.ФормаРедактированияПараметров", СтруктураПараметров, ЭтаФорма,,,,
			Обработчик, Режим);
		
	КонецЕсли;
		
КонецПроцедуры

&НаСервере
Функция ПолучитьПредставлениеТаблицыУсловийПоИдентификатору(Знач Идентификатор)
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Идентификатор", Число(Идентификатор));
	СуществующиеУсловия = ТаблицаУсловий.НайтиСтроки(ПараметрыОтбора);
	
	УсловияЭтогоПоля = ТаблицаУсловий.Выгрузить(СуществующиеУсловия);
	Результат = Новый Массив;
	Для Каждого СтрокаУсловия Из УсловияЭтогоПоля Цикл
		ОписаниеСтроки = Новый Структура();
		Для Каждого КолонкаУсловия Из УсловияЭтогоПоля.Колонки Цикл
			ОписаниеСтроки.Вставить(КолонкаУсловия.Имя, СтрокаУсловия[КолонкаУсловия.Имя]);
		КонецЦикла;
		Результат.Добавить(ОписаниеСтроки);
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ЭтоСтрокаВыводаПромокода(СтрокаДерева)
	
	Результат = Ложь;
	РодительСтроки = СтрокаДерева.ПолучитьРодителя();
	Если СтрокаДерева.Элемент = "ПромокодыКВыдаче" Тогда
		Результат = Истина;
	ИначеЕсли Не РодительСтроки = Неопределено Тогда
		Результат = ЭтоСтрокаВыводаПромокода(РодительСтроки);
	КонецЕсли;
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ТекущееПолеЗапрещеноРедактировать()
	
	Результат = Ложь;
	ТекущаяСтрокаДерева = Элементы.ШаблонЧека.ТекущиеДанные;
	
	Если ТекущаяСтрокаДерева <> Неопределено Тогда
		
		Если ТекущаяСтрокаДерева.ИмяКолонки = "НефискальныйДокумент"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ФискальныйДокумент"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ПараметрыШапкиЧека"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "СоставЧека"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ТаблицаОплат"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ТаблицаТовары"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "Промокоды"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "Таблица"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "Отрезка"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ФискальныйРаздел"
			ИЛИ ТекущаяСтрокаДерева.ИндексКартинки = 6 Тогда
			
				Результат = Истина;
				
		КонецЕсли;
			
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиенте
Функция ТекущееПолеЗапрещеноУдалять()
	
	Результат = Ложь;
	ТекущаяСтрокаДерева = Элементы.ШаблонЧека.ТекущиеДанные;
	
	Если ТекущаяСтрокаДерева <> Неопределено Тогда
		
		Если ТекущаяСтрокаДерева.ИмяКолонки = "ФискальныйДокумент"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ПараметрыШапкиЧека"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "СоставЧека"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ТаблицаОплат"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ТаблицаТовары"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ФискальныйРаздел"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "ТаблицаПромокоды"
			ИЛИ ТекущаяСтрокаДерева.ИмяКолонки = "Промокод"
			ИЛИ ТекущаяСтрокаДерева.ИндексКартинки = 6 Тогда
				Результат = Истина;
		КонецЕсли;
			
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

&НаКлиентеНаСервереБезКонтекста
Функция ЕстьСвойство(Объект, ИмяРеквизита)
	
	Возврат ОбщегоНазначенияРМККлиентСервер.ЕстьСвойство(Объект, ИмяРеквизита);
	
КонецФункции

&НаСервере
Функция ПараметрыДляСохранения()
	
	ШаблонДерево = РеквизитФормыВЗначение("ШаблонЧека");
	ШаблонСтруктура = Новый Структура;
	ШаблонСтруктура.Вставить("Шаблон", ШаблонДерево);
	ШаблонСтруктура.Вставить("ДокументСсылка", ДокументСсылка);
	ШаблонСтруктура.Вставить("ШиринаЧека", ШиринаЧека);
	ШаблонСтруктура.Вставить("ТаблицаУсловий", ТаблицаУсловий.Выгрузить());
	
	Возврат ШаблонСтруктура;
	
КонецФункции

&НаСервере
Процедура СохранитьХранилищеШаблоновРМКНаСервере(Отказ)
	
	Если НЕ ПроверитьЗаполнение() Тогда
		Отказ = Истина;
		ПараметрЗакрытия = Ложь;
	КонецЕсли;
	
	Если Отказ Тогда
		Возврат;
	КонецЕсли;
	
	ШаблонСтруктура = ПараметрыДляСохранения();
	СКД = ПолучитьИзВременногоХранилища(АдресСКД);
	
	ХранилищеШаблоновРМКОбъект = РеквизитФормыВЗначение("ХранилищеШаблоновРМК");
	ХранилищеШаблоновРМКОбъект.Наименование          = Наименование;
	ХранилищеШаблоновРМКОбъект.Шаблон                = Новый ХранилищеЗначения(ШаблонСтруктура);
	ХранилищеШаблоновРМКОбъект.СхемаКомпоновкиДанных = Новый ХранилищеЗначения(СКД);
	ХранилищеШаблоновРМКОбъект.Условия.Загрузить(ШаблонСтруктура.ТаблицаУсловий);
	
	Если НЕ ОбщегоНазначенияРМК.ОбязательныеПараметрыСКДЗаполнены(ХранилищеШаблоновРМКОбъект) Тогда
		Отказ = Истина;
		ПараметрЗакрытия = Ложь;
		Возврат;
	КонецЕсли;
	
	Попытка
		ХранилищеШаблоновРМКОбъект.Записать();
		ЗначениеВРеквизитФормы(ХранилищеШаблоновРМКОбъект, "ХранилищеШаблоновРМК");
		Модифицированность = Ложь;
		Заголовок = СформироватьЗаголовок(Наименование);
	Исключение
		Отказ = Истина;
		ПараметрЗакрытия = Ложь;
		ТекстОшибки = НСтр("ru = 'Ошибка при записи шаблона чека ККТ. %1Подробнее: %2'",
			ОбщегоНазначения.КодОсновногоЯзыка());
		ТекстОшибки = СтрШаблон(ТекстОшибки,
			Символы.ПС,ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		
		ВызватьИсключение ТекстОшибки;
	КонецПопытки;
	
КонецПроцедуры

&НаСервереБезКонтекста
Функция СформироватьЗаголовок(НаименованиеШаблона, СозданиеШаблона = Ложь)
	
	ТекстЗаголовка =  НСтр("ru = 'Шаблон чека ККТ'", ОбщегоНазначения.КодОсновногоЯзыка());
	
	Если СозданиеШаблона Тогда
		ШаблонЗаголовка = НСтр("ru = '%1 (создание)'", ОбщегоНазначения.КодОсновногоЯзыка());
		ТекстЗаголовка = СтрШаблон(ШаблонЗаголовка, ТекстЗаголовка);
	Иначе
		ТекстЗаголовка = НаименованиеШаблона;
	КонецЕсли;
	
	Возврат ТекстЗаголовка;
	
КонецФункции

&НаСервере
Процедура НастроитьФормуТолькоДляПросмотра()
	
	Элементы.ФормаСохранитьЗакрытьФорму.Видимость = Ложь;
	Элементы.ФормаСохранитьФорму.Видимость = Ложь;
	Элементы.ФормаЗагрузитьШаблонПоУмолчанию.Видимость = Ложь;
	Элементы.ФормаЗагрузитьШаблонРасширенный.Видимость = Ложь;
	Элементы.ФормаИмпортироватьШаблонИзФайла.Видимость = Ложь;
	Элементы.ФормаРедактироватьШаблон.Видимость = Ложь;
	Элементы.Наименование.ТолькоПросмотр = Истина;
	Элементы.ШиринаЧека.ТолькоПросмотр = Истина;
	
КонецПроцедуры

&НаСервере
Процедура РедактироватьШаблонНаСервере()
	
	ВидимостьНастроекШаблона = НЕ ОбщегоНазначенияКлиентСервер.ЗначениеСвойстваЭлементаФормы(
		Элементы,
		"ГруппаЛево",
		"Видимость");
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
		Элементы, 
		"ФормаРедактироватьШаблон",
		"Пометка",
		ВидимостьНастроекШаблона);
		
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
			Элементы,
			"ГруппаЛево",
			"Видимость",
			ВидимостьНастроекШаблона);
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

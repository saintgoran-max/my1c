
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	РазрешенПодбор = ПравоДоступа("ИнтерактивноеДобавление", Метаданные.Справочники.КТРУ);
	Если РазрешенПодбор Тогда
		РазрешенПодбор = Не КлассификаторКТРУ.ЗагружатьКлассификаторПолностью();
	КонецЕсли;
	Элементы.ПодобратьКлассификатор.Видимость = РазрешенПодбор;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ПодобратьКлассификатор(Команда)
	
	ОткрытьФорму("Справочник.КТРУ.Форма.ДобавлениеЭлементовВКлассификатор",, ЭтотОбъект);
	
КонецПроцедуры

#КонецОбласти
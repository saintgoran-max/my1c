
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Шаблоны = Параметры.Шаблоны;
	Если Не ПустаяСтрока(Параметры.ЗаголовокКнопки) Тогда
		Элементы.ФормаВыбрать.Заголовок = Параметры.ЗаголовокКнопки;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура ШаблоныяВыборЗначения(Элемент, Значение, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	СтрокаШаблоны = Шаблоны.НайтиПоИдентификатору(Значение);
	
	Если СтрокаШаблоны = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	//ОповеститьОВыборе(СтрокаКИ.Значение);
	Закрыть(СтрокаШаблоны.Значение);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура Выбрать(Команда)
	
	Перем ВыбранноеЗначение;
	
	Если Элементы.Шаблоны.ТекущиеДанные <> Неопределено Тогда
		ВыбранноеЗначение = Элементы.Шаблоны.ТекущиеДанные.Значение;
	КонецЕсли;
	
	Закрыть(ВыбранноеЗначение);
	
КонецПроцедуры

#КонецОбласти

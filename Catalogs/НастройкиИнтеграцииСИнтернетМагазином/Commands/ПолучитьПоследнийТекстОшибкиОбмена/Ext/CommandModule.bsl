
#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ТекстДанных = ИнтеграцияСИнтернетМагазиномВызовСервера.ПрочитатьТекстОшибкиНаСервере(ПараметрКоманды);
	
	Текст = Новый ТекстовыйДокумент;
	Текст.УстановитьТекст(ТекстДанных);
	Текст.Показать(НСтр("ru = 'Текст ошибки'"));
	
КонецПроцедуры

#КонецОбласти
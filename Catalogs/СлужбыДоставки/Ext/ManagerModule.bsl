#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

#Область ОбновлениеВерсииИБ

// Определяет настройки начального заполнения элементов.
//
// Параметры:
//  Настройки - Структура - настройки заполнения
//   * ПриНачальномЗаполненииЭлемента - Булево - Если Истина, то для каждого элемента будет
//      вызвана процедура индивидуального заполнения ПриНачальномЗаполненииЭлемента.
Процедура ПриНастройкеНачальногоЗаполненияЭлементов(Настройки) Экспорт

	Настройки.ПриНачальномЗаполненииЭлемента = Ложь;

КонецПроцедуры

// Смотри также ОбновлениеИнформационнойБазыПереопределяемый.ПриНачальномЗаполненииЭлементов
// 
// Параметры:
//   КодыЯзыков - см. ОбновлениеИнформационнойБазыПереопределяемый.ПриНачальномЗаполненииЭлементов.КодыЯзыков
//   Элементы - см. ОбновлениеИнформационнойБазыПереопределяемый.ПриНачальномЗаполненииЭлементов.Элементы
//   ТабличныеЧасти - см. ОбновлениеИнформационнойБазыПереопределяемый.ПриНачальномЗаполненииЭлементов.ТабличныеЧасти
//
Процедура ПриНачальномЗаполненииЭлементов(КодыЯзыков, Элементы, ТабличныеЧасти) Экспорт
	
	Элемент = Элементы.Добавить();
	Элемент.ИмяПредопределенныхДанных = "ДоставкаСобственнымиСилами";
	Элемент.Наименование = НСтр("ru='Доставка собственными силами'");
	Элемент.ВариантУчета = Перечисления.ВариантыУчетаДоставки.ДоставкаСОплатой;
	Элемент.ФормулаСтоимости = "0";
	Элемент.ФормулаСебестоимости = "0";
	
	ПараметрыРасчетов = ТабличныеЧасти.ПараметрыРасчетов.Скопировать();
	Элемент.ПараметрыРасчетов = ПараметрыРасчетов;
	
	ЗначенияФормулы = ТабличныеЧасти.ЗначенияФормулы.Скопировать();
	Элемент.ЗначенияФормулы = ЗначенияФормулы;
	
	ДоступныеСпособыОтгрузки = ТабличныеЧасти.ДоступныеСпособыОтгрузки.Скопировать();
	Элемент.ДоступныеСпособыОтгрузки = ДоступныеСпособыОтгрузки;
	
	ДоступныеСпособыДоставки = ТабличныеЧасти.ДоступныеСпособыДоставки.Скопировать();
	ЭлементТЧ = ДоступныеСпособыДоставки.Добавить();
	ЭлементТЧ.СпособДоставки = Перечисления.СпособыДоставки.Курьер;
	ЭлементТЧ = ДоступныеСпособыДоставки.Добавить();
	ЭлементТЧ.СпособДоставки = Перечисления.СпособыДоставки.Почта;
	Элемент.ДоступныеСпособыДоставки = ДоступныеСпособыДоставки;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#КонецОбласти

#КонецЕсли

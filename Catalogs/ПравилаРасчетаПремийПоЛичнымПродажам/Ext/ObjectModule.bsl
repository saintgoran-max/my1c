#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если НЕ ЭтоГруппа 
		И НЕ СпособРасчетаПремий = Перечисления.СпособыРасчетаПремийПоЛичнымПродажам.Сумма Тогда
		
		РеквизитВалюта = ПроверяемыеРеквизиты.Найти("ВалютаРасчета");
		Если РеквизитВалюта <> Неопределено Тогда
			
			ПроверяемыеРеквизиты.Удалить(РеквизитВалюта);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Иначе
ВызватьИсключение НСтр("ru = 'Недопустимый вызов объекта на клиенте.'");
#КонецЕсли
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

// Возвращает массив категорий пособий, которые после вступления в прямые выплаты выплачиваются напрямую ФСС.
//
// Возвращаемое значение:
//   Массив
//
Функция КатегорииПрямыхВыплатФСС() Экспорт
	Массив = Новый Массив;
	Массив.Добавить(ОплатаБольничногоЛиста);
	Массив.Добавить(ОплатаБольничногоНесчастныйСлучайНаПроизводстве);
	Массив.Добавить(ОплатаБольничногоПрофзаболевание);
	Массив.Добавить(ОтпускПоБеременностиИРодам);
	Возврат Массив;
КонецФункции

// Возвраащает массив категорий начислений которые отвечают за оплату РК.
//
// Возвращаемое значение:
//    Массив - Значения типа ПеречислениеСсылка.КатегорииНачисленийИНеоплаченногоВремени.
//
Функция КатегорииРК() Экспорт
	Массив = Новый Массив;
	Массив.Добавить(РайонныйКоэффициент);
	Массив.Добавить(ОплатаДолиРайонногоКоэффициентаВСреднемЗаработке);
	Возврат Массив;
КонецФункции

// Возвраащает массив категорий начислений которые отвечают за оплату СН.
//
// Возвращаемое значение:
//    Массив - Значения типа ПеречислениеСсылка.КатегорииНачисленийИНеоплаченногоВремени.
//
Функция КатегорииСН() Экспорт
	Массив = Новый Массив;
	Массив.Добавить(СевернаяНадбавка);
	Массив.Добавить(ОплатаДолиСевернойНадбавкиВСреднемЗаработке);
	Возврат Массив;
КонецФункции

#КонецОбласти

#КонецЕсли

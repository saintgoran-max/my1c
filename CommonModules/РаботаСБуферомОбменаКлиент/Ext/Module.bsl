#Область СлужебныйПрограммныйИнтерфейс
// Обработчик подключенной команды.
//
// Параметры:
//   СсылкаДокумента - ЛюбаяСсылка - ссылка на выбранный объект, для которого выполняется команда.
//   ПараметрыКоманды - см. ПодключаемыеКомандыКлиент.ПараметрыВыполненияКоманды
//
Процедура СкопироватьФайлы(СсылкаДокумента, ПараметрыКоманды) Экспорт
	
	Если Не ЗначениеЗаполнено(ПараметрыКоманды.Источник.ВыделенныеСтроки) Тогда
		Возврат;
	КонецЕсли;
	
	РаботаСБуферомОбменаВызовСервера.СкопироватьВыделенныеФайлы(ПараметрыКоманды.Источник.ВыделенныеСтроки);
	ПоказатьОповещениеПользователя(
	НСтр("ru = 'Скопировать файлы'"),,
	НСтр("ru='Выделенные строки скопированы'"),
	БиблиотекаКартинок.Информация32);
	
КонецПроцедуры

// Обработчик подключенной команды.
//
// Параметры:
//   СсылкаДокумента - ЛюбаяСсылка - ссылка на выбранный объект, для которого выполняется команда.
//   ПараметрыКоманды - см. ПодключаемыеКомандыКлиент.ПараметрыВыполненияКоманды
//
Процедура ВставитьФайлы(СсылкаДокумента, ПараметрыКоманды) Экспорт
	
	МассивСсылок = РаботаСБуферомОбменаВызовСервера.ПолучитьФайлыИзБуфера(ПараметрыКоманды.Форма.ВладелецФайла);
	ПараметрыКоманды.Форма.Элементы.Список.Обновить();
	
КонецПроцедуры

#КонецОбласти

///////////////////////////////////////////////////////////////////////////////////////////////////////
// Copyright (c) 2022, ООО 1С-Софт
// Все права защищены. Эта программа и сопроводительные материалы предоставляются 
// в соответствии с условиями лицензии Attribution 4.0 International (CC BY 4.0)
// Текст лицензии доступен по ссылке:
// https://creativecommons.org/licenses/by/4.0/legalcode
///////////////////////////////////////////////////////////////////////////////////////////////////////

#Область СлужебныйПрограммныйИнтерфейс

Процедура НастроитьИнтерфейсРежимаКассыСамообслуживания(Форма) Экспорт
	
	ЭлементыФормы = Форма.Элементы;
	
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаДанныеДляТекущейСтроки, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.СкидкаНаЧек, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаРучнаяСкидкаНаТовар, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ОтложитьЧек, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.КоманднаяПанельРежим, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.КоманднаяПанельЕще, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.Каталог, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.НайтиВЧеке, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.НайтиПоШтрихкодуКСО, Истина);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ПозватьСотрудника, Истина);
	
	УстановитьДоступностьЭлементаФормы(ЭлементыФормы.СкидкаНаЧек, Ложь);
	УстановитьДоступностьЭлементаФормы(ЭлементыФормы.ОтложитьЧек, Ложь);
	УстановитьДоступностьЭлементаФормы(ЭлементыФормы.КоманднаяПанельРежим, Ложь);
	УстановитьДоступностьЭлементаФормы(ЭлементыФормы.КоманднаяПанельЕще, Ложь);
	УстановитьДоступностьЭлементаФормы(ЭлементыФормы.ГруппаДанныеДляТекущейСтроки, Ложь);
	
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаВидОплатыПредоплата, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаРазделВыдачиНаличных, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаВидОплатыЗачетАванса, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаВидОплатыНСПК, Ложь);
	
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ОтменаРасчетаСкидок, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаВидОплатыВРассрочку, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаРазделДенежныйЯщик, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаВидОплатыКредитом, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаВидОплатыБезнал, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ГруппаЗаменаТоваров, Ложь);
	
	ПараметрыПлиточногоИнтерфейса = ПараметрыПлиточногоИнтерфейса();
	ВидимостьУдалитьЧек = (Форма.Объект.Товары.Количество() > 0);
	Если Не ЭлементыФормы.УдалитьЧек.Видимость = ВидимостьУдалитьЧек Тогда
		УстановитьВидимостьЭлементаФормы(ЭлементыФормы.УдалитьЧек, ВидимостьУдалитьЧек);
		Если Не (Форма.РежимПлитки Или ЭлементыФормы.Декорация1.Видимость = ВидимостьУдалитьЧек) Тогда
			УстановитьВидимостьЭлементаФормы(ЭлементыФормы.Декорация1, ВидимостьУдалитьЧек);
		КонецЕсли;
	КонецЕсли;
	
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ВыбратьКонтрагента, Ложь);
	УстановитьВидимостьЭлементаФормы(ЭлементыФормы.ВыбратьКонтрагентаКартаНеНайдена, Ложь);
	
	УстановитьПараметрыЭлементовКСО(ЭлементыФормы.ГруппаПанельДействий);
	УстановитьПараметрыЭлементовКСО(ЭлементыФормы.ГруппаПлатежнаяСистемаКоманднаяПанель);
	УстановитьПараметрыЭлементовКСО(ЭлементыФормы.ГруппаЗаголовокПлатежнаяСистема);
	
	ЭлементыФормы.ПодтвердитьВозраст.Картинка = БиблиотекаКартинок.ПодтверждениеДействияРМК;
	ЭлементыФормы.ОпровергнутьВозраст.Картинка = БиблиотекаКартинок.ОтказОтДействияРМК;
	
	ЭлементыФормы.ПродолжитьРаботуСЧеком.Картинка = БиблиотекаКартинок.ПодтверждениеДействияРМК;
	ЭлементыФормы.УдалитьЧекИзФормыЗапретаПродажи.Картинка = БиблиотекаКартинок.ОтказОтДействияРМК;

	КрупныйШрифт = ЭлементыФормы.СуммаИтого.Шрифт;
	ЭлементыФормы.ДекорацияВводПромокода.Заголовок = НСтр("ru = 'Просканируйте промокод'");
	ЭлементыФормы.ДекорацияВводПромокода.Шрифт = КрупныйШрифт;
	ЭлементыФормы.ДекорацияВводПромокода.Высота = 2;
	ЭлементыФормы.ОписаниеНайденнойСкидки.Шрифт = КрупныйШрифт;
	ЭлементыФормы.ОписаниеНайденнойСкидки.Высота = 2;
	
КонецПроцедуры

Процедура УстановитьВидимостьГруппыОперации(Элементы) Экспорт
	
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаОперации, Истина);
	Элементы.ГруппаУправления.ТекущаяСтраница = Элементы.ГруппаСтраницаОперации;
	
КонецПроцедуры

Функция ПараметрыРазмеровКомандУправленияЧекомДляКСО() Экспорт
	
	ПараметрыИнтерфейсаКСО = ПараметрыИнтерфейсаКСО();
	ПараметрыПлиточногоИнтерфейса = ПараметрыПлиточногоИнтерфейса();
	ПараметрыРазмеров = Новый Структура;
	ПараметрыРазмеров.Вставить("ВысотаКоманды", ПараметрыИнтерфейсаКСО.ВысотаКоманды);
	ПараметрыРазмеров.Вставить("ШиринаКоманды", ПараметрыИнтерфейсаКСО.ШиринаКоманды);
	ПараметрыРазмеров.Вставить("ВысотаКомандыОперации", ПараметрыПлиточногоИнтерфейса.ВысотаКоманды);
	ПараметрыРазмеров.Вставить("ШиринаКомандыОперации", ПараметрыПлиточногоИнтерфейса.ШиринаКоманды);
	
	Возврат ПараметрыРазмеров;
	
КонецФункции

// Настраивает размеры команд управления составом чека.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - форма РМК.
//  ПараметрыРазмеровКоманд - структура:
//		* ВысотаКоманды - Число,
//		* ШиринаКоманды - Число,
//		* ВысотаКомандыОперации - Число,
//		* ШиринаКомандыОперации - Число.
//  Инициализация - Булево, устанавливать размеры команд, только при инициализации формы.
//
Процедура НастроитьРазмерКомандУправленияСоставомЧека(Форма, ПараметрыРазмеровКоманд, НастройкиЭлементов,
		Инициализация = Ложь) Экспорт
	
	ЭлементыФормы = Форма.Элементы;
	РежимПлитки = Форма.РежимПлитки;

	ВысотаКомандыОперации = ПараметрыРазмеровКоманд.ВысотаКомандыОперации;
	ШиринаКомандыОперации = ПараметрыРазмеровКоманд.ШиринаКомандыОперации;
	
	СтандартныеРазмеры = Новый Структура();
	СтандартныеРазмеры.Вставить("Ширина", ПараметрыРазмеровКоманд.ШиринаКоманды);
	СтандартныеРазмеры.Вставить("Высота", ПараметрыРазмеровКоманд.ВысотаКоманды);
	УстановитьФормуИРазмерыЭлементаСУчетомНастроек(ЭлементыФормы.УдалитьТовар, Форма,
		СтандартныеРазмеры, НастройкиЭлементов);
	
	Если Инициализация Тогда
		УстановитьФормуИРазмерыЭлементаСУчетомНастроек(ЭлементыФормы.ПоказатьОстатки, Форма,
			СтандартныеРазмеры, НастройкиЭлементов);
	КонецЕсли;
	УстановитьФормуИРазмерыЭлементаСУчетомНастроек(ЭлементыФормы.ТекущийТоварУменьшитьКоличество, Форма,
		СтандартныеРазмеры, НастройкиЭлементов);
	УстановитьФормуИРазмерыЭлементаСУчетомНастроек(ЭлементыФормы.ТекущийТоварУвеличитьКоличество, Форма,
		СтандартныеРазмеры, НастройкиЭлементов);
	УстановитьФормуИРазмерыЭлементаСУчетомНастроек(ЭлементыФормы.ПолучитьВес, Форма,
		СтандартныеРазмеры, НастройкиЭлементов);
		
	Если Не Форма.РежимПлитки Тогда
		СтандартныеРазмеры.Ширина = 4;
	КонецЕсли;
	УстановитьФормуИРазмерыЭлементаСУчетомНастроек(ЭлементыФормы.ТекущийТоварУстановитьДробноеКоличество, Форма,
		СтандартныеРазмеры, НастройкиЭлементов);
		
	СтандартныеРазмеры.Вставить("Высота", ВысотаКомандыОперации);
	СтандартныеРазмеры.Вставить("Ширина", ШиринаКомандыОперации);
	УстановитьФормуИРазмерыЭлементаСУчетомНастроек(ЭлементыФормы.ОткрытьПанельИзбранныхТоваров, Форма,
		СтандартныеРазмеры, НастройкиЭлементов);
	
КонецПроцедуры

Процедура НастроитьОтображениеКнопкиФормы(КнопкаФормы, НаборСвойств) Экспорт
	
	УстановленЗаголовокДробноеКоличество = Ложь;
	ИмяКнопкиДробноеКоличество = "ТекущийТоварУстановитьДробноеКоличество";
	ФигураОвальная = "Овальная";
	Для Каждого СвойствоНабора Из НаборСвойств Цикл
		
		Если СвойствоНабора.Ключ = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ВысотаКнопок") Тогда
			КнопкаФормы.Высота = СвойствоНабора.Значение;
		ИначеЕсли СвойствоНабора.Ключ = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ШиринаКнопок") Тогда
			КнопкаФормы.Ширина = СвойствоНабора.Значение;
		ИначеЕсли СвойствоНабора.Ключ = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ФигураКнопок") Тогда
			КнопкаФормы.Фигура = ?(СвойствоНабора.Значение = ФигураОвальная, ФигураКнопки.Овал, ФигураКнопки.Обычная);
		ИначеЕсли СвойствоНабора.Ключ = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ЦветКнопок") Тогда
			КнопкаФормы.ЦветФона = СвойствоНабора.Значение;
		ИначеЕсли СвойствоНабора.Ключ = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ЦветШрифта") Тогда
			КнопкаФормы.ЦветТекста = СвойствоНабора.Значение;
		ИначеЕсли СвойствоНабора.Ключ = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ШрифтКнопок") Тогда
			КнопкаФормы.Шрифт = СвойствоНабора.Значение;
		ИначеЕсли СвойствоНабора.Ключ
				= ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ВеличинаДробногоКоличества") Тогда
				
			КнопкаФормы.Видимость = Истина;
			Если Не УстановленЗаголовокДробноеКоличество Тогда
				КнопкаФормы.Заголовок = Формат(СвойствоНабора.Значение, "ЧГ=");
			КонецЕсли;
				
		ИначеЕсли СвойствоНабора.Ключ = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ЗаголовокКнопки") Тогда
			КнопкаФормы.Заголовок = СвойствоНабора.Значение;
			Если КнопкаФормы.имя = ИмяКнопкиДробноеКоличество Тогда
				УстановленЗаголовокДробноеКоличество = Истина;
			КонецЕсли;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура УстановитьФормуИРазмерыЭлементаСУчетомНастроек(ЭлементФормы, Форма, СтандартныеРазмеры,
		НастройкиЭлементов) Экспорт
		
	РежимПлитки = Форма.РежимПлитки;
	ЗаголовкиОбластей = ИнтерфейсРМКСлужебныйКлиентСервер.ПолучитьЗаголовкиОбластейДляНастроек();
	
	СвойствоВысота = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ВысотаКнопок");
	СвойствоШирина = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ШиринаКнопок");
	СвойствоФигура = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ФигураКнопок");
	СвойствоЦвет = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ЦветКнопок");
	СвойствоШрифт = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ШрифтКнопок");
	
	ФигураЭлемента = ФигураКнопки.Авто;
	АвтоШрифт = Новый Шрифт();
	ЦветЭлемента = Новый Цвет();
	ШрифтЭлемента = АвтоШрифт;
	Если ЭлементФормы = Форма.Элементы.ТекущийТоварУстановитьДробноеКоличество Тогда
		ШрифтЭлемента = Форма.Элементы.ТекущийТоварУстановитьДробноеКоличество.Шрифт;
	КонецЕсли;
	ВысотаЭлемента = СтандартныеРазмеры.Высота;
	ШиринаЭлемента = СтандартныеРазмеры.Ширина;
	НаборСвойствИндивидуальный = Неопределено;
	Если РежимПлитки Тогда
		НаборСвойств = НастройкиЭлементов.Получить(ЗаголовкиОбластей.ПлиточныйИнтерфейс);
		Если Форма.РежимКассыСамообслуживания Тогда
			НаборСвойствИндивидуальный = НастройкиЭлементов.Получить(ЭлементФормы.Имя);
		КонецЕсли;
	Иначе
		НаборСвойств = НастройкиЭлементов.Получить(ЭлементФормы.Имя);
	КонецЕсли;
	Если Не НаборСвойств = Неопределено Тогда
		ЗначениеВысоты = НаборСвойств.Получить(СвойствоВысота);
		ЗначениеШирины = НаборСвойств.Получить(СвойствоШирина);
		Если РежимПлитки И Не НаборСвойствИндивидуальный = Неопределено Тогда
			ЗначениеШириныИндивидуальное = НаборСвойствИндивидуальный.Получить(СвойствоШирина);
			Если ЗначениеЗаполнено(ЗначениеШириныИндивидуальное) Тогда
				ЗначениеШирины = ЗначениеШириныИндивидуальное;
			КонецЕсли;
		КонецЕсли;
		ЗначениеФигуры = НаборСвойств.Получить(СвойствоФигура);
		ЗначениеЦвета = НаборСвойств.Получить(СвойствоЦвет);
		ЗначениеШрифта = НаборСвойств.Получить(СвойствоШрифт);
		Если ЗначениеЗаполнено(ЗначениеВысоты) Тогда
			ВысотаЭлемента = ЗначениеВысоты;
		КонецЕсли;
		Если ЗначениеЗаполнено(ЗначениеШирины) Тогда
			ШиринаЭлемента = ЗначениеШирины;
		КонецЕсли;
		Если Не ЗначениеФигуры = Неопределено И Не ЗначениеФигуры = "Авто" Тогда
			ФигураЭлемента = ?(ЗначениеФигуры = "Овальная", ФигураКнопки.Овал, ФигураКнопки.Обычная);
		КонецЕсли;
		Если Не ЗначениеЦвета = Неопределено Тогда
			ЦветЭлемента = ЗначениеЦвета;
		КонецЕсли;
		Если Не ЗначениеШрифта = Неопределено И Не ЗначениеШрифта = АвтоШрифт Тогда
			ШрифтЭлемента = ЗначениеШрифта;
		КонецЕсли;
	КонецЕсли;
	ЭлементФормы.Высота = ВысотаЭлемента;
	ЭлементФормы.Ширина = ШиринаЭлемента;
	ЭлементФормы.ЦветФона = ЦветЭлемента;
	ЭлементФормы.Фигура = ФигураЭлемента;
	ЭлементФормы.Шрифт = ШрифтЭлемента;
	
КонецПроцедуры

Функция ПолучитьЗначениеСвойстваИзНастроек(ИмяЭлемента, НастройкиЭлементов, СвойствоЭлементов) Экспорт
	
	ЗначениеСвойства = Неопределено;
	
	НаборСвойств = НастройкиЭлементов.Получить(ИмяЭлемента);
	Если Не НаборСвойств = Неопределено Тогда
		ЗначениеСвойства = НаборСвойств.Получить(СвойствоЭлементов);
	КонецЕсли;
	
	Возврат ЗначениеСвойства;
	
КонецФункции

Процедура УстановитьШиринуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов, ЭлементФормы, Ширина) Экспорт
	
	СвойствоШирина = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ШиринаКнопок");
	ШиринаИзНастроек = ПолучитьЗначениеСвойстваИзНастроек(ЭлементФормы.Имя, НастройкиЭлементов, СвойствоШирина);
	Если Не ЗначениеЗаполнено(ШиринаИзНастроек) Тогда
		ЭлементФормы.Ширина = Ширина;
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов, ЭлементФормы, Высота) Экспорт
	
	СвойствоВысота = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ВысотаКнопок");
	ВысотаИзНастроек = ПолучитьЗначениеСвойстваИзНастроек(ЭлементФормы.Имя, НастройкиЭлементов, СвойствоВысота);
	Если Не ЗначениеЗаполнено(ВысотаИзНастроек) Тогда
		ЭлементФормы.Высота = Высота;
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьЗаголовкиОбластейДляНастроек() Экспорт
	
	ЗаголовкиОбластей = Новый Структура();
	
	ЗаголовкиОбластей.Вставить("ОбластьПанельОпераций", НСтр("ru = 'Панель операций'"));
	ЗаголовкиОбластей.Вставить("ОбластьПробитьЧек", НСтр("ru = 'Пробить чек'"));
	ЗаголовкиОбластей.Вставить("ОбластьВернутьсяКПодбору", НСтр("ru = 'Вернуться к подбору'"));
	ЗаголовкиОбластей.Вставить("ОбластьВидыОплат", НСтр("ru = 'Виды оплат'"));
	ЗаголовкиОбластей.Вставить("ОбластьДанныеТекущегоТовара", НСтр("ru = 'Данные текущего товара'"));
	ЗаголовкиОбластей.Вставить("ОбластьСписокТоваров", НСтр("ru = 'Список товаров'"));
	ЗаголовкиОбластей.Вставить("ПлиточныйИнтерфейс", НСтр("ru = 'ПлиточныйИнтерфейс'"));
	
	Возврат ЗаголовкиОбластей;
	
КонецФункции

Функция ПолучитьЭлементыОбластиПробитьЧек(ОсновнаяКнопка = Истина) Экспорт
	
	ЭлементыОбласти = Новый Массив;
	
	ЭлементыОбласти.Добавить("ПерейтиКОплате");
	ЭлементыОбласти.Добавить("ВыполнитьПовторноеПробитие");
	ЭлементыОбласти.Добавить("ПробитьЧекНСПК");
	ЭлементыОбласти.Добавить("ОплатитьБонусамиВПроцессеПереходаКОплате");
	ЭлементыОбласти.Добавить("ПродолжитьПослеПробитияЧека");
	ЭлементыОбласти.Добавить("ПерейтиВОплату");
	ЭлементыОбласти.Добавить("ОтправитьЧек");
	ЭлементыОбласти.Добавить("ОтправкаПробитьЧек");
	ЭлементыОбласти.Добавить("ОтправкаОтправитьЧек");
	Если ОсновнаяКнопка Тогда
		ЭлементыОбласти.Добавить("ПробитьЧек");
	КонецЕсли;
	
	Возврат ЭлементыОбласти;
	
КонецФункции

Функция ПолучитьЭлементыОбластиПанельОпераций() Экспорт
	
	ЭлементыОбласти = Новый Массив;
	
	ЭлементыОбласти.Добавить("ОткрытьПанельИзбранныхТоваров");
	ЭлементыОбласти.Добавить("ОткрытьПлиточныйИнтерфейсКаталога");
	ЭлементыОбласти.Добавить("Каталог");
	ЭлементыОбласти.Добавить("НайтиВЧеке");
	ЭлементыОбласти.Добавить("ПоказатьДействующиеСкидки");
	ЭлементыОбласти.Добавить("ПечатьСкидок");
	ЭлементыОбласти.Добавить("ДобавитьПакет");
	ЭлементыОбласти.Добавить("НайтиПоШтрихкодуКСО");
	ЭлементыОбласти.Добавить("СкидкаНаЧек");
	ЭлементыОбласти.Добавить("УправляемыеСкидки");
	ЭлементыОбласти.Добавить("ПозватьСотрудника");
	ЭлементыОбласти.Добавить("ВвестиКомментарийКЧеку");
	ЭлементыОбласти.Добавить("ОтменитьИзмененияЧека");
	ЭлементыОбласти.Добавить("ОтложитьЧек");
	ЭлементыОбласти.Добавить("УдалитьЧек");
	ЭлементыОбласти.Добавить("КомандаРежим");
	ЭлементыОбласти.Добавить("КомандаЕще");
	
	Возврат ЭлементыОбласти;
	
КонецФункции

Функция ПолучитьЭлементыОбластиВернутьсяКПодбору() Экспорт
	
	ЭлементыОбласти = Новый Массив;
	
	ЭлементыОбласти.Добавить("РаспечататьТекущийТоварныйЧек");
	ЭлементыОбласти.Добавить("ПробитьТекущийЧекПовторно");
	ЭлементыОбласти.Добавить("ВыполнитьВозвратТекущегоЧека");
	ЭлементыОбласти.Добавить("РаспечататьТекущееЗаявлениеНаВозврат");
	ЭлементыОбласти.Добавить("ПовторитьЧек");
	ЭлементыОбласти.Добавить("ВернутьсяВРежимПодбораТовара");
	
	Возврат ЭлементыОбласти;
	
КонецФункции

Функция ПолучитьЭлементыОбластиВидыОплат() Экспорт
	
	ЭлементыОбласти = Новый Массив;
	
	ЭлементыОбласти.Добавить("ОплатаНаличными");
	ЭлементыОбласти.Добавить("ОплатаКартой");
	ЭлементыОбласти.Добавить("ОплатаСБП");
	ЭлементыОбласти.Добавить("ОплатаЮКасса");
	ЭлементыОбласти.Добавить("ОплатаСертификатомНСПК");
	ЭлементыОбласти.Добавить("Предоплата");
	ЭлементыОбласти.Добавить("ОплатаВРассрочку");
	ЭлементыОбласти.Добавить("ЗачетАванса");
	ЭлементыОбласти.Добавить("Безнал");
	ЭлементыОбласти.Добавить("ОплатаКредитом");
	ЭлементыОбласти.Добавить("ОплатаСертификатом");
	ЭлементыОбласти.Добавить("ОплатаБонусами");
	ЭлементыОбласти.Добавить("ПодготовитьДанныеВыдачиНаличных");
	ЭлементыОбласти.Добавить("ОткрытьДенежныйЯщикПриВыдачеНаличных");
	ЭлементыОбласти.Добавить("ОтменитьВсеОплаты");
	
	Возврат ЭлементыОбласти;
	
КонецФункции

Функция ПолучитьЭлементыОбластиДанныхТекущегоТовара() Экспорт
	
	ЭлементыОбласти = Новый Массив;
	
	ЭлементыОбласти.Добавить("УказатьСерии");
	ЭлементыОбласти.Добавить("УказатьХарактеристику");
	ЭлементыОбласти.Добавить("ЗапроситьБалансСертификата");
	ЭлементыОбласти.Добавить("УдалитьТовар");
	ЭлементыОбласти.Добавить("ПоказатьОстатки");
	ЭлементыОбласти.Добавить("ТекущийТоварУменьшитьКоличество");
	ЭлементыОбласти.Добавить("ТекущийТоварУвеличитьКоличество");
	ЭлементыОбласти.Добавить("ТекущийТоварУстановитьДробноеКоличество");
	ЭлементыОбласти.Добавить("ПолучитьВес");
	ЭлементыОбласти.Добавить("СкидкаТекущийТоварПроцент");
	ЭлементыОбласти.Добавить("СкидкаТекущийТоварСумма");
	ЭлементыОбласти.Добавить("СкидкаТекущийТоварУправляемая");
	ЭлементыОбласти.Добавить("СкидкаНаЧекПроцентом");
	ЭлементыОбласти.Добавить("СкидкаНаЧекСуммой");
	ЭлементыОбласти.Добавить("ПоказатьРассчитанныеСкидки");
	
	Возврат ЭлементыОбласти;
	
КонецФункции

Функция ПараметрыРазмеровИнтерфейса() Экспорт
	
	РезультатФункции = Новый Структура();
	РезультатФункции.Вставить("ШиринаПолейОплат", 9);
	РезультатФункции.Вставить("ШиринаПанелиВидовОплат", 25);
	РезультатФункции.Вставить("ШиринаИнтервалов", 4);
	РезультатФункции.Вставить("ШиринаКнопокОплатыСтандартная", 12);
	РезультатФункции.Вставить("ШиринаПанелиДанныхПокупателя", 24);
	
	Возврат РезультатФункции;
	
КонецФункции

Процедура НастроитьРазмерыКомандВРежимеКассыСамообслуживания(Форма, НастройкиЭлементов) Экспорт
	
	ПараметрыИнтерфейсаКСО = ПараметрыИнтерфейсаКСО();
	ПараметрыПлиточногоИнтерфейса = ПараметрыПлиточногоИнтерфейса();
	ВысотаКоманд = ПараметрыИнтерфейсаКСО.ВысотаКоманды;
	ШиринаКоманд = ПараметрыИнтерфейсаКСО.ШиринаКоманды;
	ВысотаКомандГруппыОперации = ПараметрыПлиточногоИнтерфейса.ВысотаКоманды;
	ШиринаКомандГруппыОперации = ПараметрыПлиточногоИнтерфейса.ШиринаКомандыСНадписью;
	ЭлементыФормы = Форма.Элементы;
	
	УстановитьШиринуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ОткрытьПлиточныйИнтерфейсКаталога, ШиринаКомандГруппыОперации);
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ОткрытьПлиточныйИнтерфейсКаталога, ВысотаКомандГруппыОперации);
		
	УстановитьШиринуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.НайтиПоШтрихкодуКСО, ШиринаКомандГруппыОперации);
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.НайтиПоШтрихкодуКСО, ВысотаКомандГруппыОперации);
		
	УстановитьШиринуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ДобавитьПакет, ШиринаКомандГруппыОперации);
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ДобавитьПакет, ВысотаКомандГруппыОперации);
		
	УстановитьШиринуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ПозватьСотрудника, ШиринаКомандГруппыОперации);
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ПозватьСотрудника, ВысотаКомандГруппыОперации);
		
	ЗаголовкиОбластей = ИнтерфейсРМКСлужебныйКлиентСервер.ПолучитьЗаголовкиОбластейДляНастроек();
	НаборСвойствПлитки = НастройкиЭлементов.Получить(ЗаголовкиОбластей.ПлиточныйИнтерфейс);
	СвойствоВысота = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ВысотаКнопок");
	
	ЗначениеВысоты = Неопределено;
	Если Не НаборСвойствПлитки = Неопределено Тогда
		ЗначениеВысоты = НаборСвойствПлитки.Получить(СвойствоВысота);
	КонецЕсли;
	ЭлементыФормы.ОтборВесовогоТовараКСО.Ширина = ШиринаКомандГруппыОперации;
	Если Не ЗначениеЗаполнено(ЗначениеВысоты) Тогда
		ЭлементыФормы.ОтборВесовогоТовараКСО.Высота = ВысотаКомандГруппыОперации;
	КонецЕсли;
	
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ОплатаКартой, ВысотаКоманд);
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ОплатаСБП, ВысотаКоманд);
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ОплатаСертификатом, ВысотаКоманд);
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ОплатаБонусами, ВысотаКоманд);
	УстановитьВысотуЭлементаПриОтсутствииПерсональнойНастройки(НастройкиЭлементов,
		ЭлементыФормы.ОтменитьВводКарты, ВысотаКоманд);
	
КонецПроцедуры

Процедура ВывестиОшибкуОткрытияРабочегоМеста(Элементы, ТекстЗаголовка, ТекстСообщения) Экспорт

	ПараметрыИнтерфейса = ПараметрыИнтерфейсаКСО();
	ВысотаКоманд = ПараметрыИнтерфейса.ВысотаКоманды;
	ШиринаКоманд = ПараметрыИнтерфейса.ШиринаКоманды;
	
	УстановитьВидимостьГруппыОперации(Элементы);
	Элементы.УдалитьЧек.Картинка = БиблиотекаКартинок.ЗавершитьРаботу;
	Элементы.УдалитьЧек.Высота = ВысотаКоманд;
	Элементы.УдалитьЧек.Ширина = ШиринаКоманд;
	Элементы.УдалитьЧек.Отображение = ОтображениеКнопки.Картинка;
	УстановитьДоступностьЭлементаФормы(Элементы.УдалитьЧек, Истина);
	УстановитьДоступностьЭлементаФормы(Элементы.ГруппаОперации, Истина);
	
	ЭлементыИсключения = Новый Массив;
	ЭлементыИсключения.Добавить(Элементы.ПодгруппаУдалитьЧек);
	ЭлементыИсключения.Добавить(Элементы.Декорация1);
	ЭлементыИсключения.Добавить(Элементы.ДекорацияРастяжениеСправа);
	ЭлементыИсключения.Добавить(Элементы.ПодгруппаЧасыСтатусСмены);
	ЭлементыИсключения.Добавить(Элементы.ДекорацияСтатусСмены);
	
	Для Каждого ЭлементГруппы Из Элементы.ГруппаОперации.ПодчиненныеЭлементы Цикл
		Если ЭлементыИсключения.Найти(ЭлементГруппы) = Неопределено Тогда
			УстановитьВидимостьЭлементаФормы(ЭлементГруппы, Ложь);
		КонецЕсли;
	КонецЦикла;
	
	ОбщегоНазначенияРМККлиентСервер.УстановитьТекущуюСтраницу(Элементы.СтраницаЗавершение);
	Элементы.ЗаголовокОшибкиВхода.Заголовок = ТекстЗаголовка;
	Элементы.ТекстОшибкиВхода.Заголовок = ТекстСообщения;
	
КонецПроцедуры

Процедура УстановитьРежимРаботыПустойЧек(Форма) Экспорт
	
	Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.ЧекПустой;
	НастроитьПанельУправленияЧекомВРежимеВводаТоваров(Форма, Форма.Элементы);
	
КонецПроцедуры

Функция ЭтоЭтапРаботыСОплатой(Форма) Экспорт
	
	РезультатФункции = (Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.Оплата
		Или Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.ПереходКОплате);
	Возврат РезультатФункции;
	
КонецФункции

Функция ЭтоЭтапРаботыДоОплаты(Форма) Экспорт
	
	РезультатФункции = (Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.ЧекПустой
		Или Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.ВводТоваров);
	Возврат РезультатФункции;
	
КонецФункции

Процедура НастроитьДоступностьМаркетинговыхКомандПоЭтапуРаботыСЧеком(Форма, ЭтоОперацияПродажи) Экспорт
	
	Элементы = Форма.Элементы;
	РежимРаботыСЧеком = Форма.РежимРаботыСЧеком;
	РежимыРаботыСЧеком = Форма.РежимыРаботыСЧеком;
	ДоступностьЭлементов = (Не (РежимРаботыСЧеком = РежимыРаботыСЧеком.Оплата));
	УстановитьДоступностьЭлементаФормы(Элементы.РассчитатьСкидки, ДоступностьЭлементов);
	УстановитьДоступностьЭлементаФормы(Элементы.ОтменаРасчетаСкидок,
		ДоступностьЭлементов И Форма.СкидкиРассчитаны);
	УстановитьДоступностьЭлементаФормы(Элементы.ГруппаПромокоды, ДоступностьЭлементов);
	УстановитьДоступностьЭлементаФормы(Элементы.ПоказатьСкидкиНаЧек, ДоступностьЭлементов);
	УстановитьДоступностьЭлементаФормы(Элементы.ВвестиДанныеПокупателя, Не Форма.ЭтоЧекКоррекции);
	УстановитьДоступностьЭлементаФормы(Элементы.ОплатаБонусами, ДоступностьЭлементов Или Форма.БонусыДоступноКОплате > 0);
	
КонецПроцедуры

Процедура НастроитьДоступностьГруппДанныхПокупателя(Форма) Экспорт
	
	ДоступностьДанных = (Не Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.Оплата);
	ДанныеКартыЗаполнены = ДанныеДисконтнойКартыЗаполнены(Форма);
	Элементы = Форма.Элементы;
	УстановитьДоступностьЭлементаФормы(Элементы.ГруппаСлоевДанныеПокупателяДополнительнаяОбласть, ДоступностьДанных);
	УстановитьДоступностьЭлементаФормы(Элементы.ГруппаПоискКарты, ДоступностьДанных);
	УстановитьДоступностьЭлементаФормы(Элементы.ОсновнаяГруппаСозданиеКарты, ДоступностьДанных И Не ДанныеКартыЗаполнены);
	УстановитьДоступностьЭлементаФормы(Элементы.ОсновнаяГруппаКартаНайдена, ДоступностьДанных);
	
КонецПроцедуры

Процедура НастроитьДоступностьЭлементовФормыПриПереключенииРежима(Форма,
	ЭтоОперацияПродажи,
	НастроитьМаркетинговыеКоманды = Истина) Экспорт
	
	Элементы = Форма.Элементы;
	ДоступностьЭлементов = (Не ЭтоЭтапРаботыСОплатой(Форма));
	НастроитьДоступностьЭлементовГруппыОперацииКромеОтложитьУдалить(Элементы, ДоступностьЭлементов);
	УстановитьДоступностьЭлементаФормы(Элементы.СменитьКассира, ДоступностьЭлементов);
	УстановитьДоступностьЭлементаФормы(Элементы.ГруппаЛево, ДоступностьЭлементов);
	Если НастроитьМаркетинговыеКоманды Тогда
		НастроитьДоступностьМаркетинговыхКомандПоЭтапуРаботыСЧеком(Форма, ЭтоОперацияПродажи);
	КонецЕсли;
	
КонецПроцедуры

Процедура НастроитьДоступностьЭлементовГруппыОперацииКромеОтложитьУдалить(Элементы, ДоступностьЭлементов) Экспорт
	
	ЭлементыИсключения = Новый Массив;
	ЭлементыИсключения.Добавить(Элементы.ПодгруппаУдалитьЧек);
	ЭлементыИсключения.Добавить(Элементы.ПодгруппаОтложитьЧек);
	ЭлементыИсключения.Добавить(Элементы.ПодгруппаВвестиКомментарийКЧеку);
	ЭлементыИсключения.Добавить(Элементы.ПозватьСотрудника);
	ЭлементыИсключения.Добавить(Элементы.ПодгруппаЧасыСтатусСмены);
	ЭлементыИсключения.Добавить(Элементы.ДекорацияСтатусСмены);
	
	Для Каждого ЭлементГруппы Из Элементы.ГруппаОперации.ПодчиненныеЭлементы Цикл
		Если ЭлементыИсключения.Найти(ЭлементГруппы) = Неопределено Тогда
			УстановитьДоступностьЭлементаФормы(ЭлементГруппы, ДоступностьЭлементов);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура ВосстановитьКнопкиВидовОплат(Форма) Экспорт
	
	Элементы = Форма.Элементы;
	Для Каждого ГруппаВидаОплат Из Элементы.ГруппаВидовОплат.ПодчиненныеЭлементы Цикл
		Если ТипЗнч(ГруппаВидаОплат) = Тип("ГруппаФормы") Тогда
			Для Каждого ГруппаВидаОплатГоризонтально Из ГруппаВидаОплат.ПодчиненныеЭлементы Цикл
				Если ТипЗнч(ГруппаВидаОплатГоризонтально) = Тип("ГруппаФормы") Тогда
					Для Каждого ЭлементПодгруппы Из ГруппаВидаОплатГоризонтально.ПодчиненныеЭлементы Цикл
						Если ТипЗнч(ЭлементПодгруппы) = Тип("КнопкаФормы") И (Не ЭлементПодгруппы.Видимость) Тогда
							УстановитьВидимостьЭлементаФормы(ЭлементПодгруппы, Истина);
						КонецЕсли;
						Если ЭлементПодгруппы.Вид = ВидГруппыФормы.Страницы Тогда
							ВосстановитьПодсказки(ЭлементПодгруппы);
						КонецЕсли;
					КонецЦикла;
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаОплатаСБП, Ложь);
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаОплатаЮКасса, Ложь);
	УстановитьВидимостьЭлементаФормы(Элементы.ДекорацияУдалитьСБП, Истина);
	УстановитьВидимостьЭлементаФормы(Элементы.ДекорацияУдалитьЮКасса, Истина);
	
	УстановитьВидимостьЭлементаФормы(Элементы.ПодготовитьДанныеВыдачиНаличных, Истина);
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаВыдачиНаличных, Истина);
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаРазделДенежныйЯщик, Истина);
	УстановитьВидимостьЭлементаФормы(Элементы.ОткрытьДенежныйЯщикПриВыдачеНаличных, Истина); 
	УстановитьВидимостьЭлементаФормы(Элементы.ДекорацияПодсказкаНаличныеВКассеДенежныйЯщик, Истина);
	
	ДельтаШирины = ?(Форма.ВидОтображения = ПредопределенноеЗначение("Перечисление.ВидыОтображения.Увеличенный"), 1, 2);
	Элементы.СуммаОплаченоСБП.Ширина = Элементы.СуммаОплаченоНал.Ширина - ДельтаШирины;
	Элементы.СуммаОплаченоЮКасса.Ширина = Элементы.СуммаОплаченоНал.Ширина - ДельтаШирины;
	
КонецПроцедуры

Процедура СкрытьКнопкиНулевыхВидовОплат(Форма) Экспорт
	
	Элементы = Форма.Элементы;
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ОплатаНаличными,
		Элементы.ГруппаОплатаНал, Элементы.ГруппаПодсказкаОплатаНаличными, Форма.СуммаОплаченоНал);
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ОплатаКартой,
		Элементы.ГруппаОплатаКартой, Элементы.ГруппаПодсказкаОплатаКартой, Форма.СуммаОплаченоБН);
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ОплатаСБП,
		Элементы.ГруппаОплатаСБП, Элементы.ГруппаПодсказкаОплатаСБП, Форма.СуммаОплаченоСБП);
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ОплатаЮКасса,
		Элементы.ГруппаОплатаЮКасса, Неопределено, Форма.СуммаОплаченоЮКасса);
	
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ОплатаСертификатомНСПК,
		Элементы.ГруппаСуммаОплатаСертификатамиНСПК, Элементы.ГруппаПодсказкаОплатаСертификатомНСПК, Форма.СуммаОплаченоСертификатамиНСПК);
	
	Если Форма.СуммаОплаченоВРассрочку = 0 Тогда
		УстановитьВидимостьЭлементаФормы(Элементы.ГруппаОплатаПредоплата, Ложь);
		УстановитьВидимостьЭлементаФормы(Элементы.ГруппаПодсказкаПредоплата, Ложь);
		УстановитьВидимостьЭлементаФормы(Элементы.Предоплата, Ложь);
		УстановитьВидимостьЭлементаФормы(Элементы.ГруппаОплатаВРассрочку, Ложь);
		УстановитьВидимостьЭлементаФормы(Элементы.ГруппаПодсказкаОплатаВРассрочку, Ложь);
		УстановитьВидимостьЭлементаФормы(Элементы.ОплатаВРассрочку, Ложь);
	ИначеЕсли Не Элементы.ГруппаОплатаПредоплата.Видимость Тогда
		УстановитьВидимостьЭлементаФормы(Элементы.Предоплата, Ложь);
	Иначе
		УстановитьВидимостьЭлементаФормы(Элементы.ОплатаВРассрочку, Ложь);
	КонецЕсли;
	
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ЗачетАванса,
		Элементы.ГруппаОплатаЗачетАванса, Неопределено, Форма.СуммаОплаченоЗачетАванса);
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.Безнал, Элементы.ГруппаОплатаБезнал, Неопределено, Форма.СуммаОплаченоБезнал);
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ОплатаКредитом,
		Элементы.ГруппаОплатаКредитом, Элементы.ГруппаПодсказкаОплатаКредитом, Форма.СуммаОплаченоКредитом);
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ОплатаСертификатом,
		Элементы.ГруппаОплатаСертификатом, Элементы.ГруппаПодсказкаОплатаСертификатом, Форма.СуммаОплаченоСертификатами);
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ОплатаБонусами,
		Элементы.ГруппаОплатаБонусами, Элементы.ГруппаПодсказкаОплатаБонусами, Форма.СуммаОплаченоБонусами);
	СкрытьРазделОплатыПриНеобходимости(Элементы, Элементы.ПодготовитьДанныеВыдачиНаличных,
		Элементы.ГруппаВыдачиНаличных, Неопределено, Форма.СуммаНаличныхКВыдаче);
	УстановитьВидимостьЭлементаФормы(Элементы.ОткрытьДенежныйЯщикПриВыдачеНаличных, Ложь);
	УстановитьВидимостьЭлементаФормы(Элементы.ДекорацияПодсказкаНаличныеВКассеДенежныйЯщик, Ложь);
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаКнопкиОтменыВидовОплат, Ложь);
	
	УстановитьВидимостьЭлементаФормы(Элементы.ДекорацияУдалитьСБП, Ложь);
	УстановитьВидимостьЭлементаФормы(Элементы.ДекорацияУдалитьЮКасса, Ложь);
	Элементы.СуммаОплаченоСБП.Ширина = Элементы.СуммаОплаченоНал.Ширина;
	Элементы.СуммаОплаченоЮКасса.Ширина = Элементы.СуммаОплаченоНал.Ширина;
	
КонецПроцедуры

// Возвращает текущую активную группу родительской группы.
//
// Параметры:
//  ЭлементГруппа - ГруппаФормы.
//
// Возвращаемое значение:
//  АктивнаяГруппа - ГруппаФормы.
//
Функция ПолучитьТекущийСлойГруппы(ЭлементГруппа) Экспорт
	
	АктивнаяГруппа = Неопределено;
	КоличествоПотомков = ЭлементГруппа.ПодчиненныеЭлементы.Количество();
	ИндексПотомка = 0;
	Пока ИндексПотомка < КоличествоПотомков И АктивнаяГруппа = Неопределено Цикл
		ТекущийПотомок = ЭлементГруппа.ПодчиненныеЭлементы.Получить(ИндексПотомка);
		Если ТекущийПотомок.Видимость Тогда
			АктивнаяГруппа = ТекущийПотомок;
		КонецЕсли;
		ИндексПотомка = ИндексПотомка + 1;
	КонецЦикла;
	
	Возврат АктивнаяГруппа;
	
КонецФункции

// Возвращает признак, использует ли форма вместо страниц слои групп.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения.
//
// Возвращаемое значение:
//  Булево - Истина - используются слои.
//
Функция ИспользоватьСлои(Форма) Экспорт
	
	РезультатФункции = Ложь;
	Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Форма, "ИспользоватьСлои") Тогда
		РезультатФункции = (Форма.ИспользоватьСлои = Истина);
	КонецЕсли;
	Возврат РезультатФункции;
	
КонецФункции

// Устанавливает переданную группу активной у родительской группы формы РМК
//
// Параметры:
//  ЭлементГруппа - ГруппаФормы.
//
Процедура УстановитьТекущийСлойГруппы(ЭлементГруппа) Экспорт
	
	ГруппаРодитель = ЭлементГруппа.Родитель;
	ПереключитьНаСлойГруппы(ГруппаРодитель, ЭлементГруппа);
	
КонецПроцедуры

Процедура АктивизироватьСлойГруппы(Форма, ЭлементГруппа) Экспорт
	Элементы = Форма.Элементы;
	УстановитьТекущийСлойГруппы(ЭлементГруппа);
	
	ВозвратНазадДоступен = (ЭтоЭтапРаботыСОплатой(Форма)
		Или Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.ВозвратКоррекция)
		И ((Форма.ВертикальнаяОриентация И НЕ Форма.РежимЦифровойПанели = Форма.РежимыЦифровойПанели.ДанныеПокупателя) 
			Или Форма.РежимЦифровойПанели = Форма.РежимыЦифровойПанели.Пустой)
		И ПолучитьТекущийСлойГруппы(Элементы.ГруппаСлоевСлужебнаяИнформацияЧека)
			= Элементы.ОбластьОперативногоУправленияЧеком
		И Не ПолучитьТекущийСлойГруппы(Элементы.ГруппаСлоевЛеваяВерхняяВспомогательнаяПанель) = Элементы.ГруппаПроверкаНСПК;
		
	УстановитьВидимостьВозвратаВПодбор(Форма, ВозвратНазадДоступен);
	Если Не Форма.ВертикальнаяОриентация Тогда
		Если ВозвратНазадДоступен Тогда
			УстановитьТекущийСлойГруппы(Элементы.ГруппаКомандВозвратИзПробитияЧека);
		ИначеЕсли Элементы.ГруппаКомандВозвратИзПробитияЧека.Видимость Тогда
			УстановитьТекущийСлойГруппы(Элементы.ГруппаДополнительнаяКоманднаяПанельПустая);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьВидимостьВозвратаВПодбор(Форма, ВозвратНазадДоступен) Экспорт
	Элементы = Форма.Элементы;
	Если Форма.ВертикальнаяОриентация Тогда
		УстановитьВидимостьЭлементаФормы(Элементы.ГруппаВозвратВПодборИзУправления, ВозвратНазадДоступен);
		УстановитьВидимостьЭлементаФормы(Элементы.ГруппаВозвратВПодборИзСертификатов, ВозвратНазадДоступен);
		УстановитьВидимостьЭлементаФормы(Элементы.ГруппаВозвратВПодборИзПанелиНаличных, ВозвратНазадДоступен);
	КонецЕсли;
КонецПроцедуры

Процедура УстановитьВидимостьДоступныхБонусов(Элементы, ПоказыватьДоступныеБонусы) Экспорт
	УстановитьВидимостьЭлементаФормы(Элементы.ДополнительнаяГруппаКартаНайденаКонтейнер, ПоказыватьДоступныеБонусы);
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаИнформацияБонусы, ПоказыватьДоступныеБонусы);
КонецПроцедуры

Процедура ВключитьДополнительнуюКоманднуюПанельУправленияЧеком(ЭлементГруппа) Экспорт
	
	ГруппаРодитель = ЭлементГруппа.Родитель;
	УстановитьВидимостьЭлементаФормы(ГруппаРодитель, Истина);
	ПереключитьНаСлойГруппы(ГруппаРодитель, ЭлементГруппа);
	
КонецПроцедуры

Функция РазделительДробнойЧасти() Экспорт
	Возврат Сред("" + 1.1 , 2, 1);
КонецФункции

Функция ТелефонныйКодСтраны() Экспорт
	Возврат НСтр("ru = '+7'");
КонецФункции

Функция МаскаВводаТелефона() Экспорт
	Возврат НСтр("ru = '+7 999 999 99 99'");
КонецФункции

Функция ТолькоЦифры(ИсходнаяСтрока) Экспорт
	
	РезультатФункции = "";
	ИндексСимвола = 0;
	Пока ИндексСимвола < СтрДлина(ИсходнаяСтрока) Цикл
		ТекущийСимвол = Сред(ИсходнаяСтрока, ИндексСимвола + 1, 1);
		Если СтрНайти("0123456789", ТекущийСимвол) > 0 Тогда
			РезультатФункции = РезультатФункции + ТекущийСимвол;
		КонецЕсли;
		ИндексСимвола = ИндексСимвола + 1;
	КонецЦикла;
	Возврат РезультатФункции;
	
КонецФункции

Функция ТелефонБезКодаСтраны(ИсходнаяСтрока) Экспорт
	
	ДлинаНомерБезКодаСтраны = 10;
	СтрокаТолькоЦифры = ТолькоЦифры(ИсходнаяСтрока);
	Если СтрДлина(СтрокаТолькоЦифры) > ДлинаНомерБезКодаСтраны Тогда
		СтрокаТолькоЦифры = Прав(СтрокаТолькоЦифры, ДлинаНомерБезКодаСтраны);
	КонецЕсли;
	Возврат СтрокаТолькоЦифры;
	
КонецФункции

Функция ПривестиЗначениеКМаскеТелефона(ИсходноеЗначение, МаскаТелефона) Экспорт
	
	РезультатФункции = ТелефонныйКодСтраны();
	ЧастьБезКодаСтраны = ОставитьЦифрыТелефонаБезКодаСтраны(ИсходноеЗначение);
	НомерСимволаМаски = СтрДлина(ТелефонныйКодСтраны()) + 1;
	ДлинаМаски = СтрДлина(МаскаТелефона);
	ДлинаЧасти = СтрДлина(ЧастьБезКодаСтраны);
	НомерСимволаЧасти = 1;
	Пока НомерСимволаМаски <= ДлинаМаски И НомерСимволаЧасти <= ДлинаЧасти Цикл
		СимволМаски = Сред(МаскаТелефона, НомерСимволаМаски, 1);
		Если СимволМаски = "9" Тогда
			СимволЦифрыТелефона = Сред(ЧастьБезКодаСтраны, НомерСимволаЧасти, 1);
			РезультатФункции = СтрШаблон("%1%2", РезультатФункции, СимволЦифрыТелефона);
			НомерСимволаЧасти = НомерСимволаЧасти + 1;
		Иначе
			РезультатФункции = СтрШаблон("%1%2", РезультатФункции, СимволМаски);
		КонецЕсли;
		НомерСимволаМаски = НомерСимволаМаски + 1;
	КонецЦикла;
	Возврат РезультатФункции;
	
КонецФункции

// Преобразует строку даты формата "dd.MM.yyyy" в массив из 8 цифр в порядке от дней до года
//
// Параметры:
//  СтрокаФорматаДаты - Строка, 10.
//
// Возвращаемое значение:
//  Массив - массив цифр даты.
//
Функция СформироватьМассивЦифрДаты(СтрокаФорматаДаты) Экспорт
	
	МассивЦифр = Новый Массив;
	ПозицияПервойТочки = 2;
	ПозицияВторойТочки = 4;
	КоличествоЦифрДаты = 8;
	Для ИндексЦифры = 1 По КоличествоЦифрДаты Цикл
		СмещениеИндекса = ?(ИндексЦифры > ПозицияВторойТочки, 2, ?(ИндексЦифры > ПозицияПервойТочки, 1, 0));
		ИндексСимвола = ИндексЦифры + СмещениеИндекса;
		ТекущийЭлементМассива = Неопределено;
		Если ИндексСимвола <= СтрДлина(СтрокаФорматаДаты) Тогда
			СимволСтроки = Сред(СтрокаФорматаДаты, ИндексСимвола, 1);
			Если СтрНайти("0123456789", СимволСтроки) > 0 Тогда
				ТекущийЭлементМассива = Число(СимволСтроки);
			КонецЕсли;
		КонецЕсли;
		МассивЦифр.Добавить(ТекущийЭлементМассива);
	КонецЦикла;
	Возврат МассивЦифр;
	
КонецФункции

Функция СформироватьКаноническуюСтрокуДатыИзМассиваЦифр(МассивЦифр) Экспорт
	
	СтрокаДатыКаноническая = "";
	Если МассивЦифрЗаполненПолностью(МассивЦифр) Тогда
		Для ИндексЭлемента = 4 По 7 Цикл
			СтрокаДатыКаноническая = СтрокаДатыКаноническая + Строка(МассивЦифр[ИндексЭлемента]);
		КонецЦикла;
		СтрокаДатыКаноническая = СтрокаДатыКаноническая + Строка(МассивЦифр[2]) + Строка(МассивЦифр[3]);
		СтрокаДатыКаноническая = СтрокаДатыКаноническая + Строка(МассивЦифр[0]) + Строка(МассивЦифр[1]);
	КонецЕсли;
	Возврат СтрокаДатыКаноническая;
	
КонецФункции

Функция МассивЦифрЗаполненПолностью(МассивЦифр) Экспорт
	
	РезультатФункции = (МассивЦифр.Количество() = 8);
	ИндексЭлемента = 0;
	Пока ИндексЭлемента < 8 И РезультатФункции Цикл
		Если МассивЦифр[ИндексЭлемента] = Неопределено Тогда
			РезультатФункции = Ложь;
		КонецЕсли;
		ИндексЭлемента = ИндексЭлемента + 1;
	КонецЦикла;
	Возврат РезультатФункции;
	
КонецФункции

Функция МассивЦифрПустой(МассивЦифр) Экспорт
	
	РезультатФункции = Истина;
	ИндексЭлемента = 0;
	Пока РезультатФункции И ИндексЭлемента < МассивЦифр.Количество() Цикл
		Если ТипЗнч(МассивЦифр[ИндексЭлемента]) = Тип("Число") Тогда
			РезультатФункции = Ложь;
		КонецЕсли;
		ИндексЭлемента = ИндексЭлемента + 1;
	КонецЦикла;
	Возврат РезультатФункции;
	
КонецФункции

// Добавляет к строковому представлению даты цифру в первую свободную позицию, если такая есть.
// Например, "27.02.19  " + "5" = "27.02.195 "
//
// Параметры:
//  ТекстВвода - Строка, 10. Строка из поля ввода даты с маской "99.99.9999",
//  Цифра - Строка, 1. Символ цифры.
//
// Возвращаемое значение:
//  Строка, 10 - Строка с представлением даты с маской "99.99.9999".
//
Функция ДобавитьЦифруКТекстуДаты(ТекстВвода, Цифра) Экспорт
	
	РезультатФункции = ТекстВвода;
	ДлинаПредставленияДаты = 10;
	ПозицияДляВставки = ПозицияПоследнейЦифрыВСтроке(ТекстВвода) + 1;
	Если ПозицияДляВставки <= ДлинаПредставленияДаты Тогда
		Если ПозицияДляВставки = 3 Или ПозицияДляВставки = 6 Тогда
			ПозицияДляВставки = ПозицияДляВставки + 1;
		КонецЕсли;
		ЛеваяЧасть = ?(ПозицияДляВставки = 1, "", Лев(ТекстВвода, ПозицияДляВставки - 1));
		ПраваяЧасть = ?(ПозицияДляВставки < ДлинаПредставленияДаты, Сред(ТекстВвода, ПозицияДляВставки + 1), "");
		РезультатФункции = ЛеваяЧасть + Цифра + ПраваяЧасть;
	КонецЕсли;
	Возврат РезультатФункции;
	
КонецФункции

Функция ЭтоПолеДатыСМаской(ЭлементПоля) Экспорт
	
	РезультатФункции = Ложь;
	Если Не ЭлементПоля = Неопределено Тогда
		Если ЭлементПоля.Маска = МаскаВводаДаты() Тогда
			РезультатФункции = Истина;
		КонецЕсли;
	КонецЕсли;
	Возврат РезультатФункции;
	
КонецФункции

Функция ЭтоПолеТелефонаСМаской(ЭлементПоля) Экспорт
	
	РезультатФункции = Ложь;
	ДлинаТелефонаБезКодаСтраны = 10;
	Если Не ЭлементПоля = Неопределено Тогда
		КодСтраны = ТелефонныйКодСтраны();
		Если СтрДлина(ЭлементПоля.Маска) > ДлинаТелефонаБезКодаСтраны И Лев(ЭлементПоля.Маска, СтрДлина(КодСтраны)) = КодСтраны Тогда
			РезультатФункции = Истина;
		КонецЕсли;
	КонецЕсли;
	Возврат РезультатФункции;
	
КонецФункции

Функция ПараметрыКартыЛояльности() Экспорт
	
	ПараметрыКарты = Новый Структура();
	ПараметрыКарты.Вставить("ИдентификаторКарты", "");
	ПараметрыКарты.Вставить("КодКарты", "");
	ПараметрыКарты.Вставить("ФИО", "");
	ПараметрыКарты.Вставить("НомерТелефонаВладельцаКарты", "");
	ПараметрыКарты.Вставить("СуммаПродаж", 0);
	ПараметрыКарты.Вставить("АдресЭПВладельцаКарты", "");
	ПараметрыКарты.Вставить("ДатаРождения", Дата(1, 1, 1));
	ПараметрыКарты.Вставить("ЭтоКомпания", Ложь);
	
	Возврат ПараметрыКарты;
	
КонецФункции

Функция ДанныеЛояльностиЗаполнены(Форма) Экспорт
	
	РезультатФункции = ЗначениеЗаполнено(Форма.Объект.ДисконтнаяКарта)
		Или ЗначениеЗаполнено(Форма.Объект.Контрагент)
		Или ПараметрыКартыЗаполнены(Форма.ПараметрыКартыЛояльности);
		
	Возврат РезультатФункции;
	
КонецФункции

Функция ДанныеДисконтнойКартыЗаполнены(Форма) Экспорт
	
	РезультатФункции = ЗначениеЗаполнено(Форма.Объект.ДисконтнаяКарта)
		Или ПараметрыКартыЗаполнены(Форма.ПараметрыКартыЛояльности);
		
	Возврат РезультатФункции;
	
КонецФункции

Функция СохраненТолькоНомерКартыЛояльности(ПараметрыКартыЛояльности) Экспорт
	
	РезультатФункции = Ложь;

	Если ТипЗнч(ПараметрыКартыЛояльности) = Тип("Структура") И ЗначениеЗаполнено(ПараметрыКартыЛояльности.КодКарты)
			И Не ЗначениеЗаполнено(ПараметрыКартыЛояльности.ИдентификаторКарты) Тогда
		
		РезультатФункции = Истина;
		
	КонецЕсли;
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ПолученИдентификаторКартыЛояльности(ПараметрыКартыЛояльности) Экспорт
	
	РезультатФункции = Ложь;
	Если ТипЗнч(ПараметрыКартыЛояльности) = Тип("Структура")
			И ЗначениеЗаполнено(ПараметрыКартыЛояльности.ИдентификаторКарты) Тогда
		
		РезультатФункции = Истина;
		
	КонецЕсли;
	Возврат РезультатФункции;
	
КонецФункции

Функция ПараметрыКартыЗаполнены(ПараметрыКартыЛояльности) Экспорт
	
	РезультатФункции = Ложь;
	
	Если ТипЗнч(ПараметрыКартыЛояльности) = Тип("Структура")
		И (ЗначениеЗаполнено(ПараметрыКартыЛояльности.ИдентификаторКарты)
		Или ЗначениеЗаполнено(ПараметрыКартыЛояльности.КодКарты)) Тогда
		
		РезультатФункции = Истина;
		
	КонецЕсли;
	
	Возврат РезультатФункции;
	
КонецФункции

Процедура ОтобразитьИнформациюОБонусныхБаллах(Форма, ДанныеБонусов) Экспорт
	
	Элементы = Форма.Элементы;
	УстановитьВидимостьДоступныхБонусов(Элементы, Истина);
	
	Если Форма.СписатьБаллыПоВладельцу
		Или Форма.ОбъединитьКарты Тогда
		КоличествоБонусов = 0;
		КоличествоБонусовКОплате = 0;
		Для Каждого СтрокаТаблицы Из Форма.ТаблицаВыбранныхКарт Цикл
			КоличествоБонусов = КоличествоБонусов + СтрокаТаблицы.ОстатокБонусныхБаллов;
			КоличествоБонусовКОплате = КоличествоБонусовКОплате + СтрокаТаблицы.ОстатокБонусныхБаллов * ?(СтрокаТаблицы.КурсБонусов = 0, 1, СтрокаТаблицы.КурсБонусов);
		КонецЦикла;
		Если Форма.ПроцентБонуснойПрограммы = 0 Тогда
			Форма.ПроцентОплатыБонусами = ДанныеБонусов.PaymentPercent;
		Иначе
			Форма.ПроцентОплатыБонусами = Форма.ПроцентБонуснойПрограммы;
		КонецЕсли;
	Иначе
		КоличествоБонусов = ДанныеБонусов.BonusCount;
		КоличествоБонусовКОплате = КоличествоБонусов * ?(ДанныеБонусов.BonusRate = 0, 1, ДанныеБонусов.BonusRate);
		Форма.ПроцентОплатыБонусами = ДанныеБонусов.PaymentPercent;
	КонецЕсли;
	Форма.КоличествоБонусов = КоличествоБонусов;
	Форма.КурсБонусов = ДанныеБонусов.BonusRate;
	Форма.БонусыДоступноКОплате = КоличествоБонусовКОплате;
	
	УстановитьДоступностьЭлементаФормы(Элементы.ОплатитьБонусами,
		Форма.КоличествоБонусов > 0 И Элементы.ГруппаОплатаБонусами.Доступность);
		
	ТекстСообщения = СтрШаблон(НСтр("ru = 'Бонусами можно оплатить до %1%% от суммы чека'"), Форма.ПроцентОплатыБонусами);
	Элементы.ДекорацияВозможноОплатитьБонусами.Заголовок = ТекстСообщения;
	Элементы.ИнформацияМожноОплатитьБонусами.Заголовок = ТекстСообщения;
		
КонецПроцедуры

Процедура НастроитьДоступностьСозданияКарты(Форма, ДоступенВводКарты, ДоступенВводКонтрагента) Экспорт

	Элементы = Форма.Элементы;
	Если ДоступенВводКарты Тогда
		Элементы.ОсновнаяГруппаСозданиеКарты.Доступность = Истина;
		Элементы.ГруппаНоваяКартаТелефон.Доступность = ДоступенВводКонтрагента;
		Элементы.ГруппаНоваяКартаФИО.Доступность = ДоступенВводКонтрагента;
		Элементы.ГруппаНоваяКартаПочта.Доступность = ДоступенВводКонтрагента;
		Элементы.ГруппаНоваяКартаДатаРождения.Доступность = ДоступенВводКонтрагента;
	Иначе
		Элементы.ОсновнаяГруппаСозданиеКарты.Доступность = Ложь;
	КонецЕсли;
	УстановитьМаскуВводаДатыРожденияНовойКарты(Форма);
	
КонецПроцедуры

Процедура УстановитьМаскуВводаДатыРожденияНовойКарты(Форма) Экспорт

	МаскаДата = "";
	Элементы = Форма.Элементы;
	Если Элементы.ОсновнаяГруппаСозданиеКарты.Доступность И Элементы.ГруппаНоваяКартаДатаРождения.Доступность
		И Элементы.ГруппаРеквизитовКарты.Доступность Тогда
		
		МаскаДаты = МаскаВводаДаты();
		
	КонецЕсли;
	Элементы.НоваяКартаДатаРождения.Маска = МаскаДаты;
	
КонецПроцедуры

Функция ПреобразоватьДатуИзСтроковогоПредставления(ТекстДаты) Экспорт
	
	ОписаниеТипаДата = Новый ОписаниеТипов("Дата");
	ДатаРожденияКлиента = Дата(1, 1, 1);
	МассивЦифрДаты = СформироватьМассивЦифрДаты(ТекстДаты);
	Если МассивЦифрЗаполненПолностью(МассивЦифрДаты) Тогда
		ДатаКаноническаяСтрока = СформироватьКаноническуюСтрокуДатыИзМассиваЦифр(МассивЦифрДаты);
		ДатаРожденияКлиента = ОписаниеТипаДата.ПривестиЗначение(ДатаКаноническаяСтрока);
	КонецЕсли;
	Возврат ДатаРожденияКлиента;
	
КонецФункции

Процедура ОтобразитьИнформациюОПродажах(Форма, ДанныеПродаж) Экспорт
	
	Элементы = Форма.Элементы;

	Форма.НайденнаяКартаСуммаПродаж = Формат(ДанныеПродаж.SellCount,"ЧДЦ=2");
	Форма.СуммаПродаж = Формат(ДанныеПродаж.SellCount,"ЧДЦ=2");

		
КонецПроцедуры

Функция ЭтоЭтапРаботыСОплатойИлиКоррекцией(Форма) Экспорт
	
	РезультатФункции = (Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.Оплата
		Или Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.ПереходКОплате
		Или Форма.РежимРаботыСЧеком = Форма.РежимыРаботыСЧеком.ВозвратКоррекция);
	Возврат РезультатФункции;
	
КонецФункции

Функция ПредставлениеПустойКлавиши() Экспорт
	
	Возврат НСтр("ru = 'Нет'");
	
КонецФункции

Функция ИсточникиДанныхДляПлитки() Экспорт
	
	ИсточникиДанныхПлитки = Новый Структура;
	ИсточникиДанныхПлитки.Вставить("Остальное", 0);
	ИсточникиДанныхПлитки.Вставить("Палитра", 1);
	ИсточникиДанныхПлитки.Вставить("Избранное", 2);
	ИсточникиДанныхПлитки.Вставить("ВесовойТовар", 3);
	ИсточникиДанныхПлитки.Вставить("СлужебныеГруппы", 4);
	
	Возврат ИсточникиДанныхПлитки;
	
КонецФункции

Функция ИмяГруппыДругихВесовыхТоваров() Экспорт
	
	Возврат НСтр("ru = 'Другие товары'");
	
КонецФункции

Функция НаименованиеКнопкиЗакрытияДанныхПокупателя(РежимКСО) Экспорт
	
	РезультатФункции = "";
	Если РежимКСО Тогда
		РезультатФункции = НСтр("ru = 'Вернуться к покупке'");
	Иначе
		РезультатФункции = НСтр("ru = 'Вернуться к продаже'");
	КонецЕсли;
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ПодготовитьПараметрыОперацииНаРабочемМестеКассира(Форма) Экспорт
	
	ОбъектФормы = Форма.Объект;
	ПараметрыОперации = Новый Структура();
	
	ПараметрыОперации.Вставить("РабочееМесто", Форма.РабочееМесто);
	ПараметрыОперации.Вставить("ТорговыйОбъект", ОбъектФормы.ТорговыйОбъект);
	ПараметрыОперации.Вставить("Кассир", ОбъектФормы.Кассир);
	ПараметрыОперации.Вставить("ПользовательБД", ОбъектФормы.ПользовательБД);
	ПараметрыОперации.Вставить("РегистрируемаяОперация", Неопределено);
	ПараметрыОперации.Вставить("Чек", Неопределено);
	ПараметрыОперации.Вставить("Сумма", 0);
	
	Возврат ПараметрыОперации;
	
КонецФункции

Функция РегистрироватьДействияКассира(Форма) Экспорт
	
	Возврат (Форма.ВестиЖурналДействийКассира И Не Форма.ЭтоПолноправныйПользователь);
	
КонецФункции

Функция ШаблонКэшаКонтроляПродаж() Экспорт
	
	РезультатФункции = Новый Структура();
	РезультатФункции.Вставить("ЭлементыКонтроляПродаж", Новый Массив);
	РезультатФункции.Вставить("СтрокиВидовКонтроля", Новый Массив);
	РезультатФункции.Вставить("НоменклатураПлиткиКонтроляПродаж", Новый Массив);
	РезультатФункции.Вставить("НоменклатураКонтроляПродаж", Новый Массив);
	РезультатФункции.Вставить("ОбщийКонтрольПродаж", Новый Массив);
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ШаблонЭлементаКонтроляПродаж() Экспорт

	ПустаяДата = Дата(1, 1, 1, 0, 0, 0);
	
	РезультатФункции = Новый Структура();
	РезультатФункции.Вставить("КонтрольПродаж", Неопределено);
	РезультатФункции.Вставить("НаименованиеГруппыТоваров", "");
	РезультатФункции.Вставить("СтрокиКонтроля", Новый Массив);
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ШаблонСтрокиКонтроляПродаж() Экспорт
	
	ПустаяДата = Дата(1, 1, 1, 0, 0, 0);
	
	РезультатФункции = Новый Структура();
	РезультатФункции.Вставить("ВидКонтроля", Неопределено);
	РезультатФункции.Вставить("НачалоПериода", ПустаяДата);
	РезультатФункции.Вставить("КонецПериода", ПустаяДата);
	РезультатФункции.Вставить("ДеньНедели", Неопределено);
	РезультатФункции.Вставить("ВремяНачала", ПустаяДата);
	РезультатФункции.Вставить("ВремяОкончания", ПустаяДата);
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ШаблонСтрокиВидаКонтроляПродаж() Экспорт
	
	РезультатФункции = Новый Структура();
	РезультатФункции.Вставить("ВидКонтроля", Неопределено);
	РезультатФункции.Вставить("ТипКонтроля", Неопределено);
	РезультатФункции.Вставить("ТекстШаблонаПредупреждения", "");
	РезультатФункции.Вставить("ПараметрыКонтроля", Новый Массив);
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ШаблонСтрокиПараметровВидаКонтроля() Экспорт
	
	РезультатФункции = Новый Структура();
	РезультатФункции.Вставить("Параметр", Неопределено);
	РезультатФункции.Вставить("Значение", Неопределено);
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ШаблонСтрокиНоменклатурыКонтроляПродаж() Экспорт
	
	РезультатФункции = Новый Структура();
	РезультатФункции.Вставить("Номенклатура", Неопределено);
	РезультатФункции.Вставить("Характеристика", Неопределено);
	РезультатФункции.Вставить("МассивКонтролейПродаж", Новый Массив);
	
	Возврат РезультатФункции;
	
КонецФункции

Процедура ДобавитьНовыеДанныеКэшаПродаж(ДанныеПриемник, ДанныеИсточник, ЭтоКэшПлитки = Ложь) Экспорт

	Если ДанныеПриемник.Количество() = 0 Или ДанныеИсточник.Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	// Общий контроль продаж
	ПеренестиНедостающиеЭлементыПростогоМассива(ДанныеИсточник.ОбщийКонтрольПродаж, ДанныеПриемник.ОбщийКонтрольПродаж);
	
	// Контроль продаж по номенклатуре
	Для Каждого СтрокаНоменклатурыКонтроляИсточник Из ДанныеИсточник.НоменклатураКонтроляПродаж Цикл
		
		СтрокаНоменклатурыКонтроляПриемник
			= ПолучитьСтрокуНоменклатурыКонтроляПродаж(ДанныеПриемник.НоменклатураКонтроляПродаж,
				СтрокаНоменклатурыКонтроляИсточник);
		
		СтрокаНоменклатурыКонтроляПлиткиПриемник
			= ПолучитьСтрокуНоменклатурыКонтроляПродаж(ДанныеПриемник.НоменклатураПлиткиКонтроляПродаж,
				СтрокаНоменклатурыКонтроляИсточник);
		
		Если СтрокаНоменклатурыКонтроляПриемник = Неопределено
				И СтрокаНоменклатурыКонтроляПлиткиПриемник = Неопределено Тогда
			
			НоменклатураКонтроляПродажПриемник
				= ?(ЭтоКэшПлитки, ДанныеПриемник.НоменклатураПлиткиКонтроляПродаж, ДанныеПриемник.НоменклатураКонтроляПродаж);
			НоменклатураКонтроляПродажПриемник.Добавить(СтрокаНоменклатурыКонтроляИсточник);
			
		Иначе
			Если Не СтрокаНоменклатурыКонтроляПриемник = Неопределено Тогда
				ПеренестиНедостающиеЭлементыПростогоМассива(СтрокаНоменклатурыКонтроляИсточник.МассивКонтролейПродаж,
					СтрокаНоменклатурыКонтроляПриемник.МассивКонтролейПродаж);
			КонецЕсли;
			Если Не СтрокаНоменклатурыКонтроляПлиткиПриемник = Неопределено Тогда
				ПеренестиНедостающиеЭлементыПростогоМассива(СтрокаНоменклатурыКонтроляИсточник.МассивКонтролейПродаж,
					СтрокаНоменклатурыКонтроляПлиткиПриемник.МассивКонтролейПродаж);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	// Элементы контроля продаж
	Для Каждого СтрокаЭлементаКонтроляИсточник Из ДанныеИсточник.ЭлементыКонтроляПродаж Цикл
		СтрокаЭлементаКонтроляПриемник = ПолучитьСтрокуЭлементаКонтроляПродаж(ДанныеПриемник.ЭлементыКонтроляПродаж,
			СтрокаЭлементаКонтроляИсточник.КонтрольПродаж);
		
		Если СтрокаЭлементаКонтроляПриемник = Неопределено Тогда
			ДанныеПриемник.ЭлементыКонтроляПродаж.Добавить(СтрокаЭлементаКонтроляИсточник);
		Иначе
			Для Каждого СтрокаКонтроляИсточник Из СтрокаЭлементаКонтроляИсточник.СтрокиКонтроля Цикл
				СтрокаКонтроляПриемник
					= ПолучитьСтрокуКонтроля(СтрокаЭлементаКонтроляПриемник.СтрокиКонтроля, СтрокаКонтроляИсточник);
				Если СтрокаКонтроляПриемник = Неопределено Тогда
					СтрокаЭлементаКонтроляПриемник.СтрокиКонтроля.Добавить(СтрокаКонтроляИсточник);
				КонецЕсли;
			КонецЦикла;
		КонецЕсли;
	КонецЦикла;
	
	// Строки вида контроля
	Для Каждого СтрокаВидаКонтроляИсточник Из ДанныеИсточник.СтрокиВидовКонтроля Цикл
		СтрокаВидаКонтроляПриемник = ПолучитьСтрокуВидаКонтроля(ДанныеПриемник.СтрокиВидовКонтроля,
			СтрокаВидаКонтроляИсточник.ВидКонтроля);
		
		Если СтрокаВидаКонтроляПриемник = Неопределено Тогда
			 ДанныеПриемник.СтрокиВидовКонтроля.Добавить(СтрокаВидаКонтроляИсточник);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Функция DecToHex(Знач Значение) Экспорт
	
	РезультатФункции = "";
	Если Значение <= 0 Тогда
		РезультатФункции = "0";
	Иначе
		Размерность = 16;
		Значение = Цел(Значение);
		Пока Значение > 0 Цикл
			СимволЦифра = Сред("0123456789ABCDEF", Значение%Размерность + 1, 1);
			РезультатФункции = СтрШаблон("%1%2", СимволЦифра, РезультатФункции);
			Значение = Цел(Значение/Размерность);
		КонецЦикла;
	КонецЕсли;

	Пока СтрДлина(РезультатФункции) <= 1 Цикл
		РезультатФункции = СтрШаблон("%1%2", "0", РезультатФункции);
	КонецЦикла;

	Возврат РезультатФункции;

КонецФункции

Функция ОпределитьПрименитьВыравниваниеГруппыОперации(Форма, НаборСвойств) Экспорт
	
	ПоложениеЛево = НСтр("ru = 'Лево'");
	ПоложениеЦентр = НСтр("ru = 'Центр'");
	ПоложениеПраво = НСтр("ru = 'Право'");
	ВыравниваниеАвто = НСтр("ru = 'Авто'");
	СвойствоВыравнивание = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ВыравниваниеЭлементов");
	ЗначениеВыравнивания = НаборСвойств.Получить(СвойствоВыравнивание);
	РастягиватьСередину = Истина;
	ГоризонтальноеПоложениеКоманд = ГоризонтальноеПоложениеЭлемента.Авто;
	
	Если Не (ЗначениеВыравнивания = Неопределено Или ЗначениеВыравнивания = ВыравниваниеАвто) Тогда
		
		РастягиватьСередину = Ложь;
		
		Если ЗначениеВыравнивания = ПоложениеЛево Тогда
			ГоризонтальноеПоложениеКоманд = ГоризонтальноеПоложениеЭлемента.Лево;
		ИначеЕсли ЗначениеВыравнивания = ПоложениеЦентр Тогда
			ГоризонтальноеПоложениеКоманд = ГоризонтальноеПоложениеЭлемента.Центр;
		ИначеЕсли ЗначениеВыравнивания = ПоложениеПраво Тогда
			ГоризонтальноеПоложениеКоманд = ГоризонтальноеПоложениеЭлемента.Право;
		КонецЕсли;
		
	КонецЕсли;
	
	Элементы = Форма.Элементы;
	Если Не Элементы.ДекорацияРастяжениеСправа.Видимость = РастягиватьСередину Тогда
		Элементы.ДекорацияРастяжениеСправа.Видимость = РастягиватьСередину;
		Элементы.ДекорацияОперацияОтступСправа.Видимость = Не РастягиватьСередину;
	КонецЕсли;
	Если Не Элементы.ГруппаОперации.ГоризонтальноеПоложениеПодчиненных = ГоризонтальноеПоложениеКоманд Тогда
		Элементы.ГруппаОперации.ГоризонтальноеПоложениеПодчиненных = ГоризонтальноеПоложениеКоманд;
	КонецЕсли;
	
КонецФункции

Функция РежимыФормыПодтвержденияДобавленияТоваров() Экспорт
	
	РежимыФормы = Новый Структура();

	РежимыФормы.Вставить("ПодтверждениеВеса", 0);
	РежимыФормы.Вставить("РучнойВводВеса", 1);
	РежимыФормы.Вставить("ВводДробногоКоличества", 2);
	
	Возврат РежимыФормы;
	
КонецФункции

Функция ПолучитьРазмерыПлиткиПоУмолчанию(ВертикальнаяОриентация) Экспорт
	
	РезультатФункции = Новый Структура();
	РезультатФункции.Вставить("КоличествоЭлементовВРяду", ?(ВертикальнаяОриентация, 3, 4));
	РезультатФункции.Вставить("КоличествоРядовНаСтранице", ?(ВертикальнаяОриентация, 4, 3));
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ПоказыватьСтатусКассовыхСмен(НастройкиЭлементов) Экспорт
	
	РезультатФункции = Ложь;
	
	ЗаголовкиОбластей = ПолучитьЗаголовкиОбластейДляНастроек();
	СвойствоСтатусСмены = ПредопределенноеЗначение("Перечисление.СвойстваЭлементовРМК.ПоказыватьСтатусСмены");
	НаборСвойствПанелиОпераций = НастройкиЭлементов.Получить(ЗаголовкиОбластей.ОбластьПанельОпераций);
	Если Не НаборСвойствПанелиОпераций = Неопределено Тогда
		ЗначениеСтатусСмены = НаборСвойствПанелиОпераций.Получить(СвойствоСтатусСмены);
		Если ЗначениеСтатусСмены = Истина Тогда
			РезультатФункции = Истина;
		КонецЕсли;
	КонецЕсли;
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ГрупповыеСтатусыКассовыхСмен() Экспорт
	
	ГрупповыеСтатусы = Новый Структура();
	
	ГрупповыеСтатусы.Вставить("Неопределен", 0);
	ГрупповыеСтатусы.Вставить("СменыАктуальны", 1);
	ГрупповыеСтатусы.Вставить("СменыАктуальныЧастично", 2);
	ГрупповыеСтатусы.Вставить("ТребуетсяОткрытиеСмен", 3);
	
	Возврат ГрупповыеСтатусы;
	
КонецФункции

Функция ПолучитьГрупповойСтатусКассовыхСмен(Форма, ДатаТекущая, СтатусСменИзменился) Экспорт
	
	ГрупповыеСтатусы = ГрупповыеСтатусыКассовыхСмен();
	РезультатФункции = ГрупповыеСтатусы.Неопределен;
	
	Сутки = 86400;
	ВремяНачалаСменыВСекундах = Форма.ВремяНачалаСмены - Дата(1, 1, 1);
	МинимальнаяДатаНачалаСмены = НачалоДня(ДатаТекущая) + ВремяНачалаСменыВСекундах;
	Если ДатаТекущая < МинимальнаяДатаНачалаСмены Тогда
		МинимальнаяДатаНачалаСмены = МинимальнаяДатаНачалаСмены - Сутки;
	КонецЕсли;
	ЕстьАктуальнаяСмена = Ложь;
	ЕстьУстаревшаяСмена = Ложь;
	
	КоличествоКасс = Форма.ТаблицаКассККМОрганизаций.Количество();
	ИндексСтроки = 0;
	Пока ИндексСтроки < КоличествоКасс И НЕ (ЕстьАктуальнаяСмена И ЕстьУстаревшаяСмена) Цикл
		СтрокаКассыККМ = Форма.ТаблицаКассККМОрганизаций[ИндексСтроки];
		Если СтрокаКассыККМ.Открыта Тогда
			Если ДатаТекущая - СтрокаКассыККМ.НачалоКассовойСмены >= Сутки
					Или СтрокаКассыККМ.НачалоКассовойСмены < МинимальнаяДатаНачалаСмены Тогда
				ЕстьУстаревшаяСмена = Истина;
			Иначе
				ЕстьАктуальнаяСмена = Истина;
			КонецЕсли;
		Иначе
			ЕстьУстаревшаяСмена = Истина;
		КонецЕсли;
		ИндексСтроки = ИндексСтроки + 1;
	КонецЦикла;
	Если ЕстьАктуальнаяСмена И ЕстьУстаревшаяСмена Тогда
		РезультатФункции = ГрупповыеСтатусы.СменыАктуальныЧастично;
	ИначеЕсли ЕстьАктуальнаяСмена Тогда
		РезультатФункции = ГрупповыеСтатусы.СменыАктуальны;
	ИначеЕсли ЕстьУстаревшаяСмена Тогда
		РезультатФункции = ГрупповыеСтатусы.ТребуетсяОткрытиеСмен;
	КонецЕсли;
	Если Не Форма.ГрупповойСтатусСмен = РезультатФункции Тогда
		СтатусСменИзменился = Истина;
	КонецЕсли;
	
	Возврат РезультатФункции;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ПолучитьСтрокуВидаКонтроля(СтрокиВидовКонтроля, ВидКонтроля)
	
	РезультатФункции = Неопределено;
	КоличествоСтрок = СтрокиВидовКонтроля.Количество();
	ИндексСтроки = 0;
	Пока ИндексСтроки < КоличествоСтрок И РезультатФункции = Неопределено Цикл
		СтрокаВидаКонтроля = СтрокиВидовКонтроля.Получить(ИндексСтроки);
		Если СтрокаВидаКонтроля.ВидКонтроля = ВидКонтроля Тогда
			РезультатФункции = СтрокаВидаКонтроля;
		КонецЕсли;
		ИндексСтроки = ИндексСтроки + 1;
	КонецЦикла;
	Возврат РезультатФункции;
	
КонецФункции

Функция ПолучитьСтрокуЭлементаКонтроляПродаж(ЭлементыКонтроляПродаж, КонтрольПродаж)
	
	РезультатФункции = Неопределено;
	КоличествоСтрок = ЭлементыКонтроляПродаж.Количество();
	ИндексСтроки = 0;
	Пока ИндексСтроки < КоличествоСтрок И РезультатФункции = Неопределено Цикл
		СтрокаЭлементаКонтроляПродаж = ЭлементыКонтроляПродаж.Получить(ИндексСтроки);
		Если СтрокаЭлементаКонтроляПродаж.КонтрольПродаж = КонтрольПродаж Тогда
			РезультатФункции = СтрокаЭлементаКонтроляПродаж;
		КонецЕсли;
		ИндексСтроки = ИндексСтроки + 1;
	КонецЦикла;
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ПолучитьСтрокуКонтроля(СтрокиКонтроля, СтрокаКонтроляИсточник)
	
	РезультатФункции = Неопределено;
	КоличествоСтрок = СтрокиКонтроля.Количество();
	ИндексСтроки = 0;
	ИменаСравниваемыхПолей = "ВидКонтроля,НачалоПериода,КонецПериода,ДеньНедели,ВремяНачала,ВремяОкончания";
	Пока ИндексСтроки < КоличествоСтрок И РезультатФункции = Неопределено Цикл
		СтрокаКонтроля = СтрокиКонтроля.Получить(ИндексСтроки);
		
		Если СтруктурыИдентичны(СтрокаКонтроля, СтрокаКонтроляИсточник, ИменаСравниваемыхПолей) Тогда
			РезультатФункции = СтрокаКонтроля;
		КонецЕсли;
		
		ИндексСтроки = ИндексСтроки + 1;
	КонецЦикла;
	Возврат РезультатФункции;
	
КонецФункции

Функция СтруктурыИдентичны(СтруктураПервая, СтруктураВторая, ИменаПолей)
	
	РезультатФункции = Истина;
	МассивПолей = СтрРазделить(ИменаПолей, ",");
	
	КоличествоПолей = МассивПолей.Количество();
	ИндексПоля = 0;
	Пока ИндексПоля < КоличествоПолей И РезультатФункции = Истина Цикл
		ИмяПоля = МассивПолей[ИндексПоля];
		ЗначениеПервое = Неопределено;
		ЗначениеВторое = Неопределено;
		СтруктураПервая.Свойство(ИмяПоля, ЗначениеПервое);
		СтруктураВторая.Свойство(ИмяПоля, ЗначениеВторое);
		Если Не ЗначениеПервое = ЗначениеВторое Тогда
			РезультатФункции = Ложь;
		КонецЕсли;
		ИндексПоля = ИндексПоля + 1;
	КонецЦикла;
	
	Возврат РезультатФункции;
	
КонецФункции

Процедура ПеренестиНедостающиеЭлементыПростогоМассива(МассивИсточник, МассивПриемник)
	
	Для Каждого ЭлементМассива Из МассивИсточник Цикл
		Если МассивПриемник.Найти(ЭлементМассива) = Неопределено Тогда
			МассивПриемник.Добавить(ЭлементМассива);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Функция ПолучитьСтрокуНоменклатурыКонтроляПродаж(НоменклатураКонтроляПродажПриемник,
		СтрокаНоменклатурыКонтроляИсточник)
		
	РезультатФункции = Неопределено;
	КоличествоСтрок = НоменклатураКонтроляПродажПриемник.Количество();
	ИндексСтроки = 0;
	Пока ИндексСтроки < КоличествоСтрок И РезультатФункции = Неопределено Цикл
		СтрокаНоменклатурыКонтроляПродаж = НоменклатураКонтроляПродажПриемник.Получить(ИндексСтроки);
		Если СтрокаНоменклатурыКонтроляПродаж.Номенклатура = СтрокаНоменклатурыКонтроляИсточник.Номенклатура
			И СтрокаНоменклатурыКонтроляПродаж.Характеристика = СтрокаНоменклатурыКонтроляИсточник.Характеристика Тогда
			
			РезультатФункции = СтрокаНоменклатурыКонтроляПродаж;
			
		КонецЕсли;
		ИндексСтроки = ИндексСтроки + 1;
	КонецЦикла;
	
	Возврат РезультатФункции;
	
КонецФункции

Процедура ВосстановитьПодсказки(ЭлементПодгруппы)
	Для Каждого ЭлементСтраницы Из ЭлементПодгруппы.ПодчиненныеЭлементы Цикл
		Если Найти(ЭлементСтраницы.Имя, "Подсказка") > 0 Тогда
			УстановитьВидимостьЭлементаФормы(ЭлементСтраницы, Истина);
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

Функция МаскаВводаДаты()
	Возврат НСтр("ru = '99.99.9999'");
КонецФункции

Функция ОставитьЦифрыТелефонаБезКодаСтраны(ИсходноеЗначение)
	
	ДлинаНомераТелефона = 10;
	РезультатФункции = ИсходноеЗначение;
	Если СтрНайти(РезультатФункции, ТелефонныйКодСтраны()) = 1 Тогда
		РезультатФункции = Сред(РезультатФункции, СтрДлина(ТелефонныйКодСтраны()) + 1);
		РезультатФункции = ТолькоЦифры(РезультатФункции);
	Иначе
		РезультатФункции = ТолькоЦифры(РезультатФункции);
		Если СтрДлина(РезультатФункции) > ДлинаНомераТелефона Тогда
			РезультатФункции = Прав(РезультатФункции, ДлинаНомераТелефона);
		КонецЕсли;
	КонецЕсли;
	Возврат РезультатФункции;
	
КонецФункции

Функция ПозицияПоследнейЦифрыВСтроке(ПроверяемаяСтрока)
	
	РезультатФункции = 0;
	
	КоличествоСимволов = СтрДлина(ПроверяемаяСтрока);
	ИндексСимвола = КоличествоСимволов;
	Пока ИндексСимвола > 0 И РезультатФункции = 0 Цикл
		ПроверяемыйСимвол = Сред(ПроверяемаяСтрока, ИндексСимвола, 1);
		Если СтрНайти("0123456789", ПроверяемыйСимвол) > 0 Тогда
			РезультатФункции = ИндексСимвола;
		КонецЕсли;
		ИндексСимвола = ИндексСимвола - 1;
	КонецЦикла;
	
	Возврат РезультатФункции;
	
КонецФункции

Функция ПараметрыИнтерфейсаКСО()
	
	ПараметрыИнтерфейса = Новый Структура;
	ПараметрыИнтерфейса.Вставить("ВысотаКоманды", 2);
	ПараметрыИнтерфейса.Вставить("ШиринаКоманды", 5);
	ПараметрыИнтерфейса.Вставить("ШиринаКомандыСНадписью", 13);
	
	Возврат ПараметрыИнтерфейса;
	
КонецФункции

Функция ПараметрыПлиточногоИнтерфейса() Экспорт
	
	ПараметрыИнтерфейса = Новый Структура;
	ПараметрыИнтерфейса.Вставить("ВысотаКоманды", 3);
	ПараметрыИнтерфейса.Вставить("ШиринаКоманды", 6);
	ПараметрыИнтерфейса.Вставить("ШиринаКомандыСНадписью", 13);
	
	Возврат ПараметрыИнтерфейса;
	
КонецФункции

Процедура НастроитьПанельУправленияЧекомВРежимеВводаТоваров(Форма, Элементы)
	
	УстановитьТекущийСлойГруппы(Элементы.ГруппаДополнительноеУправлениеЧеком);
	УстановитьВидимостьЭлементаФормы(Элементы.ПанельВидовОплат, Ложь);
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаИнформацияПокупателя, Истина);
	УстановитьВидимостьЭлементаФормы(Элементы.ГруппаQRCode, Не Форма.РежимКассыСамообслуживания);
	АктивизироватьСлойГруппы(Форма, Элементы.ГруппаКомандПереходаКОплате);
	
КонецПроцедуры

Процедура СкрытьРазделОплатыПриНеобходимости(Элементы, КнопкаРаздела, ГруппаОплатыРаздела, ГруппаПодсказкаРаздела, РеквизитРаздела)
	Если РеквизитРаздела = 0 Тогда
		УстановитьВидимостьЭлементаФормы(ГруппаОплатыРаздела, Ложь);
		УстановитьВидимостьЭлементаФормы(КнопкаРаздела, Ложь);
		Если ГруппаПодсказкаРаздела <> Неопределено Тогда
			УстановитьВидимостьЭлементаФормы(ГруппаПодсказкаРаздела, Ложь);
		КонецЕсли;
	Иначе
		Если (Элементы.ГруппаОплатаСБП = ГруппаОплатыРаздела Или Элементы.ГруппаОплатаЮКасса = ГруппаОплатыРаздела)
			И (Не ГруппаОплатыРаздела.Видимость) Тогда
			
			УстановитьВидимостьЭлементаФормы(ГруппаОплатыРаздела, Истина);
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

Процедура ПереключитьНаСлойГруппы(ГруппаРодитель, ЭлементГруппа)
	Для Каждого СлойГруппы Из ГруппаРодитель.ПодчиненныеЭлементы Цикл
		УстановитьВидимостьЭлементаФормы(СлойГруппы, (ЭлементГруппа = СлойГруппы));
	КонецЦикла;
КонецПроцедуры

Процедура УстановитьВидимостьЭлементаФормы(Элемент, Видимость)
	Элемент.Видимость = Видимость;
КонецПроцедуры

Процедура УстановитьДоступностьЭлементаФормы(Элемент, Доступность)
	Элемент.Доступность = Доступность;
КонецПроцедуры

Процедура УстановитьПараметрыЭлементовКСО(ГруппаПанельДействий)
	
	Для Каждого ПодчиненнаяГруппа Из ГруппаПанельДействий.ПодчиненныеЭлементы Цикл
		
		Если СтрНачинаетсяС(ПодчиненнаяГруппа.Имя, "НЕКСО") Тогда
			ПодчиненнаяГруппа.Видимость = Ложь;
		ИначеЕсли СтрНачинаетсяС(ПодчиненнаяГруппа.Имя, "КСО") Тогда
			ПодчиненнаяГруппа.Видимость = Истина;
		ИначеЕсли СтрНачинаетсяС(ПодчиненнаяГруппа.Имя, "Группа") Тогда
			УстановитьПараметрыЭлементовКСО(ПодчиненнаяГруппа);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

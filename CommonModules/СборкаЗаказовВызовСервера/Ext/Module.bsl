#Область СлужебныйПрограммныйИнтерфейс

Функция КоэффициентПриСборке(ЕдиницаСтроки, ЕдиницаШтрихкода) Экспорт
	
	Если ТипЗнч(ЕдиницаСтроки) = Тип("СправочникСсылка.КлассификаторЕдиницИзмерения") Тогда
		КоэффициентСтроки = 1;
	Иначе
		КоэффициентСтроки = ЕдиницаСтроки.Коэффициент;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ЕдиницаШтрихкода) Тогда
		КоэффициентШтрихкода = 1;
	ИначеЕсли ТипЗнч(ЕдиницаШтрихкода) = Тип("СправочникСсылка.КлассификаторЕдиницИзмерения") Тогда
		КоэффициентШтрихкода = 1;
	Иначе
		КоэффициентШтрихкода = ЕдиницаШтрихкода.Коэффициент;
	КонецЕсли;
	
	Коэффициент = КоэффициентШтрихкода / КоэффициентСтроки;
	
	Возврат Коэффициент;
	
КонецФункции

#КонецОбласти
#Область ПрограммныйИнтерфейс

// Подсчитывает количество комплектов в документе заказа
//
// Параметры:
//  ЗаказПокупателя           - ДокументСсылка - заказ, который необходимо подсчитать
//
// Возвращаемое значение:
//   Число   - Искомое количество
//
Функция ПолучитьКоличествоКомплектов(ЗаказПокупателя) Экспорт
	Запрос = Новый Запрос("ВЫБРАТЬ
	                      |	ЗаказПокупателяЗапасы.Номенклатура КАК Номенклатура
	                      |ИЗ
	                      |	Документ.ЗаказПокупателя.Запасы КАК ЗаказПокупателяЗапасы
	                      |		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.КомплектацииПоУмолчанию КАК КомплектацииПоУмолчанию
	                      |		ПО ЗаказПокупателяЗапасы.Номенклатура = КомплектацииПоУмолчанию.Номенклатура
	                      |			И ЗаказПокупателяЗапасы.Характеристика = КомплектацииПоУмолчанию.Характеристика
	                      |ГДЕ
	                      |	ЗаказПокупателяЗапасы.Ссылка = &Ссылка
	                      |
	                      |СГРУППИРОВАТЬ ПО
	                      |	ЗаказПокупателяЗапасы.Номенклатура");  
	Запрос.УстановитьПараметр("Ссылка",ЗаказПокупателя);
	Результат = Запрос.Выполнить().Выгрузить();
	Возврат Результат.Количество();
КонецФункции  

// Создает документы комплектации на основании заказа
//
// Параметры:
//  Основание           - ДокументСсылка - заказ-основание
//
// Возвращаемое значение:
//   Массив   - Коллекция ссылок на созданные документы
//
Функция СоздатьДокументыКомплектации(Основание) Экспорт
	МассивКомплектаций = Новый Массив();
	
	ЗапасыККомплектации = ПолучитьИзВременногоХранилища(Основание.АдресТовары);
	
	Для Каждого Запас Из ЗапасыККомплектации Цикл 
		ДанныеДляЗаполнения = Новый Структура();
		ДанныеДляЗаполнения.Вставить("Основание",Основание.Основание); 
		МассивЗапасов = Новый Массив;
		МассивЗапасов.Добавить(Запас);
		АдресТовары = ПоместитьВоВременноеХранилище(МассивЗапасов);
		ДанныеДляЗаполнения.Вставить("АдресТовары", АдресТовары);
		НоваяКомплектация = Документы.КомплектацияЗапасов.СоздатьДокумент();
		НоваяКомплектация.Дата = ТекущаяДатаСеанса();
		НоваяКомплектация.Заполнить(ДанныеДляЗаполнения);
		НоваяКомплектация.ЗаполнитьТабличнуюЧастьПоКомплектации();
		НоваяКомплектация.Записать();
		МассивКомплектаций.Добавить(НоваяКомплектация.Ссылка);
	КонецЦикла; 
	
	Возврат МассивКомплектаций;

КонецФункции     

// Возвращает значение реквизита, прочитанного из информационной базы по ссылке на объект.
// Если доступа к реквизиту нет, возникнет исключение прав доступа.
// Если необходимо зачитать реквизит независимо от прав текущего пользователя,
// то следует использовать предварительный переход в привилегированный режим.
//
// Параметры:
//   Ссылка - ЛюбаяСсылка- ссылка на объект.
//
// Возвращаемое значение:
//   Произвольный - зависит от типа значения прочитанного реквизита.
//
Функция ЗначениеРеквизитаТипДенежныхСредств(Ссылка) Экспорт
	
	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Ссылка, "ТипДенежныхСредств");
	
КонецФункции 

// Возвращает значение реквизита, прочитанного из информационной базы по ссылке на объект.
// Если доступа к реквизиту нет, возникнет исключение прав доступа.
// Если необходимо зачитать реквизит независимо от прав текущего пользователя,
// то следует использовать предварительный переход в привилегированный режим.
//
// Параметры:
//   Ссылка - ЛюбаяСсылка- ссылка на объект.
//
// Возвращаемое значение:
//   Произвольный - зависит от типа значения прочитанного реквизита.
//
Функция ЗначениеРеквизитаОрганизация(Ссылка) Экспорт
	
	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Ссылка, "Организация");
	
КонецФункции 

// Возвращает структуру, содержащую значения реквизитов, прочитанные из информационной базы по ссылке на объект.
// Если необходимо зачитать реквизит независимо от прав текущего пользователя,
// то следует использовать предварительный переход в привилегированный режим.
//
// Параметры:
//  Ссылка    - ЛюбаяСсылка - объект, значения реквизитов которого необходимо получить.
//            - Строка      - полное имя предопределенного элемента, значения реквизитов которого необходимо получить.
//
// Возвращаемое значение:
//  Структура - содержит имена (ключи) и значения затребованных реквизитов.
//              Если в параметр Ссылка передана пустая ссылка, то возвращается структура, 
//              соответствующая именам реквизитов со значениями Неопределено.
//              Если в параметр Ссылка передана ссылка несуществующего объекта (битая ссылка), 
//              то все реквизиты вернутся со значением Неопределено.
//
Функция ЗначенияРеквизитовЧека(Ссылка) Экспорт
	
	Возврат ОбщегоНазначения.ЗначенияРеквизитовОбъекта(Ссылка, "НеприменениеККТ, ДокументРасчетов");
	
КонецФункции

#КонецОбласти

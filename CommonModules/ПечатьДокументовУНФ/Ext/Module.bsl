#Область ПрограммныйИнтерфейс

// Перед началом формирования документа
// 
// Параметры:
//  ТабличныйДокумент	 - ТабличныйДокумент
//  ПервыйДокумент		 - Булево
//  НомерСтрокиНачало	 - Число
//  ДанныеПечати		 - Структура
//
Процедура ПередНачаломФормированияДокумента(ТабличныйДокумент, ПервыйДокумент, НомерСтрокиНачало,
	ДанныеПечати = Неопределено) Экспорт
	
	Если ПервыйДокумент = Истина
		ИЛИ ПервыйДокумент = Неопределено Тогда
		
		ПервыйДокумент = Ложь;
		
	Иначе
		
		ТабличныйДокумент.ВывестиГоризонтальныйРазделительСтраниц();
		
	КонецЕсли;
	
	НомерСтрокиНачало = ТабличныйДокумент.ВысотаТаблицы + 1;
	
	Если ТипЗнч(ДанныеПечати) = Тип("Структура") Тогда
		
		ДанныеПечати.Очистить();
		
	КонецЕсли;
	
КонецПроцедуры

// Возвращает признак необходимости выделить как составляющую набора
//
// Параметры:
//  ПараметрыНоменклатуры	 - Структура - .
// 
// Возвращаемое значение:
//  Булево - необходимо выделить как состав набора
//
Функция НеобходимоВыделитьКакСоставляющуюНабора(ПараметрыНоменклатуры) Экспорт
	
	Перем НеобходимоВыделитьКакСоставНабора;
	
	ПараметрыНоменклатуры.Свойство("НеобходимоВыделитьКакСоставНабора", НеобходимоВыделитьКакСоставНабора);
	Возврат НеобходимоВыделитьКакСоставНабора = Истина;
	
КонецФункции

// Формирует представление номенклатуры в печатной форме
//
// Параметры:
//  ПараметрыНоменклатуры - Структура - параметры, по которым формируется представление, возможные ключи:
//   * ПредставлениеНоменклатуры - Строка, СправочникСсылка.Номенклатура - .
//   * ПредставлениеХарактеристики - Строка, СправочникСсылка.ХарактеристикиНоменклатуры - .
//   * ПредставлениеСерииНоменклатуры - Строка, СправочникСсылка.ПартииНоменклатуры - .
//   * ПредставлениеПартии  - Строка, СправочникСсылка.ПартииНоменклатуры - .
//   * ПредставлениеКодаТНВЭД - Строка, СправочникСсылка.КлассификаторТНВЭД - .
//
// Возвращаемое значение:
//  Строка - представление номенклатуры в печатной форме.
//
Функция ПредставлениеНоменклатуры(ПараметрыНоменклатуры) Экспорт
	
	ПредставлениеНоменклатуры = "";
	ПредставлениеХарактеристики = "";
	ПредставлениеСерииНоменклатуры = "";
	ПредставлениеПартии = "";
	ПредставлениеКодаТНВЭД = "";
	// "ПредставлениеАртикула" начиная с версии 1.6.11 не используется в представлении номенклатуры.
	
	НеобходимоВыделитьКакСоставНабора = НеобходимоВыделитьКакСоставляющуюНабора(ПараметрыНоменклатуры);
	
	Если ПараметрыНоменклатуры.Свойство("Содержание") 
		И НЕ ПустаяСтрока(ПараметрыНоменклатуры.Содержание) Тогда
		
		ПредставлениеНоменклатуры = ПараметрыНоменклатуры.Содержание;
		
	Иначе
		
		ПредставлениеНоменклатуры = СтрокаПредставленияНоменклатуры(ПараметрыНоменклатуры);
		ПредставлениеХарактеристики = СтрокаПредставленияХарактеристики(ПараметрыНоменклатуры);
		ПредставлениеСерииНоменклатуры = СтрокаПредставленияСерииНоменклатуры(ПараметрыНоменклатуры);
		ПредставлениеПартии = СтрокаПредставленияПартии(ПараметрыНоменклатуры);
		ПредставлениеКодаТНВЭД = СтрокаПредставленияКодаТНВЭД(ПараметрыНоменклатуры);
		
	КонецЕсли;
	
	Возврат ПредставлениеНоменклатурыДляПечати(ПредставлениеНоменклатуры, ПредставлениеХарактеристики,
		ПредставлениеСерииНоменклатуры, ПредставлениеПартии, ПредставлениеКодаТНВЭД, НеобходимоВыделитьКакСоставНабора);
	
КонецФункции // ПредставлениеНоменклатуры()

Функция СтрокаПредставленияНоменклатуры(ПараметрыНоменклатуры) Экспорт
	
	ПредставлениеНоменклатуры = "";
	Если ПараметрыНоменклатуры.Свойство("ПредставлениеНоменклатуры") Тогда
		
		Если ТипЗнч(ПараметрыНоменклатуры.ПредставлениеНоменклатуры) = Тип("СправочникСсылка.Номенклатура") Тогда
			
			ПредставлениеНоменклатуры = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ПараметрыНоменклатуры.ПредставлениеНоменклатуры, "НаименованиеПолное");
			Если ПустаяСтрока(ПредставлениеНоменклатуры) Тогда
				
				ПредставлениеНоменклатуры = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ПараметрыНоменклатуры.ПредставлениеНоменклатуры, "Наименование");
				
			КонецЕсли;
			
		Иначе
			
			ПредставлениеНоменклатуры = СокрЛП(ПараметрыНоменклатуры.ПредставлениеНоменклатуры);
			
		КонецЕсли;
		
	КонецЕсли;
	
	
	Возврат ПредставлениеНоменклатуры;
	
КонецФункции

Функция СтрокаПредставленияХарактеристики(ПараметрыНоменклатуры) Экспорт
	
	ПредставлениеХарактеристики = "";
	Если ПолучитьФункциональнуюОпцию("ИспользоватьХарактеристики") Тогда
		
		Если ПараметрыНоменклатуры.Свойство("ПредставлениеХарактеристики") Тогда
			
			Если ТипЗнч(ПараметрыНоменклатуры.ПредставлениеХарактеристики) = Тип("СправочникСсылка.ХарактеристикиНоменклатуры") Тогда
				
				ПредставлениеХарактеристики = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ПараметрыНоменклатуры.ПредставлениеХарактеристики, "НаименованиеДляПечати");
				Если ПустаяСтрока(ПредставлениеХарактеристики) Тогда
					
					ПредставлениеХарактеристики = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ПараметрыНоменклатуры.ПредставлениеХарактеристики, "Наименование");
					
				КонецЕсли;
				
			Иначе
				
				ПредставлениеХарактеристики = СокрЛП(ПараметрыНоменклатуры.ПредставлениеХарактеристики);
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ПредставлениеХарактеристики;
	
КонецФункции

Функция СтрокаПредставленияПартии(ПараметрыНоменклатуры) Экспорт
	
	ПредставлениеПартии = "";
	Если ПолучитьФункциональнуюОпцию("ИспользоватьПартии") Тогда
		
		Если ПараметрыНоменклатуры.Свойство("ПредставлениеПартии") Тогда
			
			Если ТипЗнч(ПараметрыНоменклатуры.ПредставлениеПартии) = Тип("СправочникСсылка.ПартииНоменклатуры") Тогда
				
				ПредставлениеПартии = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ПараметрыНоменклатуры.ПредставлениеПартии, "Наименование");
				
			Иначе
				
				ПредставлениеПартии = СокрЛП(ПараметрыНоменклатуры.ПредставлениеПартии);
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ПредставлениеПартии;
	
КонецФункции

Функция СтрокаПредставленияКодаТНВЭД(ПараметрыНоменклатуры) Экспорт
	
	ПредставлениеКодаТНВЭД = "";
	Если ПараметрыНоменклатуры.Свойство("КодТНВЭД") Тогда
		
		Если ТипЗнч(ПараметрыНоменклатуры.КодТНВЭД) = Тип("СправочникСсылка.КлассификаторТНВЭД") Тогда
			
			ПредставлениеКодаТНВЭД = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ПараметрыНоменклатуры.КодТНВЭД, "Код");
			
		Иначе
			
			ПредставлениеКодаТНВЭД = СокрЛП(ПараметрыНоменклатуры.КодТНВЭД);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ПредставлениеКодаТНВЭД;
	
КонецФункции

Функция СтрокаПредставленияСерииНоменклатуры(ПараметрыНоменклатуры) Экспорт
	
	ПредставлениеСерииНоменклатуры = "";
	Если ПолучитьФункциональнуюОпцию("ИспользоватьСерииНоменклатуры") Тогда
		
		Если ПараметрыНоменклатуры.Свойство("ПредставлениеСерииНоменклатуры") Тогда
			
			Если ТипЗнч(ПараметрыНоменклатуры.ПредставлениеСерииНоменклатуры) = Тип("СправочникСсылка.СерииНоменклатуры") Тогда
				
				ПредставлениеСерииНоменклатуры = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ПараметрыНоменклатуры.ПредставлениеСерииНоменклатуры, "Наименование");
				
			Иначе
				
				ПредставлениеСерииНоменклатуры = СокрЛП(ПараметрыНоменклатуры.ПредставлениеСерииНоменклатуры);
				
			КонецЕсли;
			
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ПредставлениеСерииНоменклатуры;
	
КонецФункции

Функция ПредставлениеДатыВДокументах(ЗначениеДаты) Экспорт
	
	Возврат Формат(ЗначениеДаты, "ДЛФ=DD");
	
КонецФункции

Функция ПредставлениеЗаголовкаНДС(СуммаНДС, СуммаВключаетНДС, ЧастичнаяОплата, ЕстьСтавкаНольПроцентов = Ложь) Экспорт
	Перем ЗаголовокИтоговНДС;
	
	ТекстНДС = НСтр("ru ='НДС'");
	Если ЧастичнаяОплата Тогда
		
		ТекстНДС = ТекстНДС + НСтр("ru =' оплаты'");
		
	КонецЕсли;
	
	Если СуммаНДС = 0 
		И НЕ ЕстьСтавкаНольПроцентов Тогда
		
		ЗаголовокИтоговНДС = НСтр("ru ='Без налога (НДС)'");
		
	ИначеЕсли СуммаВключаетНДС Тогда
		
		ЗаголовокИтоговНДС = СтрШаблон(НСтр("ru ='В том числе %1:'"), ТекстНДС);
		
	Иначе
		
		ЗаголовокИтоговНДС = СтрШаблон(НСтр("ru ='Сумма %1:'"), ТекстНДС);
		
	КонецЕсли;
	
	Возврат ЗаголовокИтоговНДС;
	
КонецФункции

// Строка - представление итогов НДС документа с выводом % ставки, если она одна
//
// Параметры:
//  СуммаНДС		 - Число
//  СуммаВключаетНДС - Булево
//  ЧастичнаяОплата	 - Булево
//  СтавкиНДС		 - Массив из СправочникСсылка.СтавкиНДС
// 
// Возвращаемое значение:
//   - Строка - представление строки итогов
//
Функция ПредставлениеИтоговНДС(СуммаНДС, СуммаВключаетНДС, СтавкиНДС) Экспорт
	
	ЗаголовокИтоговНДС = "";
	
	Если СуммаНДС = 0 Тогда
		
		Если СтавкиНДС.Найти(УправлениеНебольшойФирмойПовтИсп.ПолучитьСтавкуНДСНоль()) = Неопределено Тогда
			ЗаголовокИтоговНДС      = НСтр("ru='Без налога (НДС)'");
 		Иначе
			ЗаголовокИтоговНДС      = НСтр("ru='НДС 0%'");
		КонецЕсли; 
		
	Иначе
		
		ЧастиЗаголовка = Новый Массив;
		Если СуммаВключаетНДС Тогда
			ЧастиЗаголовка.Добавить(НСтр("ru='В том числе НДС'"));
		Иначе
			ЧастиЗаголовка.Добавить(НСтр("ru='Сумма НДС'"));
		КонецЕсли; 
		
		УникальныеСтавкиНДС = ОбщегоНазначенияКлиентСервер.СвернутьМассив(СтавкиНДС);
		Если УникальныеСтавкиНДС.Количество() = 1 Тогда
			ЧастиЗаголовка.Добавить(УникальныеСтавкиНДС[0]);
		КонецЕсли; 
		
		ЗаголовокИтоговНДС = СтрШаблон("%1:", СтрСоединить(ЧастиЗаголовка, " "));
	КонецЕсли;
	
	Возврат ЗаголовокИтоговНДС;	
КонецФункции

Функция ПредставлениеОснованияПечати(ОснованиеПечатиСсылка) Экспорт
	
	Если НЕ ЗначениеЗаполнено(ОснованиеПечатиСсылка) Тогда
		
		Если ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.ЗаказПокупателя") Тогда
			
			Возврат ПараметрЭтотДокументЗаказПокупателя();
			
		ИначеЕсли ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.СчетНаОплату") Тогда
			
			Возврат ПараметрЭтотДокументСчетНаОплату();
			
		Иначе
			
			Возврат "";
			
		КонецЕсли;
		
	ИначеЕсли ТипЗнч(ОснованиеПечатиСсылка) = Тип("СправочникСсылка.ДоговорыКонтрагентов") Тогда
		
		ЗаголовокПредставления = "";
		Если Константы.ДобавитьСловоДоговорВПредставлениеДоговораОснованияПечати.Получить() Тогда
			
			ЗаголовокПредставления = НСтр("ru = 'Договор:'") + " ";
			
		КонецЕсли;
		
		Возврат ЗаголовокПредставления + Строка(ОснованиеПечатиСсылка.Наименование);
		
	ИначеЕсли ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.СчетНаОплату")
		ИЛИ ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.ЗаказПокупателя")
		ИЛИ ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.ЗаказПоставщику")
		ИЛИ ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.РасходнаяНакладная")
		ИЛИ ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.АктВыполненныхРабот")
		ИЛИ ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.ПриемИПередачаВРемонт")
		Тогда
		
		Основание = ДобавитьЗначокНомераКПредставлению(ОснованиеПечатиСсылка);
		Возврат Основание;
		
	КонецЕсли;
	
КонецФункции

Функция ПредставлениеНомераДатыОснованияПечати(ОснованиеПечатиСсылка) Экспорт
	
	ДанныеОснованияПечати = Новый Структура("Номер, Дата");
	
	Если ЗначениеЗаполнено(ОснованиеПечатиСсылка) Тогда
		
		Если ТипЗнч(ОснованиеПечатиСсылка) = Тип("СправочникСсылка.ДоговорыКонтрагентов") Тогда
			
			ДанныеОснованияПечати.Номер = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОснованиеПечатиСсылка, "НомерДоговора");
			ДанныеОснованияПечати.Дата = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОснованиеПечатиСсылка, "ДатаДоговора");
			
		ИначеЕсли ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.СчетНаОплату")
			ИЛИ ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.ЗаказПокупателя")
			ИЛИ ТипЗнч(ОснованиеПечатиСсылка) = Тип("ДокументСсылка.ЗаказПоставщику")
			Тогда
			
			ЗаполнитьЗначенияСвойств(ДанныеОснованияПечати, ОснованиеПечатиСсылка, "Номер, Дата");
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат ДанныеОснованияПечати;
	
КонецФункции

Функция ПредставлениеКодаНоменклатуры(ПараметрыНоменклатуры) Экспорт
	
	ПредставлениеКодаНоменклатуры = "";
	ИмяПоля = "";
	
	ВидПредставления = Константы.ПредставлениеКодовВПечатныхФормах.Получить();
	Если ВидПредставления = Перечисления.КодыНоменклатурыВДокументах.Код Тогда
		
		ИмяПоля = "Код";
		
	ИначеЕсли ВидПредставления = Перечисления.КодыНоменклатурыВДокументах.Артикул Тогда
		
		ИмяПоля = "Артикул";
		
	ИначеЕсли ВидПредставления = Перечисления.КодыНоменклатурыВДокументах.Штрихкод Тогда
		
		ИмяПоля = "ШтрихКод";
		
	КонецЕсли;
	
	Если ПустаяСтрока(ИмяПоля) Тогда
		
		// обработка не требуется
		
	ИначеЕсли ТипЗнч(ПараметрыНоменклатуры) = Тип("СтрокаТаблицыЗначений")
		ИЛИ ТипЗнч(ПараметрыНоменклатуры) = Тип("СтрокаДереваЗначений")
		ИЛИ ТипЗнч(ПараметрыНоменклатуры) = Тип("ВыборкаИзРезультатаЗапроса") Тогда
		
		Попытка
			ПредставлениеКодаНоменклатуры = ПараметрыНоменклатуры[ИмяПоля];
		Исключение
		КонецПопытки;
		
	ИначеЕсли ТипЗнч(ПараметрыНоменклатуры) = Тип("Структура") Тогда
		
		ПараметрыНоменклатуры.Свойство(ИмяПоля, ПредставлениеКодаНоменклатуры);
		
	КонецЕсли;
	
	Возврат ПредставлениеКодаНоменклатуры;
	
КонецФункции

Функция ПредставлениеСерииНоменклатурыПоКлючу(ТаблицаСерийНоменклатуры, КлючСвязи) Экспорт
	
	ПредставлениеСерииНоменклатуры = "";
	
	Если ТипЗнч(ТаблицаСерийНоменклатуры) = Тип("ТаблицаЗначений")
		И ТаблицаСерийНоменклатуры.Количество() > 0 Тогда
		
		МассивНайденныхСтрок = ТаблицаСерийНоменклатуры.НайтиСтроки(Новый Структура("КлючСвязи", КлючСвязи));
		Для каждого СтрокаМассива Из МассивНайденныхСтрок Цикл
			
			ПредставлениеСерииНоменклатуры = ПредставлениеСерииНоменклатуры + ?(ПустаяСтрока(ПредставлениеСерииНоменклатуры), "", ", ") + СокрЛП(СтрокаМассива.Серия);
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат ПредставлениеСерииНоменклатуры;
	
КонецФункции

Функция ПредставлениеСкидки(СтрокаТабличнойЧасти, СтруктураИтогов) Экспорт
	
	СкидкаСумма = 0;
	СкидкаПроцент = 0;
	
	ТочностьОкругленияСкидки = 2; // Знаков после запятой
	
	Если СтрокаТабличнойЧасти.ПроцентСкидкиНаценки = 100 Тогда
		
		СкидкаСумма = СтрокаТабличнойЧасти.Цена * СтрокаТабличнойЧасти.Количество;
		СкидкаПроцент = НСтр("ru ='100%'");
		
	ИначеЕсли СтрокаТабличнойЧасти.ПроцентСкидкиНаценки = 0 
		И СтрокаТабличнойЧасти.СуммаАвтоматическойСкидки = 0 Тогда
		
		СкидкаСумма = 0;
		СкидкаПроцент = НСтр("ru ='-'");
		
	Иначе
		
		СкидкаСумма = (СтрокаТабличнойЧасти.Количество * СтрокаТабличнойЧасти.Цена) - СтрокаТабличнойЧасти.Сумма;
		
		Если СкидкаСумма > 0 Тогда
			
			Знаменатель = (СтрокаТабличнойЧасти.Количество * СтрокаТабличнойЧасти.Цена);
			Если Знаменатель = 0 Тогда
				
				СкидкаПроцент = 0;
				
			Иначе
				
				СкидкаПроцент = (СтрокаТабличнойЧасти.Сумма * 100) / Знаменатель;
				СкидкаПроцент = Строка(100 - Окр(СкидкаПроцент, ТочностьОкругленияСкидки)) + "%";
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если СтруктураИтогов.Свойство("СкидкаПоСтроке") Тогда
		
		СтруктураИтогов.СкидкаПоСтроке = СкидкаСумма;
		
	КонецЕсли;
	
	Возврат ?(СтруктураИтогов.ПредставлениеСкидки = Перечисления.СпособОтображенияСкидок.Процент, СкидкаПроцент, Формат(СкидкаСумма, "ЧДЦ=2; ЧГ="));
	
КонецФункции

Функция ПредставлениеКомплектаДокументов() Экспорт
	
	Возврат НСтр("ru = 'Настраиваемый комплект документов'");
	
КонецФункции

Функция ПараметрЭтотДокументЗаказПокупателя() Экспорт
	
	Возврат НСтр("ru ='Этот документ (Заказ покупателя)'");
	
КонецФункции

Функция ПараметрЭтотДокументСчетНаОплату() Экспорт
	
	Возврат НСтр("ru ='Этот документ (Счет на оплату)'");
	
КонецФункции

// Представление номенклатуры для печати
//
// Параметры:
//  ПредставлениеНоменклатуры - Строка, СправочникСсылка.Номенклатура - .
//  ПредставлениеХарактеристики - Строка, СправочникСсылка.ХарактеристикиНоменклатуры - .
//  ПредставлениеСерииНоменклатуры - Строка, СправочникСсылка.ПартииНоменклатуры - .
//  ПредставлениеПартии - Строка, СправочникСсылка.ПартииНоменклатуры - .
//  ПредставлениеКодаТНВЭД - Строка, СправочникСсылка.КлассификаторТНВЭД - .
//  НеобходимоВыделитьКакСоставНабора - Булево
// 
// Возвращаемое значение:
//  Строка - представление номенклатуры в печатной форме.
Функция ПредставлениеНоменклатурыДляПечати(ПредставлениеНоменклатуры, ПредставлениеХарактеристики = "",
	ПредставлениеСерииНоменклатуры = "", ПредставлениеПартии = "", ПредставлениеКодаТНВЭД = "",
	НеобходимоВыделитьКакСоставНабора = Ложь) Экспорт
	
	ПараметрыСтроки = Новый Структура;
	ПараметрыСтроки.Вставить("ПредставлениеНоменклатуры", ПредставлениеНоменклатуры);
	ПараметрыСтроки.Вставить("ПредставлениеХарактеристики", ПредставлениеХарактеристики);
	ПараметрыСтроки.Вставить("ПредставлениеСерииНоменклатуры", ПредставлениеСерииНоменклатуры);
	ПараметрыСтроки.Вставить("ПредставлениеПартии", ПредставлениеПартии);
	ПараметрыСтроки.Вставить("ПредставлениеКодаТНВЭД", ПредставлениеКодаТНВЭД);
	
	// Шаблон представления:
	// [ПредставлениеНоменклатуры] ([ПредставлениеХарактеристики], [ПредставлениеСерииНоменклатуры]), [ПредставлениеПартии],
	// {код ТН ВЭД [ПредставлениеКодаТНВЭД]};
	
	Если ПустаяСтрока(ПредставлениеХарактеристики)
		И ПустаяСтрока(ПредставлениеСерииНоменклатуры) Тогда
		
		ШаблонПредставления = "[ПредставлениеНоменклатуры]";
		
	ИначеЕсли НЕ ПустаяСтрока(ПредставлениеХарактеристики)
		И НЕ ПустаяСтрока(ПредставлениеСерииНоменклатуры) Тогда
		
		ШаблонПредставления = "[ПредставлениеНоменклатуры] ([ПредставлениеХарактеристики], [ПредставлениеСерииНоменклатуры])";
		
	ИначеЕсли ПустаяСтрока(ПредставлениеСерииНоменклатуры) Тогда 
		
		ШаблонПредставления = "[ПредставлениеНоменклатуры] ([ПредставлениеХарактеристики])";
		
	Иначе
		
		ШаблонПредставления = "[ПредставлениеНоменклатуры] ([ПредставлениеСерииНоменклатуры])";
		
	КонецЕсли;
	
	Если НЕ ПустаяСтрока(ПредставлениеПартии) Тогда
		
		ШаблонПредставления = ШаблонПредставления + ", [ПредставлениеПартии]";
		
	КонецЕсли;
	
	Если НЕ ПустаяСтрока(ПредставлениеКодаТНВЭД) Тогда
		
		ШаблонПредставления = ШаблонПредставления + НСтр("ru=', код ТН ВЭД'") + " " + " [ПредставлениеКодаТНВЭД]";
		
	КонецЕсли;
	
	Если НеобходимоВыделитьКакСоставНабора Тогда
		
		ШаблонПредставления = СтрШаблон("    • %1", ШаблонПредставления);
		
	КонецЕсли;
	
	Возврат СтроковыеФункцииКлиентСервер.ВставитьПараметрыВСтроку(ШаблонПредставления, ПараметрыСтроки);
	
КонецФункции

Функция ПолучитьИсходныйДокументКорректировки(ДокументСсылка) Экспорт
	
	Если НЕ ЗначениеЗаполнено(ДокументСсылка) Тогда
		
		Возврат Неопределено;
		
	ИначеЕсли ТипЗнч(ДокументСсылка) = Тип("ДокументСсылка.КорректировкаРеализации") Тогда
		
		Возврат ПолучитьИсходныйДокументКорректировки(ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ДокументСсылка, "ДокументОснование"));
		
	Иначе
		
		Возврат ДокументСсылка;
		
	КонецЕсли;
	
КонецФункции

Функция ПолучитьОбластьБезопасно(Макет, ИмяОбласти, ПредставлениеОбласти, Ошибки) Экспорт
	Перем ОбластьМакета;
	
	Если Макет.Области.Найти(ИмяОбласти) = Неопределено Тогда
		
		ТекстСообщения = НСтр("ru ='ВНИМАНИЕ! Не обнаружена область макета %1. Возможно используется пользовательский макет.'");
		ТекстСообщения = СтрШаблон(ТекстСообщения, ?(ПустаяСтрока(ПредставлениеОбласти), ИмяОбласти, ПредставлениеОбласти));
		
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(Ошибки, , ТекстСообщения, Неопределено);
		
	Иначе
		
		ОбластьМакета = Макет.ПолучитьОбласть(ИмяОбласти);
		
	КонецЕсли;
	
	Возврат ОбластьМакета;
	
КонецФункции

Процедура УстановитьПараметрБезопасно(Область, ИмяПараметра, Значение) Экспорт
	
	ЗначенияПараметров = Новый Структура;
	ЗначенияПараметров.Вставить(ИмяПараметра, Значение);
	Область.Параметры.Заполнить(ЗначенияПараметров);
	
КонецПроцедуры

// Установить картинку безопасно 
//
// Параметры:
//  Рисунки			 - 	КоллекцияРисунковТабличногоДокумента - рисунки табличного документа
//  ИмяРисунка		 - 	Строка - имя рисунка
//  ДанныеКартинки	 - 	Картинка - картинка для установки
//
Процедура УстановитьКартинкуБезопасно(Рисунки, ИмяРисунка, Картинка) Экспорт
	
	РисунокНайден = Ложь;
	
	Для Каждого Рисунок Из Рисунки Цикл
		
		РисунокНайден = Рисунок.Имя = ИмяРисунка;
		
		Если РисунокНайден Тогда
			Прервать;
		КонецЕсли; 
		
	КонецЦикла;
	
	Если НЕ РисунокНайден Тогда 
		Возврат;
	КонецЕсли;
	
	Рисунки[ИмяРисунка].Картинка = Картинка;
	
КонецПроцедуры

// Получить плашку подписи безопасно
//
// Параметры:
//  ОбластьМакета - ТабличныйДокумент - .
//  ИмяПлашки - Строка - .
//  ПредставлениеПодписи - Строка - .
//  Ошибки - Массив - .
// 
// Возвращаемое значение:
//  ОбластьЯчеекТабличногоДокумента - область ячеек с плашкой подписи.
//
Функция ПолучитьПлашкуПодписиБезопасно(ОбластьМакета, ИмяПлашки, ПредставлениеПодписи, Ошибки) Экспорт
	
	ПлашкаПодписи = ОбластьМакета.Области.Найти(ИмяПлашки);
	Если ПлашкаПодписи = Неопределено Тогда
		
		ТекстСообщения = НСтр("ru ='ВНИМАНИЕ: нет места для подписи %1. Возможно используется пользовательский макет.'");
		ТекстСообщения = СтрШаблон(ТекстСообщения, ?(ПустаяСтрока(ПредставлениеПодписи), ИмяПлашки, ПредставлениеПодписи));
		
		ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(Ошибки, , ТекстСообщения, Неопределено);
		
	Иначе
		
		ПлашкаПодписи.Линия = Новый Линия(ТипЛинииРисункаТабличногоДокумента.НетЛинии);
		
	КонецЕсли;
	
	Возврат ПлашкаПодписи;
	
КонецФункции

Функция ПолучитьНомерНаПечатьСУчетомДатыДокумента(ДатаДокумента, НомерДокумента, Префикс) Экспорт
	
	Если ДатаДокумента < Дата('20110101') Тогда
		
		Возврат НомерНаПечать(НомерДокумента, Префикс);
		
	Иначе
		
		БезПрефиксаИнформационнойБазы = Константы.ПредставлениеНомераДокументаБезПрефиксаИнформационнойБазы.Получить();
		БезПрефиксаПользователя = Константы.ПредставлениеНомераДокументаБезПрефиксаПользователя.Получить();
		
		Возврат ПрефиксацияОбъектовКлиентСервер.НомерНаПечать(НомерДокумента, БезПрефиксаИнформационнойБазы, БезПрефиксаПользователя);
		
	КонецЕсли;
	
КонецФункции

Процедура КорректировкаРазмещениеПодчиненнойГруппыКомандПечати(Форма, ЭлементФормы_ПодменюПечать, ЭлементФормы_ГруппаКомандУНФ) Экспорт
	
	ИмяГруппыФормыПоУмолчанию = "ПодменюПечатьОбычное";
	
	ГруппаФормыСКомандамиПечати = Форма.Элементы.Найти(ИмяГруппыФормыПоУмолчанию);
	Если ГруппаФормыСКомандамиПечати = Неопределено Тогда
		
		ГруппаФормыСКомандамиПечати = ЭлементФормы_ПодменюПечать;
		
	КонецЕсли;
	
	Форма.Элементы.Переместить(ЭлементФормы_ГруппаКомандУНФ, ГруппаФормыСКомандамиПечати);
	
КонецПроцедуры

Функция ШаблонТекстаПредупрежденияПравкиМакета() Экспорт
	
	Результат = НСтр("ru = 'Внимание: возможно используется пользовательский макет.
	|Штатный механизм печати счетов может работать некорректно.'");
	
	Возврат Результат;
	
КонецФункции

Функция ИтоговаяСтрока(КоличествоПозиций, СуммаДокумента, ВалютаДокумента) Экспорт
	
	Шаблон = НСтр("ru='Всего наименований %1, на сумму %2'");
	Сумма = ФорматСумм(СуммаДокумента, ВалютаДокумента);
	Результат = СтрШаблон(Шаблон, КоличествоПозиций, Сумма);
	Возврат Результат;
	
КонецФункции

Функция ДокументФормируетсяСогласноФЗ56(Дата) Экспорт
	
	// Используется при формирование
	// - СФ
	// - УПД
	// - Корректировочный СФ
	
	НачалоПримененияФЗ56 = '20170701';
	ПрименяетсяПостановление981 = (Дата >= Константы.НачалоИспользованияСФ981.Получить());
	
	Возврат (Дата >= НачалоПримененияФЗ56) И НЕ ПрименяетсяПостановление981;
	
КонецФункции

// Возвращает версию постановления Правительства РФ от 26.12.2011 г. № 1137
//
// Параметры
//  Период  -  тип дата, в данном параметре передается
//             дата на которую необходимо определить версию постановления
// Возвращаемое значение:
//  Строка   -  версия постановления,
//              "1137"    (БП = 1) - исходная версия постановления Правительства РФ от 26.12.2011 г. № 1137
//              "952"     (БП = 2) - постановление Правительства РФ от 26.12.2011 г. № 1137 в редакции постановления № 952
//              "735"     (БП = 3) - постановление Правительства РФ от 26.12.2011 г. № 1137 в редакции постановления № 735
//              "981"     (БП = 4) - постановление Правительства РФ от 26.12.2011 г. № 1137 в редакции постановления № 981
//              "534"  	  (БП = 5) - постановление Правительства РФ от 26.12.2011 г. № 1137 в редакции постановления № 534
//              "2023Кв4" (БП = 6) - постановление Правительства РФ от 26.12.2011 г. № 1137 в редакции постановления № 534
//              "1096"    (БП = 7) - постановление Правительства РФ от 26.12.2011 г. № 1137 в редакции постановления № 1096
//
Функция ВерсияПостановленияНДС1137(Период, ЕстьПрослеживаемыеТовары = Ложь) Экспорт
	
	Если Период >= '20241001' Тогда			// Постановление № 1096 вступает в силу с 1 октября 2024 года.
		Возврат "1096";                     // в БП = 7
	ИначеЕсли Период >= '20231001' 
		И ЕстьПрослеживаемыеТовары Тогда    // ФЗ № 389 вступает в силу с 1 октября 2023 года.
		Возврат "2023Кв4";                  //  в БП = 6
    ИначеЕсли Период >= '20210701' Тогда	// Постановление № 534 вступает в силу с 1 июля 2021 года.
		Возврат "534"; 						//  в БП = 5
	ИначеЕсли Период >= '20171001' Тогда	// Постановление № 981 вступает в силу с 1 октября 2017 года.
		Возврат "981"; 						//  в БП = 4
	ИначеЕсли Период >= '20141001' Тогда	// Постановление № 735 вступает в силу с 1 октября 2014 года.
		Возврат "735";						//  в БП = 3
	ИначеЕсли Период >= '20131106' Тогда	// Постановление № 952 вступает в силу с 6 ноября 2013 года.
		Возврат "952";						//  в БП = 2
	Иначе									// Исходная версия Постановления Правительства РФ от 26.12.2011 г. № 1137.
		Возврат "1137";						//  в БП = 1
	КонецЕсли;
	
КонецФункции // ВерсияПостановленияНДС1137()

Функция ПроверитьВыводТабличногоДокумента(ТабличныйДокумент, ВыводимыеОбласти, Ошибки) Экспорт
	
	Попытка
		
		Возврат ТабличныйДокумент.ПроверитьВывод(ВыводимыеОбласти);
		
	Исключение
		
		ОписаниеОшибки = ИнформацияОбОшибке();
		ТекстОшибки = НСтр("ru = 'Невозможно получить информацию о текущем принтере.
		|Возможно, в системе не установлено ни одного принтера.'");
		
		ЗаписьЖурналаРегистрации(ТекстОшибки, УровеньЖурналаРегистрации.Ошибка , , , ОписаниеОшибки.Описание);
		
	КонецПопытки;

КонецФункции

Процедура ОбработкаЗаполненияОснованияПечати(ЭтотОбъект) Экспорт
	
	СоответствиеТиповКонстант = Новый Соответствие;
	СоответствиеТиповКонстант.Вставить(Тип("ДокументСсылка.ЗаказПокупателя"), "ОснованиеПечатиЗаказПокупателя");
	СоответствиеТиповКонстант.Вставить(Тип("ДокументСсылка.СчетНаОплату"), "ОснованиеПечатиСчетНаОплату");
	
	ИмяКонстанты = СоответствиеТиповКонстант.Получить(ТипЗнч(ЭтотОбъект.Ссылка));
	
	ЗначениеКонстанты = Константы[ИмяКонстанты].Получить();
	Если ЗначениеКонстанты = Перечисления.ОснованиеПечатиСпособНачальногоЗаполнение.ТекущийДокумент Тогда
		
		Если ИмяКонстанты = "ОснованиеПечатиЗаказПокупателя" Тогда
			
			ЭтотОбъект.ОснованиеПечатиСсылка = Документы.ЗаказПокупателя.ПустаяСсылка();
			ЭтотОбъект.ОснованиеПечати = ПараметрЭтотДокументЗаказПокупателя();
			
		ИначеЕсли ИмяКонстанты = "ОснованиеПечатиСчетНаОплату" Тогда
			
			ЭтотОбъект.ОснованиеПечатиСсылка = Документы.СчетНаОплату.ПустаяСсылка();
			ЭтотОбъект.ОснованиеПечати = ПараметрЭтотДокументСчетНаОплату();
			
		КонецЕсли;
		
	ИначеЕсли ЗначениеКонстанты = Перечисления.ОснованиеПечатиСпособНачальногоЗаполнение.ДоговорКонтрагента Тогда
		
		Если ЗначениеЗаполнено(ЭтотОбъект.Договор) Тогда
			
			ЭтотОбъект.ОснованиеПечатиСсылка = ЭтотОбъект.Договор;
			ЭтотОбъект.ОснованиеПечати = ПредставлениеОснованияПечати(ЭтотОбъект.ОснованиеПечатиСсылка);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

// Функция преобразует строку к множественному числу
// Предназначена для формирования "правильной" подписи к числительным
//
// Параметры: 
//  Слово1 - форма слова в ед числе      ("шкаф")
//  Слово2 - форма слова для числит 2-4  ("шкафа")
//  Слово3 - форма слова для числит 5-10 ("шкафов")
//  ЦелоеЧисло - целое число
//
// Возвращаемое значение:
//  Строка - одну из строк в зависимости от параметра ЦелоеЧисло
//
Функция ФормаМножественногоЧисла(Слово1, Слово2, Слово3, Знач ЦелоеЧисло) Экспорт
	
	// Изменим знак целого числа, иначе отрицательные числа будут неправильно преобразовываться
	Если ЦелоеЧисло < 0 Тогда
		ЦелоеЧисло = -1 * ЦелоеЧисло;
	КонецЕсли;
	
	Если ЦелоеЧисло <> Цел(ЦелоеЧисло) Тогда 
		// для нецелых чисел - всегда вторая форма
		Возврат Слово2;
	КонецЕсли;
	
	// остаток
	Остаток = ЦелоеЧисло%10;
	Если (ЦелоеЧисло >10) И (ЦелоеЧисло<20) Тогда
		// для второго десятка - всегда третья форма
		Возврат Слово3;
	ИначеЕсли Остаток=1 Тогда
		Возврат Слово1;
	ИначеЕсли (Остаток>1) И (Остаток<5) Тогда
		Возврат Слово2;
	Иначе
		Возврат Слово3;
	КонецЕсли;

КонецФункции

// Процедура переопределяет отображение подменю "Печать" в формах
//
// Параметры:
//  ГруппаФормыПечать	 - ГруппаФормы	 - элемент формы, содержащий команды печати
//
Процедура УстановитьОтображениеПодменюПечати(ГруппаФормыПечать) Экспорт
	
	ГруппаФормыПечать.Вид			= ВидГруппыФормы.Подменю;
	ГруппаФормыПечать.Отображение	= ОтображениеКнопки.Картинка;
	ГруппаФормыПечать.Картинка		= БиблиотекаКартинок.Печать;
	ГруппаФормыПечать.Подсказка		= НСтр("ru='Печать с предварительным просмотром'");
	ГруппаФормыПечать.Заголовок		= НСтр("ru='Печать'");
	
КонецПроцедуры

// Функция проверяет возможность выполнения команды печати в серверном контексте
// Параметры:
//  ОписаниеКомандыПечати - СтрокаТаблицыЗначений - подробности см. УправлениеПечатью.СоздатьКоллекциюКомандПечати
//                           а также см. ШаблоныСообщенийСлужебный неэкспортный метод ОпределитьСписокПечатныхФорм
//  КолонкаИдентификаторКоманды - Строка - идентификатор команды.
// Возвращаемое значение:
//  Булево - истина означает, что для команды печати определен вывод в табличный документ с использованием процедуры
//           Печать() в модуле менеджера печати.
Функция КомандаПечатаетсяВСерверномКонтексте(знач ОписаниеКомандыПечати, КолонкаИдентификаторКоманды = Неопределено) Экспорт
	
	Если ОписаниеКомандыПечати.Представление = НСтр("ru='Настраиваемый комплект документов'") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	Если КолонкаИдентификаторКоманды = Неопределено Тогда
		КолонкаИдентификаторКоманды = "Идентификатор";
	КонецЕсли;
	
	КомандыИсключения = Новый Массив;
	КомандыИсключения.Добавить("Конверт");
	КомандыИсключения.Добавить("КонтактнаяИнформация");
	КомандыИсключения.Добавить("ПечатьЭтикетокИзОприходованиеЗапасов");
	КомандыИсключения.Добавить("ПечатьЦенниковИзОприходованиеЗапасов");
	КомандыИсключения.Добавить("ПечатьЭтикетокИзПеремещенияТоваров");
	КомандыИсключения.Добавить("ПечатьЦенниковИзПеремещенияТоваров");
	КомандыИсключения.Добавить("ПечатьЭтикетокИзПриходнойНакладной");
	КомандыИсключения.Добавить("ПечатьЦенниковИзПриходнойНакладной");
	КомандыИсключения.Добавить("ПечатьЭтикетокИзРасходнойНакладной");
	КомандыИсключения.Добавить("ПечатьЦенниковИзРасходнойНакладной");
	КомандыИсключения.Добавить("ПечатьЭтикетокИзЗаказаПокупателя");
	КомандыИсключения.Добавить("ПечатьЦенниковИзЗаказаПокупателя");  
	КомандыИсключения.Добавить("ПечатьЦенниковИзУстановкиЦенНоменклатуры");
	КомандыИсключения.Добавить("УниверсальныйПередаточныйДокумент");
	КомандыИсключения.Добавить("УниверсальныйПередаточныйДокументФаксимиле");
	КомандыИсключения.Добавить("УниверсальныйКорректировочныйДокумент");
	КомандыИсключения.Добавить("ПечатьЭтикетокИзСборкиЗапасов");
	КомандыИсключения.Добавить("ТН");
	
	Возврат КомандыИсключения.Найти(ОписаниеКомандыПечати[КолонкаИдентификаторКоманды]) = Неопределено;
	
КонецФункции

// Возвращает номер документа для печати.
//
// Параметры:
//  НомерДокумента - Строка - номер документа,
//  Префикс - Строка - префикс, который будет удален из номера документа.
// Возвращаемое значение:
//  Строка - номер документа для печати.
Функция НомерНаПечать(НомерДокумента, Префикс) Экспорт

	Если Не ЗначениеЗаполнено(НомерДокумента) Тогда
		Возврат 0;
	КонецЕсли;

	Номер = СокрЛП(НомерДокумента);
	
	// удаление префикса из номера документа
	Если СтрНайти(Номер, Префикс) = 1 Тогда
		Номер = Сред(Номер, СтрДлина(Префикс) + 1);
	КонецЕсли;

	ПрефиксОбмен = "";

	Если ПолучитьФункциональнуюОпцию("ИспользоватьСинхронизациюДанных") И ЗначениеЗаполнено(
		Константы.ПрефиксУзлаРаспределеннойИнформационнойБазы.Получить()) Тогда
		ПрефиксОбмен = СокрЛП(Константы.ПрефиксУзлаРаспределеннойИнформационнойБазы.Получить());
	КонецЕсли;
	
	// удаление префикса из номера документа
	Если СтрНайти(Номер, ПрефиксОбмен) = 1 Тогда
		Номер = Сред(Номер, СтрДлина(ПрефиксОбмен) + 1);
	КонецЕсли;
	
	// так же, может остаться "минус" впереди
	Если Лев(Номер, 1) = "-" Тогда
		Номер = Сред(Номер, 2);
	КонецЕсли;
	
	// удаление ведущих нулей
	Пока Лев(Номер, 1) = "0" Цикл
		Номер = Сред(Номер, 2);
	КонецЦикла;

	Возврат Номер;

КонецФункции

// Возвращает представление организации.
//
// Параметры: 
//  СведенияОЮрФизЛице - см. СведенияОЮрФизЛице
//  ПоляПредставления - Строка - поля представления, перечисленные через запятую,
//  СПрефиксом - Булево - признак вывода префикса параметра организации.
//
// Возвращаемое значение:
//  Строка - представление организации для печати.
//
Функция ОписаниеОрганизации(СведенияОЮрФизЛице, ПоляПредставления = "", СПрефиксом = Истина) Экспорт

	Если ПустаяСтрока(ПоляПредставления) Тогда
		ПоляПредставления = "%1,ИНН,Свидетельство,ЮридическийАдрес,Телефоны,Факс,НомерСчета,Банк,БИК,КоррСчет";
		Если СведенияОЮрФизЛице.Свойство("ВидКонтрагента") И ЗначениеЗаполнено(СведенияОЮрФизЛице.ВидКонтрагента) Тогда
			ПоляПредставления = СтрЗаменить(ПоляПредставления, "%1", "ПолноеНаименование");
		Иначе
			ПоляПредставления = СтрЗаменить(ПоляПредставления, "%1", "НаименованиеДляПечати");
		КонецЕсли;
	КонецЕсли; 

	КомпонентыПредставления = Новый Массив;

	ПрефиксыПолей = Новый Соответствие();
	ПрефиксыПолей["ПолноеНаименование"] = " ";
	ПрефиксыПолей["СокращенноеНаименование"] = " ";
	ПрефиксыПолей["НаименованиеДляПечати"] = " ";
	ПрефиксыПолей["ИНН"] = СтрШаблон(" %1 ", НСтр("ru = 'ИНН'"));
	ПрефиксыПолей["КПП"] = СтрШаблон(" %1 ", НСтр("ru = 'КПП'"));
	ПрефиксыПолей["РегистрационныйНомер"] = " ";
	ПрефиксыПолей["Свидетельство"] = " ";
	ПрефиксыПолей["СвидетельствоДатаВыдачи"] = СтрШаблон(" %1 ", НСтр("ru = 'от'"));
	ПрефиксыПолей["ЮридическийАдрес"] = " ";
	ПрефиксыПолей["ПочтовыйАдрес"] = " ";
	ПрефиксыПолей["Телефоны"] = СтрШаблон(" %1 ", НСтр("ru = 'тел.:'"));
	ПрефиксыПолей["Факс"] = СтрШаблон(" %1 ", НСтр("ru = 'факс:'"));
	ПрефиксыПолей["НомерСчета"] = СтрШаблон(" %1 ", НСтр("ru = 'р/с'"));
	ПрефиксыПолей["Банк"] = СтрШаблон(" %1 ", НСтр("ru = 'в банке'"));
	ПрефиксыПолей["БИК"] = СтрШаблон(" %1 ", НСтр("ru = 'БИК'"));
	ПрефиксыПолей["КоррСчет"] = СтрШаблон(" %1 ", НСтр("ru = 'к/с'"));
	ПрефиксыПолей["КодПоОКПО"] = СтрШаблон(" %1 ", НСтр("ru = 'Код по ОКПО'"));
	ПрефиксыПолей["Грузополучатель"] = СтрШаблон(" %1 ", НСтр("ru = 'грузополучатель:'"));
	ПрефиксыПолей["АдресДоставки"] = СтрШаблон(" %1 ", НСтр("ru = 'доставка:'"));

	Для Каждого ТекПараметр Из СтрРазделить(ПоляПредставления, ", ", Ложь) Цикл

		Попытка
			ТекПредставление = "";
			СведенияОЮрФизЛице.Свойство(ТекПараметр, ТекПредставление);
			
			Если ТипЗнч(ТекПредставление) = Тип("СправочникСсылка.Банки") Тогда
				ТекПредставление = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ТекПредставление, "Наименование");
			КонецЕсли;
			
			Если ПустаяСтрока(ТекПредставление) Тогда
				Продолжить;
			КонецЕсли;
			
			Если СПрефиксом Тогда
				КомпонентыПредставления.Добавить(СтрШаблон("%1%2", ПрефиксыПолей[ТекПараметр], ТекПредставление));
			Иначе
				КомпонентыПредставления.Добавить(ТекПредставление)
			КонецЕсли;
			
		Исключение
			
			ТекстСообщения = СтрШаблон(НСтр("ru = 'Не удалось определить значение параметра организации: %1'"),
				ТекПараметр);
			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
			
		КонецПопытки;
			
	КонецЦикла;

	Возврат СтрСоединить(КомпонентыПредставления, ", ");

КонецФункции

// Возвращает сведения об указанном ЮрФизЛице.
// К сведениям относятся наименование, адрес, номер телефона, банковские реквизиты.
//
// Параметры: 
//  ЮрФизЛицо - СправочникСсылка.Организации - организация, о которой собираются сведения,
//            - СправочникСсылка.Контрагенты - контрагент, о котором собираются сведения,
//            - СправочникСсылка.СтруктурныеЕдиницы - структурная единица, о которой собираются сведения,
//  ДатаПериода - Дата - дата, на которую выбираются сведения о ЮрФизЛице,
//  ДляФизЛицаТолькоИнициалы - Булево - Для физ. лица выводить только инициалы имени и отчества,
//  БанковскийСчет - СправочникСсылка.БанковскиеСчета - банковский счет.
//
// Возвращаемое значение:
//  Структура - См. ПечатьДокументовУНФ.НовыеСведенияОЮрФизЛице
Функция СведенияОЮрФизЛице(ЮрФизЛицо, ДатаПериода, ДляФизЛицаТолькоИнициалы = Истина, БанковскийСчет = Неопределено) Экспорт
	
	Результат = НовыеСведенияОЮрФизЛице();
	
	Если Не ПодходящийТипДляСбораСведенийОЮрФизЛице(ЮрФизЛицо) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ЮрФизЛицо) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(БанковскийСчет) Тогда
		
		ТекБанковскийСчет = БанковскийСчет;
		
	Иначе
		
		ТекБанковскийСчет = Справочники.БанковскиеСчета.ПустаяСсылка();
		
		ДопустимыеТипы = Новый Массив;
		ДопустимыеТипы.Добавить(Тип("СправочникСсылка.Организации"));
		ДопустимыеТипы.Добавить(Тип("СправочникСсылка.Контрагенты"));
		
		Если ДопустимыеТипы.Найти(ТипЗнч(ЮрФизЛицо)) <> Неопределено Тогда
			
			ТекБанковскийСчет = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЮрФизЛицо, "БанковскийСчетПоУмолчанию",
				Истина);
			
		КонецЕсли;
		
	КонецЕсли;
	
	ЮрФизЛицоМетаданные = ЮрФизЛицо.Метаданные();
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ПарЮрФизЛицо", ЮрФизЛицо);
	Запрос.УстановитьПараметр("ПарБанковскийСчет", ТекБанковскийСчет);
	Запрос.Текст = НовыйТекстЗапросаСведенийОЮрФизЛице(Запрос, ЮрФизЛицоМетаданные.Имя, ТекБанковскийСчет);
	РезультатЗапроса = Запрос.Выполнить();
	Если РезультатЗапроса.Пустой() Тогда
		Возврат Результат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Выборка.Следующий();
	
	Результат.КодПоОКПО = Выборка.КодПоОКПО;
	СтрокаШаблона = НСтр("ru='Свидетельство %1 от %2'");
	Если ЗначениеЗаполнено(Выборка.СвидетельствоСерияНомер)
		Или ЗначениеЗаполнено(Выборка.СвидетельствоДатаВыдачи) Тогда
		Результат.Свидетельство = СтрШаблон(СтрокаШаблона, СокрЛП(Выборка.СвидетельствоСерияНомер),
			Формат(Выборка.СвидетельствоДатаВыдачи, "ДЛФ=D"));
	КонецЕсли;
	Результат.СвидетельствоСерияНомер = Выборка.СвидетельствоСерияНомер;
	Результат.СвидетельствоДатаВыдачи = Выборка.СвидетельствоДатаВыдачи;
	Результат.КодПоОКПО = Выборка.КодПоОКПО;
	Результат.КПП = КППЮрФизЛицаНаДату(ЮрФизЛицо, Результат.КПП, ДатаПериода);
	Результат.ПолноеНаименование = ПолноеНаименованиеЮрФизЛицаНаДату(ЮрФизЛицо,
		Выборка.ПолноеНаименование,
		ДатаПериода);
	
	ВидыКонтактнойИнформации = ВидыКонтактнойИнформации(ЮрФизЛицо);
		
	Результат.Представление = НаименованиеЮрФизЛицаНаДату(ЮрФизЛицо, Выборка.Наименование, ДатаПериода);
	
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Организации") Тогда
		Результат.НаименованиеДляПечати = НаименованиеДляПечатиЮрФизЛицаНаДату(ЮрФизЛицо, ДатаПериода);
	Иначе
		Результат.НаименованиеДляПечати = ПолноеНаименованиеЮрФизЛицаНаДату(ЮрФизЛицо, Выборка.Наименование, ДатаПериода);
	КонецЕсли;
	
	Результат.ИНН = Выборка.ИНН;
	Результат.Телефоны = КонтактнаяИнформация(ЮрФизЛицо, ВидыКонтактнойИнформации.Телефон, ДатаПериода);
	Результат.Факс = КонтактнаяИнформация(ЮрФизЛицо, ВидыКонтактнойИнформации.Факс, ДатаПериода);
	Результат.НомерСчета = Выборка.НомерСчета;
	Результат.Банк = Выборка.Банк;
	Результат.БИК = Выборка.БИК;
	Результат.АдресБанка = Выборка.АдресБанка;
	Результат.КоррСчет = Выборка.КоррСчет;
	Результат.ТекстКорреспондента = Выборка.ТекстКорреспондента;
	Результат.ЮридическийАдрес = КонтактнаяИнформация(ЮрФизЛицо, ВидыКонтактнойИнформации.ЮрАдрес, ДатаПериода);
	Результат.ФактическийАдрес = КонтактнаяИнформация(ЮрФизЛицо, ВидыКонтактнойИнформации.ФактАдрес, ДатаПериода);
	Результат.ПочтовыйАдрес = КонтактнаяИнформация(ЮрФизЛицо, ВидыКонтактнойИнформации.ПочтАдрес, ДатаПериода);
	Результат.ЭлектроннаяПочта = КонтактнаяИнформация(ЮрФизЛицо, ВидыКонтактнойИнформации.ЭлПочта, ДатаПериода);
	Результат.СтранаРегистрации = Выборка.СтранаРегистрации;
	Результат.ВидКонтрагента = Выборка.ВидКонтрагента;
	Если ЗначениеЗаполнено(Выборка.СтранаРегистрации) 
		И Выборка.СтранаРегистрации <> Справочники.СтраныМира.Россия Тогда
		Результат.РегистрационныйНомер = Выборка.РегистрационныйНомер;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Результат.ПолноеНаименование) Тогда
		Результат.ПолноеНаименование = Результат.Представление;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Результат.НаименованиеДляПечати) Тогда
		Результат.НаименованиеДляПечати = Результат.Представление;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Функция находит актуальное значение адреса в контактной информации.
//
// Параметры:
//  ОбъектКонтактнойИнформации - СправочникСсылка - объект контактной информации,
//  ВидКонтактнойИнформации - СправочникСсылка.ВидыКонтактнойИнформации - вид контактной информации,
//  ДатаПериода - Дата - Дата действия контактной информации.
//
// Возвращаемое значение:
//  Строка - представление найденного адреса.
Функция КонтактнаяИнформация(ОбъектКонтактнойИнформации, ВидКонтактнойИнформации, ДатаПериода = Неопределено) Экспорт
	
	ДатаПечатиКИ = ДатаПериода;
	
	Если Не ЗначениеЗаполнено(ВидКонтактнойИнформации) Тогда
		Возврат "";
	КонецЕсли;
	
	Если ДатаПечатиКИ = Неопределено ИЛИ НЕ ЗначениеЗаполнено(ДатаПечатиКИ) Тогда
		ДатаПечатиКИ = ТекущаяДатаСеанса();
	КонецЕсли;
	
	Возврат УправлениеКонтактнойИнформацией.КонтактнаяИнформацияОбъекта(ОбъектКонтактнойИнформации, 
		ВидКонтактнойИнформации,
		ДатаПечатиКИ,
		Истина);
	
КонецФункции

Процедура ДобавитьСуммовыеПоляЭквивалентовВНациональнойВалюте(ДанныеДокументов, ИспользуемыеТЧ) Экспорт
	
	НеобходимПересчет = ПолучитьФункциональнуюОпцию("УчетВалютныхОпераций");
	НациональнаяВалюта = Константы.НациональнаяВалюта.Получить();
	
	Для каждого ДанныеОбъекта Из ДанныеДокументов Цикл
		
		НеобходимПересчет = НеобходимПересчет И (НациональнаяВалюта <> ДанныеОбъекта.ВалютаДокумента);
		
		Для каждого ОписаниеТЧ Из ИспользуемыеТЧ Цикл
			
			ПараметрыЗапроса = Новый Структура;
			ПараметрыЗапроса.Вставить("Ссылка", ДанныеОбъекта.Ссылка);
			ПараметрыЗапроса.Вставить("КурсДокумента", ?(НеобходимПересчет, ДанныеОбъекта.Курс, 1));
			ПараметрыЗапроса.Вставить("КратностьДокумента", ?(НеобходимПересчет, ДанныеОбъекта.Кратность, 1));
			
			ДанныеОбъекта[ОписаниеТЧ.Ключ] = ДобавитьСуммовыеПоля(ПараметрыЗапроса, ДанныеОбъекта[ОписаниеТЧ.Ключ],
				ОписаниеТЧ.Значение);
			
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

// Возвращает представление количества прописью.
//
// Параметры:
//  Количество - Число - число, которое требуется форматировать.
//
// Возвращаемое значение:
//  Строка - представление количества прописью.
Функция КоличествоПрописью(Количество) Экспорт

	ЦелаяЧасть = Цел(Количество);
	ДробнаяЧасть = Окр(Количество - ЦелаяЧасть, 3);

	Если ДробнаяЧасть = Окр(ДробнаяЧасть,0) Тогда
		ПараметрыПрописи = ", , , , , , , , 0";
	ИначеЕсли ДробнаяЧасть = Окр(ДробнаяЧасть, 1) Тогда
		ПараметрыПрописи = НСтр("ru = 'целая, целых, целых, ж, десятая, десятых, десятых, м, 1'");
	ИначеЕсли ДробнаяЧасть = Окр(ДробнаяЧасть, 2) Тогда
		ПараметрыПрописи = НСтр("ru = 'целая, целых, целых, ж, сотая, сотых, сотых, м, 2'");
	Иначе
		ПараметрыПрописи = НСтр("ru = 'целая, целых, целых, ж, тысячная, тысячных, тысячных, м, 3'");
	КонецЕсли;

	Возврат ЧислоПрописью(Количество, ,ПараметрыПрописи);

КонецФункции

// Возвращает представление суммы прописью.
//
// Параметры: 
//  Сумма - Число - число, которое требуется форматировать 
//  Валюта - СправочникСсылка.Валюты - ссылка на элемент справочника валют, если задан, то в результирующую строку будет
//                                     добавлено представление валюты,
//  ЧН - Строка - строка, представляющая нулевое значение числа,
//  ЧРГ - Строка - символ-разделитель групп целой части числа.
//
// Возвращаемое значение:
//  Строка - Отформатированная должным образом строковое представление суммы.
Функция ФорматСумм(Сумма, Валюта = Неопределено, ЧН = "", ЧРГ = "") Экспорт

	КомпонентыФорматнойСтроки = Новый Массив;
	КомпонентыФорматнойСтроки.Добавить("ЧЦ=15");
	КомпонентыФорматнойСтроки.Добавить("ЧДЦ=2");
	
	Если ЗначениеЗаполнено(ЧН) Тогда
		КомпонентыФорматнойСтроки.Добавить(СтрШаблон("ЧН=%1", ЧН));
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ЧРГ) Тогда
		КомпонентыФорматнойСтроки.Добавить(СтрШаблон("ЧРГ=%1", ЧРГ));
	КонецЕсли;
	
	ФорматнаяСтрока = СтрСоединить(КомпонентыФорматнойСтроки, ";");
	
	РезультирующаяСтрока = СокрЛ(Формат(Сумма, ФорматнаяСтрока));
	
	Если ЗначениеЗаполнено(Валюта) Тогда
		РезультирующаяСтрока = СтрШаблон("%1 %2", РезультирующаяСтрока, СокрП(Валюта));
	КонецЕсли;
	
	Возврат РезультирующаяСтрока;
	
КонецФункции

// Функция формирует представление суммы прописью в указанной валюте.
//
// Параметры:
//  Сумма - Число - .
//  Валюта - СправочникСсылка.Валюты - .
// 
// Возвращаемое значение:
//  Строка - сумма прописью.
//
Функция СформироватьСуммуПрописью(Сумма, Валюта) Экспорт

	Если Валюта.ПараметрыПрописи = "" Тогда
		Возврат ФорматСумм(Сумма);
	Иначе
		Возврат ЧислоПрописью(Сумма, , Валюта.ПараметрыПрописи);
	КонецЕсли;

КонецФункции

// Форматирует сумму банковского платежного документа.
//
// Параметры:
//  Сумма - Число - реквизит, который надо отформатировать,
//  ВыводитьСуммуБезКопеек - Булево - флаг представления суммы без копеек.
//
// Возвращаемое значение:
//  Строка - отформатированная строка.
Функция ФорматироватьСуммуПлатежногоДокумента(Сумма, ВыводитьСуммуБезКопеек = Ложь) Экспорт
	
	Результат  = Сумма;
	ЦелаяЧасть = Цел(Сумма);
	
	Если Результат = ЦелаяЧасть Тогда
		Если ВыводитьСуммуБезКопеек Тогда
			Результат = Формат(Результат, "ЧДЦ=2; ЧРД='='; ЧГ=0");
			Результат = Лев(Результат, СтрНайти(Результат, "="));
		Иначе
			Результат = Формат(Результат, "ЧДЦ=2; ЧРД='-'; ЧГ=0");
		КонецЕсли;
	Иначе
		Результат = Формат(Результат, "ЧДЦ=2; ЧРД='-'; ЧГ=0");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Форматирует сумму прописью банковского платежного документа.
//
// Параметры:
//  Сумма - Число - реквизит, который надо представить прописью, 
//  Валюта - СправочникСсылка.Валюты - валюта, в которой нужно представить сумму,
//  ВыводитьСуммуБезКопеек - Булево - флаг представления суммы без копеек.
//
// Возвращаемое значение:
//  Строка - отформатированная строка.
Функция ФорматироватьСуммуПрописьюПлатежногоДокумента(Сумма, Валюта, ВыводитьСуммуБезКопеек = Ложь) Экспорт
	
	Результат     = Сумма;
	ЦелаяЧасть    = Цел(Сумма);
	ФорматСтрока  = "ДП=Ложь";
	ПарамПредмета = Валюта.ПараметрыПрописи;
	
	Если Результат = ЦелаяЧасть Тогда
		Если ВыводитьСуммуБезКопеек Тогда
			Результат = ЧислоПрописью(Результат, ФорматСтрока, ПарамПредмета);
			Результат = Лев(Результат, СтрНайти(Результат, "0") - 1);
		Иначе
			Результат = ЧислоПрописью(Результат, ФорматСтрока, ПарамПредмета);
		КонецЕсли;
	Иначе
		Результат = ЧислоПрописью(Результат, ФорматСтрока, ПарамПредмета);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Проверяет возможность печати универсального корректировочного документа.
// 
// Параметры:
// 	ПараметрКоманды - Массив - печатаемые документы
// 	Ошибки - Массив - массив ошибок
Процедура ПроверитьВозможностьПечатиУКД(ПараметрКоманды, Ошибки) Экспорт
	
	Счетчик = 0;
	
	Пока Счетчик <= ПараметрКоманды.Количество()-1 Цикл
		
		ДокументСсылка = ПараметрКоманды[Счетчик];
		
		Если ДокументСсылка.Дата < Дата('20130101') Тогда 
			
			ТекстСообщения = НСтр("ru = '__________________
										|Печать универсального корректировочного документа доступна c 1 января 2013. 
										|Для документа %1 печатная форма не сформирована.'");
			
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстСообщения, ДокументСсылка);
			ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(Ошибки, , ТекстСообщения, Неопределено);
			
			ПараметрКоманды.Удалить(Счетчик);
			
		ИначеЕсли НЕ ЗначениеЗаполнено(ДокументСсылка.Контрагент) Тогда 
			
			ТекстСообщения = НСтр("ru = '__________________
										|Для печати универсального корректировочного документа необходимо заполнить контрагента. 
										|Для документа %1 печатная форма не сформирована.'");
			
			ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстСообщения, ДокументСсылка);
			ОбщегоНазначенияКлиентСервер.ДобавитьОшибкуПользователю(Ошибки, , ТекстСообщения, Неопределено);
			
			ПараметрКоманды.Удалить(Счетчик);
			
		Иначе
			
			Счетчик = Счетчик + 1;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

// Функция подготовки данных для печати этикетов и ценников.
//
// Параметры:
//  МассивДокументов - Массив - .
//  ЭтоЦенники		 - Булево - .
// 
// Возвращаемое значение:
//   Строка - адрес структуры данных во временном хранилище.
//
Функция ПодготовитьСтруктуруДанныхПечатиЭтикетокИЦенниковИзДокументов(МассивДокументов, ЭтоЦенники, КомпоновщикНастроек = Неопределено) Экспорт
	
	// АПК:96 -выкл результат должен содержать уникальные значения
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	АвансовыйОтчетЗапасы.Номенклатура КАК Номенклатура,
	|	АвансовыйОтчетЗапасы.Характеристика КАК Характеристика,
	|	АвансовыйОтчетЗапасы.Партия КАК Партия,
	|	NULL КАК Серия,
	|	АвансовыйОтчетЗапасы.ЕдиницаИзмерения КАК ЕдиницаИзмерения,
	|	NULL КАК Количество,
	|	АвансовыйОтчетЗапасы.Ссылка.Организация КАК Организация
	|ИЗ
	|	Документ.АвансовыйОтчет.Запасы КАК АвансовыйОтчетЗапасы
	|ГДЕ
	|	АвансовыйОтчетЗапасы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	АвансовыйОтчетЗапасы.Номенклатура,
	|	АвансовыйОтчетЗапасы.Характеристика,
	|	АвансовыйОтчетЗапасы.Партия,
	|	АвансовыйОтчетЗапасы.ЕдиницаИзмерения,
	|	АвансовыйОтчетЗапасы.Ссылка.Организация
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВводНачальныхОстатковЗапасы.Номенклатура,
	|	ВводНачальныхОстатковЗапасы.Характеристика,
	|	ВводНачальныхОстатковЗапасы.Партия,
	|	ВводНачальныхОстатковСерииНоменклатуры.Серия,
	|	ВводНачальныхОстатковЗапасы.ЕдиницаИзмерения,
	|	СУММА(ЕСТЬNULL(ВводНачальныхОстатковСерииНоменклатуры.Количество, ВводНачальныхОстатковЗапасы.Количество)),
	|	ВводНачальныхОстатковЗапасы.Ссылка.Организация
	|ИЗ
	|	Документ.ВводНачальныхОстатков.Запасы КАК ВводНачальныхОстатковЗапасы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ВводНачальныхОстатков.СерииНоменклатуры КАК ВводНачальныхОстатковСерииНоменклатуры
	|		ПО (ВводНачальныхОстатковСерииНоменклатуры.Ссылка В (&МассивДокументов))
	|			И ВводНачальныхОстатковЗапасы.КлючСвязи = ВводНачальныхОстатковСерииНоменклатуры.КлючСвязи
	|ГДЕ
	|	ВводНачальныхОстатковЗапасы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ВводНачальныхОстатковЗапасы.Номенклатура,
	|	ВводНачальныхОстатковЗапасы.Характеристика,
	|	ВводНачальныхОстатковЗапасы.Партия,
	|	ВводНачальныхОстатковЗапасы.ЕдиницаИзмерения,
	|	ВводНачальныхОстатковЗапасы.Ссылка.Организация,
	|	ВводНачальныхОстатковСерииНоменклатуры.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	КомплектацияЗапасов.Номенклатура,
	|	КомплектацияЗапасов.Характеристика,
	|	КомплектацияЗапасов.Партия,
	|	КомплектацияЗапасовСерииНоменклатуры.Серия,
	|	КомплектацияЗапасов.ЕдиницаИзмерения,
	|	СУММА(ЕСТЬNULL(КомплектацияЗапасовСерииНоменклатуры.Количество, КомплектацияЗапасов.Количество)),
	|	КомплектацияЗапасов.Организация
	|ИЗ
	|	Документ.КомплектацияЗапасов КАК КомплектацияЗапасов
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.КомплектацияЗапасов.СерииНоменклатуры КАК КомплектацияЗапасовСерииНоменклатуры
	|		ПО (КомплектацияЗапасовСерииНоменклатуры.Ссылка В (&МассивДокументов))
	|ГДЕ
	|	КомплектацияЗапасов.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	КомплектацияЗапасов.Номенклатура,
	|	КомплектацияЗапасов.Характеристика,
	|	КомплектацияЗапасов.Партия,
	|	КомплектацияЗапасов.ЕдиницаИзмерения,
	|	КомплектацияЗапасов.Организация,
	|	КомплектацияЗапасовСерииНоменклатуры.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	КорректировкаПоступленияЗапасы.Номенклатура,
	|	КорректировкаПоступленияЗапасы.Характеристика,
	|	КорректировкаПоступленияЗапасы.Партия,
	|	КорректировкаПоступленияСерииНоменклатуры.Серия,
	|	КорректировкаПоступленияЗапасы.ЕдиницаИзмерения,
	|	СУММА(ЕСТЬNULL(КорректировкаПоступленияСерииНоменклатуры.Количество, КорректировкаПоступленияЗапасы.Количество)),
	|	КорректировкаПоступленияЗапасы.Ссылка.Организация
	|ИЗ
	|	Документ.КорректировкаПоступления.Запасы КАК КорректировкаПоступленияЗапасы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.КорректировкаПоступления.СерииНоменклатуры КАК КорректировкаПоступленияСерииНоменклатуры
	|		ПО (КорректировкаПоступленияСерииНоменклатуры.Ссылка В (&МассивДокументов))
	|			И КорректировкаПоступленияЗапасы.КлючСвязи = КорректировкаПоступленияСерииНоменклатуры.КлючСвязи
	|ГДЕ
	|	КорректировкаПоступленияЗапасы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	КорректировкаПоступленияЗапасы.Номенклатура,
	|	КорректировкаПоступленияЗапасы.Характеристика,
	|	КорректировкаПоступленияЗапасы.Партия,
	|	КорректировкаПоступленияЗапасы.ЕдиницаИзмерения,
	|	КорректировкаПоступленияЗапасы.Ссылка.Организация,
	|	КорректировкаПоступленияСерииНоменклатуры.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ОтчетОПереработкеПродукция.Номенклатура,
	|	ОтчетОПереработкеПродукция.Характеристика,
	|	ОтчетОПереработкеПродукция.Партия,
	|	ОтчетОПереработкеСерииНоменклатуры.Серия,
	|	ОтчетОПереработкеПродукция.ЕдиницаИзмерения,
	|	СУММА(ЕСТЬNULL(ОтчетОПереработкеСерииНоменклатуры.Количество, ОтчетОПереработкеПродукция.Количество)),
	|	ОтчетОПереработкеПродукция.Ссылка.Организация
	|ИЗ
	|	Документ.ОтчетОПереработке.Продукция КАК ОтчетОПереработкеПродукция
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ОтчетОПереработке.СерииНоменклатуры КАК ОтчетОПереработкеСерииНоменклатуры
	|		ПО (ОтчетОПереработкеСерииНоменклатуры.Ссылка В (&МассивДокументов))
	|			И ОтчетОПереработкеПродукция.КлючСвязи = ОтчетОПереработкеСерииНоменклатуры.КлючСвязи
	|ГДЕ
	|	ОтчетОПереработкеПродукция.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ОтчетОПереработкеПродукция.Номенклатура,
	|	ОтчетОПереработкеПродукция.Характеристика,
	|	ОтчетОПереработкеПродукция.Партия,
	|	ОтчетОПереработкеПродукция.ЕдиницаИзмерения,
	|	ОтчетОПереработкеПродукция.Ссылка.Организация,
	|	ОтчетОПереработкеСерииНоменклатуры.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Номенклатура,
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Характеристика,
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Партия,
	|	ПередачаТоваровМеждуОрганизациямиСерииНоменклатуры.Серия,
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.ЕдиницаИзмерения,
	|	СУММА(ЕСТЬNULL(ПередачаТоваровМеждуОрганизациямиСерииНоменклатуры.Количество, ПередачаТоваровМеждуОрганизациямиЗапасы.Количество)),
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Ссылка.Организация
	|ИЗ
	|	Документ.ПередачаТоваровМеждуОрганизациями.Запасы КАК ПередачаТоваровМеждуОрганизациямиЗапасы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПередачаТоваровМеждуОрганизациями.СерииНоменклатуры КАК ПередачаТоваровМеждуОрганизациямиСерииНоменклатуры
	|		ПО (ПередачаТоваровМеждуОрганизациямиСерииНоменклатуры.Ссылка В (&МассивДокументов))
	|			И ПередачаТоваровМеждуОрганизациямиЗапасы.КлючСвязи = ПередачаТоваровМеждуОрганизациямиСерииНоменклатуры.КлючСвязи
	|ГДЕ
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Номенклатура,
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Характеристика,
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Партия,
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.ЕдиницаИзмерения,
	|	ПередачаТоваровМеждуОрганизациямиЗапасы.Ссылка.Организация,
	|	ПередачаТоваровМеждуОрганизациямиСерииНоменклатуры.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ПередачаТоваровМеждуОрганизациямиДобавленныеНаборы.НоменклатураНабора,
	|	ПередачаТоваровМеждуОрганизациямиДобавленныеНаборы.ХарактеристикаНабора,
	|	ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|	NULL,
	|	ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка),
	|	СУММА(ПередачаТоваровМеждуОрганизациямиДобавленныеНаборы.Количество),
	|	ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
	|ИЗ
	|	Документ.ПередачаТоваровМеждуОрганизациями.ДобавленныеНаборы КАК ПередачаТоваровМеждуОрганизациямиДобавленныеНаборы
	|ГДЕ
	|	ПередачаТоваровМеждуОрганизациямиДобавленныеНаборы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ПередачаТоваровМеждуОрганизациямиДобавленныеНаборы.НоменклатураНабора,
	|	ПередачаТоваровМеждуОрганизациямиДобавленныеНаборы.ХарактеристикаНабора,
	|	ПередачаТоваровМеждуОрганизациямиДобавленныеНаборы.Ссылка.ОрганизацияПолучатель
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ПоступлениеТоваровУслугТовары.Номенклатура,
	|	ПоступлениеТоваровУслугТовары.Характеристика,
	|	ПоступлениеТоваровУслугТовары.Партия,
	|	ПриходнаяНакладнаяСерииНоменклатуры.Серия,
	|	ПоступлениеТоваровУслугТовары.ЕдиницаИзмерения,
	|	СУММА(ЕСТЬNULL(ПриходнаяНакладнаяСерииНоменклатуры.Количество, ПоступлениеТоваровУслугТовары.Количество)),
	|	ПоступлениеТоваровУслугТовары.Ссылка.Организация
	|ИЗ
	|	Документ.ПриходнаяНакладная.Запасы КАК ПоступлениеТоваровУслугТовары
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПриходнаяНакладная.СерииНоменклатуры КАК ПриходнаяНакладнаяСерииНоменклатуры
	|		ПО (ПриходнаяНакладнаяСерииНоменклатуры.Ссылка В (&МассивДокументов))
	|			И ПоступлениеТоваровУслугТовары.КлючСвязи = ПриходнаяНакладнаяСерииНоменклатуры.КлючСвязи
	|ГДЕ
	|	ПоступлениеТоваровУслугТовары.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ПоступлениеТоваровУслугТовары.Номенклатура,
	|	ПоступлениеТоваровУслугТовары.Характеристика,
	|	ПоступлениеТоваровУслугТовары.Партия,
	|	ПоступлениеТоваровУслугТовары.ЕдиницаИзмерения,
	|	ПоступлениеТоваровУслугТовары.Ссылка.Организация,
	|	ПриходнаяНакладнаяСерииНоменклатуры.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ПоступлениеТоваровУслугНаборы.НоменклатураНабора,
	|	ПоступлениеТоваровУслугНаборы.ХарактеристикаНабора,
	|	ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|	NULL,
	|	ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка),
	|	СУММА(ПоступлениеТоваровУслугНаборы.Количество),
	|	ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
	|ИЗ
	|	Документ.ПриходнаяНакладная.ДобавленныеНаборы КАК ПоступлениеТоваровУслугНаборы
	|ГДЕ
	|	ПоступлениеТоваровУслугНаборы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ПоступлениеТоваровУслугНаборы.НоменклатураНабора,
	|	ПоступлениеТоваровУслугНаборы.ХарактеристикаНабора,
	|	ПоступлениеТоваровУслугНаборы.Ссылка.Организация
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	РасходнаяНакладнаяЗапасы.Номенклатура,
	|	РасходнаяНакладнаяЗапасы.Характеристика,
	|	РасходнаяНакладнаяЗапасы.Партия,
	|	РасходнаяНакладнаяСерии.Серия,
	|	РасходнаяНакладнаяЗапасы.ЕдиницаИзмерения,
	|	СУММА(ЕСТЬNULL(РасходнаяНакладнаяСерии.Количество, РасходнаяНакладнаяЗапасы.Количество)),
	|	РасходнаяНакладнаяЗапасы.Ссылка.Организация
	|ИЗ
	|	Документ.РасходнаяНакладная.Запасы КАК РасходнаяНакладнаяЗапасы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.РасходнаяНакладная.СерииНоменклатуры КАК РасходнаяНакладнаяСерии
	|		ПО (РасходнаяНакладнаяСерии.Ссылка В (&МассивДокументов))
	|			И РасходнаяНакладнаяЗапасы.КлючСвязи = РасходнаяНакладнаяСерии.КлючСвязи
	|ГДЕ
	|	РасходнаяНакладнаяЗапасы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	РасходнаяНакладнаяЗапасы.Номенклатура,
	|	РасходнаяНакладнаяЗапасы.Характеристика,
	|	РасходнаяНакладнаяЗапасы.Партия,
	|	РасходнаяНакладнаяСерии.Серия,
	|	РасходнаяНакладнаяЗапасы.ЕдиницаИзмерения,
	|	РасходнаяНакладнаяЗапасы.Ссылка.Организация
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	РасходнаяНакладнаяНаборы.НоменклатураНабора,
	|	РасходнаяНакладнаяНаборы.ХарактеристикаНабора,
	|	ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|	NULL,
	|	ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка),
	|	СУММА(РасходнаяНакладнаяНаборы.Количество),
	|	ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
	|ИЗ
	|	Документ.РасходнаяНакладная.ДобавленныеНаборы КАК РасходнаяНакладнаяНаборы
	|ГДЕ
	|	РасходнаяНакладнаяНаборы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	РасходнаяНакладнаяНаборы.НоменклатураНабора,
	|	РасходнаяНакладнаяНаборы.ХарактеристикаНабора,
	|	РасходнаяНакладнаяНаборы.Ссылка.Организация
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЗаказПокупателя.Номенклатура,
	|	ЗаказПокупателя.Характеристика,
	|	ЗаказПокупателя.Партия,
	|	ЗаказПокупателяСерии.Серия,
	|	ЗаказПокупателя.ЕдиницаИзмерения,
	|	СУММА(ЗаказПокупателя.Количество),
	|	ЗаказПокупателя.Ссылка.Организация
	|ИЗ
	|	Документ.ЗаказПокупателя.Запасы КАК ЗаказПокупателя
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ЗаказПокупателя.СерииНоменклатуры КАК ЗаказПокупателяСерии
	|		ПО (ЗаказПокупателяСерии.Ссылка В (&МассивДокументов))
	|			И ЗаказПокупателя.КлючСвязи = ЗаказПокупателяСерии.КлючСвязи
	|ГДЕ
	|	ЗаказПокупателя.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ЗаказПокупателя.Номенклатура,
	|	ЗаказПокупателя.Характеристика,
	|	ЗаказПокупателя.Партия,
	|	ЗаказПокупателя.ЕдиницаИзмерения,
	|	ЗаказПокупателя.Ссылка.Организация,
	|	ЗаказПокупателяСерии.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЗаказНаПроизводствоПродукция.Номенклатура,
	|	ЗаказНаПроизводствоПродукция.Характеристика,
	|	ЗаказНаПроизводствоПродукция.Партия,
	|	NULL,
	|	ЗаказНаПроизводствоПродукция.ЕдиницаИзмерения,
	|	СУММА(ЗаказНаПроизводствоПродукция.Количество),
	|	ЗаказНаПроизводствоПродукция.Ссылка.Организация
	|ИЗ
	|	Документ.ЗаказНаПроизводство.Продукция КАК ЗаказНаПроизводствоПродукция
	|ГДЕ
	|	ЗаказНаПроизводствоПродукция.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ЗаказНаПроизводствоПродукция.Номенклатура,
	|	ЗаказНаПроизводствоПродукция.Характеристика,
	|	ЗаказНаПроизводствоПродукция.Партия,
	|	ЗаказНаПроизводствоПродукция.ЕдиницаИзмерения,
	|	ЗаказНаПроизводствоПродукция.Ссылка.Организация
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ПеремещениеЗапасовЗапасы.Номенклатура,
	|	ПеремещениеЗапасовЗапасы.Характеристика,
	|	ПеремещениеЗапасовЗапасы.Партия,
	|	ПеремещениеЗапасовСерии.Серия,
	|	ПеремещениеЗапасовЗапасы.ЕдиницаИзмерения,
	|	СУММА(ПеремещениеЗапасовЗапасы.Количество),
	|	ПеремещениеЗапасовЗапасы.Ссылка.Организация
	|ИЗ
	|	Документ.ПеремещениеЗапасов.Запасы КАК ПеремещениеЗапасовЗапасы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ПеремещениеЗапасов.СерииНоменклатуры КАК ПеремещениеЗапасовСерии
	|		ПО (ПеремещениеЗапасовСерии.Ссылка В (&МассивДокументов))
	|			И ПеремещениеЗапасовЗапасы.КлючСвязи = ПеремещениеЗапасовСерии.КлючСвязи
	|ГДЕ
	|	ПеремещениеЗапасовЗапасы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ПеремещениеЗапасовЗапасы.Номенклатура,
	|	ПеремещениеЗапасовЗапасы.Характеристика,
	|	ПеремещениеЗапасовЗапасы.Партия,
	|	ПеремещениеЗапасовЗапасы.ЕдиницаИзмерения,
	|	ПеремещениеЗапасовЗапасы.Ссылка.Организация,
	|	ПеремещениеЗапасовСерии.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ОприходованиеЗапасовЗапасы.Номенклатура,
	|	ОприходованиеЗапасовЗапасы.Характеристика,
	|	ОприходованиеЗапасовЗапасы.Партия,
	|	ОприходованиеЗапасовСерии.Серия,
	|	ОприходованиеЗапасовЗапасы.ЕдиницаИзмерения,
	|	СУММА(ОприходованиеЗапасовЗапасы.Количество),
	|	ОприходованиеЗапасовЗапасы.Ссылка.Организация
	|ИЗ
	|	Документ.ОприходованиеЗапасов.Запасы КАК ОприходованиеЗапасовЗапасы
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ОприходованиеЗапасов.СерииНоменклатуры КАК ОприходованиеЗапасовСерии
	|		ПО (ОприходованиеЗапасовСерии.Ссылка В (&МассивДокументов))
	|			И ОприходованиеЗапасовЗапасы.КлючСвязи = ОприходованиеЗапасовСерии.КлючСвязи
	|ГДЕ
	|	ОприходованиеЗапасовЗапасы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	ОприходованиеЗапасовЗапасы.Номенклатура,
	|	ОприходованиеЗапасовЗапасы.Характеристика,
	|	ОприходованиеЗапасовЗапасы.Партия,
	|	ОприходованиеЗапасовЗапасы.ЕдиницаИзмерения,
	|	ОприходованиеЗапасовЗапасы.Ссылка.Организация,
	|	ОприходованиеЗапасовСерии.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	СборкаЗапасовПродукция.Номенклатура,
	|	СборкаЗапасовПродукция.Характеристика,
	|	СборкаЗапасовПродукция.Партия,
	|	СборкаЗапасовСерииНоменклатурыПродукция.Серия,
	|	СборкаЗапасовПродукция.ЕдиницаИзмерения,
	|	СУММА(СборкаЗапасовПродукция.Количество),
	|	СборкаЗапасовПродукция.Ссылка.Организация
	|ИЗ
	|	Документ.СборкаЗапасов.Продукция КАК СборкаЗапасовПродукция
	|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.СборкаЗапасов.СерииНоменклатурыПродукция КАК СборкаЗапасовСерииНоменклатурыПродукция
	|		ПО (СборкаЗапасовСерииНоменклатурыПродукция.Ссылка В (&МассивДокументов))
	|			И СборкаЗапасовПродукция.КлючСвязи = СборкаЗапасовСерииНоменклатурыПродукция.КлючСвязи
	|ГДЕ
	|	СборкаЗапасовПродукция.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	СборкаЗапасовПродукция.Номенклатура,
	|	СборкаЗапасовПродукция.Характеристика,
	|	СборкаЗапасовПродукция.Партия,
	|	СборкаЗапасовПродукция.ЕдиницаИзмерения,
	|	СборкаЗапасовПродукция.Ссылка.Организация,
	|	СборкаЗапасовСерииНоменклатурыПродукция.Серия
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	УстановкаЦенНоменклатурыЗапасы.Номенклатура,
	|	УстановкаЦенНоменклатурыЗапасы.Характеристика,
	|	ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|	NULL,
	|	УстановкаЦенНоменклатурыЗапасы.ЕдиницаИзмерения,
	|	1,
	|	ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
	|ИЗ
	|	Документ.УстановкаЦенНоменклатуры.Запасы КАК УстановкаЦенНоменклатурыЗапасы
	|ГДЕ
	|	УстановкаЦенНоменклатурыЗапасы.Ссылка В(&МассивДокументов)
	|
	|СГРУППИРОВАТЬ ПО
	|	УстановкаЦенНоменклатурыЗапасы.Номенклатура,
	|	УстановкаЦенНоменклатурыЗапасы.Характеристика,
	|	УстановкаЦенНоменклатурыЗапасы.ЕдиницаИзмерения
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ИсходныеДанные.Номенклатура,
	|	ИсходныеДанные.Характеристика,
	|	ИсходныеДанные.Партия,
	|	NULL,
	|	ИсходныеДанные.ЕдиницаИзмерения,
	|	1,
	|	ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка)
	|ИЗ
	|	(ВЫБРАТЬ
	|		ИсходныеДанныеБезЕдиниц.Номенклатура КАК Номенклатура,
	|		ИсходныеДанныеБезЕдиниц.Характеристика КАК Характеристика,
	|		ИсходныеДанныеБезЕдиниц.Партия КАК Партия,
	|		ИсходныеДанныеБезЕдиниц.ЕдиницаИзмерения КАК ЕдиницаИзмерения
	|	ИЗ
	|		(ВЫБРАТЬ
	|			ИсходныеДанныеБезПартий.Номенклатура КАК Номенклатура,
	|			ИсходныеДанныеБезПартий.Характеристика КАК Характеристика,
	|			ИсходныеДанныеБезПартий.Партия КАК Партия,
	|			ИсходныеДанныеБезПартий.ЕдиницаИзмерения КАК ЕдиницаИзмерения
	|		ИЗ
	|			(ВЫБРАТЬ
	|				НоменклатураБезХарактеристик.Ссылка КАК Номенклатура,
	|				ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка) КАК Характеристика,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка) КАК Партия,
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка) КАК ЕдиницаИзмерения
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураБезХарактеристик
	|			ГДЕ
	|				НоменклатураБезХарактеристик.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураБезХарактеристик.ЭтоГруппа
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСоСвоейХарактеристикой
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСоСвоейХарактеристикой.Ссылка)
	|			ГДЕ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСоСвоейХарактеристикой.ЭтоГруппа
	|				И НоменклатураСоСвоейХарактеристикой.ИспользоватьХарактеристики
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСХарактеристикойКатегории.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСХарактеристикойКатегории
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСХарактеристикойКатегории.КатегорияНоменклатуры)
	|			ГДЕ
	|				НоменклатураСХарактеристикойКатегории.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСХарактеристикойКатегории.ЭтоГруппа
	|				И ЕСТЬNULL(НоменклатураСХарактеристикойКатегории.ИспользоватьХарактеристики, ЛОЖЬ)) КАК ИсходныеДанныеБезПартий
	|		
	|		ОБЪЕДИНИТЬ
	|		
	|		ВЫБРАТЬ
	|			ИсходныеДанныеСПартиями.Номенклатура,
	|			ИсходныеДанныеСПартиями.Характеристика,
	|			Партии.Ссылка,
	|			ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|		ИЗ
	|			(ВЫБРАТЬ
	|				НоменклатураБезХарактеристик.Ссылка КАК Номенклатура,
	|				ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка) КАК Характеристика,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка) КАК Партия
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураБезХарактеристик
	|			ГДЕ
	|				НоменклатураБезХарактеристик.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураБезХарактеристик.ЭтоГруппа
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСоСвоейХарактеристикой
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСоСвоейХарактеристикой.Ссылка)
	|			ГДЕ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСоСвоейХарактеристикой.ЭтоГруппа
	|				И НоменклатураСоСвоейХарактеристикой.ИспользоватьХарактеристики
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСХарактеристикойКатегории.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСХарактеристикойКатегории
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСХарактеристикойКатегории.КатегорияНоменклатуры)
	|			ГДЕ
	|				НоменклатураСХарактеристикойКатегории.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСХарактеристикойКатегории.ЭтоГруппа
	|				И ЕСТЬNULL(НоменклатураСХарактеристикойКатегории.ИспользоватьХарактеристики, ЛОЖЬ)) КАК ИсходныеДанныеСПартиями
	|				ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ПартииНоменклатуры КАК Партии
	|				ПО ИсходныеДанныеСПартиями.Номенклатура = Партии.Владелец) КАК ИсходныеДанныеБезЕдиниц
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ИсходныеДанныеСЕдиницами.Номенклатура,
	|		ИсходныеДанныеСЕдиницами.Характеристика,
	|		ИсходныеДанныеСЕдиницами.Партия,
	|		ЕдиницыИзмерения.Ссылка
	|	ИЗ
	|		(ВЫБРАТЬ
	|			ИсходныеДанныеБезПартий.Номенклатура КАК Номенклатура,
	|			ИсходныеДанныеБезПартий.Характеристика КАК Характеристика,
	|			ИсходныеДанныеБезПартий.Партия КАК Партия,
	|			ИсходныеДанныеБезПартий.ЕдиницаИзмерения КАК ЕдиницаИзмерения
	|		ИЗ
	|			(ВЫБРАТЬ
	|				НоменклатураБезХарактеристик.Ссылка КАК Номенклатура,
	|				ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка) КАК Характеристика,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка) КАК Партия,
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка) КАК ЕдиницаИзмерения
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураБезХарактеристик
	|			ГДЕ
	|				НоменклатураБезХарактеристик.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураБезХарактеристик.ЭтоГруппа
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСоСвоейХарактеристикой
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСоСвоейХарактеристикой.Ссылка)
	|			ГДЕ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСоСвоейХарактеристикой.ЭтоГруппа
	|				И НоменклатураСоСвоейХарактеристикой.ИспользоватьХарактеристики
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСХарактеристикойКатегории.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСХарактеристикойКатегории
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСХарактеристикойКатегории.КатегорияНоменклатуры)
	|			ГДЕ
	|				НоменклатураСХарактеристикойКатегории.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСХарактеристикойКатегории.ЭтоГруппа
	|				И ЕСТЬNULL(НоменклатураСХарактеристикойКатегории.ИспользоватьХарактеристики, ЛОЖЬ)) КАК ИсходныеДанныеБезПартий
	|		
	|		ОБЪЕДИНИТЬ
	|		
	|		ВЫБРАТЬ
	|			ИсходныеДанныеСПартиями.Номенклатура,
	|			ИсходныеДанныеСПартиями.Характеристика,
	|			Партии.Ссылка,
	|			ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|		ИЗ
	|			(ВЫБРАТЬ
	|				НоменклатураБезХарактеристик.Ссылка КАК Номенклатура,
	|				ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка) КАК Характеристика,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка) КАК Партия
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураБезХарактеристик
	|			ГДЕ
	|				НоменклатураБезХарактеристик.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураБезХарактеристик.ЭтоГруппа
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСоСвоейХарактеристикой
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСоСвоейХарактеристикой.Ссылка)
	|			ГДЕ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСоСвоейХарактеристикой.ЭтоГруппа
	|				И НоменклатураСоСвоейХарактеристикой.ИспользоватьХарактеристики
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСХарактеристикойКатегории.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСХарактеристикойКатегории
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСХарактеристикойКатегории.КатегорияНоменклатуры)
	|			ГДЕ
	|				НоменклатураСХарактеристикойКатегории.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСХарактеристикойКатегории.ЭтоГруппа
	|				И ЕСТЬNULL(НоменклатураСХарактеристикойКатегории.ИспользоватьХарактеристики, ЛОЖЬ)) КАК ИсходныеДанныеСПартиями
	|				ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ПартииНоменклатуры КАК Партии
	|				ПО ИсходныеДанныеСПартиями.Номенклатура = Партии.Владелец) КАК ИсходныеДанныеСЕдиницами
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ЕдиницыИзмерения КАК ЕдиницыИзмерения
	|			ПО ИсходныеДанныеСЕдиницами.Номенклатура = ЕдиницыИзмерения.Владелец
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ИсходныеДанныеСЕдиницамиКатегории.Номенклатура,
	|		ИсходныеДанныеСЕдиницамиКатегории.Характеристика,
	|		ИсходныеДанныеСЕдиницамиКатегории.Партия,
	|		ЕдиницыИзмерения.Ссылка
	|	ИЗ
	|		(ВЫБРАТЬ
	|			ИсходныеДанныеБезПартий.Номенклатура КАК Номенклатура,
	|			ИсходныеДанныеБезПартий.Характеристика КАК Характеристика,
	|			ИсходныеДанныеБезПартий.Партия КАК Партия,
	|			ИсходныеДанныеБезПартий.ЕдиницаИзмерения КАК ЕдиницаИзмерения
	|		ИЗ
	|			(ВЫБРАТЬ
	|				НоменклатураБезХарактеристик.Ссылка КАК Номенклатура,
	|				ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка) КАК Характеристика,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка) КАК Партия,
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка) КАК ЕдиницаИзмерения
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураБезХарактеристик
	|			ГДЕ
	|				НоменклатураБезХарактеристик.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураБезХарактеристик.ЭтоГруппа
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСоСвоейХарактеристикой
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСоСвоейХарактеристикой.Ссылка)
	|			ГДЕ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСоСвоейХарактеристикой.ЭтоГруппа
	|				И НоменклатураСоСвоейХарактеристикой.ИспользоватьХарактеристики
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСХарактеристикойКатегории.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСХарактеристикойКатегории
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСХарактеристикойКатегории.КатегорияНоменклатуры)
	|			ГДЕ
	|				НоменклатураСХарактеристикойКатегории.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСХарактеристикойКатегории.ЭтоГруппа
	|				И ЕСТЬNULL(НоменклатураСХарактеристикойКатегории.ИспользоватьХарактеристики, ЛОЖЬ)) КАК ИсходныеДанныеБезПартий
	|		
	|		ОБЪЕДИНИТЬ
	|		
	|		ВЫБРАТЬ
	|			ИсходныеДанныеСПартиями.Номенклатура,
	|			ИсходныеДанныеСПартиями.Характеристика,
	|			Партии.Ссылка,
	|			ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|		ИЗ
	|			(ВЫБРАТЬ
	|				НоменклатураБезХарактеристик.Ссылка КАК Номенклатура,
	|				ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка) КАК Характеристика,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка) КАК Партия
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураБезХарактеристик
	|			ГДЕ
	|				НоменклатураБезХарактеристик.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураБезХарактеристик.ЭтоГруппа
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСоСвоейХарактеристикой
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСоСвоейХарактеристикой.Ссылка)
	|			ГДЕ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСоСвоейХарактеристикой.ЭтоГруппа
	|				И НоменклатураСоСвоейХарактеристикой.ИспользоватьХарактеристики
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСХарактеристикойКатегории.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСХарактеристикойКатегории
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСХарактеристикойКатегории.КатегорияНоменклатуры)
	|			ГДЕ
	|				НоменклатураСХарактеристикойКатегории.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСХарактеристикойКатегории.ЭтоГруппа
	|				И ЕСТЬNULL(НоменклатураСХарактеристикойКатегории.ИспользоватьХарактеристики, ЛОЖЬ)) КАК ИсходныеДанныеСПартиями
	|				ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ПартииНоменклатуры КАК Партии
	|				ПО ИсходныеДанныеСПартиями.Номенклатура = Партии.Владелец) КАК ИсходныеДанныеСЕдиницамиКатегории
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ЕдиницыИзмерения КАК ЕдиницыИзмерения
	|			ПО ИсходныеДанныеСЕдиницамиКатегории.Номенклатура.КатегорияНоменклатуры = ЕдиницыИзмерения.Владелец
	|	
	|	ОБЪЕДИНИТЬ ВСЕ
	|	
	|	ВЫБРАТЬ
	|		ИсходныеДанныеСЕдиницамиНабора.Номенклатура,
	|		ИсходныеДанныеСЕдиницамиНабора.Характеристика,
	|		ИсходныеДанныеСЕдиницамиНабора.Партия,
	|		ЕдиницыИзмерения.Ссылка
	|	ИЗ
	|		(ВЫБРАТЬ
	|			ИсходныеДанныеБезПартий.Номенклатура КАК Номенклатура,
	|			ИсходныеДанныеБезПартий.Характеристика КАК Характеристика,
	|			ИсходныеДанныеБезПартий.Партия КАК Партия,
	|			ИсходныеДанныеБезПартий.ЕдиницаИзмерения КАК ЕдиницаИзмерения
	|		ИЗ
	|			(ВЫБРАТЬ
	|				НоменклатураБезХарактеристик.Ссылка КАК Номенклатура,
	|				ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка) КАК Характеристика,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка) КАК Партия,
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка) КАК ЕдиницаИзмерения
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураБезХарактеристик
	|			ГДЕ
	|				НоменклатураБезХарактеристик.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураБезХарактеристик.ЭтоГруппа
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСоСвоейХарактеристикой
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСоСвоейХарактеристикой.Ссылка)
	|			ГДЕ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСоСвоейХарактеристикой.ЭтоГруппа
	|				И НоменклатураСоСвоейХарактеристикой.ИспользоватьХарактеристики
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСХарактеристикойКатегории.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка),
	|				ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСХарактеристикойКатегории
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСХарактеристикойКатегории.КатегорияНоменклатуры)
	|			ГДЕ
	|				НоменклатураСХарактеристикойКатегории.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСХарактеристикойКатегории.ЭтоГруппа
	|				И ЕСТЬNULL(НоменклатураСХарактеристикойКатегории.ИспользоватьХарактеристики, ЛОЖЬ)) КАК ИсходныеДанныеБезПартий
	|		
	|		ОБЪЕДИНИТЬ
	|		
	|		ВЫБРАТЬ
	|			ИсходныеДанныеСПартиями.Номенклатура,
	|			ИсходныеДанныеСПартиями.Характеристика,
	|			Партии.Ссылка,
	|			ЗНАЧЕНИЕ(Справочник.ЕдиницыИзмерения.ПустаяСсылка)
	|		ИЗ
	|			(ВЫБРАТЬ
	|				НоменклатураБезХарактеристик.Ссылка КАК Номенклатура,
	|				ЗНАЧЕНИЕ(Справочник.ХарактеристикиНоменклатуры.ПустаяСсылка) КАК Характеристика,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка) КАК Партия
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураБезХарактеристик
	|			ГДЕ
	|				НоменклатураБезХарактеристик.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураБезХарактеристик.ЭтоГруппа
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСоСвоейХарактеристикой
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСоСвоейХарактеристикой.Ссылка)
	|			ГДЕ
	|				НоменклатураСоСвоейХарактеристикой.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСоСвоейХарактеристикой.ЭтоГруппа
	|				И НоменклатураСоСвоейХарактеристикой.ИспользоватьХарактеристики
	|			
	|			ОБЪЕДИНИТЬ
	|			
	|			ВЫБРАТЬ
	|				НоменклатураСХарактеристикойКатегории.Ссылка,
	|				ХарактеристикиНоменклатуры.Ссылка,
	|				ЗНАЧЕНИЕ(Справочник.ПартииНоменклатуры.ПустаяСсылка)
	|			ИЗ
	|				Справочник.Номенклатура КАК НоменклатураСХарактеристикойКатегории
	|					ЛЕВОЕ СОЕДИНЕНИЕ Справочник.ХарактеристикиНоменклатуры КАК ХарактеристикиНоменклатуры
	|					ПО (ХарактеристикиНоменклатуры.Владелец = НоменклатураСХарактеристикойКатегории.КатегорияНоменклатуры)
	|			ГДЕ
	|				НоменклатураСХарактеристикойКатегории.Ссылка В(&МассивДокументов)
	|				И НЕ НоменклатураСХарактеристикойКатегории.ЭтоГруппа
	|				И ЕСТЬNULL(НоменклатураСХарактеристикойКатегории.ИспользоватьХарактеристики, ЛОЖЬ)) КАК ИсходныеДанныеСПартиями
	|				ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ПартииНоменклатуры КАК Партии
	|				ПО ИсходныеДанныеСПартиями.Номенклатура = Партии.Владелец) КАК ИсходныеДанныеСЕдиницамиНабора
	|			ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ЕдиницыИзмерения КАК ЕдиницыИзмерения
	|			ПО ИсходныеДанныеСЕдиницамиНабора.Номенклатура.НаборЕдиницИзмерения = ЕдиницыИзмерения.Владелец
	|				И (ИсходныеДанныеСЕдиницамиНабора.Номенклатура.ИспользоватьНаборыЕдиницИзмерения)) КАК ИсходныеДанные
	|
	|СГРУППИРОВАТЬ ПО
	|	ИсходныеДанные.Номенклатура,
	|	ИсходныеДанные.Характеристика,
	|	ИсходныеДанные.Партия,
	|	ИсходныеДанные.ЕдиницаИзмерения
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
	|	АвансовыйОтчет.Организация КАК Организация,
	|	ЗНАЧЕНИЕ(Справочник.СтруктурныеЕдиницы.ПустаяСсылка) КАК СтруктурнаяЕдиница,
	|	ЗНАЧЕНИЕ(Справочник.ВидыЦен.ПустаяСсылка) КАК ВидЦен
	|ИЗ
	|	Документ.АвансовыйОтчет КАК АвансовыйОтчет
	|ГДЕ
	|	АвансовыйОтчет.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВводНачальныхОстатков.Организация,
	|	ЗНАЧЕНИЕ(Справочник.СтруктурныеЕдиницы.ПустаяСсылка),
	|	ЗНАЧЕНИЕ(Справочник.ВидыЦен.ПустаяСсылка)
	|ИЗ
	|	Документ.ВводНачальныхОстатков КАК ВводНачальныхОстатков
	|ГДЕ
	|	ВводНачальныхОстатков.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	КомплектацияЗапасов.Организация,
	|	КомплектацияЗапасов.СтруктурнаяЕдиница,
	|	КомплектацияЗапасов.СтруктурнаяЕдиница.РозничныйВидЦен
	|ИЗ
	|	Документ.КомплектацияЗапасов КАК КомплектацияЗапасов
	|ГДЕ
	|	КомплектацияЗапасов.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	КорректировкаПоступления.Организация,
	|	КорректировкаПоступления.СтруктурнаяЕдиница,
	|	КорректировкаПоступления.СтруктурнаяЕдиница.РозничныйВидЦен
	|ИЗ
	|	Документ.КорректировкаПоступления КАК КорректировкаПоступления
	|ГДЕ
	|	КорректировкаПоступления.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ОтчетОПереработке.Организация,
	|	ОтчетОПереработке.СтруктурнаяЕдиница,
	|	ОтчетОПереработке.СтруктурнаяЕдиница.РозничныйВидЦен
	|ИЗ
	|	Документ.ОтчетОПереработке КАК ОтчетОПереработке
	|ГДЕ
	|	ОтчетОПереработке.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ПередачаТоваровМеждуОрганизациями.Организация,
	|	ПередачаТоваровМеждуОрганизациями.СтруктурнаяЕдиница,
	|	ПередачаТоваровМеждуОрганизациями.СтруктурнаяЕдиница.РозничныйВидЦен
	|ИЗ
	|	Документ.ПередачаТоваровМеждуОрганизациями КАК ПередачаТоваровМеждуОрганизациями
	|ГДЕ
	|	ПередачаТоваровМеждуОрганизациями.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ПоступлениеТоваровУслуг.Организация,
	|	ПоступлениеТоваровУслуг.СтруктурнаяЕдиница,
	|	ПоступлениеТоваровУслуг.СтруктурнаяЕдиница.РозничныйВидЦен
	|ИЗ
	|	Документ.ПриходнаяНакладная КАК ПоступлениеТоваровУслуг
	|ГДЕ
	|	ПоступлениеТоваровУслуг.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	РасходнаяНакладная.Организация,
	|	РасходнаяНакладная.СтруктурнаяЕдиница,
	|	РасходнаяНакладная.СтруктурнаяЕдиница.РозничныйВидЦен
	|ИЗ
	|	Документ.РасходнаяНакладная КАК РасходнаяНакладная
	|ГДЕ
	|	РасходнаяНакладная.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ЗаказНаПроизводство.Организация,
	|	ЗаказНаПроизводство.СтруктурнаяЕдиница,
	|	ЗаказНаПроизводство.СтруктурнаяЕдиница.РозничныйВидЦен
	|ИЗ
	|	Документ.ЗаказНаПроизводство КАК ЗаказНаПроизводство
	|ГДЕ
	|	ЗаказНаПроизводство.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ПеремещениеЗапасов.Организация,
	|	ПеремещениеЗапасов.СтруктурнаяЕдиницаПолучатель,
	|	ПеремещениеЗапасов.СтруктурнаяЕдиницаПолучатель.РозничныйВидЦен
	|ИЗ
	|	Документ.ПеремещениеЗапасов КАК ПеремещениеЗапасов
	|ГДЕ
	|	ПеремещениеЗапасов.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ОприходованиеЗапасов.Организация,
	|	ОприходованиеЗапасов.СтруктурнаяЕдиница,
	|	ОприходованиеЗапасов.ВидЦен
	|ИЗ
	|	Документ.ОприходованиеЗапасов КАК ОприходованиеЗапасов
	|ГДЕ
	|	ОприходованиеЗапасов.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ЗаказПокупателя.Организация,
	|	ЗаказПокупателя.СтруктурнаяЕдиницаПродажи,
	|	ЗаказПокупателя.ВидЦен
	|ИЗ
	|	Документ.ЗаказПокупателя КАК ЗаказПокупателя
	|ГДЕ
	|	ЗаказПокупателя.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	СборкаЗапасов.Организация,
	|	СборкаЗапасов.СтруктурнаяЕдиницаПродукции,
	|	ЗНАЧЕНИЕ(Справочник.ВидыЦен.ПустаяСсылка)
	|ИЗ
	|	Документ.СборкаЗапасов КАК СборкаЗапасов
	|ГДЕ
	|	СборкаЗапасов.Ссылка В(&МассивДокументов)
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ЗНАЧЕНИЕ(Справочник.Организации.ПустаяСсылка),
	|	ЗНАЧЕНИЕ(Справочник.СтруктурныеЕдиницы.ПустаяСсылка),
	|	УстановкаЦенНоменклатурыЗапасы.ВидЦены
	|ИЗ
	|	Документ.УстановкаЦенНоменклатуры.Запасы КАК УстановкаЦенНоменклатурыЗапасы
	|ГДЕ
	|	УстановкаЦенНоменклатурыЗапасы.Ссылка В(&МассивДокументов)");
	
	// АПК:96 -вкл
	
	ИсключитьНедоступнуюТаблицуИзТекстаЗапроса(Запрос, "ЗаказНаПроизводство");
	ИсключитьНедоступнуюТаблицуИзТекстаЗапроса(Запрос, "СборкаЗапасов");
	ИсключитьНедоступнуюТаблицуИзТекстаЗапроса(Запрос, "ПеремещениеЗапасов");
	ИсключитьНедоступнуюТаблицуИзТекстаЗапроса(Запрос, "ОприходованиеЗапасов");
	ИсключитьНедоступнуюТаблицуИзТекстаЗапроса(Запрос, "КомплектацияЗапасов");
	
	Запрос.УстановитьПараметр("МассивДокументов", МассивДокументов);
	
	МассивРезультатов = Запрос.ВыполнитьПакет();
	
	ТаблицаРеквизитыДокументов	= МассивРезультатов[1].Выгрузить();
	МассивОрганизаций			= Обработки.ПечатьЭтикетокИЦенников.СвернутьТаблицуЗначенийПоРеквизиту(ТаблицаРеквизитыДокументов, "Организация").ВыгрузитьКолонку(0);
	МассивСкладов				= Обработки.ПечатьЭтикетокИЦенников.СвернутьТаблицуЗначенийПоРеквизиту(ТаблицаРеквизитыДокументов, "СтруктурнаяЕдиница").ВыгрузитьКолонку(0);
	МассивВидовЦен				= Обработки.ПечатьЭтикетокИЦенников.СвернутьТаблицуЗначенийПоРеквизиту(ТаблицаРеквизитыДокументов, "ВидЦен").ВыгрузитьКолонку(0);
	
	// Подготовка структуры действий для обработки печати этикеток и ценников
	СтруктураДействий = Новый Структура;
	СтруктураДействий.Вставить("ЗаполнитьОрганизацию", ?(МассивОрганизаций.Количество() = 1,МассивОрганизаций[0],
		УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(Пользователи.ТекущийПользователь(), "ОсновнаяОрганизация")));
	СтруктураДействий.Вставить("ЗаполнитьСклад", ?(МассивСкладов.Количество() = 1,МассивСкладов[0], МассивСкладов));
	СтруктураДействий.Вставить("ЗаполнитьВидЦен", ?(МассивВидовЦен.Количество() = 1,МассивВидовЦен[0],
		УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(Пользователи.ТекущийПользователь(), "ОсновнойВидЦенПродажи")));
	СтруктураДействий.Вставить("ПоказыватьКолонкуКоличествоВДокументе", Истина);
	СтруктураДействий.Вставить("УстановитьРежимПечатиИзДокумента");
	Если ЭтоЦенники Тогда
		
		СтруктураДействий.Вставить("УстановитьРежим", "ПечатьЦенников");
		СтруктураДействий.Вставить("ЗаполнитьКоличествоЦенниковПоДокументу");
		
	Иначе
		
		СтруктураДействий.Вставить("УстановитьРежим", "ПечатьЭтикеток");
		СтруктураДействий.Вставить("ЗаполнитьКоличествоЭтикетокПоДокументу");
		
	КонецЕсли;
	СтруктураДействий.Вставить("ЗаполнитьТаблицуТоваров");
	
	Если КомпоновщикНастроек <> Неопределено Тогда
		СтруктураДействий.Вставить("КомпоновщикНастроек", КомпоновщикНастроек);
	КонецЕсли;
	
	Если Не ВыключеноИспользованиеНовойПечатиЦенников() Тогда
		ТоварыДляПечати = Новый Массив();
		Товары = МассивРезультатов[0].Выгрузить();
		ТоварыДляПечати = ЗаполнитьТаблицуТоваров(Товары, СтруктураДействий);
	Иначе
		ТоварыДляПечати = МассивРезультатов[0].Выгрузить();
	КонецЕсли;
	
	// Подготовка данных для заполнения табличной части обработки печати этикеток и ценников
	СтруктураРезультат = Новый Структура;
	СтруктураРезультат.Вставить("Запасы", ТоварыДляПечати);
	СтруктураРезультат.Вставить("МассивДокументов", МассивДокументов);
	СтруктураРезультат.Вставить("СтруктураДействий", СтруктураДействий);
	
	Возврат ПоместитьВоВременноеХранилище(СтруктураРезультат);
	
КонецФункции // ПодготовитьСтруктуруДанныхПечатиЭтикетокИЦенниковИзДокументов()

// Возвращает список макетов для печати счетов-фактур, перечисленных через запятую.
// 
// Параметры:
// 	СчетаФактуры - Массив - счета-фактуры, для которых следует получить список макетов.
// Возвращаемое значение:
// 	Строка - список макетов для печати счетов-фактур, перечисленных через запятую.
Функция СписокМакетовСчетовФактур(СчетаФактуры) Экспорт
	
	СписокМакетов = "";
	ЕстьСчетФактура = Ложь;
	ЕстьКорректировочныйСчетФактура = Ложь;
	
	Для Каждого Объект Из СчетаФактуры Цикл
		
		Если ТипЗнч(Объект) = Тип("ДокументСсылка.СчетФактура") Тогда
			Если Объект.ВидОперации = Перечисления.ВидыОперацийСчетФактура.Корректировка Тогда
				Если НЕ ЕстьКорректировочныйСчетФактура Тогда
					СписокМакетов = СписокМакетов + ?(СписокМакетов = "", "СчетФактураКорректировочный", ",СчетФактураКорректировочный");
					ЕстьКорректировочныйСчетФактура = Истина;
				КонецЕсли;
				
			Иначе
				Если НЕ ЕстьСчетФактура Тогда
					СписокМакетов = СписокМакетов + ?(СписокМакетов = "", "СчетФактура", ",СчетФактура");
					ЕстьСчетФактура = Истина;
				КонецЕсли;
			КонецЕсли;
		Иначе
			Если Объект.ВидОперации = Перечисления.ВидыОперацийСчетФактураПолученный.Корректировка Тогда
				Если НЕ ЕстьКорректировочныйСчетФактура Тогда
					СписокМакетов = СписокМакетов + ?(СписокМакетов = "", "СчетФактураКорректировочный", ",СчетФактураКорректировочный");
					ЕстьКорректировочныйСчетФактура = Истина;
				КонецЕсли;
				
			Иначе
				Если НЕ ЕстьСчетФактура Тогда
					СписокМакетов = СписокМакетов + ?(СписокМакетов = "", "СчетФактура", ",СчетФактура");
					ЕстьСчетФактура = Истина;
				КонецЕсли;
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Возврат СписокМакетов;
	
КонецФункции

// Возвращает ссылку на Организацию, указанную в первом документе параметра команды.
//
// Параметры:
//  ПараметрКоманды	 - Массив Из ДокументСсылка - массив ссылок на документы для печати.
// 
// Возвращаемое значение:
//  СправочникСсылка.Организации - организация, для которой вызывается печатная форма.
//  В случае если печать вызывается для форм разных организаций - возвращается пустая ссылка.
Функция ОрганизацияИзПараметраКоманды(ПараметрКоманды) Экспорт
	
	Результат = Справочники.Организации.ПустаяСсылка();
	
	Если ТипЗнч(ПараметрКоманды) <> Тип("Массив") Тогда
		Возврат Результат;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ПараметрКоманды) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Для Каждого ТекПараметр Из ПараметрКоманды Цикл
		
		Если ТипЗнч(ПараметрКоманды[0]) <> ТипЗнч(ТекПараметр) Тогда
			Возврат Результат;
		КонецЕсли;
		
		Если Не ОбщегоНазначения.ЗначениеСсылочногоТипа(ТекПараметр) Тогда
			Возврат Результат;
		КонецЕсли;
		
		Если Не ОбщегоНазначения.ЕстьРеквизитОбъекта("Организация", ТекПараметр.Метаданные()) Тогда
			Возврат Результат;
		КонецЕсли;
		
	КонецЦикла;
	
	ЗначенияРеквизитаОбъектов = ОбщегоНазначения.ЗначениеРеквизитаОбъектов(ПараметрКоманды, "Организация");
	
	Для Каждого КлючИЗначение Из ЗначенияРеквизитаОбъектов Цикл
		Если Не ЗначениеЗаполнено(Результат) Тогда
			Результат = КлючИЗначение.Значение;
		КонецЕсли;
		Если Результат <> КлючИЗначение.Значение Тогда
			Возврат Справочники.Организации.ПустаяСсылка();
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура ПриИзмененииВидаЦены(Объект) Экспорт
	
	Запрос = Обработки.ПечатьЭтикетокИЦенников.НовыйЗапросЗаполнитьПоляТаблицы();
	ТоварыЗапроса = Объект.Товары.Выгрузить(,"НоменклатураБПО, ХарактеристикаБПО, Партия, УпаковкаБПО");
	Колонка = ТоварыЗапроса.Колонки.Найти("НоменклатураБПО");
	Колонка.Имя = "Номенклатура";
	Колонка = ТоварыЗапроса.Колонки.Найти("ХарактеристикаБПО");
	Колонка.Имя = "Характеристика";
	
	Колонка = ТоварыЗапроса.Колонки.Найти("УпаковкаБПО");
	Колонка.Имя = "ЕдиницаИзмерения";
	
	Запрос.УстановитьПараметр("Товары",			ТоварыЗапроса);
	Запрос.УстановитьПараметр("НуженШтрихкод",	Ложь);
	Запрос.УстановитьПараметр("НуженОстаток",	Ложь);
	Запрос.УстановитьПараметр("НужныШаблоны",	Ложь);
	Запрос.УстановитьПараметр("НужнаЦена",		Истина);
	Запрос.УстановитьПараметр("Период",			ТекущаяДатаСеанса());
	Запрос.УстановитьПараметр("ВидЦен",			Объект.ВидЦены);
	Запрос.УстановитьПараметр("ИспользоватьНаборы", ПолучитьФункциональнуюОпцию("ИспользоватьНаборы"));
	
	РезультатЗапроса = Запрос.ВыполнитьПакет();
	
	ТоварыСоШтрихкодом = РезультатЗапроса[2].Выгрузить();
	ТоварыСЦеной = РезультатЗапроса[4].Выгрузить();
	
	Для Каждого Товар Из Объект.Товары Цикл
		
		РезультатПоиска = ТоварыСЦеной.НайтиСтроки(Новый Структура("Номенклатура, Характеристика", Товар.НоменклатураБПО, Товар.ХарактеристикаБПО));
		Если РезультатПоиска.Количество() > 0 Тогда
			Товар.Цена = РезультатПоиска[0].Цена;
		Иначе
			Товар.Цена = Товар.Цена;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры 

Процедура ПолучитьДанныеПоШтрихКодам(СтруктураДанные, МассивШтрихкодов) Экспорт
	
	Организация = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(
	Пользователи.ТекущийПользователь(), "ОсновнаяОрганизация");
	
	СтруктураДанные.Вставить("МассивШтрихкодов", МассивШтрихкодов);
	СтруктураДанные.Вставить("Организация", Организация);
	
	Для каждого ТекШтрихкод Из СтруктураДанные.МассивШтрихкодов Цикл
		
		РегистрыСведений.ШтрихкодыНоменклатуры.ПреобразоватьВесовойШтрихкод(ТекШтрихкод);
		
	КонецЦикла;
	
	ДанныеПоШтрихКодам = РегистрыСведений.ШтрихкодыНоменклатуры.ПолучитьДанныеПоШтрихкодамВМассиве(МассивШтрихкодов);
	
	СтруктураДанные.Вставить("ДанныеПоШтрихКодам", ДанныеПоШтрихКодам); 
	
КонецПроцедуры

Процедура ОбработкаПодбораТоваровПоОтборуДляПечатиЭтикетокИЦенниковЗавершение(ПараметрыОперации, МассивТоваровДляПечати) Экспорт
	
	ИспользоватьХарактеристикиНоменклатуры = ПолучитьФункциональнуюОпцию("ИспользоватьХарактеристики");
	ИспользоватьПартииНоменклатуры = ПолучитьФункциональнуюОпцию("ИспользоватьПартии");
	ИспользоватьСерииНоменклатуры = ПолучитьФункциональнуюОпцию("ИспользоватьСерииНоменклатуры");
	ИспользоватьЕдиницыИзмерения = ПолучитьФункциональнуюОпцию("УчетВРазличныхЕдиницахИзмерения");
	ИспользоватьSKU = ПолучитьФункциональнуюОпцию("ИспользоватьОбменСПодключаемымОборудованиемOffline");
	
	ЭлементыОтбора = ПараметрыОперации.КомпоновщикНастроек.Настройки.Отбор.Элементы;
	
	Для Каждого ЭлементОтбора Из ЭлементыОтбора Цикл
		
		Если ТипЗнч(ЭлементОтбора) <> Тип("ГруппаЭлементовОтбораКомпоновкиДанных") И ТипЗнч(ЭлементОтбора.ПравоеЗначение) = Тип("СправочникСсылка.Организации") Тогда
			Организация = ЭлементОтбора.ПравоеЗначение;
		КонецЕсли;
		
	КонецЦикла;
	
	НастройкиПечати = Обработки.ПечатьЭтикетокИЦенников.НовыеНастройкиПечати();
	
	НастройкиПечати.ОбязательныеПоля.Добавить("Цена");
	НастройкиПечати.ОбязательныеПоля.Добавить("Весовой");
	НастройкиПечати.ОбязательныеПоля.Добавить("Штрихкод");
	НастройкиПечати.ОбязательныеПоля.Добавить("Количество");
	НастройкиПечати.ОбязательныеПоля.Добавить("Номенклатура");
	Если ИспользоватьSKU Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("SKU");
	КонецЕсли;
	Если ИспользоватьХарактеристикиНоменклатуры Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("Характеристика");
	КонецЕсли;
	Если ИспользоватьПартииНоменклатуры Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("Партия");
	КонецЕсли;
	Если ИспользоватьСерииНоменклатуры Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("Серия");
	КонецЕсли;
	Если ИспользоватьЕдиницыИзмерения Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("ЕдиницаИзмерения");
	КонецЕсли;
	НастройкиПечати.ОбязательныеПоля.Добавить("ОстатокНаСкладе");
	НастройкиПечати.ОбязательныеПоля.Добавить("Организация");
	
	НастройкиПечати.ОбязательныеПоля.Добавить("НоменклатураНаименованиеПолное");
	
	// Шаблоны этикеток и ценников.
	НастройкиПечати.ОбязательныеПоля.Добавить("Номенклатура.КатегорияНоменклатуры.ШаблонЭтикетки");
	НастройкиПечати.ОбязательныеПоля.Добавить("Номенклатура.КатегорияНоменклатуры.ШаблонЦенника");
	ПолеСКДШаблонЦенникаБПО = "Номенклатура.КатегорияНоменклатуры.ШаблонЦенникаБПО";
	НастройкиПечати.ОбязательныеПоля.Добавить(ПолеСКДШаблонЦенникаБПО);
	
	НастройкиПечати.ПараметрыДанных.Вставить("ВидЦен"              , ПараметрыОперации.ВидЦены);
	НастройкиПечати.ПараметрыДанных.Вставить("ВидЦенАкционный"     , ПараметрыОперации.ВидЦеныАкционный);
	НастройкиПечати.ПараметрыДанных.Вставить("ЦеныПоВидуЦены"      , Ложь);
	НастройкиПечати.ПараметрыДанных.Вставить("ЦеныНаДату"          , ПараметрыОперации.ЦеныНаДату);
	НастройкиПечати.ПараметрыДанных.Вставить("ЦеныНаДатуАкционные" , ПараметрыОперации.ЦеныНаДату);
	
	НастройкиПечати.ПараметрыДанных.Вставить("ИспользоватьАссортимент", Ложь);
	
	НастройкиПечати.КомпоновщикНастроек = ПараметрыОперации.КомпоновщикНастроек;
	НастройкиПечати.ИмяМакетаСхемыКомпоновкиДанных = "ПоляШаблонаБД";
	
	// Загрузка сформированного списка товаров.
	ДанныеДляПечати = Обработки.ПечатьЭтикетокИЦенников.ПодготовитьДанныеДляПечати(НастройкиПечати);
	
	Для Каждого Товар Из ДанныеДляПечати.ТаблицаТоваров Цикл
		
		СтруктураТовара = ПечатьЭтикетокИЦенниковБПОКлиентСервер.ТоварДляПечати();
		Если ИспользоватьПартииНоменклатуры Тогда
			СтруктураТовара.Вставить("Партия", Товар.Партия);
		КонецЕсли;
		Если ИспользоватьСерииНоменклатуры Тогда
			СтруктураТовара.Вставить("Серия", Товар.Серия);
		КонецЕсли;
		Если ИспользоватьХарактеристикиНоменклатуры Тогда
			СтруктураТовара.ХарактеристикаБПО = Товар.Характеристика;
		КонецЕсли;
		Если ИспользоватьЕдиницыИзмерения Тогда
			СтруктураТовара.УпаковкаБПО       = Товар.ЕдиницаИзмерения;
		КонецЕсли;
		Если ИспользоватьSKU Тогда
			СтруктураТовара.Вставить("ШтрихАвтопроверки", СформироватьСобственныйШтрихкод(Товар));
		КонецЕсли;
		СтруктураТовара.НоменклатураБПО   = Товар.Номенклатура;
		СтруктураТовара.ОрганизацияБПО    = Организация;
		СтруктураТовара.Штрихкод = Товар.Штрихкод;
		СтруктураТовара.Цена = Товар.Цена;
		Если ПараметрыОперации.РежимПечати = "ПечатьЭтикеток" Тогда
			СтруктураТовара.Количество = Товар.ОстатокНаСкладе;
		Иначе
			СтруктураТовара.Количество = 1;
		КонецЕсли;
		КолонкаШаблон = ДанныеДляПечати.СоответствиеПолейСКДКолонкамТаблицыТоваров.Получить(ПолеСКДШаблонЦенникаБПО);
		СтруктураТовара.Вставить("ШаблонЦенникаЭтикетки", Товар[КолонкаШаблон]);
		
		МассивТоваровДляПечати.Добавить(СтруктураТовара);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ОбработкаПодбораТоваровДляПечатиЭтикетокИЦенниковЗавершение(Результат, ПараметрыОперации, МассивТоваровДляПечати) Экспорт
	
	Если ТипЗнч(Результат) = Тип("Структура") Тогда
		
		АдресЗапасовВХранилище	= Результат.АдресКорзиныВХранилище;
		
		ЗаполняемыеПоля = Новый Структура;
		ЗаполняемыеПоля.Вставить("Цена");
		ЗаполняемыеПоля.Вставить("ОстатокНаСкладе");
		ЗаполняемыеПоля.Вставить("Шаблоны");
		ЗаполняемыеПоля.Вставить("Штрихкод");
		
		Если НЕ ПустаяСтрока(АдресЗапасовВХранилище) Тогда
			
			ТаблицаДляЗагрузки = ПолучитьИзВременногоХранилища(АдресЗапасовВХранилище);
			
			Товары = Новый ТаблицаЗначений();
			Товары.Колонки.Добавить("Номенклатура",         Новый ОписаниеТипов("СправочникСсылка.Номенклатура"));
			Товары.Колонки.Добавить("Характеристика",       Новый ОписаниеТипов("СправочникСсылка.ХарактеристикиНоменклатуры"));
			Товары.Колонки.Добавить("Партия",               Новый ОписаниеТипов("СправочникСсылка.ПартииНоменклатуры"));
			Товары.Колонки.Добавить("ЕдиницаИзмерения",     Новый ОписаниеТипов("СправочникСсылка.ЕдиницыИзмерения"));
			
			Для Каждого Товар Из ТаблицаДляЗагрузки Цикл 
				НовыйТовар = Товары.Добавить();
				ЗаполнитьЗначенияСвойств(НовыйТовар, Товар);
			КонецЦикла;
			
			Запрос = Обработки.ПечатьЭтикетокИЦенников.НовыйЗапросЗаполнитьПоляТаблицы();
			Запрос.УстановитьПараметр("Товары",			Товары);
			Запрос.УстановитьПараметр("НуженШтрихкод",	ЗаполняемыеПоля.Свойство("Штрихкод"));
			Запрос.УстановитьПараметр("НуженОстаток",	ЗаполняемыеПоля.Свойство("ОстатокНаСкладе"));
			Запрос.УстановитьПараметр("НужныШаблоны",	ЗаполняемыеПоля.Свойство("Шаблоны"));
			Запрос.УстановитьПараметр("НужнаЦена",		ЗаполняемыеПоля.Свойство("Цена"));
			Запрос.УстановитьПараметр("Период",			ПараметрыОперации.ЦеныНаДату);
			Запрос.УстановитьПараметр("ВидЦен",			ПараметрыОперации.ВидЦены);
			Запрос.УстановитьПараметр("ИспользоватьНаборы", ПолучитьФункциональнуюОпцию("ИспользоватьНаборы"));
			
			РезультатЗапроса = Запрос.ВыполнитьПакет();
			          
			ТоварыСоШтрихкодом = РезультатЗапроса[2].Выгрузить();
			ТоварыСЦеной = РезультатЗапроса[4].Выгрузить();
			ТоварыСШаблонами = РезультатЗапроса[5].Выгрузить();
			
			Для Каждого Товар Из ТаблицаДляЗагрузки Цикл
				
				СтруктураТовара = ПечатьЭтикетокИЦенниковБПОКлиентСервер.ТоварДляПечати();
				СтруктураТовара.Вставить("Партия", Товар.Партия);
				СтруктураТовара.НоменклатураБПО   = Товар.Номенклатура;
				СтруктураТовара.ХарактеристикаБПО = Товар.Характеристика;
				СтруктураТовара.УпаковкаБПО       = Товар.ЕдиницаИзмерения;
				СтруктураТовара.ОрганизацияБПО    = Товар.СтруктурнаяЕдиница.Организация;
				РезультатПоиска = ТоварыСоШтрихкодом.НайтиСтроки(Новый Структура("Номенклатура, Характеристика, Партия",
					Товар.Номенклатура, Товар.Характеристика, Товар.Партия));
				Если РезультатПоиска.Количество() > 0 Тогда
					СтруктураТовара.Штрихкод = РезультатПоиска[0].Штрихкод;
					СтруктураТовара.Вставить("SKU", РезультатПоиска[0].SKU);
				КонецЕсли;
				
				РезультатПоиска = ТоварыСЦеной.НайтиСтроки(Новый Структура("Номенклатура, Характеристика", Товар.Номенклатура, Товар.Характеристика));
				Если РезультатПоиска.Количество() > 0 Тогда
					СтруктураТовара.Цена = РезультатПоиска[0].Цена;
				Иначе
					СтруктураТовара.Цена = Товар.Цена;
				КонецЕсли;
				
				Если СтруктураТовара.Свойство("SKU") Тогда
					СтруктураТовара.Вставить("ШтрихАвтопроверки", СформироватьСобственныйШтрихкод(СтруктураТовара));
				КонецЕсли;
				
				СтруктураТовара.Количество = Товар.Количество;
				РезультатПоиска = ТоварыСШаблонами.НайтиСтроки(Новый Структура("Номенклатура", Товар.Номенклатура));
				Если РезультатПоиска.Количество() > 0 Тогда
					СтруктураТовара.Вставить("ШаблонЦенникаЭтикетки", РезультатПоиска[0].ШаблонЦенникаБПО);
				КонецЕсли;
				
				МассивТоваровДляПечати.Добавить(СтруктураТовара);
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры 

Функция СформироватьСобственныйШтрихкод(Товар)
	
	Штрихкод = "";
	
	Если ЗначениеЗаполнено(Товар.SKU) Тогда
		КодИдентификатораSKUТовара = "99";
		КодSKUТовара = ПриведенноеКДлинеЗначение(Товар.SKU, 9);
		Штрихкод = КодИдентификатораSKUТовара + КодSKUТовара;
	КонецЕсли;
	
	КодИдентификатораЦены = "392N";
	Если ЗначениеЗаполнено(Товар.Цена) Тогда
		Если Товар.Цена = Цел(Товар.Цена) Тогда
			ЦенаТовара = ПриведенноеКДлинеЗначение(Товар.Цена, 9);
			ЦенаТовара = ЦенаТовара + "00";
		Иначе
			ЦенаТовара = ПриведенноеКДлинеЗначение(Товар.Цена, 12);
			ЦенаТовара = СтрЗаменить(ЦенаТовара, ",", "");
		КонецЕсли;
		Если НЕ Штрихкод = "" Тогда
			Штрихкод = Штрихкод + КодИдентификатораЦены + ЦенаТовара;
		КонецЕсли;
	Иначе
		Если НЕ Штрихкод = "" Тогда
			ЦенаТовара = ПриведенноеКДлинеЗначение(0, 12);
			Штрихкод = Штрихкод + КодИдентификатораЦены + ЦенаТовара;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Штрихкод;
	
КонецФункции

Функция ПриведенноеКДлинеЗначение(Знач ИсходнаяСтрока, Длина)
	Если ТипЗнч(ИсходнаяСтрока) = Тип("Число") Тогда
		Строка = Формат(ИсходнаяСтрока, "ЧН=0; ЧГ=0");
	Иначе
		Строка = СокрЛП(ИсходнаяСтрока);
	КонецЕсли;
	ТекущаяДлина = СтрДлина(Строка);
	Пока ТекущаяДлина < Длина Цикл
		Строка = "0" + Строка;
		ТекущаяДлина = ТекущаяДлина + 1;
	КонецЦикла;
	
	Возврат Строка;
КонецФункции


Процедура ЗаполнитьПоДаннымШтрихкодовЗавершение(ПараметрыОперации, Параметры) Экспорт
	
	ДанныеШтрихкода = ПараметрыОперации.МассивНоменклатуры[0];
	ТекШтрихкод = ПараметрыОперации.ТекШтрихкод;
	
	ТаблицаТоваров = Новый ТаблицаЗначений();
	ТаблицаТоваров.Колонки.Добавить("Номенклатура",         Новый ОписаниеТипов("СправочникСсылка.Номенклатура"));
	ТаблицаТоваров.Колонки.Добавить("Характеристика",       Новый ОписаниеТипов("СправочникСсылка.ХарактеристикиНоменклатуры"));
	ТаблицаТоваров.Колонки.Добавить("Партия",             Новый ОписаниеТипов("СправочникСсылка.ПартииНоменклатуры"));
	ТаблицаТоваров.Колонки.Добавить("ЕдиницаИзмерения",   Новый ОписаниеТипов("СправочникСсылка.ЕдиницыИзмерения"));
	ТаблицаТоваров.Колонки.Добавить("Штрихкод", Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(200)));
	
	Товар = ТаблицаТоваров.Добавить();
	Товар.ЕдиницаИзмерения = ДанныеШтрихкода.ЕдиницаИзмерения;
	Товар.Партия           = ДанныеШтрихкода.Партия;
	Товар.Характеристика   = ДанныеШтрихкода.Характеристика;
	Товар.Номенклатура     = ДанныеШтрихкода.Номенклатура;
	Товар.Штрихкод         = ТекШтрихкод.Штрихкод;
	Запрос = Обработки.ПечатьЭтикетокИЦенников.НовыйЗапросЗаполнитьПоляТаблицы();
	
	Запрос.УстановитьПараметр("Товары",			ТаблицаТоваров.Скопировать(,"Номенклатура, Характеристика, Партия, ЕдиницаИзмерения"));
	Запрос.УстановитьПараметр("НуженШтрихкод",	Ложь);
	Запрос.УстановитьПараметр("НуженОстаток",	Ложь);
	Запрос.УстановитьПараметр("НужныШаблоны",	Истина);
	Запрос.УстановитьПараметр("НужнаЦена",		Истина);
	Запрос.УстановитьПараметр("Период",			Параметры.СтруктураДанные.Дата);
	Запрос.УстановитьПараметр("ВидЦен",			Параметры.СтруктураДанные.ВидЦены);
	Запрос.УстановитьПараметр("ИспользоватьНаборы", ПолучитьФункциональнуюОпцию("ИспользоватьНаборы"));
	
	РезультатЗапроса = Запрос.ВыполнитьПакет();
	
	ТоварыСЦеной = РезультатЗапроса[4].Выгрузить();
	ТоварыСШаблонами = РезультатЗапроса[5].Выгрузить();
	МассивТоваровДляПечати = Новый Массив;
	Для Каждого Товар Из ТаблицаТоваров Цикл
		
		СтруктураТовара = ПечатьЭтикетокИЦенниковБПОКлиентСервер.ТоварДляПечати();
		СтруктураТовара.Вставить("Партия", Товар.Партия);
		СтруктураТовара.НоменклатураБПО   = Товар.Номенклатура;
		СтруктураТовара.ХарактеристикаБПО = Товар.Характеристика;
		СтруктураТовара.УпаковкаБПО       = Товар.ЕдиницаИзмерения;
		СтруктураТовара.ОрганизацияБПО    = Параметры.СтруктураДанные.Организация;
		
		РезультатПоиска = ТоварыСЦеной.НайтиСтроки(Новый Структура("Номенклатура, Характеристика", Товар.Номенклатура, Товар.Характеристика));
		Если РезультатПоиска.Количество() > 0 Тогда
			СтруктураТовара.Цена = РезультатПоиска[0].Цена;
		Иначе
			СтруктураТовара.Цена = Товар.Цена;
		КонецЕсли;
		
		СтруктураТовара.Штрихкод = Товар.Штрихкод;
		
		СтруктураТовара.Количество = 1;
		РезультатПоиска = ТоварыСШаблонами.НайтиСтроки(Новый Структура("Номенклатура", Товар.Номенклатура));
		Если РезультатПоиска.Количество() > 0 Тогда
			СтруктураТовара.Вставить("ШаблонЦенникаЭтикетки", РезультатПоиска[0].ШаблонЦенникаБПО);
		КонецЕсли;
		
		МассивТоваровДляПечати.Добавить(СтруктураТовара);
		
	КонецЦикла; 
	ПараметрыОперации.Вставить("МассивТоваровДляПечати", МассивТоваровДляПечати);
	
КонецПроцедуры

Процедура ФормаПечатьЭтикетокИЦенниковПриСозданииНаСервере(Форма) Экспорт
	
	ПараметрыФормы = Форма.Параметры;
	
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	Форма.Объект.ВидЦены = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(ТекущийПользователь, "ОсновнойВидЦенПродажи");
	
	Форма.Элементы.ПечатьНаОбычномПринтере.ВидФлажка    = ВидФлажка.Флажок;
	Форма.Элементы.КаждаяЭтикеткаНаНовомЛисте.ВидФлажка = ВидФлажка.Флажок;
	
	Форма.Элементы.ТоварыУпаковкаБПО.Заголовок = НСтр("ru = 'Ед. изм.'");
	
	Если Не ЗначениеЗаполнено(Форма.Объект.ВидЦены) Тогда
		Форма.Объект.ВидЦены = ПредопределенноеЗначение("Справочник.ВидыЦен.Учетная");
	КонецЕсли;
	
	ДобавляемыеРеквизиты = Новый Массив;
	
	ДобавляемыйРеквизит = Новый РеквизитФормы("Партия", Новый ОписаниеТипов("СправочникСсылка.ПартииНоменклатуры"), "Объект.Товары", "Партия");
	ДобавляемыеРеквизиты.Добавить(ДобавляемыйРеквизит);
	
	ДобавляемыйРеквизит = Новый РеквизитФормы("Серия", Новый ОписаниеТипов("СправочникСсылка.СерииНоменклатуры"), "Объект.Товары", "Серия");
	ДобавляемыеРеквизиты.Добавить(ДобавляемыйРеквизит);
	
	ДобавляемыйРеквизит = Новый РеквизитФормы("ШтрихАвтопроверки", Новый ОписаниеТипов("Строка"), "Объект.Товары", "ШтрихАвтопроверки");
	ДобавляемыеРеквизиты.Добавить(ДобавляемыйРеквизит);
	
	Форма.ИзменитьРеквизиты(ДобавляемыеРеквизиты);
	
	НовыйЭлемент = Форма.Элементы.Добавить("КолонкаПартия", Тип("ПолеФормы"), Форма.Элементы.Товары); 
	НовыйЭлемент.ПутьКДанным = "Объект.Товары.Партия";
	НовыйЭлемент.Вид = ВидПоляФормы.ПолеВвода;
	НовыйЭлемент.Доступность = Ложь;
	Форма.Элементы.Переместить(НовыйЭлемент, Форма.Элементы.Товары, Форма.Элементы.ТоварыУпаковкаБПО);
	
	НовыйЭлемент = Форма.Элементы.Добавить("КолонкаСерия", Тип("ПолеФормы"), Форма.Элементы.Товары); 
	НовыйЭлемент.ПутьКДанным = "Объект.Товары.Серия";
	НовыйЭлемент.Вид = ВидПоляФормы.ПолеВвода;
	НовыйЭлемент.Доступность = Ложь;
	Форма.Элементы.Переместить(НовыйЭлемент, Форма.Элементы.Товары, Форма.Элементы.ТоварыУпаковкаБПО);
	
	НовыйЭлемент = Форма.Элементы.Добавить("КолонкаШтрихАвтопроверки", Тип("ПолеФормы"), Форма.Элементы.Товары); 
	НовыйЭлемент.ПутьКДанным = "Объект.Товары.ШтрихАвтопроверки";
	НовыйЭлемент.Вид = ВидПоляФормы.ПолеВвода;
	НовыйЭлемент.Видимость = Ложь;
	
	Если ПараметрыФормы.Свойство("АдресВХранилище") И ЗначениеЗаполнено(ПараметрыФормы.АдресВХранилище) Тогда
		
		ПараметрыЗаполнения = ПолучитьИзВременногоХранилища(Форма.Параметры.АдресВХранилище);
		ТоварыФормы = Форма.Объект.Товары;
		
		Для Каждого Товар Из ПараметрыЗаполнения.Запасы Цикл
			НовыйТовар = ТоварыФормы.Добавить();
			ЗаполнитьЗначенияСвойств(НовыйТовар, Товар);
		КонецЦикла;
		
		НовыйРеквизит = Новый РеквизитФормы("МассивДокументов", Новый ОписаниеТипов("СписокЗначений"));
		МассивДобавляыемыхРеквизитов = Новый Массив;
		МассивДобавляыемыхРеквизитов.Добавить(НовыйРеквизит);
		Форма.ИзменитьРеквизиты(МассивДобавляыемыхРеквизитов);
		Форма.МассивДокументов.ЗагрузитьЗначения(ПараметрыЗаполнения.МассивДокументов);
		
		НоваяКоманда = Форма.Команды.Добавить("Перезаполнить");
		НоваяКоманда.Действие = "Подключаемый_Перезаполнить";
		НоваяКоманда.Заголовок = НСтр("ru = 'Перезаполнить'");
		
		НовыйЭлемент = Форма.Элементы.Добавить("КомандаПерезаполнить", Тип("КнопкаФормы"), Форма.Элементы.ГруппаКоманднаяПанельТовары);
		НовыйЭлемент.ИмяКоманды = "Перезаполнить";
		
		Форма.Элементы.Переместить(НовыйЭлемент, Форма.Элементы.ГруппаКоманднаяПанельТовары, Форма.Элементы.ЗаполнитьПоОтбору);
		
		Форма.Элементы.ЗаполнитьПоОтбору.Видимость = Ложь;
		
		Форма.Заголовок = Форма.Заголовок + НСтр("ru = ' по объекту'");
		
		Форма.Элементы.КомпоновщикНастроекНастройкиОтбор.ИзменятьСоставСтрок = Ложь;
		Форма.Элементы.КомпоновщикНастроекНастройкиОтбор.КоманднаяПанель.Видимость = Ложь;
		
	КонецЕсли;
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(Форма.Элементы, "НовыйШтрихкод", "Заголовок", НСтр("ru = 'Новый штрихкод EAN-13'"));
	
КонецПроцедуры

Процедура ФормаШаблоныЭтикетокИЦенниковБПОПриСозданииНаСервере(Форма) Экспорт
	Форма.Элементы.ТипЦенника.СписокВыбора.Вставить(0, "Ценник акционный");
	Форма.Элементы.ТипЦенника.СписокВыбора.Вставить(1, "Ценник");
	Форма.Элементы.ТипЦенника.СписокВыбора.Вставить(2, "Этикетка для обуви");
КонецПроцедуры

Функция ДанныеДляФормированияПечатныхФорм(ПараметрыПечати, ПоляДляЗаполнения) Экспорт
	
	СписокПредопределенныхПолей = ПечатьЭтикетокИЦенниковБПОКлиентСервер.СписокПредопределенныхПолей();
	
	НастройкиПечати = Обработки.ПечатьЭтикетокИЦенников.НовыеНастройкиПечати();
	СоответствиеШаблонов = Новый Соответствие;
	
	НастройкиПечати.ОбязательныеПоля.Добавить("КоличествоЦенниковДляПечати");
	НастройкиПечати.ОбязательныеПоля.Добавить("КоличествоЭтикетокДляПечати");
	НастройкиПечати.ОбязательныеПоля.Добавить("ШаблонЦенникаДляПечати");
	НастройкиПечати.ОбязательныеПоля.Добавить("ШаблонЭтикеткиДляПечати");
	НастройкиПечати.ОбязательныеПоля.Добавить("Номенклатура");
	
	ИсходныеДанные = Новый ТаблицаЗначений;
	ИсходныеДанные.Колонки.Добавить("Номенклатура", Новый ОписаниеТипов("СправочникСсылка.Номенклатура"));
	ИсходныеДанные.Колонки.Добавить("Характеристика", Новый ОписаниеТипов("СправочникСсылка.ХарактеристикиНоменклатуры"));
	ИсходныеДанные.Колонки.Добавить("КоличествоЦенников", Новый ОписаниеТипов("Число"));
	ИсходныеДанные.Колонки.Добавить("ШаблонЦенника", Новый ОписаниеТипов("СправочникСсылка.ШаблоныЭтикетокИЦенниковБПО"));
	ИсходныеДанные.Колонки.Добавить("Организация", Новый ОписаниеТипов("СправочникСсылка.Организации"));
	ИсходныеДанные.Колонки.Добавить("Партия", Новый ОписаниеТипов("СправочникСсылка.ПартииНоменклатуры"));
	ИсходныеДанные.Колонки.Добавить("Серия", Новый ОписаниеТипов("СправочникСсылка.СерииНоменклатуры"));
	ИсходныеДанные.Колонки.Добавить("КоличествоЭтикеток", Новый ОписаниеТипов("Число"));
	ИсходныеДанные.Колонки.Добавить("ШаблонЭтикетки", Новый ОписаниеТипов("СправочникСсылка.ХранилищеШаблонов"));
	ИсходныеДанные.Колонки.Добавить("Штрихкод", Новый ОписаниеТипов("Строка",,Новый КвалификаторыСтроки(128)));
	ИсходныеДанные.Колонки.Добавить("ЕдиницаИзмерения", Новый ОписаниеТипов("СправочникСсылка.ЕдиницыИзмерения"));
	ИсходныеДанные.Колонки.Добавить("ШтрихАвтопроверки", Новый ОписаниеТипов("Строка"));
	
	Для Каждого Товар Из ПараметрыПечати.Товары Цикл
		Если Товар.Выбран Тогда
			Строка = ИсходныеДанные.Добавить();
			Строка.Номенклатура = Товар.НоменклатураБПО;
			Строка.ШаблонЦенника = Товар.ШаблонЦенникаЭтикетки;
			Строка.КоличествоЦенников = Товар.Количество;
			Строка.Характеристика = Товар.ХарактеристикаБПО;
			Строка.ЕдиницаИзмерения = Товар.УпаковкаБПО;
			Строка.Организация = Товар.ОрганизацияБПО;
			Строка.Партия = Товар.Партия;
			Строка.Серия = Товар.Серия;
			Строка.Штрихкод = Товар.Штрихкод;
			Строка.ШтрихАвтопроверки = Товар.ШтрихАвтопроверки;
		КонецЕсли;
	КонецЦикла;
	
	НастройкиПечати.ИсходныеДанные = ИсходныеДанные;
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	
	ВидЦены = ПараметрыПечати.ВидЦены;
	ВидЦеныАкционный = ПараметрыПечати.ВидЦеныАкционный;
	
	Если Не ЗначениеЗаполнено(ВидЦены) Тогда 
		ВидЦены = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(ТекущийПользователь, "ОсновнойВидЦенПродажи");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ВидЦены) Тогда
		ВидЦены = ПредопределенноеЗначение("Справочник.ВидыЦен.Учетная");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ПараметрыПечати.ТорговыйОбъект) Тогда
		Организация = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(ТекущийПользователь, "ОсновнаяОрганизация");
	Иначе
		Организация = ПараметрыПечати.ТорговыйОбъект;
	КонецЕсли;
	
	НастройкиПечати.ПараметрыДанных.Вставить("ВидЦен", ВидЦены);
	НастройкиПечати.ПараметрыДанных.Вставить("ВидЦенАкционный", ВидЦеныАкционный);
	НастройкиПечати.ПараметрыДанных.Вставить("Организация", Организация);
	НастройкиПечати.ПараметрыДанных.Вставить("ЦеныПоВидуЦены", Ложь);
	НастройкиПечати.ПараметрыДанных.Вставить("ЦеныНаДату", ПараметрыПечати.ЦеныНаДату);
	НастройкиПечати.ПараметрыДанных.Вставить("ЦеныНаДатуАкционные", ПараметрыПечати.ЦеныНаДату);
	
	ДанныеНоменклатурыДляФормированияПечатныхФорм = Новый Массив;
	Для Каждого Шаблон Из ПоляДляЗаполнения Цикл;
		ШаблонМакета = Шаблон.Ключ;
		СписокПолей = Шаблон.Значение;
		
		ДобавленныеПоля = Новый Массив;
		СтруктураШаблона = Новый Структура;
		СтруктураШаблона.Вставить("Шаблон", ШаблонМакета);
		СтруктураШаблона.Вставить("ИмяКолонкиШаблон", "ШаблонЦенника");
		Если Не ПустаяСтрока(ШаблонМакета.ТипЦенника) Тогда
			НастройкиПечати.ИмяМакетаСхемыКомпоновкиДанных = МакетСхемыКомпоновкиДанных(ШаблонМакета.ТипЦенника);
		Иначе
			НастройкиПечати.ИмяМакетаСхемыКомпоновкиДанных = "ПоляШаблона";
		КонецЕсли;
		// Заполняем коллекцию обязательных полей и формируем соответствие шаблонов.
		Для Каждого Поле Из СписокПолей Цикл
			Если Не ЗначениеЗаполнено(ДобавленныеПоля.Найти(Поле.Значение)) Тогда
				НастройкиПечати.ОбязательныеПоля.Добавить(Поле);
				ДобавленныеПоля.Добавить(Поле.Значение);
			КонецЕсли;
		КонецЦикла;
		
		ТаблицаДанных = Обработки.ПечатьЭтикетокИЦенников.ПодготовитьДанныеДляПечати(НастройкиПечати, СтруктураШаблона);
		КолонкаИзображений = ТаблицаДанных.ТаблицаТоваров.Колонки.Найти("НоменклатураФайлКартинки"); 
		КолонкаИзображенийХарактеристик = ТаблицаДанных.ТаблицаТоваров.Колонки.Найти("ХарактеристикаФайлКартинки");

		Если КолонкаИзображений <> Неопределено Тогда
			МассивФайлов = ТаблицаДанных.ТаблицаТоваров.ВыгрузитьКолонку("НоменклатураФайлКартинки");
			ЗаголовокКолонки = КолонкаИзображений.Заголовок;
			ТаблицаДанных.ТаблицаТоваров.Колонки.Удалить("НоменклатураФайлКартинки");
			ТаблицаДанных.ТаблицаТоваров.Колонки.Добавить("НоменклатураФайлКартинки", Новый ОписаниеТипов("Строка"), ЗаголовокКолонки);
			ИндексКартинки = 0;
			Для Каждого ФайлКартинки Из МассивФайлов Цикл
				Если ЗначениеЗаполнено(ФайлКартинки) Тогда
					ДанныеФайла = РаботаСФайлами.ДанныеФайла(ФайлКартинки);
					ДвоичныеДанныеКартинки = ПолучитьИзВременногоХранилища(ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
					СтрокаBase64 = Base64Строка(ДвоичныеДанныеКартинки);
					СтрокаBase64 = СтрЗаменить(СтрокаBase64, Символы.ПС, "");
					ТаблицаДанных.ТаблицаТоваров[ИндексКартинки].НоменклатураФайлКартинки = СтрокаBase64;
				Иначе
					ТаблицаДанных.ТаблицаТоваров[ИндексКартинки].НоменклатураФайлКартинки = "";
				КонецЕсли;
				ИндексКартинки = ИндексКартинки + 1;
			КонецЦикла;
		КонецЕсли;
		
		Если КолонкаИзображенийХарактеристик <> Неопределено Тогда
			МассивФайлов = ТаблицаДанных.ТаблицаТоваров.ВыгрузитьКолонку("ХарактеристикаФайлКартинки");
			ЗаголовокКолонки = КолонкаИзображенийХарактеристик.Заголовок;
			ТаблицаДанных.ТаблицаТоваров.Колонки.Удалить("ХарактеристикаФайлКартинки");
			ТаблицаДанных.ТаблицаТоваров.Колонки.Добавить("ХарактеристикаФайлКартинки", Новый ОписаниеТипов("Строка"), ЗаголовокКолонки);
			ИндексКартинки = 0;
			Для Каждого ФайлКартинки Из МассивФайлов Цикл
				Если ЗначениеЗаполнено(ФайлКартинки) Тогда
					ДанныеФайла = РаботаСФайлами.ДанныеФайла(ФайлКартинки);
					ДвоичныеДанныеКартинки = ПолучитьИзВременногоХранилища(ДанныеФайла.СсылкаНаДвоичныеДанныеФайла);
					СтрокаBase64 = Base64Строка(ДвоичныеДанныеКартинки);
					СтрокаBase64 = СтрЗаменить(СтрокаBase64, Символы.ПС, "");
					ТаблицаДанных.ТаблицаТоваров[ИндексКартинки].ХарактеристикаФайлКартинки = СтрокаBase64;
				Иначе
					ТаблицаДанных.ТаблицаТоваров[ИндексКартинки].ХарактеристикаФайлКартинки = "";
				КонецЕсли;
				ИндексКартинки = ИндексКартинки + 1;
			КонецЦикла;
		КонецЕсли;
		
		СтруктураДанных = Новый Структура;
		СтруктураДанных.Вставить("Шаблон", ШаблонМакета);
		СтруктураДанных.Вставить("ТаблицаНоменклатуры", ТаблицаДанных.ТаблицаТоваров);
		ДанныеНоменклатурыДляФормированияПечатныхФорм.Добавить(СтруктураДанных);
		
	КонецЦикла;
	
	Возврат ДанныеНоменклатурыДляФормированияПечатныхФорм;
	
КонецФункции

Функция МакетСхемыКомпоновкиДанных(ТипЦенника = "") Экспорт
	
	Если ПустаяСтрока(ТипЦенника) Тогда
		Макет = Обработки.ПечатьЭтикетокИЦенников.ПолучитьМакет("ПоляШаблонаПоУмолчанию");
	ИначеЕсли ТипЦенника = "Ценник акционный" Тогда
		Макет = Обработки.ПечатьЭтикетокИЦенников.ПолучитьМакет("ПоляШаблонаЦенникАкционный");
	ИначеЕсли ТипЦенника = "Этикетка для обуви" Тогда
		Макет = Обработки.ПечатьЭтикетокИЦенников.ПолучитьМакет("ПоляШаблонаЦенникАкционный");
	Иначе
		Макет = Обработки.ПечатьЭтикетокИЦенников.ПолучитьМакет("ПоляШаблона");
	КонецЕсли;
	
	Возврат Макет;
	
КонецФункции

Функция МакетСхемыКомпоновкиДанныхДляОтбора(Форма) Экспорт
	
	Если Форма.Параметры.Свойство("АдресВХранилище") И ЗначениеЗаполнено(Форма.Параметры.АдресВХранилище) Тогда
		Макет = Обработки.ПечатьЭтикетокИЦенников.ПолучитьМакет("ПоляШаблонаПоУмолчанию");
	Иначе
		Макет = Обработки.ПечатьЭтикетокИЦенников.ПолучитьМакет("ПоляШаблонаБД");
		Форма.ПечатьПоОтбору = Истина;
	КонецЕсли;
	
	Возврат Макет;
	
КонецФункции

Функция ВыключеноИспользованиеНовойПечатиЦенников(ТипЦенника = "") Экспорт
	Возврат ПолучитьФункциональнуюОпцию("ИспользоватьСтаруюОбработкуПечатиЦенников");
КонецФункции

Процедура УстановитьШтрихкодыНоменклатуры(ВыделенныеТовары, Товары, Штрихкоды) Экспорт
	
	EAN = 13;
	
	НачатьТранзакцию();
	
	Попытка
		
		Блокировка = Новый БлокировкаДанных();
		ЭлементБлокировки = Блокировка.Добавить("РегистрСведений.ШтрихкодыНоменклатуры");
		ЭлементБлокировки.Режим = РежимБлокировкиДанных.Исключительный;
		Блокировка.Заблокировать();
		
		Блокировка = Новый БлокировкаДанных();
		ЭлементБлокировки = Блокировка.Добавить("Справочник.Номенклатура");
		ЭлементБлокировки.Режим = РежимБлокировкиДанных.Исключительный;
		Блокировка.Заблокировать();
		
		ПрефиксШтучногоТовара = РегистрыСведений.ШтрихкодыНоменклатуры.ПрефиксШтучногоТовара();
		ПрефиксУзлаШтрихкода = РегистрыСведений.ШтрихкодыНоменклатуры.ПрефиксУзлаШтрихкода();
		
		КодШтучногоТовара = РегистрыСведений.ШтрихкодыНоменклатуры.ПоследнийКодШтучногоТовара(ПрефиксШтучногоТовара, ПрефиксУзлаШтрихкода, EAN);
		
		Для Каждого НомерСтроки Из ВыделенныеТовары Цикл
			
			СтрокаТЧ = Товары.НайтиПоИдентификатору(НомерСтроки);
			
			Если КодШтучногоТовара >= РегистрыСведений.ШтрихкодыНоменклатуры.МаксимальныйКодШтучногоТовара(EAN) Тогда
				ВызватьИсключение РегистрыСведений.ШтрихкодыНоменклатуры.ТекстСообщенияНетСвободныхКодовШтучныхШтрихкодов();
			КонецЕсли;

			КодШтучногоТовара = КодШтучногоТовара + 1;

			Штрихкод = РегистрыСведений.ШтрихкодыНоменклатуры.ПолучитьШтрихкодПоКоду(КодШтучногоТовара, ПрефиксШтучногоТовара, ПрефиксУзлаШтрихкода, EAN);

			НовыйШтрихкод = РегистрыСведений.ШтрихкодыНоменклатуры.СоздатьМенеджерЗаписи();
			НовыйШтрихкод.Номенклатура = СтрокаТЧ.НоменклатураБПО;
			Если ПолучитьФункциональнуюОпцию("ИспользоватьХарактеристики") Тогда
				НовыйШтрихкод.Характеристика = СтрокаТЧ.ХарактеристикаБПО;
			КонецЕсли;
			Если ПолучитьФункциональнуюОпцию("ИспользоватьПартии") Тогда
				НовыйШтрихкод.Партия = СтрокаТЧ.Партия;
			КонецЕсли;
			Если ПолучитьФункциональнуюОпцию("УчетВРазличныхЕдиницахИзмерения") Тогда
				НовыйШтрихкод.ЕдиницаИзмерения = СтрокаТЧ.УпаковкаБПО;
			КонецЕсли;
			НовыйШтрихкод.Штрихкод = Штрихкод;
			НовыйШтрихкод.Записать();
			
			НоменклатураОбъект = НовыйШтрихкод.Номенклатура.ПолучитьОбъект();
			НоменклатураОбъект.Штрихкод = НовыйШтрихкод.Штрихкод;
			НоменклатураОбъект.Записать();
			
			Штрихкоды.Вставить(НомерСтроки, НовыйШтрихкод.Штрихкод);
			
		КонецЦикла;
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ОтменитьТранзакцию();
		
		ТекстСообщенияПользователю = СтрШаблон(
		НСтр(
		"ru = 'При записи штрихкодов произошла ошибка.
		|Запись штрихкодов не выполнена.
		|Дополнительное описание:
		|%1'"),
		ИнформацияОбОшибке().Описание);
		
		ОбщегоНазначения.СообщитьПользователю(ТекстСообщенияПользователю);
		
		Результат = Ложь;
		
		Возврат;
		
	КонецПопытки;
	
	Результат = Истина;
	
КонецПроцедуры

Функция ПерезаполнитьСУчетомОтбора(МассивДокументов, РежимПечати, Компоновщик) Экспорт
	
	АдресВХранилище = ПодготовитьСтруктуруДанныхПечатиЭтикетокИЦенниковИзДокументов(
		МассивДокументов,
		РежимПечати = "ПечатьЦенников",
		Компоновщик);
	
	ТоварыДляПечати = ПолучитьИзВременногоХранилища(АдресВХранилище).Запасы;
	
	Возврат ТоварыДляПечати;
	
КонецФункции

// Формирует строку-представление веса и объема для печатных форм УПД
//  
// Параметры:
//	ЗначениеДляПечати - Число - значение веса или объема
//  ЭтоВес - Булево
// Возвращаемое значение:
//  Строка
//
Функция ИнформацияОГрузеПрописью(ЗначениеДляПечати, ЭтоВес) Экспорт

	Если Не ЗначениеЗаполнено(ЗначениеДляПечати) Тогда

		Возврат "";

	КонецЕсли;

	Если ЭтоВес Тогда
		СтрокаЕдиницыИзмерения = НСтр("ru = 'килограмм,килограмма,килограммов,м,грамм,грамма,граммов,м,3'");
		Возврат НРег(ЧислоПрописью(ЗначениеДляПечати, "ДП=Истина",
			СтрокаЕдиницыИзмерения));
	Иначе

		Возврат Формат(ЗначениеДляПечати, "ЧГ=0") + НСтр("ru = ' м³'");

	КонецЕсли;

КонецФункции

// Заполняет настройки использования подписей и печатей для печатных форм
// 
// Параметры:
//  Настройки - Структура:
//   * ИспользоватьПодписиИПечати - Булево - при установке значения Ложь отключается возможность установки подписей 
//                                           и печатей в печатных формах.
//   * СкрыватьПодписиИПечатиДляРедактирования - Булево - удалять рисунки подписей и печатей табличных документов при
//                                           снятии флажка "Подписи и печати" в форме "Печать документов", для того,
//                                           чтобы они не мешали редактировать текст, находящийся под ними.
//   * ПроверкаПроведенияПередПечатью    - Булево - признак необходимости проверки проведенности
//                                        документов перед печатью, является значением по умолчанию для команды печати
//                                        см. УправлениеПечатью.СоздатьКоллекциюКомандПечати.
//                                        Для непроведенных документов команда печати не выполняется.
//                                        Если параметр не указан, то проверка проведенности не выполняется.
//   * ОбъектыПечати - Массив - менеджеры объектов с процедурой ПриОпределенииНастроекПечати.
//
Процедура ПриОпределенииНастроекПечати(Настройки) Экспорт
	
	Настройки.ИспользоватьПодписиИПечати = Истина;
	Настройки.СкрыватьПодписиИПечатиДляРедактирования = Истина;
	
КонецПроцедуры

// Определяет набор подписей и печатей для документов.
//
// Параметры:
//  Документы      - Массив Из ЛюбаяСсылка   - коллекция ссылок на объекты печати;
//  ПодписиИПечати - Соответствие из КлючИЗначение - коллекция объектов печати и комплектов подписей/печатей к ним:
//   * Ключ     - ЛюбаяСсылка - ссылка на объект печати;
//   * Значение - Структура   - комплект подписей и печатей:
//     ** Ключ     - Строка - идентификатор подписи или печати в макете печатной формы, 
//                            должен начинаться с "Подпись...", "Печать..." или "Факсимиле...",
//                            например, "ПодписьРуководителя", "ПечатьОрганизации";
//     ** Значение - Картинка - изображение подписи или печати.
//
Процедура ПриПолученииПодписейИПечатей(Документы, ПодписиИПечати) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	ОбъектыПоТипам = Новый Соответствие;
	Для Каждого Документ Из Документы Цикл // Разбираем объекты по типам
		ТипДокумента = ТипЗнч(Документ);
		Если ОбъектыПоТипам[ТипДокумента] = Неопределено Тогда
			ОбъектыПоТипам[ТипДокумента] = Новый Массив;
		КонецЕсли;
		КоллекцияДокументов = ОбъектыПоТипам[ТипДокумента];
		КоллекцияДокументов.Добавить(Документ); 
	КонецЦикла;
	
	Для Каждого ОбъектыПоТипу Из ОбъектыПоТипам Цикл // по объектам каждого типа получаем подписи
		ТипОбъекта = ОбъектыПоТипу.Ключ;
		СписокОбъектов = ОбъектыПоТипу.Значение;
		
		ПодписиТипа = ПолучитьПодписиТипа(ТипОбъекта);
		ЕстьОрганизация = ЕстьРеквизитОрганизация(ТипОбъекта); 
		Для Каждого ОбъектСписка Из СписокОбъектов Цикл
			ПодписиОбъекта = Новый Структура;
			Для Каждого ПодписьТипа Из ПодписиТипа Цикл
				ЗначениеПодписи = ОбъектСписка[ПодписьТипа];
				Если ЗначениеЗаполнено(ЗначениеПодписи) Тогда
					ПрисоединенныйФайл = ЗначениеПодписи.Факсимиле;
					ИмяПодписи = ПодписьТипа;
					Если ЗначениеЗаполнено(ПрисоединенныйФайл) Тогда
						ДанныеКартинки = РаботаСФайлами.ДанныеФайла(ПрисоединенныйФайл);
						ДвоичныеДанныеКартинки = ПолучитьИзВременногоХранилища(ДанныеКартинки.СсылкаНаДвоичныеДанныеФайла);
						Если ДвоичныеДанныеКартинки <> Неопределено Тогда
							КартинкаФаксимилеПодписанта = Новый Картинка(ДвоичныеДанныеКартинки);
							ПодписиОбъекта.Вставить(ИмяПодписи, КартинкаФаксимилеПодписанта);
							Если ИмяПодписи = "ПодписьРуководителя" Тогда
								ПодписиОбъекта.Вставить("ПодписьПредпринимателя", КартинкаФаксимилеПодписанта);
								ПодписиОбъекта.Вставить("ПодписьПБОЮЛ", КартинкаФаксимилеПодписанта);
								ПодписиОбъекта.Вставить("ПодписьОтветственногоЗаОформление", КартинкаФаксимилеПодписанта);
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
				КонецЕсли;
			КонецЦикла;
			Если ЕстьОрганизация Тогда
				ДополнитьПодписиФаксимилеПечати(ПодписиОбъекта, ОбъектСписка);
			КонецЕсли;
			// Название картинки в макетах может быть как "Подпись...", так и "Факсимиле..."
			Для Каждого Подпись Из ПодписиОбъекта Цикл
				Если СтрНайти(Подпись.Ключ, "Подпись") <> 0 Тогда
					ИмяФаксимиле = СтрЗаменить(Подпись.Ключ, "Подпись", "Факсимиле");
					ПодписиОбъекта.Вставить(ИмяФаксимиле, Подпись.Значение);
				КонецЕсли;
			КонецЦикла;
			
			ПодписиИПечати.Вставить(ОбъектСписка, ПодписиОбъекта);
			
		КонецЦикла;
		
	КонецЦикла;
	
	УстановитьПривилегированныйРежим(Ложь);
	
КонецПроцедуры

// Определяет наличие реквизитов подписей и организации у типа данных
// 
// Параметры:
//  ТипДанных - Тип
//  
// Возвращаемое значение:
//  Булево
//
Функция ЕстьРеквизитыПодписиИПечать(ТипДанных) Экспорт
	
	ПодписиТипа = ПолучитьПодписиТипа(ТипДанных);
	ЕстьОрганизация = ЕстьРеквизитОрганизация(ТипДанных);
	
	Если ПодписиТипа.Количество() > 0 И ЕстьОрганизация Тогда
		Результат = Истина;
	Иначе
		Результат = Ложь;
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

// Вызывается из обработчика ПриСозданииНаСервере формы печати документов (ОбщаяФорма.ПечатьДокументов).
// Позволяет изменить внешний вид и поведение формы, например, разместить на ней дополнительные элементы:
// информационные надписи, кнопки, гиперссылки, различные настройки и т.п.
// 
// Параметры:
//  Форма                - ФормаКлиентскогоПриложения - форма ОбщаяФорма.ПечатьДокументов.
//  Отказ                - Булево - признак отказа от создания формы. Если установить
//                                  данному параметру значение Истина, то форма создана не будет.
//  СтандартнаяОбработка - Булево - в данный параметр передается признак выполнения стандартной (системной) обработки
//                                  события. Если установить данному параметру значение Ложь, 
//                                  стандартная обработка события производиться не будет.
//
Процедура ПечатьДокументовПриСозданииНаСервере(Форма, Отказ, СтандартнаяОбработка) Экспорт
	
	НастроитьФормуПечатьДокументов(Форма);
	ОформитьФлажокПодписиИПечати(Форма);
	
КонецПроцедуры

// Добавляет на форму группу элементов с подсказкой о новой схеме печати факсимиле.
// 
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - форма объекта
//  МладшийЭлемент - Строка - имя элемента формы, над которым вставляем группу
//
Процедура ДобавитьИнформациюПоНовомуФаксимиле(Форма, МладшийЭлемент = "Шапка") Экспорт
	
	Контекст = Форма["Объект"];
	ИмяКлючаОбъекта = "Информирование/НовоеВВерсии";
	ИмяНастройки = "ПоказыватьИнформациюОНовомФаксимиле";
	ПоказыватьИнформациюОНовомФаксимиле = ХранилищеСистемныхНастроек.Загрузить(ИмяКлючаОбъекта, ИмяНастройки);
	
	Если ПоказыватьИнформациюОНовомФаксимиле = Неопределено Тогда
		ПоказыватьИнформациюОНовомФаксимиле = Истина;
	КонецЕсли;
	
	Если Не ПоказыватьИнформациюОНовомФаксимиле Тогда
		Возврат;
	КонецЕсли;
	
	ФаксимилеПечати = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Контекст.Организация, "ФайлФаксимильнаяПечать");
	ЕстьЗаполненныеРеквизитыПодписи =  ЗначениеЗаполнено(ФаксимилеПечати);
	
	Если Не ЕстьЗаполненныеРеквизитыПодписи Тогда
		ТипДанных = ТипЗнч(Контекст.Ссылка);
		РеквизитыПодписей = ПолучитьПодписиТипа(ТипДанных);
		Для Каждого РеквизитПодписи Из РеквизитыПодписей Цикл
			Если ЗначениеЗаполнено(Контекст[РеквизитПодписи]) Тогда
				ЕстьЗаполненныеРеквизитыПодписи = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Если Не ЕстьЗаполненныеРеквизитыПодписи Тогда
		Возврат;
	КонецЕсли;
	
	// Группа
	ЭлементШапка = Форма.Элементы.Найти(МладшийЭлемент);
	Группа = Форма.Элементы.Вставить("ГруппаИнформацияНовоеФаксимиле", Тип("ГруппаФормы"), , ЭлементШапка);
	Группа.Вид                      = ВидГруппыФормы.ОбычнаяГруппа;
	Группа.ОтображатьЗаголовок      = Ложь;
	Группа.Отображение              = ОтображениеОбычнойГруппы.Нет;
	Группа.ЦветФона                 = ЦветаСтиля.ЦветФонаПодсказки;
	Группа.РастягиватьПоГоризонтали = Истина;
	
	// Надпись
	Элемент = Форма.Элементы.Вставить("ДекорацияИнформацияНовоеФаксимиле", Тип("ДекорацияФормы"), Группа);
	Элемент.АвтоМаксимальнаяШирина = Ложь;
	СтрокаЗаголовок = Новый ФорматированнаяСтрока(НСтр(
		"ru = 'Новое в версии: для печати документа с факсимиле сформируйте печатную форму без факсимиле и в открывшемся окне установите флажок ""Подпись и печать"".'"));
	Элемент.Заголовок = СтрокаЗаголовок;
	Элемент.РастягиватьПоГоризонтали = Истина;
	
	// Кнопка
	Элемент = Форма.Элементы.Вставить("ДекорацияИнформацияНовоеФаксимилеЗакрыть", Тип("ДекорацияФормы"), Группа);
	Элемент.Вид = ВидДекорацииФормы.Картинка;
	Элемент.Картинка = БиблиотекаКартинок.ОчиститьЗначение;
	Элемент.Гиперссылка = Истина;
	Элемент.ГоризонтальноеПоложениеВГруппе = ГоризонтальноеПоложениеЭлемента.Право;
	Элемент.УстановитьДействие("Нажатие", "Подключаемый_НажатиеДекорацияИнформацияНовоеФаксимилеЗакрыть");
	
КонецПроцедуры

// Выключает в формах документов вывод уведомлений о новом факсимиле
// Параметры:
//  ИмяПользователя - Строка
//
Процедура ВыключитьИнформированиеНовоеФаксимиле(ИмяПользователя = "") Экспорт
	
	ИмяКлючаОбъекта = "Информирование/НовоеВВерсии";
	ИмяНастройки = "ПоказыватьИнформациюОНовомФаксимиле";
	ЗначениеНастройки = Ложь;
	
	Если ПустаяСтрока(ИмяПользователя) Тогда
		ХранилищеСистемныхНастроек.Сохранить(ИмяКлючаОбъекта, ИмяНастройки, ЗначениеНастройки);
	Иначе
		УстановитьПривилегированныйРежим(Истина);
		ХранилищеСистемныхНастроек.Сохранить(ИмяКлючаОбъекта, ИмяНастройки, ЗначениеНастройки, , ИмяПользователя);
	КонецЕсли;
	
КонецПроцедуры

// Формирует текст сообщения пользователю об измененном макете.
// 
// Параметры:
//  ИмяПечатнойФормы - Строка
//  
// Возвращаемое значение:
//  Строка
//
Функция ТекстСообщенияМакетИзменен(ИмяПечатнойФормы) Экспорт
	
	ШаблонСообщения = НСтр(
		"ru = 'Изменился типовой макет печатной формы %1. Для корректной
		|работы печатной формы необходимо обновить отредактированный ранее макет. 
		|Это можно выполнить самостоятельно в разделе ""Настройки - Администрирование - Печатные формы,
		|отчеты, обработки - Макеты печатных форм"" или обратиться к специалисту обслуживающей организации.'");
	
	ТекстСообщения = СтрШаблон(ШаблонСообщения, ИмяПечатнойФормы);
	
	Возврат ТекстСообщения;
	
КонецФункции

// Записывает настройку пользователя по заполнению факсимиле в печатных формах
//
// Параметры:
//  ЗначениеНастройки  - Булево
//  ИменаМакетов - Строка - Имена макетов, для которых сохраняется настройка
//
Процедура ЗаписатьНастройкуПользователяЗаполнятьФаксимиле(ЗначениеНастройки, ИменаМакетов = "") Экспорт
	
	ИмяКлючаОбъекта = "НастройкиПользователя/Печать";
	ИмяНастройки = "ЗаполнятьФаксимиле_" + ИменаМакетов;
	ЗначениеНастройки = ЗначениеНастройки;
	
	ХранилищеСистемныхНастроек.Сохранить(ИмяКлючаОбъекта, ИмяНастройки, ЗначениеНастройки);
	
КонецПроцедуры

// Возвращает настройку пользователя по заполнению факсимиле в печатных формах
// 
// Возвращаемое значение:
//   Булево
//
Функция НастройкаПользователяЗаполнятьФаксимиле(ИменаМакетов = "") Экспорт
	
	ИмяКлючаОбъекта = "НастройкиПользователя/Печать";
	ИмяНастройки = "ЗаполнятьФаксимиле_" + ИменаМакетов;
	ЗначениеНастройки = ХранилищеСистемныхНастроек.Загрузить(ИмяКлючаОбъекта, ИмяНастройки);
	Если ЗначениеНастройки = Неопределено Тогда
		ЗначениеНастройки = Ложь;
	КонецЕсли;
	
	Возврат ЗначениеНастройки;
	
КонецФункции

// Вызывается из обработчика ПриЗагрузкеДанныхИзНастроекНаСервере формы печати документов (ОбщаяФорма.ПечатьДокументов).
// Совместно с ПечатьДокументовПриСохраненииДанныхВНастройкахНаСервере позволяет реализовать загрузку и сохранение 
// настроек элементов управления, размещенных с помощью ПечатьДокументовПриСозданииНаСервере.
//
// Параметры:
//  Форма     - ФормаКлиентскогоПриложения - форма ОбщаяФорма.ПечатьДокументов.
//  Настройки - Соответствие     - значения реквизитов формы.
//
Процедура ПечатьДокументовПриЗагрузкеДанныхИзНастроекНаСервере(Форма, Настройки) Экспорт
	
	Если Не Настройки["ПодписьИПечать"] = Неопределено Тогда
		ИменаМакетов = Форма.ИменаМакетов;
		ПодписьИПечать = НастройкаПользователяЗаполнятьФаксимиле(ИменаМакетов);
		Настройки["ПодписьИПечать"] = ПодписьИПечать;
		Форма.ПодписьИПечать = ПодписьИПечать;
	КонецЕсли;
	
КонецПроцедуры

// Вызывается из обработчика ПриСохраненииДанныхВНастройкахНаСервере формы печати документов (ОбщаяФорма.ПечатьДокументов).
// Совместно с ПечатьДокументовПриЗагрузкеДанныхИзНастроекНаСервере позволяет реализовать загрузку и сохранение 
// настроек элементов управления, размещенных с помощью ПечатьДокументовПриСозданииНаСервере.
//
// Параметры:
//  Форма     - ФормаКлиентскогоПриложения - форма ОбщаяФорма.ПечатьДокументов.
//  Настройки - Соответствие     - значения реквизитов формы.
//
Процедура ПечатьДокументовПриСохраненииДанныхВНастройкахНаСервере(Форма, Настройки) Экспорт
	
	Если Не Настройки["ПодписьИПечать"] = Неопределено Тогда
		ЗначениеНастройки = Настройки["ПодписьИПечать"];
		ИменаМакетов = Форма.ИменаМакетов;
		Если ЗначениеНастройки = Неопределено Тогда
			ЗначениеНастройки = Ложь;
		КонецЕсли;
		ЗаписатьНастройкуПользователяЗаполнятьФаксимиле(ЗначениеНастройки, ИменаМакетов);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция КППЮрФизЛицаНаДату(ЮрФизЛицо, ТекущееКПП, ДатаПериода);
	
	Если НЕ ХранитьИсториюИзмененийОбъекта(ЮрФизЛицо) Тогда
		Возврат ТекущееКПП;
	КонецЕсли;
	
	МодульМенеджераВладельцаКПП = ОбщегоНазначения.МенеджерОбъектаПоСсылке(ЮрФизЛицо);
	Возврат МодульМенеджераВладельцаКПП.КППНаДату(ЮрФизЛицо, ДатаПериода);
	
КонецФункции

Функция ПолноеНаименованиеЮрФизЛицаНаДату(ЮрФизЛицо, ТекущееНаименование, ДатаПериода);
	
	Если НЕ ХранитьИсториюИзмененийОбъекта(ЮрФизЛицо) Тогда
		Возврат ТекущееНаименование;
	КонецЕсли;
	
	МодульМенеджераВладельцаНаименования = ОбщегоНазначения.МенеджерОбъектаПоСсылке(ЮрФизЛицо);
	Возврат МодульМенеджераВладельцаНаименования.ПолноеНаименованиеНаДату(ЮрФизЛицо, ДатаПериода);
	
КонецФункции

Функция НаименованиеЮрФизЛицаНаДату(ЮрФизЛицо, ТекущееНаименование, ДатаПериода);
	
	Если НЕ ХранитьИсториюИзмененийОбъекта(ЮрФизЛицо) Тогда
		Возврат ТекущееНаименование;
	КонецЕсли;
	
	НаименованиеНаДату = Справочники.Организации.НаименованиеНаДату(ЮрФизЛицо, ДатаПериода);
	
	Если НЕ ЗначениеЗаполнено(НаименованиеНаДату) Тогда
		Возврат ТекущееНаименование;
	КонецЕсли;
	
	Возврат НаименованиеНаДату;
	
КонецФункции

Функция НаименованиеДляПечатиЮрФизЛицаНаДату(ЮрФизЛицо, ДатаПериода);
	
	Если НЕ ХранитьИсториюИзмененийОбъекта(ЮрФизЛицо) Тогда
		Возврат НаименованиеОрганизацииПоНастройке(ЮрФизЛицо);
	КонецЕсли;
	
	НаименованиеДляПечатиНаДату = Справочники.Организации.НаименованиеДляПечатиНаДату(ЮрФизЛицо, ДатаПериода);
	
	Если НЕ ЗначениеЗаполнено(НаименованиеДляПечатиНаДату) Тогда
		Возврат НаименованиеОрганизацииПоНастройке(ЮрФизЛицо);
	КонецЕсли;
	
	Возврат НаименованиеДляПечатиНаДату;
	
КонецФункции

Функция НаименованиеОрганизацииПоНастройке(ЮрФизЛицо)
	
	ВидНаименования = Константы.ВидНаименованияОрганизацииДляПечатиДокументов.Получить();
	Если ВидНаименования = Перечисления.ВидыНаименованийДляПечатиДокументов.Сокращенное Тогда
		НаименованиеДляПечати = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЮрФизЛицо, "НаименованиеСокращенное");
	Иначе
		НаименованиеДляПечати = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЮрФизЛицо, "НаименованиеПолное");
	КонецЕсли;
	
	Возврат НаименованиеДляПечати;
	
КонецФункции

Функция ХранитьИсториюИзмененийОбъекта(ЮрФизЛицо)
	
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Контрагенты") 
			ИЛИ ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Организации") Тогда
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Функция НовыеСведенияОЮрФизЛице()
	Результат = Новый Структура;
	Результат.Вставить("Представление", "");
	Результат.Вставить("ПолноеНаименование", "");
	Результат.Вставить("СокращенноеНаименование", "");
	Результат.Вставить("НаименованиеДляПечати", "");
	Результат.Вставить("КодПоОКПО", "");
	Результат.Вставить("ИНН", "");
	Результат.Вставить("КПП", "");
	Результат.Вставить("Свидетельство", "");
	Результат.Вставить("СвидетельствоСерияНомер", "");
	Результат.Вставить("СвидетельствоДатаВыдачи", "");
	Результат.Вставить("Телефоны", "");
	Результат.Вставить("Факс", "");
	Результат.Вставить("ПочтовыйАдрес", "");
	Результат.Вставить("ЮридическийАдрес", "");
	Результат.Вставить("ФактическийАдрес", "");
	Результат.Вставить("Банк", "");
	Результат.Вставить("БИК", "");
	Результат.Вставить("КоррСчет", "");
	Результат.Вставить("ТекстКорреспондента", "");
	Результат.Вставить("НомерСчета", "");
	Результат.Вставить("АдресБанка", "");
	Результат.Вставить("ЭлектроннаяПочта", "");
	Результат.Вставить("СтранаРегистрации", "");
	Результат.Вставить("РегистрационныйНомер", "");
	Результат.Вставить("ВидКонтрагента", "");
	Возврат Результат;
КонецФункции

Функция ПодходящийТипДляСбораСведенийОЮрФизЛице(ЮрФизЛицо)

	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Организации") Тогда
		Возврат Истина;
	КонецЕсли;

	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.ФизическиеЛица") Тогда
		Возврат Истина;
	КонецЕсли;

	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Контрагенты") Тогда
		Возврат Истина;
	КонецЕсли;

	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.СтруктурныеЕдиницы") Тогда
		Возврат Истина;
	КонецЕсли;

	Возврат Ложь;

КонецФункции

Функция НовыйТекстЗапросаСведенийОЮрФизЛице(Запрос, ИмяСправочника, ТекБанковскийСчет)
	
	Результат = 
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	Таблица.Представление КАК Наименование,
	|	Таблица.НаименованиеПолное КАК ПолноеНаименование,
	|	"""" КАК СокращенноеНаименование,
	|	"""" КАК СвидетельствоСерияНомер,
	|	"""" КАК СвидетельствоДатаВыдачи,
	|	"""" КАК ИНН,
	|	"""" КАК КПП,
	|	"""" КАК КодПоОКПО,
	|	"""" КАК РегистрационныйНомер,
	|	"""" КАК СтранаРегистрации,
	|	"""" КАК НомерСчета,
	|	"""" КАК ТекстКорреспондента,
	|	"""" КАК Банк,
	|	"""" КАК БИК,
	|	"""" КАК КоррСчет,
	|	"""" КАК ВидКонтрагента,
	|	"""" КАК АдресБанка
	|ИЗ
	|	&ИмяТаблицы КАК Таблица
	|ГДЕ
	|	Таблица.Ссылка = &ПарЮрФизЛицо";
	
	Если ИмяСправочника = "Организации" Тогда
		Результат = СтрЗаменить(Результат, """"" КАК СокращенноеНаименование",
			"Таблица.НаименованиеСокращенное КАК СокращенноеНаименование");
		Результат = СтрЗаменить(Результат, """"" КАК СвидетельствоСерияНомер",
			"Таблица.СвидетельствоСерияНомер КАК СвидетельствоСерияНомер");
		Результат = СтрЗаменить(Результат, """"" КАК СвидетельствоДатаВыдачи",
			"Таблица.СвидетельствоДатаВыдачи КАК СвидетельствоДатаВыдачи");
		Результат = СтрЗаменить(Результат, """"" КАК ИНН", "Таблица.ИНН КАК ИНН");
		Результат = СтрЗаменить(Результат, """"" КАК КПП", "Таблица.КПП КАК КПП");
		Результат = СтрЗаменить(Результат, """"" КАК КодПоОКПО", "Таблица.КодПоОКПО КАК КодПоОКПО");
	КонецЕсли;
	
	Если ИмяСправочника = "Контрагенты" Тогда
		Результат = СтрЗаменить(Результат, """"" КАК РегистрационныйНомер",
			"Таблица.РегистрационныйНомер КАК РегистрационныйНомер");
		Результат = СтрЗаменить(Результат, """"" КАК СтранаРегистрации",
			"Таблица.СтранаРегистрации КАК СтранаРегистрации");
		Результат = СтрЗаменить(Результат, """"" КАК ИНН", "Таблица.ИНН КАК ИНН");
		Результат = СтрЗаменить(Результат, """"" КАК КПП", "Таблица.КПП КАК КПП");
		Результат = СтрЗаменить(Результат, """"" КАК КодПоОКПО", "Таблица.КодПоОКПО КАК КодПоОКПО");
		Результат = СтрЗаменить(Результат, """"" КАК ВидКонтрагента",
		"Таблица.ВидКонтрагента КАК ВидКонтрагента");
	КонецЕсли;
	
	ИмяРеквизитаБанка = ИмяРеквизитаБанка(ТекБанковскийСчет);
	
	Если ЗначениеЗаполнено(ТекБанковскийСчет) Тогда
		Результат = СтрЗаменить(Результат, """"" КАК НомерСчета", "БанковскиеСчета.НомерСчета КАК НомерСчета");
		Результат = СтрЗаменить(Результат, """"" КАК ТекстКорреспондента",
			"БанковскиеСчета.ТекстКорреспондента КАК ТекстКорреспондента");
		Результат = СтрЗаменить(Результат, """"" КАК Банк", СтрШаблон("БанковскиеСчета.%1 КАК Банк", ИмяРеквизитаБанка));
		Результат = СтрЗаменить(Результат, """"" КАК БИК", СтрШаблон("БанковскиеСчета.%1.Код КАК БИК", ИмяРеквизитаБанка));
		Результат = СтрЗаменить(Результат, """"" КАК КоррСчет",
			СтрШаблон("БанковскиеСчета.%1.КоррСчет КАК КоррСчет", ИмяРеквизитаБанка));
		Результат = СтрЗаменить(Результат, """"" КАК АдресБанка",
			СтрШаблон("БанковскиеСчета.%1.Адрес КАК АдресБанка", ИмяРеквизитаБанка));
		Результат = СтрЗаменить(Результат, "&ИмяТаблицы КАК Таблица",
			"&ИмяТаблицы КАК Таблица, Справочник.БанковскиеСчета КАК БанковскиеСчета");
		Результат = СтрЗаменить(Результат, "Таблица.Ссылка = &ПарЮрФизЛицо",
			"Таблица.Ссылка = &ПарЮрФизЛицо И БанковскиеСчета.Ссылка = &ПарБанковскийСчет");
	КонецЕсли;
	
	Результат = СтрЗаменить(Результат, "&ИмяТаблицы", СтрШаблон("Справочник.%1", ИмяСправочника));
	
	Возврат Результат;
	
КонецФункции

Функция ВидыКонтактнойИнформации(ЮрФизЛицо)
	
	Результат = НовыеВидыКонтактнойИнформации();
	
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Организации") Тогда
		Результат.Телефон = Справочники.ВидыКонтактнойИнформации.ТелефонОрганизации;
		Результат.Факс = Справочники.ВидыКонтактнойИнформации.ФаксОрганизации;
		Результат.ЮрАдрес = Справочники.ВидыКонтактнойИнформации.ЮрАдресОрганизации;
		Результат.ФактАдрес = Справочники.ВидыКонтактнойИнформации.ФактАдресОрганизации;
		Результат.ПочтАдрес = Справочники.ВидыКонтактнойИнформации.ПочтовыйАдресОрганизации;
		Результат.ЭлПочта = Справочники.ВидыКонтактнойИнформации.EmailОрганизации;
		Возврат Результат;
	КонецЕсли;
		
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.ФизическиеЛица") Тогда
		Результат.Телефон = Справочники.ВидыКонтактнойИнформации.ТелефонФизЛица;
		Результат.ЮрАдрес = Справочники.ВидыКонтактнойИнформации.АдресПоПропискеФизическиеЛица;
		Результат.ФактАдрес = Справочники.ВидыКонтактнойИнформации.АдресПоПропискеФизическиеЛица;
		Результат.ЭлПочта = Справочники.ВидыКонтактнойИнформации.EmailФизическиеЛица;
		Возврат Результат;
	КонецЕсли;
		
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.Контрагенты") Тогда
		Результат.Телефон = Справочники.ВидыКонтактнойИнформации.ТелефонКонтрагента;
		Результат.Факс = Справочники.ВидыКонтактнойИнформации.ФаксКонтрагента;
		Результат.ЮрАдрес = Справочники.ВидыКонтактнойИнформации.ЮрАдресКонтрагента;
		Результат.ФактАдрес = Справочники.ВидыКонтактнойИнформации.ФактАдресКонтрагента;
		Результат.ПочтАдрес = Справочники.ВидыКонтактнойИнформации.ПочтовыйАдресКонтрагента;
		Результат.ЭлПочта = Справочники.ВидыКонтактнойИнформации.EmailКонтрагента;
		Возврат Результат;
	КонецЕсли;
		
	Если ТипЗнч(ЮрФизЛицо) = Тип("СправочникСсылка.СтруктурныеЕдиницы") Тогда
		Результат.Телефон = Справочники.ВидыКонтактнойИнформации.ТелефонСтруктурнойЕдиницы;
		Результат.ФактАдрес = Справочники.ВидыКонтактнойИнформации.ФактАдресСтруктурнойЕдиницы;
		Возврат Результат;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НовыеВидыКонтактнойИнформации()
	
	Результат = Новый Структура;
	Результат.Вставить("Телефон", Справочники.ВидыКонтактнойИнформации.ПустаяСсылка());
	Результат.Вставить("Факс", Справочники.ВидыКонтактнойИнформации.ПустаяСсылка());
	Результат.Вставить("ЮрАдрес", Справочники.ВидыКонтактнойИнформации.ПустаяСсылка());
	Результат.Вставить("ФактАдрес", Справочники.ВидыКонтактнойИнформации.ПустаяСсылка());
	Результат.Вставить("ПочтАдрес", Справочники.ВидыКонтактнойИнформации.ПустаяСсылка());
	Результат.Вставить("ЭлПочта", Справочники.ВидыКонтактнойИнформации.ПустаяСсылка());
	
	Возврат Результат;
	
КонецФункции

Функция ИмяРеквизитаБанка(ТекБанковскийСчет)
	
	ИмяРеквизитаПоУмолчанию = "Банк";
	
	Если Не ЗначениеЗаполнено(ТекБанковскийСчет) Тогда
		Возврат ИмяРеквизитаПоУмолчанию;
	КонецЕсли;
	
	БанкРасчетов = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ТекБанковскийСчет, "БанкРасчетов", Истина);
	Если Не ЗначениеЗаполнено(БанкРасчетов) Тогда
		Возврат ИмяРеквизитаПоУмолчанию;
	КонецЕсли;
	
	Возврат "БанкРасчетов";
	
КонецФункции

Функция ДобавитьСуммовыеПоля(ПараметрыЗапроса, ТабличнаяЧастьДокумента, ЗначениеПеречисленияТабличныеЧастиДокументов)
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Ссылка", ПараметрыЗапроса.Ссылка);
	Запрос.УстановитьПараметр("КурсДокумента", ПараметрыЗапроса.КурсДокумента);
	Запрос.УстановитьПараметр("КратностьДокумента", ПараметрыЗапроса.КратностьДокумента);
	Запрос.УстановитьПараметр("ТабличнаяЧастьДокумента", ТабличнаяЧастьДокумента);
	Запрос.УстановитьПараметр("ЗначениеПеречисленияТабличныеЧастиДокументов", ЗначениеПеречисленияТабличныеЧастиДокументов);
	
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ТабличнаяЧастьДокумента.НомерСтроки КАК НомерСтроки
	|	,ТабличнаяЧастьДокумента.ПредставлениеНоменклатуры
	|	,ТабличнаяЧастьДокумента.Код
	|	,ТабличнаяЧастьДокумента.Артикул
	|	,ТабличнаяЧастьДокумента.Штрихкод
	|	,ТабличнаяЧастьДокумента.ТипНоменклатуры
	|	,ТабличнаяЧастьДокумента.Склад
	|	,ТабличнаяЧастьДокумента.Ячейка
	|	,ТабличнаяЧастьДокумента.Характеристика
	|	,ТабличнаяЧастьДокумента.Партия
	|	,ТабличнаяЧастьДокумента.ЕдиницаИзмерения
	|	,ТабличнаяЧастьДокумента.ЕдиницаИзмеренияПоОКЕИ_Наименование
	|	,ТабличнаяЧастьДокумента.ЕдиницаИзмеренияПоОКЕИ_Код
	|	,ТабличнаяЧастьДокумента.Цена
	|	,ТабличнаяЧастьДокумента.Сумма
	|	,ТабличнаяЧастьДокумента.СтавкаНДС
	|	,ТабличнаяЧастьДокумента.СуммаНДС
	|	,ТабличнаяЧастьДокумента.Всего
	|	,ТабличнаяЧастьДокумента.Количество
	|	,ТабличнаяЧастьДокумента.КоличествоПоКоэффициенту
	|	,ТабличнаяЧастьДокумента.Содержание
	|	,ТабличнаяЧастьДокумента.ПроцентСкидкиНаценки
	|	,ТабличнаяЧастьДокумента.ЕстьСкидка
	|	,ТабличнаяЧастьДокумента.СуммаАвтоматическойСкидки
	|	,ТабличнаяЧастьДокумента.КлючСвязи
	|	,ТабличнаяЧастьДокумента.Заказ
	|	,ТабличнаяЧастьДокумента.ЭтоРазделитель
	|	,ТабличнаяЧастьДокумента.СуммаБезСкидки
	|ПОМЕСТИТЬ ТабличнаяЧастьДокумента
	|ИЗ &ТабличнаяЧастьДокумента КАК ТабличнаяЧастьДокумента
	|Индексировать ПО НомерСтроки
	|
	|;Выбрать
	|	СтрокиДокумента.НомерСтроки КАК НомерСтроки
	|	,СтрокиДокумента.ПредставлениеНоменклатуры
	|	,СтрокиДокумента.Код
	|	,СтрокиДокумента.Артикул
	|	,СтрокиДокумента.Штрихкод
	|	,СтрокиДокумента.ТипНоменклатуры
	|	,СтрокиДокумента.Склад
	|	,СтрокиДокумента.Ячейка
	|	,СтрокиДокумента.Характеристика
	|	,СтрокиДокумента.Партия
	|	,СтрокиДокумента.ЕдиницаИзмерения
	|	,СтрокиДокумента.ЕдиницаИзмеренияПоОКЕИ_Наименование
	|	,СтрокиДокумента.ЕдиницаИзмеренияПоОКЕИ_Код
	|	,СтрокиДокумента.Цена
	|	,СтрокиДокумента.Сумма
	|	,СтрокиДокумента.СтавкаНДС
	|	,СтрокиДокумента.СуммаНДС
	|	,ЕстьNULL(СуммыДокументовРегламентированныйУчет.НДС, СтрокиДокумента.СуммаНДС * &КурсДокумента / &КратностьДокумента) КАК СуммаНДСВНациональнойВалюте
	|	,СтрокиДокумента.Всего
	|	,ЕстьNULL(СуммыДокументовРегламентированныйУчет.Всего, СтрокиДокумента.Всего * &КурсДокумента / &КратностьДокумента) КАК ВсегоВНациональнойВалюте
	|	,СтрокиДокумента.Количество
	|	,СтрокиДокумента.КоличествоПоКоэффициенту
	|	,СтрокиДокумента.Содержание
	|	,СтрокиДокумента.ПроцентСкидкиНаценки
	|	,СтрокиДокумента.ЕстьСкидка
	|	,СтрокиДокумента.СуммаАвтоматическойСкидки
	|	,СтрокиДокумента.КлючСвязи
	|	,СтрокиДокумента.Заказ
	|	,СтрокиДокумента.ЭтоРазделитель
	|	,СтрокиДокумента.СуммаБезСкидки
	|ИЗ ТабличнаяЧастьДокумента КАК СтрокиДокумента
	|	ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СуммыДокументовРегламентированныйУчет КАК СуммыДокументовРегламентированныйУчет
	|	ПО СуммыДокументовРегламентированныйУчет.Регистратор = &Ссылка
	|		И СуммыДокументовРегламентированныйУчет.ТабличнаяЧастьДокумента = &ЗначениеПеречисленияТабличныеЧастиДокументов
	|		И СтрокиДокумента.НомерСтроки = СуммыДокументовРегламентированныйУчет.НомерСтрокиДокумента
	|УПОРЯДОЧИТЬ ПО НомерСтроки";
	
	// Наборы
	Если ТабличнаяЧастьДокумента.Колонки.Найти("НоменклатураНабора")<>Неопределено Тогда
		Запрос.Текст = СтрЗаменить(
		Запрос.Текст,
		"ПОМЕСТИТЬ ТабличнаяЧастьДокумента", 
		"	,ТабличнаяЧастьДокумента.НоменклатураНабора
		|	,ТабличнаяЧастьДокумента.ХарактеристикаНабора
		|	,ТабличнаяЧастьДокумента.ЭтоНабор
		|	,ТабличнаяЧастьДокумента.НеобходимоВыделитьКакСоставНабора
		|ПОМЕСТИТЬ ТабличнаяЧастьДокумента");
		Запрос.Текст = СтрЗаменить(
		Запрос.Текст,
		"ИЗ ТабличнаяЧастьДокумента КАК СтрокиДокумента", 
		"	,СтрокиДокумента.НоменклатураНабора КАК НоменклатураНабора
		|	,СтрокиДокумента.ХарактеристикаНабора КАК ХарактеристикаНабора
		|	,СтрокиДокумента.ЭтоНабор КАК ЭтоНабор
		|	,СтрокиДокумента.НеобходимоВыделитьКакСоставНабора КАК НеобходимоВыделитьКакСоставНабора
		|ИЗ ТабличнаяЧастьДокумента КАК СтрокиДокумента");
	КонецЕсли; 
	// Конец Наборы
	
	Возврат Запрос.Выполнить().Выгрузить();
	
КонецФункции

Процедура ИсключитьНедоступнуюТаблицуИзТекстаЗапроса(Запрос, ИмяТаблицы)
	
	Если ПравоДоступа("Чтение", Метаданные.Документы[ИмяТаблицы]) Тогда
		Возврат;
	КонецЕсли;
	
	// Привилегированный режим нужен, т.к. при отсутствии доступа схема запроса будет выдавать ошибки.
	УстановитьПривилегированныйРежим(Истина);
	
	СхемаЗапроса = Новый СхемаЗапроса;
	СхемаЗапроса.УстановитьТекстЗапроса(Запрос.Текст);
	
	ИндексыДляУдаления = Новый Соответствие;
	
	Для Каждого ТекПакет Из СхемаЗапроса.ПакетЗапросов Цикл
		
		Для Каждого ТекОператор Из ТекПакет.Операторы Цикл
			
			ИндексыДляУдаления.Очистить();
			
			Для Каждого ТекИсточник Из ТекОператор.Источники Цикл
				Если ТипЗнч(ТекИсточник.Источник) <> Тип("ТаблицаСхемыЗапроса") Тогда
					Продолжить;
				КонецЕсли;
				Если СтрНайти(ТекИсточник.Источник.ИмяТаблицы, ИмяТаблицы) <> 0 Тогда
					ИндексыДляУдаления[ТекПакет.Операторы.Индекс(ТекОператор)] = Истина;
				КонецЕсли;
			КонецЦикла;
			
			// BSLLS:DeletingCollectionItem-off
			// Удаление выполняется для каждого оператора пакета
			Для Каждого ТекИндексДляУдаления Из ИндексыДляУдаления Цикл
				ТекПакет.Операторы.Удалить(ТекИндексДляУдаления.Ключ);
			КонецЦикла;
			// BSLLS:DeletingCollectionItem-on
			
		КонецЦикла;
		
	КонецЦикла;
	
	Запрос.Текст = СхемаЗапроса.ПолучитьТекстЗапроса();
	
КонецПроцедуры

Функция ЗаполнитьТаблицуТоваров(Товары, СтруктураДействий)
	
	ИспользоватьХарактеристикиНоменклатуры = ПолучитьФункциональнуюОпцию("ИспользоватьХарактеристики");
	ИспользоватьПартииНоменклатуры = ПолучитьФункциональнуюОпцию("ИспользоватьПартии");
	ИспользоватьСерииНоменклатуры = ПолучитьФункциональнуюОпцию("ИспользоватьСерииНоменклатуры");
	ИспользоватьSKU = ПолучитьФункциональнуюОпцию("ИспользоватьОбменСПодключаемымОборудованиемOffline");
	ИспользоватьЕдиницыИзмерения = ПолучитьФункциональнуюОпцию("УчетВРазличныхЕдиницахИзмерения"); 
	
	ИсходныеДанные = Новый ТаблицаЗначений;
	ИсходныеДанные.Колонки.Добавить("Номенклатура", Новый ОписаниеТипов("СправочникСсылка.Номенклатура"));
	ИсходныеДанные.Колонки.Добавить("Характеристика", Новый ОписаниеТипов("СправочникСсылка.ХарактеристикиНоменклатуры"));
	ИсходныеДанные.Колонки.Добавить("Количество", Новый ОписаниеТипов("Число"));
	ИсходныеДанные.Колонки.Добавить("Организация", Новый ОписаниеТипов("СправочникСсылка.Организации"));
	ИсходныеДанные.Колонки.Добавить("Партия", Новый ОписаниеТипов("СправочникСсылка.ПартииНоменклатуры"));
	ИсходныеДанные.Колонки.Добавить("Серия", Новый ОписаниеТипов("СправочникСсылка.СерииНоменклатуры"));
	ИсходныеДанные.Колонки.Добавить("ЕдиницаИзмерения", Новый ОписаниеТипов("СправочникСсылка.ЕдиницыИзмерения"));
	
	Для Каждого Товар Из Товары Цикл
		Строка = ИсходныеДанные.Добавить();
		Строка.Номенклатура = Товар.Номенклатура;
		Строка.Количество = Товар.Количество;
		Строка.Характеристика = Товар.Характеристика;
		Строка.Организация = Товар.Организация;
		Строка.Партия = Товар.Партия;
		Строка.Серия = Товар.Серия;
		Строка.ЕдиницаИзмерения = Товар.ЕдиницаИзмерения;
	КонецЦикла;
	
	НастройкиПечати = Обработки.ПечатьЭтикетокИЦенников.НовыеНастройкиПечати();
	
	НастройкиПечати.ОбязательныеПоля.Добавить("Цена");
	НастройкиПечати.ОбязательныеПоля.Добавить("Весовой");
	НастройкиПечати.ОбязательныеПоля.Добавить("Штрихкод");
	НастройкиПечати.ОбязательныеПоля.Добавить("Количество");
	НастройкиПечати.ОбязательныеПоля.Добавить("Номенклатура");
	Если ИспользоватьSKU Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("SKU");
	КонецЕсли;
	Если ИспользоватьХарактеристикиНоменклатуры Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("Характеристика");
	КонецЕсли;
	Если ИспользоватьПартииНоменклатуры Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("Партия");
	КонецЕсли;
	Если ИспользоватьСерииНоменклатуры Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("Серия");
	КонецЕсли;
	Если ИспользоватьЕдиницыИзмерения Тогда
		НастройкиПечати.ОбязательныеПоля.Добавить("ЕдиницаИзмерения");
	КонецЕсли;
	НастройкиПечати.ОбязательныеПоля.Добавить("ОстатокНаСкладе");
	НастройкиПечати.ОбязательныеПоля.Добавить("Организация");
	
	НастройкиПечати.ОбязательныеПоля.Добавить("НоменклатураНаименованиеПолное");
	
	// Шаблоны этикеток и ценников.
	НастройкиПечати.ОбязательныеПоля.Добавить("Номенклатура.КатегорияНоменклатуры.ШаблонЭтикетки");
	НастройкиПечати.ОбязательныеПоля.Добавить("Номенклатура.КатегорияНоменклатуры.ШаблонЦенника");
	ПолеСКДШаблонЦенникаБПО = "Номенклатура.КатегорияНоменклатуры.ШаблонЦенникаБПО";
	НастройкиПечати.ОбязательныеПоля.Добавить(ПолеСКДШаблонЦенникаБПО);
	
	ТекущийПользователь = Пользователи.ТекущийПользователь();
	ВидЦены = УправлениеНебольшойФирмойПовтИсп.ПолучитьЗначениеПоУмолчаниюПользователя(ТекущийПользователь, "ОсновнойВидЦенПродажи");
	ДатаЦены = ТекущаяДата();
	
	НастройкиПечати.ПараметрыДанных.Вставить("ВидЦен"    , ВидЦены);
	НастройкиПечати.ПараметрыДанных.Вставить("ВидЦенАкционный"    , ВидЦены);
	НастройкиПечати.ПараметрыДанных.Вставить("ЦеныНаДату" , ДатаЦены);
	НастройкиПечати.ПараметрыДанных.Вставить("ЦеныНаДатуАкционные" , ДатаЦены);
	
	НастройкиПечати.ПараметрыДанных.Вставить("ИспользоватьАссортимент", Ложь);
	
	Компоновщик = Неопределено;
	СтруктураДействий.Свойство("КомпоновщикНастроек", Компоновщик);
	Если Компоновщик = Неопределено Тогда
		СхемаКомпоновкиДанных = Обработки.ПечатьЭтикетокИЦенников.ПолучитьМакет("ПоляШаблонаПоУмолчанию");
		Компоновщик = Новый КомпоновщикНастроекКомпоновкиДанных;
		АдресСКД = ПоместитьВоВременноеХранилище(СхемаКомпоновкиДанных);
		Источник = Новый ИсточникДоступныхНастроекКомпоновкиДанных(АдресСКД);
		Компоновщик.Инициализировать(Источник);
		Компоновщик.ЗагрузитьНастройки(СхемаКомпоновкиДанных.НастройкиПоУмолчанию);
		Компоновщик.Восстановить(СпособВосстановленияНастроекКомпоновкиДанных.ПроверятьДоступность);
	КонецЕсли;
	НастройкиПечати.КомпоновщикНастроек = Компоновщик;
	НастройкиПечати.ИмяМакетаСхемыКомпоновкиДанных = "ПоляШаблонаПоУмолчанию";
	НастройкиПечати.ИсходныеДанные = ИсходныеДанные;
	
	Если ИспользоватьSKU Тогда
		НастройкиПечати.ИсходныеДанные.Колонки.Добавить("ШтрихАвтопроверки", Новый ОписаниеТипов("Строка",, Новый КвалификаторыСтроки(500)));
	КонецЕсли;
	
	// Загрузка сформированного списка товаров.
	ДанныеДляПечати = Обработки.ПечатьЭтикетокИЦенников.ПодготовитьДанныеДляПечати(НастройкиПечати);
	
	Если Не ЗначениеЗаполнено(СтруктураДействий.ЗаполнитьОрганизацию) Тогда
		СтруктураДействий.ЗаполнитьОрганизацию = УправлениеНебольшойФирмойПовтИсп.ОрганизацияПоУмолчанию();
	КонецЕсли;
	
	ТоварыДляПечати = Новый Массив;
	Для Каждого Товар Из ДанныеДляПечати.ТаблицаТоваров Цикл
		СтруктураТовара = ПечатьЭтикетокИЦенниковБПОКлиентСервер.ТоварДляПечати();
		СтруктураТовара.НоменклатураБПО = Товар.Номенклатура;
		Если ИспользоватьПартииНоменклатуры Тогда
			СтруктураТовара.Вставить("Партия", Товар.Партия);
		КонецЕсли;
		Если ИспользоватьСерииНоменклатуры Тогда
			СтруктураТовара.Вставить("Серия", Товар.Серия);
		КонецЕсли;
		Если ИспользоватьХарактеристикиНоменклатуры Тогда
			СтруктураТовара.ХарактеристикаБПО = Товар.Характеристика;
		КонецЕсли;
		Если ИспользоватьЕдиницыИзмерения Тогда
			СтруктураТовара.УпаковкаБПО       = Товар.ЕдиницаИзмерения;
		КонецЕсли;
		СтруктураТовара.ОрганизацияБПО = СтруктураДействий.ЗаполнитьОрганизацию;
		СтруктураТовара.Штрихкод = Товар.Штрихкод;
		СтруктураТовара.Цена = Товар.Цена;
		Если ИспользоватьSKU Тогда
			СтруктураТовара.Вставить("SKU", Товар.SKU);
			СтруктураТовара.Вставить("ШтрихАвтопроверки", СформироватьСобственныйШтрихкод(СтруктураТовара));
		КонецЕсли;
		Если СтруктураДействий.Свойство("ЗаполнитьКоличествоЭтикетокПоДокументу") Тогда
			СтруктураТовара.Вставить("Количество", Товар.Количество);
		Иначе
			СтруктураТовара.Вставить("Количество", 1);
		КонецЕсли;
		КолонкаШаблон = ДанныеДляПечати.СоответствиеПолейСКДКолонкамТаблицыТоваров.Получить(ПолеСКДШаблонЦенникаБПО);
		СтруктураТовара.Вставить("ШаблонЦенникаЭтикетки", Товар[КолонкаШаблон]);
		
		ТоварыДляПечати.Добавить(СтруктураТовара);
	КонецЦикла;
	
	Возврат ТоварыДляПечати;
	
КонецФункции

Функция ТаблицаЗначенийТоваров(СтандартнаяОбработка, ТаблицаЗначенийТоваров) Экспорт
	
	ТаблицаЗначенийТоваров.Колонки.Добавить("Партия", Новый ОписаниеТипов("СправочникСсылка.ПартииНоменклатуры"));
	ТаблицаЗначенийТоваров.Колонки.Добавить("Серия", Новый ОписаниеТипов("СправочникСсылка.СерииНоменклатуры"));
	ТаблицаЗначенийТоваров.Колонки.Добавить("Штрихавтопроверки", Новый ОписаниеТипов("Строка"));
	
КонецФункции

Функция ДобавитьЗначокНомераКПредставлению(ОснованиеПечати)
	
	Источник = ОснованиеПечати.Метаданные().Имя;
	ПустаяСсылкаОбъекта = Документы[Источник].ПустаяСсылка();
	ПредставленияОбъектовМетаданных = ДокументыУНФКлиентСерверПовтИсп.ПредставленияОбъектовМетаданных();
	ПредставлениеМетаданных = ПредставленияОбъектовМетаданных[ПустаяСсылкаОбъекта];
	ПредставлениеДокумента = Строка(ОснованиеПечати);
	ШаблонПредставления = НСтр("ru = '%1 №'");
	НовоеПредставлениеМетаданных = СтрШаблон(ШаблонПредставления, ПредставлениеМетаданных);
	ИтоговаяСтрока = СтрЗаменить(ПредставлениеДокумента, ПредставлениеМетаданных + " ", НовоеПредставлениеМетаданных);
	
	Возврат ИтоговаяСтрока;
	
КонецФункции

Процедура НастроитьФормуПечатьДокументов(Форма);
	
	СписокОбъектов = Форма.ОбъектыПечати;
	Организация = ПолучитьОрганизациюОбъектов(СписокОбъектов);
	ИмяРеквизитаОрганизация = "Организация";
	НовыеРеквизитыФормы = Новый Массив;
	РеквизитФормы = Новый РеквизитФормы(ИмяРеквизитаОрганизация,
		Новый ОписаниеТипов("СправочникСсылка.Организации"),
		,
		НСтр("ru = 'Организация'"));
	НовыеРеквизитыФормы.Добавить(РеквизитФормы);
	Форма.ИзменитьРеквизиты(НовыеРеквизитыФормы);
	Если Не Организация.Пустая() Тогда
		Форма[ИмяРеквизитаОрганизация] = Организация;
	КонецЕсли;
	
КонецПроцедуры

// Возвращает организацию списка документов. Если организации различаются, то возвращает пустую ссылку
//
Функция ПолучитьОрганизациюОбъектов(СписокОбъектов)
	
	ОрганизацииОбъектов = Новый Массив;
	Для Каждого ОбъектПечати Из СписокОбъектов Цикл
		
		Если ТипЗнч(ОбъектПечати.Значение) = Тип("СправочникСсылка.Организации") Тогда
			ОрганизацииОбъектов.Добавить(ОбъектПечати.Значение);
			Продолжить;
		КонецЕсли;
		
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ОбъектПечати.Значение, "Организация") Тогда
			Если ОрганизацииОбъектов.Найти(ОбъектПечати.Значение.Организация) = Неопределено Тогда
				ОрганизацииОбъектов.Добавить(ОбъектПечати.Значение.Организация);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;

	Результат = Справочники.Организации.ПустаяСсылка();
	
	Если ОрганизацииОбъектов.Количество() = 1 Тогда
		Результат = ОрганизацииОбъектов[0];
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЕстьПечатныеФормыСПодписьюИПечатью(НастройкиПечатныхФорм)
	
	Результат = Ложь;
	Для Каждого НастройкаПечатнойФормы Из НастройкиПечатныхФорм Цикл
		Результат = Результат Или НастройкаПечатнойФормы.ПодписьИПечать;
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

Процедура ОформитьФлажокПодписиИПечати(Форма)
	
	ЭлементФлагПодписьИПечать =  Форма.Элементы.Найти("ФлажокПодписьИПечать");
	Если ЭлементФлагПодписьИПечать = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	// Флажок всегда показываем для поддерживаемых документов, чтобы пользователь знал о возможности вывести факсимиле
	Если Не ЭлементФлагПодписьИПечать.Видимость И ЕстьПечатныеФормыСПодписьюИПечатью(Форма.НастройкиПечатныхФорм) Тогда
		 ЭлементФлагПодписьИПечать.Видимость = Истина;
		 ЭлементФлагПодписьИПечать.Доступность = Ложь;
		 Форма.ПодписьИПечать = Ложь;
	КонецЕсли;
	
	ГруппаПодписьИПечать = Форма.Элементы.Вставить("ГруппаПодписьИПечатьУНФ", Тип("ГруппаФормы"),
		Форма.Элементы.ГруппаКоманднаяПанель, Форма.Элементы.КоманднаяПанельПраваяЧасть);
	ГруппаПодписьИПечать.Вид                      = ВидГруппыФормы.ОбычнаяГруппа;
	ГруппаПодписьИПечать.ОтображатьЗаголовок      = Ложь;
	ГруппаПодписьИПечать.Отображение              = ОтображениеОбычнойГруппы.Нет;
	ГруппаПодписьИПечать.ЦветФона                 = ЦветаСтиля.БыстрыеНастройкиФонГруппы;
	
	Форма.Элементы.Переместить(ЭлементФлагПодписьИПечать, ГруппаПодписьИПечать);
	
	ТекстПодсказки = ТекстПодсказкиПодписьИПечать(Форма);
	ЭлементФлагПодписьИПечать.РасширеннаяПодсказка.Заголовок = ТекстПодсказки;
	ЭлементФлагПодписьИПечать.РасширеннаяПодсказка.УстановитьДействие("ОбработкаНавигационнойСсылки",
		 "Подключаемый_ОбработкаНавигационнойСсылки");
	
КонецПроцедуры

Функция ТекстПодсказкиПодписьИПечать(Форма)
	
	ОрганизацииОбъектов = Новый Массив;
	Для Каждого ОбъектПечати Из Форма.ОбъектыПечати Цикл
		Если ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(ОбъектПечати.Значение, "Организация") Тогда
			Если ОрганизацииОбъектов.Найти(ОбъектПечати.Значение.Организация) = Неопределено Тогда
				ОрганизацииОбъектов.Добавить(ОбъектПечати.Значение.Организация);
			КонецЕсли;
		КонецЕсли;
	КонецЦикла;
	
	Если ОрганизацииОбъектов.Количество() = 1 Тогда
		СтрокаЗаголовок = Новый ФорматированнаяСтрока(НСтр(
			"ru = 'Заполнить факсимиле подписей и печати можно в карточке'"));
		СтрокаСсылка =  Новый ФорматированнаяСтрока(НСтр("ru = 'организации'"), , , , "ФаксимилеОрганизации");
		ТекстПодсказки = Новый ФорматированнаяСтрока(СтрокаЗаголовок, " ", СтрокаСсылка, ".");
	Иначе
		ТекстПодсказки = НСтр("ru = 'Заполнить факсимиле подписей и печати можно в карточке организации.'");
	КонецЕсли;
	
	Возврат ТекстПодсказки;
	
КонецФункции

Функция ПолучитьПодписиТипа(ТипДанных)
	
	Результат = Новый Массив;
	
	РеквизитыОбъектаМетаданных = Метаданные.НайтиПоТипу(ТипДанных).Реквизиты;
	Для Каждого Реквизит Из РеквизитыОбъектаМетаданных Цикл
		Если Реквизит.Тип = Новый ОписаниеТипов("СправочникСсылка.Подписи") Тогда
			Результат.Добавить(Реквизит.Имя);
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ЕстьРеквизитОрганизация(ТипДанных)
	
	Если ТипДанных = Тип("СправочникСсылка.Организации") Тогда
		Возврат Истина;
	КонецЕсли;
	
	МетаданныеОбъекта = Метаданные.НайтиПоТипу(ТипДанных);
	Возврат ОбщегоНазначения.ЕстьРеквизитОбъекта("Организация", МетаданныеОбъекта);
	
КонецФункции

Процедура ДополнитьПодписиФаксимилеПечати(Подписи, СсылкаНаОбъект)
	
	Если ТипЗнч(СсылкаНаОбъект) = Тип("СправочникСсылка.Организации") Тогда
		Организация = СсылкаНаОбъект;
	Иначе
		Организация = СсылкаНаОбъект.Организация;
	КонецЕсли;
	
	ФаксимилеПечати = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Организация, "ФайлФаксимильнаяПечать");
	Если ЗначениеЗаполнено(ФаксимилеПечати) Тогда
		ДанныеКартинки = РаботаСФайлами.ДанныеФайла(ФаксимилеПечати);
		ДвоичныеДанныеКартинки = ПолучитьИзВременногоХранилища(ДанныеКартинки.СсылкаНаДвоичныеДанныеФайла);
		Если ДвоичныеДанныеКартинки <> Неопределено Тогда
			КартинкаФаксимилеПодписанта = Новый Картинка(ДвоичныеДанныеКартинки);
			Подписи.Вставить("ПечатьОрганизации", КартинкаФаксимилеПодписанта);
			Подписи.Вставить("ПечатьПредпринимателя", КартинкаФаксимилеПодписанта);
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

Процедура ОбновитьСКДШаблоновЭтикеток(Параметры) Экспорт
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	ШаблоныЭтикетокИЦенниковБПО.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.ШаблоныЭтикетокИЦенниковБПО КАК ШаблоныЭтикетокИЦенниковБПО
	|ГДЕ
	|	НЕ ШаблоныЭтикетокИЦенниковБПО.Предопределенный");
	РезультатЗапроса = Запрос.Выполнить();
	
	Если РезультатЗапроса.Пустой() Тогда
		Параметры.ОбработкаЗавершена = Истина;
		Возврат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	Пока Выборка.Следующий() Цикл
		
		ШаблонОбъект = Выборка.Ссылка.ПолучитьОбъект();
		
		Попытка
			ШаблонОбъект.Заблокировать();
			СхемаКомпоновкиДанных = МакетСхемыКомпоновкиДанных(ШаблонОбъект.ТипЦенника);
			ШаблонОбъект.СхемаКомпоновкиДанных = Новый ХранилищеЗначения(СхемаКомпоновкиДанных);
			ОбновлениеИнформационнойБазы.ЗаписатьОбъект(ШаблонОбъект);
		Исключение
			ИнформацияОбОшибке = ИнформацияОбОшибке();
			ПодробноеПредставлениеОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
			КодЯзыка = ОбщегоНазначения.КодОсновногоЯзыка();
			Событие = НСтр("ru = 'Ошибка выполнения обработчика обновления СКД в шаблонах этикеток и ценников'", КодЯзыка);
			ЗаписьЖурналаРегистрации(
				Событие, УровеньЖурналаРегистрации.Ошибка,, ШаблонОбъект, ПодробноеПредставлениеОшибки);
			Параметры.ОбработкаЗавершена = Ложь;
			Возврат;
		КонецПопытки;
		
		Параметры.ОбработкаЗавершена = Истина;
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти


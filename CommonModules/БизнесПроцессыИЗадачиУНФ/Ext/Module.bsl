
#Область ПрограммныйИнтерфейс

// См. БизнесПроцессыИЗадачиПереопределяемый.ПриНачальномЗаполненииРолейИсполнителей
Процедура ПриНачальномЗаполненииРолейИсполнителей(КодыЯзыков, Элементы, ТабличныеЧасти) Экспорт
	
	Реквизиты = Новый Структура;
	Реквизиты.Вставить("ИспользуетсяБезОбъектовАдресации", Ложь);
	Реквизиты.Вставить("ИспользуетсяСОбъектамиАдресации",  Истина);
	Реквизиты.Вставить("ТипыОсновногоОбъектаАдресации", ТипыОсновногоОбъектаАдресации());
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаСправкиНДФЛ",
		"ru = 'Справки 2-НДФЛ'", Реквизиты); // @НСтр-1
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаИзменениеЛичныхДанных",
		"ru = 'Изменение личных данных'", Реквизиты); // @НСтр-1
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаСправкиСМестаРаботы",
		"ru = 'Справки с места работы'", Реквизиты); // @НСтр-1
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаЗаявленияНаОтпуск",
		"ru = 'Заявления на отпуск'", Реквизиты); // @НСтр-1
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаСправкиОбОстаткахОтпусков",
		"ru = 'Справки об остатках отпусков'", Реквизиты); // @НСтр-1
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаНалоговыеВычеты",
		"ru = 'Налоговые вычеты'", Реквизиты); // @НСтр-1
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаСогласованиеОтсутствий",
		"ru = 'Согласование отсутствий'", Реквизиты); // @НСтр-1
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаУдержаниеДСВ",
		"ru = 'Удержание добровольных страховых взносов'", Реквизиты); // @НСтр-1
	
	ОписатьПредопределенный(КодыЯзыков, Элементы, "ОтветственныйЗаОбращенияСотрудников",
		"ru = 'Обращения сотрудников'", Реквизиты); // @НСтр-1
	
КонецПроцедуры

// См. БизнесПроцессыИЗадачиПереопределяемый.ПриНачальномЗаполненииОбъектовАдресацииЗадач
Процедура ПриНачальномЗаполненииОбъектовАдресацииЗадач(КодыЯзыков, Элементы, ТабличныеЧасти) Экспорт
	
	Элемент = ОписатьПредопределенный(КодыЯзыков, Элементы, "Организации",
		"ru = 'Организации'"); // @НСтр-1
	Элемент.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.Организации");
	
	Элемент = ОписатьПредопределенный(КодыЯзыков, Элементы, "ПодразделенияОрганизаций",
		"ru = 'Подразделения организаций'"); // @НСтр-1
	Элемент.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.ПодразделенияОрганизаций");
	
	Элемент = ОписатьПредопределенный(КодыЯзыков, Элементы, "СтруктураПредприятия",
		"ru = 'Структура предприятия'"); // @НСтр-1
	Элемент.ТипЗначения = Новый ОписаниеТипов("СправочникСсылка.СтруктураПредприятия");
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция ТипыОсновногоОбъектаАдресации(Знач ПубликоватьСтруктуруЮридическихЛиц = Неопределено)
	
	ТипыОсновногоОбъектаАдресации = Неопределено;
	
	Если ПолучитьФункциональнуюОпцию("ИспользоватьБизнесПроцессыЗаявокСотрудников") Тогда
		Если ПубликоватьСтруктуруЮридическихЛиц = Неопределено Тогда
			ПубликоватьСтруктуруЮридическихЛиц = ИнтеграцияУправлениеПерсоналом.ПубликоватьСтруктуруЮридическихЛиц()
		КонецЕсли;
		Если ПубликоватьСтруктуруЮридическихЛиц Тогда
			ТипыОсновногоОбъектаАдресации = ПланыВидовХарактеристик.ОбъектыАдресацииЗадач.ПодразделенияОрганизаций;
		Иначе
			ТипыОсновногоОбъектаАдресации = ПланыВидовХарактеристик.ОбъектыАдресацииЗадач.СтруктураПредприятия;
		КонецЕсли;
	КонецЕсли;
	
	Возврат ТипыОсновногоОбъектаАдресации;
	
КонецФункции

Функция ОписатьПредопределенный(КодыЯзыков, Элементы, ИмяПредопределенныхДанных, Наименование, Реквизиты = Неопределено)
	Элемент = Элементы.Добавить();
	Элемент.ИмяПредопределенныхДанных = ИмяПредопределенныхДанных;
	МультиязычностьСервер.ЗаполнитьМультиязычныйРеквизит(Элемент, "Наименование", Наименование, КодыЯзыков);
	Если Реквизиты <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(Элемент, Реквизиты);
	КонецЕсли;
	Возврат Элемент;
КонецФункции

#КонецОбласти

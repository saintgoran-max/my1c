
#Область ПрограммныйИнтерфейс

// В процедуре вызывается вызов процедуры отписки от новостей
//  вызовы процедур и функций, соответственно новости с помощью гиперссылок могут запускать эти процедуры / функции.
//
// Параметры:
//  ЛентаИлиНовость    - СправочникСсылка.Новости - Ссылка на новость;
//  ИмяПользователяИБ  - строка - имя пользователя
//
// Возвращаемое значение:
//  Структура 
//    * Успешно - Булево - успешно выполненной операции;
//    * ЛентаНовостей - СправочникСсылка.ЛентаНовостей - лента новостей;
//    * ПредставлениеЛентыНовостей - Строка - представление ленты новости;
//
Функция ОтписатьсяОтЛентыНовостей(ЛентаИлиНовость, ИмяПользователяИБ = "") Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Успешно", Ложь);
	Результат.Вставить("ЛентаНовостей", Неопределено);
	Результат.Вставить("ПредставлениеЛентыНовостей", "");
	
	Если Не ЗначениеЗаполнено(ЛентаИлиНовость) Тогда
		Возврат Результат;
	КонецЕсли;
	
	ЛентаНовостей = ЛентаИлиНовость;
	
	Если ТипЗнч(ЛентаИлиНовость) = Тип("СправочникСсылка.Новости") Тогда
		ЛентаНовостей = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ЛентаИлиНовость, "ЛентаНовостей");
	КонецЕсли;
	
	Результат.ЛентаНовостей = ЛентаНовостей;
	Результат.ПредставлениеЛентыНовостей = Строка(ЛентаНовостей);
	
	МассивОтключенныхЛентНовостей = ХранилищаНастроек.НастройкиНовостей.Загрузить(
		"ОтключенныеЛентыНовостей",
		"",
		,
		ИмяПользователяИБ);
	
	Если ТипЗнч(МассивОтключенныхЛентНовостей) <> Тип("Массив") Тогда
		МассивОтключенныхЛентНовостей = Новый Массив;
	КонецЕсли;
	
	Если МассивОтключенныхЛентНовостей.Найти(ЛентаНовостей) = Неопределено Тогда
		
		МассивОтключенныхЛентНовостей.Добавить(ЛентаНовостей);
		
		ХранилищаНастроек.НастройкиНовостей.Сохранить(
			"ОтключенныеЛентыНовостей",
			"",
			МассивОтключенныхЛентНовостей,
			,
			ИмяПользователяИБ);
		
		Результат.Успешно = Истина;
		Возврат Результат;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Проверяет, является ли текущий или указанный пользователь полноправным.
// 
// Возвращаемое значение:
//  Булево - если Истина, пользователь является полноправным.
//
Функция ЭтоПолноправныйПользователь() Экспорт
	
	Возврат Пользователи.ЭтоПолноправныйПользователь();
	
КонецФункции

// Возвращает возможность работы с новостями в текущей области
// 
// Возвращаемое значение:
//  Булево
//
Функция РазрешенаРаботаСНовостямиВТекущейОбласти() Экспорт
	
	Возврат ПолучитьФункциональнуюОпцию("ИспользоватьРаботуСНовостями");
	
КонецФункции

#КонецОбласти
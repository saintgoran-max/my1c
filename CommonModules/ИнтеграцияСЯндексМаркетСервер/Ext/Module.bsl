#Область ПрограммныйИнтерфейс

// См. РегламентныеЗаданияПереопределяемый.ПриОпределенииНастроекРегламентныхЗаданий.
Процедура ПриОпределенииНастроекРегламентныхЗаданий(Настройки) Экспорт
	
	
КонецПроцедуры

// Функция получает данные авторизации
//
// Параметры:
//  СтруктураОтвета  - Структура - структура содержит набор ключей, которые необходимо вернуть.
//
// Возвращаемое значение:
//  СтруктураДанныхАвторизации  - Структура - структура содержит запрошенный набор ключей.
//
Функция ДанныеАвторизацииИзОтветаЯндексаПолучить(УчетнаяЗапись, СтруктураОтвета) Экспорт
	
	СтруктураДанныхАвторизации = Новый Структура;
	
	Если СтруктураОтвета.Свойство("access_token")
	И СтруктураОтвета.Свойство("expires_in")
	И СтруктураОтвета.Свойство("refresh_token") Тогда
		
		expires_in = СтруктураОтвета.expires_in;
		
		Если ЭтаСтрокаЯвляетсяЦелымНеотрицательнымЧислом(expires_in) Тогда
			СрокЖизниТокена = НачалоДня(ТекущаяДатаСеанса() + Число(expires_in));
			СтруктураДанныхАвторизации.Вставить("access_token", СтруктураОтвета.access_token);
			СтруктураДанныхАвторизации.Вставить("access_token_expires", СрокЖизниТокена);
			СтруктураДанныхАвторизации.Вставить("refresh_token", СтруктураОтвета.refresh_token);
			
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( "Данные авторизации Яндекс маркет получены.", УчетнаяЗапись );
			
		Иначе
			ТекстОшибки = НСтр("ru = 'Ошибка получения данных авторизации Яндекс маркет: Некорректное значение свойства expires_in:'") + " " + СтруктураОтвета.expires_in;
			ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
			ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки );
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );
		КонецЕсли;
	КонецЕсли;
	
	Возврат СтруктураДанныхАвторизации;
	
КонецФункции

// Функция возвращает HTTPЗапрос запрос для обновления ключей доступа к сервису для учетную запись.
//
// Параметры:
//  УчетнаяЗапись  - СправочникСсылка.УчетныеЗаписиМаркетплейсов - ссылка на учетную запись, для которой запрашиваются ключи.
//
// Возвращаемое значение:
//  HTTPЗапрос  - HTTPЗапрос - HTTPЗапрос для получения ключей.
//
Функция ЗапросОбновитьТокеныДоступа(УчетнаяЗапись) Экспорт
	
	ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
	client_id = ЯндексПриложениеДанные.client_id;
	client_secret = ЯндексПриложениеДанные.client_secret;
	
	СтруктураДанныхАвторизации = ЯндексМаркетДанныеАвторизацииПолучить(УчетнаяЗапись);
	
	Если СтруктураДанныхАвторизации <> Неопределено Тогда
		refresh_token = СтруктураДанныхАвторизации.refresh_token;	
	КонецЕсли;
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/x-www-form-urlencoded");	
	
	ПараметрыURL = Новый Массив;
	ПараметрыURL.Добавить("grant_type=refresh_token");
	ПараметрыURL.Добавить("refresh_token" + refresh_token);
	ПараметрыURL.Добавить("client_id=" + client_id);
	ПараметрыURL.Добавить("client_secret=" + client_secret);
	
	Адрес = "/token?"; 
	HTTPЗапрос = Новый HTTPЗапрос( Адрес, Заголовки );
	Тело = СтрСоединить( ПараметрыURL, "&");
	
	HTTPЗапрос.УстановитьТелоИзСтроки( Тело, "windows-1251" );
	
	Возврат HTTPЗапрос;
	
КонецФункции

// Функция возвращает имя сервера авторизации для обновления ключей доступа к сервису.
//
// Возвращаемое значение:
//  Сервер  - строка - имя сервера сервиса
//
Функция СерверАвторизацииЯндекс() Экспорт
	
	Сервер = "oauth.yandex.ru";	
	
	Возврат  Сервер;
	
КонецФункции

// Функция возвращает имя сервера партнерского API.
//
// Возвращаемое значение:
//  Сервер  - строка - имя сервера сервиса
//
Функция СерверПартнерскогоAPIЯндексМаркет() Экспорт
	Возврат "api.partner.market.yandex.ru";
КонецФункции

// Функция возвращает url авторизации.
//
// Возвращаемое значение:
//  Сервер  - строка - 
//
Функция АдресАвторизацииЯндекс() Экспорт

	ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
	client_id = ЯндексПриложениеДанные.client_id;
	client_secret = ЯндексПриложениеДанные.client_secret;

	Адрес = "https://oauth.yandex.ru/authorize?response_type=code&client_id=" + client_id + "&client_secret=" + client_secret + "&redirect_uri=" + "https://oauth.yandex.ru/verification_code";		
	Возврат Адрес;
	
КонецФункции

Функция ЗапросПолучитьТокеныПоКоду(ВременныйКод) Экспорт
	
	ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
	client_id = ЯндексПриложениеДанные.client_id;
	client_secret = ЯндексПриложениеДанные.client_secret;
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/x-www-form-urlencoded");	
	
	ПараметрыURL = Новый Массив;
	ПараметрыURL.Добавить( "grant_type=authorization_code" );
	ПараметрыURL.Добавить( "code=" + ВременныйКод );
	ПараметрыURL.Добавить( "client_id=" + client_id );
	ПараметрыURL.Добавить( "client_secret=" + client_secret );
	
	Адрес = "/token?"; 
	HTTPЗапрос = Новый HTTPЗапрос( Адрес, Заголовки );
	Тело = СтрСоединить( ПараметрыURL, "&" );
	
	HTTPЗапрос.УстановитьТелоИзСтроки( Тело, "windows-1251" );
	
	Возврат HTTPЗапрос;
	
КонецФункции

// Проверяет установку соединения с маркетплейсом.
//
// Параметры:
//  УчетнаяЗаписьМаркетплейса - СправочникСсылка.УчетныеЗаписиМаркетплейсов или Структура - Учетная запись или данные, подключение к котороым проверяется.
// 
// Возвращаемое значение:
//  Структура - Содержит свойства КодСостояния и ТекстОшибки.
//
Функция ПроверитьСвязьСМаркетплейсом( УчетнаяЗапись ) Экспорт
    
    Результат = Новый Структура("КодСостояния, ТекстОшибки", 0, "");
    
	Сервер = ИнтеграцияСЯндексМаркетСервер.СерверПартнерскогоAPIЯндексМаркет();
	ИмяМетода = "/campaigns";
	Адрес = ИмяМетода;
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/x-www-form-urlencoded");
	ДанныеАвторизации = Новый Структура( "api_key", УчетнаяЗапись.КлючAPI );
	HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗапись, Заголовки, ДанныеАвторизации );
	
	ЗащищенноеСоединение = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение();
	ИнтернетПрокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси( "HTTPS" );
	HTTPСоединение = Новый HTTPСоединение( Сервер,,,, ИнтернетПрокси,, ЗащищенноеСоединение );
	
	HTTPЗапрос = Новый HTTPЗапрос( Адрес, Заголовки );
	Попытка
		HTTPОтвет = HTTPСоединение.ВызватьHTTPМетод( "GET", HTTPЗапрос );
	Исключение 
		ТекстОшибки = НСтр("ru = 'ПроверитьСвязьСМаркетплейсом: Ошибка выполнения запроса: '") + " " + ПодробноеПредставлениеОшибки( ИнформацияОбОшибке() );
		ЗаписьЖурналаРегистрации(ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
		
		Результат.ТекстОшибки = ТекстОшибки;
		Результат.КодСостояния = -1;
		
		Возврат Результат;
	КонецПопытки;	
	Результат.КодСостояния = HTTPОтвет.КодСостояния;

	Если Результат.КодСостояния = 200 Тогда
		Результат.ТекстОшибки = "OK";
	ИначеЕсли Результат.КодСостояния = 301 Тогда
		Результат.ТекстОшибки = "Сайт перемещен";
	ИначеЕсли Результат.КодСостояния = 400 Тогда
		Результат.ТекстОшибки = "Ошибочный запрос";
	ИначеЕсли Результат.КодСостояния = 401 Тогда
		Результат.ТекстОшибки = "Ошибка авторизации";
	ИначеЕсли Результат.КодСостояния = 403 Тогда
		Результат.ТекстОшибки = "Нет доступа";
	ИначеЕсли Результат.КодСостояния = 404 Тогда
		Результат.ТекстОшибки = "Ресурс не найден";
	ИначеЕсли Результат.КодСостояния = 409 Тогда
		Результат.ТекстОшибки = "Конфликт";
	ИначеЕсли Результат.КодСостояния = 500 Тогда
		Результат.ТекстОшибки = "Внутренняя ошибка сервера маркетплейса";
	КонецЕсли;

	Возврат Результат;

КонецФункции


Функция ЗаписатьНастройкиАвторизации(УчетнаяЗапись, ДанныеАвторизации) Экспорт
	
	Владелец = УчетнаяЗапись;
	
	Если ДанныеАвторизации.Свойство( "access_token_expires" ) Тогда
		expires_in = ДанныеАвторизации.access_token_expires;
	ИначеЕсли ДанныеАвторизации.Свойство( "expires_in" ) Тогда
		expires_in = ДанныеАвторизации.expires_in;
	Иначе
		expires_in = Неопределено;
	КонецЕсли;
	
	Если ДанныеАвторизации.Свойство("access_token")
	И ЗначениеЗаполнено( expires_in )
	И ДанныеАвторизации.Свойство("refresh_token") Тогда
	
		ОбщегоНазначения.ЗаписатьДанныеВБезопасноеХранилище(Владелец, ДанныеАвторизации.access_token, "access_token");
		ОбщегоНазначения.ЗаписатьДанныеВБезопасноеХранилище(Владелец, expires_in, "access_token_expires");	
		ОбщегоНазначения.ЗаписатьДанныеВБезопасноеХранилище(Владелец, ДанныеАвторизации.refresh_token, "refresh_token");
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( "Данные авторизации Яндекс маркет записаны в безопасное хранилище", УчетнаяЗапись );
		Возврат Истина;
	Иначе
		ТекстОшибки = НСтр("ru = 'Не удалось сохранить данные авторизации в безопасном хранилище, получены не все обязательные поля. Учетная запись:'") + " " + УчетнаяЗапись;
		ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
		ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );
		Возврат Ложь;	
	КонецЕсли;
	
КонецФункции

Функция ЯндексМаркетДанныеАвторизацииПолучить(УчетнаяЗапись) Экспорт
	
	Владелец = УчетнаяЗапись;
	// api_key: бессрочный токен
	ДанныеАвторизации = "access_token, access_token_expires, refresh_token, api_key";
	ДанныеАвторизации =	ОбщегоНазначения.ПрочитатьДанныеИзБезопасногоХранилища(Владелец, ДанныеАвторизации, Ложь);
	
	Возврат ДанныеАвторизации;
КонецФункции

Процедура ЯндексМаркетДанныеАвторизацииУдалить(УчетнаяЗапись) Экспорт
	
	Владелец = УчетнаяЗапись;
	ДанныеАвторизации = "access_token, access_token_expires, refresh_token, api_key";
	ОбщегоНазначения.УдалитьДанныеИзБезопасногоХранилища(Владелец, ДанныеАвторизации);
	
	ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( "Данные авторизации ""Яндекс маркет"" удалены", УчетнаяЗапись );
КонецПроцедуры

Процедура ЯндексМаркетУчетнаяЗаписьУдалить(УчетнаяЗапись) Экспорт

	ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетДанныеАвторизацииУдалить(УчетнаяЗапись);
	
	НЗ = РегистрыСведений.СтатусыПубликацииТоваровЯндексМаркет.СоздатьНаборЗаписей();
	НЗ.Отбор.УчетнаяЗаписьМаркетплейса.Установить( УчетнаяЗапись, Истина );
	НЗ.Записать();
	
	НЗ = РегистрыСведений.СоответствияОбъектовМаркетплейсов.СоздатьНаборЗаписей();
	НЗ.Отбор.УчетнаяЗаписьМаркетплейса.Установить( УчетнаяЗапись, Истина );
	НЗ.Записать();

	НЗ = РегистрыСведений.ОстаткиТоваровМаркетплейсов.СоздатьНаборЗаписей();
	НЗ.Отбор.УчетнаяЗаписьМаркетплейса.Установить( УчетнаяЗапись, Истина );
	НЗ.Записать();
	
	УчетнаяЗаписьОбъект = УчетнаяЗапись.ПолучитьОбъект();
	УчетнаяЗаписьОбъект.ПометкаУдаления = Истина;
	УчетнаяЗаписьОбъект.Записать();
	
	ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( "Данные регистров сведений ""Яндекс маркет"" удалены", УчетнаяЗапись );
	
КонецПроцедуры

Функция ОбменДаннымиЯндексМаркетОбновитьВсеДанные(УчетнаяЗапись) Экспорт

	ДанныеАвторизации = Неопределено;
	ДанныеАвторизацииПомещеныВХранилище = 
	ЯндексМаркетПроверитьДанныеАвторизацииПомещеныВХранилище( УчетнаяЗапись, ДанныеАвторизации );
	Если Не ДанныеАвторизацииПомещеныВХранилище Тогда
		ТекстОшибки =  "Ошибка при обновлении данных в Яндекс Маркет: Токен для авторизации не задан. Необходимо выполнить авторизацию в форме настройки.";
		ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись );
		Возврат Ложь;
	КонецЕсли;
	
	ОбменДаннымиОбновитьТоварныйКаталог( УчетнаяЗапись );
	ОбменДаннымиВыгрузитьЦеныИз1СВЯндексМаркет( УчетнаяЗапись );
	ОбменДаннымиВыгрузитьОстаткиТоваровВЯндексМаркет(  УчетнаяЗапись );
	
	Возврат Истина;
КонецФункции

Функция ОбменДаннымиОбновитьТоварныйКаталог(УчетнаяЗапись) Экспорт

	ДанныеАвторизации = Неопределено;
	ДанныеАвторизацииПомещеныВХранилище = 
	ЯндексМаркетПроверитьДанныеАвторизацииПомещеныВХранилище( УчетнаяЗапись, ДанныеАвторизации );
	Если Не ДанныеАвторизацииПомещеныВХранилище Тогда
		ТекстОшибки =  "Ошибка при обновлении данных в Яндекс Маркет: Токен для авторизации не задан. Необходимо выполнить авторизацию в форме настройки.";
		ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись );
		Возврат Ложь;
	КонецЕсли;

	ОбменДаннымиРекомендацииПолучитьИЗаписать( УчетнаяЗапись );
	ОбменДаннымиТоварыВСервисеДобавитьИзменить( УчетнаяЗапись );
	ОбменДаннымиСтатусыМодерацииПолучитьИЗаписать( УчетнаяЗапись );
	
КонецФункции

Процедура HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗапись, Заголовки, ДанныеАвторизации = Неопределено )
	
	Если ДанныеАвторизации = Неопределено Тогда
		ДанныеАвторизации = ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетДанныеАвторизацииПолучить( УчетнаяЗапись );
	КонецЕсли;

	Если ЗначениеЗаполнено( ДанныеАвторизации.api_key ) Тогда
		// API-Key-токен, https://yandex.ru/dev/market/partner-api/doc/ru/concepts/api-key
		Заголовки.Вставить( "Api-Key", ДанныеАвторизации.api_key );
	Иначе
		// OAuth-токен, устаревший, https://yandex.ru/dev/market/partner-api/doc/ru/concepts/oauth-2.0
		ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
		Заголовки.Вставить( "Authorization", "OAuth oauth_token=" + ДанныеАвторизации.access_token
		+ ", oauth_client_id=" + ЯндексПриложениеДанные.client_id );
	КонецЕсли;
	
КонецПроцедуры

Функция ОбменДаннымиРекомендацииПолучитьИЗаписать(УчетнаяЗапись, ИдентификаторПубликации = Неопределено)
	
	Склады = СкладыСоответствияПолучить( УчетнаяЗапись );
	Если Склады.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	СкладыСтрока = Склады[0];
	
	ПолеDescriptionМаксимальнаяДлина = 6000;// значение из документации
	Если ИдентификаторПубликации = Неопределено Тогда
		ЗапросТекст = 
		"ВЫБРАТЬ ПЕРВЫЕ 200
		|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации КАК offerId,
		|	СтатусыПубликацииТоваровЯндексМаркет.ПредставлениеТовара КАК name,
		|	СтатусыПубликацииТоваровЯндексМаркет.КатегорияНоменклатуры КАК category,
		|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Производитель.Наименование, """") КАК vendor,
		|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Производитель.Наименование, """") КАК ПроизводительНаименование,
		|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.СтранаПроисхождения.Наименование, """") КАК СтранаПроисхожденияНаименование,
		|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Артикул, """") КАК vendorCode,
		|	ПОДСТРОКА(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Комментарий, 1, &ПолеDescriptionМаксимальнаяДлина) КАК Description
		|ИЗ
		|	РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет КАК СтатусыПубликацииТоваровЯндексМаркет
		|ГДЕ
		|	СтатусыПубликацииТоваровЯндексМаркет.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыВыгрузкиТоваровЯндексМаркет.Новый)
		|	И СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись";
	Иначе
		ЗапросТекст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации КАК offerId,
		|	СтатусыПубликацииТоваровЯндексМаркет.ПредставлениеТовара КАК name,
		|	СтатусыПубликацииТоваровЯндексМаркет.КатегорияНоменклатуры КАК category,
		|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Производитель.Наименование, """") КАК vendor,
		|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Производитель.Наименование, """") КАК ПроизводительНаименование,
		|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.СтранаПроисхождения.Наименование, """") КАК СтранаПроисхожденияНаименование,
		|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Артикул, """") КАК vendorCode,
		|	ПОДСТРОКА(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Комментарий, 1, &ПолеDescriptionМаксимальнаяДлина) КАК Description
		|ИЗ
		|	РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет КАК СтатусыПубликацииТоваровЯндексМаркет
		|ГДЕ
		|	СтатусыПубликацииТоваровЯндексМаркет.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыВыгрузкиТоваровЯндексМаркет.Новый)
		|	И СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации = &offerId
		|	И СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись";
	КонецЕсли;	

	Запрос = Новый Запрос( ЗапросТекст );
	Запрос.УстановитьПараметр( "offerId", ИдентификаторПубликации );
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись );
	Запрос.УстановитьПараметр( "ПолеDescriptionМаксимальнаяДлина", ПолеDescriptionМаксимальнаяДлина );
	
	РезультатЗапроса = Запрос.Выполнить();
	ТоварыДляПолученияРекомендаций = РезультатЗапроса.Выбрать();
	
	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
		СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
		
		ТоварыДляПолученияРекомендацийПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( РезультатЗапроса.Выгрузить() );
		
		ЛогКомментарий = НСтр("ru = 'ОбменДаннымиРекомендацииПолучитьИЗаписать: ТоварыДляПолученияРекомендаций:'") + " " +  Символы.ПС
		+ ТоварыДляПолученияРекомендацийПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ПолучениеРекомендации );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
	КонецЕсли;	
	
	Если ТоварыДляПолученияРекомендаций.Количество() = 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	ДанныеАвторизации = ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетДанныеАвторизацииПолучить(УчетнаяЗапись);
	
	ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
	client_id = ЯндексПриложениеДанные.client_id;
	ssl = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение();
	
	Сервер = ИнтеграцияСЯндексМаркетСервер.СерверПартнерскогоAPIЯндексМаркет();
	// документация: https://yandex.ru/dev/market/partner-api/doc/ru/reference/business-assortment/getSuggestedOfferMappings
	ИмяМетода = "/businesses/%1/offer-mappings/suggestions";
	
	СкладыСтрока = Склады[0];
	
	business_id = СокрЛП( СкладыСтрока.ИдентификаторКабинетаМаркетплейса );
	Адрес = СтрШаблон( ИмяМетода, business_id );	

	ИнтернетПрокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси("HTTPS");
	HTTPСоединение = Новый HTTPСоединение(Сервер,,,, ИнтернетПрокси,,ssl);
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/json");
	HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗапись, Заголовки, ДанныеАвторизации );
	
	HTTPЗапрос = Новый HTTPЗапрос(Адрес, Заголовки);
	
	Пока ТоварыДляПолученияРекомендаций.Следующий() Цикл
		
		СтруктураПредложения = Новый Структура( "offerId, name, category, vendor, vendorCode, description");
		
		ЗаполнитьЗначенияСвойств( СтруктураПредложения, ТоварыДляПолученияРекомендаций );
		
		offers = Новый Массив;
		offers.Добавить(СтруктураПредложения);	
		
		СтруктураДляJSON = Новый Структура;
		СтруктураДляJSON.Вставить("offers",offers);
		
		ТелоЗапроса = ИнтеграцияСЯндексМаркетСервер.ВJSON( СтруктураДляJSON );
		HTTPЗапрос.УстановитьТелоИзСтроки(ТелоЗапроса, "UTF-8");

		Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
			HTTPЗапросПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPЗапрос );
			
			ЛогКомментарий = НСтр("ru = 'HTTP: Запрос:'") + Символы.ПС
			+ HTTPЗапросПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			
			ЛогПараметры = Новый Структура;
			ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ПолучениеРекомендации );
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
		КонецЕсли;	
		
		Попытка
			HTTPОтвет = HTTPСоединение.ОтправитьДляОбработки( HTTPЗапрос );
		Исключение
			ТекстОшибки = НСтр("ru = 'Ошибка при получении рекомендаций Яндекс Маркет:'") + " " + ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
			ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );
			Возврат Ложь;
		КонецПопытки;

		Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
			HTTPМетод = "POST";
			HTTPСоединениеПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPСоединение );
			HTTPОтветПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPОтвет );
			
			ЛогКомментарий = НСтр("ru = 'HTTP: Ответ: Метод:'") + " " + HTTPМетод + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
			+ HTTPОтветПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ HTTPСоединениеПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			
			ЛогПараметры = Новый Структура;
			ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ПолучениеРекомендации );
			ЛогПараметры.Вставить( "Сервер", HTTPСоединение.Сервер );
			ЛогПараметры.Вставить( "АдресРесурса", HTTPЗапрос.АдресРесурса );
			
			ЛогЭтоОшибка = HTTPОтвет.КодСостояния <> 200;
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, ЛогЭтоОшибка, ЛогПараметры );
		КонецЕсли;	
		
		HTTPОтветТелоСтрока = HTTPОтвет.ПолучитьТелоКакСтроку();
		СтруктураОтвета = ИнтеграцияСЯндексМаркетСервер.ИзJSON(HTTPОтветТелоСтрока);
		КодСостояния = HTTPОтвет.КодСостояния;
		
		Если СтруктураОтвета = Неопределено 
			Или НЕ ОбщегоНазначенияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(СтруктураОтвета, "status") Тогда
			ЗаписатьОшибкуПоИдентификаторуПубликации(УчетнаяЗапись, ТоварыДляПолученияРекомендаций.offerId, "Ошибка выполнения запроса: " + HTTPОтветТелоСтрока, "E153825" );
			Продолжить;
		КонецЕсли;
		
		Если КодСостояния = 200 И СтруктураОтвета.status = "OK" Тогда
			ЗаписатьРекомендацииПубликацийТоваров( УчетнаяЗапись, СтруктураОтвета.result );
		Иначе
			Если СтруктураОтвета.status = "ERROR" Тогда
				ЗаписатьОшибкиПоИдентификаторуПубликации(УчетнаяЗапись, ТоварыДляПолученияРекомендаций.offerId, СтруктураОтвета.errors);
			Иначе
				ЗаписатьОшибкуПоИдентификаторуПубликации(УчетнаяЗапись, ТоварыДляПолученияРекомендаций.offerId, "Ошибка выполнения запроса: " + HTTPОтветТелоСтрока, "E85716" );
			КонецЕсли;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Истина;
КонецФункции

Процедура ЗаписатьОшибкиПоИдентификаторуПубликации( УчетнаяЗапись, ИдентификаторПубликации, МассивОшибок )
	
	ТекстыОшибок = "" + ИдентификаторПубликации + ": " + Символы.ПС;
	КодыОшибок = "";
	Для Каждого СтруктураОшибки Из МассивОшибок Цикл
	
		ТекстОшибки = "" + СтруктураОшибки.message + " ( " + СтруктураОшибки.code + " )";
		ТекстОшибкиПояснение = ОбменСВнешнимиСистемамиЛогирование.БазаЗнанийСовпаденияНайтиСтрокойПолучить( "Yandex: " + ТекстОшибки );
		ТекстОшибкиПодробный = ?(ПустаяСтрока( ТекстОшибкиПояснение ), "", ТекстОшибкиПояснение + " " ) + ТекстОшибки;

		ТекстыОшибок = ТекстыОшибок + ТекстОшибкиПодробный + ";" + Символы.ПС;
		КодыОшибок = КодыОшибок + СтруктураОшибки.code + ";" + Символы.ПС;
		
	КонецЦикла;
	
	ТекстыОшибок = СокрП( ТекстыОшибок );
	КодыОшибок = СокрП( КодыОшибок );
	
	ЗаписатьОшибкуПоИдентификаторуПубликации( УчетнаяЗапись, ИдентификаторПубликации, ТекстыОшибок, КодыОшибок );
	
КонецПроцедуры

Процедура ЗаписатьОшибкуПоИдентификаторуПубликации( УчетнаяЗапись, ИдентификаторПубликации, ТекстОшибки, КодОшибки )

	ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
	ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );

	СтруктураИзмерений = ПолучитьСтруктуруИзмеренийПоИдентификаторуПубликации(УчетнаяЗапись, ИдентификаторПубликации);
	
	МенеджерЗаписи = РегистрыСведений.СтатусыПубликацииТоваровЯндексМаркет.СоздатьМенеджерЗаписи();
	МенеджерЗаписи.УчетнаяЗаписьМаркетплейса = СтруктураИзмерений.УчетнаяЗапись;
	МенеджерЗаписи.Номенклатура = СтруктураИзмерений.Номенклатура;
	МенеджерЗаписи.Характеристика = СтруктураИзмерений.Характеристика;
	
	МенеджерЗаписи.Прочитать();
	
	Если МенеджерЗаписи.Выбран() Тогда
		МенеджерЗаписи.КодОшибки = КодОшибки;
		МенеджерЗаписи.ОписаниеОшибки = ТекстОшибки;
		МенеджерЗаписи.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ОшибкаПриОтправкеНаМодерацию;
		МенеджерЗаписи.Записать();
	КонецЕсли;
	
КонецПроцедуры

Функция ПолучитьСтруктуруИзмеренийПоИдентификаторуПубликации( УчетнаяЗапись, ИдентификаторПубликации) Экспорт
	
	Структура = Новый Структура( "УчетнаяЗапись, Номенклатура, Характеристика" );

	ЗапросТекст = 
	"ВЫБРАТЬ Первые 1
	|	СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса КАК УчетнаяЗапись,
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура КАК Номенклатура,
	|	СтатусыПубликацииТоваровЯндексМаркет.Характеристика КАК Характеристика
	|ИЗ
	|	РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет КАК СтатусыПубликацииТоваровЯндексМаркет
	|ГДЕ
	|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации = &ИдентификаторПубликации
	|	И СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись";

	Запрос = Новый Запрос( ЗапросТекст );
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр( "ИдентификаторПубликации",ИдентификаторПубликации);
	ЗапросВыборка = Запрос.Выполнить().Выбрать();
	
	Если ЗапросВыборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств( Структура, ЗапросВыборка );
	КонецЕсли;
	
	Возврат Структура;
	
КонецФункции

Процедура ЗаписатьРекомендацииПубликацийТоваров( УчетнаяЗапись, Рекомендации )
	
	Для Каждого СтруктураПредложения Из Рекомендации.offers Цикл
		
		ИдентификаторПубликации = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( СтруктураПредложения, "offer.offerId", Неопределено );
		
		Если Не ЗначениеЗаполнено( ИдентификаторПубликации ) Тогда
			Продолжить;
		КонецЕсли;
		
		СоответствиеКарточкеНаМаркете = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( СтруктураПредложения, "mapping", Неопределено );
		Если Не ЗначениеЗаполнено( СоответствиеКарточкеНаМаркете ) Тогда
			Продолжить;
		КонецЕсли;
		
		Набор = РегистрыСведений.СтатусыПубликацииТоваровЯндексМаркет.СоздатьНаборЗаписей();
		СтруктураИзмерений = ПолучитьСтруктуруИзмеренийПоИдентификаторуПубликации( УчетнаяЗапись, ИдентификаторПубликации );
		
		Набор.Отбор.УчетнаяЗаписьМаркетплейса.Установить(СтруктураИзмерений.УчетнаяЗапись);
		Набор.Отбор.Номенклатура.Установить(СтруктураИзмерений.Номенклатура);
		Набор.Отбор.Характеристика.Установить(СтруктураИзмерений.Характеристика);
		
		Набор.Прочитать();
		
		Если Набор.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		Запись = Набор[ 0 ];
		
		
		Если СоответствиеКарточкеНаМаркете.Свойство( "marketSku" ) Тогда
			marketSku = СокрЛП(СтрЗаменить(СоответствиеКарточкеНаМаркете.marketSku, Символ(160), "" ));
			
			Если ЗначениеЗаполнено( marketSku ) 
			И Запись.ИдентификаторТовараПлощадки <> marketSku Тогда

				Запись.ИдентификаторТовараПлощадки = marketSku; 
				Запись.ЕстьИдентификаторПлощадки = Истина;

			КонецЕсли;
			
		КонецЕсли;
		
		Если СоответствиеКарточкеНаМаркете.Свойство("marketSkuName") Тогда 
		
			Запись.НаименованиеТовараПлощадки = СоответствиеКарточкеНаМаркете.marketSkuName;
			Запись.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ПолученаРекомендация;
			
		Иначе
		
			Запись.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.РекомендацияНеНайдена;
			Запись.ОписаниеОшибки = 
			"Для товара с указанными свойствами не была подобрана рекомендованная карточка товара на Яндекс Маркет."; 
			
		КонецЕсли;
		
		Если СоответствиеКарточкеНаМаркете.Свойство("marketCategoryName") Тогда 
		
			Запись.НаименованиеКатегорииПлощадки = СоответствиеКарточкеНаМаркете.marketCategoryName;
			
		КонецЕсли;
		
		Если СоответствиеКарточкеНаМаркете.Свойство("marketModelName") Тогда 
		
			Запись.НаименованиеМоделиПлощадки = СоответствиеКарточкеНаМаркете.marketModelName;
			
		КонецЕсли;
		
		Набор.Записать();
		
	КонецЦикла;
	
КонецПроцедуры

Функция ОбменДаннымиТоварыВСервисеДобавитьИзменить(УчетнаяЗапись)

	ИдентификаторыКабинетов = ИдентификаторыКабинетовИзСоответствияСкладовПолучить( УчетнаяЗапись );
	Если ИдентификаторыКабинетов.Количество() = 0 Тогда
	
        ЛогКомментарий = "ОбменДаннымиТоварыВСервисеДобавитьИзменить: Идентифкаторы кабинетов не заполнены в настройках учетной записи <" + УчетнаяЗапись + ">";
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаТоваровИз1С );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, Истина, ЛогПараметры );
		
		Возврат Ложь;
	КонецЕсли;
	
	ПолеDescriptionМаксимальнаяДлина = 6000;// значение из документации: https://yandex.ru/dev/market/partner-api/doc/ru/reference/offer-mappings/updateOfferMappingEntries
	
	ЗапросТекст = 
	"ВЫБРАТЬ ПЕРВЫЕ 500
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура КАК Номенклатура,
	|	СтатусыПубликацииТоваровЯндексМаркет.Характеристика КАК Характеристика,
	|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации КАК offerId,
	|	"""" КАК vendor,
	|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Артикул, """") КАК vendorCode,
	|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Производитель.Наименование, """") КАК manufacturer,
	|	ЕСТЬNULL(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.СтранаПроисхождения.Наименование, """") КАК manufacturerCountry,
	|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторТовараПлощадки КАК marketSku,
	|	СтатусыПубликацииТоваровЯндексМаркет.НаименованиеТовараПлощадки КАК name,
	|	СтатусыПубликацииТоваровЯндексМаркет.НаименованиеКатегорииПлощадки КАК category,
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Вес КАК НоменклатураВес,
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Высота КАК НоменклатураВысота,
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Длина КАК НоменклатураДлина,
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Ширина КАК НоменклатураШирина,
	|	ПОДСТРОКА(СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.Комментарий, 1, &ПолеDescriptionМаксимальнаяДлина) КАК Description,
	|	СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса КАК УчетнаяЗаписьМаркетплейса,
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура.ТоварнаяНоменклатураВЭД.Код КАК ТНВЭДКод
	|ИЗ
	|	РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет КАК СтатусыПубликацииТоваровЯндексМаркет
	|ГДЕ
	|	СтатусыПубликацииТоваровЯндексМаркет.Статус В(&СтатусыДляВыгрузки)
	|	И СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись";
	
	
	СтатусыДляВыгрузки = Новый Массив;
	СтатусыДляВыгрузки.Добавить( Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.СозданиеНового );
	СтатусыДляВыгрузки.Добавить( Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.УтвержденаРекомендация );
	
	Запрос = Новый Запрос( ЗапросТекст );
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись );
	Запрос.УстановитьПараметр( "СтатусыДляВыгрузки", СтатусыДляВыгрузки );
	Запрос.УстановитьПараметр( "ПолеDescriptionМаксимальнаяДлина", ПолеDescriptionМаксимальнаяДлина );
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
		СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
		
		ТоварыДляМодерацииПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( РезультатЗапроса.Выгрузить() );
		КабинетыПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( ИдентификаторыКабинетов );
		
		ЛогКомментарий = НСтр("ru = 'ОбменДаннымиТоварыВСервисеДобавитьИзменить: ТоварыДляМодерацииПредставление:'") + Символы.ПС
		+ ТоварыДляМодерацииПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ "Кабинеты:" + Символы.ПС
		+ КабинетыПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаТоваровИз1С );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
	КонецЕсли;	
	
	Товары = РезультатЗапроса.Выбрать();
	Если Товары.Количество() = 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	ssl = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение();
	
	ДанныеАвторизации = ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетДанныеАвторизацииПолучить(УчетнаяЗапись);
	
	ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
	client_id = ЯндексПриложениеДанные.client_id;
	
	Сервер = ИнтеграцияСЯндексМаркетСервер.СерверПартнерскогоAPIЯндексМаркет();
	ИмяМетода = "/businesses/%1/offer-mappings/update.json";

	ИнтернетПрокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси("HTTPS");
	HTTPСоединение = Новый HTTPСоединение(Сервер,,,, ИнтернетПрокси,,ssl);
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/json");
	HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗапись, Заголовки, ДанныеАвторизации );
	
	Пока Товары.Следующий() Цикл
		
		Для Каждого ИдентификаторКабинета Из ИдентификаторыКабинетов Цикл
			
			BusinessId = СокрЛП( ИдентификаторКабинета );
			Адрес = СтрШаблон( ИмяМетода, BusinessId );
			
			СтруктураОписанияТовара = Новый Структура( "offerId, name, category, manufacturer, weightDimensions, description" );
			ЗаполнитьЗначенияСвойств(СтруктураОписанияТовара, Товары );
			
			Если ЗначениеЗаполнено( Товары.manufacturerCountry ) Тогда
				manufacturerCountries = Новый Массив;
				manufacturerCountries.Добавить( Товары.manufacturerCountry );
				СтруктураОписанияТовара.Вставить( "manufacturerCountries", manufacturerCountries );
			КонецЕсли;

			Если ЗначениеЗаполнено( Товары.vendor ) Тогда
				СтруктураОписанияТовара.Вставить( "vendor", Товары.vendor );
			КонецЕсли;

			Если ЗначениеЗаполнено( Товары.vendorCode ) Тогда
				СтруктураОписанияТовара.Вставить( "vendorCode", Товары.vendorCode );
			КонецЕсли;
			
			Если ЗначениеЗаполнено( Товары.marketSku ) Тогда
				urls = Новый Массив;
				urls.Добавить( "https://pokupki.market.yandex.ru/product/" + СокрЛП(СтрЗаменить(Товары.marketSku, Символ(160),"")));
				СтруктураОписанияТовара.Вставить( "urls", urls );
			КонецЕсли;
			
			Если ЗначениеЗаполнено( Товары.ТНВЭДКод ) Тогда
				СтруктураОписанияТовара.Вставить( "customsCommodityCode", Товары.ТНВЭДКод );
			КонецЕсли;
			
			barcodes = ПолучитьШрихКоды( Товары.Номенклатура, Товары.Характеристика );
			Если barcodes.Количество() > 0 Тогда
				СтруктураОписанияТовара.Вставить("barcodes", barcodes);
			КонецЕсли;
			
			СтруктураВесогабаритов = ПолучитьВесогабариты(Товары.НоменклатураВес,
			Товары.НоменклатураВысота,
			Товары.НоменклатураДлина,
			Товары.НоменклатураШирина);
			
			СтруктураОписанияТовара.weightDimensions = СтруктураВесогабаритов;
			
			UpdateOfferMappingDTO = Новый Структура( "offer" );
			
			Если ЗначениеЗаполнено( Товары.marketSku ) Тогда
				UpdateOfferMappingDTO.Вставить( "mapping" );
			
				UpdateOfferMappingDTO.mapping = Новый Структура( "marketSku" );
				UpdateOfferMappingDTO.mapping.marketSku = Товары.marketSku;
			КонецЕсли;
			UpdateOfferMappingDTO.offer = СтруктураОписанияТовара; 
			
			offerMappings = Новый Массив;
			// можно до 500 товаров: https://yandex.ru/dev/market/partner-api/doc/ru/reference/offer-mappings/updateOfferMappingEntries
			// но в случае ошибки в одном товаре ответ сервера {"status":"ERROR","errors":[{"code":"BAD_REQUEST","message":"bad request"}]}
			// и невозможно определить товар с ошибкой
			offerMappings.Добавить( UpdateOfferMappingDTO );
			
			СтруктураДляJSON = Новый Структура;
			СтруктураДляJSON.Вставить( "offerMappings", offerMappings );
			
			ТелоЗапроса = ИнтеграцияСЯндексМаркетСервер.ВJSON( СтруктураДляJSON );
			
			HTTPЗапрос = Новый HTTPЗапрос( Адрес, Заголовки );
			HTTPЗапрос.УстановитьТелоИзСтроки( ТелоЗапроса, "UTF-8" );
			
			Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
				HTTPЗапросПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPЗапрос );
				
				ЛогКомментарий = НСтр("ru = 'HTTP: Запрос:'") + Символы.ПС
				+ HTTPЗапросПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
				+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
				
				ЛогПараметры = Новый Структура;
				ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаТоваровИз1С );
				ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
			КонецЕсли;
			
			Попытка
				HTTPОтвет = HTTPСоединение.ОтправитьДляОбработки( HTTPЗапрос );
			Исключение 
				ТекстОшибки = НСтр("ru = 'Ошибка выполнения запроса'") + " " + ПодробноеПредставлениеОшибки( ИнформацияОбОшибке() );
				ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
				ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );
				Возврат Ложь;
			КонецПопытки;	
			
			Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
				HTTPМетод = "POST";
				HTTPСоединениеПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPСоединение );
				HTTPОтветПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPОтвет );
				
				ЛогКомментарий = НСтр("ru = 'HTTP: Ответ: Метод:'") + " " + HTTPМетод + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
				+ HTTPОтветПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
				+ HTTPСоединениеПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
				+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
				
				ЛогПараметры = Новый Структура;
				ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаТоваровИз1С );
				ЛогПараметры.Вставить( "Сервер", HTTPСоединение.Сервер );
				ЛогПараметры.Вставить( "АдресРесурса", HTTPЗапрос.АдресРесурса );
				
				ЛогЭтоОшибка = HTTPОтвет.КодСостояния <> 200;
				ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, ЛогЭтоОшибка, ЛогПараметры );
			КонецЕсли;
			
			СтрокаОтвета = HTTPОтвет.ПолучитьТелоКакСтроку();
			СтруктураОтвета = ИнтеграцияСЯндексМаркетСервер.ИзJSON(СтрокаОтвета);
			КодСостояния = HTTPОтвет.КодСостояния;
			
			offerId = Товары.offerId;
			Если КодСостояния= 200 И СтруктураОтвета.status = "OK" Тогда
				ЗаписатьСтатусНаМодерации( УчетнаяЗапись, offerId );
			Иначе	
				Если СтруктураОтвета.status = "ERROR" Тогда
					ЗаписатьОшибкиПоИдентификаторуПубликации( УчетнаяЗапись, offerId, СтруктураОтвета.errors );
				Иначе
					КодОшибки = "E53253";
					ТоварПредставление = ПредставлениеНоменклатуры( Товары.Номенклатура, Товары.Характеристика );
					ТекстОшибки = "Ошибка при отправке товара на модерацию: " + ТоварПредставление + " ( " + Товары.offerId + " ) "
					 + ": Идентификатор кабинета: " + BusinessId + ": " + СтрокаОтвета;
					ЗаписатьОшибкуПоИдентификаторуПубликации( УчетнаяЗапись, offerId, СтрокаОтвета, КодОшибки );
				КонецЕсли;
				
			КонецЕсли;
		
			// Если успешно, то сделать цикл по складам для размещения товара в магазине
			// https://yandex.ru/dev/market/partner-api/doc/ru/reference/assortment/updateCampaignOffers
			// https://api.partner.market.yandex.ru/campaigns/{campaignId}/offers/update

			
		КонецЦикла;	// кабинеты
		
		
	КонецЦикла;	// товары
	
	Возврат Истина;
КонецФункции

Функция ПредставлениеНоменклатуры( Номенклатура, Характеристика )

	СтрПредставление = СокрЛП(Номенклатура);

	Если ЗначениеЗаполнено(Характеристика)Тогда
		СтрПредставление = СтрПредставление + ", " + СокрЛП(Характеристика);
	КонецЕсли;

	Возврат СтрПредставление;

КонецФункции

Функция ПолучитьШрихКоды(Номенклатура, Характеристика)
	
	Запрос = Новый Запрос;
	Запрос.Параметры.Вставить("Номенклатура",Номенклатура);

	ТипХарактеристикиНоменклатуры = Новый ОписаниеТипов( "СправочникСсылка.ХарактеристикиНоменклатуры" );
	Запрос.Параметры.Вставить("Характеристика", ТипХарактеристикиНоменклатуры.ПривестиЗначение( Характеристика ));
	
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ШтрихкодыНоменклатуры.Штрихкод КАК barcode,
	|	ШтрихкодыНоменклатуры.Номенклатура КАК Номенклатура,
	|	ШтрихкодыНоменклатуры.Характеристика КАК Характеристика
	|ИЗ
	|	РегистрСведений.ШтрихкодыНоменклатуры КАК ШтрихкодыНоменклатуры
	|ГДЕ
	|	ШтрихкодыНоменклатуры.Номенклатура = &Номенклатура
	|	И ШтрихкодыНоменклатуры.Характеристика = &Характеристика";
	
	Результат = Запрос.Выполнить().Выгрузить();
	ШтрихКоды = Результат.ВыгрузитьКолонку( "barcode" );
	
	Возврат ШтрихКоды;
КонецФункции

Функция ПолучитьВесогабариты(пВес, пВысота, пДлина, пШирина) Экспорт
	
	СтруктураВесогабаритов = Новый Структура("length,width,height,weight", 0, 0, 0, 0);
	
	СтруктураВесогабаритов.weight = Формат(пВес, "ЧДЦ=2; ЧРД=.");
	СтруктураВесогабаритов.height = Формат(пВысота, "ЧДЦ=2; ЧРД=.");
	СтруктураВесогабаритов.length = Формат(пДлина, "ЧДЦ=2; ЧРД=.");
	СтруктураВесогабаритов.width = Формат(пШирина, "ЧДЦ=2; ЧРД=.");
	
	Возврат СтруктураВесогабаритов;
	
КонецФункции

Процедура ЗаписатьСтатусНаМодерации( УчетнаяЗапись, shopSku )
	
	Набор = РегистрыСведений.СтатусыПубликацииТоваровЯндексМаркет.СоздатьНаборЗаписей();
	СтруктураИзмерений = ПолучитьСтруктуруИзмеренийПоИдентификаторуПубликации( УчетнаяЗапись, shopSku);
	
	Набор.Отбор.УчетнаяЗаписьМаркетплейса.Установить(СтруктураИзмерений.УчетнаяЗапись);
	Набор.Отбор.Номенклатура.Установить(СтруктураИзмерений.Номенклатура);
	Набор.Отбор.Характеристика.Установить(СтруктураИзмерений.Характеристика);
	
	Набор.Прочитать();
	Если Набор.Количество() > 0 Тогда
		Запись = Набор[0];
		Если Запись.Статус <> Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации Тогда
			Запись.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации;
			Набор.Записать();
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

Функция ОбменДаннымиПолучитьОписанияТоваровИзСервиса( УчетнаяЗапись, Товары )

	offerIds    = Новый Массив;
	Предложения = Новый Соответствие;
	
	Для Каждого ТоварыСтрока Из Товары Цикл
		offerIds.Добавить( ТоварыСтрока.ИдентификаторПубликации );
	КонецЦикла;
	
	Если offerIds.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	ДанныеЗапроса = Новый Структура;
	ДанныеЗапроса.Вставить("offerIds", offerIds);
	ТелоЗапроса = ВJSON(ДанныеЗапроса);

	ИдентификаторыКабинетов = ИдентификаторыКабинетовИзСоответствияСкладовПолучить( УчетнаяЗапись );
	Если ИдентификаторыКабинетов.Количество() = 0 Тогда
	
        ЛогКомментарий = "ОбменДаннымиПолучитьОписанияТоваровИзСервиса: Идентифкаторы кабинетов не заполнены в настройках учетной записи <" + УчетнаяЗапись + ">";
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаТоваровИз1С );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, Истина, ЛогПараметры );
		
		Возврат Ложь;
	КонецЕсли;
	
	ДанныеАвторизации = ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетДанныеАвторизацииПолучить(УчетнаяЗапись);
	
	ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
	client_id = ЯндексПриложениеДанные.client_id;
	
	Сервер = ИнтеграцияСЯндексМаркетСервер.СерверПартнерскогоAPIЯндексМаркет();
	ИмяМетода = "/businesses/%1/offer-mappings";

	ssl = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение();
	Таймаут = 60;
	ИнтернетПрокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси("HTTPS");
	HTTPСоединение = Новый HTTPСоединение(Сервер,,,, ИнтернетПрокси, Таймаут, ssl);

	Заголовки = Новый Соответствие;
	Заголовки.Вставить( "Content-Type", "application/json" );
	HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗапись, Заголовки, ДанныеАвторизации );

	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
		СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
	КонецЕсли;
	
	Результат = Новый Массив;
	
	Для Каждого ИдентификаторКабинета Из ИдентификаторыКабинетов Цикл
		
		business_id = СокрЛП( ИдентификаторКабинета );
		Адрес = СтрШаблон( ИмяМетода, business_id  );
		
		HTTPЗапрос = Новый HTTPЗапрос( Адрес, Заголовки );
		HTTPЗапрос.УстановитьТелоИзСтроки(ТелоЗапроса, "UTF-8");
		
		Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
			
			HTTPЗапросПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPЗапрос );
			
			ЛогКомментарий = НСтр("ru = 'HTTP: Запрос:'") + Символы.ПС
			+ HTTPЗапросПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			
			ЛогПараметры = Новый Структура;
			ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ПроверкаСтатусаПубликации );
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
		КонецЕсли;
		
		Попытка
			HTTPОтвет = HTTPСоединение.ОтправитьДляОбработки( HTTPЗапрос );  
		Исключение 
			ТекстОшибки = НСтр("ru = 'ОбменДаннымиПолучитьОписанияТоваровИзСервиса: Ошибка выполнения HTTP запроса:'") + " " + ПодробноеПредставлениеОшибки( ИнформацияОбОшибке() );
			ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );
		КонецПопытки;	
		
		Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
			HTTPМетод = "GET";
			HTTPСоединениеПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPСоединение );
			HTTPОтветПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPОтвет );
			
			ЛогКомментарий = НСтр("ru = 'HTTP: Ответ: Метод:'") + " " + HTTPМетод + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
			+ HTTPОтветПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ HTTPСоединениеПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			
			ЛогПараметры = Новый Структура;
			ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ПроверкаСтатусаПубликации );
			ЛогПараметры.Вставить( "Сервер", HTTPСоединение.Сервер );
			ЛогПараметры.Вставить( "АдресРесурса", HTTPЗапрос.АдресРесурса );
			
			ЛогЭтоОшибка = HTTPОтвет.КодСостояния <> 200;
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, ЛогЭтоОшибка, ЛогПараметры );
		КонецЕсли;
		
		СтрокаОтвета = HTTPОтвет.ПолучитьТелоКакСтроку();
		СтруктураОтвета = ИнтеграцияСЯндексМаркетСервер.ИзJSON(СтрокаОтвета);
		КодСостояния = HTTPОтвет.КодСостояния;
		
		Если КодСостояния = 200 И СтруктураОтвета.status = "OK" Тогда

			РезультатЗначение = СтруктураОтвета;
			Ошибка = Неопределено;
			
		Иначе
			
			СобытиеОписание = "Получение статуса публикации";
			ТекстОшибки = СтрокаОтвета;
			ТекстОшибкиПояснение = ОбменСВнешнимиСистемамиЛогирование.БазаЗнанийСовпаденияНайтиСтрокойПолучить( "Yandex: " + ТекстОшибки );
			ТекстОшибкиПодробный = СобытиеОписание + ": " + ?(ПустаяСтрока( ТекстОшибкиПояснение ), "", ТекстОшибкиПояснение + " " ) + ТекстОшибки;
			
			РезультатЗначение = Неопределено;
			Ошибка = ТекстОшибкиПодробный;
			
		КонецЕсли;

		Результат.Добавить( Новый Структура( "ИдентификаторКабинета, Значение, Ошибка", business_id, РезультатЗначение, Ошибка ) );
		
	КонецЦикла; // ИдентификаторыКабинетов
	
	Возврат Результат;
КонецФункции 

Функция ЗаписатьСтатусТовараНаМодерацииИСоответствиеКарточкеНаМаркете( УчетнаяЗапись, ИдентификаторПубликации, СтатусыМодерации, СоответствиеКарточкеНаМаркете = Неопределено )

    Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
    	СтатусыМодерацииПоСкладамПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( СтатусыМодерации );
		ЛогКомментарий = "ЗаписатьСтатусТовараНаМодерацииИСоответствиеКарточкеНаМаркете: Идентификатор публикации = " + ИдентификаторПубликации + ": " + Символы.ПС 
		+ "СтатусыМодерацииПоСкладам: " +  Символы.ПС + СтатусыМодерацииПоСкладамПредставление;

		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ПроверкаСтатусаПубликации );
		
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
	КонецЕсли;
	
	СтатусыМодерацииОсновной = Неопределено;
	Для Каждого СтатусМодерации Из СтатусыМодерации Цикл 
		
		// обновим статус "худшим" статусом по кабинетам
		
		Статус = СтатусМодерации.Статус.Статус;
		Если Статус = Неопределено Тогда
			Прервать;
		ИначеЕсли Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена Тогда
			Прервать;
		ИначеЕсли Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации Тогда
			Прервать;
		КонецЕсли;
			
	КонецЦикла;
	
	Набор = РегистрыСведений.СтатусыПубликацииТоваровЯндексМаркет.СоздатьНаборЗаписей();
	СтруктураИзмерений = ПолучитьСтруктуруИзмеренийПоИдентификаторуПубликации( УчетнаяЗапись, ИдентификаторПубликации );
	
	Набор.Отбор.УчетнаяЗаписьМаркетплейса.Установить( СтруктураИзмерений.УчетнаяЗапись );
	Набор.Отбор.Номенклатура.Установить( СтруктураИзмерений.Номенклатура );
	Набор.Отбор.Характеристика.Установить( СтруктураИзмерений.Характеристика );
	
	Набор.Прочитать();
	Если Набор.Количество() = 0 Тогда
		Возврат Неопределено;
	КонецЕсли;

	Записать = Ложь;
	
	Запись = Набор[0];
	
	// Соответствие товара карточке на маркете ( marketSku )
	Если ЗначениеЗаполнено( СоответствиеКарточкеНаМаркете ) Тогда
		
		ИдентификаторТовараПлощадки = XMLСтрока( СоответствиеКарточкеНаМаркете.ИдентификаторТовараПлощадки );
		Если ЗначениеЗаполнено( ИдентификаторТовараПлощадки ) 
		И Запись.ИдентификаторТовараПлощадки <> ИдентификаторТовараПлощадки Тогда
			
			Запись.ИдентификаторТовараПлощадки = ИдентификаторТовараПлощадки;
			Запись.НаименованиеТовараПлощадки = СоответствиеКарточкеНаМаркете.НаименованиеТовараПлощадки;
			
			Записать = Истина;
		КонецЕсли;
		
		Если ЗначениеЗаполнено( СоответствиеКарточкеНаМаркете.НаименованиеКатегорииПлощадки )
		И Запись.НаименованиеКатегорииПлощадки <> СоответствиеКарточкеНаМаркете.НаименованиеКатегорииПлощадки Тогда
			Запись.НаименованиеКатегорииПлощадки = СоответствиеКарточкеНаМаркете.НаименованиеКатегорииПлощадки;
			
			Записать = Истина;
		КонецЕсли;
		
		Если ЗначениеЗаполнено( СоответствиеКарточкеНаМаркете.НаименованиеМоделиПлощадки )
		И Запись.НаименованиеМоделиПлощадки <> СоответствиеКарточкеНаМаркете.НаименованиеМоделиПлощадки Тогда
			Запись.НаименованиеМоделиПлощадки = СоответствиеКарточкеНаМаркете.НаименованиеМоделиПлощадки;
			
			Записать = Истина;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено( Статус ) Тогда
		Если Запись.Статус <> Статус Тогда
			Запись.Статус = Статус;
			
			Записать = Истина;
		КонецЕсли;
	Иначе

		СтатусДляЛога = ИнтеграцияСМаркетплейсамиСервер.СтруктураСвойствоЗначениеПолучить( СтатусМодерации.Статус, "СтатусМодерацииИзОтветаЯндекса", Неопределено );
		ЛогКомментарий = "Ошибка: ЗаписатьСтатусТовараНаМодерацииИСоответствиеКарточкеНаМаркете: " + ИдентификаторПубликации + ": Значение не обработано: Статус = <" + СтатусДляЛога + ">";

		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ПроверкаСтатусаПубликации );

		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, Истина, ЛогПараметры );

	КонецЕсли;
	
	Если СтатусМодерации.Статус.ОписаниеОшибки <> Неопределено Тогда
		Если Запись.ОписаниеОшибки <> СтатусМодерации.Статус.ОписаниеОшибки Тогда
			Запись.ОписаниеОшибки = СтатусМодерации.Статус.ОписаниеОшибки;
			Записать = Истина;
		КонецЕсли;
	КонецЕсли;
	
	Если Записать = Истина Тогда
		Набор.Записать();
	КонецЕсли;
	
	Возврат СтатусМодерации;
КонецФункции

Процедура ОбменДаннымиСтатусыМодерацииПолучитьИЗаписать( УчетнаяЗапись )
	
	ЗапросТекст = 
	"ВЫБРАТЬ
	|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации КАК ИдентификаторПубликации,
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура КАК Номенклатура,
	|	СтатусыПубликацииТоваровЯндексМаркет.Характеристика КАК Характеристика
	|ИЗ
	|	РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет КАК СтатусыПубликацииТоваровЯндексМаркет
	|ГДЕ
	|	СтатусыПубликацииТоваровЯндексМаркет.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации)
	|	И СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись";
	
	Запрос = Новый Запрос(ЗапросТекст);
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись );
   
   	РезультатЗапроса = Запрос.Выполнить();
   	
	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
		СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
		
		ТоварыПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( РезультатЗапроса.Выгрузить() );
		
		ЛогКомментарий = НСтр("ru = 'ОбменДаннымиСтатусыМодерацииПолучитьИЗаписать: Товары:'") + " " +  Символы.ПС
		+ ТоварыПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ПолучениеРекомендации );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
	КонецЕсли;
	
	Если РезультатЗапроса.Пустой() Тогда
		Возврат;
	КонецЕсли;
	
	Товары = РезультатЗапроса.Выгрузить();
	Товары.Индексы.Добавить( "ИдентификаторПубликации" );
	
	ОписанияТоваровИзСервиса = ОбменДаннымиПолучитьОписанияТоваровИзСервиса( УчетнаяЗапись, Товары );

	СтатусыМодерацииТоваров = ПолучитьСтатусыМодерацииИзОтветаСервиса( ОписанияТоваровИзСервиса, Товары );
	
	ЗаписатьСтатусыТоваровНаМодерацииИСоответствиеКарточкеНаМаркете( УчетнаяЗапись, СтатусыМодерацииТоваров );
	
КонецПроцедуры

Функция ПолучитьСтатусыМодерацииИзОтветаСервиса( ОписанияТоваровИзСервиса, Товары )
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить( "ИдентификаторПубликации" );
	Результат.Колонки.Добавить( "Номенклатура" );
	Результат.Колонки.Добавить( "Характеристика" );
	Результат.Колонки.Добавить( "СтатусыПоКабинетам", Новый ОписаниеТипов( "Массив" ) );
	Результат.Индексы.Добавить( "ИдентификаторПубликации" );
	Результат.Колонки.Добавить( "СоответствиеКарточкеНаМаркете" );
	
	ОтборПоИдентификатору = Новый Структура( "ИдентификаторПубликации" );
	Для Каждого ОписанияТоваровИзСервисаЭлемент Из ОписанияТоваровИзСервиса Цикл
		
		ИдентификаторКабинета = ОписанияТоваровИзСервисаЭлемент.ИдентификаторКабинета;
		ОписанияТоваровКабинета = ОписанияТоваровИзСервисаЭлемент.Значение;
		
		offerMappings = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ОписанияТоваровКабинета, "result.offerMappings", Неопределено );
		Если offerMappings = Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		Для Каждого offerMappingsЭлемент Из offerMappings Цикл
			
			offer = offerMappingsЭлемент.offer;
			
			Если Не offer.Свойство( "cardStatus" ) Тогда
				Продолжить;
			КонецЕсли;
			
			offerId = offer.offerId;
			ОтборПоИдентификатору.ИдентификаторПубликации = offerId;
			
			ТоварыСтроки = Товары.НайтиСтроки( ОтборПоИдентификатору );
			Если ТоварыСтроки.Количество() = 0 Тогда
				Продолжить;
			КонецЕсли;
			
			ТоварыСтрока = ТоварыСтроки[ 0 ];
			
			РезультатСтроки = Результат.НайтиСтроки( ОтборПоИдентификатору );
			Если РезультатСтроки.Количество() = 0 Тогда
				РезультатСтрока = Результат.Добавить();
			Иначе
				РезультатСтрока = РезультатСтроки[ 0 ];
			КонецЕсли;
			
			РезультатСтрока.ИдентификаторПубликации = offerId;
			РезультатСтрока.Номенклатура = ТоварыСтрока.Номенклатура;
			РезультатСтрока.Характеристика = ТоварыСтрока.Характеристика;
			
			Статус = ПолучитьСтатусМодерацииСОписаниемПоЗначениюИзСервиса( offer.cardStatus );
			СтатусПоКабинету = Новый Структура( "ИдентификаторКабинета, Статус", ИдентификаторКабинета, Статус );
			РезультатСтрока.СтатусыПоКабинетам.Добавить( СтатусПоКабинету );
			
			Если offerMappingsЭлемент.Свойство( "mapping" ) Тогда
				mapping = offerMappingsЭлемент.mapping;

				СоответствиеКарточкеНаМаркете = Новый Структура;
				
				СоответствиеКарточкеНаМаркете.Вставить( "ИдентификаторТовараПлощадки", ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( mapping, "marketSku", Неопределено ) );
				СоответствиеКарточкеНаМаркете.Вставить( "НаименованиеТовараПлощадки", ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( mapping, "marketSkuName", Неопределено ) );
				
				СоответствиеКарточкеНаМаркете.Вставить( "ИдентификаторМоделиПлощадки", ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( mapping, "marketModelId", Неопределено ) );
				СоответствиеКарточкеНаМаркете.Вставить( "НаименованиеМоделиПлощадки", ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( mapping, "marketModelName", Неопределено ) );

				СоответствиеКарточкеНаМаркете.Вставить( "ИдентификаторКатегорииПлощадки",  ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( mapping, "marketCategoryId", Неопределено ) );
				СоответствиеКарточкеНаМаркете.Вставить( "НаименованиеКатегорииПлощадки",  ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( mapping, "marketCategoryName", Неопределено ) );
				
				РезультатСтрока.СоответствиеКарточкеНаМаркете = СоответствиеКарточкеНаМаркете;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Результат;
КонецФункции

Процедура ЗаписатьСтатусыТоваровНаМодерацииИСоответствиеКарточкеНаМаркете( УчетнаяЗапись, СтатусыМодерацииТоваров )
	Для Каждого СтатусыМодерацииТоваровЭлемент Из СтатусыМодерацииТоваров Цикл
		ЗаписатьСтатусТовараНаМодерацииИСоответствиеКарточкеНаМаркете( УчетнаяЗапись, СтатусыМодерацииТоваровЭлемент.ИдентификаторПубликации, 
		СтатусыМодерацииТоваровЭлемент.СтатусыПоКабинетам, СтатусыМодерацииТоваровЭлемент.СоответствиеКарточкеНаМаркете );
	КонецЦикла;
КонецПроцедуры

// Определяет расширенное описание статуса публикации товарной позиции.
//
// Параметры:
//   СтатусПубликации - Строка - статус публикации товара (OfferProcessingStatusType);
//   ТекущийСтатус    - ПеречислениеСсылка.СтатусыВыгрузкиТоваровЯндексМаркет - текущий статус товарной позиции;
//   ПричиныОтказа    - Массив Из Структура - причины, по которым товар не прошел модерацию (OfferProcessingNoteDTO):
//     * type           - Строка - тип причины, по которой товар не прошел модерацию;
//     * payload        - Строка - дополнительная информация о причине отклонения товара.
//
Функция ПолучитьСтатусМодерацииСОписаниемПоЗначениюИзСервиса(СтатусПубликации, ПричиныОтказа = Неопределено)
	
	Структура = Новый Структура("Статус, ОписаниеОшибки");
	
	Если СтатусПубликации = "READY" Тогда

		// Товар прошел модерацию. Чтобы разместить его на Маркете, установите для него цену
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияПройдена; 
		Структура.ОписаниеОшибки = "";
		
	ИначеЕсли СтатусПубликации = "IN_WORK" Тогда
	
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации; 
		
	ИначеЕсли СтатусПубликации = "NEED_CONTENT" Тогда
	
		// Для товара без SKU на Маркете marketSku нужно найти карточку самостоятельно (через API или личный кабинет магазина) или создать ее, если товар еще не продается на Маркете
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена;
		Структура.ОписаниеОшибки = НСтр("ru = 'Для товара не указан SKU на Яндексе, и сотрудники Маркета не смогли привязать его к карточке. Найдите карточку товара самостоятельно в поиске по разделу «Покупки» или создайте ее, если товар еще не продается на Маркете.'");
		
	ИначеЕсли СтатусПубликации = "NEED_INFO" Тогда
	
		// Товар не прошел модерацию из-за ошибок или недостающих сведений в описании товара. Информация о причинах отклонения возвращается в параметре notes
		ОписаниеОшибки = НСтр("ru = 'Для товара указана неверная или недостаточная информация.'");
		
		Если ЗначениеЗаполнено(ПричиныОтказа) Тогда
			Если ТипЗнч(ПричиныОтказа) = Тип("Строка") Тогда 
				ОписаниеОшибки = ОписаниеОшибки + " " + ПричиныОтказа;
				
			ИначеЕсли ТипЗнч(ПричиныОтказа) = Тип("Массив") Тогда 
				Для каждого ЭлементКоллекции Из ПричиныОтказа Цикл
					Если ЭлементКоллекции.type = "ASSORTMENT" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Товар производится в разных вариантах. Каждый из них нужно описать как отдельный товар'");
					ИначеЕсли ЭлементКоллекции.type = "CANCELLED" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Товар отозван с модерации по вашей инициативе'");
					ИначеЕсли ЭлементКоллекции.type = "CONFLICTING_INFORMATION" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Предоставлена противоречивая информацию о товаре'");
					ИначеЕсли ЭлементКоллекции.type = "DEPARTMENT_FROZEN" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Правила размещения товаров в данной категории перерабатываются, поэтому товар пока не может пройти модерацию'");
					ИначеЕсли ЭлементКоллекции.type = "INCORRECT_INFORMATION" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Предоставленная информация о товаре, противоречит описанию от производителя'");
					ИначеЕсли ЭлементКоллекции.type = "LEGAL_CONFLICT" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Товар не прошел модерацию по юридическим причинам. Например, он официально не продается в России или нет разрешения на его продажу'");
					ИначеЕсли ЭлементКоллекции.type = "NEED_CLASSIFICATION_INFORMATION" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Предоставленной информации о товаре не хватает, чтобы отнести его к категории. Проверьте, что правильно указано название, категория, производитель и страна производства товара, а также URL изображений или страниц с описанием, по которым можно идентифицировать товар'");
					ИначеЕсли ЭлементКоллекции.type = "NEED_INFORMATION" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Товар раньше не продавался в России и пока не размещается на Маркете. Для него можно создать карточку'");
					ИначеЕсли ЭлементКоллекции.type = "NEED_PICTURES" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Для идентификации товара нужны его изображения. Отправьте URL изображений товара или загрузите обновленный каталог через личный кабинет магазина'");
					ИначеЕсли ЭлементКоллекции.type = "NEED_VENDOR" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Неверно указан производитель товара'");
					ИначеЕсли ЭлементКоллекции.type = "NO_CATEGORY, NO_KNOWLEDGE" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Товары из указанной категории пока не размещаются на Маркете. Если категория появится, товар будет снова отправлен на модерацию'");
					ИначеЕсли ЭлементКоллекции.type = "NO_PARAMETERS_IN_SHOP_TITLE" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Товар производится в разных вариантах, и из указанного названия непонятно, о каком идет речь'");
					ИначеЕсли ЭлементКоллекции.type = "NO_SIZE_MEASURE" Тогда
						ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Для этого товара нужна размерная сетка. Отправьте ее в службу поддержки или вашему менеджеру'");
					ИначеЕсли ЭлементКоллекции.type = "UNKNOWN" Тогда
						ОписаниеОшибки = НСтр("ru = 'Товар не прошел модерацию по другой причине. Обратитесь в службу поддержки или к вашему менеджеру'");
					КонецЕсли;
					
					Если ЭлементКоллекции.Свойство("payload") И ЗначениеЗаполнено(ЭлементКоллекции.payload) Тогда
						ОписаниеОшибки = ОписаниеОшибки + " (" + ЭлементКоллекции.payload + ")";
					КонецЕсли;
					ОписаниеОшибки = ОписаниеОшибки + ".";
				КонецЦикла;
			Иначе
				ОписаниеОшибки = ОписаниеОшибки + " " + НСтр("ru = 'Уточните входные данные запроса и отправьте связь на модерацию повторно.'");
			КонецЕсли;
		КонецЕсли;	
			
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена;
		Структура.ОписаниеОшибки = ОписаниеОшибки;
		
	ИначеЕсли СтатусПубликации = "REJECTED" Или СтатусПубликации = "EJECTED" Тогда
	
		// Товар не прошел модерацию, так как Маркет не планирует размещать подобные товары
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена;
		Структура.ОписаниеОшибки = НСтр("ru = 'В ближайшее время Маркет не планирует размещать товары данной категории. Если есть вопросы по категориям товаров, размещаемым на Маркете, обратитесь в службу поддержки.'");
		
	ИначеЕсли СтатусПубликации = "SUSPENDED" Тогда                                                   
	
		// Товар не прошел модерацию, так как Маркет пока не размещает подобные товары
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ОжидаетМодерации;
		Структура.ОписаниеОшибки = НСтр("ru = 'Маркет пока не размещает товары данной категории, но начнет в ближайшее время. После этого товар автоматически отправится на модерацию, и статус изменится на IN_WORK. Чтобы уточнить сроки появления категорий товаров на Маркете, обратитесь в службу поддержки.'");
		
	КонецЕсли;
	
	Если СтатусПубликации = "HAS_CARD_CAN_UPDATE_PROCESSING" 
		 ИЛИ СтатусПубликации = "NO_CARD_PROCESSING" Тогда 

		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации; 

	ИначеЕсли СтатусПубликации = "NO_CARD_ADD_TO_CAMPAIGN" Тогда 
	
		// Разместите товар в магазине
		Структура.Статус         = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияПройдена; 
		Структура.ОписаниеОшибки = "";

	ИначеЕсли СтатусПубликации = "NO_CARD_MARKET_WILL_CREATE" Тогда 
	
		// Создаст Маркет
		Структура.Статус         = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ОжидаетМодерации;
		Структура.ОписаниеОшибки = НСтр("ru = 'Добавлен товар, который пока не продавался на Маркете. Карточки для товаров этой категории могут создавать только сотрудники Маркета. Обычно создание карточки занимает не больше двух дней.'");

	ИначеЕсли СтатусПубликации = "HAS_CARD_CAN_NOT_UPDATE" Тогда
	
		// Карточка Маркета
		Структура.Статус         = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена;
		Структура.ОписаниеОшибки = НСтр("ru = 'Напрямую управлять карточкой с таким статусом нельзя - можно менять только условия продажи товара, его вес и габариты.
                                         |Если нужно изменить описание, изображения или другие характеристики товара — напишите в службу поддержки с помощью кнопки Помощь с карточкой на странице товара.'");
		
	ИначеЕсли СтатусПубликации = "NO_CARD_NEED_CONTENT" Тогда 
	
		// Создайте карточку
		Структура.Статус         = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена;
		Структура.ОписаниеОшибки = НСтр("ru = 'В категории есть дополнительные поля, без которых карточку нельзя создать. Заполните характеристики на странице товара или загрузите информацию для всех товаров, используя Excel-шаблон категории или конвертер контента.'");

	ИначеЕсли СтатусПубликации = "NO_CARD_ERRORS" Тогда 
	
		// Не создана из-за ошибки
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена;
		Структура.ОписаниеОшибки = НСтр("ru = 'Маркет обнаружил ошибку в данных о товаре, поэтому создать карточку не удалось.'");

	ИначеЕсли СтатусПубликации = "HAS_CARD_CAN_UPDATE" Тогда 
	
		// Можно дополнить
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияПройдена; 
		Структура.ОписаниеОшибки = НСтр("ru = 'Карточку можно дополнить недостающей информацией. 
                                         |Если на карточке мало данных - товар сложно найти в поиске и с помощью фильтров на Маркете, а покупателям будет трудно принять решение о покупке.
                                         |Дополните карточку товара и Маркет за свой счет предложит покупателям кешбэк на ваши товары.'");
		
	ИначеЕсли СтатусПубликации = "HAS_CARD_CAN_UPDATE_ERRORS" Тогда 
	
		// Изменения не приняты
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена;
		Структура.ОписаниеОшибки = НСтр("ru = 'Карточка отредактирована, но Маркет нашел ошибки в измененных данных - их нужно исправить. Карточка пока остается в том же виде, что была до редактирования.'");
		
	КонецЕсли;
	
	// Прочее
	Если НЕ ЗначениеЗаполнено(Структура.Статус) Тогда
	
		Структура.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена; 
		Структура.ОписаниеОшибки = НСтр("ru = 'Товар не прошел модерацию по неустановленной причине. Обратитесь в службу поддержки или к вашему менеджеру.'");
		
	КонецЕсли;
	
	Возврат Структура;

КонецФункции

Функция ОбменДаннымиКампанииПолучить(УчетнаяЗапись) Экспорт
	
	Кампании = Новый Массив;
	
	ДанныеАвторизации = ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетДанныеАвторизацииПолучить(УчетнаяЗапись);
	
	ЯндексПриложениеДанные = ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетПриложениеДанныеПолучить();
	client_id = ЯндексПриложениеДанные.client_id;
	
	// https://yandex.ru/dev/market/partner/doc/dg/reference/get-campaigns.html
	Сервер = ИнтеграцияСЯндексМаркетСервер.СерверПартнерскогоAPIЯндексМаркет();
	ИмяМетода = "/campaigns";
	Адрес = ИмяМетода;
	
	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/x-www-form-urlencoded");
	HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗапись, Заголовки, ДанныеАвторизации );
	
	ssl = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение();
	ИнтернетПрокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси("HTTPS");
	HTTPСоединение = Новый HTTPСоединение(Сервер,,,, ИнтернетПрокси,,ssl);
	
	HTTPЗапрос = Новый HTTPЗапрос( Адрес, Заголовки );
	
	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
		СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
		HTTPЗапросПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPЗапрос );
		
		ЛогКомментарий = НСтр("ru = 'HTTP: Запрос:'") + Символы.ПС
		+ HTTPЗапросПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ЗагрузкаСкладовВ1С );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
	КонецЕсли;	
	
	Попытка
		HTTPОтвет = HTTPСоединение.ВызватьHTTPМетод( "GET", HTTPЗапрос );
	Исключение 
		ТекстОшибки = НСтр("ru = 'ОбменДаннымиКампанииПолучить: Ошибка выполнения запроса:'") + " " + ПодробноеПредставлениеОшибки( ИнформацияОбОшибке() );
		ЗаписьЖурналаРегистрации(ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
		Возврат Неопределено;
	КонецПопытки;	
	
	ЛогЭтоОшибка = HTTPОтвет.КодСостояния <> 200;
	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Или ЛогЭтоОшибка Тогда
		HTTPМетод = "GET";
		HTTPСоединениеПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPСоединение );
		HTTPОтветПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPОтвет );
		
		ЛогКомментарий = НСтр("ru = 'HTTP: Ответ: Метод:'") + " " + HTTPМетод + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
		+ HTTPОтветПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ HTTPСоединениеПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ЗагрузкаСкладовВ1С );
		ЛогПараметры.Вставить( "Сервер", HTTPСоединение.Сервер );
		ЛогПараметры.Вставить( "АдресРесурса", HTTPЗапрос.АдресРесурса );
		
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, ЛогЭтоОшибка, ЛогПараметры );
	КонецЕсли;	
	
	СтрокаОтвета = HTTPОтвет.ПолучитьТелоКакСтроку();
	СтруктураОтвета = ИнтеграцияСЯндексМаркетСервер.ИзJSON(СтрокаОтвета);
	КодСостояния = HTTPОтвет.КодСостояния;

	Если КодСостояния <> 200 Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	КампанииДанные = ИзJSON( СтрокаОтвета );
	
	campaigns = ИнтеграцияСМаркетплейсамиСервер.СтруктураСвойствоЗначениеПолучить( КампанииДанные, "campaigns", Неопределено );
	Если ТипЗнч( campaigns ) <> Тип( "Массив" ) Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Для Каждого Элемент Из campaigns Цикл
		
		НаименованиеМагазина = ИнтеграцияСМаркетплейсамиСервер.СтруктураСвойствоЗначениеПолучить( Элемент, "domain", "" );
		Если ПустаяСтрока( НаименованиеМагазина ) Тогда
			Продолжить;
		КонецЕсли;
		
		ИдентификаторПлательщикаВЯндексБалансе = ИнтеграцияСМаркетплейсамиСервер.СтруктураСвойствоЗначениеПолучить( Элемент, "clientId", 0 );
		Если Не ЗначениеЗаполнено( ИдентификаторПлательщикаВЯндексБалансе ) Тогда
			Продолжить;
		КонецЕсли;
		ИдентификаторПлательщикаВЯндексБалансе = Формат( ИдентификаторПлательщикаВЯндексБалансе, "ЧГ=0" );
		
		ИдентификаторКампании = ИнтеграцияСМаркетплейсамиСервер.СтруктураСвойствоЗначениеПолучить( Элемент, "id", 0 );
		Если Не ЗначениеЗаполнено( ИдентификаторКампании ) Тогда
			Продолжить;
		КонецЕсли;
		ИдентификаторКампании = Формат( ИдентификаторКампании, "ЧГ=0" );
		
		ИдентификаторКабинета = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( Элемент, "business.id", Неопределено );
		ИдентификаторКабинета = Формат( ИдентификаторКабинета, "ЧГ=0" );
		
		Значение = Новый Структура( "НаименованиеМагазина, ИдентификаторПлательщикаВЯндексБалансе, ИдентификаторКампании, ИдентификаторКабинета",
		НаименованиеМагазина, ИдентификаторПлательщикаВЯндексБалансе, ИдентификаторКампании, ИдентификаторКабинета );
		
		Кампании.Добавить( Значение );
		
	КонецЦикла;

	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
        КампанииПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( Кампании );
        
		ЛогКомментарий = НСтр("ru = 'ОбменДаннымиКампанииПолучить: Кампании: '") + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
		+ КампанииПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ЗагрузкаСкладовВ1С );
		
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
	КонецЕсли;	
	
	Возврат Кампании;
КонецФункции

Функция ЯндексМаркетПриложениеДанныеПолучить() Экспорт

	Данные = Новый Структура;
	
	Данные.Вставить( "client_id", "cc9b7be075074e6caaff515a3c5ef0e5" );
	Данные.Вставить( "client_secret", "dc807379134444cca2a27e21c9bf9d7d" );
	
	Возврат Данные;
КонецФункции

Функция ОбменДаннымиВыгрузитьЦеныИз1СВЯндексМаркет( УчетнаяЗапись ) Экспорт
    
	Склады = СкладыСоответствияПолучить( УчетнаяЗапись );
	Если Склады.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;

	Статусы = Новый Массив;
	Статусы.Добавить(Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияПройдена);
	Статусы.Добавить(Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации);
	Статусы.Добавить(Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена);
	
	ВидыЦен = Склады.Скопировать( , "ВидЦены" );
	ВидыЦен.Свернуть( "ВидЦены" );
	ВидыЦен = ВидыЦен.ВыгрузитьКолонку( "ВидЦены" );
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр( "Статусы", Статусы );
	Запрос.УстановитьПараметр( "ВидыЦен", ВидыЦен );
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись );
	
	// выгрузка только измененных цен от последней успешной выгрузки
	
	Запрос.Текст = 
	"ВЫБРАТЬ РАЗЛИЧНЫЕ ПЕРВЫЕ 1000
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура КАК Номенклатура,
	|	СтатусыПубликацииТоваровЯндексМаркет.Характеристика КАК Характеристика,
	|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторТовараПлощадки КАК ИдентификаторТовараПлощадки,
	|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации КАК ИдентификаторПубликации,
	|	ЦеныНоменклатурыСрезПоследних.Цена КАК Цена,
	|	ЦеныНоменклатурыСрезПоследних.ВидЦен КАК ВидЦены,
	|	ЦеныНоменклатурыСрезПоследних.Период КАК Период,
	|	ЕСТЬNULL(ВыгруженныеЦеныМаркетплейсов.Цена, 0) КАК ВыгруженнаяЦена
	|ИЗ
	|	РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет КАК СтатусыПубликацииТоваровЯндексМаркет
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ЦеныНоменклатуры.СрезПоследних(, ВидЦен В (&ВидыЦен)) КАК ЦеныНоменклатурыСрезПоследних
	|		ПО СтатусыПубликацииТоваровЯндексМаркет.Номенклатура = ЦеныНоменклатурыСрезПоследних.Номенклатура
	|			И СтатусыПубликацииТоваровЯндексМаркет.Характеристика = ЦеныНоменклатурыСрезПоследних.Характеристика
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ВыгруженныеЦеныМаркетплейсов КАК ВыгруженныеЦеныМаркетплейсов
	|		ПО СтатусыПубликацииТоваровЯндексМаркет.Номенклатура = ВыгруженныеЦеныМаркетплейсов.Номенклатура
	|			И СтатусыПубликацииТоваровЯндексМаркет.Характеристика = ВыгруженныеЦеныМаркетплейсов.Характеристика
	|			И СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = ВыгруженныеЦеныМаркетплейсов.УчетнаяЗаписьМаркетплейса
	|			И (ЦеныНоменклатурыСрезПоследних.ВидЦен = ВыгруженныеЦеныМаркетплейсов.ВидЦены)
	|ГДЕ
	|	НЕ ЦеныНоменклатурыСрезПоследних.Номенклатура ЕСТЬ NULL
	|	И СтатусыПубликацииТоваровЯндексМаркет.Статус В(&Статусы)
	|	И СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись
	|	И ЦеныНоменклатурыСрезПоследних.Цена <> ЕСТЬNULL(ВыгруженныеЦеныМаркетплейсов.Цена, 0)";
	
	ЦеныДляВыгрузки = Запрос.Выполнить().Выгрузить();
	
	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
		СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
		
		ЦеныДляВыгрузкиПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( ЦеныДляВыгрузки );
		
		ЛогКомментарий = НСтр("ru = 'ОбменДаннымиВыгрузитьЦеныИз1СВЯндексМаркет: Цены:'") + " " +  Символы.ПС
		+ ЦеныДляВыгрузкиПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаЦенИз1С );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
	КонецЕсли;	
	
	Если ЦеныДляВыгрузки.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;

    УникальнаяЦенаДляКаждогоМагазина = 
    ОбщегоНазначения.ЗначениеРеквизитаОбъекта( УчетнаяЗапись, "УникальнаяЦенаДляКаждогоМагазина" );// если истина, то campaigns, иначе businesses
	
	Если УникальнаяЦенаДляКаждогоМагазина = Истина Тогда
		// группировка цен по складам/магазинам
		
		// каждый склад это отдельная кампания в Яндекс маркет, нужен отдельный HTTPЗапрос
		ЦеныДляВыгрузкиПоСкладам = Склады.Скопировать( , "ВидЦены, ИдентификаторКампанииМаркетплейса" );
		ЦеныДляВыгрузкиПоСкладам.Свернуть( "ВидЦены, ИдентификаторКампанииМаркетплейса" );
		
		ЦеныДляВыгрузкиПоСкладам.Колонки.Добавить( "offers" );
		ЦеныДляВыгрузкиПоСкладам.Колонки.Добавить( "Цены" );
		ЦеныДляВыгрузкиПоСкладам.Колонки.Добавить( "Результат" );
		
		ЦеныДляВыгрузки.Индексы.Добавить( "ВидЦены" );
		
		КешЦеныДляВыгрузки = Новый Соответствие;
		ОтборПоЦенам = Новый Структура( "ВидЦены" );
		Для Каждого ЦеныДляВыгрузкиПоСкладамСтрока Из ЦеныДляВыгрузкиПоСкладам Цикл
			
			ДанныеИзКеша = КешЦеныДляВыгрузки.Получить( ЦеныДляВыгрузкиПоСкладамСтрока.ВидЦены );
			Если ДанныеИзКеша <> Неопределено Тогда
				ЦеныДляВыгрузкиПоСкладамСтрока.offers = ДанныеИзКеша.offers;
				ЦеныДляВыгрузкиПоСкладамСтрока.Цены = ДанныеИзКеша.Цены;
				Продолжить;
			КонецЕсли;
			
			ОтборПоЦенам.ВидЦены = ЦеныДляВыгрузкиПоСкладамСтрока.ВидЦены;
			ЦеныПоСкладу = ЦеныДляВыгрузки.Скопировать( ОтборПоЦенам );
			
			offers = Новый Массив;
			Для Каждого ЦеныПоСкладуСтрока Из ЦеныПоСкладу Цикл
				
				СтруктураПредложения = Новый Структура( "offerId, price" );
				СтруктураПредложения.offerId = ЦеныПоСкладуСтрока.ИдентификаторПубликации;
				СтруктураЦены = Новый Структура( "currencyId, value" );
				СтруктураЦены.currencyId = "RUR";
				СтруктураЦены.value = ЦеныПоСкладуСтрока.Цена;
				СтруктураПредложения.price = СтруктураЦены;
				
				offers.Добавить( СтруктураПредложения );
			КонецЦикла;
			
			ЦеныДляВыгрузкиПоСкладамСтрока.offers = offers;
			ЦеныДляВыгрузкиПоСкладамСтрока.Цены = ЦеныПоСкладу;
			
			ДанныеВКеш = Новый Структура( "offers, Цены" );
			ДанныеВКеш.offers = offers;
			ДанныеВКеш.Цены = ЦеныПоСкладу;
			КешЦеныДляВыгрузки.Вставить( ЦеныДляВыгрузкиПоСкладамСтрока.ВидЦены, ДанныеВКеш );
			
		КонецЦикла;
		
		ЦеныДляВыгрузкиВСервис = ЦеныДляВыгрузкиПоСкладам;
		ИмяМетода = "/campaigns/%1/offer-prices/updates.json";
			
	Иначе
		// группировка цен по кабинетам
		
		ЦеныДляВыгрузкиПоКабинетам = Склады.Скопировать( , "ВидЦены, ИдентификаторКабинетаМаркетплейса" );
		ЦеныДляВыгрузкиПоКабинетам.Свернуть( "ВидЦены, ИдентификаторКабинетаМаркетплейса" );
		
		ЦеныДляВыгрузкиПоКабинетам.Колонки.Добавить( "offers" );
		ЦеныДляВыгрузкиПоКабинетам.Колонки.Добавить( "Цены" );
		ЦеныДляВыгрузкиПоКабинетам.Колонки.Добавить( "Результат" );
		
		ЦеныДляВыгрузки.Индексы.Добавить( "ВидЦены" );
		
		КешЦеныДляВыгрузки = Новый Соответствие;
		ОтборПоЦенам = Новый Структура( "ВидЦены" );
		Для Каждого ЦеныДляВыгрузкиПоКабинетамСтрока Из ЦеныДляВыгрузкиПоКабинетам Цикл
			
			ДанныеИзКеша = КешЦеныДляВыгрузки.Получить( ЦеныДляВыгрузкиПоКабинетамСтрока.ВидЦены );
			Если ДанныеИзКеша <> Неопределено Тогда
				ЦеныДляВыгрузкиПоКабинетамСтрока.offers = ДанныеИзКеша.offers;
				ЦеныДляВыгрузкиПоКабинетамСтрока.Цены = ДанныеИзКеша.Цены;
				Продолжить;
			КонецЕсли;
			
			ОтборПоЦенам.ВидЦены = ЦеныДляВыгрузкиПоКабинетамСтрока.ВидЦены;
			ЦеныПоКабинету = ЦеныДляВыгрузки.Скопировать( ОтборПоЦенам );
			
			offers = Новый Массив;
			Для Каждого ЦеныПоКабинетуСтрока Из ЦеныПоКабинету Цикл
				
				СтруктураПредложения = Новый Структура( "offerId, price" );
				СтруктураПредложения.offerId = ЦеныПоКабинетуСтрока.ИдентификаторПубликации;
				СтруктураЦены = Новый Структура( "currencyId, value" );
				СтруктураЦены.currencyId = "RUR";
				СтруктураЦены.value =  Формат( ЦеныПоКабинетуСтрока.Цена, "ЧДЦ=2; ЧРД=.; ЧН=; ЧГ=0");
				СтруктураПредложения.price = СтруктураЦены;
				
				offers.Добавить( СтруктураПредложения );
			КонецЦикла;
			
			ЦеныДляВыгрузкиПоКабинетамСтрока.offers = offers;
			ЦеныДляВыгрузкиПоКабинетамСтрока.Цены = ЦеныПоКабинету;
			
			ДанныеВКеш = Новый Структура( "offers, Цены" );
			ДанныеВКеш.offers = offers;
			ДанныеВКеш.Цены = ЦеныПоКабинету;
			КешЦеныДляВыгрузки.Вставить( ЦеныДляВыгрузкиПоКабинетамСтрока.ВидЦены, ДанныеВКеш );
			
		КонецЦикла;
		
		ЦеныДляВыгрузкиВСервис = ЦеныДляВыгрузкиПоКабинетам;
		ИмяМетода = "/businesses/%1/offer-prices/updates.json";
		
	КонецЕсли;
 
 	ДанныеАвторизации = ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетДанныеАвторизацииПолучить(УчетнаяЗапись);
	
	ssl = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение();
	Сервер = ИнтеграцияСЯндексМаркетСервер.СерверПартнерскогоAPIЯндексМаркет();
	
	ИнтернетПрокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси("HTTPS");
	HTTPСоединение = Новый HTTPСоединение(Сервер,,,, ИнтернетПрокси,,ssl);
	
	client_id = ЯндексМаркетПриложениеДанныеПолучить().client_id;

	Заголовки = Новый Соответствие;
	Заголовки.Вставить( "Content-Type", "application/json" );
	HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗапись, Заголовки, ДанныеАвторизации );

	БылиОшибки = Ложь;	
	Для Каждого ЦеныДляВыгрузкиВСервисСтрока Из ЦеныДляВыгрузкиВСервис Цикл

		offers = ЦеныДляВыгрузкиВСервисСтрока.offers;
		Если offers.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		Если УникальнаяЦенаДляКаждогоМагазина = Истина Тогда
			ИдентификаторДляАдресаСервиса = СокрЛП( ЦеныДляВыгрузкиВСервисСтрока.ИдентификаторКампанииМаркетплейса );
		Иначе
			ИдентификаторДляАдресаСервиса = СокрЛП( ЦеныДляВыгрузкиВСервисСтрока.ИдентификаторКабинетаМаркетплейса );
		КонецЕсли;
		Адрес = СтрШаблон( ИмяМетода, ИдентификаторДляАдресаСервиса );

		
		СтруктураДляJSON = Новый Структура;
		СтруктураДляJSON.Вставить( "offers", offers );
		ТелоЗапроса = ИнтеграцияСЯндексМаркетСервер.ВJSON( СтруктураДляJSON );
		
		HTTPЗапрос = Новый HTTPЗапрос( Адрес, Заголовки );
		HTTPЗапрос.УстановитьТелоИзСтроки( ТелоЗапроса, "UTF-8" );

		Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
			HTTPЗапросПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPЗапрос );
			
			ЛогКомментарий = НСтр("ru = 'HTTP: Запрос:'") + Символы.ПС
			+ HTTPЗапросПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			
			ЛогПараметры = Новый Структура;
			ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаЦенИз1С );
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
		КонецЕсли;	

		Попытка
			HTTPОтвет = HTTPСоединение.ОтправитьДляОбработки( HTTPЗапрос );
		Исключение 
			ТекстОшибки = НСтр("ru = 'Выгрузка цены из 1С в Яндекс маркет: Ошибка выполнения запроса'") + ": " + Символы.ПС + ПодробноеПредставлениеОшибки( ИнформацияОбОшибке() );
			ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
			ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );
			БылиОшибки = Истина;
			Продолжить;
		КонецПопытки;	

		СтрокаОтвета = HTTPОтвет.ПолучитьТелоКакСтроку();
		СтруктураОтвета = ИнтеграцияСЯндексМаркетСервер.ИзJSON(СтрокаОтвета);
		КодСостояния = HTTPОтвет.КодСостояния;

		Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
			HTTPМетод = "POST";
			HTTPСоединениеПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPСоединение );
			HTTPОтветПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPОтвет );
			
			ЛогКомментарий = НСтр("ru = 'HTTP: Ответ: Метод:'") + " " + HTTPМетод + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
			+ HTTPОтветПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ HTTPСоединениеПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			
			ЛогПараметры = Новый Структура;
			ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаЦенИз1С );
			ЛогПараметры.Вставить( "Сервер", HTTPСоединение.Сервер );
			ЛогПараметры.Вставить( "АдресРесурса", HTTPЗапрос.АдресРесурса );
			
			ЛогЭтоОшибка = HTTPОтвет.КодСостояния <> 200;
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, ЛогЭтоОшибка, ЛогПараметры );
		КонецЕсли;	
		
		Если КодСостояния = 200 И СтруктураОтвета.status = "OK" Тогда
			
			ЗаписатьДатуУстановкиЦены( УчетнаяЗапись, ЦеныДляВыгрузкиВСервисСтрока.Цены );
			
		Иначе
			
			СобытиеОписание = "Выгрузка цены из 1С в Яндекс маркет: Идентификатор "
			+ ?( УникальнаяЦенаДляКаждогоМагазина = Истина, "кампании", "кабинета" )
			+ " """ + ИдентификаторДляАдресаСервиса + """";
			
			ТекстОшибки = СтрокаОтвета;
			ТекстОшибкиПояснение = ОбменСВнешнимиСистемамиЛогирование.БазаЗнанийСовпаденияНайтиСтрокойПолучить( "Yandex: " + ТекстОшибки );
			ТекстОшибкиПодробный = СобытиеОписание + ": " + ?(ПустаяСтрока( ТекстОшибкиПояснение ), "", ТекстОшибкиПояснение + " " ) + ТекстОшибки;
			
			ОбщегоНазначения.СообщитьПользователю( ТекстОшибкиПодробный );
			ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибкиПодробный);
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибкиПодробный, УчетнаяЗапись, Истина );
			
			БылиОшибки = Истина;
			Продолжить;
			
		КонецЕсли;
		
	КонецЦикла;// ЦеныДляВыгрузкиПоСкладам

	ВыгруженныеЦеныНеактуальныеУдалить( УчетнаяЗапись );
	
	Возврат (БылиОшибки = Ложь);
КонецФункции

Процедура ЗаписатьДатуУстановкиЦены( УчетнаяЗапись, Цены )

	Для Каждого ЦеныСтрока Из Цены Цикл
	
		Набор = РегистрыСведений.СтатусыПубликацииТоваровЯндексМаркет.СоздатьНаборЗаписей();
		
		Набор.Отбор.УчетнаяЗаписьМаркетплейса.Установить( УчетнаяЗапись );
		Набор.Отбор.Номенклатура.Установить( ЦеныСтрока.Номенклатура );
		Набор.Отбор.Характеристика.Установить( ЦеныСтрока.Характеристика );
		
		Набор.Прочитать();
		
		ДатаУстановкиЦены = ТекущаяДатаСеанса();
		Если Набор.Количество() > 0 Тогда
			Запись = Набор[0];
			Запись.ДатаУстановкиЦены = ДатаУстановкиЦены;
			Набор.Записать();
		КонецЕсли;
		
		// запишем новое значение выгруженной цены
		Запись = РегистрыСведений.ВыгруженныеЦеныМаркетплейсов.СоздатьМенеджерЗаписи();
		Запись.УчетнаяЗаписьМаркетплейса = УчетнаяЗапись;
		Запись.ВидЦены = ЦеныСтрока.ВидЦены;
		Запись.Номенклатура = ЦеныСтрока.Номенклатура;
		Запись.Характеристика = ЦеныСтрока.Характеристика;
		Запись.Цена = ЦеныСтрока.Цена;
		Запись.ДатаВыгрузки = ДатаУстановкиЦены;
		Запись.Записать();
		
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

// Проверяет, является ли строка целым неотрицательным числом	
// Параметры:
//  ПроверяемаяСтрока - Строка - проверяемый строка.
//
// Возвращаемое значение:
//  Булево - Истина, если строка содержит только цифры.
//
Функция ЭтаСтрокаЯвляетсяЦелымНеотрицательнымЧислом(ПроверяемаяСтрока) Экспорт
	
	СтрокаЯвляетсяЧислом = Истина;
	ПроверяемаяСтрока = СокрЛП(ПроверяемаяСтрока);
	Длина = СтрДлина(ПроверяемаяСтрока);
	
	Для НомерСимвола = 1 По Длина Цикл
		Симв = Сред(ПроверяемаяСтрока, НомерСимвола, 1);
		Если Найти("0123456789", Симв) = 0 И КодСимвола(Симв)<>160 Тогда
			СтрокаЯвляетсяЧислом = Ложь;		
		КонецЕсли;		
	КонецЦикла;
	
	Возврат СтрокаЯвляетсяЧислом;
	
КонецФункции

Функция ВJSON( Структура )Экспорт
	
	ЗаписьJSON = Новый ЗаписьJSON;
	ЗаписьJSON.УстановитьСтроку();	
	ЗаписьJSON.ПроверятьСтруктуру = Истина;
	
	Попытка
		ЗаписатьJSON( ЗаписьJSON, Структура );
	Исключение
		ИнформацияОбОшибке = ИнформацияОбОшибке();
		
		ИнформацияОбОшибкеПричина = ОбменСВнешнимиСистемамиЛогирование.ИнформацияОбОшибкеПричина( ИнформацияОбОшибке );
		СтруктураПредставление = СтрЗаменить( ЗначениеВСтрокуВнутр( Структура ), Символы.ПС, "" );
		
		ТекстОшибки = "Ошибка при подготовке JSON: " + ИнформацияОбОшибкеПричина + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ СтруктураПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ ПодробноеПредставлениеОшибки( ИнформацияОбОшибке );
		
		ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
		ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, , Истина );
		
		ВызватьИсключение ТекстОшибки;
	КонецПопытки;

	Результат = ЗаписьJSON.Закрыть();
	
	Возврат Результат;
	
КонецФункции

Функция ИзJSON( Строка, СтрДат = "", ФлСоответствие = Ложь )Экспорт
	
	ЧтениеJSON = Новый ЧтениеJSON;
	ЧтениеJSON.УстановитьСтроку(Строка);
	ЗапросСтруктура = ПрочитатьJSON(ЧтениеJSON, ФлСоответствие, СтрДат );
	
	Возврат ЗапросСтруктура;
	
КонецФункции

Функция ЯндексМаркетСобытиеЖурналаРегистрацииПолучить() Экспорт
	Возврат НСтр( "ru = 'Яндекс маркет'" );
КонецФункции

Функция ОбменДаннымиВыгрузитьОстаткиТоваровВЯндексМаркет( УчетнаяЗапись ) Экспорт

	ДанныеАвторизации = Неопределено;
	ДанныеАвторизацииПомещеныВХранилище = 
	ЯндексМаркетПроверитьДанныеАвторизацииПомещеныВХранилище( УчетнаяЗапись, ДанныеАвторизации );
	Если Не ДанныеАвторизацииПомещеныВХранилище Тогда
		ТекстСообщения = НСтр( "ru = 'Ошибка при выгрузке остатков товаров в Яндекс Маркет: Токен для авторизации не задан. Необходимо выполнить авторизацию в форме настройки.'" );
		ОбщегоНазначения.СообщитьПользователю( ТекстСообщения );
		Возврат Ложь;
	КонецЕсли;
	
	ЗапросТекст = 
	"ВЫБРАТЬ ПЕРВЫЕ 2000
	|	СтатусыПубликацииТоваровЯндексМаркет.Номенклатура КАК Номенклатура,
	|	СтатусыПубликацииТоваровЯндексМаркет.Характеристика КАК Характеристика,
	|	ВЫРАЗИТЬ(СоответствияОбъектовМаркетплейсов.Объект1С КАК Справочник.СтруктурныеЕдиницы) КАК Склад,
	|	СоответствияОбъектовМаркетплейсов.ИдентификаторОбъектаМаркетплейса КАК СкладМаркетплейсИдентификатор,
	|	СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации КАК ТоварМаркетплейсИдентификатор,
	|	СоответствияОбъектовМаркетплейсов.ИдентификаторВладельцаОбъектаМаркетплейса КАК ИдентификаторКампанииМаркетплейса
	|ПОМЕСТИТЬ ВТТоварыИСклады
	|ИЗ
	|	РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет КАК СтатусыПубликацииТоваровЯндексМаркет
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ РегистрСведений.СоответствияОбъектовМаркетплейсов КАК СоответствияОбъектовМаркетплейсов
	|		ПО (СоответствияОбъектовМаркетплейсов.УчетнаяЗаписьМаркетплейса.ВидМаркетплейса = ЗНАЧЕНИЕ(Перечисление.ВидыМаркетплейсов.МаркетплейсЯндексМаркет))
	|			И (СоответствияОбъектовМаркетплейсов.ВидОбъектаМаркетплейса = ЗНАЧЕНИЕ(Перечисление.ВидыОбъектовМаркетплейсов.Склад))
	|			И (СоответствияОбъектовМаркетплейсов.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись)
	|			И (СоответствияОбъектовМаркетплейсов.МодельСклада <> &МодельСкладаFBY)
	|			И ((ВЫРАЗИТЬ(СоответствияОбъектовМаркетплейсов.Объект1С КАК Справочник.СтруктурныеЕдиницы)) <> ЗНАЧЕНИЕ(Справочник.СтруктурныеЕдиницы.ПустаяСсылка))
	|ГДЕ
	|	СтатусыПубликацииТоваровЯндексМаркет.Статус = ЗНАЧЕНИЕ(Перечисление.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияПройдена)
	|	И СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТТоварыИСклады.Номенклатура КАК Номенклатура,
	|	ВТТоварыИСклады.ТоварМаркетплейсИдентификатор КАК ТоварМаркетплейсИдентификатор,
	|	ВТТоварыИСклады.Характеристика КАК Характеристика,
	|	ВТТоварыИСклады.Склад КАК Склад,
	|	ВТТоварыИСклады.СкладМаркетплейсИдентификатор КАК СкладМаркетплейсИдентификатор,
	|	СУММА(ЕСТЬNULL(ОстаткиТоваров.Количество, 0)) КАК Остаток,
	|	СУММА(ЕСТЬNULL(ОстаткиТоваровМаркетплейсов.Количество, 0)) КАК ОстатокВыгружен,
	|	ВТТоварыИСклады.ИдентификаторКампанииМаркетплейса КАК ИдентификаторКампанииМаркетплейса
	|ИЗ
	|	ВТТоварыИСклады КАК ВТТоварыИСклады
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ОстаткиТоваров КАК ОстаткиТоваров
	|		ПО (ОстаткиТоваров.СтруктурнаяЕдиница = ВТТоварыИСклады.Склад)
	|			И (ОстаткиТоваров.Характеристика = ВТТоварыИСклады.Характеристика)
	|			И (ОстаткиТоваров.Номенклатура = ВТТоварыИСклады.Номенклатура)
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.ОстаткиТоваровМаркетплейсов КАК ОстаткиТоваровМаркетплейсов
	|		ПО ВТТоварыИСклады.Номенклатура = ОстаткиТоваровМаркетплейсов.Номенклатура
	|			И ВТТоварыИСклады.Характеристика = ОстаткиТоваровМаркетплейсов.Характеристика
	|			И ВТТоварыИСклады.Склад = ОстаткиТоваровМаркетплейсов.Склад
	|			И (ОстаткиТоваровМаркетплейсов.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись)
	|
	|СГРУППИРОВАТЬ ПО
	|	ВТТоварыИСклады.Склад,
	|	ВТТоварыИСклады.СкладМаркетплейсИдентификатор,
	|	ВТТоварыИСклады.Номенклатура,
	|	ВТТоварыИСклады.ТоварМаркетплейсИдентификатор,
	|	ВТТоварыИСклады.Характеристика,
	|	ВТТоварыИСклады.ИдентификаторКампанииМаркетплейса
	|
	|ИМЕЮЩИЕ
	|	СУММА(ЕСТЬNULL(ОстаткиТоваров.Количество, 0)) <> СУММА(ЕСТЬNULL(ОстаткиТоваровМаркетплейсов.Количество, 0))";
	
	Запрос = Новый Запрос( ЗапросТекст );
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись);
	Запрос.УстановитьПараметр( "МодельСкладаFBY", ПредопределенноеЗначение( "Перечисление.МоделиСкладовЯндексМаркета.FBY" ) );
	Остатки = Запрос.Выполнить().Выгрузить();

	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
		СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
		
		ОстаткиПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( Остатки );
		
		ЛогКомментарий = НСтр("ru = 'ОбменДаннымиВыгрузитьОстаткиТоваровВЯндексМаркет: Остатки:'") + " " +  Символы.ПС
		+ ОстаткиПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаОстатковИз1С );
		
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
	КонецЕсли;	
	
	Если Остатки.Количество() = 0 Тогда
		Возврат Истина;
	КонецЕсли;
	
	// каждый склад это отдельная кампания в Яндекс маркет, нужен отдельный HTTPЗапрос
	ОстаткиДляВыгрузкиПоСкладам = Остатки.Скопировать( , "Склад, ИдентификаторКампанииМаркетплейса" );
	
	ОстаткиДляВыгрузкиПоСкладам.Свернуть( "Склад, ИдентификаторКампанииМаркетплейса" );
	
	ОстаткиДляВыгрузкиПоСкладам.Колонки.Добавить( "ОстаткиДляВыгрузки" );
	ОстаткиДляВыгрузкиПоСкладам.Колонки.Добавить( "Результат" );

	Остатки.Индексы.Добавить( "Склад" );
	
	ОтборПоСкладам = Новый Структура( "Склад" );
	Для Каждого ОстаткиДляВыгрузкиПоСкладамСтрока Из ОстаткиДляВыгрузкиПоСкладам Цикл
	
		ОтборПоСкладам.Склад = ОстаткиДляВыгрузкиПоСкладамСтрока.Склад;
		ОстаткиПоСкладу = Остатки.НайтиСтроки( ОтборПоСкладам );
		
		ОстаткиДляВыгрузки = Новый Массив;	
		Для Каждого ОстаткиПоСкладуСтрока Из ОстаткиПоСкладу Цикл
			
			СтруктураОстатков = Новый Структура( "sku, warehouseId, items" );
			
			СтруктураОстатков.sku = ОстаткиПоСкладуСтрока.ТоварМаркетплейсИдентификатор;  
			СтруктураОстатков.warehouseId = ОстаткиПоСкладуСтрока.СкладМаркетплейсИдентификатор;
			
			СтруктураПозиции = Новый Структура( "type, count, updatedAt" );
			
			СтруктураПозиции.type = "FIT";// из документации API Яндекса: "type - Этот параметр пока не используется. Всегда передавайте одно значение — FIT"
			СтруктураПозиции.count = ОстаткиПоСкладуСтрока.Остаток;
			СтруктураПозиции.updatedAt = ЗаписатьДатуJSON( ТекущаяДатаСеанса(), ФорматДатыJSON.ISO, ВариантЗаписиДатыJSON.УниверсальнаяДата );
			
			items = Новый Массив;
			items.Добавить( СтруктураПозиции );
			
			СтруктураОстатков.items = items;// из документации API Яндекса: "items - Информация об остатках товара. Представлена в виде массива, состоящего из одного объекта."
			
			ОстаткиДляВыгрузки.Добавить( СтруктураОстатков );
		КонецЦикла;
		
		ОстаткиДляВыгрузкиПоСкладамСтрока.ОстаткиДляВыгрузки = ОстаткиДляВыгрузки;
	КонецЦикла;

	ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
	
	ИнтернетПрокси = Неопределено;
	Если ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.ПолучениеФайловИзИнтернета") Тогда
		МодульПолучениеФайловИзИнтернета = ОбщегоНазначения.ОбщийМодуль("ПолучениеФайловИзИнтернета");
		ИнтернетПрокси = МодульПолучениеФайловИзИнтернета.ПолучитьПрокси("https");
	КонецЕсли;

	Сервер = ИнтеграцияСЯндексМаркетСервер.СерверПартнерскогоAPIЯндексМаркет();
	ИмяМетода = "/campaigns/%1/offers/stocks.json";	
	Таймаут = 180;
	СоединениеOpenSSL = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение();
	HTTPСоединение = Новый HTTPСоединение( Сервер, , , , ИнтернетПрокси, Таймаут, СоединениеOpenSSL );

	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Content-Type", "application/json");
	HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗапись, Заголовки, ДанныеАвторизации );
    
	СтруктураДляJSON = Новый Структура;
	
    // отправим запросы по остаткам в Яндекс маркет
	Для Каждого ОстаткиДляВыгрузкиПоСкладамСтрока Из ОстаткиДляВыгрузкиПоСкладам Цикл
		
		campaign_id = СокрЛП( ОстаткиДляВыгрузкиПоСкладамСтрока.ИдентификаторКампанииМаркетплейса );
		Адрес = СтрШаблон( ИмяМетода, campaign_id );
		
		ОстаткиДляВыгрузки = ОстаткиДляВыгрузкиПоСкладамСтрока.ОстаткиДляВыгрузки;
		
		СтруктураДляJSON.Вставить( "skus", ОстаткиДляВыгрузки );
		
		ТелоЗапроса = ИнтеграцияСЯндексМаркетСервер.ВJSON( СтруктураДляJSON );
		
		HTTPЗапрос = Новый HTTPЗапрос( Адрес, Заголовки );
		HTTPЗапрос.УстановитьТелоИзСтроки( ТелоЗапроса, "UTF-8");
		
		Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
			СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
			
			HTTPЗапросПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPЗапрос );
			
			ЛогКомментарий = НСтр("ru = 'HTTP: Запрос:'") + Символы.ПС
			+ HTTPЗапросПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			
			ЛогПараметры = Новый Структура;
			ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаОстатковИз1С );
			
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, , ЛогПараметры );
		КонецЕсли;	
		
		Попытка
			HTTPОтвет = HTTPСоединение.Записать( HTTPЗапрос );
		Исключение 
			ТекстОшибки = НСтр("ru = 'Ошибка при выгрузке остатков товаров в Яндекс Маркет:'") + " " + campaign_id + ": " + ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
			ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );
			ОстаткиДляВыгрузкиПоСкладамСтрока.Результат = Ложь;
			Продолжить;
		КонецПопытки;	
		
		Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗапись ) Тогда
			СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
			
			HTTPМетод = "PUT";
			HTTPСоединениеПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPСоединение );
			HTTPОтветПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPОтвет );
			
			ЛогКомментарий = НСтр("ru = 'HTTP: Ответ: Метод:'") + " " + HTTPМетод + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
			+ HTTPОтветПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ HTTPСоединениеПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			
			ЛогПараметры = Новый Структура;
			ЛогПараметры.Вставить( "Действие", Перечисления.ОбменСВнешнимиСистемамиЛогированиеДействия.ВыгрузкаОстатковИз1С );
			ЛогПараметры.Вставить( "Сервер", HTTPСоединение.Сервер );
			ЛогПараметры.Вставить( "АдресРесурса", HTTPЗапрос.АдресРесурса );
			
			ЛогЭтоОшибка = HTTPОтвет.КодСостояния <> 200;
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗапись, ЛогЭтоОшибка, ЛогПараметры );
		КонецЕсли;	
		
		СтрокаОтвета = HTTPОтвет.ПолучитьТелоКакСтроку();
		СтруктураОтвета = ИнтеграцияСЯндексМаркетСервер.ИзJSON(СтрокаОтвета);
		КодСостояния = HTTPОтвет.КодСостояния;
		
		Если КодСостояния= 200 И СтруктураОтвета.status = "OK" Тогда
			
			ВыгруженныеВЯндексОстаткиЗаписать( УчетнаяЗапись, Остатки );
			
		Иначе
			ТекстОшибки = НСтр("ru = 'Ошибка при выгрузке остатков товаров в Яндекс Маркет:'") + " " + campaign_id + ": " + КодСостояния + ": "+ СтрокаОтвета;
			ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
			ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);		
			ОстаткиДляВыгрузкиПоСкладамСтрока.Результат = Ложь;
			Продолжить;
			
		КонецЕсли;
		
		ОстаткиДляВыгрузкиПоСкладамСтрока.Результат = Истина;
	КонецЦикла;
	
	Если ОстаткиДляВыгрузкиПоСкладам.Найти( Ложь, "Результат" ) = Неопределено Тогда
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;
	
КонецФункции

Процедура ВыгруженныеВЯндексОстаткиЗаписать( УчетнаяЗапись, Остатки )
	
	Если Не ЗначениеЗаполнено( УчетнаяЗапись ) Тогда
		ТекстОшибки = НСтр( "ru = 'ВыгруженныеВЯндексОстаткиЗаписать: УчетнаяЗапись не заполнена.'" );
		ЗаписьЖурналаРегистрации(ЯндексМаркетСобытиеЖурналаРегистрацииПолучить(), УровеньЖурналаРегистрации.Ошибка,,,ТекстОшибки);
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗапись, Истина );
		Возврат;
	КонецЕсли;
	
	Для Каждого ОстаткиСтрока Из Остатки Цикл
		Набор = РегистрыСведений.ОстаткиТоваровМаркетплейсов.СоздатьНаборЗаписей(); 
		
		Набор.Отбор.УчетнаяЗаписьМаркетплейса.Установить(УчетнаяЗапись);
		Набор.Отбор.Склад.Установить(ОстаткиСтрока.Склад);
		Набор.Отбор.Номенклатура.Установить(ОстаткиСтрока.Номенклатура);
		Набор.Отбор.Характеристика.Установить(ОстаткиСтрока.Характеристика);
		
		Набор.Прочитать();
		Если Набор.Количество() > 0 Тогда
			Запись = Набор[0];
			Запись.Количество = ОстаткиСтрока.Остаток; 
			Запись.ДатаВыгрузкиОстатков = ТекущаяДатаСеанса();
		Иначе
			Запись = Набор.Добавить();  
			Запись.УчетнаяЗаписьМаркетплейса = УчетнаяЗапись; 
			Запись.Склад = ОстаткиСтрока.Склад;
			Запись.Номенклатура = ОстаткиСтрока.Номенклатура;  
			Запись.Характеристика = ОстаткиСтрока.Характеристика;
			Запись.Количество = ОстаткиСтрока.Остаток;
			Запись.ДатаВыгрузкиОстатков = ТекущаяДатаСеанса();	
		КонецЕсли;
		Набор.Записать();
	КонецЦикла; 	
	
	ТоварыДляЗаписи = Остатки.Скопировать();
	ТоварыДляЗаписи.Свернуть( "Номенклатура, Характеристика" );
	
	ЗапросТекст = 
	"ВЫБРАТЬ
	|	ВТТовары.Номенклатура КАК Номенклатура,
	|	ВТТовары.Характеристика КАК Характеристика
	|ПОМЕСТИТЬ ВТТовары
	|ИЗ
	|	&ТоварыДляЗаписи КАК ВТТовары
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ОстаткиТоваровМаркетплейсов.Номенклатура КАК Номенклатура,
	|	ОстаткиТоваровМаркетплейсов.Характеристика КАК Характеристика,
	|	СУММА(ОстаткиТоваровМаркетплейсов.Количество) КАК Остаток
	|ИЗ
	|	РегистрСведений.ОстаткиТоваровМаркетплейсов КАК ОстаткиТоваровМаркетплейсов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТТовары КАК ВТТовары
	|		ПО ОстаткиТоваровМаркетплейсов.Номенклатура = ВТТовары.Номенклатура
	|			И ОстаткиТоваровМаркетплейсов.Характеристика = ВТТовары.Характеристика
	|ГДЕ
	|	ОстаткиТоваровМаркетплейсов.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись
	|
	|СГРУППИРОВАТЬ ПО
	|	ОстаткиТоваровМаркетплейсов.Номенклатура,
	|	ОстаткиТоваровМаркетплейсов.Характеристика";
	
	Запрос = Новый Запрос( ЗапросТекст );
	Запрос.УстановитьПараметр( "ТоварыДляЗаписи", ТоварыДляЗаписи );
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись );
	ОбщиеОстаткиБезСкладов = Запрос.Выполнить().Выбрать();
	
	Пока ОбщиеОстаткиБезСкладов.Следующий() Цикл	
		Набор = РегистрыСведений.СтатусыПубликацииТоваровЯндексМаркет.СоздатьНаборЗаписей();
		
		Набор.Отбор.УчетнаяЗаписьМаркетплейса.Установить( УчетнаяЗапись );
		Набор.Отбор.Номенклатура.Установить( ОбщиеОстаткиБезСкладов.Номенклатура );
		Набор.Отбор.Характеристика.Установить( ОбщиеОстаткиБезСкладов.Характеристика );
		
		Набор.Прочитать();
		
		Если Набор.Количество() > 0 Тогда
			Запись = Набор[0];
			Запись.ОстатокВУпаковках = ОбщиеОстаткиБезСкладов.Остаток;
			Набор.Записать();
		КонецЕсли; 
	КонецЦикла;
	
КонецПроцедуры

Функция СкладыСоответствияПолучить( УчетнаяЗапись ) Экспорт

	ЗапросТекст = 
	"ВЫБРАТЬ
	|	СоответствияОбъектовМаркетплейсов.ИдентификаторОбъектаМаркетплейса КАК СкладМаркетплейсИдентификатор,
	|	СоответствияОбъектовМаркетплейсов.НаименованиеОбъектаМаркетплейса КАК НаименованиеСкладаМаркетплейса,
	|	СоответствияОбъектовМаркетплейсов.Объект1С КАК Склад1С,
	|	СоответствияОбъектовМаркетплейсов.ИдентификаторВладельцаОбъектаМаркетплейса КАК ИдентификаторКампанииМаркетплейса,
	|	СоответствияОбъектовМаркетплейсов.ВидЦены КАК ВидЦены,
	|	СоответствияОбъектовМаркетплейсов.МодельСклада КАК МодельСклада,
	|	СоответствияОбъектовМаркетплейсов.УчетнаяЗаписьМаркетплейса КАК УчетнаяЗаписьМаркетплейса,
	|	СоответствияОбъектовМаркетплейсов.ИдентификаторКабинета КАК ИдентификаторКабинетаМаркетплейса
	|ИЗ
	|	РегистрСведений.СоответствияОбъектовМаркетплейсов КАК СоответствияОбъектовМаркетплейсов
	|ГДЕ
	|	СоответствияОбъектовМаркетплейсов.ВидОбъектаМаркетплейса = ЗНАЧЕНИЕ(Перечисление.ВидыОбъектовМаркетплейсов.Склад)
	|	И СоответствияОбъектовМаркетплейсов.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись";
	
	Запрос = Новый Запрос( ЗапросТекст );
	
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись );
	
	Возврат Запрос.Выполнить().Выгрузить();
КонецФункции

Функция ИдентификаторыКабинетовИзСоответствияСкладовПолучить( УчетнаяЗапись ) Экспорт

	ЗапросТекст = 
	"ВЫБРАТЬ
	|	СоответствияОбъектовМаркетплейсов.ИдентификаторКабинета КАК ИдентификаторКабинетаМаркетплейса
	|ИЗ
	|	РегистрСведений.СоответствияОбъектовМаркетплейсов КАК СоответствияОбъектовМаркетплейсов
	|ГДЕ
	|	СоответствияОбъектовМаркетплейсов.ВидОбъектаМаркетплейса = ЗНАЧЕНИЕ(Перечисление.ВидыОбъектовМаркетплейсов.Склад)
	|	И СоответствияОбъектовМаркетплейсов.ИдентификаторКабинета > """"
	|	И СоответствияОбъектовМаркетплейсов.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись
	|
	|СГРУППИРОВАТЬ ПО
	|	СоответствияОбъектовМаркетплейсов.ИдентификаторКабинета";
	
	Запрос = Новый Запрос( ЗапросТекст );
	
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись );
	
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку( "ИдентификаторКабинетаМаркетплейса" );
КонецФункции

Процедура СкладыСоответствияЗаписать( УчетнаяЗапись, Склады ) Экспорт
	
	НаборЗаписей = РегистрыСведений.СоответствияОбъектовМаркетплейсов.СоздатьНаборЗаписей();   
	
	НаборЗаписей.Отбор.УчетнаяЗаписьМаркетплейса.Установить(УчетнаяЗапись);
	НаборЗаписей.Отбор.ВидОбъектаМаркетплейса.Установить(ПредопределенноеЗначение("Перечисление.ВидыОбъектовМаркетплейсов.Склад"));  
			
	ДатаАктуальности = ТекущаяДатаСеанса();
	Для Каждого СкладыСтрока Из Склады Цикл  	
		
		Запись = НаборЗаписей.Добавить();  
		
		Запись.УчетнаяЗаписьМаркетплейса = УчетнаяЗапись;	 	                                                
		Запись.ВидОбъектаМаркетплейса = ПредопределенноеЗначение("Перечисление.ВидыОбъектовМаркетплейсов.Склад"); 
		Запись.ИдентификаторОбъектаМаркетплейса = СкладыСтрока.СкладМаркетплейсИдентификатор; 
		Запись.ИдентификаторВладельцаОбъектаМаркетплейса = СкладыСтрока.ИдентификаторКампанииМаркетплейса; 
		Запись.НаименованиеОбъектаМаркетплейса =  СкладыСтрока.НаименованиеСкладаМаркетплейса; 
		Запись.Объект1С = СкладыСтрока.Склад1С; 
		Запись.ДатаАктуальности = ДатаАктуальности;
		Запись.ВидЦены = СкладыСтрока.ВидЦены; 
		Запись.МодельСклада = СкладыСтрока.МодельСклада; 
		Запись.ИдентификаторКабинета = СкладыСтрока.ИдентификаторКабинетаМаркетплейса;
		
	КонецЦикла;
	
	НаборЗаписей.Записать();  
	
КонецПроцедуры

Функция ВыгруженныеЦеныНеактуальныеУдалить( УчетнаяЗапись )

	// очистим старые записи на случай если менялись виды цен для складов
	
    Склады = СкладыСоответствияПолучить( УчетнаяЗапись );
	АктуальныеВидыЦен = Склады.Скопировать( , "ВидЦены" );
	АктуальныеВидыЦен.Свернуть( "ВидЦены" );
	
	ЗапросТекст = 
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВыгруженныеЦеныМаркетплейсов.ВидЦены КАК ВидЦены
	|ИЗ
	|	РегистрСведений.ВыгруженныеЦеныМаркетплейсов КАК ВыгруженныеЦеныМаркетплейсов
	|ГДЕ
	|	ВыгруженныеЦеныМаркетплейсов.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись
	|	И НЕ ВыгруженныеЦеныМаркетплейсов.ВидЦены В (&АктуальныеВидыЦен)";
	
	Запрос = Новый Запрос( ЗапросТекст );
	Запрос.УстановитьПараметр( "УчетнаяЗапись", УчетнаяЗапись );
	Запрос.УстановитьПараметр( "АктуальныеВидыЦен", АктуальныеВидыЦен );
	
	ЗапросВыборка = Запрос.Выполнить().Выбрать();
	Пока ЗапросВыборка.Следующий() Цикл
		НаборЗаписей = РегистрыСведений.ВыгруженныеЦеныМаркетплейсов.СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.УчетнаяЗаписьМаркетплейса.Установить( УчетнаяЗапись );
		НаборЗаписей.Отбор.ВидЦены.Установить( ЗапросВыборка.ВидЦены );
		НаборЗаписей.Записать();
	КонецЦикла;
				
	Возврат Истина;
КонецФункции

Функция ЯндексМаркетПроверитьДанныеАвторизацииПомещеныВХранилище(УчетнаяЗапись, ДанныеАвторизации = Неопределено) Экспорт
	Попытка
		ДанныеАвторизации = ЯндексМаркетДанныеАвторизацииПолучить(УчетнаяЗапись);
	Исключение
		Возврат Ложь;
	КонецПопытки;
	Возврат ЗначениеЗаполнено( ДанныеАвторизации.access_token ) Или ЗначениеЗаполнено( ДанныеАвторизации.api_key );
КонецФункции

Процедура ОбновлениеИнформационнойБазыУстановитьРеквизитУникальнаяЦенаДляКаждогоМагазина( Параметры ) Экспорт
	
	Параметры.ОбработкаЗавершена = Ложь;
	
	// Справочник.УчетныеЗаписиМаркетплейсов
	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	УчетныеЗаписиМаркетплейсов.Ссылка КАК Ссылка
	|ИЗ
	|	Справочник.УчетныеЗаписиМаркетплейсов КАК УчетныеЗаписиМаркетплейсов
	|ГДЕ
	|	УчетныеЗаписиМаркетплейсов.ПометкаУдаления = ЛОЖЬ
	|	И УчетныеЗаписиМаркетплейсов.ВидМаркетплейса = &ВидМаркетплейсаЯндексМаркет
	|	И УчетныеЗаписиМаркетплейсов.УникальнаяЦенаДляКаждогоМагазина = ЛОЖЬ";
	
	Запрос.УстановитьПараметр( "ВидМаркетплейсаЯндексМаркет", Перечисления.ВидыМаркетплейсов.МаркетплейсЯндексМаркет );
	
	Результат = Запрос.Выполнить();
	
	Если Результат.Пустой() Тогда

		ТекстСообщения = НСтр("ru='Процедура ОбновлениеИнформационнойБазыУстановитьРеквизитУникальнаяЦенаДляКаждогоМагазина завершила обработку.'");
		ЗаписьЖурналаРегистрации(ОбновлениеИнформационнойБазы.СобытиеЖурналаРегистрации(), УровеньЖурналаРегистрации.Информация,
			Метаданные.Справочники.НастройкиИнтеграцииСИнтернетМагазином,, ТекстСообщения);
			
		Параметры.ОбработкаЗавершена = Истина;
		
		Возврат;
	КонецЕсли;
	
	
	ПроблемныхОбъектов = 0;
	ОбъектовОбработано = 0;
	
	Выборка = Результат.Выбрать();
	Пока Выборка.Следующий() Цикл
	
		НачатьТранзакцию();
		
		Попытка
		
			ПредставлениеОбъекта = Строка(Выборка.Ссылка);
			Объект = Выборка.Ссылка.ПолучитьОбъект();
			
			Объект.УникальнаяЦенаДляКаждогоМагазина = Истина;
			
			ОбновлениеИнформационнойБазы.ЗаписатьДанные( Объект );
			
			ОбъектовОбработано = ОбъектовОбработано + 1;
			
			ЗафиксироватьТранзакцию();
			
		Исключение
		
			ОтменитьТранзакцию();
			
			ПроблемныхОбъектов = ПроблемныхОбъектов + 1;
			
			ТекстОшибки = ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
			ТекстСообщения = НСтр("ru='Не удалось обработать объект ""%1"" по причине:
			|%2'");
			ТекстСообщения = СтрШаблон(ТекстСообщения, ПредставлениеОбъекта, ТекстОшибки);
			ЗаписьЖурналаРегистрации(ОбновлениеИнформационнойБазы.СобытиеЖурналаРегистрации(), УровеньЖурналаРегистрации.Ошибка,
				Метаданные.Справочники.НастройкиИнтеграцииСИнтернетМагазином, Выборка.Ссылка, ТекстСообщения);
				
		КонецПопытки;
	КонецЦикла;

	Если ОбъектовОбработано > 0 И ПроблемныхОбъектов = 0 Тогда

		ТекстСообщения = НСтр("ru='Процедура ОбновлениеИнформационнойБазыУстановитьРеквизитУникальнаяЦенаДляКаждогоМагазина завершила обработку.'");
		ЗаписьЖурналаРегистрации(ОбновлениеИнформационнойБазы.СобытиеЖурналаРегистрации(), УровеньЖурналаРегистрации.Информация,
			Метаданные.Справочники.НастройкиИнтеграцииСИнтернетМагазином,, ТекстСообщения);
			
		Параметры.ОбработкаЗавершена = Истина;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ИмпортИзЯндексМаркетПараметрыНовый() Экспорт

	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить( "ВариантЗагрузкиДанных", Неопределено );
	ПараметрыФункции.Вставить( "КэшКатегорий", Неопределено );
	ПараметрыФункции.Вставить( "ЗагрузитьЦеныПродажи", Ложь );
	
	ПараметрыФункции.Вставить( "ИмяИдентификатора", "ИдентификаторПубликации" );
	ПараметрыФункции.Вставить( "РазмерПорцииЗагрузкиДанных", 30 );
	
	ПараметрыФункции.Вставить( "ЗагрузитьОписанияАрхивныхПозиций", Ложь );
	
	ПараметрыФункции.Вставить( "СоздатьЭлементыНоменклатуры", Ложь );
	ПараметрыФункции.Вставить( "ПерезаполнитьЗначенияРеквизитовНоменклатуры", Ложь );
	ПараметрыФункции.Вставить( "ГруппаДляСозданияНовыхТоваров", Неопределено );
	ПараметрыФункции.Вставить( "СоздатьИерархиюНоменклатуры", Ложь );
	ПараметрыФункции.Вставить( "СоздатьХарактеристикиНоменклатуры", Ложь );
	ПараметрыФункции.Вставить( "ЗагрузитьИзображения", Ложь );
	
	ПараметрыФункции.Вставить( "СоздатьКатегорииНоменклатуры", Ложь );
	ПараметрыФункции.Вставить( "СоздатьИерархиюКатегорийНоменклатуры", Ложь );
	
	ПараметрыФункции.Вставить( "СортироватьСтрокиДереваТоваров", Ложь );
	ПараметрыФункции.Вставить( "ОтображатьСопоставленные", Ложь );
	ПараметрыФункции.Вставить( "ОтображатьПоСтатусу", Ложь );
	ПараметрыФункции.Вставить( "СледующаяПорцияДанных", Ложь );
	ПараметрыФункции.Вставить( "ИдентификаторыТоваров", Неопределено );
	
	ПараметрыФункции.Вставить( "ТоварыНайти", Ложь );

	ПараметрыФункции.Вставить( "НоменклатураКоличествоСоздано", 0 );
	ПараметрыФункции.Вставить( "НоменклатураКоличествоОбновлено", 0 );
	ПараметрыФункции.Вставить( "ХарактеристикиКоличествоСоздано", 0 );
	ПараметрыФункции.Вставить( "ХарактеристикиКоличествоОбновлено", 0 );
	ПараметрыФункции.Вставить( "ФайлыКоличествоСоздано", 0 );
	ПараметрыФункции.Вставить( "ШтрихкодыКоличествоСоздано", 0 );
	ПараметрыФункции.Вставить( "КатегорииНоменклатурыСоздано", 0 );

	ПараметрыФункции.Вставить( "УчетнаяЗаписьМаркетплейса", Неопределено );
	
	ПараметрыФункции.Вставить( "ЗаписатьТолькоЦены", Ложь );
	ПараметрыФункции.Вставить( "ТолькоСоздатьНоменклатуру", Ложь );
	
	ПараметрыФункции.Вставить( "ИсточникКатегории", Перечисления.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры );
	ПараметрыФункции.Вставить( "КэшКатегорийДерево", Неопределено );
	
	ПараметрыФункции.Вставить( "ДокументУстановкаЦенНоменклатуры", Неопределено );

	ПараметрыФункции.Вставить( "ИмпортируемыеТовары", Неопределено );
	
	Возврат ПараметрыФункции;
КонецФункции

Функция ИмпортИзЯндексМаркетВыбратьНеЗаписанныеВ1СИдентификаторы( УчетнаяЗаписьМаркетплейса, Параметры )

	// выборка только новых идентификаторов с сайта, которые ещё не записаны в 1С
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ТаблицаИдентификаторовТоваров.УчетнаяЗаписьМаркетплейса КАК УчетнаяЗаписьМаркетплейса,
	|	ТаблицаИдентификаторовТоваров.ИдентификаторПубликации КАК ИдентификаторПубликации,
	|	ТаблицаИдентификаторовТоваров.ИдентификаторОбъектаМаркетплейса КАК ИдентификаторОбъектаМаркетплейса
	|ПОМЕСТИТЬ ТаблицаИдентификаторовТоваров
	|ИЗ
	|	&ТаблицаИдентификаторовТоваров КАК ТаблицаИдентификаторовТоваров
	|
	|ИНДЕКСИРОВАТЬ ПО 
	|	ИдентификаторПубликации
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ 
	|	ТаблицаИдентификаторовТоваров.УчетнаяЗаписьМаркетплейса КАК УчетнаяЗаписьМаркетплейса,
	|	ТаблицаИдентификаторовТоваров.ИдентификаторПубликации КАК ИдентификаторПубликации,
	|	ТаблицаИдентификаторовТоваров.ИдентификаторОбъектаМаркетплейса КАК ИдентификаторОбъектаМаркетплейса
	|ИЗ
	|	ТаблицаИдентификаторовТоваров КАК ТаблицаИдентификаторовТоваров
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет КАК СтатусыПубликацииТоваровЯндексМаркет
	|		ПО (СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса = &УчетнаяЗаписьМаркетплейса)
	|			И ТаблицаИдентификаторовТоваров.ИдентификаторПубликации = СтатусыПубликацииТоваровЯндексМаркет.ИдентификаторПубликации
	|ГДЕ
	|	СтатусыПубликацииТоваровЯндексМаркет.УчетнаяЗаписьМаркетплейса ЕСТЬ NULL
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|УНИЧТОЖИТЬ ТаблицаИдентификаторовТоваров";
	
	Запрос.УстановитьПараметр("УчетнаяЗаписьМаркетплейса", УчетнаяЗаписьМаркетплейса);
	Запрос.УстановитьПараметр("ТаблицаИдентификаторовТоваров", Параметры.ИдентификаторыТоваров );
	
	
	УстановитьПривилегированныйРежим( Истина );
	
	ВыборкаДанных = Запрос.Выполнить().Выбрать();
	
	УстановитьПривилегированныйРежим( Ложь );
	
	Возврат ВыборкаДанных;
КонецФункции

// Возвращает простые типы.
// 
// Возвращаемое значение:
//  Массив - массив простых типов.
//
Функция ПростыеТипы()

	Результат = Новый Массив;
	Результат.Добавить(Тип("Строка"));
	Результат.Добавить(Тип("Число"));
	Результат.Добавить(Тип("Дата"));
	Результат.Добавить(Тип("Булево"));

	Возврат Результат;

КонецФункции

// Определяет текущую сопоставленную категорию для указанной номенклатуры.
//
// Параметры:                                                                   
//   УчетнаяЗаписьМаркетплейса - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису.
//   Номенклатура              - СправочникСсылка.Номенклатура - номенклатура, для которой требуется найти категорию.
//   ИдентификаторКатегории    - Строка - искомый идентификатор категории маркетплейса;
//   ИсточникКатегории         - ПеречислениеСсылка.ИсточникиКатегорийДляМаркетплейса - источник категории из настроек сопоставления.
//
// Возвращаемое значение:
//   Структура - результат поиска категории:
//     * ИсточникКатегории      - СправочникСсылка.ВидыНоменклатуры, СправочникСсылка.Номенклатура, СправочникСсылка.ТоварныеКатегории -
//                                  источник категории 1С;
//                              - Неопределено - источник категории не найден;
//     * Категория              - СправочникСсылка.КатегорииНоменклатуры, СправочникСсылка.Номенклатура -
//                                  подобранная категория 1С;
//                              - Неопределено - категория не найдена;
//     * ИдентификаторКатегории - Строка - идентификатор сопоставленной категории маркетплейса;
//     * НаименованиеКатегории  - Строка - наименование сопоставленной категории маркетплейса.
//
Функция ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоНоменклатуре(УчетнаяЗаписьМаркетплейса, Номенклатура, ИдентификаторКатегории, ИсточникКатегории) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить( "ИсточникКатегории", Неопределено );
	Результат.Вставить( "Категория", Неопределено );
	Результат.Вставить( "ИдентификаторКатегории", "" );
	Результат.Вставить( "НаименованиеКатегории", "" );
	
	Если НЕ ЗначениеЗаполнено( Номенклатура ) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Объект1С = Неопределено;
	
	Если ИсточникКатегории = Неопределено Тогда
		ИсточникКатегории = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗаписьМаркетплейса, "ИсточникКатегории");
	КонецЕсли;
	
	Если ИсточникКатегории = ПредопределенноеЗначение("Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры") Тогда
		Объект1С = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Номенклатура, "КатегорияНоменклатуры");
	ИначеЕсли ИсточникКатегории = ПредопределенноеЗначение("Перечисление.ИсточникиКатегорийДляМаркетплейса.ИерархияНоменклатуры") Тогда
		Объект1С = Номенклатура;
	КонецЕсли;
	
	Если ЗначениеЗаполнено( Объект1С ) Тогда
		
		Результат.Вставить( "ИсточникКатегории", Объект1С );
		Результат.Вставить( "Категория", Объект1С );

		Если ТипЗнч( Объект1С ) = Тип( "СправочникСсылка.Номенклатура" ) Тогда
			НоменклатураЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта( Объект1С, "ЭтоГруппа, Родитель" );
			Если НоменклатураЗначенияРеквизитов.ЭтоГруппа = Ложь Тогда
				Результат.Вставить( "ИсточникКатегории", НоменклатураЗначенияРеквизитов.Родитель );
				Результат.Вставить( "Категория", НоменклатураЗначенияРеквизитов.Родитель );
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Процедура ДеревоТоваровСтрокаПоНоменклатуреДозаполнить( ОписанияТоваровСтрока, Параметры )
	
	ТекущиеДанные = ОписанияТоваровСтрока;

	НоменклатураЗначенияРеквизитов = ОбщегоНазначения.ЗначенияРеквизитовОбъекта( ТекущиеДанные.Номенклатура, "Родитель, КатегорияНоменклатуры, ЕдиницаИзмерения, ИспользоватьХарактеристики" );
	
	Если Параметры.ИсточникКатегории = ПредопределенноеЗначение( "Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры" ) Тогда
		НоменклатураДляПоискаСопоставленияКатегории = ТекущиеДанные.Номенклатура;
	Иначе
		НоменклатураДляПоискаСопоставленияКатегории = НоменклатураЗначенияРеквизитов.Родитель;
	КонецЕсли;
	
	ТекущаяКатегория  = ОпределитьТекущуюСопоставленнуюКатегориюМаркетплейсаПоНоменклатуре(
	Параметры.УчетнаяЗаписьМаркетплейса,
	НоменклатураДляПоискаСопоставленияКатегории,
	ТекущиеДанные.ИдентификаторКатегорииМаркетплейса,
	Параметры.ИсточникКатегории );

	ТекущиеДанные.Категория = НоменклатураЗначенияРеквизитов.КатегорияНоменклатуры;
	
	ТекущиеДанные.ИсточникКатегорииМаркетплейса = ТекущаяКатегория.ИсточникКатегории;
	ТекущиеДанные.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = ТекущаяКатегория.ИсточникКатегории;
	
	Если Не ЗначениеЗаполнено( ТекущиеДанные.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет ) Тогда
		Если Параметры.ИсточникКатегории = ПредопределенноеЗначение( "Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры" ) Тогда
			ТекущиеДанные.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = ТекущиеДанные.Категория;
		Иначе
			ТекущиеДанные.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = НоменклатураЗначенияРеквизитов.Родитель;
		КонецЕсли;
	КонецЕсли;
	
	ТекущиеДанные.ТекущееИдентификаторКатегорииМаркетплейса = ТекущаяКатегория.ИдентификаторКатегории;
	ТекущиеДанные.ТекущееНаименованиеКатегорииМаркетплейса = ТекущаяКатегория.НаименованиеКатегории;
		
	ТекущиеДанные.ЕдиницаИзмерения = НоменклатураЗначенияРеквизитов.ЕдиницаИзмерения;
	ТекущиеДанные.ХарактеристикиИспользуются = НоменклатураЗначенияРеквизитов.ИспользоватьХарактеристики;
	
КонецПроцедуры

Функция ТоварыНайти( Параметры ) Экспорт

	Результат = Новый Структура;
	Результат.Вставить( "ПоискВыполнен", Ложь );
	Результат.Вставить( "ИмпортируемыеТовары", Неопределено );

	// заполним описания товаров из элементов формы в параметры поиска
	ФормаИмпортируемыеТовары = Параметры.ИмпортируемыеТовары;
	
	ФормаОписанияТоваровДляПоиска = ФормаИмпортируемыеТовары.Строки.НайтиСтроки( Новый Структура( "Пометка", Истина ), Истина );
	Если ФормаОписанияТоваровДляПоиска.Количество() = 0 Тогда
		Возврат Результат;
	КонецЕсли;
	
	ПараметрыКонтекст = ОбменСВнешнимиСистемамиНоменклатура.ТоварыНайтиПараметрыКонтекстСоздать();

	ПараметрыКонтекст.ИспользоватьПоискПоБазеДанных1С = Истина;
	ПараметрыКонтекст.ИспользоватьПолнотекстовыйПоиск = Истина;

	ПараметрыКонтекст.ПоискПоНоменклатуре = Истина;
	ПараметрыКонтекст.ПоискПоХарактеристикамНоменклатуры = Истина;
	ПараметрыКонтекст.ПоискПоШтрихкодам = Истина;
	
	ПараметрыКонтекст.ПоискПоНаименованиям = Истина;

	ПараметрыКонтекст.КоличествоОписанийТоваровВОднойПорции = 10;
	ПараметрыКонтекст.ОтладкаВключена = Ложь;
	
	ПараметрыКонтекст.Вставить( "УчетнаяЗапись", Параметры.УчетнаяЗаписьМаркетплейса );
	
	Для Каждого ФормаОписанияТоваровСтрока Из ФормаОписанияТоваровДляПоиска Цикл
	
		Если Не ЗначениеЗаполнено( ФормаОписанияТоваровСтрока.ИдентификаторОбъектаМаркетплейса ) Тогда
			Продолжить;
		КонецЕсли;
		
		ОписаниеТовараЭлемент = ОбменСВнешнимиСистемамиНоменклатура.ТоварыНайтиОписанияТоваровЭлементСоздать( ПараметрыКонтекст.ОписанияТоваров );
		
		ОписаниеТовараЭлемент.ИдентификаторЭлемента = ФормаОписанияТоваровСтрока.ИдентификаторОбъектаМаркетплейса;
		ОписаниеТовараЭлемент.Наименование = ФормаОписанияТоваровСтрока.ПредставлениеОбъектаМаркетплейса;
		
		ОписаниеТовараЭлемент.Категория = ФормаОписанияТоваровСтрока.Категория;
		
		ДеревоРеквизитовАртикулСтроки = ФормаОписанияТоваровСтрока.ДеревоРеквизитов.Строки.НайтиСтроки( Новый Структура( "НаименованиеАтрибутаМаркетплейса", "Артикул" ), Истина );
		Если ДеревоРеквизитовАртикулСтроки.Количество() > 0 Тогда
			ОписаниеТовараЭлемент.Артикул = ДеревоРеквизитовАртикулСтроки[ 0 ].ЗначениеАтрибутаМаркетплейса;
		КонецЕсли;

		ФормаШтрихкодыСтроки = ФормаОписанияТоваровСтрока.ДеревоРеквизитов.Строки.НайтиСтроки( Новый Структура( "НаименованиеАтрибутаМаркетплейса", "Штрихкоды" ), Истина );
		Если ФормаШтрихкодыСтроки.Количество() > 0 Тогда

			ФормаШтрихкоды = ФормаШтрихкодыСтроки[ 0 ].ЗначенияАтрибутаМаркетплейса;
			
			Для Каждого ФормаШтрихкод Из ФормаШтрихкоды Цикл
				Штрихкод = ОписаниеТовараЭлемент.Штрихкоды.Добавить();
				Штрихкод.Имя = "-";
				Штрихкод.Значение = ФормаШтрихкод.Значение;
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Время001 = ТекущаяУниверсальнаяДатаВМиллисекундах();
	
	ПоискВыполнен = ОбменСВнешнимиСистемамиНоменклатура.ТоварыНайти( ПараметрыКонтекст );
	
	Время002 = ТекущаяУниверсальнаяДатаВМиллисекундах();
	ДлительностьМс = Время002 - Время001;
	
	Если ПоискВыполнен = Истина Тогда
		// заполним результаты поиска в элементы формы
		
		ОтборПоИдентификатору = Новый Структура( "ИдентификаторОбъектаМаркетплейса" );
		Для Каждого ОписаниеТовараЭлемент Из ПараметрыКонтекст.ОписанияТоваров Цикл
			
			ОтборПоИдентификатору.ИдентификаторОбъектаМаркетплейса = ОписаниеТовараЭлемент.ИдентификаторЭлемента;
			ФормаОписанияТоваровСтроки = ФормаИмпортируемыеТовары.Строки.НайтиСтроки( ОтборПоИдентификатору, Истина );
			
			Если ФормаОписанияТоваровСтроки.Количество() = 0 Тогда
				ОбщегоНазначения.СообщитьПользователю( "Не найден элемент по идентификатору " + ОтборПоИдентификатору.ИдентификаторОбъектаМаркетплейса );
				Продолжить;
			КонецЕсли;
			
			ФормаОписанияТоваровСтрока = ФормаОписанияТоваровСтроки[ 0 ];
			
			ФормаОписанияТоваровСтрока.ПоискВыполнен = Истина;

			ФормаРезультатыПоиска = ФормаОписанияТоваровСтрока.РезультатыПоиска;
			ФормаРезультатыПоиска.Очистить();
			
			РезультатыПоиска = ОписаниеТовараЭлемент.РезультатыПоиска;
			
			РезультатПоискаКоличество = 0;
			Для Каждого РезультатПоиска Из РезультатыПоиска Цикл
				РезультатПоискаКоличество = РезультатПоискаКоличество + 1;
				
				ФормаРезультатПоиска = ФормаРезультатыПоиска.Добавить();
				
				ФормаРезультатПоиска.Номенклатура = РезультатПоиска.Номенклатура;
				ФормаРезультатПоиска.Характеристика = РезультатПоиска.Характеристика;
				
				ФормаРезультатПоиска.МетодПоиска = РезультатПоиска.МетодПоиска;
				ФормаРезультатПоиска.Описание = РезультатПоиска.Описание;
				ФормаРезультатПоиска.ВероятностьСовпадения = РезультатПоиска.ВероятностьСовпадения;
				ФормаРезультатПоиска.ВесПоиска = РезультатПоиска.ВесПоиска;
			КонецЦикла;
			
			ФормаОписанияТоваровСтрока.РезультатПоискаКоличество = РезультатПоискаКоличество;
			
			ФормаОписанияТоваровСтрока.РезультатПоискаНоменклатура = ОписаниеТовараЭлемент.РезультатПоискаНоменклатура;
			ФормаОписанияТоваровСтрока.РезультатПоискаХарактеристика = ОписаниеТовараЭлемент.РезультатПоискаХарактеристика;
			ФормаОписанияТоваровСтрока.РезультатПоискаВероятностьСовпадения = ОписаниеТовараЭлемент.РезультатПоискаВероятностьСовпадения;
			ФормаОписанияТоваровСтрока.РезультатПоискаВесПоиска = ОписаниеТовараЭлемент.РезультатПоискаВесПоиска;
			ФормаОписанияТоваровСтрока.РезультатПоискаОписание = ОписаниеТовараЭлемент.РезультатПоискаОписание;
			
			Если ЗначениеЗаполнено( ФормаОписанияТоваровСтрока.РезультатПоискаНоменклатура )
			Или ЗначениеЗаполнено( ФормаОписанияТоваровСтрока.РезультатПоискаХарактеристика ) Тогда
				ФормаОписанияТоваровСтрока.РезультатПоискаКнопкаВыбрать = 1;
			Иначе
				ФормаОписанияТоваровСтрока.РезультатПоискаКнопкаВыбрать = -1;
			КонецЕсли;

            // Заполним Номенклатуру и Характеристику результатами поиска
			Если Не ЗначениеЗаполнено( ФормаОписанияТоваровСтрока.Номенклатура ) Тогда
			
				Если ЗначениеЗаполнено( ОписаниеТовараЭлемент.РезультатПоискаНоменклатура )
					ИЛИ ЗначениеЗаполнено( ОписаниеТовараЭлемент.РезультатПоискаХарактеристика ) Тогда
					
					ФормаОписанияТоваровСтрока.Номенклатура = ОписаниеТовараЭлемент.РезультатПоискаНоменклатура;
					ФормаОписанияТоваровСтрока.Характеристика = ОписаниеТовараЭлемент.РезультатПоискаХарактеристика;
					
					ДеревоТоваровСтрокаПоНоменклатуреДозаполнить( ФормаОписанияТоваровСтрока, Параметры );
					
					ФормаОписанияТоваровСтрока.Пометка = Истина;
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Результат.ПоискВыполнен = ПоискВыполнен;
	Результат.ИмпортируемыеТовары = ФормаИмпортируемыеТовары;
	
	Возврат Результат;
КонецФункции

Функция НовоеДеревоРеквизитов()

	ОписаниеТипаСтрока50	= Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(50));
	ОписаниеТипаСтрока250	= Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(250));
	ОписаниеТипаСтрока500	= Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(500));
	ОписаниеТипаБулево		= Новый ОписаниеТипов("Булево");
	ОписаниеТипаЧисло2		= Новый ОписаниеТипов("Число", ,, Новый КвалификаторыЧисла(2));
	ОписаниеТипаЧисло5		= Новый ОписаниеТипов("Число", ,, Новый КвалификаторыЧисла(5));

	ДеревоРеквизитов = Новый ДеревоЗначений;
	ДеревоРеквизитов.Колонки.Добавить("ИдентификаторКатегорииМаркетплейса", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("ИдентификаторАтрибутаМаркетплейса", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("ИдентификаторАтрибута1С", ОписаниеТипаСтрока250);
	ДеревоРеквизитов.Колонки.Добавить("НаименованиеАтрибутаМаркетплейса", ОписаниеТипаСтрока250);
	ДеревоРеквизитов.Колонки.Добавить("ПредставлениеРеквизита1С", ОписаниеТипаСтрока500);
	ДеревоРеквизитов.Колонки.Добавить("ОбязательныйАтрибут", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("АспектныйАтрибут", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ЯвляетсяКоллекциейЗначений", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ДоступенВыборЗначенияИзСписка", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("МаксимальноеКоличествоЗначений",            ОписаниеТипаЧисло5);
	ДеревоРеквизитов.Колонки.Добавить("ТипЗначенияАтрибута", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("ТипЗначенияАтрибута1С", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("ОписаниеНазначенияАтрибута", Новый ОписаниеТипов("Строка"));
	ДеревоРеквизитов.Колонки.Добавить("ЗначениеРеквизита1С"); // Любая ссылка и простые типы.
	ДеревоРеквизитов.Колонки.Добавить("ЗначениеАтрибутаМаркетплейса", Новый ОписаниеТипов(ПростыеТипы()));
	ДеревоРеквизитов.Колонки.Добавить("ЗначениеАтрибутаМаркетплейсаБулево", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ИдентификаторЗначенияАтрибутаМаркетплейса", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("ЗначенияАтрибутаМаркетплейса", Новый ОписаниеТипов("СписокЗначений"));
	ДеревоРеквизитов.Колонки.Добавить("ЗначениеАтрибутаЗаполнено", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ЗначениеАтрибутаЗаданоПользователем", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("НеЗаполненРеквизит1С", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("НеЗаполненАтрибут", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ПредставлениеТипаСоответствияЗначений", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("НомерКартинкиКоллекцииТипов", ОписаниеТипаЧисло2);
	ДеревоРеквизитов.Колонки.Добавить("Уровень", ОписаниеТипаЧисло2);
	ДеревоРеквизитов.Колонки.Добавить("ПорядокСортировки", ОписаниеТипаЧисло2);
	ДеревоРеквизитов.Колонки.Добавить("АдресХранилищаДоступныхЗначений", ОписаниеТипаСтрока500);
	ДеревоРеквизитов.Колонки.Добавить("ИзменилсяРеквизит1С", ОписаниеТипаБулево); // Любая ссылка и простые типы.
	ДеревоРеквизитов.Колонки.Добавить("КэшЗначениеРеквизита1С"); // Любая ссылка и простые типы.

	ДеревоРеквизитов.Колонки.Добавить("ОсновныеПоказатели", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ЦеновыеПоказатели", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ЕстьРазличия", ОписаниеТипаБулево);
	
	ДеревоРеквизитов.Колонки.Добавить("ТекущееОбязательныйАтрибут", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееАспектныйАтрибут", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееЯвляетсяКоллекциейЗначений", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееДоступенВыборЗначенияИзСписка", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееМаксимальноеКоличествоЗначений", ОписаниеТипаЧисло5);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееТипЗначенияАтрибута", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееТипЗначенияАтрибута1С", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееЗначениеАтрибутаМаркетплейса", Новый ОписаниеТипов(ПростыеТипы()));
	ДеревоРеквизитов.Колонки.Добавить("ТекущееИдентификаторЗначенияАтрибутаМаркетплейса", ОписаниеТипаСтрока50);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееЗначениеАтрибутаМаркетплейсаБулево", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееЗначенияАтрибутаМаркетплейса", Новый ОписаниеТипов("СписокЗначений"));
	ДеревоРеквизитов.Колонки.Добавить("ТекущееЗначениеАтрибутаЗаполнено", ОписаниеТипаБулево);
	ДеревоРеквизитов.Колонки.Добавить("ТекущееНомерКартинкиКоллекцииТипов", ОписаниеТипаЧисло2);

	Возврат ДеревоРеквизитов;

КонецФункции

Процедура ИмпортИзЯндексМаркетДеревоТоваровДеревоРеквизитовЗаполнитьДляСтроки( СтрокаДереваВторогоУровня, Параметры )

	Отбор = Новый Структура( "ИдентификаторПубликации", СтрокаДереваВторогоУровня.ИдентификаторПубликации );

	СтрокаДереваВторогоУровня.ДеревоРеквизитов = НовоеДеревоРеквизитов();
	
	ДеревоРеквизитовГруппа = СтрокаДереваВторогоУровня.ДеревоРеквизитов.Строки.Вставить(0);
	ДеревоРеквизитовГруппа.Уровень = 0;
	ДеревоРеквизитовГруппа.ОсновныеПоказатели = Истина;
	ДеревоРеквизитовГруппа.НаименованиеАтрибутаМаркетплейса = НСтр("ru = 'Основные данные товара'");
	ДеревоРеквизитовГруппа.НомерКартинкиКоллекцииТипов = 99;
	ДеревоРеквизитовГруппа.ТекущееНомерКартинкиКоллекцииТипов = 99;
	
	ЗначенияАтрибутовТовараИзСервиса = Параметры.ИдентификаторыТоваров.НайтиСтроки(Отбор);
	Если ЗначенияАтрибутовТовараИзСервиса.Количество() > 0 Тогда
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "Артикул";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Артикул";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = СтрокаДереваВторогоУровня.ИдентификаторПубликации;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "ИдентификаторСопоставленнойКарточкиНаМаркете";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Сопоставленая карточка каталога Яндекс Маркета: Идентификатор";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = СтрокаДереваВторогоУровня.ИдентификаторТовараПлощадки;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "НаименованиеСопоставленнойКарточкиНаМаркете";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Сопоставленая карточка каталога Яндекс Маркета: Наименование";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = СтрокаДереваВторогоУровня.НаименованиеМоделиПлощадки;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "Описание";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Описание";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = СтрокаДереваВторогоУровня.ОписаниеОбъектаМаркетплейса;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "ВесБруттоКг";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Вес брутто, кг";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = ЗначенияАтрибутовТовараИзСервиса[ 0 ].ВесБруттоКг;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "ВысотаСм";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Высота, см";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = ЗначенияАтрибутовТовараИзСервиса[ 0 ].ВысотаСм;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "ДлинаСм";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Длина, см";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = ЗначенияАтрибутовТовараИзСервиса[ 0 ].ДлинаСм;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "ШиринаСм";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Ширина, см";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = ЗначенияАтрибутовТовараИзСервиса[ 0 ].ШиринаСм;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "Производитель";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Производитель";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = ЗначенияАтрибутовТовараИзСервиса[ 0 ].Производитель;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "АртикулТовараПроизводителя";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Артикул товара производителя";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = ЗначенияАтрибутовТовараИзСервиса[ 0 ].АртикулТовараПроизводителя;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "СтранаПроизводства";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Страна производства";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = ЗначенияАтрибутовТовараИзСервиса[ 0 ].СтранаПроизводства;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "ТНВЭДКод";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Код ТНВЭД";
		ДеревоРеквизитовСтрока.ЗначениеАтрибутаМаркетплейса = ЗначенияАтрибутовТовараИзСервиса[ 0 ].ТНВЭДКод;
		
		ДеревоРеквизитовСтрока = ДеревоРеквизитовГруппа.Строки.Добавить();
		ДеревоРеквизитовСтрока.Уровень = 3;
		ДеревоРеквизитовСтрока.ИдентификаторАтрибута1С = "Штрихкоды";
		ДеревоРеквизитовСтрока.НаименованиеАтрибутаМаркетплейса = "Штрихкоды";
		ДеревоРеквизитовСтрока.ЗначенияАтрибутаМаркетплейса.ЗагрузитьЗначения( ЗначенияАтрибутовТовараИзСервиса[ 0 ].Штрихкоды );
		ДеревоРеквизитовСтрока.ЯвляетсяКоллекциейЗначений = Истина;
		
	КонецЕсли;
	
КонецПроцедуры

// Формирует список товаров с описаниями для последующего импорта.
//
// Параметры:
//   УчетнаяЗаписьМаркетплейса - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису.
//   Параметры                 - Структура - параметры выполнения функции:
//   ДеревоТоваров             - ДеревоЗначений - структура дерева описана в реквизите "ИмпортируемыеТовары", 
//                             - Неопределено - заполнение дерева товаров не производится.
//
Функция ИмпортИзЯндексМаркетПолучитьИмпортируемыеДанныеСТорговойПлощадки(Знач УчетнаяЗаписьМаркетплейса,
Знач Параметры, ДеревоТоваров = Неопределено) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить( "Ошибка", ИнтеграцияСМаркетплейсамиСервер.НоваяОшибка());
	Результат.Вставить( "ДеревоТоваров", ДеревоТоваров);
    Если Параметры.Свойство("КэшКатегорий") 
         И ЗначениеЗаполнено(Параметры.КэшКатегорий) Тогда
        Результат.Вставить("КэшКатегорий", Параметры.КэшКатегорий);
    КонецЕсли;
    
    Если Параметры.Свойство("ИдентификаторыТоваров") Тогда
        Результат.Вставить("ИдентификаторыТоваров", Параметры.ИдентификаторыТоваров);
    КонецЕсли;
    
	Если ДеревоТоваров = Неопределено Тогда
    	Результат.Вставить("ОписаниеТоваров",              НоваяТаблицаОписанияТоваров());
    	Результат.Вставить("ОписаниеХарактеристикТоваров", НоваяТаблицаОписанияХарактеристикТоваров());
    	Результат.Вставить("ОстаткиТоваров",               НоваяТаблицаОстатковТоваров());
    	Результат.Вставить("ЦеныТоваров",                  Неопределено);
    КонецЕсли;

    Если Параметры.Свойство( "ТоварыНайти" ) Тогда
        ТоварыНайти = (Параметры.ТоварыНайти = Истина);
    Иначе
    	ТоварыНайти = Ложь;
    КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(УчетнаяЗаписьМаркетплейса) Тогда
		Результат.Ошибка.КодОшибки      = КодыОшибокРегистраСтатусовПубликацииТоваров().ОшибкаПрочие;
		Результат.Ошибка.ОписаниеОшибки = НСтр("ru = 'Не указана учетная запись торговой площадки для импорта данных по товарам.'");
		Возврат Результат;
	КонецЕсли;
	
	Если Параметры.КэшКатегорий = Неопределено Тогда
		Параметры.КэшКатегорий = Новый Структура;
	КонецЕсли;
	
	Отказ = Ложь;
		
	ТипНалогообложенияНДС  = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС;
	НастройкиУчетнойЗаписи = Справочники.УчетныеЗаписиМаркетплейсов.НастройкиУчетнойЗаписи(УчетнаяЗаписьМаркетплейса);
	ВидыЦен = ВидыЦенУчетнойЗаписи(УчетнаяЗаписьМаркетплейса, Истина, Истина);
	ИсточникКатегории = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗаписьМаркетплейса, "ИсточникКатегории");
	КэшИдентификаторов = Новый Соответствие;
	КешНайденныхКатегорий = Новый Соответствие;
	
	ИмяИдентификатора           = Параметры.ИмяИдентификатора;
	Идентификаторы              = Новый Массив;
	ИдентификаторыПовторные     = Новый Массив;
	ИдентификаторыНовые         = Новый Массив;
	СтрокаДереваГруппаЗагрузить = Неопределено;
	СтрокаДереваГруппаОшибки    = Неопределено;
	
	Попытка
		
		Если ДеревоТоваров <> Неопределено Тогда
			Для Каждого СтрокаДереваПервогоУровня Из ДеревоТоваров.Строки Цикл
				Если СтрокаДереваПервогоУровня.ВариантЗагрузки = 0 Тогда
					// СтрокаДереваГруппаОбновить
				ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 2 Тогда
					СтрокаДереваГруппаЗагрузить = СтрокаДереваПервогоУровня;
				ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 3 Тогда
					СтрокаДереваГруппаОшибки = СтрокаДереваПервогоУровня;
				Иначе
					Продолжить;
				КонецЕсли;	
					
				Для Каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
					Если ЗначениеЗаполнено(СтрокаДереваВторогоУровня.ИдентификаторПубликации) Тогда
						КэшИдентификаторов.Вставить(СтрокаДереваВторогоУровня.ИдентификаторПубликации, СтрокаДереваВторогоУровня.ИдентификаторОбъектаМаркетплейса);
					КонецЕсли;
					
					Если НЕ СтрокаДереваВторогоУровня.Пометка
						 ИЛИ СтрокаДереваВторогоУровня.ИндексСтатуса = 4 Тогда
						ИдентификаторыПовторные.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);
						Продолжить;
					КонецЕсли;
					
					Если Параметры.СледующаяПорцияДанных
						 И СтрокаДереваВторогоУровня.ИндексСтатуса <> 5 Тогда
						ИдентификаторыПовторные.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);
						Продолжить;
					КонецЕсли;
					
					// Дополнительные фильтры
					Если Параметры.ОтображатьСопоставленные = 1
						 И (НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
						 	ИЛИ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
						        И НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика)
						 	ИЛИ НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория)) Тогда
						ИдентификаторыПовторные.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);
						Продолжить;
						
					ИначеЕсли Параметры.ОтображатьСопоставленные = 2
						 	  И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
						 	  И (НЕ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
						         ИЛИ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика))
						 	  И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория) Тогда
						ИдентификаторыПовторные.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);
						Продолжить;
					КонецЕсли;
					
					Если Параметры.ОтображатьПоСтатусу <> -1
						 И СтрокаДереваВторогоУровня.ИндексСтатуса <> Параметры.ОтображатьПоСтатусу Тогда
						ИдентификаторыПовторные.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);
						Продолжить;
					КонецЕсли;
					
					Если ЗначениеЗаполнено(СтрокаДереваВторогоУровня[ИмяИдентификатора]) Тогда
						Если ЗначениеЗаполнено(Параметры.РазмерПорцииЗагрузкиДанных)
							 И Идентификаторы.Количество() >= Параметры.РазмерПорцииЗагрузкиДанных Тогда
							ИдентификаторыПовторные.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);							
						Иначе
							СтрокаДереваВторогоУровня.ИндексСтатуса = 7;
							Идентификаторы.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);
							
							Если СтрокаДереваВторогоУровня.ВариантЗагрузки = 2 Тогда
								ИдентификаторыНовые.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);
							КонецЕсли;
						КонецЕсли;
					КонецЕсли;
				КонецЦикла;
			КонецЦикла;
		КонецЕсли;
		
		// Новые товары с торговой площадки
		Если Параметры.ВариантЗагрузкиДанных >= 1 
			 И НЕ (ЗначениеЗаполнено(Параметры.РазмерПорцииЗагрузкиДанных)
			 	   И Идентификаторы.Количество() >= Параметры.РазмерПорцииЗагрузкиДанных) Тогда
			
			ЗагрузитьОписанияАрхивныхПозиций = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( Параметры, "ЗагрузитьОписанияАрхивныхПозиций", Истина );
			ФильтрПоТоварам = НовыйФильтрЗапросаПоТоварам( ЗагрузитьОписанияАрхивныхПозиций );
			НомерПП = 0;
			
			Пока Истина Цикл    
				НомерПП = НомерПП + 1;
				КоличествоПозиций = Параметры.ИдентификаторыТоваров.Количество();
				
				Если ЗначениеЗаполнено(Параметры.РазмерПорцииЗагрузкиДанных)
					 И Идентификаторы.Количество() >= Параметры.РазмерПорцииЗагрузкиДанных Тогда
					Прервать;
				КонецЕсли;
				
				
				// получение значений основных атрибутов товаров с сайта Яндекс Маркет
				ПолучитьИдентификаторыТоваровИзЯндексМаркет(
					УчетнаяЗаписьМаркетплейса, 
					ФильтрПоТоварам, 
					Параметры.ИдентификаторыТоваров,
					Параметры.РазмерПорцииЗагрузкиДанных,
					КэшИдентификаторов, 
					Отказ);
                
                
				Если Отказ Тогда
					Результат.Ошибка.КодОшибки = КодыОшибокРегистраСтатусовПубликацииТоваров().ОшибкаПрочие;
					Результат.Ошибка.ОписаниеОшибки = НСтр("ru = 'При получении идентификаторов товаров с торговой площадки возникли ошибки. Подробности см. в журнале регистрации.'");
					Возврат Результат;
				КонецЕсли;
				
				Если КоличествоПозиций = Параметры.ИдентификаторыТоваров.Количество() Тогда
					Прервать;
				КонецЕсли;
				
				ВыборкаДанных = ИмпортИзЯндексМаркетВыбратьНеЗаписанныеВ1СИдентификаторы( УчетнаяЗаписьМаркетплейса, Параметры );
				
				Пока ВыборкаДанных.Следующий() Цикл
					
					ВыборкаДанныхИдентификатор = ВыборкаДанных.ИдентификаторПубликации;
					
					Если Идентификаторы.Найти( ВыборкаДанныхИдентификатор ) = Неопределено 
						 И ИдентификаторыПовторные.Найти( ВыборкаДанныхИдентификатор ) = Неопределено Тогда
						 
						Идентификаторы.Добавить( ВыборкаДанныхИдентификатор );
						ИдентификаторыНовые.Добавить( ВыборкаДанныхИдентификатор );
						
						Если ДеревоТоваров <> Неопределено Тогда
							Если СтрокаДереваГруппаЗагрузить = Неопределено Тогда
								
								СтрокаДереваГруппаЗагрузить = ДеревоТоваров.Строки.Вставить( Мин( 2, ДеревоТоваров.Строки.Количество() ) );
								
								СтрокаДереваГруппаЗагрузить.ВариантЗагрузки = 2;
								СтрокаДереваГруппаЗагрузить.ИндексСтатуса = -2;
							КонецЕсли;
							
							СтрокаДереваВторогоУровня = СтрокаДереваГруппаЗагрузить.Строки.Добавить();
							
							СтрокаДереваВторогоУровня.ВариантЗагрузки = 2;
							СтрокаДереваВторогоУровня.ИндексСтатуса = 6;
							СтрокаДереваВторогоУровня.ИдентификаторПубликации = ВыборкаДанных.ИдентификаторПубликации;
							СтрокаДереваВторогоУровня.ИдентификаторОбъектаМаркетплейса = ВыборкаДанных.ИдентификаторПубликации;
							СтрокаДереваВторогоУровня.ДанныеОбъекта = НоваяТаблицаЗначенийРеквизитов();
							СтрокаДереваВторогоУровня.ДанныеТовараИзСервиса = НоваяТаблицаОписанияТоваров();
							СтрокаДереваВторогоУровня.ЗначенияРеквизитовИзСервиса = НоваяТаблицаЗначенийРеквизитов(Истина);
							СтрокаДереваВторогоУровня.ЦеныТовара = Новый Массив;
							СтрокаДереваВторогоУровня.УчетнаяЗаписьМаркетплейса = УчетнаяЗаписьМаркетплейса;
							
							СтрокаДереваВторогоУровня.РезультатыПоиска = ОбменСВнешнимиСистемамиНоменклатура.ТоварыНайтиРезультатыПоискаНовый();
							
							// дозаполнение по полученным данными из Яндекс Маркет
							ТаблицаИдентификаторыТоваровСтроки = Параметры.ИдентификаторыТоваров.НайтиСтроки( Новый Структура( "ИдентификаторПубликации", ВыборкаДанных.ИдентификаторПубликации ) );
							Если ТаблицаИдентификаторыТоваровСтроки.Количество() > 0 Тогда
								ТаблицаИдентификаторыТоваровСтрока = ТаблицаИдентификаторыТоваровСтроки[ 0 ];
								
								СтрокаДереваВторогоУровня.ПредставлениеОбъектаМаркетплейса = ТаблицаИдентификаторыТоваровСтрока.ПредставлениеОбъектаМаркетплейса;
								СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса = Формат( ТаблицаИдентификаторыТоваровСтрока.ИдентификаторКатегорииМаркетплейса, "ЧГ=0" );
								СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса = ТаблицаИдентификаторыТоваровСтрока.НаименованиеКатегорииМаркетплейса;
								СтрокаДереваВторогоУровня.ИдентификаторТовараПлощадки = ТаблицаИдентификаторыТоваровСтрока.ИдентификаторСопоставленнойКарточкиНаМаркете;
								СтрокаДереваВторогоУровня.НаименованиеМоделиПлощадки = ТаблицаИдентификаторыТоваровСтрока.НаименованиеСопоставленнойКарточкиНаМаркете;
								
								СтрокаДереваВторогоУровня.ОписаниеОбъектаМаркетплейса = ТаблицаИдентификаторыТоваровСтрока.ПодробноеОписание;
								
								СтрокаДереваВторогоУровня.Изображения = ТаблицаИдентификаторыТоваровСтрока.Изображения;
								
								СтатусСОписанием = ПолучитьСтатусМодерацииСОписаниемПоЗначениюИзСервиса( ТаблицаИдентификаторыТоваровСтрока.СтатусКарточкиТовара );
								
								СтрокаДереваВторогоУровня.Статус = СтатусСОписанием.Статус;
								СтрокаДереваВторогоУровня.МаркерСтатуса = ИнтеграцияСЯндексМаркетСервер.МаркерСтатусаПоСтатусуПолучить( СтрокаДереваВторогоУровня.Статус );
								
							КонецЕсли;
							
						КонецЕсли;

						Если ЗначениеЗаполнено(Параметры.РазмерПорцииЗагрузкиДанных)
							 И Идентификаторы.Количество() >= Параметры.РазмерПорцииЗагрузкиДанных Тогда
							Прервать;
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЦикла; // Пока ВыборкаДанных.Следующий() Цикл
				
			КонецЦикла;
		КонецЕсли;
		
		Если Идентификаторы.Количество() = 0 Тогда
			Результат.Ошибка.КодОшибки = КодыОшибокРегистраСтатусовПубликацииТоваров().ОшибкаПрочие;
			Результат.Ошибка.ОписаниеОшибки = НСтр("ru = 'Данных для загрузки с торговой площадки не найдено.'");
			Возврат Результат;
		КонецЕсли;
		
		РезультатПоискаКатегорий = Неопределено;

		// Заполнение дерева товаров  
		Если ДеревоТоваров <> Неопределено Тогда
			Для Каждого СтрокаДереваПервогоУровня Из ДеревоТоваров.Строки Цикл
			
				Для Каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
				
					Если ИдентификаторыПовторные.Найти(СтрокаДереваВторогоУровня[ИмяИдентификатора]) <> Неопределено Тогда
						Продолжить;
					КонецЕсли;					
					
					СтрокаДереваВторогоУровня.ДанныеТовараИзСервиса.Очистить();
					СтрокаДереваВторогоУровня.ЗначенияРеквизитовИзСервиса.Очистить();
					СтрокаДереваВторогоУровня.ЦеныТовара = Новый Массив;
					
					Если СтрокаДереваВторогоУровня.ВариантЗагрузки = ИнтеграцияСЯндексМаркетКлиентСервер.ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
						СтрокаДереваВторогоУровня.Пометка = (Параметры.ВариантЗагрузкиДанных >= 1);
						СтрокаДереваВторогоУровня.ИндексСтатуса = 6;
					КонецЕсли;
						
                    ИмпортИзЯндексМаркетДеревоТоваровДеревоРеквизитовЗаполнитьДляСтроки( СтрокаДереваВторогоУровня, Параметры );
					
					Если Не ЗначениеЗаполнено( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса ) Тогда
						СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса = "";
					КонецЕсли;
					
					// Дозаполнение категорий через поиск
					Если ЗначениеЗаполнено( СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса ) Тогда
						
						// Категория
						Если Не ЗначениеЗаполнено( СтрокаДереваВторогоУровня.Категория ) Тогда
							
							КлючПоиска = "1#" + СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса;
							КатегорияСсылка = КешНайденныхКатегорий.Получить( КлючПоиска );
							Если Не ЗначениеЗаполнено( КатегорияСсылка ) Тогда
								КатегорияСсылка = ОбменСВнешнимиСистемамиНоменклатура.КатегорияНоменклатурыПоНаименованиюНайти( СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса, Ложь );

								КешНайденныхКатегорий.Вставить( КлючПоиска, ?(ЗначениеЗаполнено( КатегорияСсылка ), КатегорияСсылка, "-" ));
							КонецЕсли;
							
							СтрокаДереваВторогоУровня.Категория = КатегорияСсылка;
							
						КонецЕсли;
						
					КонецЕсли;
					
				КонецЦикла;// Для Каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
				
				СтрокаДереваПервогоУровня.Строки.Сортировать( "НаименованиеКатегорииМаркетплейса, ЗначениеОбъединенияВОднуКарточку, ПредставлениеОбъектаМаркетплейса", Ложь );
				
			КонецЦикла;
			
			Для Каждого СтрокаДереваПервогоУровня Из ДеревоТоваров.Строки Цикл
				Если СтрокаДереваПервогоУровня.ВариантЗагрузки = 2 
					 ИЛИ СтрокаДереваПервогоУровня.ВариантЗагрузки = 3 Тогда
					Продолжить;
				КонецЕсли;
				
				Индекс = 0;
				
				Пока Индекс < СтрокаДереваПервогоУровня.Строки.Количество() Цикл
					СтрокаДереваВторогоУровня = СтрокаДереваПервогоУровня.Строки[Индекс];
					
					Если СтрокаДереваВторогоУровня.ИндексСтатуса = 4 Тогда
						Если СтрокаДереваГруппаОшибки = Неопределено Тогда
							СтрокаДереваГруппаОшибки = ДеревоТоваров.Строки.Вставить(Мин(3, ДеревоТоваров.Строки.Количество()));
							СтрокаДереваГруппаОшибки.ВариантЗагрузки = 3;
							СтрокаДереваГруппаОшибки.ИндексСтатуса = -2;
						КонецЕсли;
						
						ИмпортИзЯндексМаркетПеренестиСтрокуДереваТоваровВДругуюГруппу(ДеревоТоваров, СтрокаДереваВторогоУровня, СтрокаДереваГруппаОшибки);
						
					Иначе
						Индекс = Индекс + 1;
					КонецЕсли;
				КонецЦикла;
			КонецЦикла;
			
			ИмпортИзЯндексМаркетОпределитьНаименованияГруппДереваТоваров(
				ДеревоТоваров, 
				Ложь, 
				Параметры.ОтображатьСопоставленные,
				Параметры.ОтображатьПоСтатусу);
				
			
			Если ТоварыНайти = Истина Тогда
				ПараметрыФункции = Новый Структура;
				
				ПараметрыФункции.Вставить( "ИмпортируемыеТовары", ДеревоТоваров );
				ПараметрыФункции.Вставить( "УчетнаяЗаписьМаркетплейса", УчетнаяЗаписьМаркетплейса );
				ПараметрыФункции.Вставить( "ИсточникКатегории", ИсточникКатегории );

				ТоварыНайти( ПараметрыФункции );
				ПараметрыФункции = Неопределено;
			КонецЕсли;
			
		КонецЕсли;	
		
		Результат.Вставить("ДеревоТоваров", ДеревоТоваров);

		Если ЗначениеЗаполнено(Параметры.КэшКатегорий) Тогда
			Результат.Вставить("КэшКатегорий", Параметры.КэшКатегорий);
		КонецЕсли;		
		
		Результат.Вставить("ИдентификаторыТоваров", Параметры.ИдентификаторыТоваров);
		
	Исключение
		ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		Отказ = Истина;
		
		ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
		
		ЗаписьЖурналаРегистрации(СобытиеЖурналаРегистрации(),
		УровеньЖурналаРегистрации.Ошибка,,,
		ТекстОшибки );
		
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗаписьМаркетплейса, Истина );
	КонецПопытки;

	Если Отказ Тогда
		Результат.Ошибка.КодОшибки = КодыОшибокРегистраСтатусовПубликацииТоваров().ОшибкаПрочие;
		Результат.Ошибка.ОписаниеОшибки = ТекстОшибки;
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

// Конструктор таблицы значений для описания товаров, полученных из сервиса.
//
Функция НоваяТаблицаОписанияТоваров() Экспорт

	ОписаниеТипаБулево          = Новый ОписаниеТипов("Булево");
	ОписаниеТипаДата            = Новый ОписаниеТипов("Дата");
	ОписаниеТипаСтрока          = Новый ОписаниеТипов("Строка");
	ОписаниеТипаСтрока10        = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(10));
	ОписаниеТипаСтрока50        = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(50));
	ОписаниеТипаСтрока250       = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(250));
	ОписаниеТипаСтрока1024      = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(1024));
	ОписаниеТипаЧисло           = Новый ОписаниеТипов("Число");
	ОписаниеТипаДенежнаяСумма   = Новый ОписаниеТипов(Метаданные.ОпределяемыеТипы.ДенежнаяСуммаНеотрицательная.Тип);
	ОписаниеТипаТаблицаЗначений = Новый ОписаниеТипов("ТаблицаЗначений");
	
	Результат = Новый ТаблицаЗначений;
	
	// Основные
	Результат.Колонки.Добавить("УчетнаяЗаписьМаркетплейса",            Новый ОписаниеТипов("СправочникСсылка.УчетныеЗаписиМаркетплейсов"));
	Результат.Колонки.Добавить("ВидОбъектаМаркетплейса",               Новый ОписаниеТипов("ПеречислениеСсылка.ВидыОбъектовМаркетплейсов"));
	Результат.Колонки.Добавить("ИдентификаторПубликации",              ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИдентификаторОбъектаМаркетплейса",     ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИдентификаторFBOSKU",                  ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИдентификаторFBSSKU",                  ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ПредставлениеОбъектаМаркетплейса",     ОписаниеТипаСтрока250);
	Результат.Колонки.Добавить("ОписаниеОбъектаМаркетплейса",          ОписаниеТипаСтрока1024);
	Результат.Колонки.Добавить("Штрихкод",                             ОписаниеТипаСтрока);
	
	// Категория маркетплейса
	Результат.Колонки.Добавить("ИдентификаторКатегорииМаркетплейса",   ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("НаименованиеКатегорииМаркетплейса",    ОписаниеТипаСтрока250);
	
	// Ценовые показатели
	Результат.Колонки.Добавить("ЦеновойИндекс",                        ОписаниеТипаСтрока10);
	Результат.Колонки.Добавить("ЦенаДоСкидки",                         ОписаниеТипаДенежнаяСумма);
	Результат.Колонки.Добавить("ЦенаСоСкидкой",                        ОписаниеТипаДенежнаяСумма);
	Результат.Колонки.Добавить("МинимальнаяЦена",                      ОписаниеТипаДенежнаяСумма);
	Результат.Колонки.Добавить("ЦенаНаВитрине",                        ОписаниеТипаДенежнаяСумма);
	Результат.Колонки.Добавить("ЦенаПремиальная",                      ОписаниеТипаДенежнаяСумма);
	Результат.Колонки.Добавить("ЦенаРекомендованная",                  ОписаниеТипаДенежнаяСумма);
	Результат.Колонки.Добавить("КодВалюты",                            ОписаниеТипаСтрока10);
	Результат.Колонки.Добавить("СтавкаНДС",                            ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("УцененныйТовар",                       ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ЕстьУцененныеАналоги",                 ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ПредоплатаОбязательна",                ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ПредоплатаВозможна",                   ОписаниеТипаБулево);
	
	// Статус публикации	
	Результат.Колонки.Добавить("ЗаданиеПубликации",                    ОписаниеТипаСтрока10);
	Результат.Колонки.Добавить("Статус",                               Новый ОписаниеТипов("ПеречислениеСсылка.СтатусыВыгрузкиТоваровЯндексМаркет"));
	Результат.Колонки.Добавить("Состояние",                            ОписаниеТипаСтрока250);
	Результат.Колонки.Добавить("ОписаниеСостояния",                    ОписаниеТипаСтрока1024);
	Результат.Колонки.Добавить("КодОшибки",                            ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("ОписаниеОшибки",                       ОписаниеТипаСтрока);
	Результат.Колонки.Добавить("ВыставленНаПродажу",                   ОписаниеТипаБулево);
	
	// Прочее
	Результат.Колонки.Добавить("ДатаСозданияНаМаркетплейсе",           ОписаниеТипаДата);
	Результат.Колонки.Добавить("ДатаОбновленияНаМаркетплейсе",         ОписаниеТипаДата);
	Результат.Колонки.Добавить("МаркетинговыйЦвет", 			       ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИнформацияОКомиссиях",                 ОписаниеТипаТаблицаЗначений);
	Результат.Колонки.Добавить("ИсточникиСхожихПредложений",           ОписаниеТипаТаблицаЗначений);
	Результат.Колонки.Добавить("КрупногабаритныйТовар",                ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ОбъемныйВес",                		   ОписаниеТипаЧисло);

	// Остатки товара
	Результат.Колонки.Добавить("ДатаАктуальностиОстатковМаркетплейса", ОписаниеТипаДата);
	Результат.Колонки.Добавить("ОстаткиТовара_ВНаличии",               ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("ОстаткиТовара_Зарезервировано",        ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("ОстаткиТовара_Ожидается",              ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("ОстатокFBO",                           ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("РезервFBO",                            ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("ОстатокFBS",                           ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("РезервFBS",                            ОписаниеТипаЧисло);
	
	// Схемы продаж
	Результат.Колонки.Добавить("ПродаетсяПоСхемеРаботыFBO",            ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ПродаетсяПоСхемеРаботыFBS",            ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ПродаетсяПоСхемеРаботыDBS",            ОписаниеТипаБулево);
	
	// Параметры объединения в одну карточку
	Результат.Колонки.Добавить("ОбъединяетсяВОднуКарточку",            ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ЗначениеОбъединенияВОднуКарточку",     ОписаниеТипаСтрока250);
	
	Результат.Колонки.Добавить("ИдентификаторАтрибутаОбъединенияВОднуКарточку",     ОписаниеТипаСтрока50);
	
	// Изображения
	Результат.Колонки.Добавить("Изображения",                          ОписаниеТипаТаблицаЗначений);
	
	Возврат Результат;

КонецФункции

// Возвращает коды ошибок.
// 
// Возвращаемое значение:
//  Структура - описания ошибок.
//
Функция КодыОшибокРегистраСтатусовПубликацииТоваров() Экспорт

	КодыОшибок = Новый Структура;
	КодыОшибок.Вставить("ОшибкаЗаполненияДанных", "Ошибка_заполнения_данных_в_1С");
	КодыОшибок.Вставить("ОшибкаПубликацииТоваров", "Ошибка_публикации");
	КодыОшибок.Вставить("ОшибкаЗагрузкиСертификата", "Ошибка_загрузки_сертификата");
	КодыОшибок.Вставить("ОшибкаВыборкиТоваров", "Ошибка_выборки_товаров");
	КодыОшибок.Вставить("ОшибкаПереносаТоваров", "Ошибка_переноса_в_архив");
	КодыОшибок.Вставить("ОшибкаВозвратаТоваров", "Ошибка_возврата_из_архива");
	КодыОшибок.Вставить("ОшибкаУдаленияТоваров", "Ошибка_удаления_товаров");
	КодыОшибок.Вставить("ОшибкаОбновлениеДанныхЗапрещено", "Ошибка_обновление_данных_торговой_площадки_запрещено");
	КодыОшибок.Вставить("ОшибкаПрочие", "Ошибка_прочие");
	КодыОшибок.Вставить("ОшибкаКатегорияУстарела", "Ошибка_категория_устарела");

	Возврат КодыОшибок;

КонецФункции

// Конструктор таблицы значений для описания характеристик товаров, полученных из сервиса.
//
// Возвращаемое значение:
//	 ТаблицаЗначений - таблица с колонками:
//	   * УчетнаяЗаписьМаркетплейса		    - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису;
//     * ИдентификаторПубликации            - Строка - внутренний идентификатор товара в системе продавца;
//     * ИдентификаторОбъектаМаркетплейса   - Строка - идентификатор товара на маркетплейсе.
//     * ФильтрВидимости                    - Строка - фильтр по видимости товара;
//     * Штрихкод                           - Строка - штрихкоды, через ";";
//     * ИдентификаторКатегорииМаркетплейса - Строка - идентификатор категории маркетплейса;
//     * ЗначенияАтрибутов                  - ТаблицаЗначений - значения реквизитов товара, 
//                                              см. НоваяТаблицаЗначенийРеквизитов;
//     * Изображения                        - ТаблицаЗначений - изображения товара, 
//                                              см. НоваяТаблицаИзображений;
//     * ДокументыPDF                       - ТаблицаЗначений - документы PDF, 
//                                              см. НоваяТаблицаДокументовPDF.
//
Функция НоваяТаблицаОписанияХарактеристикТоваров() Экспорт

	ОписаниеТипаСтрока50        = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(50));
	ОписаниеТипаСтрока250       = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(250));
	ОписаниеТипаТаблицаЗначений = Новый ОписаниеТипов("ТаблицаЗначений");

	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("УчетнаяЗаписьМаркетплейса", 		 Новый ОписаниеТипов("СправочникСсылка.УчетныеЗаписиМаркетплейсов"));
	Результат.Колонки.Добавить("ИдентификаторПубликации",  		     ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИдентификаторОбъектаМаркетплейса",   ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ФильтрВидимости",  		             ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("Штрихкод",                  		 ОписаниеТипаСтрока250);
	Результат.Колонки.Добавить("ИдентификаторКатегорииМаркетплейса", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ЗначенияАтрибутов",                  ОписаниеТипаТаблицаЗначений);
	Результат.Колонки.Добавить("Изображения",                        ОписаниеТипаТаблицаЗначений);
	Результат.Колонки.Добавить("ДокументыPDF",                       ОписаниеТипаТаблицаЗначений);

	Возврат Результат;

КонецФункции

// Конструктор таблицы значений для полученных остатков товаров.
//
// Возвращаемое значение:
//	 ТаблицаЗначений - таблица с колонками:
//	   * УчетнаяЗаписьМаркетплейса        - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису;
//     * ИдентификаторПубликации          - Строка - внутренний идентификатор товара в системе продавца;
//     * ИдентификаторОбъектаМаркетплейса - Строка - идентификатор товара на маркетплейсе;
//     * ИдентификаторSKU                 - Строка - SKU товара, который продается со склада продавца (схемы FBS и rFBS);
//     * ИдентификаторСкладаМаркетплейса  - Строка - идентификатор склада на маркетплейсе;
//     * НаименованиеСкладаМаркетплейса   - Строка - наименование склада на маркетплейсе;
//     * ТипСкладаМаркетплейса            - Строка - тип склада на маркетплейсе;
//	   * КоличествоВНаличии 		      - Число - количество товара в наличии;
//	   * КоличествоЗарезервировано        - Число - количество товара зарезервировано;
//
Функция НоваяТаблицаОстатковТоваров() Экспорт
	
	ОписаниеТипаСтрока50  = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(50));
	ОписаниеТипаСтрока250 = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(250));
	ОписаниеТипаЧисло15_3 = Новый ОписаниеТипов("Число", Новый КвалификаторыЧисла(15, 3));
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("УчетнаяЗаписьМаркетплейса",        Новый ОписаниеТипов("СправочникСсылка.УчетныеЗаписиМаркетплейсов"));
	Результат.Колонки.Добавить("ИдентификаторПубликации",          ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИдентификаторОбъектаМаркетплейса", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИдентификаторSKU",                 ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИдентификаторСкладаМаркетплейса",  ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("НаименованиеСкладаМаркетплейса",   ОписаниеТипаСтрока250);
	Результат.Колонки.Добавить("ТипСкладаМаркетплейса",            ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("КоличествоВНаличии",               ОписаниеТипаЧисло15_3);
	Результат.Колонки.Добавить("КоличествоЗарезервировано",        ОписаниеТипаЧисло15_3);
	
	Возврат Результат;

КонецФункции

// Возвращает детальную информацию по типам цен, используемым учетной записью.
//
// Параметры:
//  ВключатьВыгружаемые       - Булево - Признак включения в результат функции выгружаемых типов цен.
//  ВключатьЗагружаемые       - Булево - Признак включения в результат функции загружаемых типов цен.
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Таблица с описанием указанных типов цен учетной записи. Содержит колонки:
//                  * Идентификатор - Строка - Строковое обозначение типа цен;
//                  * ИдентификаторДляФормул - Шаблон для формирования идентификатора вида цен;
//                  * Наименование - Строка - Наименование типа цен;
//                  * ДляУчетнойЗаписи - Булево - Признак отношения типа цен к учетной записи или к интеграции в целом;
//                  * ОбязательноеЗаполнение - Булево - Признак обязательного заполнения в формах;
//                  * Значение - СправочникСсылка.ВидыЦен - Пустая ссылка вида цен.
//
Функция ТипыЦенЯндексМаркет(ВключатьВыгружаемые = Истина, ВключатьЗагружаемые = Истина) Экспорт

	ОписаниеТиповСтрока50 = Новый ОписаниеТипов("Строка",, Новый КвалификаторыСтроки(50, ДопустимаяДлина.Переменная));

	ТипыЦен = Новый ТаблицаЗначений;
	ТипыЦен.Колонки.Добавить("Идентификатор", ОписаниеТиповСтрока50);
	ТипыЦен.Колонки.Добавить("ИдентификаторДляФормул", ОписаниеТиповСтрока50);
	ТипыЦен.Колонки.Добавить("Наименование", ОписаниеТиповСтрока50);
	ТипыЦен.Колонки.Добавить("ДляУчетнойЗаписи", Новый ОписаниеТипов("Булево"), НСтр("ru = 'Тип цен уникален для учетной записи'"));
	ТипыЦен.Колонки.Добавить("ОбязательноеЗаполнение", Новый ОписаниеТипов("Булево"));
	ТипыЦен.Колонки.Добавить("Значение", Новый ОписаниеТипов("СправочникСсылка.ВидыЦен"));

	Если ВключатьВыгружаемые Тогда
		СтрокаТаблицыЗначений = ТипыЦен.Добавить();
		СтрокаТаблицыЗначений.Идентификатор = "ВидЦеныСУчетомСкидок";
		СтрокаТаблицыЗначений.ИдентификаторДляФормул = "";
		СтрокаТаблицыЗначений.Наименование = НСтр("ru = 'Цена с учетом скидок'");
		СтрокаТаблицыЗначений.ДляУчетнойЗаписи = Истина;
		СтрокаТаблицыЗначений.ОбязательноеЗаполнение = Истина;

		СтрокаТаблицыЗначений = ТипыЦен.Добавить();
		СтрокаТаблицыЗначений.Идентификатор = "ВидЦеныДоСкидок";
		СтрокаТаблицыЗначений.ИдентификаторДляФормул = "";
		СтрокаТаблицыЗначений.Наименование = НСтр("ru = 'Цена до скидок'");
		СтрокаТаблицыЗначений.ДляУчетнойЗаписи = Истина;
		СтрокаТаблицыЗначений.ОбязательноеЗаполнение = Ложь;

		СтрокаТаблицыЗначений = ТипыЦен.Добавить();
		СтрокаТаблицыЗначений.Идентификатор = "ВидЦеныМинимальныхЦен";
		СтрокаТаблицыЗначений.ИдентификаторДляФормул = "";
		СтрокаТаблицыЗначений.Наименование = НСтр("ru = 'Минимальная цена после применения всех акций'");
		СтрокаТаблицыЗначений.ДляУчетнойЗаписи = Истина;
		СтрокаТаблицыЗначений.ОбязательноеЗаполнение = Ложь;
	КонецЕсли;
	
	Если ВключатьЗагружаемые Тогда
		СтрокаТаблицыЗначений = ТипыЦен.Добавить();
		СтрокаТаблицыЗначений.Идентификатор = "ВидЦеныСАкциямиПродавца";
		СтрокаТаблицыЗначений.ИдентификаторДляФормул = "marketing_seller_price";
		СтрокаТаблицыЗначений.Наименование = НСтр("ru = 'Цены с учетом акций продавца'");
		СтрокаТаблицыЗначений.ДляУчетнойЗаписи = Истина;
		СтрокаТаблицыЗначений.ОбязательноеЗаполнение = Ложь;

		СтрокаТаблицыЗначений = ТипыЦен.Добавить();
		СтрокаТаблицыЗначений.Идентификатор = "ВидЦеныСоВсемиАкциями";
		СтрокаТаблицыЗначений.ИдентификаторДляФормул = "marketing_price";
		СтрокаТаблицыЗначений.Наименование = НСтр("ru = 'Цены с учетом всех акций'");
		СтрокаТаблицыЗначений.ДляУчетнойЗаписи = Истина;
		СтрокаТаблицыЗначений.ОбязательноеЗаполнение = Ложь;

		СтрокаТаблицыЗначений = ТипыЦен.Добавить();
		СтрокаТаблицыЗначений.Идентификатор = "ВидЦеныРекомендованный";
		СтрокаТаблицыЗначений.ИдентификаторДляФормул = "recommended_price";
		СтрокаТаблицыЗначений.Наименование = НСтр("ru = 'Рекомендованные цены'");
		СтрокаТаблицыЗначений.ДляУчетнойЗаписи = Ложь;
		СтрокаТаблицыЗначений.ОбязательноеЗаполнение = Ложь;

		СтрокаТаблицыЗначений = ТипыЦен.Добавить();
		СтрокаТаблицыЗначений.Идентификатор = "ВидЦеныПоставщика";
		СтрокаТаблицыЗначений.ИдентификаторДляФормул = "retail_price";
		СтрокаТаблицыЗначений.Наименование = НСтр("ru = 'Цены поставщика товаров'");
		СтрокаТаблицыЗначений.ДляУчетнойЗаписи = Ложь;
		СтрокаТаблицыЗначений.ОбязательноеЗаполнение = Ложь;
	КонецЕсли;

	Возврат ТипыЦен;

КонецФункции

// Возвращает детальную информацию по видам цен, используемым учетной записью с заполненным значением цены.
//
// Параметры:
//  УчетнаяЗаписьМаркетплейса - СправочникСсылка.УчетныеЗаписиМаркетплейсов - Учетная запись для детализации.
//  ВключатьВыгружаемые       - Булево - Признак включения в результат функции выгружаемых видов цен.
//  ВключатьЗагружаемые       - Булево - Признак включения в результат функции загружаемых видов цен.
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Таблица с запрошенными видами цен и заполненным значением цены.
//                    см.описание функции ТипыЦенЯндексМаркет()
//
Функция ВидыЦенУчетнойЗаписи(УчетнаяЗаписьМаркетплейса, ВключатьВыгружаемые = Истина, ВключатьЗагружаемые = Истина) Экспорт
	
	ТипыЦен = ТипыЦенЯндексМаркет(ВключатьВыгружаемые, ВключатьЗагружаемые);
	
	// Выгружаемые виды цен
	Если ВключатьВыгружаемые Тогда
		Для каждого ВидЦены Из УчетнаяЗаписьМаркетплейса.ВидыЦен Цикл
			Результат = ТипыЦен.НайтиСтроки(Новый Структура("Идентификатор", ВидЦены.ИмяНастройки));
			Если Результат.Количество() = 1 Тогда
				Результат[0].Значение = ВидЦены.ВидЦены;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;

	Возврат ТипыЦен;

КонецФункции

// Конструктор списка параметров для фильтра по товарам.
//
// Параметры:
//   ЗагрузитьОписанияАрхивныхПозиций   - Булево - Истина, если в выборку включаются товары из архива.
//   ИдентификаторыПубликации           - Массив Из Строка - список внутренних идентификаторов товаров в системе продавца;
//                                      - Неопределено - выборка по идентификаторам публикации не используется.
//   ИдентификаторыОбъектовМаркетплейса - Массив Из Строка - список идентификаторов товаров на маркетплейсе;
//                                      - Неопределено - выборка по идентификаторам товаров на маркетплейсе не используется.
//
// Возвращаемое значение:
//   Структура - фильтры для получения списка товаров (filter):
//     * ФильтрВидимости                    - Массив Из Строка - для указания нескольких фильтров по видимости;
//                                          - Строка - фильтр по видимости товара (visibility): 
//                                              "ALL"                      - все товары, кроме архивных;
//                                              "VISIBLE"                  - товары, которые видны покупателям;.
//                                              "INVISIBLE"                - товары, которые не видны покупателям;
//                                              "EMPTY_STOCK"              - товары, у которых не указано наличие;
//                                              "NOT_MODERATED"            - товары, которые не прошли модерацию;
//                                              "MODERATED"                - товары, которые прошли модерацию;
//                                              "DISABLED"                 - товары, которые видны покупателям, но недоступны к покупке;
//                                              "STATE_FAILED"             - товары, создание которых завершилось ошибкой;
//                                              "READY_TO_SUPPLY"          - товары, готовые к поставке;
//                                              "VALIDATION_STATE_PENDING" - товары, которые проходят проверку валидатором на предварительной модерации;
//                                              "VALIDATION_STATE_FAIL"    - товары, которые не прошли проверку валидатором на предварительной модерации;
//                                              "VALIDATION_STATE_SUCCESS" - товары, которые прошли проверку валидатором на предварительной модерации;
//                                              "TO_SUPPLY"                - товары, готовые к продаже;
//                                              "IN_SALE"                  - товары в продаже;
//                                              "REMOVED_FROM_SALE"        - товары, скрытые от покупателей;
//                                              "BANNED"                   - заблокированные товары;
//                                              "OVERPRICED"               - товары с завышенной ценой;
//                                              "CRITICALLY_OVERPRICED"    - товары со слишком завышенной ценой;
//                                              "EMPTY_BARCODE"            - товары без штрихкода;
//                                              "BARCODE_EXISTS"           - товары со штрихкодом;
//                                              "QUARANTINE"               - товары на карантине после изменения цены более чем на 50%;
//                                              "ARCHIVED"                 - товары в архиве;
//                                              "OVERPRICED_WITH_STOCK"    - товары в продаже со стоимостью выше, чем у конкурентов;
//                                              "PARTIAL_APPROVED"         - товары в продаже с пустым или неполным описанием;
//                                              "IMAGE_ABSENT"             - товары без изображений;
//                                              "MODERATION_BLOCK"                  - товары, для которых заблокирована модерация.
//     * ИдентификаторыПубликации           - Массив Из Строка - список внутренних идентификаторов товаров в системе продавца (offer_id);
//     * ИдентификаторыОбъектовМаркетплейса - Массив Из Строка - список идентификаторов товаров на маркетплейсе (product_id).
//
Функция НовыйФильтрЗапросаПоТоварам( ЗагрузитьОписанияАрхивныхПозиций = Ложь, ИдентификаторыПубликации = Неопределено, ИдентификаторыОбъектовМаркетплейса = Неопределено ) Экспорт

	Результат = Новый Структура;
	
	Если ЗагрузитьОписанияАрхивныхПозиций Тогда
	
		ФильтрыВидимости = Новый Массив;
		ФильтрыВидимости.Добавить( "ALL" );
		ФильтрыВидимости.Добавить( "ARCHIVED" );

		Результат.Вставить( "ФильтрВидимости", ФильтрыВидимости );
		
	Иначе
		Результат.Вставить( "ФильтрВидимости", "ALL" );
	КонецЕсли;

	Результат.Вставить( "ИдентификаторыПубликации", ИдентификаторыПубликации );
	Результат.Вставить( "ИдентификаторыОбъектовМаркетплейса", ИдентификаторыОбъектовМаркетплейса );

	Возврат Результат;

КонецФункции

// Возвращает новую пустую таблицу значений для заполнения данных по реквизитам публикуемых товаров.
//
// Возвращаемое значение:
//	ТаблицаЗначений - таблица с колонками:
//	 * Номенклатура 		          			   - СправочникСсылка.Номенклатура - номенклатура.
//	 * Характеристика    			               - СправочникСсылка.ХарактеристикиНоменклатуры - Характеристика номенклатуры.
//	 * Упаковка 		             			   - СправочникСсылка.ЕдиницыИзмерения - упаковка.
//	 * Категория1С 		             			   - см. ОписаниеТипаКатегории1С() - категория 1С из номенклатуры.
//	 * ИсточникКатегорииМаркетплейса 			   - см. ОписаниеТипаКатегории1С() - категория 1С, 
//															для которой задано соответствие категории маркетплейса, может быть указан элемент или группа элементов.
//	 * ИдентификаторИсточникаКатегорииМаркетплейса - Строка - идентификатор источника категории маркетплейса.
//	 * ИдентификаторКатегорииМаркетплейса		   - Строка - идентификатор категории маркетплейса.
//	 * ИдентификаторРодителяАтрибутаМаркетплейса   - Строка - идентификатор родителя атрибута маркетплейса.
//	 * НаименованиеРодителяАтрибутаМаркетплейса    - Строка - наименование родителя атрибута маркетплейса.
//	 * ИдентификаторАтрибутаМаркетплейса 		   - Строка - идентификатор атрибута маркетплейса.
//	 * НаименованиеАтрибутаМаркетплейса  		   - Строка - наименование атрибута.
//	 * ПолныйПутьКРеквизиту1С 			 		   - Строка - путь к реквизиту из регистра сведений СоответствияАтрибутовМаркетплейсов.
//	 * ОбязательныйАтрибут				   		   - Булево - признак обязательного атрибута.
//	 * ЯвляетсяКоллекциейЗначений 		 		   - Булево - признак того, что атрибут содержит список значений выбора.
//	 * ДоступенВыборЗначенияИзСписка  	  		   - Булево - признак наличия списка выбора значений атрибута в 1С.
//	 * ТипЗначенияАтрибута                		   - Строка - тип значения характеристики.
//	 * ЗначениеПоУмолчанию                		   - Строка, Число, Булево, Дата - значение по умолчанию.
//	 * ИдентификаторЗначенияПоУмолчанию  		   - Строка - идентификатор значения по умолчанию при выборе значения из списка.
//	 * ЗначениеРеквизита1С 				  		   - ЛюбаяСсылка, Строка, Число, Булево, Дата - значение реквизита 1С.
//	 * ЗначениеАтрибутаМаркетплейса 	  		   - Строка, Число, Булево, Дата - значение для маркетплейса.
//	 * ИдентификаторЗначенияАтрибутаМаркетплейса   - Строка - идентификатор значения для маркетплейса при выборе значения из списка.
//   * НеЗаполненРеквизит1С 				       - Булево - признак незаполненного реквизита 1С.
//	 * НеЗаполненоСоответствиеАтрибута	           - Булево - признак незаполненного соответствия атрибуту маркетплейса.
//	 * НеЗаполненАтрибут			 			   - Булево - признак незаполненного обязательного атрибута маркетплейса.
//	 * УстановленоЗначениеПоУмолчанию			   - Булево - признак установки значения по умолчанию из-за незаполненного реквизита 1С.
//															или незаполненного соответствия атрибуту маркетплейса.
//	 * ЗначениеАтрибутаЗаданоПользователем         - Булево - признак установки значения пользователем.
//
Функция НоваяТаблицаЗначенийРеквизитов(Знач РеквизитыДляСравнения = Ложь) Экспорт

	ОписаниеТипаСтрока50    = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(50));
	ОписаниеТипаСтрока250   = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(250));
	ОписаниеТипаСтрока500   = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(500));
	ОписаниеТипаБулево      = Новый ОписаниеТипов("Булево");
	ОписаниеТипаКатегории1С = ОписаниеТипаКатегории1С();
	ОписаниеТипаЧисло5	    = Новый ОписаниеТипов("Число",,, Новый КвалификаторыЧисла(5));

	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("УчетнаяЗаписьМаркетплейса", Новый ОписаниеТипов("СправочникСсылка.УчетныеЗаписиМаркетплейсов"));
	Результат.Колонки.Добавить("Номенклатура", Новый ОписаниеТипов("СправочникСсылка.Номенклатура"));
	Результат.Колонки.Добавить("Характеристика", Новый ОписаниеТипов("СправочникСсылка.ХарактеристикиНоменклатуры"));
	Результат.Колонки.Добавить("Упаковка", Новый ОписаниеТипов("СправочникСсылка.ЕдиницыИзмерения"));
	Результат.Колонки.Добавить("Категория1С", ОписаниеТипаКатегории1С);
	Результат.Колонки.Добавить("ИсточникКатегорииМаркетплейса", ОписаниеТипаКатегории1С);
	Результат.Колонки.Добавить("ИдентификаторИсточникаКатегорииМаркетплейса", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ИдентификаторКатегорииМаркетплейса", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("НаименованиеКатегорииМаркетплейса", ОписаниеТипаСтрока250);
	Результат.Колонки.Добавить("ИдентификаторРодителяАтрибутаМаркетплейса", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("НаименованиеРодителяАтрибутаМаркетплейса", ОписаниеТипаСтрока250);
	Результат.Колонки.Добавить("ИдентификаторАтрибутаМаркетплейса", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("НаименованиеАтрибутаМаркетплейса", ОписаниеТипаСтрока250);
	Результат.Колонки.Добавить("ПолныйПутьКРеквизиту1С", ОписаниеТипаСтрока500);
	Результат.Колонки.Добавить("ПредставлениеРеквизита1С", ОписаниеТипаСтрока500);
	Результат.Колонки.Добавить("ОбязательныйАтрибут", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("АспектныйАтрибут",  ОписаниеТипаБулево);
	Результат.Колонки.Добавить("МаксимальноеКоличествоЗначений", ОписаниеТипаЧисло5);
	Результат.Колонки.Добавить("ЯвляетсяКоллекциейЗначений", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ДоступенВыборЗначенияИзСписка", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ТипЗначенияАтрибута", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ЗначениеПоУмолчанию");// произвольный
	Результат.Колонки.Добавить("ЗначениеПоУмолчаниюСтрокаНеограниченная");// произвольный
	Результат.Колонки.Добавить("ИдентификаторЗначенияПоУмолчанию", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("ЗначениеРеквизита1С"); // Любая ссылка и простые типы.
	Результат.Колонки.Добавить("ЗначениеАтрибутаМаркетплейса" ); // произвольный
	Результат.Колонки.Добавить("ИдентификаторЗначенияАтрибутаМаркетплейса", ОписаниеТипаСтрока50);
	Результат.Колонки.Добавить("НеЗаполненРеквизит1С", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("НеЗаполненоСоответствиеАтрибута", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("НеЗаполненАтрибут", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("НеЗаполненПутьКРеквизиту1С", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("УстановленоЗначениеПоУмолчанию", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ЗначениеАтрибутаЗаданоПользователем", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("ОписаниеНазначенияАтрибута", Новый ОписаниеТипов("Строка"));
	Результат.Колонки.Добавить("ИзменилсяРеквизит1С", ОписаниеТипаБулево);
	Результат.Колонки.Добавить("НовоеЗначениеРеквизита1С"); // Любая ссылка и простые типы.

	Если РеквизитыДляСравнения Тогда
		Результат.Колонки.Добавить("ЕстьРазличия",  								   ОписаниеТипаБулево);
		Результат.Колонки.Добавить("ТолькоТекущее", 								   ОписаниеТипаБулево);
		Результат.Колонки.Добавить("ТекущееОбязательныйАтрибут",                       ОписаниеТипаБулево);
		Результат.Колонки.Добавить("ТекущееАспектныйАтрибут",                       ОписаниеТипаБулево);
		Результат.Колонки.Добавить("ТекущееМаксимальноеКоличествоЗначений",			   ОписаниеТипаЧисло5);
		Результат.Колонки.Добавить("ТекущееЯвляетсяКоллекциейЗначений",                ОписаниеТипаБулево);
		Результат.Колонки.Добавить("ТекущееДоступенВыборЗначенияИзСписка",             ОписаниеТипаБулево);
		Результат.Колонки.Добавить("ТекущееТипЗначенияАтрибута",                       ОписаниеТипаСтрока50);
		Результат.Колонки.Добавить("ТекущееЗначениеАтрибутаМаркетплейса",              Новый ОписаниеТипов(ИнтеграцияСЯндексМаркетКлиентСервер.ПростыеТипы()));
		Результат.Колонки.Добавить("ТекущееНеЗаполненАтрибут", 						   ОписаниеТипаБулево);
		Результат.Колонки.Добавить("ТекущееИдентификаторЗначенияАтрибутаМаркетплейса", ОписаниеТипаСтрока50);
	КонецЕсли;

	Возврат Результат;

КонецФункции

// Конструктор таблицы значений для идентификаторов товаров, полученных из сервиса.
//
// Возвращаемое значение:
//	 ТаблицаЗначений - таблица с колонками:
//	   * УчетнаяЗаписьМаркетплейса		  - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису;
//     * ИдентификаторПубликации          - Строка - внутренний идентификатор товара в системе продавца;
//     * ИдентификаторОбъектаМаркетплейса - Строка - идентификатор товара на маркетплейсе.
//     * ПродаетсяСоСкидкой               - Булево - товар продается со скидкой;
//     * Архивный                         - Булево - признак архивного товара;
//     * ПродаетсяПоСхемеРаботыFBO        - Булево - продается по схеме FBО;
//     * ПродаетсяПоСхемеРаботыFBS        - Булево - продается по схеме FBS;
//     * ФильтрВидимости                  - Строка - фильтр по видимости товара;
//     * ИдентификаторПоследнегоЗначения  - Строка - идентификатор последнего значения на странице.
//
Функция НоваяТаблицаИдентификаторовТоваров() Экспорт

	ОписаниеТипаБулево = Новый ОписаниеТипов("Булево");
	ОписаниеТипаСтрока50 = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(50));
	ОписаниеТипаСтрока150 = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(150));

	ОписаниеТипаСтрока = Новый ОписаниеТипов( "Строка" );
	ОписаниеТипаЧисло = Новый ОписаниеТипов( "Число" );
	ОписаниеТипаТаблицаЗначений = Новый ОписаниеТипов( "ТаблицаЗначений" );

	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить( "УчетнаяЗаписьМаркетплейса", Новый ОписаниеТипов( "СправочникСсылка.УчетныеЗаписиМаркетплейсов") );
	Результат.Колонки.Добавить( "ИдентификаторПубликации", ОписаниеТипаСтрока50 );
	Результат.Колонки.Добавить( "ИдентификаторОбъектаМаркетплейса", ОписаниеТипаСтрока50 );
	Результат.Колонки.Добавить( "ПродаетсяСоСкидкой", ОписаниеТипаБулево );
	Результат.Колонки.Добавить( "Архивный", ОписаниеТипаБулево );
	Результат.Колонки.Добавить( "ПродаетсяПоСхемеРаботыFBO", ОписаниеТипаБулево );
	Результат.Колонки.Добавить( "ПродаетсяПоСхемеРаботыFBS", ОписаниеТипаБулево );
	Результат.Колонки.Добавить( "ФильтрВидимости", ОписаниеТипаСтрока50 );
	Результат.Колонки.Добавить( "ИдентификаторПоследнегоЗначения", ОписаниеТипаСтрока150 );
	
	Результат.Колонки.Добавить( "ПредставлениеОбъектаМаркетплейса", ОписаниеТипаСтрока );
	
	Результат.Колонки.Добавить( "НаименованиеКатегорииМаркетплейса", ОписаниеТипаСтрока );
	Результат.Колонки.Добавить( "ИдентификаторКатегорииМаркетплейса", ОписаниеТипаЧисло );
	Результат.Колонки.Добавить( "ИдентификаторСопоставленнойКарточкиНаМаркете", ОписаниеТипаСтрока );
	Результат.Колонки.Добавить( "НаименованиеСопоставленнойКарточкиНаМаркете", ОписаниеТипаСтрока );

	Результат.Колонки.Добавить( "Изображения", ОписаниеТипаТаблицаЗначений );
	Результат.Колонки.Добавить( "Видео", ОписаниеТипаТаблицаЗначений );
	Результат.Колонки.Добавить( "Производитель" );
	Результат.Колонки.Добавить( "АртикулТовараПроизводителя" );
	Результат.Колонки.Добавить( "Инструкции" );
	Результат.Колонки.Добавить( "Штрихкоды" );
	Результат.Колонки.Добавить( "ПодробноеОписание" );
	Результат.Колонки.Добавить( "СтранаПроизводства" );

	Результат.Колонки.Добавить( "ДлинаСм" );
	Результат.Колонки.Добавить( "ШиринаСм" );
	Результат.Колонки.Добавить( "ВысотаСм" );
	Результат.Колонки.Добавить( "ВесБруттоКг" );
	
	Результат.Колонки.Добавить( "Метки" );
	Результат.Колонки.Добавить( "СрокГодности" );
	Результат.Колонки.Добавить( "СрокСлужбы" );
	Результат.Колонки.Добавить( "ГарантийныйСрок" );
	Результат.Колонки.Добавить( "ТНВЭДКод" );
	Результат.Колонки.Добавить( "ДокументыНаТовар" );
	Результат.Колонки.Добавить( "КоличествоГрузовыхМест" );
	Результат.Колонки.Добавить( "СостояниеУцененногоТовара" );
	Результат.Колонки.Добавить( "ОсобыйТипТовара" );
	Результат.Колонки.Добавить( "ПризнакЦифровогоТовара" );
	Результат.Колонки.Добавить( "ТоварТолькоДляВзрослых" );
	Результат.Колонки.Добавить( "ТоварДляОпределенногоВозраста" );
	Результат.Колонки.Добавить( "АтрибутыТовараПоКатегории" );
	Результат.Колонки.Добавить( "БазоваяЦена" );
	Результат.Колонки.Добавить( "Себестоимость" );
	Результат.Колонки.Добавить( "ДополнительныРасходыНаТовар" );
	Результат.Колонки.Добавить( "ЦенаДляСкидокСМаркетом" );
	Результат.Колонки.Добавить( "СтатусКарточкиТовара" );
	Результат.Колонки.Добавить( "СписокМагазиновРазмещенТовар" );
	Результат.Колонки.Добавить( "ДоступныМоделиРазмещения" );

	Возврат Результат;

КонецФункции

// Возвращает имя события журнала регистрации подсистемы Взаимодействия.
//
// Возвращаемое значение:
//  Строка - Наименование события для записей в журнале регистрации.
//
Функция СобытиеЖурналаРегистрации() Экспорт

	Возврат НСтр("ru = 'Интеграция с Яндекс Маркет'", ОбщегоНазначения.КодОсновногоЯзыка());

КонецФункции

// Конструктор списка параметров для запроса получения описаний товаров из сервиса.
// 
// Параметры:
//   ИдентификаторыПубликации           - Массив Из Строка - список внутренних идентификаторов товаров в системе продавца;
//                                      - Неопределено - выборка по идентификаторам публикации не используется.
//   ИдентификаторыОбъектовМаркетплейса - Массив Из Строка - список идентификаторов товаров на маркетплейсе;
//                                      - Неопределено - выборка по идентификаторам товаров на маркетплейсе не используется.
//   ИдентификаторыSKU                  - Массив Из Строка - список идентификаторов SKU;
//                                      - Неопределено - выборка по идентификаторам SKU не используется.
//
// Возвращаемое значение:
//   Структура - список параметров:
Функция НовыеПараметрыЗапросаПолученияОписанияТоваров(ИдентификаторыПубликации = Неопределено, ИдентификаторыОбъектовМаркетплейса = Неопределено, ИдентификаторыSKU = Неопределено) Экспорт

	Результат = Новый Структура;
	
	Результат.Вставить("ИдентификаторыПубликации",           ИдентификаторыПубликации);
	Результат.Вставить("ИдентификаторыОбъектовМаркетплейса", ИдентификаторыОбъектовМаркетплейса);
	Результат.Вставить("ИдентификаторыSKU",                  ИдентификаторыSKU);
	Результат.Вставить("МаксимальноеКоличествоВЗапросе",     1000);

	Возврат Результат;

КонецФункции

// Осуществляет перенос строки дерева товаров между родителями.
// 
// Параметры:
//   ДеревоТоваров - ДеревоЗначений       - структура дерева описана в реквизите "ИмпортируемыеТовары", 
//   Источник      - СтрокаДереваЗначений - строка дерева товаров, которую необходимо перенести.
//   Приемник      - СтрокаДереваЗначений - строка-родитель дерева товаров, в которую необходимо перенести Источник.
//
Процедура ИмпортИзЯндексМаркетПеренестиСтрокуДереваТоваровВДругуюГруппу(ДеревоТоваров, Источник, Приемник) 
	
	СтрокаДереваВторогоУровня = Приемник.Строки.Добавить();
	ЗаполнитьЗначенияСвойств(СтрокаДереваВторогоУровня, Источник,, "ДеревоРеквизитов, Изображения, ДанныеОбъекта, ДанныеТовараИзСервиса, ЗначенияРеквизитовИзСервиса");

	СтрокаДереваВторогоУровня.ДеревоРеквизитов            = Источник.ДеревоРеквизитов.Скопировать();
	СтрокаДереваВторогоУровня.Изображения                 = Источник.Изображения.Скопировать();
	СтрокаДереваВторогоУровня.ДанныеОбъекта               = Источник.ДанныеОбъекта.Скопировать();
	СтрокаДереваВторогоУровня.ДанныеТовараИзСервиса       = Источник.ДанныеТовараИзСервиса.Скопировать();
	СтрокаДереваВторогоУровня.ЗначенияРеквизитовИзСервиса = Источник.ЗначенияРеквизитовИзСервиса.Скопировать();
	СтрокаДереваВторогоУровня.ЦеныТовара                  = ОбщегоНазначения.СкопироватьРекурсивно(Источник.ЦеныТовара);
	
	СтрокаДереваПервогоУровня = Источник.Родитель;
	СтрокаДереваПервогоУровня.Строки.Удалить(Источник);
	
КонецПроцедуры

// Формирует наименования групп первого уровня дерева товаров.
//
// Параметры:
//   ДеревоТоваров                  - ДеревоЗначений - структура дерева описана в реквизите "ИмпортируемыеТовары", 
//   СортироватьСтрокиДереваТоваров - Булево - Истина, если после загрузки требуется отсортировать строки второго уровня дерева товаров.
//   ОтображатьСопоставленные       - Число - установленный фильтр по сопоставленным позициям дерева товаров.
//   ОтображатьПоСтатусу            - Число - установленный фильтр по статусу позиций дерева товаров.
//
Процедура ИмпортИзЯндексМаркетОпределитьНаименованияГруппДереваТоваров(ДеревоТоваров, Знач СортироватьСтрокиДереваТоваров = Истина, 
            Знач ОтображатьСопоставленные = 0, Знач ОтображатьПоСтатусу = -1)
    
    Индекс = 0;
    
    Пока Индекс < ДеревоТоваров.Строки.Количество() Цикл
        СтрокаДереваПервогоУровня = ДеревоТоваров.Строки[Индекс];
        
        Если СтрокаДереваПервогоУровня.Строки.Количество() = 0 Тогда
            ДеревоТоваров.Строки.Удалить(СтрокаДереваПервогоУровня);
            Продолжить;
        ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 0 Тогда
            ШаблонПредставления = НСтр("ru = 'Обновить (%1 из %2)'");
        ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 2 Тогда
            ШаблонПредставления = НСтр("ru = 'Загрузить (%1 из %2)'");
        ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 3 Тогда
            ШаблонПредставления = НСтр("ru = 'Не найдены (%2)'");
        КонецЕсли;
        
        КоличествоСтрок           = СтрокаДереваПервогоУровня.Строки.Количество();
        КоличествоОтмеченныхСтрок = 0;
        
        Для каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
            Если ОтображатьСопоставленные = 1
                 И (НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
                    ИЛИ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
                        И НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика)
                    ИЛИ НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория)) Тогда
                КоличествоСтрок = КоличествоСтрок - 1;
                Продолжить;
                
            ИначеЕсли ОтображатьСопоставленные = 2
                      И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
                      И (НЕ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
                         ИЛИ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика))
                      И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория) Тогда
                КоличествоСтрок = КоличествоСтрок - 1;
                Продолжить;
            КонецЕсли;
            
            Если ОтображатьПоСтатусу <> -1
                 И СтрокаДереваВторогоУровня.ИндексСтатуса <> ОтображатьПоСтатусу Тогда
                КоличествоСтрок = КоличествоСтрок - 1;
                Продолжить;
            КонецЕсли;
            
            КоличествоОтмеченныхСтрок = КоличествоОтмеченныхСтрок + ?(СтрокаДереваВторогоУровня.Пометка, 1, 0);
        КонецЦикла;
        
        Если КоличествоСтрок = 0 Тогда
            ШаблонПредставления = СтрЗаменить(ШаблонПредставления, " (%1 из %2)", "");
            ШаблонПредставления = СтрЗаменить(ШаблонПредставления, " (%2)", "");
        КонецЕсли;
        
        СтрокаДереваПервогоУровня.Пометка = (КоличествоОтмеченныхСтрок > 0);
        СтрокаДереваПервогоУровня.КоличествоОтмеченныхСтрок = КоличествоОтмеченныхСтрок;
        СтрокаДереваПервогоУровня.ПредставлениеОбъектаМаркетплейса = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
            ШаблонПредставления,
            КоличествоОтмеченныхСтрок,
            КоличествоСтрок);
        
		Если СортироватьСтрокиДереваТоваров Тогда
		    СтрокаДереваПервогоУровня.Строки.Сортировать("ПредставлениеОбъектаМаркетплейса");
		КонецЕсли;
        
        Индекс = Индекс + 1;
    КонецЦикла;
        
КонецПроцедуры

Функция ОписаниеТипаКатегории1С()

	МассивТиповКатегории1С = Новый Массив;
	МассивТиповКатегории1С.Добавить(Тип("СправочникСсылка.КатегорииНоменклатуры"));
	МассивТиповКатегории1С.Добавить(Тип("СправочникСсылка.Номенклатура"));
	ОписаниеТипаКатегории1С = Новый ОписаниеТипов(МассивТиповКатегории1С);

	Возврат ОписаниеТипаКатегории1С;

КонецФункции

// Формирует список настроек учетной записи. 
//
// Параметры:
//  УчетнаяЗаписьМаркетплейса - СправочникСсылка.УчетныеЗаписиМаркетплейсов - Учетная запись для выполнения операции.
//  ТолькоЗагружаемые         - Булево - Признак удаления выгружаемых видов цен из результата.
// 
// Возвращаемое значение:
//  Структура - Настройки учетной записи.
//
Функция НастройкиУчетнойЗаписиСЗагружаемымиВидамиЦен(УчетнаяЗаписьМаркетплейса, ТолькоЗагружаемые = Ложь) Экспорт

	НастройкиУчетнойЗаписи = Справочники.УчетныеЗаписиМаркетплейсов.НастройкиУчетнойЗаписи(УчетнаяЗаписьМаркетплейса);

	ВидыЦен = НастройкиУчетнойЗаписи.ВидыЦен;
	Если ТолькоЗагружаемые Тогда
		ВидыЦен.Очистить();
	КонецЕсли;
	ТипыЦен = ТипыЦенЯндексМаркет(Ложь);
	Для каждого ТипЦены Из ТипыЦен Цикл
		ВидыЦен.Вставить(ТипЦены.Идентификатор, ПредопределенноеЗначение("Справочник.ВидыЦен.ПустаяСсылка"));
	КонецЦикла;
    
    Возврат НастройкиУчетнойЗаписи;
КонецФункции

// Получает таблицу идентификаторов товаров, полученных из сервиса с учетом установленного отбора.
//
// Параметры:
// 	 УчетнаяЗаписьМаркетплейса     - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису.
//   ФильтрПоТоварам               - Структура - фильтр по товарам, см. НовыйФильтрЗапросаПоТоварам.
//   ТаблицаИдентификаторовТоваров - ТаблицаЗначений - таблица идентификаторов товаров, 
//                                     см. НоваяТаблицаИдентификаторовТоваров.
//   РазмерПорцииЗагрузкиДанных    - Число - размер порции загрузки данных с торговой площадки за один запрос;
//                                 - Неопределено - размер порции для загрузки не ограничен.
//   КэшИдентификаторов            - Соответствие - кэш идентификаторов (связь между идентификаторами публикации и идентификаторами).
//   Отказ                         - Булево - выходной параметр, определяет наличие ошибки при выполнении запросов к сервису.
//
// Возвращаемое значение:
//   ТаблицаЗначений - таблица идентификаторов товаров, см. НоваяТаблицаИдентификаторовТоваров.
//
Процедура ПолучитьИдентификаторыТоваровИзЯндексМаркет(УчетнаяЗаписьМаркетплейса, ФильтрПоТоварам, ТаблицаИдентификаторовТоваров, 
			РазмерПорцииЗагрузкиДанных = Неопределено, КэшИдентификаторов = Неопределено, Отказ = Ложь) Экспорт
	
	Если ТаблицаИдентификаторовТоваров = Неопределено Тогда
		ТаблицаИдентификаторовТоваров = НоваяТаблицаИдентификаторовТоваров();
	КонецЕсли;
	
	Если КэшИдентификаторов = Неопределено Тогда
		КэшИдентификаторов = Новый Соответствие;
	КонецЕсли;

	ФильтрыВидимости = ЗначениеВМассив(ФильтрПоТоварам.ФильтрВидимости);
	НовыхПозиций = 0;

	Для Каждого ЭлементФильтра Из ФильтрыВидимости Цикл
		Если ЗначениеЗаполнено(РазмерПорцииЗагрузкиДанных)
			 И НовыхПозиций >= РазмерПорцииЗагрузкиДанных Тогда
			Прервать;
		КонецЕсли;
		
		Если ФильтрПоТоварам.ИдентификаторыПубликации <> Неопределено
			 И НовыхПозиций >= ФильтрПоТоварам.ИдентификаторыПубликации.Количество() Тогда
			Прервать;
		ИначеЕсли ФильтрПоТоварам.ИдентификаторыОбъектовМаркетплейса <> Неопределено
			 И НовыхПозиций >= ФильтрПоТоварам.ИдентификаторыОбъектовМаркетплейса.Количество() Тогда
			Прервать;
		КонецЕсли;
		
		ПараметрыЗапроса = НовыеПараметрыЗапросаПолученияСпискаТоваров(ФильтрПоТоварам);
		ПараметрыЗапроса.ФильтрВидимости = ЭлементФильтра;
		Если ЗначениеЗаполнено(РазмерПорцииЗагрузкиДанных) Тогда
			ПараметрыЗапроса.Вставить("МаксимальноеКоличествоВОтвете", Мин(ПараметрыЗапроса.МаксимальноеКоличествоВОтвете, РазмерПорцииЗагрузкиДанных));
		КонецЕсли;
		
		НайденныеСтроки = ТаблицаИдентификаторовТоваров.НайтиСтроки(Новый Структура("ФильтрВидимости", ЭлементФильтра));
		Если НайденныеСтроки.Количество() > 0 Тогда
			ИдентификаторПоследнегоЗначения = НайденныеСтроки[НайденныеСтроки.Количество() - 1].ИдентификаторПоследнегоЗначения;
			Если ЗначениеЗаполнено( ИдентификаторПоследнегоЗначения ) Тогда
				ПараметрыЗапроса.Вставить("ИдентификаторПоследнегоЗначения", ИдентификаторПоследнегоЗначения );
			Иначе
				Продолжить;
			КонецЕсли;
		КонецЕсли;

		Пока Истина Цикл
			Если ЗначениеЗаполнено(РазмерПорцииЗагрузкиДанных)
				 И НовыхПозиций >= РазмерПорцииЗагрузкиДанных Тогда
				Прервать;
			КонецЕсли;
			
			Если ФильтрПоТоварам.ИдентификаторыПубликации <> Неопределено
				 И НовыхПозиций >= ФильтрПоТоварам.ИдентификаторыПубликации.Количество() Тогда
				Прервать;
			ИначеЕсли ФильтрПоТоварам.ИдентификаторыОбъектовМаркетплейса <> Неопределено
				 И НовыхПозиций >= ФильтрПоТоварам.ИдентификаторыОбъектовМаркетплейса.Количество() Тогда
				Прервать;
			КонецЕсли;
			
			ОтветСервиса = ПолучитьИдентификаторыТоваровИзЯндексМаркетИзСервиса(УчетнаяЗаписьМаркетплейса, ПараметрыЗапроса);
			
			Если ОтветСервиса = Ложь Или ОтветСервиса.Результат = Неопределено Тогда
				Отказ = Истина;
				Прервать;
			КонецЕсли;
			
			ПараметрыЗапроса.ИдентификаторПоследнегоЗначения = ОтветСервиса.ИдентификаторПоследнегоЗначения;
			КоличествоТоваров = ОтветСервиса.Результат.Количество();

			Для Каждого ЭлементОтветаСервиса Из ОтветСервиса.Результат Цикл
			    
				ЭлементОтветаСервисаПредложение = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервиса, "offer" );
				ЭлементОтветаСервисаMapping = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервиса, "mapping" );


			    ПредставлениеОбъектаМаркетплейса = СокрЛП( ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "name" ) );
			    Если ПустаяСтрока( ПредставлениеОбъектаМаркетплейса ) Тогда
			    	Продолжить;
			    КонецЕсли;

				ТаблицаИдентификаторовТоваровСтрока = ТаблицаИдентификаторовТоваров.Добавить();
				
				ТаблицаИдентификаторовТоваровСтрока.Изображения = НоваяТаблицаИзображений();
				ТаблицаИдентификаторовТоваровСтрока.Видео = НоваяТаблицаВидео();
				
				ТаблицаИдентификаторовТоваровСтрока.УчетнаяЗаписьМаркетплейса = УчетнаяЗаписьМаркетплейса;
				ТаблицаИдентификаторовТоваровСтрока.ИдентификаторПубликации = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "offerId" );
				ТаблицаИдентификаторовТоваровСтрока.ИдентификаторОбъектаМаркетплейса = ТаблицаИдентификаторовТоваровСтрока.ИдентификаторПубликации;
				ТаблицаИдентификаторовТоваровСтрока.Архивный = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "archived" );
				ТаблицаИдентификаторовТоваровСтрока.ФильтрВидимости = ЭлементФильтра;
				ТаблицаИдентификаторовТоваровСтрока.СтатусКарточкиТовара = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "cardStatus" );
				ТаблицаИдентификаторовТоваровСтрока.ИдентификаторПоследнегоЗначения = ПараметрыЗапроса.ИдентификаторПоследнегоЗначения;
				
				ТаблицаИдентификаторовТоваровСтрока.ПредставлениеОбъектаМаркетплейса = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "name" );
				ТаблицаИдентификаторовТоваровСтрока.НаименованиеКатегорииМаркетплейса = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаMapping, "marketCategoryName" );
				ТаблицаИдентификаторовТоваровСтрока.ИдентификаторКатегорииМаркетплейса = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаMapping, "marketCategoryId" );
				ТаблицаИдентификаторовТоваровСтрока.ИдентификаторСопоставленнойКарточкиНаМаркете = Формат( ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаMapping, "marketSku" ), "ЧГ=0" );
				ТаблицаИдентификаторовТоваровСтрока.НаименованиеСопоставленнойКарточкиНаМаркете = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаMapping, "marketModelName" );

				
				ИзображенияСервиса = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "pictures" );
				Если ТипЗнч( ИзображенияСервиса ) = Тип( "Массив" ) Тогда
					Для Каждого ИзображениеСервиса Из ИзображенияСервиса Цикл
						ИзображенияСтрока = ТаблицаИдентификаторовТоваровСтрока.Изображения.Добавить();
						ИзображенияСтрока.ПутьКФайлу = ИзображениеСервиса;
					КонецЦикла;
				КонецЕсли;
				
				ТаблицаИдентификаторовТоваровСтрока.Производитель = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "vendor", "" );
				ТаблицаИдентификаторовТоваровСтрока.АртикулТовараПроизводителя = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "vendorCode", "" );
				ТаблицаИдентификаторовТоваровСтрока.Штрихкоды = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "barcodes", Новый Массив );
				ТаблицаИдентификаторовТоваровСтрока.ПодробноеОписание = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "description", "" );
				
				СтраныПроизводства = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "manufacturerCountries", Новый Массив );
				Если СтраныПроизводства.Количество() > 0 Тогда
					ТаблицаИдентификаторовТоваровСтрока.СтранаПроизводства = СтраныПроизводства[ 0 ]; 
				КонецЕсли;

				ВесРазмер = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "weightDimensions" );
				Если ТипЗнч( ВесРазмер ) = Тип( "Соответствие" ) Тогда
					ТаблицаИдентификаторовТоваровСтрока.ДлинаСм = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ВесРазмер, "length", 0 );
					ТаблицаИдентификаторовТоваровСтрока.ШиринаСм = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ВесРазмер, "width", 0 );
					ТаблицаИдентификаторовТоваровСтрока.ВысотаСм = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ВесРазмер, "height", 0 );
					ТаблицаИдентификаторовТоваровСтрока.ВесБруттоКг = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ВесРазмер, "weight", 0 );
				КонецЕсли;
				
				ТаблицаИдентификаторовТоваровСтрока.Метки = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "tags", Новый Массив );
				ТаблицаИдентификаторовТоваровСтрока.ТНВЭДКод = ОбменСВнешнимиСистемамиЛогирование.ПолучитьЗначениеПоля( ЭлементОтветаСервисаПредложение, "customsCommodityCode" );
				
				Если ЗначениеЗаполнено(ТаблицаИдентификаторовТоваровСтрока.ИдентификаторПубликации) Тогда
					КэшИдентификаторов.Вставить(ТаблицаИдентификаторовТоваровСтрока.ИдентификаторПубликации, ТаблицаИдентификаторовТоваровСтрока.ИдентификаторОбъектаМаркетплейса);
				КонецЕсли;
				
				НовыхПозиций = НовыхПозиций + 1;
			КонецЦикла;
			
			Если ОтветСервиса.Результат.Количество() < ПараметрыЗапроса.МаксимальноеКоличествоВОтвете Тогда
				Прервать;
			КонецЕсли;
			
			НайденныеСтроки = ТаблицаИдентификаторовТоваров.НайтиСтроки(Новый Структура("ФильтрВидимости", ЭлементФильтра));
			Если НайденныеСтроки.Количество() >= КоличествоТоваров Тогда
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если Отказ Тогда
			Прервать;
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры

// Конструктор списка параметров для запроса получения идентификаторов товаров.
// 
// Параметры:
//   ФильтрПоТоварам - Структура - фильтр по товарам, см. НовыйФильтрЗапросаПоТоварам();
//                   - Неопределено - использовать фильтр по умолчанию.
//
// Возвращаемое значение:
//   Структура - список параметров:
//     * ФильтрВидимости                    - Строка - фильтр по видимости товара (filter.visibility): 
//     * ИдентификаторыПубликации           - Массив Из Строка - список внутренних идентификаторов товаров в системе продавца (filter.offer_id);
//     * ИдентификаторыОбъектовМаркетплейса - Массив Из Строка - список идентификаторов товаров на маркетплейсе (filter.product_id).
//     * ИдентификаторПоследнегоЗначения    - Строка - идентификатор последнего значения на странице (last_id);
//     * МаксимальноеКоличествоВОтвете      - Число - количество значений на странице (limit).
//
Функция НовыеПараметрыЗапросаПолученияСпискаТоваров(ФильтрПоТоварам = Неопределено)
	
	Если ФильтрПоТоварам = Неопределено Тогда
		ФильтрПоТоварам = НовыйФильтрЗапросаПоТоварам();
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить( "ИдентификаторПоследнегоЗначения", "" );
	Результат.Вставить( "МаксимальноеКоличествоВОтвете", 200 );
	Результат.Вставить( "МаксимальноеКоличествоВЗапросе", 200 );
	
	Для каждого КлючЗначение Из ФильтрПоТоварам Цикл
		Результат.Вставить(КлючЗначение.Ключ, КлючЗначение.Значение);
	КонецЦикла;

	Возврат Результат;

КонецФункции

// Получает список идентификаторов товаров из сервиса.
//
// Параметры:
//   УчетнаяЗаписьМаркетплейса - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису.
//   Параметры                 - Структура - список параметров запроса, 
//                                 см. НовыеПараметрыЗапросаПолученияСпискаТоваров.
// 
// Возвращаемое значение:
//   Структура    - результат выполнения запроса:
//     * Результат                       - Массив Из Соответствие - список идентификаторов товаров (productv2GetProductListResponseItem):
//       ** offer_id                       - Строка - идентификатор товара в системе продавца (артикул);
//       ** product_id                     - Число - идентификатор товара.
//       ** is_discounted                  - Булево - товар продается со скидкой;
//       ** archived                       - Булево - признак архивного товара;
//       ** is_fbo_visible                 - Булево - продается по схеме FBО;
//       ** is_fbs_visible                 - Булево - продается по схеме FBS;
//     * ИдентификаторПоследнегоЗначения - Строка - идентификатор последнего значения на странице (last_id);
//     * КоличествоТоваров               - Число - количество товаров в списке (total).
//   Неопределено - при выполнении запроса к внешнему ресурсу возникли ошибки.
//
Функция ПолучитьИдентификаторыТоваровИзЯндексМаркетИзСервиса(УчетнаяЗаписьМаркетплейса, Параметры)

	Склады = СкладыСоответствияПолучить( УчетнаяЗаписьМаркетплейса );
	Если Склады.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;
	СкладыСтрока = Склады[0];
    
    // Документация по методу: https://yandex.ru/dev/market/partner-api/doc/ru/reference/business-assortment/getOfferMappings
	// Фильтры:
    //	offerIds - Ваш SKU — идентификатор товара в вашей системе.
    //	cardStatuses - Фильтр по статусам карточек, массив.
   	//	categoryIds - Фильтр по категориям на Маркете.
   	//	vendorNames - Фильтр по брендам.
   	//	tags - Фильтр по тегам.
   	//	archived - Фильтр по нахождению в архиве. 

	ТекстЗапросаШаблон = "/businesses/%1/offer-mappings";
	business_id = СокрЛП( СкладыСтрока.ИдентификаторКабинетаМаркетплейса );

	ТекстЗапроса = СтрШаблон( ТекстЗапросаШаблон, business_id );
	
	// дополнительные параметры в URL
	АдресСервисаДополнительныеПараметры = Новый Массив;
	
	Если ЗначениеЗаполнено( Параметры.ИдентификаторПоследнегоЗначения ) Тогда
		АдресСервисаДополнительныеПараметры.Добавить( "page_token=" + Параметры.ИдентификаторПоследнегоЗначения );
	КонецЕсли;

	Если ЗначениеЗаполнено( Параметры.МаксимальноеКоличествоВОтвете ) Тогда
		АдресСервисаДополнительныеПараметры.Добавить( "limit=" + Формат( Параметры.МаксимальноеКоличествоВОтвете, "ЧГ=0" ) );
	КонецЕсли;
	
	Если АдресСервисаДополнительныеПараметры.Количество() > 0 Тогда
		ТекстЗапроса = ТекстЗапроса + "?" + СтрСоединить( АдресСервисаДополнительныеПараметры, "&" ); 
	КонецЕсли;
	
	
	ФильтрПоТоварам = Новый Структура;
	ФильтрПоТоварам.Вставить("visibility", Параметры.ФильтрВидимости);

	Если Параметры.ИдентификаторыПубликации <> Неопределено Тогда
		ФильтрПоТоварам.Вставить("offer_id", ЗначениеВМассив(Параметры.ИдентификаторыПубликации));
	КонецЕсли;

	Если Параметры.ИдентификаторыОбъектовМаркетплейса <> Неопределено Тогда
		ФильтрПоТоварам.Вставить("product_id", ЗначениеВМассив(Параметры.ИдентификаторыОбъектовМаркетплейса));
	КонецЕсли;
	
   	
	ПараметрыЗапроса = Новый Структура;

	ПустойРезультат = Новый Структура;
	ПустойРезультат.Вставить("items",   Новый Массив);
	ПустойРезультат.Вставить("last_id", "");
	ПустойРезультат.Вставить("total",   0);
	
	СлужебныеПараметры 								= СлужебныеПараметрыHTTPЗапроса();
	СлужебныеПараметры.ПодстрокаПоискаВТекстеОтвета = "item not found";
	СлужебныеПараметры.ПустойРезультат              = Новый Структура("result", ПустойРезультат);
	ПараметрыЗапроса.Вставить("СлужебныеПараметры", СлужебныеПараметры);

	АтрибутыОтвета = Новый Структура;
	АтрибутыОтвета.Вставить("Результат", "result.offerMappings");
	АтрибутыОтвета.Вставить("ИдентификаторПоследнегоЗначения", "result.paging.nextPageToken");

	// POST-запрос
	РезультатЗапроса = ПолучитьРезультатЗапроса(ТекстЗапроса, ПараметрыЗапроса, УчетнаяЗаписьМаркетплейса, АтрибутыОтвета, Истина);

	Возврат РезультатЗапроса;

КонецФункции

Функция ЧислоВСтроку(Знач Значение, ВыводитьПредставлениеНуля = Ложь)

	Если Значение = Неопределено Тогда
		Значение = 0;
	КонецЕсли;

	ФорматнаяСтрока = ?(ВыводитьПредставлениеНуля, "ЧН=; ЧРД=.; ЧГ=", "ЧРД=.; ЧГ=");
	Возврат Формат(Значение, ФорматнаяСтрока);  

КонецФункции

// Возвращает служебные параметры HTTP-запроса.
// 
// Возвращаемое значение:
//  Структура - Структура, содержащая реквизиты:
//   ПодстрокаПоискаВТекстеОтвета
//   ПустойРезультат
//   ПоляТипаДата - Строка или Массив. Подробности см. в описании функции ПрочитатьJSON.
//                  Для приведения значения реквизитов, содержащих дату, к типу Дата, следует указывать их наименования
//                  в этом реквизите.
Функция СлужебныеПараметрыHTTPЗапроса()

	СлужебныеПараметры = Новый Структура;
	СлужебныеПараметры.Вставить("ПодстрокаПоискаВТекстеОтвета", "");
	СлужебныеПараметры.Вставить("ПустойРезультат", Неопределено);
	СлужебныеПараметры.Вставить("ПоляТипаДата", "");
	СлужебныеПараметры.Вставить("ВернутьКодСостояния", Ложь);
	СлужебныеПараметры.Вставить("ВернутьСтрокуОтвета", Ложь);
	СлужебныеПараметры.Вставить("ЭтоДвоичныеДанные", Ложь);
	СлужебныеПараметры.Вставить("ContentType", "application/json");
	СлужебныеПараметры.Вставить("ContentLenght", "");

	Возврат СлужебныеПараметры;

КонецФункции

// Возвращает результат запроса.
//
// Параметры:
//  ТекстЗапроса       - Строка - метод API.
//  ПараметрыЗапроса   - Структура - параметры запроса согласно описанию запрашиваемого метода API.
//  УчетнаяЗапись      - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису.
//  АтрибутыРезультата - Структура - каждому ключу передается в качестве значения имя атрибута из ответа метода API, 
//                        в котором содержится необходимая информация. По ключам этого параметра заполняется
//                        возвращаемое значение.
// 
// Возвращаемое значение:
//  Структура - содержимое атрибутов результата на основании ключей параметра АтрибутыРезультата.
//
Функция ПолучитьРезультатЗапроса(ТекстЗапроса, ПараметрыЗапроса, УчетнаяЗаписьМаркетплейса,
			Знач АтрибутыРезультата = Неопределено, POSTЗапрос = Истина
			, ПараметрыКонтекст = Неопределено)

	Результат = Новый Структура;

	РезультатЗапроса = ВыполнитьЗапросКСервису(ТекстЗапроса, ПараметрыЗапроса, УчетнаяЗаписьМаркетплейса, POSTЗапрос, ПараметрыКонтекст);

	Если ЗначениеЗаполнено(АтрибутыРезультата) Тогда
		Для Каждого АтрибутРезультата Из АтрибутыРезультата Цикл
			Если РезультатЗапроса <> Неопределено Тогда
				ЗначениеАтрибута = РезультатЗапроса;
				КлючиРезультата = СтрРазделить(АтрибутРезультата.Значение, ".");

				Для Каждого КлючРезультата Из КлючиРезультата Цикл
					ЗначениеАтрибута = ЗначениеАтрибута[КлючРезультата];
				КонецЦикла;
			Иначе
				ЗначениеАтрибута = Неопределено;
			КонецЕсли;

			Результат.Вставить(АтрибутРезультата.Ключ, ЗначениеАтрибута);
		КонецЦикла;
	Иначе
		Результат = РезультатЗапроса;
	КонецЕсли;

	Возврат Результат;

КонецФункции

Функция ВыполнитьЗапросКСервису(ТекстЗапроса, ЗНАЧ ПараметрыЗапроса, УчетнаяЗаписьМаркетплейса, POSTЗапрос = Истина, 
	ПараметрыКонтекст = Неопределено)

	Результат = Неопределено;

	АдресСервера = СерверПартнерскогоAPIЯндексМаркет();
	ЗащищенноеСоединение = ОбщегоНазначенияКлиентСервер.НовоеЗащищенноеСоединение();
	ИнтернетПрокси = ПолучениеФайловИзИнтернета.ПолучитьПрокси("HTTPS");
	HTTPСоединение = Новый HTTPСоединение(АдресСервера, 443,,, ИнтернетПрокси, 60, ЗащищенноеСоединение);

	СлужебныеПараметры = СлужебныеПараметрыHTTPЗапроса();
	Если ПараметрыЗапроса.Свойство("СлужебныеПараметры") Тогда
		ЗаполнитьЗначенияСвойств(СлужебныеПараметры, ПараметрыЗапроса.СлужебныеПараметры);
		ПараметрыЗапроса.Удалить("СлужебныеПараметры");
	КонецЕсли;

	ПрочитатьВСоответствие = Истина;
	Если НЕ СлужебныеПараметры.Свойство("ПрочитатьВСоответствие", ПрочитатьВСоответствие) Тогда
		ПрочитатьВСоответствие = Истина;
	КонецЕсли;

	HTTPЗапрос = HTTPЗапросКСервису(УчетнаяЗаписьМаркетплейса, ТекстЗапроса, СлужебныеПараметры);

	ТоварСсылка = Неопределено;
	ТелоЗапроса = "";

	Если POSTЗапрос И ЗначениеЗаполнено(ПараметрыЗапроса) Тогда
		Если СлужебныеПараметры.ЭтоДвоичныеДанные Тогда
			HTTPЗапрос.УстановитьТелоИзДвоичныхДанных(ПараметрыЗапроса.ДвоичныеДанные);
		Иначе
			ТелоЗапроса = ИнтеграцияСЯндексМаркетСервер.ВJSON( ПараметрыЗапроса );
			HTTPЗапрос.УстановитьТелоИзСтроки(ТелоЗапроса, "UTF8", ИспользованиеByteOrderMark.НеИспользовать);
			
		КонецЕсли;
	КонецЕсли;
	
	Попытка
		ПараметрыКонтекст_ДанныеПубликации = ПараметрыКонтекст.ДанныеПубликации;
	Исключение
		ПараметрыКонтекст_ДанныеПубликации = Неопределено;
	КонецПопытки;
	
	ТоварыКоличество = 0;
	Если ТипЗнч( ПараметрыКонтекст_ДанныеПубликации ) = Тип( "ТаблицаЗначений" ) Тогда
		ТоварыКоличество = ПараметрыКонтекст_ДанныеПубликации.Количество();
		Если ТоварыКоличество = 1 Тогда
			ТоварСсылка = ПараметрыКонтекст_ДанныеПубликации[0].Номенклатура;
		КонецЕсли;
	КонецЕсли;

	Попытка
		ПараметрыКонтекст_Действие = ПараметрыКонтекст.Действие;
	Исключение
		ПараметрыКонтекст_Действие = Неопределено;
	КонецПопытки;
	
	Если ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗаписьМаркетплейса ) Тогда
		СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
		
		HTTPМетод = ?(POSTЗапрос, "POST", "GET" );
		HTTPСоединениеПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPСоединение );
		HTTPЗапросПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPЗапрос );
		ЛогКомментарий = НСтр("ru = 'HTTP: Запрос: Метод:'") + " " + HTTPМетод + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
		+ HTTPЗапросПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ HTTPСоединениеПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
		+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
		ЛогПараметры = Новый Структура;
		ЛогПараметры.Вставить( "Действие", ПараметрыКонтекст_Действие );
		ЛогПараметры.Вставить( "Сервер", HTTPСоединение.Сервер );
		ЛогПараметры.Вставить( "АдресРесурса", HTTPЗапрос.АдресРесурса );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗаписьМаркетплейса, , ЛогПараметры );
	КонецЕсли;	
	
	Попытка
		Если POSTЗапрос Тогда
			РезультатЗапроса = HTTPСоединение.ОтправитьДляОбработки(HTTPЗапрос);
		Иначе
			РезультатЗапроса = HTTPСоединение.Получить(HTTPЗапрос);
		КонецЕсли;

		СтрокаОтвета = РезультатЗапроса.ПолучитьТелоКакСтроку("UTF8");
		КодСостояния = РезультатЗапроса.КодСостояния;
		
		ЛогЭтоОшибка = (КодСостояния <> 200);
		Если ЛогЭтоОшибка = Истина Или ОбменСВнешнимиСистемамиЛогированиеПовтИсп.ЛогированиеВключено( УчетнаяЗаписьМаркетплейса ) Тогда
			СтекВызоваСтр = ОбменСВнешнимиСистемамиЛогирование.СтекВызоваПолучить();
			
			HTTPСоединениеПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( HTTPСоединение );
			HTTPОтветПредставление = ОбменСВнешнимиСистемамиЛогирование.ОбъектПредставление( РезультатЗапроса );
			ЛогКомментарий = НСтр("ru = 'HTTP: Ответ: Метод:'") + " " + HTTPМетод + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков() 
			+ HTTPОтветПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ HTTPСоединениеПредставление + ОбменСВнешнимиСистемамиЛогирование.КомментарийРазделительБлоков()
			+ НСтр("ru = 'Стек вызовов:'") + Символы.ПС + СтекВызоваСтр;
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗаписьМаркетплейса, ЛогЭтоОшибка, ЛогПараметры );
		КонецЕсли;	

		ВернутьКодСостояния = Ложь;
		Если СлужебныеПараметры.Свойство("ВернутьКодСостояния", ВернутьКодСостояния) И ВернутьКодСостояния = Истина Тогда
			Возврат КодСостояния;
		КонецЕсли;
		
		Если СлужебныеПараметры.ВернутьСтрокуОтвета Тогда
			Возврат JSONВДанные(СтрокаОтвета, ПрочитатьВСоответствие, СлужебныеПараметры.ПоляТипаДата);
		КонецЕсли;

		Если КодСостояния = 400 ИЛИ КодСостояния = 404
			И Не ПустаяСтрока(СлужебныеПараметры.ПодстрокаПоискаВТекстеОтвета)
			И СтрНайти(СтрокаОтвета, СлужебныеПараметры.ПодстрокаПоискаВТекстеОтвета) Тогда
			// При выполнении запроса получен ответ, в котором содержится заданная подстрока поиска.
			// Поэтому такой ответ не считается ошибкой - возвращается заданный пустой результат.
			Результат = СлужебныеПараметры.ПустойРезультат;
		ИначеЕсли КодСостояния <> 200 Тогда
			ТекстОшибки = СтрШаблон(НСтр("ru = 'Ошибка выполнения запроса %1: %2'"), ТекстЗапроса, СтрокаОтвета);
			
			ЗаписьЖурналаРегистрации(СобытиеЖурналаРегистрации(), УровеньЖурналаРегистрации.Ошибка,,, ТекстОшибки);
			
			ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
			
			ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, УчетнаяЗаписьМаркетплейса, Истина, ЛогПараметры );
			
		Иначе
			Результат = JSONВДанные(СтрокаОтвета, ПрочитатьВСоответствие, СлужебныеПараметры.ПоляТипаДата);
		КонецЕсли;
	Исключение
		ТекстОшибки = СтрШаблон(НСтр("ru = 'Отсутствует соединение с сервером %1 по причине: %2'"),
			АдресСервера,
			ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			
		ЗаписьЖурналаРегистрации(СобытиеЖурналаРегистрации(), УровеньЖурналаРегистрации.Ошибка,,, ТекстОшибки);
		
		ОбщегоНазначения.СообщитьПользователю(ТекстОшибки);
		
		ЛогКомментарий = ПодробноеПредставлениеОшибки( ИнформацияОбОшибке() );
		ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ЛогКомментарий, УчетнаяЗаписьМаркетплейса, Истина, ЛогПараметры );
	КонецПопытки;

	Возврат Результат;

КонецФункции

Функция HTTPЗапросКСервису(УчетнаяЗаписьМаркетплейса, ТекстЗапроса, СлужебныеПараметры)

	Заголовки = Новый Соответствие;
	Заголовки.Вставить("Charset",      "UTF-8");
	Заголовки.Вставить("Accept",       "application/json");
	Заголовки.Вставить("Content-Type", СлужебныеПараметры.ContentType);
	
	Если СлужебныеПараметры.ЭтоДвоичныеДанные Тогда
		Заголовки.Вставить("Content-Lenght", СлужебныеПараметры.ContentLenght);
	КонецЕсли;

	ЯндексПриложениеДанные = ЯндексМаркетПриложениеДанныеПолучить();
	client_id = ЯндексПриложениеДанные.client_id;

	УстановитьПривилегированныйРежим(Истина);
	ДанныеАвторизации = ИнтеграцияСЯндексМаркетСервер.ЯндексМаркетДанныеАвторизацииПолучить( УчетнаяЗаписьМаркетплейса );
	УстановитьПривилегированныйРежим(Ложь);

	HTTPЗаголовкиТокенАвторизацииДобавить( УчетнаяЗаписьМаркетплейса, Заголовки, ДанныеАвторизации );

	Возврат Новый HTTPЗапрос( ТекстЗапроса, Заголовки );

КонецФункции

// Преобразует строку, содержащую текст JSON, в структуру данных.
//
// Параметры:
//   СтрокаJSON                   - Строка - строка, содержащая текст в формате JSON.
//   ПрочитатьВСоответствие       - Булево - если установлено Истина, чтение объекта JSON будет выполнено в Соответствие. Если установлено
//                                    Ложь, объекты будут считываться в объект типа Структура. Значение по умолчанию: Ложь.
//	 ИменаСвойствСоЗначениямиДата - Массив, Строка, ФиксированныйМассив - массив, элементы которого содержат имена свойств JSON, для 
//                                    которых нужно вызывать восстановление даты из строки.
//                                - Неопределено - восстановление дат не производится.
//
// Возвращаемое значение:
//   Структура, Соответствие - результат преобразования строки JSON.
//
Функция JSONВДанные(СтрокаJSON, ПрочитатьВСоответствие = Ложь, ИменаСвойствСоЗначениямиДата = Неопределено)

	ЧтениеJSON = Новый ЧтениеJSON;
	ЧтениеJSON.УстановитьСтроку(СтрокаJSON);
	Результат = ПрочитатьJSON(ЧтениеJSON, ПрочитатьВСоответствие, ИменаСвойствСоЗначениямиДата);
	ЧтениеJSON.Закрыть();

	Возврат Результат;

КонецФункции


// Конструктор таблицы значений для изображений товаров из сервиса.
//
// Возвращаемое значение:
//	 ТаблицаЗначений - таблица с колонками:
//     * Порядок             - Число - порядок расположения на сайте;
//     * ПутьКФайлу 		 - Строка - путь к изображению;
//     * ОсновноеИзображение - Булево - признак главного изображения;
//     * Изображение360      - Булево - признак изображения 360°.
//
Функция НоваяТаблицаИзображений() Экспорт

	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("Порядок", Новый ОписаниеТипов("Число",,, Новый КвалификаторыЧисла(2, 0, ДопустимыйЗнак.Неотрицательный)));
	Результат.Колонки.Добавить("ПутьКФайлу", Новый ОписаниеТипов("Строка"));
	Результат.Колонки.Добавить("ОсновноеИзображение", Новый ОписаниеТипов("Булево"));
	Результат.Колонки.Добавить("Изображение360", Новый ОписаниеТипов("Булево"));
	
	ТипФайлСсылка = Новый Массив;
	ТипФайлСсылка.Добавить( Тип( "СправочникСсылка.НоменклатураПрисоединенныеФайлы" ) );
	ТипФайлСсылка.Добавить( Тип( "СправочникСсылка.ХарактеристикиНоменклатурыПрисоединенныеФайлы" ) );
	Результат.Колонки.Добавить( "ФайлСсылка", Новый ОписаниеТипов( ТипФайлСсылка ) );

	Возврат Результат;

КонецФункции

// Конструктор таблицы значений для видео товаров из сервиса.
//
// Возвращаемое значение:
//	 ТаблицаЗначений - таблица с колонками:
//     * Порядок             - Число - порядок расположения на сайте;
//     * ПутьКФайлу 		 - Строка - путь к изображению;
//     * ОсновноеВидео - Булево - признак главного видео;
//
Функция НоваяТаблицаВидео() Экспорт

	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить( "Порядок", Новый ОписаниеТипов( "Число",,, Новый КвалификаторыЧисла(2, 0, ДопустимыйЗнак.Неотрицательный)) );
	Результат.Колонки.Добавить( "ПутьКФайлу", Новый ОписаниеТипов( "Строка") );
	Результат.Колонки.Добавить( "ОсновноеВидео", Новый ОписаниеТипов( "Булево") );
	
	ТипФайлСсылка = Новый Массив;
	ТипФайлСсылка.Добавить( Тип( "СправочникСсылка.НоменклатураПрисоединенныеФайлы" ) );
	ТипФайлСсылка.Добавить( Тип( "СправочникСсылка.ХарактеристикиНоменклатурыПрисоединенныеФайлы" ) );
	Результат.Колонки.Добавить( "ФайлСсылка", Новый ОписаниеТипов( ТипФайлСсылка ) );

	Возврат Результат;

КонецФункции

// Конструктор таблицы значений для информации о комиссиях.
//
// Возвращаемое значение:
//	 ТаблицаЗначений - таблица с колонками:
//	   * СхемаПродажи        - Строка - схема продажи;
//	   * ПроцентКомиссии     - Число - процент комиссии;
//	   * СуммаКомиссии       - Число - сумма комиссии;
//	   * МинимальнаяКомиссия - Число - минимальная сумма комиссии;
//	   * СтоимостьДоставки   - Число - стоимость доставки;
//	   * СтоимостьВозврата   - Число - стоимость возврата.
//
Функция НоваяТаблицаИнформацияОКомиссиях() Экспорт

	ОписаниеТипаСтрока10 = Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(10));
	ОписаниеТипаЧисло    = Новый ОписаниеТипов("Число");

	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("СхемаПродажи",        ОписаниеТипаСтрока10);
	Результат.Колонки.Добавить("ПроцентКомиссии",     ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("СуммаКомиссии",       ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("МинимальнаяКомиссия", ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("СтоимостьДоставки",   ОписаниеТипаЧисло);
	Результат.Колонки.Добавить("СтоимостьВозврата",   ОписаниеТипаЧисло);
	
	Возврат Результат;

КонецФункции

Функция ЗагрузитьИСоздатьВсеТоварыИзЯндексМаркет( Параметры ) Экспорт
	

	РезультатПолучениеИзМаркетплейса = ИнтеграцияСЯндексМаркетСервер.ИмпортИзЯндексМаркетПолучитьИмпортируемыеДанныеСТорговойПлощадки(
	Параметры.УчетнаяЗаписьМаркетплейса, Параметры, Параметры.ИмпортируемыеТовары );


	РезультатЗаписьВ1С = ИнтеграцияСЯндексМаркетСервер.ИмпортИзЯндексМаркетЗаписатьЗагруженныеДанныеСТорговойПлощадки(
	Параметры.УчетнаяЗаписьМаркетплейса, Параметры.ИмпортируемыеТовары, Параметры );

	
	Возврат РезультатЗаписьВ1С;
КонецФункции

// Записывает загруженные данные с торговой площадки в информационную базу.
//
// Параметры:
//   УчетнаяЗаписьМаркетплейса - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису;
//   ДеревоТоваров             - ДеревоЗначений - структура дерева описана в реквизите "ИмпортируемыеТовары", 
//   Параметры                 - Структура - параметры выполнения функции:
//     * ВариантЗагрузкиДанных          - Число - вариант загрузки данных из сервиса, см. реквизит "ВариантЗагрузкиДанных" в
//     * ЗагрузитьЦеныПродажи           - Булево - признак записи цен, см. реквизит "ЗагрузитьЦеныПродажи" в
//     * ЗаписатьТолькоЦены             - Булево - Истина, если необходимо записать только цены товаров.
//     * ТолькоСоздатьНоменклатуру      - Булево - Истина, если необходимо только создать элементы справочника "Номенклатура".
//     * ЗагрузитьИзображения           - Булево - Истина, если необходимо загрузить изображения товаров.
//     * ИмяИдентификатора              - Строка - имя ключевого поля идентификаторов. Может принимать значения:
//                                          "ИдентификаторОбъектаМаркетплейса" и "ИдентификаторПубликации". 
//     * РазмерПорцииЗагрузкиДанных     - Число - размер порции загрузки данных с торговой площадки за один запрос;
//                                      - Неопределено - размер порции для загрузки не ограничен.
//     * СоздатьЭлементыНоменклатуры    - Булево - Истина, если несопоставленные элементы справочника "Номенклатура" необходимо создать.
//     * СортироватьСтрокиДереваТоваров - Булево - Истина, если после загрузки требуется отсортировать строки второго уровня дерева товаров.
//     * ОтображатьСопоставленные       - Число - установленный фильтр по сопоставленным позициям дерева товаров.
//     * ОтображатьПоСтатусу            - Число - установленный фильтр по статусу позиций дерева товаров.
//
// Возвращаемое значение:
//   Структура - результат выполнения функции:
//     * Ошибка         - Структура - код и описание ошибки при выполнении функции, 
//                          см. ИнтеграцияСМаркетплейсамиСервер.НоваяОшибка.
//     * ДеревоТоваров  - ДеревоЗначений - заполненное дерево товаров, см. реквизит "ИмпортируемыеТовары" в 
//     * Записано       - Число - количество записанных данных;
//     * ВТомЧислеНовых - Число - количество новых записанных данных;
//     * НеЗаписано     - Число - количество данных с ошибками записи;
//     * Пропущено      - Массив Из Структура - информация о пропущенных записях:
//       ** НомерСтроки   - Число - номер строки дерева товаров;
//       ** Сообщение     - Строка - описание ошибки.
//     * ЗагруженыЦены  - Булево - в результате выполнения функции были загружены цены товаров.
//
Функция ИмпортИзЯндексМаркетЗаписатьЗагруженныеДанныеСТорговойПлощадки(Знач УчетнаяЗаписьМаркетплейса, ДеревоТоваров, Знач Параметры ) Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить( "Ошибка", ИнтеграцияСМаркетплейсамиСервер.НоваяОшибка());
    Результат.Вставить( "ДеревоТоваров", ДеревоТоваров);
    Результат.Вставить( "Записано", 0);
    Результат.Вставить( "ВТомЧислеНовых", 0);
    Результат.Вставить( "НеЗаписано", 0);
    Результат.Вставить( "Пропущено", Новый Массив);
	Результат.Вставить( "ЗагруженыЦены", Параметры.ЗагрузитьЦеныПродажи ИЛИ Параметры.ЗаписатьТолькоЦены);
	
	Результат.Вставить( "НоменклатураКоличествоСоздано", 0 );
	Результат.Вставить( "НоменклатураКоличествоОбновлено", 0 );
	Результат.Вставить( "ХарактеристикиКоличествоСоздано", 0 );
	Результат.Вставить( "ХарактеристикиКоличествоОбновлено", 0 );
	Результат.Вставить( "ФайлыКоличествоСоздано", 0 );
	Результат.Вставить( "ШтрихкодыКоличествоСоздано", 0 );
	Результат.Вставить( "КатегорииНоменклатурыСоздано", 0 );
	
	СопоставлениеКатегорий = КатегорииИзДеревоТоварыПолучить( ДеревоТоваров );

	// Поиск категорий по коду и наименованию
	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить( "СопоставлениеКатегорий", СопоставлениеКатегорий );
	ПараметрыФункции.Вставить( "ИсточникКатегории", Параметры.ИсточникКатегории );
	Категории1СПоНаименованиюКатегорииМаркетплейсаНайтиИЗаполнить( ПараметрыФункции );
	ПараметрыФункции = Неопределено;
	
	Если Параметры.СоздатьЭлементыНоменклатуры = Истина Тогда
		
		Если Параметры.СоздатьКатегорииНоменклатуры = Истина Тогда

			КатегорииСоздатьИерархию = Параметры.СоздатьИерархиюКатегорийНоменклатуры;
			
			// Создание категорий
			ПараметрыФункции = Категории1СПоКатегориямМаркетплейсаПараметрыНовый();
			ПараметрыФункции.СопоставлениеКатегорий = СопоставлениеКатегорий;
			ПараметрыФункции.ИсточникКатегории = Параметры.ИсточникКатегории;
			ПараметрыФункции.СоздатьИерархию = КатегорииСоздатьИерархию;
			ПараметрыФункции.УчетнаяЗапись = УчетнаяЗаписьМаркетплейса;
			ПараметрыФункции.КэшКатегорийДерево = Параметры.КэшКатегорийДерево;

			Категории1СПоКатегориямМаркетплейсаСоздать( ПараметрыФункции );

			Параметры.КатегорииНоменклатурыСоздано = ПараметрыФункции.КатегорииНоменклатурыСоздано;
			
			ПараметрыФункции = Неопределено;

			ДеревоТоваровКатегорииПоСопоставлениюЗаполнить( ДеревоТоваров, СопоставлениеКатегорий, Параметры );
			
		КонецЕсли;
		
		ИмпортируемыеТоварыНоменклатураКлючЗаполнить( ДеревоТоваров );
		
		Если Параметры.СоздатьИерархиюНоменклатуры = Истина Тогда
		
			ПараметрыФункции = Новый Структура;
			ПараметрыФункции.Вставить( "СопоставлениеКатегорий", СопоставлениеКатегорий );
			ПараметрыФункции.Вставить( "УчетнаяЗапись", УчетнаяЗаписьМаркетплейса );
			ПараметрыФункции.Вставить( "КэшКатегорийДерево", Параметры.КэшКатегорийДерево );
			ПараметрыФункции.Вставить( "ДеревоТоваров", ДеревоТоваров );
			
			ИерархияНоменклатурыПоИерархииКатегорийМаркетплейсаВДеревеТоваровЗаполнить( ПараметрыФункции );
			
			ПараметрыФункции = Неопределено;
		КонецЕсли;
		
		ТоварыСозданы = ТоварыСоздать( ДеревоТоваров, Параметры );
		
		Результат.НоменклатураКоличествоСоздано = Параметры.НоменклатураКоличествоСоздано;
		Результат.НоменклатураКоличествоОбновлено = Параметры.НоменклатураКоличествоОбновлено;
		Результат.ХарактеристикиКоличествоСоздано = Параметры.ХарактеристикиКоличествоСоздано;
		Результат.ХарактеристикиКоличествоОбновлено = Параметры.ХарактеристикиКоличествоОбновлено;
		Результат.ФайлыКоличествоСоздано = Параметры.ФайлыКоличествоСоздано;
		Результат.ШтрихкодыКоличествоСоздано = Параметры.ШтрихкодыКоличествоСоздано;
		Результат.КатегорииНоменклатурыСоздано = Параметры.КатегорииНоменклатурыСоздано;
		
	КонецЕсли;
	
	
	ДеревоТоваровКатегорияИлиНоменклатураДляСопоставленияКатегорииМаркетплейсаЗаполнить( ДеревоТоваров, Параметры );

	Если НЕ ЗначениеЗаполнено(УчетнаяЗаписьМаркетплейса) Тогда
		Результат.Ошибка.КодОшибки      = КодыОшибокРегистраСтатусовПубликацииТоваров().ОшибкаПрочие;
		Результат.Ошибка.ОписаниеОшибки = НСтр("ru = 'Не указана учетная запись торговой площадки для импорта данных по товарам.'");
		Возврат Результат;
	КонецЕсли; 
	
	ДоступнаУстановкаЦен = ПравоДоступа("Проведение", Метаданные.Документы.УстановкаЦенНоменклатуры);
	
	Если НЕ ДоступнаУстановкаЦен Тогда
		Результат.Вставить("ЗагруженыЦены", Ложь);
		
		Если Параметры.ЗаписатьТолькоЦены Тогда
			Результат.Ошибка.КодОшибки      = КодыОшибокРегистраСтатусовПубликацииТоваров().ОшибкаПрочие;
			Результат.Ошибка.ОписаниеОшибки = НСтр("ru = 'Для пользователя не доступна установка цен номенклатуры.'");
			Возврат Результат;
		КонецЕсли;
	КонецЕсли;
	
	Отказ = Ложь;
	
	ТипНалогообложенияНДС  = Перечисления.ТипыНалогообложенияНДС.ОблагаетсяНДС;
	НастройкиУчетнойЗаписи = Справочники.УчетныеЗаписиМаркетплейсов.НастройкиУчетнойЗаписи(УчетнаяЗаписьМаркетплейса);
	ВидыЦен                = ВидыЦенУчетнойЗаписи(УчетнаяЗаписьМаркетплейса, Истина, Истина);
	ИсточникКатегории      = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(УчетнаяЗаписьМаркетплейса, "ИсточникКатегории");
	
	Попытка
		ТаблицаТоваров    = СведенияОВыгруженныхДанных();
		Идентификаторы    = Новый Массив;
		ИмяИдентификатора = Параметры.ИмяИдентификатора;
		
		МассивПолейТаблицыТоваров = Новый Массив;
		Для Каждого Колонка Из ТаблицаТоваров.Колонки Цикл
			МассивПолейТаблицыТоваров.Добавить(Колонка.Имя);
		КонецЦикла;
		ПоляТаблицыТоваров = СтрСоединить(МассивПолейТаблицыТоваров, ", ");
		
		ВсегоОтмечено       = 0;
		ВсегоНеПолучено     = 0;
		ВсегоНеСопоставлено = 0;
		
		Для каждого СтрокаДереваПервогоУровня Из ДеревоТоваров.Строки Цикл
			Если СтрокаДереваПервогоУровня.ВариантЗагрузки = ВариантЗагрузкиОбновитьСопоставленныеКарточкиТоваров() Тогда
				// СтрокаДереваГруппаОбновить 
			ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
				// СтрокаДереваГруппаЗагрузить 
			Иначе
				Продолжить;
			КонецЕсли;	
				
			Индекс = 0;
			Пока Индекс < СтрокаДереваПервогоУровня.Строки.Количество() Цикл
				СтрокаДереваВторогоУровня = СтрокаДереваПервогоУровня.Строки[Индекс];
				
				// Дополнительные фильтры
				Если Параметры.ОтображатьСопоставленные = 1
					 И (НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
					 	ИЛИ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
					        И НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика)
					 	ИЛИ НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория)) Тогда
					Индекс = Индекс + 1;
					Продолжить;
					
				ИначеЕсли Параметры.ОтображатьСопоставленные = 2
					 	  И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
					 	  И (НЕ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
					         ИЛИ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика))
					 	  И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория) Тогда
					Индекс = Индекс + 1;
					Продолжить;
				КонецЕсли;
				
				Если Параметры.ОтображатьПоСтатусу <> -1
					 И СтрокаДереваВторогоУровня.ИндексСтатуса <> Параметры.ОтображатьПоСтатусу Тогда
					Индекс = Индекс + 1;
					Продолжить;
				КонецЕсли;

				// Отмеченные позиции
				Если СтрокаДереваВторогоУровня.Пометка Тогда
					ВсегоОтмечено = ВсегоОтмечено + 1;
					Если СтрокаДереваВторогоУровня.ИндексСтатуса = 5 Тогда
						ВсегоНеПолучено = ВсегоНеПолучено + 1;
					КонецЕсли;
					Если НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) И НЕ Параметры.СоздатьЭлементыНоменклатуры Тогда
						ВсегоНеСопоставлено = ВсегоНеСопоставлено + 1;
					КонецЕсли;
				КонецЕсли;				
				
				Если НЕ СтрокаДереваВторогоУровня.Пометка 
					 ИЛИ НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.ИдентификаторПубликации)
					 
					 ИЛИ (СтрокаДереваВторогоУровня.ВариантЗагрузки = ВариантЗагрузкиОбновитьСопоставленныеКарточкиТоваров()
					 	И Параметры.ВариантЗагрузкиДанных = ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров())
					 	
					 ИЛИ (СтрокаДереваВторогоУровня.ВариантЗагрузки = ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() 
					 И Параметры.ВариантЗагрузкиДанных = ВариантЗагрузкиОбновитьСопоставленныеКарточкиТоваров())
					 
					 ИЛИ СтрокаДереваВторогоУровня.ВариантЗагрузки = 3 
					 ИЛИ СтрокаДереваВторогоУровня.ИндексСтатуса = 4 
					 ИЛИ СтрокаДереваВторогоУровня.ИндексСтатуса = 5
					 ИЛИ НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) И НЕ Параметры.СоздатьЭлементыНоменклатуры Тогда
					Индекс = Индекс + 1;
					Продолжить;
				КонецЕсли;
				
				Если Параметры.ЗаписатьТолькоЦены 
					 И НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) Тогда
					Индекс = Индекс + 1;
					Продолжить;
				КонецЕсли;
				
				Если Параметры.ТолькоСоздатьНоменклатуру Тогда
					СтрокаДереваВторогоУровня.Пометка = Ложь;
					СтрокаДереваВторогоУровня.ИндексСтатуса = СтрокаДереваВторогоУровня.ИндексСтатуса - 100;
					
					Индекс = Индекс + 1;
					Продолжить;
				КонецЕсли;
				
				Если Параметры.ЗагрузитьЦеныПродажи ИЛИ Параметры.ЗаписатьТолькоЦены Тогда
					ЗаполнитьЗначенияСвойств(ТаблицаТоваров.Добавить(), СтрокаДереваВторогоУровня);
					Идентификаторы.Добавить(СтрокаДереваВторогоУровня[ИмяИдентификатора]);
				
					Если Параметры.ЗаписатьТолькоЦены Тогда
						СтрокаДереваВторогоУровня.Пометка       = Ложь;
						СтрокаДереваВторогоУровня.ИндексСтатуса = СтрокаДереваВторогоУровня.ИндексСтатуса - 100;
						
						Индекс = Индекс + 1;
						Продолжить;
					КонецЕсли;
				КонецЕсли;
				
				// Запись данных объекта 
				НачатьТранзакцию();
				
				Попытка  
					// Корректировка данных
					
					Если СтрокаДереваВторогоУровня.ВариантЗагрузки = ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
					
						СтрокаДереваВторогоУровня.ДатаСоздания = ТекущаяДатаСеанса();
						СтрокаДереваВторогоУровня.НаименованиеОбъектаМаркетплейса = ПредставлениеТоварнойПозиции(
							СтрокаДереваВторогоУровня.Номенклатура, 
							СтрокаДереваВторогоУровня.Характеристика, 
							СтрокаДереваВторогоУровня.Упаковка);
							
					Иначе
						СтрокаДереваВторогоУровня.ДатаОбновления = ТекущаяДатаСеанса();
					КонецЕсли;
					
					// РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет
					БлокировкаДанных = Новый БлокировкаДанных;
					ЭлементБлокировкиДанных = БлокировкаДанных.Добавить( "РегистрСведений.СтатусыПубликацииТоваровЯндексМаркет" );
					ЭлементБлокировкиДанных.УстановитьЗначение( "УчетнаяЗаписьМаркетплейса", СтрокаДереваВторогоУровня.УчетнаяЗаписьМаркетплейса);
					ЭлементБлокировкиДанных.УстановитьЗначение( "Номенклатура", СтрокаДереваВторогоУровня.Номенклатура);
					ЭлементБлокировкиДанных.УстановитьЗначение( "Характеристика", СтрокаДереваВторогоУровня.Характеристика);
					БлокировкаДанных.Заблокировать();

					
					НаборЗаписей = РегистрыСведений.СтатусыПубликацииТоваровЯндексМаркет.СоздатьНаборЗаписей();
					
					НаборЗаписей.Отбор.УчетнаяЗаписьМаркетплейса.Установить( СтрокаДереваВторогоУровня.УчетнаяЗаписьМаркетплейса );
					НаборЗаписей.Отбор.Номенклатура.Установить( СтрокаДереваВторогоУровня.Номенклатура );
					НаборЗаписей.Отбор.Характеристика.Установить( СтрокаДереваВторогоУровня.Характеристика );
					
					НаборЗаписей.Прочитать();
					
					ДействиеДобавлениеОписанияТовара = Ложь;
					ДействиеОбновлениеОписанияТовара = Ложь;
					
					Если НаборЗаписей.Количество() = 0 Тогда
						Запись = НаборЗаписей.Добавить();
						
						ДействиеДобавлениеОписанияТовара = Истина;
						
					ИначеЕсли СтрокаДереваВторогоУровня.ВариантЗагрузки = ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
					
						ОтменитьТранзакцию();
						
						Результат.Пропущено.Добавить(Новый Структура("НомерСтроки, Сообщение", 
							Индекс + 1, СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
								НСтр("ru = 'Товарная позиция «%1» не записана, т.к. уже существует другая позиция с таким набором ключевых полей: %2, %3, %4.'"),
								СтрокаДереваВторогоУровня.ПредставлениеОбъектаМаркетплейса,
								СтрокаДереваВторогоУровня.Номенклатура,
								СтрокаДереваВторогоУровня.Характеристика,
								СтрокаДереваВторогоУровня.Упаковка)));
						Результат.НеЗаписано = Результат.НеЗаписано + 1;
						Индекс = Индекс + 1;
						
						// Удаление дубля товара
						Отбор = Новый Структура(ПоляТаблицыТоваров);
						ЗаполнитьЗначенияСвойств(Отбор, СтрокаДереваВторогоУровня);
						СтрокиТовара = ТаблицаТоваров.НайтиСтроки(Отбор);
						Для Каждого СтрокаТовара Из СтрокиТовара Цикл
							ТаблицаТоваров.Удалить(СтрокаТовара);
							Прервать;
						КонецЦикла;
						
						Продолжить;
						
					Иначе
						Запись = НаборЗаписей[0];
						
						ДействиеОбновлениеОписанияТовара = Истина;
					КонецЕсли;
									 
					Если СтрокаДереваВторогоУровня.ВариантЗагрузки = ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
						
						ЗаполнитьЗначенияСвойств( Запись, СтрокаДереваВторогоУровня );
						
						Если ЗначениеЗаполнено( СтрокаДереваВторогоУровня.ТекущееИдентификаторКатегорииМаркетплейса ) Тогда
							
							Запись.ИдентификаторКатегорииМаркетплейса = СтрокаДереваВторогоУровня.ТекущееИдентификаторКатегорииМаркетплейса;
							Запись.НаименованиеКатегорииМаркетплейса  = СтрокаДереваВторогоУровня.ТекущееНаименованиеКатегорииМаркетплейса;
							
						КонецЕсли;
									   
					КонецЕсли;
					
					Запись.Категория = СтрокаДереваВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет;
					
					Запись.ПредставлениеТовара = СтрокаДереваВторогоУровня.ПредставлениеОбъектаМаркетплейса;
					Запись.ИдентификаторТовараПлощадки = СтрокаДереваВторогоУровня.ИдентификаторТовараПлощадки;
					Запись.НаименованиеТовараПлощадки = СтрокаДереваВторогоУровня.ПредставлениеОбъектаМаркетплейса;
					Запись.НаименованиеКатегорииПлощадки = СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса;
					Запись.ИдентификаторКатегорииМаркетплейса = СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса;
					Запись.НаименованиеМоделиПлощадки = СтрокаДереваВторогоУровня.НаименованиеМоделиПлощадки;
					Запись.КатегорияНоменклатуры = СтрокаДереваВторогоУровня.НаименованиеКатегорииМаркетплейса;
					Запись.МаркерСтатуса = 0;
					Запись.ЕстьИдентификаторПлощадки = Истина;
					
					
					НаборЗаписей.Записать(Истина);					
					
					ЗафиксироватьТранзакцию();
					
				Исключение
				
				    ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки( ИнформацияОбОшибке() );
				    
					Если ТранзакцияАктивна() Тогда
						ОтменитьТранзакцию();
					КонецЕсли;
					
					ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'При записи сведений по товарной позиции «%1» возникла ошибка: %2.'", 
					ОбщегоНазначения.КодОсновногоЯзыка()),
					СтрокаДереваВторогоУровня.ПредставлениеОбъектаМаркетплейса,
					ТекстОшибки);
						
					ЗаписьЖурналаРегистрации(СобытиеЖурналаРегистрации(),
					УровеньЖурналаРегистрации.Ошибка,,,
					ТекстСообщения);
						
					ОбщегоНазначения.СообщитьПользователю( ТекстСообщения );
					
					ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстСообщения, УчетнаяЗаписьМаркетплейса, Истина );
					
					Результат.Пропущено.Добавить( 
					Новый Структура( "НомерСтроки, Сообщение", 
					Индекс + 1, СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
					НСтр("ru = 'При записи сведений по товарной позиции «%1» возникли ошибки.'"),
					СтрокаДереваВторогоУровня.ПредставлениеОбъектаМаркетплейса)));
						    
					Результат.НеЗаписано = Результат.НеЗаписано + 1;
					Индекс = Индекс + 1;
					Продолжить;
				КонецПопытки;
			
				Результат.Записано = Результат.Записано + 1;
				Если СтрокаДереваВторогоУровня.ВариантЗагрузки = ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Тогда
					Результат.ВТомЧислеНовых = Результат.ВТомЧислеНовых + 1;
					СтрокаДереваВторогоУровня.Пометка = Ложь;
				Иначе	
					СтрокаДереваВторогоУровня.Пометка = Истина;
				КонецЕсли;

				СтрокаДереваВторогоУровня.ВариантЗагрузки = ВариантЗагрузкиОбновитьСопоставленныеКарточкиТоваров();
				СтрокаДереваВторогоУровня.ИндексСтатуса   = -999;
				
				Индекс = Индекс + 1;
			КонецЦикла;
		КонецЦикла;
		
		ЦеныТоваров = Неопределено;
		
		// Окончание
		Для каждого СтрокаДереваПервогоУровня Из ДеревоТоваров.Строки Цикл
			Для каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
				Если СтрокаДереваВторогоУровня.ИндексСтатуса < 0 Тогда
					Если СтрокаДереваВторогоУровня.ИндексСтатуса = -999 Тогда
						Если СтрокаДереваВторогоУровня.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ЗагруженСОшибками 
							 ИЛИ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.ОписаниеОшибки)
								 И СтрокаДереваВторогоУровня.Статус <> Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ВАрхиве Тогда
							СтрокаДереваВторогоУровня.ИндексСтатуса = 0;
						ИначеЕсли СтрокаДереваВторогоУровня.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.СозданиеНового Тогда
							СтрокаДереваВторогоУровня.ИндексСтатуса = 1;
						ИначеЕсли СтрокаДереваВторогоУровня.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации Тогда
							СтрокаДереваВторогоУровня.ИндексСтатуса = 1;
						ИначеЕсли СтрокаДереваВторогоУровня.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ВАрхиве Тогда
							СтрокаДереваВторогоУровня.ИндексСтатуса = 2;
						ИначеЕсли СтрокаДереваВторогоУровня.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияПройдена Тогда
							СтрокаДереваВторогоУровня.ИндексСтатуса = 3;
						ИначеЕсли СтрокаДереваВторогоУровня.Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияНеПройдена Тогда
							СтрокаДереваВторогоУровня.ИндексСтатуса = 0;
						КонецЕсли;
					Иначе
						СтрокаДереваВторогоУровня.ИндексСтатуса = СтрокаДереваВторогоУровня.ИндексСтатуса + 100;
					КонецЕсли;
					
				КонецЕсли;
			КонецЦикла;
		КонецЦикла;
		
		ИмпортИзМаркетплейсаЗаполнитьНаименованияГруппДереваТоваров(
			ДеревоТоваров, 
			Ложь, 
			Параметры.ОтображатьСопоставленные,
			Параметры.ОтображатьПоСтатусу);
		
		Результат.Вставить("ДеревоТоваров", ДеревоТоваров);
		
		Если Результат.Записано = 0 И Результат.НеЗаписано = 0 Тогда
			Результат.Ошибка.КодОшибки = КодыОшибокРегистраСтатусовПубликацииТоваров().ОшибкаПрочие;
			Результат.Ошибка.ОписаниеОшибки = НСтр("ru = 'Не найдено данных для записи.'");
			
			Если ВсегоОтмечено > 0 
				 И (ВсегоНеПолучено > 0 ИЛИ ВсегоНеСопоставлено > 0) Тогда
				Результат.Ошибка.ОписаниеОшибки = НСтр("ru = 'Данные не записаны, так как для отмеченных позиций'");
				Если ВсегоНеПолучено > 0 Тогда
					Результат.Ошибка.ОписаниеОшибки = Результат.Ошибка.ОписаниеОшибки + " " + НСтр("ru = 'не получены обновленные данные с торговой площадки'");
				КонецЕсли;
				Если ВсегоНеСопоставлено > 0 Тогда
					Если ВсегоНеПолучено > 0 Тогда
						Результат.Ошибка.ОписаниеОшибки = Результат.Ошибка.ОписаниеОшибки + " " + НСтр("ru = 'или'");
					КонецЕсли;
					Результат.Ошибка.ОписаниеОшибки = Результат.Ошибка.ОписаниеОшибки + " " + НСтр("ru = 'не выполнено сопоставление данным учетной системы 1С'");
				КонецЕсли;
				Результат.Ошибка.ОписаниеОшибки = Результат.Ошибка.ОписаниеОшибки + ".";
			КонецЕсли;
		КонецЕсли;
		
	Исключение
		ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
		Отказ = Истина;
		ЗаписьЖурналаРегистрации(СобытиеЖурналаРегистрации(),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
	КонецПопытки;

	Если Отказ Тогда
		Результат.Ошибка.КодОшибки 	    = КодыОшибокРегистраСтатусовПубликацииТоваров().ОшибкаПрочие;
		Результат.Ошибка.ОписаниеОшибки = ТекстОшибки;
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция КатегорииИзДеревоТоварыПолучить( ДеревоТоваров )
	
	Категории = Новый Соответствие;
	
	Для Каждого ДеревоТоваровСтрока Из ДеревоТоваров.Строки Цикл
		Для Каждого ДеревоТоваровСтрокаВторогоУровня Из ДеревоТоваровСтрока.Строки Цикл

			Если ДеревоТоваровСтрокаВторогоУровня.Пометка <> Истина Тогда
				Продолжить;
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено( ДеревоТоваровСтрокаВторогоУровня.ИдентификаторКатегорииМаркетплейса ) Тогда
				Продолжить;
			КонецЕсли;
			
			Если Категории.Получить( ДеревоТоваровСтрокаВторогоУровня.ИдентификаторКатегорииМаркетплейса ) <> Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			КатегорииЭлемент = Новый Структура;
			КатегорииЭлемент.Вставить( "ИдентификаторКатегорииМаркетплейса", ДеревоТоваровСтрокаВторогоУровня.ИдентификаторКатегорииМаркетплейса );
			КатегорииЭлемент.Вставить( "НаименованиеКатегорииМаркетплейса", ДеревоТоваровСтрокаВторогоУровня.НаименованиеКатегорииМаркетплейса );
			КатегорииЭлемент.Вставить( "ПолноеНаименованиеКатегорииМаркетплейса", "" );
			КатегорииЭлемент.Вставить( "Категория", ДеревоТоваровСтрокаВторогоУровня.Категория ); // Категория 1С для заполнения номенклатуры
			КатегорииЭлемент.Вставить( "КатегорияИерархия", ДеревоТоваровСтрокаВторогоУровня.КатегорияИерархия );
			КатегорииЭлемент.Вставить( "КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет", ДеревоТоваровСтрокаВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет );
			
			Категории.Вставить( ДеревоТоваровСтрокаВторогоУровня.ИдентификаторКатегорииМаркетплейса, КатегорииЭлемент );
		КонецЦикла;
	КонецЦикла;
	
	Возврат Категории;
КонецФункции

Функция Категории1СПоНаименованиюКатегорииМаркетплейсаНайтиИЗаполнить( Параметры ) Экспорт

	КатегорииДанные = Новый ТаблицаЗначений;
	ОписаниеТиповСтрока50 = Новый ОписаниеТипов("Строка",, Новый КвалификаторыСтроки( 50, ДопустимаяДлина.Переменная));
	ОписаниеТиповСтрока250 = Новый ОписаниеТипов("Строка",, Новый КвалификаторыСтроки( 250, ДопустимаяДлина.Переменная));
	КатегорииДанные.Колонки.Добавить( "Наименование", ОписаниеТиповСтрока50 );
	КатегорииДанные.Колонки.Добавить( "Код", ОписаниеТиповСтрока250 );
	
	ТипСоответствие = (ТипЗнч( Параметры.СопоставлениеКатегорий ) = Тип( "Соответствие" ));
	
	КатегорииПоиск = Новый Структура( "Код, Наименование" );
	Для Каждого СопоставлениеКатегорийЭлемент Из Параметры.СопоставлениеКатегорий Цикл
		
		Если ТипСоответствие = Истина Тогда
			СопоставлениеКатегорийСтрока = СопоставлениеКатегорийЭлемент.Значение;
		Иначе
			СопоставлениеКатегорийСтрока = СопоставлениеКатегорийЭлемент;
		КонецЕсли;
		
		Если ЗначениеЗаполнено( СопоставлениеКатегорийСтрока.Категория ) Тогда
			Продолжить
		КонецЕсли;
		
		КатегорииПоиск.Код = СопоставлениеКатегорийСтрока.ИдентификаторКатегорииМаркетплейса;
		КатегорииПоиск.Наименование = СопоставлениеКатегорийСтрока.НаименованиеКатегорииМаркетплейса;
		
		КатегорииДанныеСтроки = КатегорииДанные.НайтиСтроки( КатегорииПоиск );
		Если КатегорииДанныеСтроки.Количество() > 0 Тогда
			Продолжить;
		КонецЕсли;
		
		КатегорииДанныеСтрока = КатегорииДанные.Добавить();
		КатегорииДанныеСтрока.Код = СопоставлениеКатегорийСтрока.ИдентификаторКатегорииМаркетплейса;
		КатегорииДанныеСтрока.Наименование = СопоставлениеКатегорийСтрока.НаименованиеКатегорииМаркетплейса;
	КонецЦикла;
	
	ЗапросТекст =
	"ВЫБРАТЬ
	|	КатегорииДанные.Код КАК Код,
	|	КатегорииДанные.Наименование КАК Наименование
	|ПОМЕСТИТЬ КатегорииДанные
	|ИЗ
	|	&КатегорииДанные КАК КатегорииДанные
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	КатегорииНоменклатуры.Ссылка КАК Ссылка,
	|	КатегорииНоменклатуры.Код КАК Код,
	|	КатегорииНоменклатуры.Наименование КАК Наименование
	|ИЗ
	|	Справочник.КатегорииНоменклатуры КАК КатегорииНоменклатуры
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ КатегорииДанные КАК КатегорииДанные
	|		ПО КатегорииНоменклатуры.Наименование = КатегорииДанные.Наименование
	|			И КатегорииНоменклатуры.Код = КатегорииДанные.Код
	|ГДЕ
	|	КатегорииНоменклатуры.ПометкаУдаления = ЛОЖЬ
	|	И КатегорииНоменклатуры.ЭтоГруппа = ЛОЖЬ";

	ЗапросОбъект = Новый Запрос( ЗапросТекст );
	ЗапросОбъект.УстановитьПараметр( "КатегорииДанные", КатегорииДанные );
	Категории1С = ЗапросОбъект.Выполнить().Выгрузить();
	
	ИсточникКатегорииКатегория = ( Параметры.ИсточникКатегории = Перечисления.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры );
	Для Каждого СопоставлениеКатегорийЭлемент Из Параметры.СопоставлениеКатегорий Цикл

		Если ТипСоответствие = Истина Тогда
			СопоставлениеКатегорийСтрока = СопоставлениеКатегорийЭлемент.Значение;
		Иначе
			СопоставлениеКатегорийСтрока = СопоставлениеКатегорийЭлемент;
		КонецЕсли;
		
		Если ЗначениеЗаполнено( СопоставлениеКатегорийСтрока.Категория ) Тогда
			Продолжить;
		КонецЕсли;
		
		КатегорииПоиск.Код = СопоставлениеКатегорийСтрока.ИдентификаторКатегорииМаркетплейса;
		КатегорииПоиск.Наименование = СопоставлениеКатегорийСтрока.НаименованиеКатегорииМаркетплейса;

		Категории1ССтроки = Категории1С.НайтиСтроки( КатегорииПоиск );
		Если Категории1ССтроки.Количество() = 0 Тогда
			Продолжить;
		КонецЕсли;
		
		СопоставлениеКатегорийСтрока.Категория = Категории1ССтроки[ 0 ].Ссылка;
		
		Если ИсточникКатегорииКатегория = Истина Тогда
			СопоставлениеКатегорийСтрока.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = СопоставлениеКатегорийСтрока.Категория;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

Функция Категории1СПоКатегориямМаркетплейсаПараметрыНовый() Экспорт

	ПараметрыФункции = Новый Структура;
	ПараметрыФункции.Вставить( "СопоставлениеКатегорий", Неопределено );
	ПараметрыФункции.Вставить( "ИсточникКатегории", Неопределено );
	ПараметрыФункции.Вставить( "СоздатьИерархию", Ложь );
	ПараметрыФункции.Вставить( "УчетнаяЗапись", Неопределено );
	ПараметрыФункции.Вставить( "КэшКатегорийДерево", Неопределено );
	ПараметрыФункции.Вставить( "КатегорииНоменклатурыСоздано", 0 );
	
	Возврат ПараметрыФункции;
КонецФункции

Функция Категории1СПоКатегориямМаркетплейсаСоздать( Параметры ) Экспорт
    
    СопоставлениеКатегорийТипСоответствие = (ТипЗнч( Параметры.СопоставлениеКатегорий ) = Тип( "Соответствие" ));
    ИсточникКатегорииКатегория = (Параметры.ИсточникКатегории = ПредопределенноеЗначение( "Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры" ) = Истина);
	
	КатегорияДанные = ОбменСВнешнимиСистемамиНоменклатура.КатегорияДанныеНовый();
	
	КоличествоЗаписано = 0;
	Для Каждого СопоставлениеКатегорийЭлемент Из Параметры.СопоставлениеКатегорий Цикл
		
		Если СопоставлениеКатегорийТипСоответствие = Истина Тогда
			СопоставлениеКатегории = СопоставлениеКатегорийЭлемент.Значение;
		Иначе
			СопоставлениеКатегории = СопоставлениеКатегорийЭлемент;
		КонецЕсли;
		
		Если ЗначениеЗаполнено( СопоставлениеКатегории.Категория ) Тогда
			Продолжить;
		КонецЕсли;
		
		КатегорияДанные.Наименование = СопоставлениеКатегории.НаименованиеКатегорииМаркетплейса;
		КатегорияДанные.Код = СопоставлениеКатегории.ИдентификаторКатегорииМаркетплейса;
		КатегорияДанные.Иерархия = СопоставлениеКатегории.КатегорияИерархия;
		КатегорияДанные.СоздатьИерархию = Параметры.СоздатьИерархию;
		
		КатегорияСсылка = ОбменСВнешнимиСистемамиНоменклатура.КатегорияНоменклатурыПоНаименованиюНайти( КатегорияДанные.Наименование, Ложь );
		Если ЗначениеЗаполнено( КатегорияСсылка ) Тогда
			СопоставлениеКатегории.Категория = КатегорияСсылка;
			
			Если ИсточникКатегорииКатегория Тогда
				Если Не ЗначениеЗаполнено( СопоставлениеКатегории.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет ) Тогда
					СопоставлениеКатегории.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = КатегорияСсылка;
				КонецЕсли;
			КонецЕсли;
			
			Продолжить;
		КонецЕсли;
		
		КатегорияСсылка = ОбменСВнешнимиСистемамиНоменклатура.КатегорияНоменклатурыСоздать( КатегорияДанные, Параметры );
		Если Не ЗначениеЗаполнено( КатегорияСсылка ) Тогда
			Возврат Ложь;
		КонецЕсли;
		
		СопоставлениеКатегории.Категория = КатегорияСсылка;
		
		Если ИсточникКатегорииКатегория Тогда
			Если Не ЗначениеЗаполнено( СопоставлениеКатегории.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет ) Тогда
				СопоставлениеКатегории.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = КатегорияСсылка;
			КонецЕсли;
		КонецЕсли;
		
		
		КоличествоЗаписано  = КоличествоЗаписано + 1;
	КонецЦикла;
	
	Параметры.КатегорииНоменклатурыСоздано = КоличествоЗаписано;
	
	Возврат Истина;
КонецФункции

Процедура ДеревоТоваровКатегорииПоСопоставлениюЗаполнить( ДеревоТоваров, Категории, Параметры )
	
	ИсточникКатегорииКатегория = (Параметры.ИсточникКатегории = ПредопределенноеЗначение( "Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры" ));
	Для Каждого СтрокаДереваПервогоУровня Из ДеревоТоваров.Строки Цикл
		Для Каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
			
			Если СтрокаДереваВторогоУровня.Пометка <> Истина Тогда
				Продолжить;
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса ) Тогда
				Продолжить;
			КонецЕсли;
			
			СопоставлениеКатегории = Категории.Получить( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса );
			Если СопоставлениеКатегории = Неопределено Тогда
				Продолжить;
			КонецЕсли;
			
			СтрокаДереваВторогоУровня.Категория = СопоставлениеКатегории.Категория;
			
			Если ИсточникКатегорииКатегория = Истина Тогда
				СтрокаДереваВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = СопоставлениеКатегории.Категория;
			КонецЕсли;
			
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

Процедура ИмпортируемыеТоварыНоменклатураКлючЗаполнить( ДеревоТоваров )
	
	Для Каждого ИмпортируемыеТоварыСтрока Из ДеревоТоваров.Строки Цикл
		Для Каждого ИмпортируемыеТоварыВторойУровеньСтрока Из ИмпортируемыеТоварыСтрока.Строки Цикл
			Если ЗначениеЗаполнено( ИмпортируемыеТоварыВторойУровеньСтрока.Номенклатура ) Тогда
				ИмпортируемыеТоварыВторойУровеньСтрока.НоменклатураКлюч = 
				ИмпортируемыеТоварыВторойУровеньСтрока.Номенклатура.УникальныйИдентификатор();
			Иначе
				ИмпортируемыеТоварыВторойУровеньСтрока.НоменклатураКлюч = Новый УникальныйИдентификатор;
			КонецЕсли;
		КонецЦикла;
	КонецЦикла;

КонецПроцедуры

Процедура ИерархияНоменклатурыПоИерархииКатегорийМаркетплейсаВДеревеТоваровЗаполнить( Параметры )

	ИерархияКатегорийЗаполнена = Ложь;
	Для Каждого СопоставлениеКатегорийЭлемент Из Параметры.СопоставлениеКатегорий Цикл
		Если СопоставлениеКатегорийЭлемент.Значение.КатегорияИерархия.Количество() > 0 Тогда
			ИерархияКатегорийЗаполнена = Истина;
		КонецЕсли;
		
		Прервать;
	КонецЦикла;
	
	Для Каждого СтрокаДереваПервогоУровня Из Параметры.ДеревоТоваров.Строки Цикл
		Для Каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
			
			Если СтрокаДереваВторогоУровня.Пометка = Ложь Тогда
				Продолжить;
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса ) Тогда
				
				ТекстОшибки = "ИерархияНоменклатурыПоИерархииКатегорийМаркетплейсаВДеревеТоваровЗаполнить: Ошибка: "
				+ СтрокаДереваВторогоУровня.ПредставлениеОбъектаМаркетплейса
				+ ": Не задан идентификатор категории Яндекс Маркет.";
				
				ОбщегоНазначения.СообщитьПользователю( ТекстОшибки );
				
				ОбменСВнешнимиСистемамиЛогирование.ЗаписатьВЛог( ТекстОшибки, Параметры.УчетнаяЗапись, Истина );
				
				Продолжить;
			КонецЕсли;
			
			СопоставлениеКатегорийЭлемент = Параметры.СопоставлениеКатегорий.Получить( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса );
			Если СопоставлениеКатегорийЭлемент = Неопределено Тогда
				ВызватьИсключение "ИерархияНоменклатурыПоИерархииКатегорийМаркетплейсаВДеревеТоваровЗаполнить: Ошибка при заполнении иерархии категорий: Не найден идентификатор категории";
			КонецЕсли;
			
			СтрокаДереваВторогоУровня.НоменклатураИерархия = ОбщегоНазначения.СкопироватьРекурсивно( СопоставлениеКатегорийЭлемент.КатегорияИерархия );
			
			// Добавление самой категории в иерархию групп Номенклатуры
			НоменклатураИерархияСтрока = СтрокаДереваВторогоУровня.НоменклатураИерархия.Добавить();
			НоменклатураИерархияСтрока.Код = СопоставлениеКатегорийЭлемент.ИдентификаторКатегорииМаркетплейса;
			НоменклатураИерархияСтрока.Наименование = СопоставлениеКатегорийЭлемент.НаименованиеКатегорииМаркетплейса;
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

Функция ТоварыСоздать( ДеревоТоваров, Параметры )
	
	// заполним описания товаров из элементов формы в параметры поиска
	ДеревоТоваровДляСоздания = ДеревоТоваров.Строки.НайтиСтроки( Новый Структура( "Пометка, ПоискВыполнен", Истина, Истина ), Истина );
	Если ДеревоТоваровДляСоздания.Количество() = 0 Тогда
		Возврат Ложь;
	КонецЕсли;

	ПараметрыКонтекст = ОбменСВнешнимиСистемамиНоменклатура.ТоварыСоздатьПараметрыКонтекстНовый();
	
	ПараметрыКонтекст.ГруппаДляСозданияНовыхТоваров = Параметры.ГруппаДляСозданияНовыхТоваров;
	ПараметрыКонтекст.ЗаписатьФайлы = Параметры.ЗагрузитьИзображения;

	ПараметрыКонтекст.УчетнаяЗапись = Параметры.УчетнаяЗаписьМаркетплейса;
	ПараметрыКонтекст.ПерезаполнитьЗначенияРеквизитовНоменклатуры = Параметры.ПерезаполнитьЗначенияРеквизитовНоменклатуры;

	Для Каждого ДеревоТоваровДляСозданияСтрока Из ДеревоТоваровДляСоздания Цикл
	
		Если Не ЗначениеЗаполнено( ДеревоТоваровДляСозданияСтрока.ИдентификаторОбъектаМаркетплейса ) Тогда
			Продолжить;
		КонецЕсли;
		
		ОписаниеТовараЭлемент = ОбменСВнешнимиСистемамиНоменклатура.ТоварыСоздатьОписанияТоваровЭлементНовый( ПараметрыКонтекст.ОписанияТоваров );
		
		ОписаниеТовараЭлемент.ИдентификаторЭлемента = ДеревоТоваровДляСозданияСтрока.ИдентификаторОбъектаМаркетплейса;
		ОписаниеТовараЭлемент.ТипНоменклатуры = Перечисления.ТипыНоменклатуры.Запас;

		ОписаниеТовараЭлемент.КатегорияНоменклатуры = ДеревоТоваровДляСозданияСтрока.Категория;

		ОписаниеТовараЭлемент.Наименование = ДеревоТоваровДляСозданияСтрока.ПредставлениеОбъектаМаркетплейса;
		ОписаниеТовараЭлемент.НаименованиеПолное = ДеревоТоваровДляСозданияСтрока.ПредставлениеОбъектаМаркетплейса;
	
		ОписаниеТовараЭлемент.Номенклатура = ДеревоТоваровДляСозданияСтрока.Номенклатура;
		ОписаниеТовараЭлемент.Характеристика = ДеревоТоваровДляСозданияСтрока.Характеристика;
		ОписаниеТовараЭлемент.СоздатьХарактеристику = ДеревоТоваровДляСозданияСтрока.СоздатьХарактеристику;
		ОписаниеТовараЭлемент.НоменклатураКлюч = ДеревоТоваровДляСозданияСтрока.НоменклатураКлюч;
		
		Если ОписаниеТовараЭлемент.СоздатьХарактеристику = Истина Тогда
			ОписаниеТовараЭлемент.НоменклатураНаименование = СокрЛП( ДеревоТоваровДляСозданияСтрока.ЗначениеОбъединенияВОднуКарточку );
			Если ПустаяСтрока( ОписаниеТовараЭлемент.НоменклатураНаименование ) Тогда
				ОписаниеТовараЭлемент.НоменклатураНаименование = ОписаниеТовараЭлемент.Наименование;
			КонецЕсли;
		КонецЕсли;

		// Артикул
		ДеревоРеквизитовАртикулСтроки = ДеревоТоваровДляСозданияСтрока.ДеревоРеквизитов.Строки.НайтиСтроки( Новый Структура( "НаименованиеАтрибутаМаркетплейса", "Артикул" ), Истина );
		Если ДеревоРеквизитовАртикулСтроки.Количество() > 0 Тогда
			ОписаниеТовараЭлемент.Артикул = ДеревоРеквизитовАртикулСтроки[ 0 ].ЗначениеАтрибутаМаркетплейса;
		КонецЕсли;
		
		// Вес
		ДеревоРеквизитовАртикулСтроки = ДеревоТоваровДляСозданияСтрока.ДеревоРеквизитов.Строки.НайтиСтроки( Новый Структура( "ИдентификаторАтрибута1С", "ВесБруттоКг" ), Истина );
		Если ДеревоРеквизитовАртикулСтроки.Количество() > 0 Тогда
			ОписаниеТовараЭлемент.Вес = ДеревоРеквизитовАртикулСтроки[ 0 ].ЗначениеАтрибутаМаркетплейса;
		КонецЕсли;

		// Высота
		ДеревоРеквизитовАртикулСтроки = ДеревоТоваровДляСозданияСтрока.ДеревоРеквизитов.Строки.НайтиСтроки( Новый Структура( "ИдентификаторАтрибута1С", "ВысотаСм" ), Истина );
		Если ДеревоРеквизитовАртикулСтроки.Количество() > 0 Тогда
			ОписаниеТовараЭлемент.Высота = ДеревоРеквизитовАртикулСтроки[ 0 ].ЗначениеАтрибутаМаркетплейса;
		КонецЕсли;

		// Длина
		ДеревоРеквизитовАртикулСтроки = ДеревоТоваровДляСозданияСтрока.ДеревоРеквизитов.Строки.НайтиСтроки( Новый Структура( "ИдентификаторАтрибута1С", "ДлинаСм" ), Истина );
		Если ДеревоРеквизитовАртикулСтроки.Количество() > 0 Тогда
			ОписаниеТовараЭлемент.Длина = ДеревоРеквизитовАртикулСтроки[ 0 ].ЗначениеАтрибутаМаркетплейса;
		КонецЕсли;

		// Ширина
		ДеревоРеквизитовАртикулСтроки = ДеревоТоваровДляСозданияСтрока.ДеревоРеквизитов.Строки.НайтиСтроки( Новый Структура( "ИдентификаторАтрибута1С", "ШиринаСм" ), Истина );
		Если ДеревоРеквизитовАртикулСтроки.Количество() > 0 Тогда
			ОписаниеТовараЭлемент.Ширина = ДеревоРеквизитовАртикулСтроки[ 0 ].ЗначениеАтрибутаМаркетплейса;
		КонецЕсли;

		// Описание
		ОписаниеТовараЭлемент.Комментарий = ДеревоТоваровДляСозданияСтрока.ОписаниеОбъектаМаркетплейса;
        
        // Штрихкоды
		ФормаШтрихкодыСтроки = ДеревоТоваровДляСозданияСтрока.ДеревоРеквизитов.Строки.НайтиСтроки( Новый Структура( "НаименованиеАтрибутаМаркетплейса", "Штрихкоды" ), Истина );
		Если ФормаШтрихкодыСтроки.Количество() > 0 Тогда

			ФормаШтрихкоды = ФормаШтрихкодыСтроки[ 0 ].ЗначенияАтрибутаМаркетплейса;
			
			Для Каждого ФормаШтрихкод Из ФормаШтрихкоды Цикл
				Штрихкод = ОписаниеТовараЭлемент.Штрихкоды.Добавить();
				Штрихкод.Имя = "-";
				Штрихкод.Значение = ФормаШтрихкод.Значение;
			КонецЦикла;
			
		КонецЕсли;
		
		// Файлы
		Для Каждого ФормаИзображение Из ДеревоТоваровДляСозданияСтрока.Изображения Цикл
			
			Файл = ОписаниеТовараЭлемент.Файлы.Добавить();
			
			Файл.Имя = ФормаИзображение.ПутьКФайлу;
			Файл.URL = ФормаИзображение.ПутьКФайлу;
		КонецЦикла;
		
		Если Параметры.СоздатьИерархиюНоменклатуры = Истина Тогда
			ПараметрыКонтекст.СоздатьИерархиюНоменклатуры = Истина;
			
			ОписаниеТовараЭлемент.Иерархия = ДеревоТоваровДляСозданияСтрока.НоменклатураИерархия;
		КонецЕсли;
	КонецЦикла;

	Время001 = ТекущаяУниверсальнаяДатаВМиллисекундах();
	
	
	ТоварыСозданы = ОбменСВнешнимиСистемамиНоменклатура.ТоварыСоздать( ПараметрыКонтекст );// создание элементов справочников, включая файлы
	
	
	Время002 = ТекущаяУниверсальнаяДатаВМиллисекундах();
	ДлительностьМс = Время002 - Время001;
	
	Если ТоварыСозданы = Истина Тогда

		ОтборПоИдентификатору = Новый Структура( "ИдентификаторОбъектаМаркетплейса" );
		ОтборПоURL = Новый Структура( "URL" );
		Для Каждого ОписаниеТовараЭлемент Из ПараметрыКонтекст.ОписанияТоваров Цикл
			
			ОтборПоИдентификатору.ИдентификаторОбъектаМаркетплейса = ОписаниеТовараЭлемент.ИдентификаторЭлемента;
			ДеревоТоваровСтроки = ДеревоТоваров.Строки.НайтиСтроки( ОтборПоИдентификатору, Истина );
			
			Если ДеревоТоваровСтроки.Количество() = 0 Тогда
				ОбщегоНазначения.СообщитьПользователю( "ТоварыСоздать: Не найден элемент по идентификатору "
				+ ОтборПоИдентификатору.ИдентификаторОбъектаМаркетплейса );
				Продолжить;
			КонецЕсли;
			
			ДеревоТоваровДляСозданияСтрока = ДеревоТоваровСтроки[ 0 ];
			
			ДеревоТоваровДляСозданияСтрока.Номенклатура = ОписаниеТовараЭлемент.Номенклатура;
			ДеревоТоваровДляСозданияСтрока.Характеристика = ОписаниеТовараЭлемент.Характеристика;
			
			// заполнение ссылок на файлы в дереве товаров в таблицу изображения
			Для Каждого ДеревоТоваровИзображенияСтрока Из ДеревоТоваровДляСозданияСтрока.Изображения Цикл
				
				Если ПустаяСтрока( ДеревоТоваровИзображенияСтрока.ПутьКФайлу ) Тогда
					Продолжить;
				КонецЕсли;
				
				ОтборПоURL.URL = ДеревоТоваровИзображенияСтрока.ПутьКФайлу;
				ОписаниеТовараЭлементФайлыСтроки = ОписаниеТовараЭлемент.Файлы.НайтиСтроки( ОтборПоURL );
				Если ОписаниеТовараЭлементФайлыСтроки.Количество() = 0 Тогда
					Продолжить;
				КонецЕсли;
				
				ДеревоТоваровИзображенияСтрока.ФайлСсылка = ОписаниеТовараЭлементФайлыСтроки[ 0 ].ФайлСсылка;
			КонецЦикла;
			
			ДеревоТоваровСтрокаПоНоменклатуреДозаполнить( ДеревоТоваровДляСозданияСтрока, Параметры );
		КонецЦикла;
		
		Параметры.НоменклатураКоличествоСоздано = ПараметрыКонтекст.НоменклатураКоличествоСоздано;
		Параметры.НоменклатураКоличествоОбновлено = ПараметрыКонтекст.НоменклатураКоличествоОбновлено;
		Параметры.ХарактеристикиКоличествоСоздано = ПараметрыКонтекст.ХарактеристикиКоличествоСоздано;
		Параметры.ХарактеристикиКоличествоОбновлено = ПараметрыКонтекст.ХарактеристикиКоличествоОбновлено;
		Параметры.ФайлыКоличествоСоздано = ПараметрыКонтекст.ФайлыКоличествоСоздано;
		Параметры.ШтрихкодыКоличествоСоздано = ПараметрыКонтекст.ШтрихкодыКоличествоСоздано;
		
	КонецЕсли;
	
	Возврат ТоварыСозданы;
КонецФункции

Процедура ДеревоТоваровКатегорияИлиНоменклатураДляСопоставленияКатегорииМаркетплейсаЗаполнить( ДеревоТоваров, Параметры )
	
	ИсточникКатегорииКатегория = (Параметры.ИсточникКатегории = ПредопределенноеЗначение( "Перечисление.ИсточникиКатегорийДляМаркетплейса.ВидНоменклатуры" ));
	Для Каждого СтрокаДереваПервогоУровня Из ДеревоТоваров.Строки Цикл
		Для Каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
			
			Если СтрокаДереваВторогоУровня.Пометка <> Истина Тогда
				Продолжить;
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено( СтрокаДереваВторогоУровня.ИдентификаторКатегорииМаркетплейса ) Тогда
				Продолжить;
			КонецЕсли;
			
			Если ИсточникКатегорииКатегория = Истина Тогда
				СтрокаДереваВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = СтрокаДереваВторогоУровня.Категория;
			Иначе
				
				// Родитель или сама номенклатура
				Если ЗначениеЗаполнено( СтрокаДереваВторогоУровня.Номенклатура ) Тогда
					Родитель = ОбщегоНазначения.ЗначениеРеквизитаОбъекта( СтрокаДереваВторогоУровня.Номенклатура, "Родитель" );
					Если ЗначениеЗаполнено( Родитель ) Тогда
						СтрокаДереваВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = Родитель;
					Иначе
						СтрокаДереваВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет = СтрокаДереваВторогоУровня.Номенклатура;
					КонецЕсли;
				КонецЕсли;
				
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено( СтрокаДереваВторогоУровня.ИсточникКатегорииМаркетплейса ) Тогда
				СтрокаДереваВторогоУровня.ИсточникКатегорииМаркетплейса = СтрокаДереваВторогоУровня.КатегорияИлиНоменклатураДляСопоставленияКатегорииЯндексМаркет;
			КонецЕсли;
			
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

// Конструктор таблицы значений для сведений о выгруженных данных.
// 
// Возвращаемое значение:
//	 ТаблицаЗначений - таблица с колонками:
//     * УчетнаяЗаписьМаркетплейса - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису.
//     * Номенклатура              - СправочникСсылка.Номенклатура - найденная номенклатура;
//     * Характеристика            - СправочникСсылка.ХарактеристикиНоменклатуры - найденная характеристика;
//     * Упаковка                  - СправочникСсылка.УпаковкиЕдиницыИзмерения - найденная упаковка;
//     * ВидОбъектаМаркетплейса    - ПеречислениеСсылка.ВидыОбъектовМаркетплейсов - вид объекта маркетплейса;
//     * ОбъектПубликации          - СправочникСсылка.НоменклатураПрисоединенныеФайлы - объект публикации.
//
Функция СведенияОВыгруженныхДанных() Экспорт

	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("УчетнаяЗаписьМаркетплейса", Новый ОписаниеТипов("СправочникСсылка.УчетныеЗаписиМаркетплейсов"));
	Результат.Колонки.Добавить("Номенклатура",              Новый ОписаниеТипов("СправочникСсылка.Номенклатура"));
	Результат.Колонки.Добавить("Характеристика",            Новый ОписаниеТипов("СправочникСсылка.ХарактеристикиНоменклатуры"));
	Результат.Колонки.Добавить("Упаковка",                  Новый ОписаниеТипов("СправочникСсылка.ЕдиницыИзмерения"));
	Результат.Колонки.Добавить("ВидОбъектаМаркетплейса",    Новый ОписаниеТипов("ПеречислениеСсылка.ВидыОбъектовМаркетплейсов"));
	
	ТипОбъектПубликации = Новый Массив;
	ТипОбъектПубликации.Добавить(Тип("СправочникСсылка.НоменклатураПрисоединенныеФайлы"));
	ТипОбъектПубликации.Добавить(Тип("СправочникСсылка.ХарактеристикиНоменклатурыПрисоединенныеФайлы"));
	Результат.Колонки.Добавить("ОбъектПубликации", Новый ОписаниеТипов(ТипОбъектПубликации));

	Возврат Результат;

КонецФункции

Функция ВариантЗагрузкиОбновитьСопоставленныеКарточкиТоваров() Экспорт
	Возврат 0;
КонецФункции

Функция ВариантЗагрузкиЗагрузитьНесопоставленныеКарточкиТоваров() Экспорт
	Возврат 2;
КонецФункции

// Формирует строковое представление товарной позиции по шаблону.
//
// Параметры:
//  Номенклатура   - СправочникСсылка.Номенклатура - 
//  Характеристика - СправочникСсылка.ХарактеристикиНоменклатуры - 
//  Упаковка       - СправочникСсылка.ЕдиницыИзмерения - 
//  ОбъектПубликации - СправочникСсылка.НоменклатураПрисоединенныеФайлы,
//                       - Если указано, то возвращается наименование данного объекта.
// 
// Возвращаемое значение:
//  Строка - Строковое представление товарной позиции.
//
Функция ПредставлениеТоварнойПозиции(Номенклатура, Характеристика, Упаковка = Неопределено, ОбъектПубликации = Неопределено) Экспорт
    
	Если ЗначениеЗаполнено(ОбъектПубликации) Тогда
		Возврат ОбъектПубликации.Наименование;
	Иначе
		ЧастиПредставления = Новый Массив;
		ЧастиПредставления.Добавить(Номенклатура);
		Если Не Характеристика.Пустая() Тогда
			ЧастиПредставления.Добавить(Характеристика);
		КонецЕсли;

		Возврат СтрСоединить(ЧастиПредставления, ", ");
	КонецЕсли;

КонецФункции

// Формирует наименования групп первого уровня дерева товаров.
//
// Параметры:
//   ДеревоТоваров                  - ДеревоЗначений - структура дерева описана в реквизите "ИмпортируемыеТовары", 
//   СортироватьСтрокиДереваТоваров - Булево - Истина, если после загрузки требуется отсортировать строки второго уровня дерева товаров.
//   ОтображатьСопоставленные       - Число - установленный фильтр по сопоставленным позициям дерева товаров.
//   ОтображатьПоСтатусу            - Число - установленный фильтр по статусу позиций дерева товаров.
//
Процедура ИмпортИзМаркетплейсаЗаполнитьНаименованияГруппДереваТоваров(ДеревоТоваров, Знач СортироватьСтрокиДереваТоваров = Истина, 
            Знач ОтображатьСопоставленные = 0, Знач ОтображатьПоСтатусу = -1)
    
    Индекс = 0;
    
    Пока Индекс < ДеревоТоваров.Строки.Количество() Цикл
        СтрокаДереваПервогоУровня = ДеревоТоваров.Строки[Индекс];
        
        Если СтрокаДереваПервогоУровня.Строки.Количество() = 0 Тогда
            ДеревоТоваров.Строки.Удалить(СтрокаДереваПервогоУровня);
            Продолжить;
        ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 0 Тогда
            ШаблонПредставления = НСтр("ru = 'Обновить (%1 из %2)'");
        ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 2 Тогда
            ШаблонПредставления = НСтр("ru = 'Загрузить (%1 из %2)'");
        ИначеЕсли СтрокаДереваПервогоУровня.ВариантЗагрузки = 3 Тогда
            ШаблонПредставления = НСтр("ru = 'Не найдены (%2)'");
        КонецЕсли;
        
        КоличествоСтрок           = СтрокаДереваПервогоУровня.Строки.Количество();
        КоличествоОтмеченныхСтрок = 0;
        
        Для каждого СтрокаДереваВторогоУровня Из СтрокаДереваПервогоУровня.Строки Цикл
            Если ОтображатьСопоставленные = 1
                 И (НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
                    ИЛИ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
                        И НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика)
                    ИЛИ НЕ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория)) Тогда
                КоличествоСтрок = КоличествоСтрок - 1;
                Продолжить;
                
            ИначеЕсли ОтображатьСопоставленные = 2
                      И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Номенклатура) 
                      И (НЕ СтрокаДереваВторогоУровня.ХарактеристикиИспользуются
                         ИЛИ ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Характеристика))
                      И ЗначениеЗаполнено(СтрокаДереваВторогоУровня.Категория) Тогда
                КоличествоСтрок = КоличествоСтрок - 1;
                Продолжить;
            КонецЕсли;
            
            Если ОтображатьПоСтатусу <> -1
                 И СтрокаДереваВторогоУровня.ИндексСтатуса <> ОтображатьПоСтатусу Тогда
                КоличествоСтрок = КоличествоСтрок - 1;
                Продолжить;
            КонецЕсли;
            
            КоличествоОтмеченныхСтрок = КоличествоОтмеченныхСтрок + ?(СтрокаДереваВторогоУровня.Пометка, 1, 0);
        КонецЦикла;
        
        Если КоличествоСтрок = 0 Тогда
            ШаблонПредставления = СтрЗаменить(ШаблонПредставления, " (%1 из %2)", "");
            ШаблонПредставления = СтрЗаменить(ШаблонПредставления, " (%2)", "");
        КонецЕсли;
        
        СтрокаДереваПервогоУровня.Пометка = (КоличествоОтмеченныхСтрок > 0);
        СтрокаДереваПервогоУровня.КоличествоОтмеченныхСтрок = КоличествоОтмеченныхСтрок;
        СтрокаДереваПервогоУровня.ПредставлениеОбъектаМаркетплейса = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
            ШаблонПредставления,
            КоличествоОтмеченныхСтрок,
            КоличествоСтрок);
        
		Если СортироватьСтрокиДереваТоваров Тогда
		    СтрокаДереваПервогоУровня.Строки.Сортировать("ПредставлениеОбъектаМаркетплейса");
		КонецЕсли;
        
        Индекс = Индекс + 1;
    КонецЦикла;
        
КонецПроцедуры

// Преобразует исходное значение в массив значений.
//
// Параметры:
//   Значение               - Произвольный - исходное значение.
//   ПреобразоватьВСтроку   - Булево - Истина, если необходимо преобразовать значение из числа в строку.
//   ПреобразоватьВЧисло    - Булево - Истина, если необходимо преобразовать значение из строки в число.
//   ИсключатьНеЗаполненные - Булево - Истина, если необходимо исключить незаполненные значения.
//
// Возвращаемое значение:
//   Массив Из Произвольный - массив значений.
//
Функция ЗначениеВМассив(Знач Значение, ПреобразоватьВСтроку = Ложь, ПреобразоватьВЧисло = Ложь, ИсключатьНеЗаполненные = Истина) Экспорт
	
	Результат = Новый Массив;

	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		Для каждого ЭлементКоллекции Из Значение Цикл
			Если НЕ ИсключатьНеЗаполненные
				 ИЛИ ЗначениеЗаполнено(ЭлементКоллекции) Тогда
				Если ПреобразоватьВСтроку
					 И ТипЗнч(ЭлементКоллекции) = Тип("Число") Тогда
					Результат.Добавить(ЧислоВСтроку(ЭлементКоллекции));
				ИначеЕсли ПреобразоватьВЧисло
					 И ТипЗнч(ЭлементКоллекции) = Тип("Строка") Тогда
					Результат.Добавить(СтрокаВЧисло(ЭлементКоллекции));
				Иначе
					Результат.Добавить(ЭлементКоллекции);
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
		
	Иначе
		Если НЕ ИсключатьНеЗаполненные
			 ИЛИ ЗначениеЗаполнено(Значение) Тогда
			Если ПреобразоватьВСтроку
				 И ТипЗнч(Значение) = Тип("Число") Тогда
				Результат.Добавить(ЧислоВСтроку(Значение));
			ИначеЕсли ПреобразоватьВЧисло
				 И ТипЗнч(Значение) = Тип("Строка") Тогда
				Результат.Добавить(СтрокаВЧисло(Значение));
			Иначе
				Результат.Добавить(Значение);
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Преобразует исходную строку в число без вызова исключений.
// Аналог функции СтроковыеФункцииКлиентСервер.СтрокаВЧисло() с дополнительной обработкой строки без нулей.
//
// Параметры:
//   Значение - Строка - строка, которую необходимо привести к числу.
//
// Возвращаемое значение:
//   Число        - полученное число.
//   Неопределено - если строка не является числом.
//
Функция СтрокаВЧисло(Знач Значение) Экспорт
	
	Значение  = СтрЗаменить(Значение, " ", "");
	Если СтрНачинаетсяС(Значение, "(") Тогда
		Значение = СтрЗаменить(Значение, "(", "-");
		Значение = СтрЗаменить(Значение, ")", "");
	КонецЕсли;
	
	СтрокаБезНулей = СтрЗаменить(Значение, "0", "");
	Если ПустаяСтрока(СтрокаБезНулей) ИЛИ СтрокаБезНулей = "-" ИЛИ СтрокаБезНулей = "." ИЛИ СтрокаБезНулей = "," Тогда
		Возврат 0;
	КонецЕсли;
	
	ТипЧисло  = Новый ОписаниеТипов("Число");
	Результат = ТипЧисло.ПривестиЗначение(Значение);
	
	Возврат ?(Результат <> 0 И Не ПустаяСтрока(СтрокаБезНулей), Результат, Неопределено);
	
КонецФункции 

Функция МаркерСтатусаПоСтатусуПолучить( Статус ) Экспорт

	Если Статус =  Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.Новый
		ИЛИ Статус =  Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.СозданиеНового Тогда
		
		Результат = 2;
		
	ИначеЕсли Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ПолученаРекомендация
		ИЛИ Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.УтвержденаРекомендация
		ИЛИ Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.НаМодерации
		ИЛИ Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.ОжидаетМодерации  Тогда
		
		Результат = 1;
		
	ИначеЕсли Статус = Перечисления.СтатусыВыгрузкиТоваровЯндексМаркет.МодерацияПройдена Тогда
		
		Результат = 3;
		
	Иначе
		
		Результат = 0;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции